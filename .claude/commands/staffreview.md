Review the implementation plan at $ARGUMENTS using the staffreview skill.

Read and follow the complete workflow in `.agent/skills/staffreview/SKILL.md`.

If no argument was provided (empty $ARGUMENTS), list available plans in `docs/plans/` and ask the user to select one.

Execute all steps (0-7) from the skill file. Save the review report to `docs/reviews/`.
