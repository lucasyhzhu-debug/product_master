---
phase: 53-expense-e2e-testing
plan: 05
type: execute
wave: 3
depends_on: ["53-02", "53-03", "53-04"]
files_modified:
  - .planning/phases/53-expense-e2e-testing/53-BUG-REPORT.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "All expense E2E specs pass in a single full suite run"
    - "All 690+ existing unit tests remain green"
    - "Bug report delivered with resolution status for each discovered issue"
  artifacts:
    - path: ".planning/phases/53-expense-e2e-testing/53-BUG-REPORT.md"
      provides: "Bug report with discovered issues and resolution status"
      min_lines: 10
  key_links:
    - from: "tests/e2e/expense-access.spec.ts"
      to: "tests/e2e/expense-lifecycle.spec.ts"
      via: "Full suite run validates all specs together"
      pattern: "npx playwright test"
---

<objective>
Run the full E2E test suite and all unit tests, compile bug report, and get human verification.

Purpose: This final plan verifies that all expense E2E tests pass together (not just individually), confirms existing unit tests weren't broken, delivers the bug report documenting all issues found during testing, and gets human confirmation that the test suite is satisfactory.

Output: 53-BUG-REPORT.md, green full suite run
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-expense-e2e-testing/53-CONTEXT.md
@.planning/phases/53-expense-e2e-testing/53-02-SUMMARY.md
@.planning/phases/53-expense-e2e-testing/53-03-SUMMARY.md
@.planning/phases/53-expense-e2e-testing/53-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full suite verification and bug report</name>
  <files>.planning/phases/53-expense-e2e-testing/53-BUG-REPORT.md</files>
  <action>
**Step 1: Run all expense E2E tests together**
```bash
npx playwright test tests/e2e/expense-access.spec.ts tests/e2e/expense-analytics.spec.ts tests/e2e/expense-lifecycle.spec.ts tests/e2e/expense-csv-import.spec.ts tests/e2e/expense-approval.spec.ts --reporter=list
```

If any test fails that passed individually in Plans 02-04:
- Diagnose: is it a test isolation issue (data from prior spec affecting this one)?
- Fix: add proper waitForDataLoad, use more specific selectors, or adjust test order
- Re-run until green

**Step 2: Run existing unit test suite**
```bash
npm run test
```
Assert all 690+ tests pass. If any fail, determine if it's related to expense E2E changes (it shouldn't be -- E2E tests don't modify production code, only test files) or a pre-existing issue.

**Step 3: Run TypeScript type check**
```bash
npm run type-check
```
Confirm no type errors in the new test files.

**Step 4: Compile bug report**

Create `.planning/phases/53-expense-e2e-testing/53-BUG-REPORT.md` with this structure:

```markdown
# Phase 53: Expense E2E Testing - Bug Report

**Date:** 2026-03-15
**Tester:** Claude (automated E2E via Playwright)
**Environment:** Dev (dev:exciting-fennec-671)

## Summary

| Status | Count |
|--------|-------|
| Fixed inline | {N} |
| Documented for later | {N} |
| Blocking | {N} |
| Total tests | {N} |
| Passing | {N} |

## Issues Found

### Fixed Inline

{For each bug fixed during Plans 02-04:}

#### BUG-{NN}: {Title}
- **Spec:** {spec file}
- **Symptom:** {What failed}
- **Root cause:** {Why}
- **Fix:** {What was changed}
- **Commit:** {commit hash if separate}

### Documented for Later

{For each complex bug not fixed:}

#### BUG-{NN}: {Title}
- **Spec:** {spec file}
- **Symptom:** {What failed}
- **Root cause:** {Best guess}
- **Impact:** {What it affects}
- **Screenshot:** {path}

### Test Coverage Matrix

| Spec File | Tests | Pass | Fail | Skip |
|-----------|-------|------|------|------|
| expense-access.spec.ts | {N} | {N} | {N} | {N} |
| expense-analytics.spec.ts | {N} | {N} | {N} | {N} |
| expense-lifecycle.spec.ts | {N} | {N} | {N} | {N} |
| expense-csv-import.spec.ts | {N} | {N} | {N} | {N} |
| expense-approval.spec.ts | {N} | {N} | {N} | {N} |

## Existing Test Suite

- Unit tests: {N} passing, {N} failing
- E2E tests (non-expense): Not re-run (independent, unchanged)
```

Populate with actual results from Plans 02-04 summaries and the full suite run.

If NO bugs were found, the report should say so clearly: "No bugs discovered during E2E testing. All tests passed on first implementation."
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/expense-access.spec.ts tests/e2e/expense-analytics.spec.ts tests/e2e/expense-lifecycle.spec.ts tests/e2e/expense-csv-import.spec.ts tests/e2e/expense-approval.spec.ts --reporter=list 2>&1 | tail -5</automated>
  </verify>
  <done>Full expense E2E suite passes green. Unit tests remain green. Bug report compiled with resolution status for all discovered issues.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete expense E2E test suite</name>
  <action>
Human verifies the complete expense E2E test suite covering 5 spec files: permission guards (4 roles x 9 routes), full lifecycle (submit -> approve -> reimburse -> P&L), approval edge cases (self-approval block, DoA, rejection, fraud flags), CSV import with validation, and analytics/admin page verification.
  </action>
  <what-built>
Complete expense E2E test suite: 5 spec files covering permission guards (4 roles x 9 routes), full lifecycle (submit -> approve -> reimburse -> P&L), approval edge cases (self-approval block, DoA, rejection, fraud flags), CSV import with validation, and analytics/admin page verification.
  </what-built>
  <how-to-verify>
1. Review the bug report: `.planning/phases/53-expense-e2e-testing/53-BUG-REPORT.md`
2. Check test results: `npx playwright test tests/e2e/expense-*.spec.ts --reporter=list`
3. Review screenshots in `tests/e2e/screenshots/` for visual verification of test steps
4. Confirm all existing tests still pass: `npm run test`
5. Optional: run a single spec to see it execute: `npx playwright test tests/e2e/expense-lifecycle.spec.ts --reporter=list`
  </how-to-verify>
  <verify>Human types "approved" or describes issues</verify>
  <done>Human has reviewed and approved the E2E test suite</done>
  <resume-signal>Type "approved" to complete the phase, or describe issues to address</resume-signal>
</task>

</tasks>

<verification>
- Full expense E2E suite: 5 specs, all passing
- Unit test suite: 690+ tests, all passing
- TypeScript: no type errors
- Bug report: delivered with per-issue resolution status
- Human verification: approved
</verification>

<success_criteria>
- All 5 expense E2E spec files pass in a single run
- All 690+ unit tests remain green
- npm run type-check passes
- Bug report (53-BUG-REPORT.md) delivered
- Human approves the test suite
</success_criteria>

<output>
After completion, create `.planning/phases/53-expense-e2e-testing/53-05-SUMMARY.md`
</output>
