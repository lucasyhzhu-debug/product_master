---
phase: 74.5.2.1
plan: 03
name: close-deferred-items
wave: 2
autonomous: true
files_modified:
  - .planning/phases/74.5.2-unified-deduct-cutover/deferred-items.md
  - docs/CHANNEL_INTEGRATION.md
  - docs/CHANGELOG.md
  - .planning/ROADMAP.md
---

# Plan 03 — Close deferred items + update docs

## Objective

Update documentation to reflect that 74.5.2.1 has resolved 74.5.2 deferred items #4 (gobiz auto-flip) and #5 (K3Mart composite UI). Repoint remaining deferred items to a concrete follow-up phase name (`74.5.3-packaging-bom-deduction`) instead of vague "74.5.3 TBD" placeholders.

## Context

74.5.2's `deferred-items.md` has 5 items (#2-6 after the in-phase #1). Items #4 and #5 are now RESOLVED by 74.5.2.1 Plans 01 and 02. Item #2 (sticker gap) still requires a feature extension and should be labeled as the scope of a proposed **74.5.3-packaging-bom-deduction** phase. Items #3 (shim governance) and #6 (token pattern) remain independent follow-ups.

The runbook (`docs/CHANNEL_INTEGRATION.md`) must also be updated — the manual flag-flip procedure is now automatic, and the K3Mart bundle procedure is now a one-click affordance.

## Tasks

### Task 1: Update `deferred-items.md`

**File:** `.planning/phases/74.5.2-unified-deduct-cutover/deferred-items.md`

For items #4 and #5, append a "Resolution" subsection:
```markdown
### Resolution — 2026-04-{DD}
Shipped in phase **74.5.2.1-ops-automation** (Plan {01|02}).
Commit: {sha}.
```

For item #2 (sticker gap), update the "Follow-up candidate" line from "Phase 74.5.3 (if scoped...)" to "**Phase 74.5.3-packaging-bom-deduction** (to be planned)" — removes the "if scoped" conditional and commits to the phase name.

For items #3 and #6, no change — they remain open.

### Task 2: Update runbook (`docs/CHANNEL_INTEGRATION.md`)

**GoFood atomic cutover section (~line 214):** Replace the "flip flag immediately" manual step with:
```
The GoFood flag (`channelDeductionEnabled.gobiz`) is now flipped ON automatically
by the deploy pipeline (phase 74.5.2.1 added a post-Convex-deploy step in
`.github/workflows/deploy.yml`). Manual flag-flip is NOT required.
Verification: after deploy, `/admin/product-inventory-settings` shows gobiz=ON.
If the CI step fails, fall back to the manual flag-flip procedure.
```

**K3Mart bundle flip section (~line 208):** Replace the "flip both flags manually, in order" instructions with:
```
Use the "Flip K3Mart Bundle" composite toggle on `/admin/product-inventory-settings`
(shipped in phase 74.5.2.1 per D74.5.2-L14). The button atomically sets both
`k3mart` and `consignment` flags in a single mutation — operator cannot
accidentally leave the bundle in an out-of-sync state.

If the composite toggle reports "⚠ OUT OF SYNC" (rare — only if someone flipped
via the per-source switches), click the composite button once to restore
consistency.
```

**Known gaps section (~line 386):** Sticker auto-deduction paragraph: update the Follow-up line to name the phase explicitly:
```
Follow-up candidates: phase **74.5.3-packaging-bom-deduction** (to be planned).
Until that phase ships, the daily operational procedure below remains mandatory.
```

### Task 3: Update `docs/CHANGELOG.md`

Append a new entry under v2.0:
```markdown
### Phase 74.5.2.1 — 2026-04-{DD}

#### Added
- `ensureGobizChannelDeductionOn` internal mutation (`convex/productInventory/startupMigrations.ts`) — idempotent post-deploy flag-flip.
- GitHub Actions post-Convex-deploy step that invokes the mutation; eliminates manual ops ritual from 74.5.2 runbook.
- `flipK3MartBundle` admin mutation — atomic two-flag patch satisfying D74.5.2-L14.
- K3Mart bundle composite toggle on `/admin/product-inventory-settings` with out-of-sync detection.

#### Changed
- Runbook (`docs/CHANNEL_INTEGRATION.md`) updated: manual flag-flip procedures replaced by automation references.
- Deferred items #4 and #5 from Phase 74.5.2 marked RESOLVED.
```

### Task 4: Update `.planning/ROADMAP.md`

Insert a line under Phase 74.5.2:
```markdown
- [x] **Phase 74.5.2.1: Ops Automation (INSERTED 2026-04-21, follow-up to 74.5.2)** — Closes 74.5.2 deferred items #4 (auto gobiz flag flip via CI) and #5 (D74.5.2-L14 K3Mart composite UI). (completed 2026-04-{DD})
```

And update the execution order line at the bottom of the milestone section:
```
Phases execute in numeric order: ... -> 74.5.1 -> 74.5.2 -> **74.5.2.1** -> 75 -> 76 -> 77
```

Also add a forward-reference entry for the pending 74.5.3:
```markdown
- [ ] **Phase 74.5.3: Packaging BOM Auto-Deduction (PLANNED — absorbs 74.5.2 deferred item #2)** — Extend `processChannelSaleInternal` to BOM-resolve packaging components (sticker, small-box, etc.) and emit paired `packagingInventoryTransactions` rows per sale. Eliminates the daily manual sticker adjustment procedure from the 74.5.2 runbook. Design pass required: per-source packaging SKU mapping strategy. Not yet scheduled.
```

## Success criteria
- [ ] `deferred-items.md` items #4 and #5 have Resolution subsections with commit SHAs.
- [ ] Item #2 follow-up line names `74.5.3-packaging-bom-deduction` concretely.
- [ ] `CHANNEL_INTEGRATION.md` no longer instructs manual flag-flip; K3Mart section references composite toggle.
- [ ] `CHANGELOG.md` has a Phase 74.5.2.1 entry under v2.0.
- [ ] `ROADMAP.md` marks 74.5.2.1 complete and lists 74.5.3 as planned.

## Pitfalls
1. **Commit SHA interpolation** — only fill SHAs AFTER Plans 01 and 02 merge. This plan runs LAST (Wave 2) for that reason.
2. **Date placeholder** — the `2026-04-{DD}` literal should be replaced with the actual execution date at commit time.
