---
status: diagnosed
phase: 14.1-draft-orderupdate
source: [14.1-01-SUMMARY.md, 14.1-02-SUMMARY.md]
started: 2026-02-16T10:00:00Z
updated: 2026-02-16T10:15:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Draft auto-creation on customer select
expected: Go to Orders > New Order. Select an existing customer (or create a new one). A Draft order should be created in the database immediately -- visible in the Draft Kanban column with the customer name and zero total.
result: issue
reported: "have a save as draft button too so you can save order updates and keep in draft"
severity: major

### 2. Draft card opens edit form (not slide-over)
expected: In the Kanban board, click on a Draft card. Instead of opening the read-only slide-over panel, it should navigate to /orders/new?draft={orderId} -- the order creation form with all fields pre-filled (customer name, due date, items, delivery, voucher, notes from the Draft).
result: pass

### 3. Draft column sorted newest first
expected: Create multiple Draft orders. The Draft column in the Kanban board should show the newest Draft at the top (sorted by creation date descending), not by due date like other columns.
result: pass

### 4. Edit and submit a Draft
expected: Open a Draft for editing (click its Kanban card). Modify fields (add items, set due date, add notes). Click Submit. The order should transition to AwaitingPayment with all your changes saved. Check the slide-over for the submitted order to confirm fields are correct.
result: pass

### 5. Delete Draft button
expected: Open a Draft for editing. A "Delete Draft" button should be visible (destructive/red style). Clicking it should delete the Draft and navigate back to the orders page. The Draft should no longer appear in the Kanban.
result: pass

### 6. WhatsApp language toggle
expected: Open an order's WhatsApp message (from the slide-over). Toggle the language switch between Indonesian and English. Both the title and the full message body should change language -- not just the title.
result: pass

### 7. Orders page after Kitchen navigation
expected: Navigate to Kitchen page, then click back to Orders. The Kanban board should appear immediately (or show loading skeleton then Kanban). Page should not be blank.
result: issue
reported: "when you navigate to kitchen, and then you click the 'orders' page again - you get a blank page, you have to refresh the page again for ui to appear"
severity: major

## Summary

total: 7
passed: 5
issues: 2
pending: 0
skipped: 0

## Gaps

- truth: "User can save Draft order changes without submitting (stay in Draft status)"
  status: failed
  reason: "User reported: have a save as draft button too so you can save order updates and keep in draft"
  severity: major
  test: 1
  root_cause: "OrderCreate.tsx has Submit (transitions to AwaitingPayment) and Delete Draft buttons, but no Save as Draft button that calls updateDraft + replaceItems without changing status. The backend mutations (updateDraft, replaceItems) already exist and work correctly."
  artifacts:
    - path: "src/pages/OrderCreate.tsx"
      issue: "Missing Save as Draft button that saves form state without transitioning status"
  missing:
    - "Add Save as Draft button next to Submit that calls updateDraft + replaceItems then navigates back to /orders or shows success toast"
  debug_session: ""

- truth: "All pages render correctly when navigating via SPA (no blank page on any route)"
  status: failed
  reason: "User reported: when you navigate to kitchen, and then you click the 'orders' page again - you get a blank page. Also affects Sales page and other pages. Must refresh for UI to appear."
  severity: major
  test: 7
  root_cause: "Layout.tsx wraps page content in AnimatePresence mode='wait' with motion.div. The exit/enter animation cycle causes pages to appear blank on SPA navigation. The motion.div has no explicit height, so child h-full resolves to 0 during animation. Affects all pages, not just Orders."
  artifacts:
    - path: "src/components/layout/Layout.tsx"
      issue: "AnimatePresence mode='wait' with motion.div breaks SPA navigation across all pages"
  missing:
    - "Remove AnimatePresence from Layout.tsx — the 0.15s fade adds no UX value but breaks navigation"
  debug_session: ""
