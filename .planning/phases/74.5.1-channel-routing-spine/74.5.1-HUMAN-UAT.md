---
status: partial
phase: 74.5.1-channel-routing-spine
source: [74.5.1-VERIFICATION.md]
started: 2026-04-20T17:05:00Z
updated: 2026-04-20T17:05:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Channel Routing Manager CRUD round-trip
expected: Admin navigates to `/admin/channel-routing`, sees empty routing table + Resolution Preview panel, clicks Create Rule, picks source=shopee + storage location, saves. Rule appears in table; Resolution Preview resolves (shopee, no outlet, no product) to that location. Duplicate-combination produces inline validation error; delete removes row.
result: [pending]

### 2. 8-key flag UI at Product Inventory Settings
expected: Admin opens `/admin/product-inventory-settings`, sees 8 flag rows (bigseller + grabfood DISABLED with tooltip; internal DISABLED per D74.5.1-L2). Flips shopee ON. Audit trail appears. Toggles back to OFF. Read-back shows shopee=false. 8-key flag UI renders per UI-SPEC; toggles call `setChannelDeductionFlag`; persisted to `productInventorySettings.channelDeductionEnabled`.
result: [pending]

### 3. Channel Audit Workbench full flow
expected: Admin navigates to `/admin/channel-audit`, clicks Run full audit. Summary grid shows 5-type counts. Clicks each tab, sees issue badges with legend tooltips. Selects 2 rows of same type, clicks bulk Resolve. Count decrements; resolution recorded. 5-tab workbench renders; `runFullAudit` action returns counts; per-type resolution flows complete; bulk actions work; Dismiss AlertDialog with reason Textarea blocks confirm until reason typed.
result: [pending]

### 4. Seed migration idempotency
expected: Admin runs seed migration (`seedChannelRoutingFromOutlets` action) via Convex dashboard or admin UI. Verifies channelRouting rows appear for every `externalOutlet` with `linkedStorageLocationId`. Re-runs: no-op (idempotent). Seed populates channelRouting from externalOutlets; second run produces zero new rows.
result: [pending]

### 5. Ship-dark contract (D-10) confirmation
expected: Confirm flag-OFF = zero behavior change in prod. Run a Shopee/TikTok sync against dev env with no flag flipped. Verify `externalRevenueItems` rows are created WITH `inventoryDeductedAt = undefined` AND NO `productInventoryTransactions` `channel_sale` rows appear. Ship-dark contract (D-10): `saveRevenueItems` writes revenue items but does NOT dispatch `processChannelSaleInternal` when flag is absent/false.
result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps
