---
phase: 74.5.2
plan: 08
type: execute
wave: 4
depends_on: [74.5.2-02, 74.5.2-03, 74.5.2-04, 74.5.2-05, 74.5.2-06, 74.5.2-07]
files_modified:
  - convex/productInventory/mutations.ts
  - convex/integrations/gobiz/adapter.ts
  - convex/productInventory/stockTracker.ts
  - convex/productInventory/__tests__/gofoodRegression.test.ts
  - src/components/inventory/TransactionLogPanel.tsx
autonomous: true
gap_closure: false
requirements: [R5, SC2, D74.5.2-L5, D74.5.2-L6]

must_haves:
  truths:
    - "processGofoodSales function body deleted from convex/productInventory/mutations.ts"
    - "Both call sites removed from convex/integrations/gobiz/adapter.ts (lines 731-852 + 1006-1117 try/catch wrappers)"
    - "TransactionLogPanel renders channel_sale + source=gobiz rows as GoFood display branch (hybrid with legacy gofood_sale rows during soak)"
    - "gofoodRegression.test.ts updated to assert channel_sale + source=gobiz (or deleted if superseded)"
    - "Zero grep hits for processGofoodSales in convex/ after merge"
  artifacts:
    - path: convex/productInventory/mutations.ts
      provides: "processGofoodSales DELETED"
      min_lines: 0
    - path: src/components/inventory/TransactionLogPanel.tsx
      provides: "Hybrid legacy + unified GoFood display"
      contains: "gobiz"
  key_links:
    - from: convex/integrations/gobiz/adapter.ts
      to: convex/externalData/mutations.ts
      via: "saveRevenueItems remains the ONLY deduction path for GoFood post-merge"
      pattern: "saveRevenueItems"
---

<objective>
Atomically retire the legacy `processGofoodSales` path. Delete function body + both call sites in the gobiz adapter. Update `TransactionLogPanel.tsx` to render `channel_sale + source=gobiz` rows as the GoFood display branch alongside legacy `gofood_sale` rows (hybrid support — migration in Plan 04 runs post-merge to backfill historical rows). Update or delete `gofoodRegression.test.ts`.

**CRITICAL — D74.5.2-L5 atomicity + Pitfall 2:** The code deletion ships in the SAME deploy. The runbook (Plan 09) instructs admin to flip the `channelDeductionEnabled.gobiz` flag IMMEDIATELY after deploy-complete confirmation. Between deploy-complete and flag-flip, GoFood sales produce zero deductions (under-deduction window — acceptable, recoverable via re-running Plan 02 backfill). Double-deduction is impossible because `processGofoodSales` is deleted — can never run concurrently with the unified path.

Purpose: R5 / SC2 / D74.5.2-L5 / parent D-14.

Output:
- 100+ LOC deleted from `convex/productInventory/mutations.ts`
- 120+ LOC deleted from `convex/integrations/gobiz/adapter.ts` (2 try/catch wrappers)
- `TransactionLogPanel.tsx` — extended to hybrid-render
- Regression test aligned or removed
</objective>

<execution_context>
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/workflows/execute-plan.md
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74.5.2-unified-deduct-cutover/74.5.2-CONTEXT.md
@.planning/phases/74.5.2-unified-deduct-cutover/74.5.2-RESEARCH.md
@.planning/phases/74.5.2-unified-deduct-cutover/74.5.2-PATTERNS.md
@convex/productInventory/mutations.ts
@convex/integrations/gobiz/adapter.ts
@convex/productInventory/stockTracker.ts
@convex/productInventory/__tests__/gofoodRegression.test.ts
@src/components/inventory/TransactionLogPanel.tsx
@convex/externalData/mutations.ts

<interfaces>
From RESEARCH §`gofood_sale` Readers (lines 153-164) — known read sites + post-migration behavior:
- `convex/schema.ts:1015` — union literal — stays for 74.5.2 (strip-before-drop)
- `convex/productInventory/mutations.ts:823, 846` — WRITES of `gofood_sale` — DELETED with processGofoodSales
- `convex/productInventory/queries.ts:146` — cast-based filter — update after schema literal drop (deferred to 74.5.3)
- `src/components/inventory/TransactionLogPanel.tsx:37-42, 69, 194-196` — UI display — UPDATE in this plan (hybrid)

From RESEARCH §Code Examples §GoFood atomic retirement patch (lines 566-571):
- `convex/productInventory/mutations.ts` — delete function body lines 706-867 (162 LOC)
- `convex/integrations/gobiz/adapter.ts` — delete lines 731-852 AND 1006-1117 (2 try/catch blocks)
- `convex/productInventory/stockTracker.ts:10` — delete stale comment reference to processGofoodSales
- `convex/productInventory/__tests__/gofoodRegression.test.ts` — update or delete (superseded by saveRevenueItemsHook tests)

`saveRevenueItems` in `convex/externalData/mutations.ts` ALREADY gates GoFood deduction via `channelDeductionEnabled.gobiz` flag (74.5.1 shipped). After processGofoodSales is deleted, GoFood flow is:
1. gobiz adapter calls `saveRevenueItems` (already does — 74.5.1 Plan 06)
2. `saveRevenueItems` checks `channelDeductionEnabled.gobiz` flag
3. If flag ON → calls `processChannelSaleInternal` → writes `channel_sale + source=gobiz` ledger rows
4. If flag OFF → skips deduction (under-deduction window until admin flips the flag)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete processGofoodSales function body from convex/productInventory/mutations.ts</name>
  <files>convex/productInventory/mutations.ts</files>
  <read_first>
    - convex/productInventory/mutations.ts (FULL file — confirm function body boundaries, exports, any helper functions used ONLY by processGofoodSales)
    - .planning/phases/74.5.2-unified-deduct-cutover/74.5.2-RESEARCH.md §Legacy Deduct Paths Ground Truth + §Code Examples §GoFood atomic retirement patch (lines 566-571)
  </read_first>
  <action>
In `convex/productInventory/mutations.ts`:

1. Locate the `processGofoodSales` export (around line 716 per research). Confirm function boundaries by reading until you find the closing `});` of the mutation/internalMutation definition. Per research it spans lines 706-867 (~162 LOC).

2. DELETE the entire function body — export keyword, function signature, args, handler body, closing braces.

3. Check imports: if any imports were used ONLY by processGofoodSales (e.g., stockTracker helpers not used elsewhere), remove those imports. Do NOT remove imports used by other exports in the same file.

4. Check for helper functions defined in the same file that are only called by processGofoodSales. If any exist, delete them too. If uncertain, `grep` the rest of the codebase for the helper name — if it's not used elsewhere, safe to delete.

5. Verify no internal/external API references to this export remain:
```bash
grep -rn "processGofoodSales" convex/ src/
```
If any references remain in convex/ — they're the call sites in gobiz/adapter.ts (handled in Task 2) OR missed helper definitions in this same file. If src/ has any reference, also fix.

LANDMINE: if processGofoodSales is re-exported from a barrel file (e.g., `convex/productInventory/index.ts`), remove that re-export too. Grep to be sure.

After delete, `npm run type-check` should FAIL at the adapter.ts call sites (removed in Task 2). That's expected — don't try to fix type errors in this task alone; Task 2 is the required pair.
  </action>
  <verify>
    <automated>grep -c "processGofoodSales" convex/productInventory/mutations.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "processGofoodSales" convex/productInventory/mutations.ts` returns 0
    - `grep -c "gofood_sale" convex/productInventory/mutations.ts` returns 0 (the 2 writes at lines 823/846 are inside processGofoodSales — should be gone)
    - File is shorter by 150+ lines
    - Existing non-GoFood exports preserved (grep for other function names still returns 1 match each)
  </acceptance_criteria>
  <done>processGofoodSales deleted; file type-checks IN ISOLATION (final type-check gate is Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Delete both gobiz adapter call sites</name>
  <files>convex/integrations/gobiz/adapter.ts</files>
  <read_first>
    - convex/integrations/gobiz/adapter.ts (FULL file — 1100+ lines, identify the 2 try/catch wrappers around processGofoodSales)
    - .planning/phases/74.5.2-unified-deduct-cutover/74.5.2-RESEARCH.md §Code Examples §GoFood atomic retirement patch (lines 566-571)
    - .planning/phases/74.5.2-unified-deduct-cutover/74.5.2-PATTERNS.md §6
  </read_first>
  <action>
In `convex/integrations/gobiz/adapter.ts`:

1. Locate the MANUAL-SYNC Phase C/D try/catch block at lines 731-852 (per research). It wraps `await processGofoodSales(...)` inside a try { } catch (phaseDError) { ... } that swallows errors.

2. Locate the AUTO-SYNC Phase C/D try/catch block at lines 1006-1117. Same structure, cron path.

3. DELETE both blocks in full. Delete the `try {`, the `processGofoodSales(...)` call, the `} catch (phaseDError) { ... }`, and any related logging/metrics calls inside those wrappers.

4. WHAT MUST STAY:
   - `saveRevenueItems` call earlier in the adapter flow (74.5.1 shipped this) — untouched.
   - `updateSyncLog` call (R9 counters for `itemsDeducted`/`itemsSkipped`) — untouched.
   - All normalize / emit logic — untouched.
   - Any non-GoFood adapter logic — untouched.

5. Remove the `processGofoodSales` import from the top of the file:
```typescript
// BEFORE:
import { processGofoodSales } from "../../productInventory/mutations";
// AFTER: (delete the line entirely)
```

If the import is part of a named-import list (`import { X, processGofoodSales, Y } from "..."`), remove just `processGofoodSales` from the list.

6. `npm run type-check` should PASS after Task 1 + Task 2 complete together.

CRITICAL: atomicity (D74.5.2-L5) — Task 1 + Task 2 MUST land in the SAME commit (or same PR merge). The intermediate state (function deleted, callers still present) breaks the build. Execute them sequentially + commit together.
  </action>
  <verify>
    <automated>grep -c "processGofoodSales" convex/integrations/gobiz/adapter.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "processGofoodSales" convex/integrations/gobiz/adapter.ts` returns 0
    - `grep -rn "processGofoodSales" convex/` returns 0 (across entire convex/ directory)
    - `grep -c "saveRevenueItems" convex/integrations/gobiz/adapter.ts` returns 1+ (UNTOUCHED — still the primary deduction dispatch)
    - `grep -c "updateSyncLog" convex/integrations/gobiz/adapter.ts` returns 1+ (UNTOUCHED)
    - `npm run type-check` passes (at this point — both deletions together compile cleanly)
    - `npm run build` passes
  </acceptance_criteria>
  <done>Both call sites + import removed; adapter compiles; saveRevenueItems is the only deduction path.</done>
</task>

<task type="auto">
  <name>Task 3: Remove stale stockTracker.ts comment + update gofoodRegression test</name>
  <files>convex/productInventory/stockTracker.ts, convex/productInventory/__tests__/gofoodRegression.test.ts</files>
  <read_first>
    - convex/productInventory/stockTracker.ts (lines 1-30 — find the "(multi-location)" comment referencing processGofoodSales)
    - convex/productInventory/__tests__/gofoodRegression.test.ts (full file — understand what it asserts; likely asserts gofood_sale rows land via processGofoodSales)
    - .planning/phases/74.5.2-unified-deduct-cutover/74.5.2-PATTERNS.md §5 (RESEARCH's guidance on gofoodRegression.test.ts — update OR delete)
  </read_first>
  <action>
**stockTracker.ts:**
Open `convex/productInventory/stockTracker.ts` and locate any comment referencing `processGofoodSales` (research suggests line ~10: `processGofoodSales (multi-location)` comment reference). DELETE that reference. Update surrounding comment context if stale (e.g., if the comment lists legacy deduct paths, update to reflect that only `processChannelSaleInternal` remains).

**gofoodRegression.test.ts:**
Open `convex/productInventory/__tests__/gofoodRegression.test.ts`. DECIDE between two paths:

**Path A — Update** (preferred if tests cover behavior still relevant):
- Rewrite assertions that expect `transactionType: "gofood_sale"` rows to expect `transactionType: "channel_sale"` + `source: "gobiz"`.
- Remove any direct invocation of `processGofoodSales` (it no longer exists). Replace with invocation via `saveRevenueItems` flow (or the gobiz adapter normalize → emit).
- Add a header comment: `// Phase 74.5.2 Plan 08: updated to assert unified channel_sale + source=gobiz post-processGofoodSales retirement.`

**Path B — Delete** (preferred if tests are superseded by saveRevenueItemsHook tests):
- Check `convex/externalData/__tests__/saveRevenueItemsHook.test.ts` — does it cover GoFood flow end-to-end?
- If yes, DELETE `gofoodRegression.test.ts`.
- Add a note in Plan 08 SUMMARY documenting the test was superseded.

If uncertain, ATTEMPT Path A first. If the test uses too many legacy patterns to update cleanly, fall back to Path B.
  </action>
  <verify>
    <automated>npm run type-check &amp;&amp; npm run test -- --run convex/productInventory/__tests__/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "processGofoodSales" convex/productInventory/stockTracker.ts` returns 0
    - If Path A: `grep -c "gofood_sale" convex/productInventory/__tests__/gofoodRegression.test.ts` returns 0 AND `grep -c "channel_sale.*gobiz\\|gobiz.*channel_sale\\|source.*gobiz" convex/productInventory/__tests__/gofoodRegression.test.ts` returns 1+
    - If Path B: file does NOT exist
    - Full vitest suite green: `npm run test -- --run`
    - `npm run type-check` passes
  </acceptance_criteria>
  <done>Stale comment removed; regression test aligned or deleted; full test suite green.</done>
</task>

<task type="auto">
  <name>Task 4: Update TransactionLogPanel.tsx for hybrid GoFood display</name>
  <files>src/components/inventory/TransactionLogPanel.tsx</files>
  <read_first>
    - src/components/inventory/TransactionLogPanel.tsx (FULL file — lines 37-42, 69, 194-196 per research reference the GoFood display config)
    - .planning/phases/74.5.2-unified-deduct-cutover/74.5.2-RESEARCH.md §gofood_sale Readers table (lines 153-164)
  </read_first>
  <action>
Extend the UI to render EITHER `transactionType: "gofood_sale"` OR `transactionType: "channel_sale" && source: "gobiz"` as the GoFood display branch. This is CRITICAL for D74.5.2-L6 migration soak — historical rows stay `gofood_sale` until Plan 04 migration runs post-merge, and migrated rows transition to `channel_sale + source=gobiz`. Both must render identically.

Typical changes (locations per research):

Line ~37-42 — type-to-label / type-to-color map:
```typescript
// BEFORE (likely):
const TRANSACTION_TYPE_CONFIG = {
  add: { label: "Add", color: "green", icon: Plus },
  remove: { label: "Remove", color: "red", icon: Minus },
  gofood_sale: { label: "GoFood", color: "orange", icon: Utensils },
  channel_sale: { label: "Channel sale", color: "blue", icon: ShoppingCart },
  // ...
};

// AFTER: Add a helper to resolve the effective display for channel_sale based on source.
const TRANSACTION_TYPE_CONFIG = {
  // ... (keep gofood_sale entry during hybrid soak)
  gofood_sale: { label: "GoFood", color: "orange", icon: Utensils },
  channel_sale: { label: "Channel sale", color: "blue", icon: ShoppingCart },
};

// Phase 74.5.2 Plan 08: hybrid display — channel_sale + source=gobiz renders as GoFood.
function resolveDisplayConfig(tx: { transactionType: string; source?: string }) {
  if (tx.transactionType === "channel_sale" && tx.source === "gobiz") {
    // Post-migration rows — display as GoFood for continuity.
    return { label: "GoFood", color: "orange", icon: Utensils };
  }
  return TRANSACTION_TYPE_CONFIG[tx.transactionType as keyof typeof TRANSACTION_TYPE_CONFIG];
}
```

Line ~69 — filter button (if the panel has a "Filter by type: GoFood" button):
```typescript
// The button should filter BOTH gofood_sale AND channel_sale+source=gobiz.
// Update the click handler (or filter predicate) so "GoFood" as a filter value matches either.
```

Line ~194-196 — row display that shows `tx.gofoodOrderRef`:
```typescript
// BEFORE (likely):
{tx.transactionType === "gofood_sale" && tx.gofoodOrderRef && (
  <div>Ref: {tx.gofoodOrderRef}</div>
)}

// AFTER: hybrid — show ref for both legacy AND migrated rows.
{((tx.transactionType === "gofood_sale") ||
  (tx.transactionType === "channel_sale" && tx.source === "gobiz")) &&
  (tx.gofoodOrderRef || tx.externalRef) && (
  <div>Ref: {tx.gofoodOrderRef ?? tx.externalRef}</div>
)}
```

GENERAL APPROACH:
1. Grep for every reference to `"gofood_sale"` or `gofoodOrderRef` in the file.
2. For each, extend the condition to ALSO match `transactionType === "channel_sale" && source === "gobiz"`.
3. Prefer to factor out a helper `isGoFoodTransaction(tx)` to centralize the hybrid check. Place near other helpers.

IMPORTANT: do NOT remove any `gofood_sale` branch — we're hybrid. Remove that branch only when the literal is dropped from schema (deferred to 74.5.3).
  </action>
  <verify>
    <automated>npm run type-check &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "gofood_sale" src/components/inventory/TransactionLogPanel.tsx` returns 1+ (legacy branch preserved during soak)
    - `grep -c "channel_sale.*gobiz\\|source.*gobiz\\|gobiz.*channel_sale" src/components/inventory/TransactionLogPanel.tsx` returns 1+ (new branch added)
    - Helper `isGoFoodTransaction` OR equivalent added (recommend — not strictly required): `grep -c "isGoFoodTransaction\\|resolveDisplayConfig" src/components/inventory/TransactionLogPanel.tsx` returns 1+
    - `npm run type-check` passes
    - `npm run build` passes
  </acceptance_criteria>
  <done>Hybrid display: both legacy and migrated GoFood rows render identically as "GoFood"; filter works for both.</done>
</task>

</tasks>

<verification>
FULL SUITE GATE: `npm run type-check && npm run test -- --run && npm run build && grep -rn "processGofoodSales" convex/ src/` must return exit 0 for all four checks (the grep must return 0 lines).

**D74.5.2-L5 atomicity reminder for execute agent:** ALL 4 TASKS must ship in a SINGLE commit on a single feature branch. Do NOT commit Task 1 + Task 2 separately — the intermediate state has a broken build.
</verification>

<success_criteria>
- `grep -rn "processGofoodSales" convex/ src/` returns 0 lines (R5, SC2)
- `grep -c "gofood_sale" convex/productInventory/mutations.ts` returns 0 (2 write sites inside deleted function gone)
- `saveRevenueItems` remains the sole deduction path for GoFood (grep confirms it's still called in adapter.ts)
- `TransactionLogPanel` renders both legacy and migrated GoFood rows as "GoFood"
- Full test suite green
- Build green
</success_criteria>

## Git Workflow
**Branch:** `feature/74.5.2-gofood-atomic-retirement`
**Checkpoints:** Human-verify required — checker reviews the retirement diff before merge. This is the destructive step; D74.5.2-L5 atomicity means this single merge changes GoFood deduction behavior. Human gate is appropriate.

## Implementation Waves
### Wave 4: GoFood retirement (SEQUENTIAL within plan — atomic commit)
| Agent | Task | Files |
|-------|------|-------|
| Claude executor | Task 1: Delete processGofoodSales function body | convex/productInventory/mutations.ts |
| Claude executor | Task 2: Delete gobiz adapter call sites | convex/integrations/gobiz/adapter.ts |
| Claude executor | Task 3: Clean stockTracker comment + gofoodRegression.test.ts | convex/productInventory/stockTracker.ts, convex/productInventory/__tests__/gofoodRegression.test.ts |
| Claude executor | Task 4: TransactionLogPanel hybrid display | src/components/inventory/TransactionLogPanel.tsx |

## Documentation Updates
- [ ] Deferred to Plan 10 (CHANGELOG — "GoFood legacy path retired")
- [ ] Runbook (Plan 09) documents the post-deploy flag-flip procedure

## Success Criteria
- [ ] `grep -rn "processGofoodSales" convex/ src/` returns 0 lines
- [ ] `npm run type-check` passes
- [ ] `npm run build` succeeds
- [ ] `npm run test -- --run` green
- [ ] `saveRevenueItems` still present in gobiz adapter
- [ ] TransactionLogPanel hybrid branch added

<output>
After completion, create `.planning/phases/74.5.2-unified-deduct-cutover/74.5.2-08-SUMMARY.md` documenting:
- Line counts deleted from each file
- Path A vs Path B decision for gofoodRegression test
- Helper function added to TransactionLogPanel (name + location)
- Reminder: post-merge operational procedure requires admin to flip `channelDeductionEnabled.gobiz` via /admin/product-inventory-settings IMMEDIATELY after deploy-complete confirmation (Pitfall 2)
</output>
