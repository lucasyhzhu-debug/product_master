---
phase: 74.5.2
plan: 10
type: execute
wave: 5
depends_on: [74.5.2-01, 74.5.2-02, 74.5.2-03, 74.5.2-04, 74.5.2-05, 74.5.2-06, 74.5.2-07, 74.5.2-08, 74.5.2-09]
files_modified:
  - src/components/channelIntegration/AuditIssueTypeBadge.tsx
  - src/pages/ChannelRoutingManager.tsx
  - docs/CHANGELOG.md
  - docs/SCHEMA.md
  - docs/API_REFERENCE.md
  - docs/ROADMAP.md
autonomous: true
gap_closure: false
requirements: [DOC-CHANGELOG, DOC-SCHEMA, DOC-API, D74.5.2-L3]

must_haves:
  truths:
    - "AuditIssueTypeBadge react-refresh only-export-components lint warning resolved"
    - "ChannelRoutingManager useMemo deps lint warning resolved"
    - "docs/CHANGELOG.md has Phase 74.5.2 entry covering all shipped items"
    - "docs/SCHEMA.md updated for by_source_deductedAt index + gofood_sale migration note"
    - "docs/API_REFERENCE.md documents runChannelBackfill + runGofoodSaleToChannelSaleMigration + getSettlementItems"
    - "docs/ROADMAP.md marks 74.5.2 complete"
  artifacts:
    - path: docs/CHANGELOG.md
      provides: "74.5.2 phase entry"
      contains: "74.5.2"
    - path: docs/SCHEMA.md
      provides: "Index + migration documentation"
      contains: "by_source_deductedAt"
    - path: docs/API_REFERENCE.md
      provides: "New API surface documentation"
      contains: "runChannelBackfill"
  key_links:
    - from: docs/CHANGELOG.md
      to: docs/CHANNEL_INTEGRATION.md
      via: "cross-reference for runbook"
      pattern: "CHANNEL_INTEGRATION"
---

<objective>
Final polish + doc updates. Resolve two lint warnings (AuditIssueTypeBadge react-refresh, ChannelRoutingManager useMemo deps — D74.5.2-L3). Update 4 docs files: CHANGELOG, SCHEMA, API_REFERENCE, ROADMAP.

Purpose: Clean lint state before merge. Docs are MANDATORY post-merge per CLAUDE.md. Bundling lint polish with docs minimizes churn.

Output:
- 2 files lint-clean
- 4 doc files updated
</objective>

<execution_context>
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/workflows/execute-plan.md
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74.5.2-unified-deduct-cutover/74.5.2-CONTEXT.md
@.planning/phases/74.5.2-unified-deduct-cutover/74.5.2-RESEARCH.md
@.planning/phases/74.5.2-unified-deduct-cutover/74.5.2-PATTERNS.md
@src/components/channelIntegration/AuditIssueTypeBadge.tsx
@src/pages/ChannelRoutingManager.tsx
@docs/CHANGELOG.md
@docs/SCHEMA.md
@docs/API_REFERENCE.md
@docs/ROADMAP.md

<interfaces>
From PATTERNS.md §11 — AuditIssueTypeBadge.tsx react-refresh lint:
- 74.5.1 already extracted metadata to `AuditIssueTypeMeta.ts`. Current file imports from there at lines 21-23.
- If the react-refresh warning STILL fires, the issue is that ANOTHER non-component export exists in the badge file. Check for any `export const`, `export function`, `export type` that is NOT the component itself.

From PATTERNS.md §11 — ChannelRoutingManager.tsx useMemo deps warning:
- Common cause: missing dep in `useMemo([...])` dependency array.
- Typical fix: add the missing dep, OR refactor to inline the value if the dep set is unstable.

CHANGELOG format: check `docs/CHANGELOG.md` for existing entry style. Typically:
```
## [Phase 74.5.1] - 2026-04-20
### Added
- ...
### Changed
- ...
```

ROADMAP marker: `docs/ROADMAP.md` has a status column per phase. Mark 74.5.2 as ✅ Complete with completion date.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve AuditIssueTypeBadge react-refresh lint + ChannelRoutingManager useMemo deps</name>
  <files>src/components/channelIntegration/AuditIssueTypeBadge.tsx, src/pages/ChannelRoutingManager.tsx</files>
  <read_first>
    - src/components/channelIntegration/AuditIssueTypeBadge.tsx (full file — identify any non-component exports)
    - src/pages/ChannelRoutingManager.tsx (full file — find useMemo calls + their dep arrays)
    - Run `npm run lint` and capture exact warnings (file:line + rule name)
  </read_first>
  <action>
**Step 1: Run `npm run lint` and capture the exact warnings.**

For AuditIssueTypeBadge.tsx `only-export-components`:
- Identify the non-component export. Likely candidates: a helper constant, type alias, or utility function.
- Move the non-component export to `src/components/channelIntegration/AuditIssueTypeMeta.ts` (the existing sibling) OR create a new helper file `src/components/channelIntegration/AuditIssueTypeHelpers.ts`.
- Update all import sites to the new location.
- If the warning references a `TYPE_META` or similar metadata object that SHOULD live in the badge file for cohesion but triggers the warning: the cleanest fix is to co-locate exports of the same conceptual shape in one file. Extract metadata + badge to same meta file OR accept the warning with eslint-disable comment (if justified — prefer extraction).

For ChannelRoutingManager.tsx `react-hooks/exhaustive-deps`:
- Identify the `useMemo` (or `useCallback`) with missing deps.
- Option 1: Add missing deps to the array (preferred — simplest, correct).
- Option 2: If adding the dep causes infinite loop, use `useRef` to capture stable reference + memoize against the ref.
- Option 3: If the logic can be inlined without measurable perf impact, remove the `useMemo` wrapper.

**Step 2: Verify `npm run lint` exits 0 for these two files.**

Do NOT touch unrelated lint warnings — scope is strictly these two files. If other files in the codebase have warnings, they're out of scope for 74.5.2.
  </action>
  <verify>
    <automated>npm run lint 2>&amp;1 | grep -E "AuditIssueTypeBadge|ChannelRoutingManager" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` shows zero warnings for `src/components/channelIntegration/AuditIssueTypeBadge.tsx`
    - `npm run lint` shows zero warnings for `src/pages/ChannelRoutingManager.tsx`
    - `npm run type-check` passes
    - `npm run build` passes
    - No functionality change (components render identically — visual test)
  </acceptance_criteria>
  <done>Both lint warnings resolved; no functional regression.</done>
</task>

<task type="auto">
  <name>Task 2: Update CHANGELOG, SCHEMA, API_REFERENCE, ROADMAP</name>
  <files>docs/CHANGELOG.md, docs/SCHEMA.md, docs/API_REFERENCE.md, docs/ROADMAP.md</files>
  <read_first>
    - docs/CHANGELOG.md (last 100 lines — understand entry style + latest phase entry)
    - docs/SCHEMA.md (find the externalRevenueItems + productInventoryTransactions sections)
    - docs/API_REFERENCE.md (find sections for productInventory + migrations + consignment)
    - docs/ROADMAP.md (find Phase 74.5.2 entry at around line 407)
    - .planning/phases/74.5.2-unified-deduct-cutover/74.5.2-01-SUMMARY.md through 74.5.2-09-SUMMARY.md (if they exist — reference for exact things shipped)
  </read_first>
  <action>

**docs/CHANGELOG.md** — prepend a new entry at the top (under `# Changelog` heading):

```markdown
## [Phase 74.5.2] - 2026-04-{DD} — Unified Deduct Cutover + Backfill + Retire Legacy

### Added
- `convex/productInventory/backfill.ts` — per-source historical backfill for channel deductions (admin-triggered, idempotent, 200-item chunks). Exports `runChannelBackfill`, `getChannelBackfillPreflight`, `runOneChannelBackfillPage`.
- `convex/migrations/gofoodSaleToChannelSale.ts` — forward-only migration of `productInventoryTransactions.transactionType` from `gofood_sale` to `channel_sale + source: "gobiz"`. 500-row paginated chunks; self-healing on re-run.
- `convex/consignment/queries.ts::getSettlementItems` — admin+manager-gated query returning enriched per-product breakdown for a settlement.
- `src/hooks/convex/useChannelBackfill.ts` — React hooks for per-source backfill UI.
- Admin UI extension at `/admin/unlinked-products-backfill` — "Channel Deduction Backfill" section with 6 per-source cards.
- `SettlementFormDialog.tsx` — dynamic item-row inputs with sum validation.
- `OutletCard.tsx` (or SettlementTimeline) — expandable per-settlement "Products sold" subsection.
- `docs/CHANNEL_INTEGRATION.md` — channel onboarding + cutover runbook + audit triage + backfill ops + rollback.
- New `by_source_deductedAt` compound index on `externalRevenueItems`.

### Changed
- `src/components/inventory/TransactionLogPanel.tsx` — hybrid GoFood display: renders both legacy `gofood_sale` rows AND migrated `channel_sale + source=gobiz` rows as "GoFood" during the schema-literal soak period.
- `convex/productInventory/__tests__/channelAudit.test.ts` — fixed convex-test module-resolution bug on `t.action(internal.*)` invocations (4 previously red tests now green).
- `convex/integrations/bigseller/__tests__/normalize.test.ts` — tightened `platform: string` to `ExternalSource` union type.

### Removed
- `convex/productInventory/mutations.ts::processGofoodSales` — retired atomically with GoFood `channelDeductionEnabled.gobiz` flag flip. All deductions now flow through `saveRevenueItems → processChannelSaleInternal`.
- Both `processGofoodSales` call sites in `convex/integrations/gobiz/adapter.ts` (try/catch wrappers around manual-sync + auto-sync Phase C/D blocks).
- Stale comment reference in `convex/productInventory/stockTracker.ts`.

### Fixed
- `AuditIssueTypeBadge.tsx` react-refresh `only-export-components` lint warning.
- `ChannelRoutingManager.tsx` `react-hooks/exhaustive-deps` useMemo warning.

### Deferred (follow-up phase)
- Deferred: schema drops (`gofood_sale` literal, `channelDeductionEnabled` field) — tracked as 74.5.3 TBD (strip-before-drop 72h soak gate).
- `gofood_sale` literal drop from `productInventoryTransactions.transactionType` union (strip-before-drop — await 72h post-migration soak). See 74.5.3 TBD.
- `channelDeductionEnabled` flag field removal (pending cutover completion across all channels). See 74.5.3 TBD.

### Operational notes
- See `docs/CHANNEL_INTEGRATION.md` for per-channel flag-flip runbook. Cutover order: Shopee → TikTok → BigSeller → K3Mart → GoFood.
- GoFood flag MUST be flipped immediately after Plan 08 deploy-complete confirmation to minimize under-deduction window (D74.5.2-L5 + Pitfall 2).
- Backfill is idempotent and flag-independent — admin can pre-populate historical deductions before flipping a channel's flag ON.
```

Replace `{DD}` with today's date.

**docs/SCHEMA.md** — add/update:
1. Under the `externalRevenueItems` table section, append to the indexes list:
   ```
   - `by_source_deductedAt: [source, inventoryDeductedAt]` — Phase 74.5.2: per-source backfill queries narrow to un-deducted rows in constant time
   ```
2. Under `productInventoryTransactions.transactionType`, add a note:
   ```
   **Note (Phase 74.5.2):** The `gofood_sale` literal is deprecated. Historical rows are migrated via `runGofoodSaleToChannelSaleMigration` to `channel_sale + source: "gobiz"`. The literal stays in the union until a follow-up phase drops it post-72h-soak (strip-before-drop rule).
   ```

**docs/API_REFERENCE.md** — append entries:

Under `productInventory`:
```
### runChannelBackfill (mutation, admin)
Args: `{ source: ExternalSource, token: string }`. Schedules paginated historical backfill for the given source. Idempotent via `externalRevenueItems.inventoryDeductedAt` set-once.

### runOneChannelBackfillPage (mutation, admin)
Args: `{ source: ExternalSource, token: string }`. Single-page variant for UI loop-polling. Returns `{ itemsProcessed, deducted, skipped }`.

### getChannelBackfillPreflight (query, admin)
Args: `{ source: ExternalSource, token: string }`. Returns `{ pendingItems, blockingAuditIssues }` for the source (per-source gate, not global).
```

Under `migrations`:
```
### runGofoodSaleToChannelSaleMigration (mutation, admin)
Args: `{ token: string }`. Schedules forward-only migration of `gofood_sale` → `channel_sale + source: "gobiz"` rows. Self-healing on re-run.
```

Under `consignment`:
```
### getSettlementItems (query, admin+manager)
Args: `{ settlementId: Id<"consignmentSettlements">, token: string }`. Returns enriched per-product breakdown (externalRevenueItems joined with menuProduct). Pre-74.5.1 settlements return empty array.
```

**docs/ROADMAP.md** — locate Phase 74.5.2 entry (around line 407). Update:

```markdown
### Phase 74.5.2: Unified Deduct Cutover + Backfill + Retire Legacy Paths (INSERTED 2026-04-19, split from 74.5)
**Status: ✅ Complete (2026-04-{DD})**
**Goal**: ... (unchanged)

**Plans** (10 shipped in 6 waves):
- [x] 74.5.2-01-channel-audit-test-fix-PLAN.md — Wave 0: test infra fix
- [x] 74.5.2-02-backfill-schema-and-action-PLAN.md — Wave 1: schema index + backfill.ts
- [x] 74.5.2-03-backfill-tests-PLAN.md — Wave 1: backfill test suite
- [x] 74.5.2-04-gofood-migration-action-PLAN.md — Wave 2: migration action
- [x] 74.5.2-05-gofood-migration-tests-PLAN.md — Wave 2: migration test suite
- [x] 74.5.2-06-admin-backfill-ui-PLAN.md — Wave 3: per-source admin cards
- [x] 74.5.2-07-consignment-breakdown-ui-PLAN.md — Wave 3: consignment query + form rows + breakdown UI
- [x] 74.5.2-08-gofood-atomic-retirement-PLAN.md — Wave 4: delete processGofoodSales
- [x] 74.5.2-09-runbook-PLAN.md — Wave 5: docs/CHANNEL_INTEGRATION.md
- [x] 74.5.2-10-polish-and-docs-PLAN.md — Wave 5: lint polish + CHANGELOG/SCHEMA/API_REFERENCE/ROADMAP
```

Also update the summary table entry (around line 656):
```
| 74.5.2. Unified Deduct Cutover + Backfill + Retire Legacy Paths (INSERTED 2026-04-19) | v2.0 | 10/10 | Complete | 2026-04-{DD} |
```
  </action>
  <verify>
    <automated>grep -c "74.5.2" docs/CHANGELOG.md docs/SCHEMA.md docs/API_REFERENCE.md docs/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 74.5.2\\|\\[Phase 74.5.2\\]" docs/CHANGELOG.md` returns 1+
    - `grep -c "by_source_deductedAt" docs/SCHEMA.md` returns 1+
    - `grep -c "runChannelBackfill\\|runGofoodSaleToChannelSaleMigration\\|getSettlementItems" docs/API_REFERENCE.md` returns 3 (three new APIs)
    - `grep -c "74.5.2.*Complete\\|74.5.2.*✅" docs/ROADMAP.md` returns 1+
    - `grep -c "74.5.2-01\\|74.5.2-02\\|74.5.2-03\\|74.5.2-04\\|74.5.2-05\\|74.5.2-06\\|74.5.2-07\\|74.5.2-08\\|74.5.2-09\\|74.5.2-10" docs/ROADMAP.md` returns 10 (all plan filenames)
    - `npm run build` passes (docs changes don't break build)
  </acceptance_criteria>
  <done>All 4 docs files updated; changelog has complete summary; roadmap marked ✅.</done>
</task>

</tasks>

<verification>
Gate: `npm run type-check && npm run build && npm run lint && npm run test -- --run`. Lint shows zero warnings for the two touched files. Docs grep checks pass.
</verification>

<success_criteria>
- `npm run lint` clean on the two target files
- CHANGELOG entry comprehensive (Added/Changed/Removed/Fixed/Deferred)
- SCHEMA.md documents new index + migration
- API_REFERENCE.md documents 3 new APIs (backfill, migration, getSettlementItems)
- ROADMAP.md marks 74.5.2 complete with all 10 plans listed
- Full build + test suite green
</success_criteria>

## Git Workflow
**Branch:** Docs-only commits may go direct-to-main per CLAUDE.md doc-only rule. Lint fixes on code files REQUIRE a feature branch: `feature/74.5.2-polish` — land the code fixes there, then doc updates can land on main separately if simpler. RECOMMENDATION: ship both in one PR `feature/74.5.2-polish-and-docs` to avoid cross-ref merge races.
**Checkpoints:** None

## Implementation Waves
### Wave 5b: Polish + docs (SEQUENTIAL within plan)
| Agent | Task | Files |
|-------|------|-------|
| Claude executor | Task 1: Fix 2 lint warnings | src/components/channelIntegration/AuditIssueTypeBadge.tsx, src/pages/ChannelRoutingManager.tsx |
| Claude executor | Task 2: Update 4 doc files | docs/CHANGELOG.md, docs/SCHEMA.md, docs/API_REFERENCE.md, docs/ROADMAP.md |

## Documentation Updates
- [x] docs/CHANGELOG.md (Added/Changed/Removed/Fixed)
- [x] docs/SCHEMA.md (new index + migration note)
- [x] docs/API_REFERENCE.md (3 new API entries)
- [x] docs/ROADMAP.md (mark 74.5.2 complete)

## Success Criteria
- [ ] `npm run lint` passes cleanly for the 2 touched code files
- [ ] `npm run type-check` passes
- [ ] `npm run build` succeeds
- [ ] `npm run test -- --run` green (full suite)
- [ ] All 4 docs files updated

<output>
After completion, create `.planning/phases/74.5.2-unified-deduct-cutover/74.5.2-10-SUMMARY.md` documenting:
- Lint fix approach for each of the 2 files (extraction vs disable vs refactor)
- Key CHANGELOG bullet count summary
- Any doc structure adaptations made (e.g., if SCHEMA.md doesn't have a section for indexes — explain where the note was added)
</output>
