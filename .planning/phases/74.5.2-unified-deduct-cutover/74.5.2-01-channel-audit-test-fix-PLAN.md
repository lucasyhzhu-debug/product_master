---
phase: 74.5.2
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - convex/productInventory/__tests__/channelAudit.test.ts
  - convex/integrations/bigseller/__tests__/normalize.test.ts
autonomous: true
gap_closure: false
requirements: [SC6, D74.5.2-L1, D74.5.2-L2]

must_haves:
  truths:
    - "All 7 tests in channelAudit.test.ts pass (4 previously failing t.action invocations now green)"
    - "normalize.test.ts types platform field as ExternalSource union, not string"
    - "Full vitest suite passes with zero red in these two files"
  artifacts:
    - path: convex/productInventory/__tests__/channelAudit.test.ts
      provides: "Green test suite for channelAudit via fixed module resolution"
      contains: "import.meta.glob"
    - path: convex/integrations/bigseller/__tests__/normalize.test.ts
      provides: "Tightened platform literal"
      contains: "ExternalSource"
  key_links:
    - from: convex/productInventory/__tests__/channelAudit.test.ts
      to: convex/productInventory/channelAudit.ts
      via: "t.action(internal.productInventory.channelAudit.runFullAudit, ...) module resolution"
      pattern: "t\\.action\\(internal\\.productInventory\\.channelAudit"
---

<objective>
Fix the 4 failing `t.action(internal.*)` tests in `channelAudit.test.ts` (convex-test module-resolution bug — NOT shape drift) and tighten the BigSeller normalize test platform literal to the `ExternalSource` union. This is Wave 0 test-infra fix: no feature code changes. All subsequent waves depend on a green baseline.

Purpose: 74.5.1 shipped with 4 red tests accepted via override. Compounding 10 more plans on a red suite violates the regression-gate principle. Fix the glob/loader bug FIRST, then ship features.

Output:
- `channelAudit.test.ts` — 7/7 tests green (was 3/7)
- `normalize.test.ts` — platform field uses `ExternalSource`, not `string`
</objective>

<execution_context>
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/workflows/execute-plan.md
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/74.5.2-unified-deduct-cutover/74.5.2-CONTEXT.md
@.planning/phases/74.5.2-unified-deduct-cutover/74.5.2-RESEARCH.md
@.planning/phases/74.5.2-unified-deduct-cutover/74.5.2-VALIDATION.md
@convex/productInventory/__tests__/channelAudit.test.ts
@convex/productInventory/channelAudit.ts
@tests/convex/productSubstitution.test.ts
@convex/integrations/bigseller/__tests__/normalize.test.ts
@convex/lib/externalSource.ts

<interfaces>
Key references:

From convex/productInventory/channelAudit.ts (the module that cannot be resolved):
```typescript
export const runFullAudit = internalAction({
  args: { triggeredBy: v.string() },
  handler: async (ctx, args): Promise<{ issuesDetected: number; runId: Id<"channelAuditRuns"> }> => {
    // ...
  },
});
```

From convex/productInventory/__tests__/channelAudit.test.ts (line 195-198, failure site):
```typescript
await t.action(internal.productInventory.channelAudit.runFullAudit, {
  triggeredBy: "test",
});
```

Actual error: `Error: Could not find module for: "productInventory/channelAudit"` at `node_modules/convex-test/dist/index.js:1062` in `Object.actionFromPath`.

The test file already uses correct shape (`{issueType}` at lines 126, 153, 179, 223, 270, 287, 341). Do NOT rewrite shape.

From tests/convex/productSubstitution.test.ts (known-working `t.action()` reference) — check its `import.meta.glob` pattern for comparison.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and fix channelAudit.test.ts module resolution</name>
  <files>convex/productInventory/__tests__/channelAudit.test.ts</files>
  <read_first>
    - convex/productInventory/__tests__/channelAudit.test.ts (full file — especially the `import.meta.glob(...)` line and the `convexTest(schema, modules)` call)
    - tests/convex/productSubstitution.test.ts (known-working `t.action(internal.*)` — copy its glob pattern)
    - convex/productInventory/__tests__/channelSale.test.ts (same directory, does NOT use t.action — reference for what passes without action invocation)
    - .planning/phases/74.5.2-unified-deduct-cutover/74.5.2-RESEARCH.md §`channelAudit.test.ts` Failures — Real Root Cause (lines 344-383)
    - node_modules/convex-test/dist/index.js around line 1062 (actionFromPath — to understand what key is being looked up)
  </read_first>
  <action>
Run `npm run test -- --run convex/productInventory/__tests__/channelAudit.test.ts` first and CAPTURE the exact failure output (expect "Could not find module for: 'productInventory/channelAudit'" on 4 of 7 tests).

DO NOT rewrite test shape. Tests already use `{issueType}` correctly. The bug is in module registration via `import.meta.glob`.

Try fixes in this exact order, stopping at the first that makes all 7 tests pass:

**Fix 1 — glob suffix extension:**
Change the existing `const modules = import.meta.glob("../../**/*.ts");` (likely) to:
```typescript
const modules = import.meta.glob("../../**/*.{ts,tsx}");
```
Re-run the test file.

**Fix 2 — if Fix 1 fails, try eager flag:**
```typescript
const modules = import.meta.glob("../../**/*.{ts,tsx}", { eager: true });
```
Re-run.

**Fix 3 — if Fix 2 fails, copy the exact glob pattern from `tests/convex/productSubstitution.test.ts`:**
Open that file, grep for `import.meta.glob`, and replicate pattern verbatim (including path depth relative adjustment — if productSubstitution is at `tests/convex/` vs this test at `convex/productInventory/__tests__/`, adjust `../../` prefix but keep suffix + options identical).

**Fix 4 — if Fix 3 fails, fall back to direct-handler invocation:**
Refactor the 4 failing tests to invoke `runFullAudit` handler logic via `t.run(async (ctx) => { ... })` + a test-exported helper. Create `convex/productInventory/channelAudit.ts` test-helper export:
```typescript
export const _runFullAuditForTest = async (
  ctx: MutationCtx,
  args: { triggeredBy: string },
): Promise<{ issuesDetected: number; runId: Id<"channelAuditRuns"> }> => {
  // Copy handler body verbatim (or refactor handler to call this helper).
};
```
Then tests call `await t.run(async (ctx) => await _runFullAuditForTest(ctx, { triggeredBy: "test" }))`.

This fallback requires touching `convex/productInventory/channelAudit.ts` — add `files_modified` if reached.

**What MUST NOT change:**
- Test assertion shape (`{issueType: "unmapped_sku"}` stays)
- `runFullAudit` being an `internalAction` in production code (no downgrade to `internalMutation`)
- Test intent (still verifies runFullAudit detects all 5 issue types, schedules run record, etc.)

Document which fix succeeded in a code comment at top of test file: `// Fixed 74.5.2 Plan 01: convex-test module resolution via [Fix 1|2|3|4] — see RESEARCH §channelAudit.test.ts Failures`.
  </action>
  <verify>
    <automated>npm run test -- --run convex/productInventory/__tests__/channelAudit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run convex/productInventory/__tests__/channelAudit.test.ts` exits 0 with 7 passing, 0 failing
    - `grep -c "t.action(internal.productInventory.channelAudit" convex/productInventory/__tests__/channelAudit.test.ts` returns 1+ (tests still invoke action unless Fix 4 used)
    - `grep -c "issueType" convex/productInventory/__tests__/channelAudit.test.ts` returns 5+ (shape not changed)
    - No new `// TODO` or `it.skip` / `test.skip` in the file
    - Top of file has comment documenting which fix was applied
  </acceptance_criteria>
  <done>All 7 tests pass; no shape changes; fix documented in file comment.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten normalize.test.ts platform literal to ExternalSource union</name>
  <files>convex/integrations/bigseller/__tests__/normalize.test.ts</files>
  <read_first>
    - convex/integrations/bigseller/__tests__/normalize.test.ts (full file)
    - convex/lib/externalSource.ts (the union definition)
    - .planning/phases/74.5.2-unified-deduct-cutover/74.5.2-CONTEXT.md D74.5.2-L2
  </read_first>
  <action>
Locate the test fixture(s) where `platform:` is typed as plain `string`. Tighten to `ExternalSource` by importing the union type from `convex/lib/externalSource.ts`:

```typescript
import type { ExternalSource } from "../../../lib/externalSource";
// or wherever the existing type is exported
```

Replace any `platform: string` test fixture type annotations with `platform: ExternalSource`. If the test uses object literals where inference would select `string`, use `as const` or explicit literal types:
```typescript
const fixture = {
  platform: "bigseller" satisfies ExternalSource,
  // ...
};
```

Do NOT change test assertions, fixture values, or call shapes — only the type annotation/cast.

If the file imports from a shared types module, check if that module's `platform` type is `string`. If so, leave the module alone (don't widen scope) but ensure the test file's own types are tightened.
  </action>
  <verify>
    <automated>npm run test -- --run convex/integrations/bigseller/__tests__/normalize.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run convex/integrations/bigseller/__tests__/normalize.test.ts` passes
    - `npm run type-check` passes (no new TS errors)
    - `grep -c "platform: string" convex/integrations/bigseller/__tests__/normalize.test.ts` returns 0
    - `grep -c "ExternalSource\\|externalSource" convex/integrations/bigseller/__tests__/normalize.test.ts` returns 1+
  </acceptance_criteria>
  <done>Type tightened; tests still pass; no shape changes.</done>
</task>

</tasks>

<verification>
Full suite gate: `npm run test -- --run && npm run type-check && npm run build` — all green. No other files touched.
</verification>

<success_criteria>
- All 7 `channelAudit.test.ts` tests pass
- BigSeller normalize test uses `ExternalSource` type for platform
- Full test suite remains green
- Build passes
- No production code changed (unless Fix 4 fallback required minimal helper export in `channelAudit.ts`)
</success_criteria>

## Git Workflow
**Branch:** `feature/74.5.2-test-infra-fix`
**Checkpoints:** None (autonomous Wave 0)

## Implementation Waves
### Wave 0a: Diagnose + fix module resolution (SEQUENTIAL)
| Agent | Task | Files |
|-------|------|-------|
| Claude executor | Task 1: Fix channelAudit.test.ts module resolution | convex/productInventory/__tests__/channelAudit.test.ts |
| Claude executor | Task 2: Tighten normalize.test.ts platform literal | convex/integrations/bigseller/__tests__/normalize.test.ts |

## Documentation Updates
- [ ] No doc updates (test-infra only; CHANGELOG entry bundled in Plan 10)

## Success Criteria
- [ ] `npm run type-check` passes
- [ ] `npm run build` succeeds
- [ ] `npm run test -- --run convex/productInventory/__tests__/channelAudit.test.ts` shows 7/7 passing
- [ ] `npm run test -- --run` (full suite) green
- [ ] Top-of-file comment documents which fix hypothesis succeeded

<output>
After completion, create `.planning/phases/74.5.2-unified-deduct-cutover/74.5.2-01-SUMMARY.md` documenting:
- Which fix (1/2/3/4) resolved the module resolution bug
- Final glob pattern used
- Any convex-test version observations
- Links to comparable tests (productSubstitution.test.ts pattern reference)
</output>
