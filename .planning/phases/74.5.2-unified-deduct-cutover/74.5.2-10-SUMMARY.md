---
phase: 74.5.2
plan: 10
subsystem: docs/polish-and-docs
tags: [docs, changelog, schema, api-reference, roadmap, polish, lint-verification, DOC-CHANGELOG, DOC-SCHEMA, DOC-API, D74.5.2-L3]
requires:
  - 74.5.2-01
  - 74.5.2-02
  - 74.5.2-03
  - 74.5.2-04
  - 74.5.2-05
  - 74.5.2-06
  - 74.5.2-07
  - 74.5.2-08
  - 74.5.2-09
provides:
  - Phase_74.5.2_CHANGELOG_entry
  - externalRevenueItems_by_source_deductedAt_schema_doc
  - productInventoryTransactions_gofood_sale_deprecation_note
  - runChannelBackfill_API_doc
  - runGofoodSaleToChannelSaleMigration_API_doc
  - getSettlementItems_API_doc
  - ROADMAP_74.5.2_complete_status
  - deferred_items_closure_resolution
affects:
  - 74.5.3 (TBD) — downstream phase for schema literal drops (gofood_sale, channelDeductionEnabled); CHANGELOG Deferred section enumerates scope
tech-stack:
  added: []
  patterns:
    - "Prepend-to-top CHANGELOG pattern (`### Phase X.Y — Title -- YYYY-MM-DD` under `## [Unreleased]`) matching 74.5.1 / 80.3 / 80.2 shipped entries"
    - "Schema.md additions co-located next to the table definition (table-level notes, not a separate 'Phase X.Y changes' section) for discoverability"
    - "API_REFERENCE.md grouped by subsystem (productInventory / consignment / migrations) consistent with existing Phase 74 + Phase 80.2 sections"
    - "ROADMAP.md dual-update: (a) inline status line at phase-list section, (b) summary table row at bottom — both must change in sync"
    - "Deferred-item closure via appended '## Resolution' section preserving the original audit trail (not deletion)"
key-files:
  created:
    - .planning/phases/74.5.2-unified-deduct-cutover/74.5.2-10-SUMMARY.md
  modified:
    - docs/CHANGELOG.md
    - docs/SCHEMA.md
    - docs/API_REFERENCE.md
    - .planning/ROADMAP.md
    - .planning/phases/74.5.2-unified-deduct-cutover/deferred-items.md
key-decisions:
  - "Task 1 (lint fix on AuditIssueTypeBadge + ChannelRoutingManager) was a no-op — both files were already clean under `eslint --max-warnings=0`. Verified by running `npx eslint` against the two files + searching the full project lint output for their paths. Root cause: Phase 74.5.1 triple-review commit `bf036387` (2026-04-20) landed the fixes before Plan 10 was drafted. Plan 10 was written based on pre-triple-review lint output. Documented in SUMMARY + CHANGELOG Fixed section as `(No-op)` entries so the audit trail is preserved."
  - "CHANGELOG entry prepended under `## [Unreleased]` above the 74.5.1 entry — same position as every other incremental phase. Structure mirrors 74.5.1: For-the-team paragraph → overview paragraph → Added / Changed / Removed / Fixed / Deferred / Operational notes sections."
  - "SCHEMA.md: added Phase 74.5.1 notes alongside the Phase 74.5.2 note for `productInventoryTransactions` — 74.5.1 schema additions (channel_sale literal, source, externalRef, by_source_externalRef) were never documented; retroactively captured here to keep schema docs current. This is scope-adjacent not scope-creep: the 74.5.2 migration note doesn't make sense without the 74.5.1 context."
  - "API_REFERENCE.md: placed new subsections (Product Inventory Backfill, Consignment Per-Product Breakdown) immediately before Library Utilities at end of Convex Functions Reference — matches the per-subsystem section ordering established in earlier phases. Migration entry added inline under the existing Migration Mutations section."
  - "ROADMAP.md: updated both the phase-list status line (line 188: `[ ]` → `[x]` + completion date) AND the summary table row (line 666: `8/10 In Progress` → `10/10 Complete 2026-04-21`). All 10 plan checkboxes under the 74.5.2 phase section flipped to `[x]`."
  - "deferred-items.md closure: appended `## Resolution — Plan 10 review` section rather than deleting the entry. Rationale: preserves the discovery audit trail and documents why the existing `_args` + explicit `result` annotation IS the correct structural fix (not a workaround). Picked option (a) from bonus scope per plan prompt."
patterns-established:
  - "Doc-sweep closure pattern for cutover-style phases: single Plan N (last wave) bundles lint polish + CHANGELOG/SCHEMA/API_REFERENCE/ROADMAP into one docs-only commit, so merge is doc-only-path eligible per CLAUDE.md git workflow"
  - "No-op task documentation: when an acceptance-criteria task turns out already-complete from a prior plan, document the verification (not just the outcome) in SUMMARY + CHANGELOG so the audit trail survives"
  - "Deferred-item resolution in place: append `## Resolution` section to the existing deferred-items.md entry rather than creating a new file or deleting the entry — discoverable from the original filename, no dangling references in other docs"
metrics:
  duration_minutes: 14
  tasks: 2
  files_created: 1
  files_modified: 5
  commits: 1
  completed_at: "2026-04-21T12:25:00Z"
completed: 2026-04-21
---

# Phase 74.5.2 Plan 10: Polish + Docs Summary

Final polish + comprehensive docs sweep. Completes Phase 74.5.2 at 10/10 plans. No production code touched — all changes are in `docs/` and `.planning/`. Doc-only commit eligible per CLAUDE.md `doc-only paths` rule.

## One-line summary

Phase 74.5.2 closed out via documentation sweep: CHANGELOG + SCHEMA + API_REFERENCE + ROADMAP updated; Task 1 lint warnings confirmed already-resolved (no code changes).

## Deviations from Plan

### Task 1 (lint fix on AuditIssueTypeBadge.tsx + ChannelRoutingManager.tsx) — NO-OP

**Status:** Not a deviation in the traditional sense, but deserves explicit documentation.

**Finding:** Both target files were already lint-clean (zero warnings under `npx eslint --max-warnings=0`) when Plan 10 execution started. The plan was written assuming the 2 warnings identified in PATTERNS.md §11 still existed.

**Root cause:** Phase 74.5.1 triple-review (commit `bf036387` — `fix(74.5.1): triple-review Important+Minor+Nitpick fixes`, 2026-04-20) already resolved both warnings:
- `AuditIssueTypeBadge.tsx` `only-export-components`: fixed earlier in 74.5.1 by extracting `TYPE_META` to `AuditIssueTypeMeta.ts` (Review R1 per file header comment).
- `ChannelRoutingManager.tsx` `react-hooks/exhaustive-deps`: both `useMemo` call sites already have complete dep arrays (lines 146 + 259-268).

**Verification:**
```
npx eslint src/components/channelIntegration/AuditIssueTypeBadge.tsx \
           src/pages/ChannelRoutingManager.tsx --max-warnings=0
# exit: 0
```

**Action taken:** None on the source files. Documented the finding as `(No-op)` entries in the CHANGELOG `Fixed` section so the audit trail is preserved (an observer reading only CHANGELOG will understand why Plan 10 touched these files in the planning record but not in `git log`).

### Bonus scope: deferred-items.md closure

Per plan prompt, picked option (a): leave the existing `_args` + explicit `result` type annotation in `convex/migrations/gofoodSaleToChannelSale.ts` as-is. Appended a `## Resolution — Plan 10 review` section to `.planning/phases/74.5.2-unified-deduct-cutover/deferred-items.md` explaining that the current fix IS the correct structural one:
- `triggeredBy` is part of the action's scheduler-invocation contract (audit trail); must stay in signature
- The explicit `result` type annotation breaks a legitimate `tsc -b` project-reference cycle — it's the structural fix, not a workaround

No code change made. Deferred item marked closed in the audit trail.

## Doc updates — structure adaptations

### CHANGELOG.md

- Prepended new entry under `## [Unreleased]` heading, above the 74.5.1 entry (matches existing convention — newest phase at top).
- Sections: For-the-team paragraph, overview paragraph, Added (10 bullets), Changed (4 bullets), Removed (4 bullets), Fixed (3 bullets including 2 no-ops), Deferred (3 bullets), Operational notes (4 bullets).
- Linked to `docs/CHANNEL_INTEGRATION.md` (shipped in Plan 09) at multiple reference points.

### SCHEMA.md

- **Scope expansion (intentional):** added retroactive Phase 74.5.1 documentation alongside the 74.5.2 migration note, because the 74.5.1 schema additions (`channel_sale` literal, `source`, `externalRef`, `by_source_externalRef` index, `inventoryDeductedAt` field) were never documented. The 74.5.2 migration note is meaningless without the 74.5.1 baseline context.
- Two insertion points:
  1. `externalRevenueItems` indexes line: appended `by_source_deductedAt` (74.5.2) + new "Phase 74.5.1 additions" subsection documenting `inventoryDeductedAt` field.
  2. `productInventoryTransactions` Purpose paragraph: added "Phase 74.5.1 additions" + "Phase 74.5.2 migration note" subsections immediately after the existing purpose paragraph.

### API_REFERENCE.md

- 3 new API entries across 2 new subsections + 1 extension to an existing section.
- **New subsection: "Product Inventory — Channel Deduction Backfill (Phase 74.5.2)"** — placed immediately before Library Utilities at the bottom. Documents `runChannelBackfill`, `runOneChannelBackfillPage`, `getChannelBackfillPreflight`.
- **New subsection: "Consignment — Per-Product Breakdown (Phase 74.5.2)"** — documents `getSettlementItems` with frontend integration notes.
- **Extension to existing section: "Migration Mutations"** — inserted `runGofoodSaleToChannelSaleMigration` entry after the Phase 79 backfillRevenueOutletIds entry.

### ROADMAP.md

- Line 188 (phase-list status line): `[ ]` → `[x]`, appended `(completed 2026-04-21)`.
- Line 446 ("Plans:" list under 74.5.2 phase section): all 10 plans checkboxes flipped `[ ]` → `[x]`. Phase-level counter updated `8/10 plans executed` → `10/10 plans complete (2026-04-21)`.
- Line 666 (summary table row): `| 8/10 | In Progress|  |` → `| 10/10 | Complete    | 2026-04-21 |`.

## CHANGELOG bullet count summary

| Section | Count |
|---|---|
| Added | 10 |
| Changed | 4 |
| Removed | 4 |
| Fixed (incl. 2 no-ops) | 3 |
| Deferred (74.5.3 TBD) | 3 |
| Operational notes | 4 |

## Verification gates

All green:
- `npm run type-check` — passes clean.
- `npm run build` — exit 0 (CSS warnings pre-existing, unrelated).
- `npx eslint src/components/channelIntegration/AuditIssueTypeBadge.tsx src/pages/ChannelRoutingManager.tsx --max-warnings=0` — exit 0.
- `npm run test -- --run` — 1702 passed, 2 skipped, 0 failed (137 test files).

## Doc grep verification

| Assertion | Command | Expected | Result |
|---|---|---|---|
| CHANGELOG has 74.5.2 entry | `grep -c "Phase 74.5.2" docs/CHANGELOG.md` | ≥1 | 1 ✓ |
| SCHEMA has by_source_deductedAt | `grep -c "by_source_deductedAt" docs/SCHEMA.md` | ≥1 | 1 ✓ |
| API_REFERENCE has 3 new APIs | `grep -c "runChannelBackfill\|runGofoodSaleToChannelSaleMigration\|getSettlementItems" docs/API_REFERENCE.md` | 3 | 3 ✓ |
| ROADMAP lists all 10 plan filenames | `grep -c "74.5.2-01\|...\|74.5.2-10" .planning/ROADMAP.md` | 10 | 10 ✓ |
| ROADMAP marks 74.5.2 Complete | `grep -c "74.5.2.*Complete\|74.5.2.*✅" .planning/ROADMAP.md` | ≥1 | 2 ✓ |

## Commits

| Task | Commit | Description |
|---|---|---|
| Task 1 (lint) | — | No-op — files already clean |
| Task 2 (docs) | `0198c877` | docs(74.5.2-10): Task 2 — update CHANGELOG/SCHEMA/API_REFERENCE/ROADMAP |

## Self-Check: PASSED

- docs/CHANGELOG.md — FOUND, contains Phase 74.5.2 entry ✓
- docs/SCHEMA.md — FOUND, contains by_source_deductedAt + migration note ✓
- docs/API_REFERENCE.md — FOUND, contains 3 new API entries ✓
- .planning/ROADMAP.md — FOUND, 74.5.2 status = Complete, all 10 plans [x] ✓
- .planning/phases/74.5.2-unified-deduct-cutover/deferred-items.md — FOUND, has Resolution section ✓
- Commit `0198c877` — FOUND in git log ✓
- `npm run build` — PASSED (exit 0)
- `npm run test -- --run` — PASSED (1702/1704 passed, 2 skipped, 0 failed)
- `eslint --max-warnings=0` on target files — PASSED (exit 0)
