---
phase: 56-expense-training-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/guides/ExpenseGuide.tsx
  - src/lib/helpGuides.ts
  - src/lib/__tests__/helpGuides.test.ts
autonomous: true
requirements: [EGUIDE-01, EGUIDE-02, EGUIDE-03, EGUIDE-04, EGUIDE-05]

must_haves:
  truths:
    - "Navigating to /help/expenses renders a guide page with GuideLayout sidebar TOC"
    - "Overview section shows a lifecycle flowchart with 7 color-coded nodes and a role summary table"
    - "Submitting section shows 4 step cards, 3 callout boxes, and a mini FAQ with 3 items"
    - "Approving section shows a DoA workflow diagram, 3 step cards, and 3 callout boxes"
    - "Reimbursement section shows a batch workflow diagram, 6 step cards, and 2 callout boxes"
    - "TOC sidebar tracks active section on scroll and deep links work for sections 1-4"
    - "Help Center landing page shows expenses guide as live (not dimmed)"
  artifacts:
    - path: "src/pages/guides/ExpenseGuide.tsx"
      provides: "Expense guide with sections 1-4 rendered, sections 5-8 as placeholders"
      min_lines: 300
    - path: "src/lib/helpGuides.ts"
      provides: "Expenses entry with status=live and component=ExpenseGuide"
      contains: "status: \"live\""
    - path: "src/lib/__tests__/helpGuides.test.ts"
      provides: "Updated test expectations for expenses=live"
      contains: "expenses.*live"
  key_links:
    - from: "src/lib/helpGuides.ts"
      to: "src/pages/guides/ExpenseGuide.tsx"
      via: "import and component property"
      pattern: "component: ExpenseGuide"
    - from: "src/pages/guides/ExpenseGuide.tsx"
      to: "src/components/help/"
      via: "import of all 7 help components"
      pattern: "from \"@/components/help\""
---

<objective>
Create ExpenseGuide.tsx with the first 4 content sections (Overview, Submitting, Approving, Reimbursement), wire it into the guide registry as "live", and update tests.

Purpose: Delivers the first live guide page with the core expense workflow content (submit, approve, reimburse), making the help center functional for the first time.
Output: ExpenseGuide.tsx with 4 complete sections + 4 placeholder sections, live registry entry, passing tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-expense-training-guide/56-CONTEXT.md
@.planning/phases/56-expense-training-guide/56-RESEARCH.md
@docs/superpowers/specs/2026-03-16-help-center-design.md (Sections 4-7 for content)

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from Phase 55 codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From src/components/help/index.ts:
```typescript
export { RoleTag } from "./RoleTag";
export { CalloutBox } from "./CalloutBox";
export { StepCard } from "./StepCard";
export { GuideSection } from "./GuideSection";
export { FaqAccordion } from "./FaqAccordion";
export type { FaqItem, FaqGroup } from "./FaqAccordion";
export { WorkflowDiagram } from "./WorkflowDiagram";
export { GuideLayout } from "./GuideLayout";
export type { FlowNode, FlowEdge } from "./WorkflowDiagram";
```

From src/lib/helpGuides.ts (GuideConfig interface):
```typescript
export interface GuideConfig {
  id: string;
  title: string;
  description: string;
  icon: ComponentType<{ className?: string }>;
  accentColor: string;
  sections: GuideSection[];
  readTimeMinutes: number;
  status: "live" | "coming-soon";
  isNew?: boolean;
  component?: ComponentType;
}
```

Component API summary:
- WorkflowDiagram: `nodes: FlowNode[]` (id, label, color), `edges: FlowEdge[]` (from, to, label?, style?), `title?: string`
- StepCard: `step: number`, `title: string`, `description: string`, `icon?: ComponentType`, `tip?: string`, `warning?: string`, `isLast?: boolean`
- CalloutBox: `type: "tip" | "warning" | "important"`, `children: ReactNode`
- FaqAccordion: `groups: FaqGroup[]` where FaqGroup = `{ title: string, items: FaqItem[] }`, FaqItem = `{ question: string, answer: string | ReactNode }`
- GuideSection: `id: string`, `title: string`, `role?: "all" | "manager" | "admin"`, `children: ReactNode`
- GuideLayout: `title: string`, `description?: string`, `sections: { id: string; title: string }[]`, `readTimeMinutes?: number`, `children: ReactNode`
- RoleTag: `role: "all" | "manager" | "admin"`

Registry section IDs (MUST match exactly for TOC tracking):
overview, submitting, approving, reimbursement, payroll, analytics, pnl, faq
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ExpenseGuide.tsx with sections 1-4 and registry wiring</name>
  <files>src/pages/guides/ExpenseGuide.tsx, src/lib/helpGuides.ts, src/lib/__tests__/helpGuides.test.ts</files>
  <action>
**Create `src/pages/guides/ExpenseGuide.tsx`:**

Structure the file with data constants at the top, then the component function. Use `GuideLayout` wrapper with sections from the registry. All 8 `GuideSection` blocks must be present (sections 5-8 as simple placeholders with a "Content coming soon" paragraph so the TOC works).

**Section 1: Overview (`id="overview"`):**
- Intro paragraph: "The expense system lets anyone on the team submit expense claims, which managers and admins approve. Approved personal expenses get batched into reimbursement transfers. Payroll records staff and contractor payments. Everything auto-generates accounting entries that flow to the P&L."
- Lifecycle flowchart using `WorkflowDiagram` with these nodes (in this order to minimize diagonal crossings):
  - Draft (gray), Submitted (blue), Approved (green), Awaiting Payment (amber), Reimbursed (green), Rejected (red), Voided (red)
- Edges: Draft->Submitted, Submitted->Approved, Approved->Awaiting Payment, Awaiting Payment->Reimbursed, Submitted->Rejected (dashed, label "Reject"), Rejected->Submitted (dashed, label "Revise & Resubmit")
- Note below diagram: "Company card expenses skip Awaiting Payment -- Approved is terminal. Any non-terminal expense can be Voided by an admin."
- Role summary table (HTML table with Tailwind classes, `overflow-x-auto` wrapper):
  - 6 rows: Submit expenses (all Yes), Approve <=500K (Manager+Admin), Approve >500K (Admin only), Reimbursement batching (Admin only), Payroll entry (Admin only), View analytics (Manager+Admin)
  - Use em dash for "no access" cells

**Section 2: Submitting (`id="submitting"`, `role="all"`):**
- Intro paragraph about any staff member submitting
- 4 StepCards (set `isLast` on step 4):
  1. "Go to Expenses" -- Open the **Financials** dropdown in the top menu, click **Expenses**. Then tap the "New Expense" button.
  2. "Fill in the details" -- Description, amount (IDR), GL category, expense date, vendor name, payment method (personal cash, personal transfer, or company card)
  3. "Attach a receipt" -- Take a photo or upload an image. Required for amounts over Rp 50,000.
  4. "Save as draft or submit" -- Save Draft keeps it editable. Submit sends it to the approval queue.
- 3 CalloutBoxes:
  - tip: GL category advice (use 6990 Miscellaneous OpEx if unsure)
  - warning: Late submission flag (>14 days)
  - important: Cannot edit after submission
- Mini FAQ using FaqAccordion (1 group "Common Questions" with 3 items):
  - GL category selection guidance
  - Receipt requirement (>50K)
  - Duplicate handling (soft warning)

**Section 3: Approving (`id="approving"`, `role="manager"`):**
- DoA workflow diagram using WorkflowDiagram -- flatten the decision tree into vertical flow:
  - Nodes: "Expense Arrives in Queue" (blue), "Amount Check" (amber), "Manager or Admin Reviews" (blue), "Admin Only Reviews" (orange), "Approve or Reject" (green), "Awaiting Payment" (amber), "Approved (Terminal)" (green)
  - Edges: Arrives->Check, Check->Manager (label "<=500K"), Check->Admin (label ">500K"), Manager->Approve, Admin->Approve, Approve->Awaiting (label "Personal"), Approve->Terminal (label "Company Card")
- 3 StepCards (set `isLast` on step 3):
  1. "Open approval queue" -- Open the Financials dropdown, click Expenses. The approval tab shows pending expenses. (Managers and admins only; you won't see expenses you submitted yourself.)
  2. "Review the expense" -- Check amount, receipt, GL category, vendor. Look for fraud badges (duplicate, late, rejection count).
  3. "Approve or reject" -- Approve moves it forward. Reject requires a reason the submitter will see.
- 3 CalloutBoxes:
  - important: Cannot approve own expenses
  - warning: Comment required for >=500K
  - tip: Duplicate Warning badge guidance

**Section 4: Reimbursement (`id="reimbursement"`, `role="admin"`):**
- Batch workflow diagram using WorkflowDiagram:
  - Nodes: "Approved Expenses Pool" (green), "Group by Employee" (blue), "Create Batch" (blue), "Transfer via BCA" (amber), "Enter Reference + Date" (blue), "Confirm Batch" (blue), "Expenses Reimbursed" (green)
  - Edges: Pool->Group, Group->Batch (label "RMB-MMDD-NNN"), Batch->Transfer, Transfer->Reference, Reference->Confirm, Confirm->Done
- 6 StepCards (set `isLast` on step 6):
  1. "Open Reimbursement Manager" -- Financials > Reimburse (admin only)
  2. "Review pending expenses" -- Grouped by employee with running totals
  3. "Create a batch" -- Select expenses for one employee, click "Create Batch"
  4. "Transfer via bank" -- Open BCA mobile, transfer the batch total. Use the RMB code in the transfer notes.
  5. "Confirm the batch" -- Enter the BCA reference number, select the source bank account, set the transfer date
  6. "Done" -- All linked expenses are marked Reimbursed
- 2 CalloutBoxes:
  - tip: Void batch if transfer fails
  - important: Cannot void individual reimbursed expenses, must void entire batch

**Sections 5-8 (placeholders):**
Each section gets a `GuideSection` wrapper with correct id and title. Inside, render a simple paragraph: `<p className="text-muted-foreground">Content for this section is being added.</p>`. This ensures the TOC works and deep links resolve correctly. Plan 56-02 replaces these.

**Update `src/lib/helpGuides.ts`:**
- Add import at top: `import { ExpenseGuide } from "@/pages/guides/ExpenseGuide";`
- On the expenses entry: change `status: "coming-soon"` to `status: "live"`, add `component: ExpenseGuide,`
- Do NOT modify the sections array (already correct from Phase 55)
- Remove the comment `// Phase 56 sets expenses to "live" and wires ExpenseGuide component.` since it's now done

**Update `src/lib/__tests__/helpGuides.test.ts`:**
- Line 84-87: Change the "all entries are coming-soon" test to:
  - Test that expenses entry has `status: "live"` and `component` defined
  - Test that all OTHER entries remain `status: "coming-soon"`
- Add a new test: "expenses entry has a component" verifying `typeof expenses.component === 'function'`
  </action>
  <verify>
    <automated>cd "D:/Claude/Product Manager/product_master" && npx vitest run src/lib/__tests__/helpGuides.test.ts --reporter=verbose 2>&1 | tail -30 && npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <done>
- ExpenseGuide.tsx exists with 4 complete content sections (Overview, Submitting, Approving, Reimbursement) and 4 placeholder sections
- Overview has lifecycle WorkflowDiagram (7 nodes, gray/blue/green/amber/red colors) and role summary table (6 actions x 4 roles)
- Submitting has 4 StepCards, 3 CalloutBoxes, 1 FaqAccordion with 3 items
- Approving has DoA WorkflowDiagram (7 nodes), 3 StepCards, 3 CalloutBoxes
- Reimbursement has batch WorkflowDiagram (7 nodes), 6 StepCards, 2 CalloutBoxes
- helpGuides.ts shows expenses as "live" with ExpenseGuide component wired
- helpGuides.test.ts passes with updated assertions (expenses=live, others=coming-soon)
- `npm run type-check` passes
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/__tests__/helpGuides.test.ts` -- all tests pass including updated registry assertions
- `npm run type-check` -- no TypeScript errors
- `npm run build` -- build succeeds (full verification deferred to Plan 02)
</verification>

<success_criteria>
- ExpenseGuide.tsx created with 8 GuideSection blocks (4 complete, 4 placeholder)
- All GuideSection id props exactly match registry: overview, submitting, approving, reimbursement, payroll, analytics, pnl, faq
- Registry shows expenses as "live" with component wired
- Tests pass with expenses=live assertion
- Type check passes
</success_criteria>

<output>
After completion, create `.planning/phases/56-expense-training-guide/56-01-SUMMARY.md`
</output>
