---
phase: 80.2-unlinked-products-fix
plan: 03
subsystem: tests
tags: [convex-test, tdd, unit-tests, integration-tests, regression-lock, k3mart, direct-backfill, unit-economics]

# Dependency graph
requires:
  - phase: 80.2-01-schema-k3mart-fixes
    provides: applyRetroactiveProductMapping K3Mart branch (externalRevenueUpdated field); attachLinkedMenuProductId pure helper
  - phase: 80.2-02-direct-backfill-resync-heal
    provides: backfillInternalRevenueItems admin mutation; syncInternalOrders self-heal guard; saveRevenueItemsImpl extraction
provides:
  - "cascade.test.ts — 4 tests locking K3Mart retroactive mapping cascade (patch-happy, idempotent, remap-undefined, shopee-shape)"
  - "helpers-attach-linking.test.ts — 5 tests locking pure attachLinkedMenuProductId mapping helper (replaces v2 plan's sync-linking.test.ts)"
  - "backfillInternalRevenue.test.ts — 6 tests locking all counter semantics of the paginated-WRITE backfill mutation"
  - "adapter.test.ts — 2 tests locking syncInternalOrders self-heal guard (first t.action invocation in codebase)"
  - "unitEconomics-unlinked.test.ts — 2 tests locking post-fix attribution of K3Mart linked-no-children parents to their menuProduct (not UNLINKED)"
affects: [80.2-04-production-backfill-docs]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "NOVEL: first convex-test t.action(api.integrations.internal.adapter.syncInternalOrders) invocation in convex/ — syncInternalOrders makes zero outbound fetch calls so no global fetch stub required"
    - "Pure-helper extraction enables unit-testability without fetch-stub infrastructure (Gotcha §5 avoided)"
    - "Admin-token seed harness copied verbatim from retroactive-mapping-shopee.test.ts across 3 new test files — battle-tested pattern"
    - "Schema-ground-truth seeder: customers/orders/orderItems required fields enumerated from convex/schema.ts:177-345 comments; seeders cast via `as never` to bypass TS validator while the runtime validator still enforces"

key-files:
  created:
    - "convex/integrations/k3mart/__tests__/cascade.test.ts (NEW)"
    - "convex/integrations/k3mart/__tests__/helpers-attach-linking.test.ts (NEW — replaces v2 plan's sync-linking.test.ts per PATTERNS.md §5)"
    - "convex/externalData/__tests__/backfillInternalRevenue.test.ts (NEW)"
    - "convex/integrations/internal/__tests__/adapter.test.ts (NEW — directory created; novel t.action pattern)"
    - "convex/reports/__tests__/unitEconomics-unlinked.test.ts (NEW — path corrected from v2 plan per PATTERNS.md §8)"
  modified: []

key-decisions:
  - "unitEconomics-unlinked.test.ts targets skuSnapshot.skuTop, NOT the plan-referenced skuPareto — Phase 80.1 consolidated the 11 per-widget queries into 3 grouped snapshots; skuPareto no longer exists as a standalone export (verified against unitEconomics.ts:1125-1128). Shape { rows, totalRevenue } preserved inside skuSnapshot.skuTop so assertions translate 1:1."
  - "Used createdAt: Date.now() on every externalRevenueItems seed — schema line 1159 makes it required. Initial test run surfaced the missing field; fixed inline as a Rule 1 bug before final commit."
  - "t.action pattern succeeded on first attempt — no fallback to backfill-mutation-based test needed. Duration 326ms for the heal test confirms the action invocation works cleanly in convex-test (novel but supported)."
  - "All 5 test files use convex-test + vitest as the codebase-standard harness — no new test infrastructure introduced."

patterns-established:
  - "First action-level test in convex/: t.action(api.x.y.z, args) with no fetch stub when the action only uses runQuery/runMutation"
  - "Admin-token seed pattern now used in 4 distinct test files — canonicalize via helper file in next tech-debt pass"
  - "Minimum-valid-order seeder: customers.{name, phone, createdBy} + orders.{orderNumber, customerId, customerName, status, paymentStatus, orderDate, totalAmount/totalCost/totalMargin/finalTotal, deliveryType, createdBy, itemCount} + orderItems.{orderId, productName, quantity, unitPrice, unitCost, discountAmount, lineTotal, lineCost, lineMargin}"

requirements-completed: []

# Metrics
duration: 8min
completed: 2026-04-19
---

# Phase 80.2 Plan 03: Wave 3 Tests Summary

**5 new test files locking the 4 sub-fixes from Waves 1+2 — 19 new tests total (4 cascade + 5 pure helper + 6 backfill + 2 self-heal + 2 attribution regression). First convex-test `t.action(...)` invocation in the codebase, no fetch stubs introduced.**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-04-19T01:38:00Z (Task 3.1 first commit)
- **Completed:** 2026-04-19T01:46:00Z
- **Tasks:** 5 (each committed atomically)
- **Files created:** 5 new test files (one new directory: `convex/integrations/internal/__tests__/`)
- **Tests added:** 19

## Accomplishments

- **K3Mart cascade behavior locked** — `cascade.test.ts` proves `applyRetroactiveProductMappingImpl` with `source='k3mart'` patches matching parents, leaves non-matching untouched, is idempotent on re-run, and supports remap-to-undefined. A fourth test asserts the additive return shape for non-k3mart sources, locking the Wave 1 Task 1.3 contract.
- **Pure mapping-attachment helper covered** — `helpers-attach-linking.test.ts` exercises the 5 invariants of `attachLinkedMenuProductId` (happy path, non-match, missing code, shallow-copy non-mutation, late-binding via map mutation). Replaces the v2 plan's fetch-stub test per PATTERNS.md §5.
- **Backfill counter semantics locked** — `backfillInternalRevenue.test.ts` exercises all 6 counters: happy path (parentsBackfilled=1, itemsInserted=1), idempotency (all-zeros on second run), has-children skip, missing-order skip, empty-orderItems skip, and non-internal source ignore (parentsScanned=0 for k3mart).
- **Self-heal guard exercised at the action level** — `adapter.test.ts` proves `syncInternalOrders` heals orphan parents (no children → children created) and does not duplicate children for parents that already have them. First ever `t.action(...)` invocation in convex-test across the repo.
- **Post-fix attribution regression locked** — `unitEconomics-unlinked.test.ts` proves K3Mart parents with `linkedMenuProductId` + no children now attribute to their menuProduct (revenue=115000 lands on the menuProduct key, unlinked bucket = 0). Second test locks the already-working path (internal + children) so we have both ends of the fix covered.
- **Zero regressions** — all 120 pre-existing test files pass, all 1618/1620 tests green. The 2 failures are the pre-existing `correctAttendance.test.ts` Phase 74 issues already documented in `deferred-items.md`.

## Task Commits

1. **Task 3.1: cascade.test.ts** — `fed5393d` (test)
2. **Task 3.2: helpers-attach-linking.test.ts** — `b317da46` (test) — replaces v2 plan's `sync-linking.test.ts`
3. **Task 3.3: backfillInternalRevenue.test.ts** — `5480dad0` (test)
4. **Task 3.4: adapter.test.ts** — `8a598d23` (test) — novel `t.action` pattern
5. **Task 3.5: unitEconomics-unlinked.test.ts** — `6bd59b55` (test) — path + query corrected from v2 plan

Each task committed atomically per executor convention. The plan's checkpoint section anticipated a single `test(revenue): cover K3Mart cascade, Direct backfill, re-sync heal` commit — this wave produced 5 task-atomic commits instead, which better matches the project's per-file review workflow.

## Files Created/Modified

- `convex/integrations/k3mart/__tests__/cascade.test.ts` (created) — 4 tests, 264 lines. Admin-token harness + K3Mart parent/mapping seeders + the core cascade mutation under test.
- `convex/integrations/k3mart/__tests__/helpers-attach-linking.test.ts` (created) — 5 tests, 76 lines. Pure-function tests for `attachLinkedMenuProductId` imported from `../helpers`.
- `convex/externalData/__tests__/backfillInternalRevenue.test.ts` (created) — 6 tests, 316 lines. Admin-token + internal/k3mart parent seeders + `seedOrderWithItems` covering customers/orders/orderItems schema contract.
- `convex/integrations/internal/__tests__/adapter.test.ts` (created, new directory) — 2 tests, 215 lines. First `t.action(api.integrations.internal.adapter.syncInternalOrders, ...)` in convex/, covering both heal and skip branches of the Wave 2 Task 2.4 guard.
- `convex/reports/__tests__/unitEconomics-unlinked.test.ts` (created) — 2 tests, 174 lines. K3Mart parent-with-link-no-children attribution regression + internal parent-with-children non-regression.

## Decisions Made

- **Query corrected: `skuPareto` → `skuSnapshot`** — the v2 plan referenced `api.reports.unitEconomics.skuPareto` as the query under test. Phase 80.1 (the immediately preceding phase on main) consolidated the 11 per-widget queries into 3 grouped snapshots (`kpiAndChannelSnapshot`, `timeSeriesSnapshot`, `skuSnapshot`); the standalone `skuPareto` export no longer exists. The consolidated `skuSnapshot.skuTop` preserves the `{ rows, totalRevenue }` shape (see `reduceSkuTop` comment at line 907: "Shape preserved from live skuPareto handler"), so all Task 3.5 assertions translate 1:1 — just drill through `.skuTop.rows` instead of `.rows`. Logged as Rule 3 blocking fix.
- **Missing `createdAt` on externalRevenueItems seed** — initial run of Task 3.3's "skips parent that already has children" test surfaced a convex-test validator error (schema requires `createdAt` on every externalRevenueItems row). Fixed inline as a Rule 1 bug, re-ran the test, all 6 passed. Same fix applied proactively in Task 3.4's seeder.
- **t.action pattern worked without fallback** — PATTERNS.md §7 flagged a risk that the novel `t.action(api.integrations.internal.adapter.syncInternalOrders, {...})` invocation might fail in convex-test, with a documented fallback path to the backfill-mutation-based test. The action invocation succeeded on first attempt (two tests at 326ms each) — no fallback needed. The guard-swap behavior is covered at the true action level, not approximated.
- **Used `Rule 2` proactively: `createdAt: Date.now()` added to the Task 3.4 seeder** — before running the test, I noticed Task 3.3 had surfaced the required `createdAt` field on externalRevenueItems. Added it to the Task 3.4 seeder preemptively to avoid the same error. Pure copy-paste safety; no behavior change.
- **Acceptance criteria all green** — per Plan 03's Success Criteria section: all 5 test files exist, all 19 new tests pass, no regression in any existing test (beyond the pre-existing Phase 74 staff-attendance failures documented in `deferred-items.md`), no `sync-linking.test.ts` file created, no `convex/reports/unitEconomics.test.ts` file created. Verified with file-existence checks and grep.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] skuPareto query export does not exist — corrected to skuSnapshot**
- **Found during:** Task 3.5 implementation (the plan's skeleton referenced `api.reports.unitEconomics.skuPareto`)
- **Issue:** The standalone `skuPareto` query was consolidated into `skuSnapshot.skuTop` during Phase 80.1. Plan 03 (written earlier) referenced the now-nonexistent top-level export. The test wouldn't compile against the current `_generated/api.d.ts`.
- **Fix:** Changed the test to call `api.reports.unitEconomics.skuSnapshot`, destructure through `.skuTop.rows`. Shape is identical — Phase 80.1 explicitly preserved the `{ rows, totalRevenue }` contract.
- **Files modified:** `convex/reports/__tests__/unitEconomics-unlinked.test.ts`
- **Commit:** `6bd59b55`

**2. [Rule 1 - Bug] Missing `createdAt` field on externalRevenueItems seed**
- **Found during:** Task 3.3 "skips parent that already has children" first test run
- **Issue:** convex-test's runtime schema validator rejected the seed insert: `Validator error: Missing required field 'createdAt' in object`. Schema line 1159 defines `createdAt: v.number()` (required) on `externalRevenueItems`.
- **Fix:** Added `createdAt: Date.now()` to the seed object. Proactively applied the same fix to Task 3.4's equivalent seeder before running it.
- **Files modified:** `convex/externalData/__tests__/backfillInternalRevenue.test.ts`, `convex/integrations/internal/__tests__/adapter.test.ts`
- **Commit:** `5480dad0`, `8a598d23`

### Minor Deviations

None beyond the two above.

### Out-of-scope (deferred, carried from Waves 1+2)

- 2 failing tests in `convex/staffAttendance/__tests__/correctAttendance.test.ts` — Phase 74 scope, already documented in `deferred-items.md`. Full test suite run confirms these are the ONLY non-80.2 failures.
- 3 pre-existing lint errors in `convex/externalData/mutations.ts` — already documented in `deferred-items.md`. Wave 3 introduced 0 new lint errors (verified: `npx eslint <5 new test files>` returns clean).

---

**Total deviations:** 1 Rule 3 query-export correction + 1 Rule 1 missing-field bug. Both auto-fixed inline per executor scope; no user permission needed.
**Impact on plan:** Zero semantic drift. The plan's 19-test target was hit exactly (4+5+6+2+2); all acceptance criteria green.

## Issues Encountered

- **Initial `createdAt` validator miss** — surfaced on first run of Task 3.3; fixed in under a minute. Proactively audited Task 3.4 seeder to avoid repeat.
- **skuPareto → skuSnapshot query consolidation** — caught by reading the source before filling in the skeleton; no compile-time churn because the test was written with the correct export from the start.
- **LF → CRLF warnings from git** — Windows line-ending churn, cosmetic only. Every new test file triggered this warning on `git add`; ignored per project convention (no `.gitattributes` entry enforces LF).

## User Setup Required

None — tests only, backend-only. No production or deploy actions. Wave 4 (`80.2-04-PLAN.md`) will handle the production backfill invocation + docs updates.

## Next Phase Readiness

- **Wave 4 ready:** all 4 sub-fixes from Waves 1+2 now have behavior-locking test coverage. Production backfill can proceed with confidence that a regression would be caught by CI.
- **No blockers** for Wave 4.
- **Green CI prerequisite:** the 2 pre-existing staffAttendance failures are unrelated to 80.2. Main-branch CI currently carries these same failures (verified by checking the last successful Phase 80.1 merge — the deferred items log records them from before Phase 80.2 started).

## Self-Check: PASSED

Files created:
- FOUND: convex/integrations/k3mart/__tests__/cascade.test.ts
- FOUND: convex/integrations/k3mart/__tests__/helpers-attach-linking.test.ts
- FOUND: convex/externalData/__tests__/backfillInternalRevenue.test.ts
- FOUND: convex/integrations/internal/__tests__/adapter.test.ts
- FOUND: convex/reports/__tests__/unitEconomics-unlinked.test.ts

Commits:
- FOUND: fed5393d (test(80.2-03): add K3Mart retroactive mapping cascade test)
- FOUND: b317da46 (test(80.2-03): add attachLinkedMenuProductId pure helper test)
- FOUND: 5480dad0 (test(80.2-03): add backfillInternalRevenueItems mutation test)
- FOUND: 8a598d23 (test(80.2-03): add syncInternalOrders self-heal guard test)
- FOUND: 6bd59b55 (test(80.2-03): add unitEconomics unlinked-attribution regression)

Verification:
- `npm run type-check`: PASSED (0 errors)
- `npm run test`: 1618/1620 passed — 2 pre-existing Phase 74 staffAttendance failures (deferred), 19 new Wave 3 tests all pass
- `npx eslint <5 new test files>`: clean (0 errors, 0 warnings)
- v2 plan path NOT used: `test ! -f convex/reports/unitEconomics.test.ts` PASSES
- v2 plan fetch-stub test NOT created: `test ! -f convex/integrations/k3mart/__tests__/sync-linking.test.ts` PASSES

Task acceptance criteria (all green):
- Task 3.1: 4 `it(...)` blocks, mutation invocation grep passes, `externalRevenueUpdated` appears ≥5x ✓
- Task 3.2: 5 `it(...)` blocks, pure-helper import verified, no fetch-stub ✓
- Task 3.3: 6 `it(...)` blocks, all 6 counters asserted with specific values ✓
- Task 3.4: 2 `it(...)` blocks, `t.action(...)` invoked (grep passes), both guard branches covered ✓
- Task 3.5: 2 `it(...)` blocks, `skuSnapshot` used (consolidated replacement for skuPareto), `fromTs:` + `result.skuTop.rows` grep passes, UNLINKED_KEY assertion present, no stale `startDate|endDate|result\.items` references ✓

---
*Phase: 80.2-unlinked-products-fix*
*Completed: 2026-04-19*
