---
phase: 16.1-gobiz-openapi-audit-and-official-api-migration
plan: 03
type: execute
wave: 2
depends_on: ["16.1-01"]
files_modified:
  - docs/apiS/INTEGRATION_REFERENCE.md
  - docs/CHANGELOG.md
autonomous: true

must_haves:
  truths:
    - "INTEGRATION_REFERENCE.md points to official docs as the primary GoBiz reference"
    - "Old unofficial GoBiz docs in docs/apiS/ are superseded by the new official docs"
    - "CHANGELOG.md records the Phase 16.1 documentation overhaul"
  artifacts:
    - path: "docs/apiS/INTEGRATION_REFERENCE.md"
      provides: "Updated integration reference with official API cross-references"
      contains: "gobiz-official/"
    - path: "docs/CHANGELOG.md"
      provides: "Phase 16.1 changelog entry"
      contains: "16.1"
  key_links:
    - from: "docs/apiS/INTEGRATION_REFERENCE.md"
      to: "docs/apiS/gobiz-official/"
      via: "cross-reference links"
      pattern: "gobiz-official"
---

<objective>
Update the existing integration reference to point to new official documentation, supersede old unofficial GoBiz docs, and update the project changelog.

Purpose: Ensure developers (and Claude) are directed to the correct, comprehensive official docs rather than the outdated informal docs. Clean up documentation debt.

Output: Updated INTEGRATION_REFERENCE.md, updated CHANGELOG.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16.1-gobiz-openapi-audit-and-official-api-migration/16.1-CONTEXT.md
@.planning/phases/16.1-gobiz-openapi-audit-and-official-api-migration/16.1-01-SUMMARY.md
@docs/apiS/INTEGRATION_REFERENCE.md
@docs/CHANGELOG.md
</context>

## Git Workflow
**Branch:** `feature/16.1-gobiz-openapi-audit` (same branch as Plans 01-02)
**Checkpoints:** After all tasks complete

## Implementation Waves
### Wave 1: Documentation Updates [PARALLEL]
| Agent | Task | Files |
|-------|------|-------|
| executor | Update INTEGRATION_REFERENCE + supersede old docs | docs/apiS/INTEGRATION_REFERENCE.md |
| executor | Update CHANGELOG | docs/CHANGELOG.md |

### Wave 2: Verification [SEQUENTIAL]
| Agent | Task |
|-------|------|
| code-auditor | Verify cross-references and changelog |

## Documentation Updates
- [ ] INTEGRATION_REFERENCE.md (update GoBiz section)
- [ ] CHANGELOG.md (add Phase 16.1 entry)

## Success Criteria
- [ ] INTEGRATION_REFERENCE.md references docs/apiS/gobiz-official/ for GoBiz
- [ ] Old GoBiz docs noted as superseded
- [ ] CHANGELOG.md has Phase 16.1 entry

<tasks>

<task type="auto">
  <name>Task 1: Update INTEGRATION_REFERENCE.md with official API cross-references and supersede old GoBiz docs</name>
  <files>
    docs/apiS/INTEGRATION_REFERENCE.md
  </files>
  <action>
    Read the current `docs/apiS/INTEGRATION_REFERENCE.md` and update the GoBiz section (Section 3) to:

    1. **Add a prominent notice** at the top of the GoBiz section:
    ```markdown
    > **Official API Reference:** See `docs/apiS/gobiz-official/` for comprehensive official GoBiz API documentation.
    > See `docs/apiS/gobiz-official/gap-analysis.md` for the formal endpoint mapping between our integration and the official API.
    ```

    2. **Add a "Documentation" subsection** listing all new docs:
    ```markdown
    ### 3.X Official API Documentation
    | Document | Contents |
    |----------|----------|
    | `gobiz-official/00-overview.md` | Portal structure, environments, base URLs |
    | `gobiz-official/01-authentication.md` | OAuth2 client credentials flow |
    | ... (all 12 + gap-analysis.md) |
    ```

    3. **Mark the existing unofficial endpoint documentation** as "current (unofficial)" -- do NOT delete, but add a note:
    ```markdown
    > **Note:** The endpoints documented below are reverse-engineered unofficial APIs.
    > These remain in use because the official API does not provide equivalent historical
    > data querying capabilities. See `gobiz-official/gap-analysis.md` for details.
    ```

    4. **Update the Overview table** (Section 1) to add a "Docs" column or note pointing to the official docs.

    5. **Mark old standalone GoBiz files as superseded** by adding a note. The files to mark:
    - `docs/apiS/gojek search transactions documentation.txt` -- superseded by `gobiz-official/gap-analysis.md` and integration status headers
    - `docs/apiS/stock in stock out api documentation.md` -- this is K3Mart, NOT GoBiz, leave as-is
    - `docs/apiS/stock in stock out api documentation.txt` -- this is K3Mart, NOT GoBiz, leave as-is

    Per locked decision: "Replace existing docs/apiS/ GoBiz files with new official docs -- old unofficial docs superseded." However, do NOT delete the old files -- they contain real API response examples that may be useful. Instead, add a deprecation header to the old GoBiz file:

    Read `docs/apiS/gojek search transactions documentation.txt` and if it's a GoBiz-related doc, add a note in INTEGRATION_REFERENCE.md marking it as superseded.

    **Do NOT modify K3Mart documentation** (stock in stock out files are K3Mart, not GoBiz).
  </action>
  <verify>
    - INTEGRATION_REFERENCE.md contains "gobiz-official/" references
    - GoBiz section has the "Official API Reference" notice
    - Existing unofficial endpoint docs are preserved but marked as unofficial
    - K3Mart docs are untouched
  </verify>
  <done>
    INTEGRATION_REFERENCE.md updated to direct developers to official GoBiz API docs while preserving unofficial endpoint documentation with appropriate context notes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CHANGELOG.md with Phase 16.1 entry</name>
  <files>
    docs/CHANGELOG.md
  </files>
  <action>
    Read `docs/CHANGELOG.md` and add a new entry for Phase 16.1 at the top of the changelog (below any header). Follow the existing changelog format.

    **Entry content:**
    ```markdown
    ## Phase 16.1: GoBiz OpenAPI Audit & Official API Migration (2026-02-16)

    ### Documentation
    - Added comprehensive official GoBiz API documentation in `docs/apiS/gobiz-official/` (12 files covering auth, menu, orders, payments, webhooks, events)
    - Created formal gap analysis (`gap-analysis.md`) mapping current unofficial endpoints to official API equivalents
    - Added Integration Status headers to all API docs showing our Convex function mapping
    - Updated `INTEGRATION_REFERENCE.md` to cross-reference official docs

    ### Key Findings
    - Official GoBiz API is POS-focused (receiving orders via webhook, menu sync) -- does NOT provide historical revenue/transaction queries
    - Current revenue sync (journals/search, orders/search) has no official equivalent -- must remain on unofficial endpoints
    - Auth could migrate to OAuth2 client_credentials (eliminates fragile 3-method token cascade) -- requires partner registration
    - 12+ official endpoints documented for future adoption: menu sync, order auto-accept, stock management, webhooks
    ```
  </action>
  <verify>
    - CHANGELOG.md contains "Phase 16.1" entry
    - Entry includes both Documentation and Key Findings sections
    - Date is 2026-02-16
  </verify>
  <done>
    CHANGELOG.md updated with Phase 16.1 entry summarizing the documentation overhaul and key gap analysis findings.
  </done>
</task>

</tasks>

<verification>
1. INTEGRATION_REFERENCE.md references `docs/apiS/gobiz-official/` at least once
2. INTEGRATION_REFERENCE.md has "Official API Reference" notice in GoBiz section
3. CHANGELOG.md has Phase 16.1 entry with correct date
4. No K3Mart docs were modified
5. Old GoBiz docs preserved (not deleted) with superseded notes
</verification>

<success_criteria>
- [ ] INTEGRATION_REFERENCE.md updated with official doc cross-references
- [ ] Old GoBiz docs noted as superseded (not deleted)
- [ ] CHANGELOG.md has Phase 16.1 entry
- [ ] K3Mart documentation untouched
- [ ] All cross-references use correct relative paths
</success_criteria>

<output>
After completion, create `.planning/phases/16.1-gobiz-openapi-audit-and-official-api-migration/16.1-03-SUMMARY.md`
</output>
