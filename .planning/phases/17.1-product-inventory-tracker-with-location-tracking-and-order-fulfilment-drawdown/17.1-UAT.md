---
status: diagnosed
phase: 17.1-product-inventory-tracker-with-location-tracking-and-order-fulfilment-drawdown
source: [17.1-01-SUMMARY.md, 17.1-02-SUMMARY.md, 17.1-03-SUMMARY.md, 17.1-04-SUMMARY.md]
started: 2026-02-20T12:00:00Z
updated: 2026-02-20T15:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Finished Goods tab appears in InventoryManager
expected: Open the Inventory page. The top-level tabs show three options: Packaging, Ingredients, and Finished Goods. Clicking "Finished Goods" switches to the new tab without any errors.
result: pass

### 2. Empty state on first visit
expected: With no stock added yet, the Finished Goods tab shows an empty state (prompt to add first stock). No errors or blank white screen.
result: pass

### 3. Add Stock dialog — staff flow
expected: Product dropdown shows only active POS menu products. Dialog works, toast confirms, card appears.
result: issue
reported: "Product dropdown shows ALL menu products (Original, Bite Sized Single/Double/Triple, Jumbo, Brochure - How to eat) — should only show the 2 active POS items: Original Single (45g) and Original Triple (135g)."
severity: major

### 4. Location selector UX
expected: Location selector in Add Stock / Adjust Stock dialogs uses 3 toggle buttons (not a dropdown).
result: issue
reported: "Location picker is a dropdown — should be 3 buttons for Office, Kitchen, Legato Goldfinch since there are only 3 locations."
severity: minor

### 5. Low-stock alerts and card coloring
expected: When stock is at or below threshold (default 5), red banner + amber/red card coloring appear.
result: issue
reported: "No low-stock warning at stock=2. Badge showed green. Root cause: threshold saved as 0 silently disabled all alerts."
severity: major
fix_status: FIXED — commits 9214a50 (threshold || 5 fallback + min validation of 1)

### 6. Adjust Stock dialog — manager flow
expected: Manager clicks Adjust, dialog opens with Add/Remove toggle, reason required, stock updates.
result: pass

### 7. Transaction history per product
expected: History expands showing typed entries with sign, location, reason, timestamp.
result: pass

### 8. Settings panel labels
expected: Settings labels clearly explain what each field does.
result: issue
reported: "User unsure what 'Auto-advance on Drawdown' and 'Alert Mode' do — descriptions insufficient."
severity: cosmetic

### 9. "Use Available Inventory" + "Force Complete" buttons on OrderDetail
expected: PaymentReceived orders show "Use Available Inventory" card above Actions. Admin/Manager sees "Force Complete" in Actions.
result: pass

### 10–12. Drawdown flow (availability panel, success, error)
expected: Availability panel shows Needed/Available/Status columns. Success flow: Confirm button enabled, click → toast + order advances. Short flow: row red, warning shown, Confirm disabled.
result: pass

## Summary

total: 12
passed: 6
issues: 3
pending: 0
skipped: 0

## Gaps

# ── FIXED ─────────────────────────────────────────────────────────────

- truth: "Low-stock alert banner and card coloring trigger when stock is at or below threshold (default 5)"
  status: resolved
  reason: "Threshold was saved as 0 silently disabling all alerts. Fixed: || 5 fallback + min threshold=1 validation."
  severity: major
  test: 5
  root_cause: "productInventorySettings.globalLowStockThreshold saved as 0; ?? operator does not treat 0 as falsy so fallback never fired"
  artifacts:
    - path: "convex/productInventory/queries.ts"
      issue: "lines 24+184: ?? 5 → || 5"
    - path: "src/components/inventory/FinishedGoodsTab.tsx"
      issue: "line 137: threshold < 0 → threshold < 1"
  fix_commit: "9214a50"

# ── OPEN — Major ──────────────────────────────────────────────────────

- truth: "'Use Available Inventory' button visible on OrderDetail for PaymentReceived orders"
  status: diagnosed
  reason: "Button not rendering despite correct code in file. Force Complete (ungated) also invisible — old JS bundle likely served. Component is in lg:col-span-2 left column at line ~291, Actions card also left column. On mobile both stack but button should appear above Actions."
  severity: major
  test: 9
  root_cause: "FulfillFromInventoryButton (lines 289-296) AND Actions card (lines 299-326) are BOTH in the lg:col-span-2 LEFT column, buried under the Order Header card. UAT tester looked in the right sidebar. Fix: move both blocks to TOP of lg:col-span-1 RIGHT column (before OrderItems at line 456). orderId is not undefined, 'PaymentReceived' status string is correct, no import issue — pure layout problem."
  artifacts:
    - path: "src/pages/OrderDetail.tsx"
      issue: "lines 289-326: FulfillFromInventoryButton + Actions card in wrong column (lg:col-span-2 left); should be at top of lg:col-span-1 right column before OrderItems (line 456)"
  missing:
    - "OrderDetail.tsx — cut lines 289-326 (FulfillFromInventoryButton + Actions card) from left column, paste at top of right column before OrderItems"

- truth: "Add Stock / Adjust Stock / FulfillFromInventory dialogs product dropdown shows only active POS menu products"
  status: diagnosed
  reason: "User reported: All menu products shown including inactive items. Should filter to posSlot-assigned products only (Original Single 45g, Original Triple 135g)."
  severity: major
  test: 3
  root_cause: "Both dialogs filter by isActive but NOT posSlot. Any active product without a posSlot (e.g. Brochure) appears. Fix: add `&& p.posSlot != null` to client-side filter. listPosProducts query already exists but unused here. FulfillFromInventoryButton has no product dropdown — no fix needed there."
  artifacts:
    - path: "src/components/inventory/FGAddStockDialog.tsx"
      issue: "line 124: filter only checks isActive, not posSlot — add `&& p.posSlot != null`"
    - path: "src/components/inventory/FGAdjustStockDialog.tsx"
      issue: "line 141: same missing posSlot filter — add `&& p.posSlot != null`"
  missing:
    - "FGAddStockDialog.tsx:124 — add `&& p.posSlot != null` to activeProducts filter"
    - "FGAdjustStockDialog.tsx:141 — add `&& p.posSlot != null` to activeProducts filter"

- truth: "Adjust Stock dialog shows current stock quantity at selected location before adjusting"
  status: diagnosed
  reason: "User reported: no stock count visible when location selected in Adjust dialog — user can't know current level before entering adjustment."
  severity: major
  test: 5
  root_cause: "FGAdjustStockDialog has no stock query. Parent (ProductStockCard) holds group.locations[] with per-location quantities but doesn't pass them down. Fix: add stockByLocation prop to dialog, pass group.locations from parent — zero extra Convex calls."
  artifacts:
    - path: "src/components/inventory/FGAdjustStockDialog.tsx"
      issue: "line 43: add stockByLocation prop to interface; after location selector (~line 203): derive + render 'Current stock: N units'"
    - path: "src/components/inventory/ProductStockCard.tsx"
      issue: "lines 191-198: pass stockByLocation={group.locations.map(l => ({locationId: l.locationId, quantity: l.quantity}))} to dialog"
  missing:
    - "FGAdjustStockDialog.tsx:43 — add stockByLocation prop"
    - "FGAdjustStockDialog.tsx:~203 — render 'Current stock: N units' inline between location selector and adjustment type"
    - "ProductStockCard.tsx:191-198 — pass stockByLocation from group.locations"

# ── OPEN — Minor ──────────────────────────────────────────────────────

- truth: "Location selector in Add Stock / Adjust Stock dialogs uses 3 toggle buttons not a dropdown"
  status: diagnosed
  reason: "User reported: dropdown shown — should be 3 button toggles since only 3 depot locations exist."
  severity: minor
  test: 4
  root_cause: "Both dialogs use shadcn Select for location. FulfillFromInventoryButton already has the correct button-toggle pattern at lines 169-191. Replicate it in both dialogs."
  artifacts:
    - path: "src/components/inventory/FGAddStockDialog.tsx"
      issue: "lines 181-199: Select block — replace with Button group; add cn import"
    - path: "src/components/inventory/FGAdjustStockDialog.tsx"
      issue: "lines 184-203: Select block — replace with Button group (cn already imported)"
  missing:
    - "FGAddStockDialog.tsx:181-199 — replace Select with flex Button group matching FulfillFromInventoryButton pattern"
    - "FGAdjustStockDialog.tsx:184-203 — same replacement"

- truth: "After drawdown, success message shows 'X used, Y remaining' per product"
  status: diagnosed
  reason: "User requested: post-drawdown toast/confirmation should summarise what was used and what's left."
  severity: minor
  test: 9
  root_cause: "Mutation at line 340 returns { success, itemsFulfilled } — discards per-product detail. FulfillFromInventoryButton line 122 ignores return value, shows generic toast. Fix: collect per-product results in mutation, return them, display in toast description."
  artifacts:
    - path: "convex/productInventory/mutations.ts"
      issue: "line 340: build results[] in deduction loop (productName, used, remaining), return alongside itemsFulfilled"
    - path: "src/components/inventory/FulfillFromInventoryButton.tsx"
      issue: "line 122: capture mutation return, map results to toast description lines"
  missing:
    - "mutations.ts — collect {productName, used, remaining} per item in deduction loop, return as results[]"
    - "FulfillFromInventoryButton.tsx:122 — use result.results to build per-product toast description"

# ── OPEN — Cosmetic ───────────────────────────────────────────────────

- truth: "Settings panel labels clearly explain Auto-advance on Drawdown and Alert Mode fields"
  status: diagnosed
  reason: "User unsure what the two settings do. Descriptions exist but aren't clear enough."
  severity: cosmetic
  test: 8
  root_cause: "Auto-advance uses raw status name 'AwaitingDelivery' in helper text. Alert Mode has no helper text at all. Both need plain-language rewrites."
  artifacts:
    - path: "src/components/inventory/FinishedGoodsTab.tsx"
      issue: "lines 288-291: Auto-advance label + description use internal jargon; lines 300-314: Alert Mode has no helper text"
  missing:
    - "FinishedGoodsTab.tsx:288-291 — label: 'Move order forward after fulfillment'; description: 'When on, fulfilling from inventory automatically moves the order to Awaiting Delivery — no manual status change needed.'"
    - "FinishedGoodsTab.tsx:300-314 — label: 'Low-stock notification style'; SelectItem text: 'Notification popup only' / 'Popup + red badge on menu icon'; add helper text below Select"

# ── NON-INVENTORY (separate task) ─────────────────────────────────────

- truth: "Edit Component dialog allows changing component category (ingredient vs packaging)"
  status: diagnosed
  reason: "User reported during Test 1: ComponentTypesManager Edit dialog has no field to switch category between production (ingredient) and packaging."
  severity: major
  test: 1
  root_cause: "ComponentTypesManager.tsx no longer exists — replaced by ProductionComponentsManager.tsx. Edit dialog at lines 371-514 exposes name/gramsPerUnit/color/batchSize/cogsMode but not category. Update mutation (convex/componentTypes/mutations.ts) also lacks category arg. Both backend and frontend need changes. Category change is high-impact (affects inventory batches, BOM links) — add warning if component has existing inventory."
  artifacts:
    - path: "convex/componentTypes/mutations.ts"
      issue: "line 144: add category: v.optional(v.union(v.literal('production'), v.literal('packaging'))) to update mutation args"
    - path: "src/pages/ProductionComponentsManager.tsx"
      issue: "lines 83/105-115/89-97/139-148: add category state, populate on edit, reset, pass to mutation; add Select UI after name field (~line 385)"
  missing:
    - "mutations.ts:144 — add optional category to update args; pass in update payload"
    - "ProductionComponentsManager.tsx — add category state + Select UI (Production/Packaging) after name field; warn if component has existing inventory batches"

- truth: "'Brochure - How to eat' does not exist as a menu product (it is not food)"
  status: failed
  reason: "User reported: Brochure listed in Add Stock product dropdown. Must be deleted from menuProducts table."
  severity: major
  test: 3
  root_cause: "Live data issue — 'Brochure - How to eat' is a real row in menuProducts. Delete via Menu Products Manager page (delete button exists) or via a Convex migration."
  artifacts: []
  missing:
    - "Delete 'Brochure - How to eat' from menuProducts via Menu Products Manager → delete button"
