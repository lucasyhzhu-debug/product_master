---
phase: 15-kitchen-overhaul
plan: 04
type: execute
wave: 3
depends_on: ["15-02", "15-03"]
files_modified:
  - src/pages/KitchenViewV2.tsx
  - src/hooks/convex/useKitchenProduction.ts
  - src/components/kitchen/InventoryOverrideButton.tsx
  - src/components/kitchen/index.ts
  - docs/CHANGELOG.md
autonomous: false

must_haves:
  truths:
    - "KitchenViewV2 renders DashboardHeader sticky above existing swipeable batch panels"
    - "KitchenViewV2 renders DueDateOrderList below the dashboard header alongside existing panels"
    - "Dashboard header stats are populated from real getKitchenStats + getConfig queries"
    - "Kitchen config gear icon opens TargetConfigPopover for managers"
    - "Complete Order calls markOrderReady and moves order to AwaitingDelivery"
    - "Send back to order desk calls sendBackToOrderDesk and returns order to PaymentReceived"
    - "Manager can override unavailable inventory with a reason via InventoryOverrideButton"
    - "CHANGELOG.md updated with Phase 15 changes"
    - "npm run build succeeds with zero errors"
  artifacts:
    - path: "src/pages/KitchenViewV2.tsx"
      provides: "Kitchen page with new dashboard header and due-date order list"
      min_lines: 100
    - path: "src/components/kitchen/InventoryOverrideButton.tsx"
      provides: "Manager inventory override button with reason input"
      min_lines: 40
    - path: "docs/CHANGELOG.md"
      provides: "Updated changelog with Phase 15 entry"
      contains: "Phase 15"
  key_links:
    - from: "src/pages/KitchenViewV2.tsx"
      to: "src/components/kitchen/DashboardHeader.tsx"
      via: "Renders DashboardHeader with data from useKitchenProduction + getConfig"
      pattern: "DashboardHeader"
    - from: "src/pages/KitchenViewV2.tsx"
      to: "src/components/kitchen/DueDateOrderList.tsx"
      via: "Renders DueDateOrderList with data from getKitchenPackingOrders"
      pattern: "DueDateOrderList"
    - from: "src/pages/KitchenViewV2.tsx"
      to: "convex/kitchenConfig/queries.ts"
      via: "useQuery(api.kitchenConfig.queries.getConfig)"
      pattern: "getConfig"
---

<objective>
Wire all new kitchen components into KitchenViewV2, add inventory override for managers, update documentation, and perform visual verification of the complete kitchen overhaul.

Purpose: Integrate the dashboard header, due-date order list, and K3Mart card into the live kitchen page, completing the Phase 15 kitchen overhaul.
Output: Fully functional kitchen dashboard with production targets, grouped orders, and visual verification.
</objective>

## Git Workflow
**Branch:** `feature/kitchen-overhaul`
**Checkpoints:** Visual verification of dashboard header + order grouping + K3Mart card

## Implementation Waves
### Wave 1: Integration [SEQUENTIAL]
| Agent | Task | Files |
|-------|------|-------|
| react-ui-builder | Wire components into KitchenViewV2 + inventory override | src/pages/KitchenViewV2.tsx, src/hooks/convex/useKitchenProduction.ts, src/components/kitchen/InventoryOverrideButton.tsx |

### Wave 2: Documentation [SEQUENTIAL]
| Agent | Task | Files |
|-------|------|-------|
| code-auditor | CHANGELOG + build verification | docs/CHANGELOG.md |

### Wave 3: Verification [SEQUENTIAL]
| Agent | Task |
|-------|------|
| Bash | npm run build |
| User | Visual verification |

## Documentation Updates
- [ ] CHANGELOG.md (required)

## Success Criteria
- [ ] `npm run build` succeeds
- [ ] Dashboard header visible above batch panels
- [ ] Due-date grouped orders render correctly
- [ ] Kitchen actions work (complete, send back)
- [ ] User approves visual verification

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-kitchen-overhaul/15-CONTEXT.md
@.planning/phases/15-kitchen-overhaul/15-01-SUMMARY.md
@.planning/phases/15-kitchen-overhaul/15-02-SUMMARY.md
@.planning/phases/15-kitchen-overhaul/15-03-SUMMARY.md

@src/pages/KitchenViewV2.tsx
@src/hooks/convex/useKitchenProduction.ts
@src/components/kitchen/index.ts
@src/components/kitchen/SwipeableKitchenLayout.tsx
@src/components/kitchen/PackingPanel.tsx
@docs/CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire DashboardHeader + DueDateOrderList into KitchenViewV2 + InventoryOverrideButton</name>
  <files>
    src/pages/KitchenViewV2.tsx
    src/hooks/convex/useKitchenProduction.ts
    src/components/kitchen/InventoryOverrideButton.tsx
    src/components/kitchen/index.ts
  </files>
  <action>
    **IMPORTANT -- Preserve existing batch panels unchanged (per locked decision: "Batch production panel changes — existing panels stay as-is").**
    The existing `SwipeableKitchenLayout` with its 4 panels (ProductionLog, Boxing, Stickering, Packing) MUST remain unchanged. New components are layered above/alongside, NOT replacing them.

    **Update KitchenViewV2.tsx:**
    The page layout should become:
    ```
    [Page Header - existing sticky header]
    [DashboardHeader - new sticky header with production targets]
    [DueDateOrderList + existing SwipeableKitchenLayout - scrollable content area]
    ```

    1. **Import new components:**
       ```typescript
       import { DashboardHeader, TargetConfigPopover, DueDateOrderList } from '@/components/kitchen';
       import { useQuery } from 'convex/react';
       ```

    2. **Add kitchenConfig query:**
       ```typescript
       const kitchenConfig = useQuery(api.kitchenConfig.queries.getConfig);
       ```
       Place this BEFORE any conditional returns (React hooks order rule).

    3. **Add state for target config popover:**
       ```typescript
       const [configOpen, setConfigOpen] = useState(false);
       ```

    4. **Render DashboardHeader BETWEEN the existing page header and the SwipeableKitchenLayout:**
       - Use `sticky top-[56px] z-20` (or measure existing header height) per research pitfall #1 on sticky header stacking
       - Pass data from `useKitchenProduction` hook (which already provides kitchenStats):
         - `minTarget`: from extended `kitchenStats.minTarget` (Plan 01)
         - `minTargetOrderCount`: from `kitchenStats.minTargetOrderCount`
         - `maxTarget`: from `kitchenConfig` (defaults to 200/150/50)
         - `remainingBalls`: Calculate as `{ total: kitchenStats.bigBallsNeeded + kitchenStats.midBallsNeeded, big: kitchenStats.bigBallsNeeded, mid: kitchenStats.midBallsNeeded }`. Note: "remaining" means balls still needed for kitchen-visible orders.
         - `ordersLeft`: from `kitchenStats.ordersPending`
         - `hasOverdueOrders`: from `kitchenStats.hasOverdueOrders`
         - `onOpenConfig`: `() => setConfigOpen(true)`
         - `canConfigure`: `hasPermission('canEditKitchen')` (already computed as `canEditKitchen`)
         - `isLoading`: `kitchenStats === undefined`
       - Wrap gear icon with `<TargetConfigPopover>` when `configOpen`

    5. **Render DueDateOrderList:**
       Decide placement: The new due-date order list can be rendered as a **new tab/section** alongside the existing swipeable panels, or as content that appears when a "Kitchen Orders" view mode is selected. Per the phase boundary ("A new sticky summary header appears above the existing swipeable batch production panels"), the order list lives alongside/below the header.

       **Recommended approach:** Add the DueDateOrderList as scrollable content below the DashboardHeader. The existing SwipeableKitchenLayout remains where it is. Use a simple tab or toggle if the page gets too long, but the simplest approach is to render both:
       - DashboardHeader (sticky)
       - DueDateOrderList (scrollable, shows kitchen orders by due date)
       - SwipeableKitchenLayout (existing panels, below)

       Or use a view mode toggle (e.g., "Orders" | "Production") with the header always visible. Use your judgment based on viewport.

    6. **Wire mutation handlers:**
       - `onToggleItem`: Call existing `togglePackOrderLineItem` mutation (already available in KitchenViewV2)
       - `onCompleteOrder`: Call existing `markOrderReady` mutation
       - `onSendBack`: Call new `sendBackToOrderDesk` mutation from Plan 01
       - Add `const sendBack = useProtectedMutation(api.orders.mutations.kitchen.sendBackToOrderDesk);`

    7. **K3Mart data:**
       - Use existing `useQuery(api.k3martKitchen.queries.getK3MartKitchenSummary)` or equivalent
       - Transform into `K3MartSyntheticCardProps` format
       - Pass to DueDateOrderList as `k3martData`
       - If no K3Mart data, pass `null` (card won't render)

    **Create InventoryOverrideButton.tsx (KIT-08):**
    ```typescript
    interface InventoryOverrideButtonProps {
      itemName: string;
      isUnavailable: boolean;
      onOverride: (reason: string) => void;
      canOverride: boolean; // manager role required
    }
    ```
    - Renders an inline "Override" button next to unavailable packaging items in the kitchen order card
    - Only visible when `isUnavailable=true AND canOverride=true`
    - On click: shows inline shadcn `Input` for reason + "Confirm" button
    - On confirm: calls `onOverride(reason)` which the parent wires to a mutation that marks the item available with the override reason logged
    - Uses shadcn `Button variant="outline" size="sm"` + shadcn `Input` + `AlertDialog` for confirmation
    - Manager role check: `canOverride` prop is set from `hasPermission('canEditKitchen')` in the parent

    **Update useKitchenProduction.ts (if needed):**
    If the hook doesn't already include `getConfig`, add a `kitchenConfig` query to the hook's return value. Check the hook first -- if it only wraps `getKitchenStats` and `getKitchenPackingOrders`, consider adding `getConfig` there or keeping it as a separate `useQuery` in KitchenViewV2 (simpler).

    **Update kitchen/index.ts:**
    Export InventoryOverrideButton:
    ```typescript
    export { InventoryOverrideButton } from './InventoryOverrideButton';
    ```
  </action>
  <verify>
    `npm run build` passes. `grep "DashboardHeader\|DueDateOrderList" src/pages/KitchenViewV2.tsx` returns matches. `grep "sendBackToOrderDesk" src/pages/KitchenViewV2.tsx` returns a match.
  </verify>
  <done>
    KitchenViewV2 renders DashboardHeader sticky above content with real data from getKitchenStats + getConfig. DueDateOrderList shows grouped kitchen orders. Complete Order and Send Back actions wired. InventoryOverrideButton available for managers. Existing batch panels unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG.md update + final build verification</name>
  <files>
    docs/CHANGELOG.md
  </files>
  <action>
    **Update CHANGELOG.md:**
    Add Phase 15 entry at the top of the changelog:
    ```markdown
    ## Phase 15: Kitchen Overhaul (2026-02-XX)

    ### Added
    - Dashboard summary header with 4 stat cards: min target, max target, remaining balls, orders left
    - Manager-configurable max production target via gear icon popover (absolute big/mid ball numbers)
    - Due-date grouped kitchen orders: OVERDUE (red, pinned top), Due Today, Due Tomorrow, etc.
    - Per-item production checklist with single tick per product line
    - "Complete Order" button (active when all items ticked, moves to Awaiting Delivery)
    - "Send back to order desk" button with reason (resets packages, returns to Payment Received)
    - K3Mart synthetic demand card with visually distinct blue styling and outlet breakdown
    - Manager inline quantity editing on K3Mart demand cards
    - Color-coded urgency on remaining balls: green (on track), amber (behind), red (overdue exists)
    - Tap-to-expand Big Ball / Mid Ball breakdown on dashboard stats
    - Manager inventory override for unavailable items with reason logging
    - kitchenConfig schema table for persistent target configuration

    ### Changed
    - Kitchen page layout: sticky dashboard header above existing batch production panels
    - getKitchenStats now returns minTarget, minTargetOrderCount, hasOverdueOrders
    - EXPEDITED orders pinned to top of their due-date group with orange badge

    ### Dependencies
    - Added date-fns ^4.1.0 for date arithmetic (isToday, isTomorrow, format)
    ```

    **Final build verification:**
    Run `npm run build` to confirm zero errors after all integrations.
  </action>
  <verify>
    `npm run build` passes with zero errors. `grep "Phase 15" docs/CHANGELOG.md` returns a match.
  </verify>
  <done>
    CHANGELOG.md updated with complete Phase 15 changes. Build passes with zero errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of kitchen dashboard + order grouping + K3Mart card</name>
  <files>src/pages/KitchenViewV2.tsx</files>
  <action>
    Human verifies the complete Kitchen Overhaul visually:
    1. Dashboard header with 4 stat cards
    2. Due-date grouped orders
    3. Per-item checklists
    4. K3Mart synthetic card
    5. Existing batch panels still working
  </action>
  <verify>
    1. Navigate to /kitchen -- verify sticky dashboard header appears above batch panels
    2. Dashboard header shows 4 stats: Min Target (with order count), Max Target, Remaining Balls, Orders Left
    3. Tap "Remaining Balls" stat -- verify Big Ball / Mid Ball breakdown toggles
    4. Tap gear icon (as manager) -- verify TargetConfigPopover opens with current big/mid values
    5. Change ball targets and save -- verify toast confirms update, stats refresh
    6. Scroll down -- verify orders grouped by due-date headers ("OVERDUE", "Due Today", "Due Tomorrow", etc.)
    7. Verify OVERDUE section (if any) has red styling and is pinned at top
    8. Verify EXPEDITED orders have orange badge and appear at top of their group
    9. Tap a checklist item -- verify checkbox toggles (strikethrough on checked items)
    10. Tick all items on an order -- verify "Complete Order" button becomes active
    11. Tap "Complete Order" -- verify order disappears from kitchen (moved to Awaiting Delivery)
    12. Tap "Send back" on an order -- verify confirmation dialog with reason field
    13. Confirm send-back -- verify order disappears from kitchen (returned to Payment Received)
    14. Verify K3Mart synthetic card appears with blue styling (if K3Mart data exists)
    15. Verify existing batch panels (Production Log, Boxing, Stickering, Packing) still function normally
    16. On mobile: verify 2x2 stat grid, scrollable order list, and sticky header behavior
  </verify>
  <done>User approves complete Kitchen Overhaul visual verification</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- Dashboard header renders with real data
- Due-date grouping works correctly
- Kitchen actions (complete, send back) work
- K3Mart card renders when data exists
- Existing batch panels unchanged
- CHANGELOG.md updated
- User approves visual verification
</verification>

<success_criteria>
- Dashboard header sticky above batch panels with 4 stats
- Manager can configure targets via gear icon
- Orders grouped by due date with OVERDUE at top
- Per-item checklist toggles work
- Complete Order + Send Back actions work
- K3Mart card is visually distinct
- Inventory override available for managers
- Existing panels still work
- `npm run build` succeeds
- User approves
</success_criteria>

<output>
After completion, create `.planning/phases/15-kitchen-overhaul/15-04-SUMMARY.md`
</output>
