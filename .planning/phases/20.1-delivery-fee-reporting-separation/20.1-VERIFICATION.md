---
phase: 20.1-delivery-fee-reporting-separation
verified: 2026-02-22T16:30:00Z
status: passed
score: 6/6 must-haves verified
re_verification: false
---

# Phase 20.1: Delivery Fee Reporting Separation Verification Report

**Phase Goal:** Separate delivery fees from product revenue in Sales Analytics — show delivery fees as a new summary card and exclude them from Net Sales, with corrected discount calculation, all computed at query time using existing schema fields
**Verified:** 2026-02-22T16:30:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                         | Status     | Evidence                                                                                                        |
| --- | --------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------- |
| 1   | Sales Analytics Net Sales card excludes delivery fees from its total                          | VERIFIED | `totalNet = platformNet + internalNet`; `internalNet += netProduct` where `netProduct = (finalTotal ?? totalAmount) - deliveryFee` — `queries.ts` line 490–492 |
| 2   | New Delivery Fees card shows total pass-through delivery fees collected with Truck icon and muted styling | VERIFIED | `OverviewTab.tsx` lines 1159–1178: `<Truck className="h-4 w-4 text-muted-foreground" />`, value uses `text-2xl font-bold text-muted-foreground` |
| 3   | Discounts Given calculation is correct (gross minus net product revenue, unaffected by delivery fees) | VERIFIED | `totalDiscounts += order.totalAmount - netProduct` — `queries.ts` line 493. Card at `OverviewTab.tsx` line 1143 uses `currentPeriod.totalDiscounts` |
| 4   | Internal channel net in channel breakdown excludes delivery fees                              | VERIFIED | `channels.internal.net = internalNet` — `queries.ts` line 521; `internalNet` accumulates only `netProduct` values |
| 5   | Delivery Fees card always shows even when value is Rp 0                                       | VERIFIED | Card is unconditionally rendered (no conditional guard), `formatCurrency(currentPeriod.totalDeliveryFees ?? 0)` handles zero — `OverviewTab.tsx` lines 1159–1178 |
| 6   | Summary card grid displays 5 cards on large screens                                           | VERIFIED | `lg:grid-cols-5` at `OverviewTab.tsx` line 1060 (live grid) and line 997 (loading skeleton); skeleton array is `[1,2,3,4,5]` at line 998 |

**Score:** 6/6 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `convex/externalData/queries.ts` | `totalDeliveryFees` accumulator in `aggregate()` and corrected `internalNet` formula | VERIFIED | Line 474: `let totalDeliveryFees = 0;` declared. Line 489–494: delivery fee separated from `netProduct`. Line 514: `totalDeliveryFees` in return object. |
| `src/hooks/convex/useExternalData.ts` | `totalDeliveryFees: number` in `PeriodSummary` type | VERIFIED | Line 21: `totalDeliveryFees: number;` present in `PeriodSummary` type definition. |
| `src/components/salesAnalytics/OverviewTab.tsx` | Delivery Fees card with Truck icon, muted styling, 5-column grid | VERIFIED | Line 21: `Truck` imported. Line 411: `totalDeliveryFees?: number` in `PeriodData`. Line 997/1060: `lg:grid-cols-5`. Lines 1159–1178: complete Delivery Fees card. |

---

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `convex/externalData/queries.ts` | `src/hooks/convex/useExternalData.ts` | `PeriodSummary` type must include `totalDeliveryFees` to match backend return shape | WIRED | `PeriodSummary` type at line 13–25 of `useExternalData.ts` includes `totalDeliveryFees: number` at line 21. Backend returns it at `queries.ts` line 514. Type check passes with no errors. |
| `src/hooks/convex/useExternalData.ts` | `src/components/salesAnalytics/OverviewTab.tsx` | `summary.currentPeriod.totalDeliveryFees` consumed by Delivery Fees card | WIRED | `OverviewTab.tsx` line 1167: `formatCurrency(currentPeriod.totalDeliveryFees ?? 0)`. Lines 1170–1171: `GrowthIndicator` also consumes both current and previous values. |

---

### Requirements Coverage

No formal requirement IDs were declared for this phase (decimal insertion phase). All success criteria from the PLAN frontmatter have been verified above.

---

### Anti-Patterns Found

None. No TODO/FIXME/placeholder comments found in the modified files. No stub implementations. All state is rendered.

---

### Human Verification Required

#### 1. Net Sales value accuracy with real orders containing delivery fees

**Test:** In a period with orders that include non-zero `deliveryFee` values, compare the Net Sales card total to a manual sum of `(finalTotal - deliveryFee)` across those orders.
**Expected:** Net Sales should be lower than before the fix by exactly the sum of all delivery fees.
**Why human:** Requires production data or seeded orders with known delivery fees; cannot verify arithmetic correctness programmatically without running Convex.

#### 2. Delivery Fees card visible on small/medium screens

**Test:** View Sales Analytics on a tablet (md breakpoint) and a mobile (sm breakpoint).
**Expected:** The card grid collapses gracefully to 2 columns (`md:grid-cols-2`) with all 5 cards wrapping correctly. No card is truncated or hidden.
**Why human:** CSS layout at breakpoints requires visual inspection.

---

### Gaps Summary

No gaps. All 6 observable truths verified. All 3 required artifacts exist, are substantive, and are wired. Both key links confirmed present. TypeScript type check passes (`npm run type-check` exits 0). Both implementation commits (`8bb6f20`, `c6ef0b1`) confirmed in git log.

---

_Verified: 2026-02-22T16:30:00Z_
_Verifier: Claude (gsd-verifier)_
