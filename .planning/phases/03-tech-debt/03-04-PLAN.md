---
phase: 03-tech-debt
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - convex/orders/mutations.ts
  - convex/_generated/api.d.ts
  - src/hooks/convex/useOrders.ts
  - src/pages/KitchenViewV2.tsx
  - src/pages/PackagingView.tsx
  - src/hooks/convex/useKitchenStats.ts
  - tests/convex/helpers.ts
  - tests/convex/ballDistribution.test.ts
  - tests/convex/orderLifecycle.test.ts
  - tests/convex/orders.test.ts
  - src/hooks/__tests__/useConvexHooks.test.tsx
autonomous: true

must_haves:
  truths:
    - "convex/orders/mutations.ts shim file is deleted from the codebase"
    - "All frontend, hook, and test imports reference the correct API path for order mutations"
    - "npm run build passes after the shim is removed and all callers updated"
    - "npm run test passes -- no test failures from import path changes"
  artifacts:
    - path: "convex/orders/mutations/index.ts"
      provides: "Barrel export file that is now the sole source of order mutation exports"
    - path: "src/hooks/convex/useOrders.ts"
      provides: "Updated imports using correct API path for order mutations"
  key_links:
    - from: "src/hooks/convex/useOrders.ts"
      to: "convex/orders/mutations/index.ts"
      via: "api.orders.mutations.* path in useMutation calls"
      pattern: "api\\.orders\\.mutations"
    - from: "tests/convex/orderLifecycle.test.ts"
      to: "convex/orders/mutations/index.ts"
      via: "api.orders.mutations.* path in test assertions"
      pattern: "api\\.orders\\.mutations"
---

<objective>
Remove the deprecated `convex/orders/mutations.ts` shim file and update all callers to use the correct API path in a single clean break (QFIX-03).

Purpose: Eliminate the confusing indirection layer that re-exports all order mutations through a shim file. Per user decision: single commit, clean break, full audit of frontend AND backend.
Output: Shim file deleted, all callers updated, build and tests pass.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tech-debt/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all callers, delete shim, update all callers in one shot</name>
  <files>
    convex/orders/mutations.ts
    convex/_generated/api.d.ts
    src/hooks/convex/useOrders.ts
    src/pages/KitchenViewV2.tsx
    src/pages/PackagingView.tsx
    src/hooks/convex/useKitchenStats.ts
    tests/convex/helpers.ts
    tests/convex/ballDistribution.test.ts
    tests/convex/orderLifecycle.test.ts
    tests/convex/orders.test.ts
    src/hooks/__tests__/useConvexHooks.test.tsx
  </files>
  <action>
Per user decision: "Remove convex/orders/mutations.ts shim and update all imports in one shot (single commit, clean break)" with "full audit of both frontend AND backend files."

**Step 1: Pre-deletion caller audit (comprehensive).**

Run these greps to capture the COMPLETE caller list before making any changes:

```bash
# All references to api.orders.mutations in source and tests
grep -rn "api\.orders\.mutations" src/ tests/ convex/ --include="*.ts" --include="*.tsx"

# All direct imports from the shim path
grep -rn "from.*orders/mutations" src/ tests/ convex/ --include="*.ts" --include="*.tsx"

# String literal references (test mocks, etc.)
grep -rn "orders\.mutations\." src/ tests/ --include="*.ts" --include="*.tsx"
```

Known callers from research (verify each exists and note exact reference count):

Frontend files:
- `src/hooks/convex/useOrders.ts` (~12 references)
- `src/pages/KitchenViewV2.tsx` (~5 references)
- `src/pages/PackagingView.tsx` (~1 reference)
- `src/hooks/convex/useKitchenStats.ts` (~5 references)

Test files:
- `tests/convex/helpers.ts` (~2 references)
- `tests/convex/ballDistribution.test.ts` (~30+ references)
- `tests/convex/orderLifecycle.test.ts` (~50+ references)
- `tests/convex/orders.test.ts` (~25+ references)
- `src/hooks/__tests__/useConvexHooks.test.tsx` (~10 string literal references like `'orders.mutations.create'`)

Backend files (check for any internal convex/ references beyond the shim itself and _generated/):
- `convex/orders/mutations.ts` (the shim -- will be deleted)
- `convex/_generated/api.d.ts` (auto-generated -- will regenerate)

If the audit reveals ANY callers not listed above, add them to the update list.

**Step 2: Record current API path structure.**

Read `convex/_generated/api.d.ts` and note:
- Current path for shim: `api.orders.mutations.X`
- Current path for directory: `api.orders.mutations.index.X` (if it exists)

**Step 3: Delete the shim file.**

Delete `convex/orders/mutations.ts`.

**Step 4: Determine new API path.**

After deletion, check how Convex resolves the directory's index.ts. Two outcomes:

**Outcome A (trivial):** `api.orders.mutations.X` auto-resolves via the directory index. No caller changes needed beyond deletion.

**Outcome B (migration needed):** `api.orders.mutations.X` no longer resolves. Update ALL callers found in Step 1. The new path will be one of:
- `api.orders.mutations.index.X` (if Convex generates this for `convex/orders/mutations/index.ts`)
- Or domain-specific paths like `api.orders.mutations.orderCrud.create`

**Step 5: Update ALL callers (if Outcome B).**

Update every caller found in the audit. Do NOT leave any file unreferenced.

Mutation name to source file mapping (for domain-specific paths if needed):
- `create`, `cancel`, `remove`, `completeOrder`, `revertToConfirmed`, `updateOrderDiscount`, `completeBalls` -> `orderCrud`
- `addItem`, `removeItem`, `updateItemQuantity`, `replaceItems` -> `itemCrud`
- `updateStatus`, `updatePayment`, `updateShipping`, `updateDetails` -> `statusUpdates`
- `addBallsToTray`, `fillPendingOrders`, `removeBallFromTray`, `boxProducts`, `stickerProducts`, `togglePackOrderLineItem`, `markOrderReady` -> `kitchen`
- `markPackagePacked`, `completePackaging`, `revertToPackaging`, `markAllItemPackagesPacked`, `unmarkPackagePacked`, `fillPackage`, `unfillPackage` -> `packaging`
- `backfillOrderItemProduction`, `migrateChannelCodes`, `backfillProductionRecords`, `migratePackagingToBoxed` -> `migrations`
- `reserveStockForOrder`, `consumeBoxingMaterials`, `consumeStickerMaterials`, `releaseReservation` -> `inventoryIntegration`

**Step 6: Verify completeness.**

After all updates:
```bash
# No remaining references to the deleted shim file
grep -r "from.*['\"].*orders/mutations['\"]" convex/ --include="*.ts"

# Build and test pass
npm run type-check
npm run build
npm run test
```

**IMPORTANT:** This is a single-commit clean break per user decision. Do NOT leave a transitional compatibility layer. All callers updated in one pass.
  </action>
  <verify>
Run: `npm run type-check` -- must pass with zero errors.
Run: `npm run build` -- must pass.
Run: `npm run test` -- must pass (all test files updated).
Run: `ls convex/orders/mutations.ts` -- should return "no such file".
Run: `grep -r "from.*['\"].*orders/mutations['\"]" convex/` -- should return zero results (no leftover import of the deleted shim).
  </verify>
  <done>Shim file deleted. Full caller audit completed (frontend + backend + tests). All callers updated to use correct API path. Build passes. Tests pass. Single clean-break commit per user decision.</done>
</task>

</tasks>

<verification>
1. `convex/orders/mutations.ts` does not exist
2. `npm run type-check` passes
3. `npm run build` passes
4. `npm run test` passes
5. `grep -r "orders/mutations['\"]" convex/ --include="*.ts"` returns zero results (no references to deleted file)
6. All order mutation calls in frontend and tests use the correct API path
</verification>

<success_criteria>
- Shim file deleted, no backward compatibility layer remains
- Full caller audit completed across src/, tests/, AND convex/ directories
- Every caller updated in one clean break
- `npm run build` and `npm run test` both pass
- No runtime regression -- all order mutations still work via the new paths
</success_criteria>

<output>
After completion, create `.planning/phases/03-tech-debt/03-04-SUMMARY.md`
</output>
