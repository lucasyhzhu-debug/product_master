---
phase: 03-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/LocationsManager.tsx
  - src/components/inventory/ComponentTypeDialog.tsx
  - src/components/inventory/ReceiveStockDialog.tsx
  - src/components/inventory/AdjustStockDialog.tsx
  - src/components/inventory/TransferStockDialog.tsx
  - src/pages/KitchenView.tsx
  - src/components/orders/BallCompletionButtons.tsx
  - src/components/orders/SoundToggle.tsx
  - src/components/orders/KitchenDashboard.tsx
  - src/components/orders/KitchenHelpPanel.tsx
  - src/components/orders/InventoryTray.tsx
  - src/components/orders/OrderBox.tsx
  - src/components/orders/FlyingBall.tsx
  - src/components/orders/ProductPackage.tsx
  - src/components/orders/KitchenOrderCard.tsx
  - src/lib/kitchenSounds.ts
  - src/hooks/convex/usePendingBallStats.ts
  - src/pages/index.ts
  - src/components/orders/index.ts
  - src/hooks/convex/index.ts
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "No inventory mutation uses the hardcoded string 'current-user' -- all use the authenticated user's name"
    - "KitchenView.tsx and all V1-only orphaned components are deleted from the codebase"
    - "The /kitchen route still works and renders KitchenViewV2"
    - "No build errors from orphaned imports after file deletions"
  artifacts:
    - path: "src/pages/LocationsManager.tsx"
      provides: "Authenticated username in createLocation call"
      contains: "user?.name"
    - path: "src/components/inventory/ReceiveStockDialog.tsx"
      provides: "Authenticated username in receiveBatch and createComponentAndReceive calls"
      contains: "user?.name"
    - path: "src/components/orders/index.ts"
      provides: "Barrel exports without V1-only components"
  key_links:
    - from: "src/pages/LocationsManager.tsx"
      to: "src/contexts/AuthContext.tsx"
      via: "useAuth() hook import"
      pattern: "useAuth"
    - from: "src/App.tsx"
      to: "src/pages/KitchenViewV2.tsx"
      via: "/kitchen route"
      pattern: "KitchenViewV2"
---

<objective>
Replace hardcoded "current-user" strings with authenticated usernames (QFIX-01) and remove the deprecated KitchenView V1 page with all its orphaned components (QFIX-02).

Purpose: Fix broken audit trail in inventory mutations and eliminate dead code from the V1 kitchen system that was fully replaced by V2.
Output: 5 files fixed for auth, 12 files deleted, 4 barrel exports updated, build passes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tech-debt/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded "current-user" with authenticated username (QFIX-01)</name>
  <files>
    src/pages/LocationsManager.tsx
    src/components/inventory/ComponentTypeDialog.tsx
    src/components/inventory/ReceiveStockDialog.tsx
    src/components/inventory/AdjustStockDialog.tsx
    src/components/inventory/TransferStockDialog.tsx
  </files>
  <action>
In each of the 5 files, replace the hardcoded `"current-user"` string with the authenticated user's name from AuthContext.

For each file:
1. Check if `useAuth` is already imported. If not, add `import { useAuth } from '@/contexts/AuthContext';`
2. Add `const { user } = useAuth();` at the TOP of the component function, BEFORE any conditional returns (React hooks order rule -- Pitfall 4 from research)
3. Replace every occurrence of `createdBy: "current-user"` with `createdBy: user?.name ?? "unknown"`

Specific locations (6 occurrences in 5 files):
- `src/pages/LocationsManager.tsx:121` -- 1 occurrence in `createLocation()`
- `src/components/inventory/ComponentTypeDialog.tsx:91` -- 1 occurrence in `createComponentType()`
- `src/components/inventory/ReceiveStockDialog.tsx:194` -- 1 occurrence in `receiveBatch()`
- `src/components/inventory/ReceiveStockDialog.tsx:242` -- 1 occurrence in `createComponentAndReceive()`
- `src/components/inventory/AdjustStockDialog.tsx:158` -- 1 occurrence in `adjustStock()`
- `src/components/inventory/TransferStockDialog.tsx:92` -- 1 occurrence in `transferStock()`

After changes, grep the entire `src/` directory for `"current-user"` to confirm zero remaining occurrences.
  </action>
  <verify>
Run: `grep -r "current-user" src/` -- should return zero results.
Run: `npm run type-check` -- should pass with no errors.
  </verify>
  <done>All 6 occurrences of "current-user" replaced with `user?.name ?? "unknown"` from useAuth(). No remaining hardcoded username strings in src/.</done>
</task>

<task type="auto">
  <name>Task 2: Delete KitchenView V1 and all orphaned components (QFIX-02)</name>
  <files>
    src/pages/KitchenView.tsx
    src/components/orders/BallCompletionButtons.tsx
    src/components/orders/SoundToggle.tsx
    src/components/orders/KitchenDashboard.tsx
    src/components/orders/KitchenHelpPanel.tsx
    src/components/orders/InventoryTray.tsx
    src/components/orders/OrderBox.tsx
    src/components/orders/FlyingBall.tsx
    src/components/orders/ProductPackage.tsx
    src/components/orders/KitchenOrderCard.tsx
    src/lib/kitchenSounds.ts
    src/hooks/convex/usePendingBallStats.ts
    src/pages/index.ts
    src/components/orders/index.ts
    src/hooks/convex/index.ts
    src/App.tsx
  </files>
  <action>
**Step 1: Delete 12 files** (V1 page + orphaned components + orphaned utilities):
- `src/pages/KitchenView.tsx`
- `src/components/orders/BallCompletionButtons.tsx`
- `src/components/orders/SoundToggle.tsx`
- `src/components/orders/KitchenDashboard.tsx`
- `src/components/orders/KitchenHelpPanel.tsx`
- `src/components/orders/InventoryTray.tsx`
- `src/components/orders/OrderBox.tsx`
- `src/components/orders/FlyingBall.tsx`
- `src/components/orders/ProductPackage.tsx`
- `src/components/orders/KitchenOrderCard.tsx`
- `src/lib/kitchenSounds.ts`
- `src/hooks/convex/usePendingBallStats.ts`

**Step 2: Update barrel exports (4 files):**

`src/pages/index.ts` -- Remove the `KitchenView` export line (keep all other exports).

`src/components/orders/index.ts` -- Remove export lines for ALL deleted components: `BallCompletionButtons`, `SoundToggle`, `KitchenDashboard`, `KitchenHelpPanel`, `InventoryTray`, `OrderBox`, `FlyingBall`, `ProductPackage`, `KitchenOrderCard`. Keep all other exports (especially `ChannelBadge` which is used by OrderForm.tsx).

`src/hooks/convex/index.ts` -- Remove the `usePendingBallStats` export line.

`src/App.tsx` -- Remove the `/kitchen-legacy` redirect route (line ~66). The `/kitchen` route already points to `KitchenViewV2` and must NOT be changed. Do NOT remove the KitchenViewV2 import.

**Step 3: Verify no dangling references:**
After deletions, grep for each deleted filename across `src/` to confirm no remaining imports reference them. Specifically check:
- `KitchenView` (but NOT `KitchenViewV2`)
- `BallCompletionButtons`
- `SoundToggle`
- `KitchenDashboard`
- `KitchenHelpPanel`
- `InventoryTray`
- `OrderBox`
- `FlyingBall`
- `ProductPackage`
- `KitchenOrderCard`
- `kitchenSounds`
- `usePendingBallStats`

Note: Do NOT delete `canvas-confetti` from `package.json` -- it may have other uses or can be cleaned up separately.
  </action>
  <verify>
Run: `npm run build` -- must pass with no module-not-found errors.
Run: `grep -r "KitchenView[^V]" src/` -- should return zero results (KitchenViewV2 references are fine).
Verify `/kitchen` route still exists in App.tsx pointing to KitchenViewV2.
  </verify>
  <done>12 V1-only files deleted, 4 barrel exports cleaned, /kitchen route unchanged (still points to KitchenViewV2), /kitchen-legacy redirect removed, build passes.</done>
</task>

</tasks>

<verification>
1. `grep -r "current-user" src/` returns zero results
2. `ls src/pages/KitchenView.tsx` returns "no such file"
3. `npm run type-check` passes
4. `npm run build` passes
5. `/kitchen` route in App.tsx points to KitchenViewV2
</verification>

<success_criteria>
- Zero occurrences of `"current-user"` in any source file
- KitchenView.tsx and 11 orphaned files are deleted
- All barrel exports updated (no dangling imports)
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-tech-debt/03-01-SUMMARY.md`
</output>
