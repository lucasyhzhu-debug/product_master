---
phase: 38-frontend-giant-file-splits
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dateUtils.ts
  - src/components/salesAnalytics/overviewUtils.ts
  - src/components/salesAnalytics/GrowthIndicator.tsx
  - src/components/salesAnalytics/ConfidenceBadge.tsx
  - src/components/salesAnalytics/MatchStatusBadge.tsx
  - src/components/salesAnalytics/PlatformBadge.tsx
  - src/components/salesAnalytics/RevenueItemDetails.tsx
  - src/components/salesAnalytics/InternalOrderDetails.tsx
  - src/components/salesAnalytics/StoreGroupHeader.tsx
  - src/components/salesAnalytics/ChannelSummary.tsx
  - src/components/salesAnalytics/PlatformHierarchy.tsx
  - src/components/salesAnalytics/LifetimeHero.tsx
  - src/components/salesAnalytics/RevenueTable.tsx
  - src/components/salesAnalytics/HeroCards.tsx
  - src/components/salesAnalytics/OverviewTab.tsx
  - src/components/salesAnalytics/index.ts
autonomous: true
requirements: [FFS-01]

must_haves:
  truths:
    - "OverviewTab.tsx is under 400 LOC after extraction"
    - "All extracted sub-components render identically to the original inline versions"
    - "WIB helpers are consolidated in src/lib/dateUtils.ts (no duplicates in OverviewTab)"
    - "npm run build passes with zero type errors"
  artifacts:
    - path: "src/lib/dateUtils.ts"
      provides: "Shared WIB timezone helpers"
      exports: ["WIB_OFFSET_MS", "utcToWibDateStr", "wibDateStrToUtcMs", "utcToWibTimeStr", "formatDateTimeId", "formatDateId"]
    - path: "src/components/salesAnalytics/overviewUtils.ts"
      provides: "Types, constants, and period presets for OverviewTab"
      contains: "PERIOD_PRESETS"
    - path: "src/components/salesAnalytics/HeroCards.tsx"
      provides: "Hero stat cards grid (Gross Sales, Net Sales, Commissions, Discounts, Delivery Fees)"
      exports: ["HeroCards"]
    - path: "src/components/salesAnalytics/ChannelSummary.tsx"
      provides: "Channel breakdown tree"
      exports: ["ChannelSummary"]
    - path: "src/components/salesAnalytics/RevenueTable.tsx"
      provides: "Revenue details table"
      exports: ["RevenueTable"]
    - path: "src/components/salesAnalytics/OverviewTab.tsx"
      provides: "Slim orchestrator importing extracted sub-components"
      exports: ["OverviewTab"]
      max_lines: 400
  key_links:
    - from: "src/components/salesAnalytics/OverviewTab.tsx"
      to: "src/components/salesAnalytics/HeroCards.tsx"
      via: "import { HeroCards }"
      pattern: "import.*HeroCards"
    - from: "src/components/salesAnalytics/OverviewTab.tsx"
      to: "src/components/salesAnalytics/ChannelSummary.tsx"
      via: "import { ChannelSummary }"
      pattern: "import.*ChannelSummary"
    - from: "src/components/salesAnalytics/RevenueTable.tsx"
      to: "src/lib/dateUtils.ts"
      via: "import { utcToWibDateStr, wibDateStrToUtcMs }"
      pattern: "import.*dateUtils"
---

<objective>
Split OverviewTab.tsx (1,273 LOC) into focused sub-components, achieving under 400 LOC in the main file.

Purpose: OverviewTab is the largest frontend component in the salesAnalytics directory. Extracting its 7 inline helper components, 3 major sections, and the hero cards grid into standalone files makes each piece independently readable, testable, and maintainable.

Output: 14 new files (dateUtils.ts, overviewUtils.ts, 12 component files) + slimmed OverviewTab.tsx (~280-320 LOC)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/salesAnalytics/OverviewTab.tsx
@src/components/salesAnalytics/index.ts
@src/lib/utils.ts
@src/lib/formatters.ts
@src/lib/financialHelpers.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/utils.ts:
```typescript
export function formatCurrency(amount: number | null | undefined): string;
export function cn(...inputs: ClassValue[]): string;
```

From src/lib/formatters.ts:
```typescript
export function formatRelativeTime(timestamp: number): string;
```

From src/hooks/convex/index.ts (used by OverviewTab):
```typescript
export type PeriodPreset = "past24hours" | "today" | "yesterday" | "thisWeek" | "last7days" | "last30days" | "thisMonth" | "allTime";
```

From src/components/salesAnalytics/index.ts (current barrel):
```typescript
export { OverviewTab } from "./OverviewTab";
export { SalesChart } from "./SalesChart";
export { SettingsTab } from "./SettingsTab";
// ... other existing exports
```
</interfaces>
</context>

## Git Workflow
**Branch:** `gsd/phase-38-frontend-giant-file-splits`
**Checkpoints:** None (autonomous)

## Implementation Waves
### Wave 1: Shared Utils + Extractions [SEQUENTIAL]
| Agent | Task | Files |
|-------|------|-------|
| executor | Create dateUtils.ts, overviewUtils.ts, extract all sub-components, slim OverviewTab | All files_modified |

### Wave 2: Verification [SEQUENTIAL]
| Agent | Task |
|-------|------|
| Bash | `wc -l src/components/salesAnalytics/OverviewTab.tsx` (must be <400) |
| Bash | `npm run type-check` |
| Bash | `npm run build` |

## Documentation Updates
- [ ] CHANGELOG.md (LOC numbers TBD after `wc -l`)

## Success Criteria
- [ ] `wc -l src/components/salesAnalytics/OverviewTab.tsx` < 400
- [ ] `npm run type-check` passes
- [ ] `npm run build` succeeds
- [ ] No WIB helpers remain in OverviewTab.tsx (moved to dateUtils.ts)

<tasks>

<task type="auto">
  <name>Task 1: Create shared dateUtils.ts and extract all OverviewTab sub-components</name>
  <files>
    src/lib/dateUtils.ts,
    src/components/salesAnalytics/overviewUtils.ts,
    src/components/salesAnalytics/GrowthIndicator.tsx,
    src/components/salesAnalytics/ConfidenceBadge.tsx,
    src/components/salesAnalytics/MatchStatusBadge.tsx,
    src/components/salesAnalytics/PlatformBadge.tsx,
    src/components/salesAnalytics/RevenueItemDetails.tsx,
    src/components/salesAnalytics/InternalOrderDetails.tsx,
    src/components/salesAnalytics/StoreGroupHeader.tsx,
    src/components/salesAnalytics/ChannelSummary.tsx,
    src/components/salesAnalytics/PlatformHierarchy.tsx,
    src/components/salesAnalytics/LifetimeHero.tsx,
    src/components/salesAnalytics/RevenueTable.tsx,
    src/components/salesAnalytics/HeroCards.tsx,
    src/components/salesAnalytics/OverviewTab.tsx,
    src/components/salesAnalytics/index.ts
  </files>
  <action>
**Step 1: Create `src/lib/dateUtils.ts`** (NEW file, ~30 LOC)

Extract the 3 WIB helpers from OverviewTab.tsx (lines 60-70) into a shared module:
- `WIB_OFFSET_MS` constant (line 60)
- `utcToWibDateStr()` function (lines 62-65)
- `wibDateStrToUtcMs()` function (lines 67-70)

Also add the following utility functions from the research design (these are used by GrabFoodManager and will be consumed by plans 02-04):
- `utcToWibTimeStr()` — format WIB time HH:MM from UTC ms
- `formatDateTimeId()` — Indonesian locale datetime string (matches GrabFoodManager's formatDateTime but takes a number)
- `formatDateId()` — Indonesian locale date string

Do NOT duplicate `formatRelativeTime` — it already exists in `src/lib/formatters.ts`.

**Step 2: Create `src/components/salesAnalytics/overviewUtils.ts`** (NEW file, ~40 LOC)

Extract from OverviewTab.tsx:
- `ConfidenceLevel` type (line 44)
- `MatchConfidence` type (line 45)
- `PERIOD_PRESETS` array (lines 47-56)
- `DEFAULT_PERIOD` constant (line 58)
- `PERIOD_STORAGE_KEY` constant (line 59)
- `SOURCE_DISPLAY_NAMES` record (lines 155-160, currently near PlatformBadge)
- `RevenueRecord` type (lines 346-370, currently near StoreGroupHeader)
- `PeriodData` type — extract the inline type used by ChannelSummary (lines 401-420, the props interface fields). This is the summary data shape returned by `useDashboardSalesSummaryByPeriod`.

All as named exports.

**Step 3: Extract 7 small helper components** (each as a separate .tsx file with named export)

Each component gets its own file in `src/components/salesAnalytics/`:

1. **GrowthIndicator.tsx** (~28 LOC) — Lines 74-101. Props: `{ current: number; previous: number; invertColor?: boolean }`. Import `ArrowUpRight, ArrowDownRight, Minus` from lucide-react.

2. **ConfidenceBadge.tsx** (~20 LOC) — Lines 103-122. Props: `{ level: ConfidenceLevel }`. Import `ConfidenceLevel` from `./overviewUtils`.

3. **MatchStatusBadge.tsx** (~30 LOC) — Lines 124-153. Props: `{ confidence: MatchConfidence }`. Import `MatchConfidence` from `./overviewUtils`.

4. **PlatformBadge.tsx** (~10 LOC) — Lines 166-174. Props: `{ source: string }`. Import `SOURCE_DISPLAY_NAMES` from `./overviewUtils`. Also import `Badge` from ui. The `SOURCE_DISPLAY_NAMES` map was extracted in step 2.

5. **RevenueItemDetails.tsx** (~69 LOC) — Lines 178-246 (renamed from ExpandedRevenueItems per CONTEXT.md). This component has its OWN hook (`useRevenueItems`) — keep the hook inside. Props: `{ revenueId: Id<"externalRevenue"> }`. Import `MatchStatusBadge`, `PlatformBadge` as siblings.

6. **InternalOrderDetails.tsx** (~95 LOC) — Lines 248-342 (renamed from ExpandedInternalOrder per CONTEXT.md). This component has its OWN hook (`useOrderDetailsByOrderNumber`) — keep the hook inside. Props: `{ orderNumber: string }`.

7. **StoreGroupHeader.tsx** (~35 LOC) — Lines 375-397. Props: `{ outlet: { name: string; storeId: string; status: string }; expandedStores: Set<string>; toggleStore: (id: string) => void }`. Import `ChevronDown, ChevronRight` from lucide-react.

**Step 4: Extract 3 major section components**

8. **ChannelSummary.tsx** (~167 LOC) — Lines 401-567. Props interface with currentPeriod, previousPeriod (use PeriodData type from overviewUtils). Imports `GrowthIndicator`, `getPlatformPalette` from `@/lib/platformColors`, `Card`, `Badge`, `formatCurrency`.

9. **PlatformHierarchy.tsx** (~101 LOC) — Lines 571-671. This component has its OWN hook (`useRevenueByOutlet`) — keep the hook inside. Props: `{ selectedPeriod: PeriodPreset; dateFrom?: string; dateTo?: string }`.

10. **LifetimeHero.tsx** (~44 LOC) — Lines 675-718. This component has its OWN hook (`useLifetimeTotals`) — keep the hook inside. No props needed (or minimal).

11. **RevenueTable.tsx** (~182 LOC) — Lines 722-903. Props: the revenue data array + period info. Uses `utcToWibDateStr` and `wibDateStrToUtcMs` from `@/lib/dateUtils` (not local helpers). Uses `RevenueItemDetails`, `InternalOrderDetails`, `StoreGroupHeader` as siblings.

**Step 5: Extract HeroCards component**

12. **HeroCards.tsx** (~120 LOC) — Lines 1066-1185. This is the 5-card grid (Gross Sales, Net Sales, Commissions Paid, Discounts Given, Delivery Fees). Props: `{ currentPeriod: PeriodData; previousPeriod: PeriodData }`. Imports `GrowthIndicator`, `Card`, `CardHeader`, `CardTitle`, `CardContent`, `DollarSign`, `TrendingUp`, `Percent`, `TagIcon`, `Truck` from lucide-react, `formatCurrency`.

**Step 6: Slim OverviewTab.tsx**

Remove all extracted code. The remaining OverviewTab.tsx should contain ONLY:
- Import statements for all extracted components, hooks, and ui primitives
- The `export function OverviewTab()` component body:
  - Hook calls (useState, useSearchParams, useNavigate, data hooks)
  - Period management logic (setPeriod, date range handling)
  - Refresh handler
  - Loading skeleton
  - JSX that composes: period selector bar, HeroCards, ChannelSummary, PlatformHierarchy, LifetimeHero, SalesChart, RevenueTable

Remove the `WIB_OFFSET_MS`, `utcToWibDateStr`, `wibDateStrToUtcMs` local functions (now in dateUtils.ts).
Remove all inline component definitions (now in separate files).
Remove types/constants (now in overviewUtils.ts).

**Step 7: Update barrel `src/components/salesAnalytics/index.ts`**

Keep existing exports. OverviewTab is already exported. Do NOT add the internal sub-components (GrowthIndicator, ConfidenceBadge, etc.) to the barrel — they are internal to the salesAnalytics directory.

**LOC math verification:**
- Starting: 1,273 LOC
- Extracting: types/constants (~35) + WIB helpers (~11) + 7 small components (~287) + ChannelSummary (~167) + PlatformHierarchy (~101) + LifetimeHero (~44) + RevenueTable (~182) + HeroCards (~120) = ~947 LOC extracted
- Remaining: 1,273 - 947 = ~326 LOC
- Add back: ~15 new import lines = ~341 LOC
- Remove: ~20 LOC of now-unused original imports = ~321 LOC
- **Expected final: ~320 LOC (well under 400 target)**
  </action>
  <verify>
    <automated>cd "D:\Claude\Product Manager\product_master" && wc -l src/components/salesAnalytics/OverviewTab.tsx && npm run type-check && npm run build</automated>
  </verify>
  <done>
    - OverviewTab.tsx is under 400 LOC
    - All 14 new files exist with named exports
    - src/lib/dateUtils.ts is the single WIB helper source
    - npm run build passes with zero errors
    - Barrel index.ts unchanged except existing OverviewTab re-export still works
  </done>
</task>

</tasks>

<verification>
1. `wc -l src/components/salesAnalytics/OverviewTab.tsx` — must show < 400
2. `npm run type-check` — zero TypeScript errors
3. `npm run build` — successful build
4. `grep -r "WIB_OFFSET_MS" src/components/salesAnalytics/OverviewTab.tsx` — should return empty (moved to dateUtils.ts)
5. `grep -c "export function\|export const\|export type\|export interface" src/lib/dateUtils.ts` — at least 5 exports
</verification>

<success_criteria>
- OverviewTab.tsx under 400 LOC (target ~320)
- All sub-components extracted to individual files
- WIB helpers consolidated in src/lib/dateUtils.ts
- No type errors, build passes
- Barrel index.ts still exports OverviewTab
</success_criteria>

<output>
After completion, create `.planning/phases/38-frontend-giant-file-splits/38-01-SUMMARY.md`
</output>
