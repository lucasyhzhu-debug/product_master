---
phase: 27.2-grabfood-menu-simulator
verified: 2026-02-28T00:00:00Z
status: passed
score: 16/16 must-haves verified
re_verification: false
gaps: []
human_verification:
  - test: "Navigate to /grabfood-menu, click a product name to inline-edit, change value and click away"
    expected: "Field saves via onBlur, updated name appears in card"
    why_human: "onBlur commit pattern requires live user interaction to confirm DOM focus/blur events fire correctly"
  - test: "Toggle availability switch on a card"
    expected: "Card grays out with semi-transparent overlay; toggle persists on reload"
    why_human: "Visual overlay and reactive state update require visual inspection"
  - test: "Upload a photo via the camera area on a card"
    expected: "Photo appears in card; menuProduct also gains photoStorageId in DB"
    why_human: "Convex file storage upload requires a live environment to test the full POST flow"
  - test: "Click Push to GrabFood after making changes; review diff dialog; confirm push"
    expected: "Dialog shows correct diff sections (price changes, availability changes, new items); push succeeds; diff clears after push"
    why_human: "Real GrabFood API call and persistent diff clearing require a live environment"
  - test: "Verify dark mode throughout the page and cards"
    expected: "All elements use CSS variable tokens; no hardcoded light-mode colors remain"
    why_human: "Visual dark mode fidelity requires browser inspection"
---

# Phase 27.2: GrabFood Menu Simulator Verification Report

**Phase Goal:** Admin can view, edit, and push the GrabFood menu from a standalone simulator page with GrabFood-app-like visual fidelity, inline editing (name, price, description, availability), photo upload with write-back to internal products, add-from-menu-products flow, and push-to-GrabFood with confirmation dialog showing change diff.

**Verified:** 2026-02-28
**Status:** PASSED (with human verification items)
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                                  | Status     | Evidence                                                                                      |
|----|--------------------------------------------------------------------------------------------------------|------------|-----------------------------------------------------------------------------------------------|
| 1  | grabfoodMenuItems table stores overrides (name, desc, price, availability, photos, sequence, push state) | VERIFIED   | `convex/schema.ts` lines 1586-1605: table with all required fields + 3 indexes                |
| 2  | Admin can create, update, delete, and reorder menu items                                               | VERIFIED   | `convex/grabfoodMenu/mutations.ts`: createItem, updateItem, deleteItem, reorderItems all implemented with requireRole(admin) |
| 3  | Photo upload generates storage URL and writes back to both grabfoodMenuItems and menuProducts           | VERIFIED   | `savePhoto` mutation (mutations.ts L25-51) patches both tables; `PhotoUpload.tsx` implements full generateUploadUrl → fetch POST → savePhoto flow |
| 4  | Push action sends price and availability changes via batch API + notification                           | VERIFIED   | `pushMenuChanges` action (adapter.ts L403+): two-step batchMenuUpdate (PRICE, AVAILABILITY) + notifyMenu always called |
| 5  | GET /menu webhook serves from grabfoodMenuItems (with fallback to externalProductMappings)              | VERIFIED   | `webhooks.ts` L172-199: runQuery(internal.grabfoodMenu.queries.listMenuItemsInternal), falls back to legacy path if empty |
| 6  | Push action updates lastPushedPrice/lastPushedAvailability/lastPushedAt for persistent diff tracking    | VERIFIED   | `updatePushState` internalMutation called by pushMenuChanges (adapter.ts L498+); schema fields confirmed at lines 1597-1599 |
| 7  | Populate action seeds grabfoodMenuItems from externalProductMappings on first use                      | VERIFIED   | `populateFromMappings` mutation (mutations.ts L185-234): reads externalProductMappings by_source_code, skips duplicates, creates items |
| 8  | Admin can view all menu items in a GrabFood-app-like card grid with photos, names, prices, availability toggles | VERIFIED | `GrabFoodMenuSimulator.tsx` L339: `grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4`; `MenuItemCard.tsx` has aspect-square photo, switch toggle, green price |
| 9  | Admin can inline edit item name, price, and description directly on the card                           | VERIFIED   | `MenuItemCard.tsx`: InlineEditText + InlineEditPrice components with onBlur commit pattern; calls onUpdate handler which calls updateItem mutation |
| 10 | Admin can add new items from internal menu products with auto-filled name/price/description             | VERIFIED   | `AddItemDialog.tsx`: filters existingMenuProductIds, handleSelectProduct auto-fills overrideName/overridePrice, allows override before Add to Menu |
| 11 | Admin can soft-remove items by toggling availability off                                                | VERIFIED   | `MenuItemCard.tsx` L209-211: handleToggleAvailability calls onUpdate with isAvailable toggled; unavailable overlay applied at L230-232 |
| 12 | Admin can push changes with confirmation dialog showing diff (persistent across page reloads)           | VERIFIED   | `PushConfirmDialog.tsx`: priceChanges, availabilityChanges, newItems sections; diff computed from lastPushed* DB fields in useMemo (GrabFoodMenuSimulator.tsx L61-132) |
| 13 | Admin can populate menu from existing product mappings on first use                                     | VERIFIED   | "Populate from Mappings" button shown only when isEmpty (GrabFoodMenuSimulator.tsx L281-296); calls populateFromMappings mutation |
| 14 | Sync status badge shows latest menu sync result                                                         | VERIFIED   | `SyncStatusBadge.tsx`: 81 lines, full status mapping (success=green, error=red, started=yellow, none=gray) + relative time |
| 15 | Page accessible at /grabfood-menu with admin access                                                     | VERIFIED   | `App.tsx` L310-314: Route path="grabfood-menu" with ProtectedRoute requiredPermission="canAccessDashboard" |
| 16 | GrabFood CSS variable used throughout (not hardcoded hex)                                               | VERIFIED   | `src/index.css` L69: `--color-grabfood: #00B14F`; all components use `var(--color-grabfood)` |

**Score:** 16/16 truths verified

---

### Required Artifacts

| Artifact                                                    | Expected                                            | Status    | Details                                      |
|-------------------------------------------------------------|-----------------------------------------------------|-----------|----------------------------------------------|
| `convex/schema.ts`                                          | grabfoodMenuItems table + photoStorageId on menuProducts | VERIFIED | grabfoodMenuItems at L1586, photoStorageId on menuProducts at L107 |
| `convex/grabfoodMenu/queries.ts`                            | listMenuItems, getSyncStatus, listMenuItemsInternal  | VERIFIED  | All 3 exports present, substantive (107 lines) |
| `convex/grabfoodMenu/mutations.ts`                          | 7 public mutations + updatePushState internal        | VERIFIED  | 260 lines; 7 public (generateUploadUrl, savePhoto, createItem, updateItem, deleteItem, reorderItems, populateFromMappings) + 1 internal |
| `convex/integrations/grabfood/adapter.ts`                   | pushMenuChanges action                              | VERIFIED  | Exported at L403, substantive two-step push + state update |
| `convex/integrations/grabfood/webhooks.ts`                  | handleGetMenuWebhook reads from grabfoodMenuItems   | VERIFIED  | L172-199: prefers grabfoodMenuItems, falls back to legacy |
| `src/pages/GrabFoodMenuSimulator.tsx`                       | Main page (min 200 lines)                           | VERIFIED  | 375 lines; header, card grid, push flow, all dialogs wired |
| `src/components/grabfoodMenu/MenuItemCard.tsx`               | GrabFood card with photo, inline edit, toggle (min 80 lines) | VERIFIED | 294 lines; InlineEditText, InlineEditPrice, availability overlay |
| `src/components/grabfoodMenu/AddItemDialog.tsx`              | Dialog with product search and auto-fill (min 60 lines) | VERIFIED | 235 lines; search, filter, auto-fill, override fields |
| `src/components/grabfoodMenu/PushConfirmDialog.tsx`          | Diff sections before push (min 60 lines)            | VERIFIED  | 187 lines; price, availability, new items sections; merchant ID display |
| `src/components/grabfoodMenu/PhotoUpload.tsx`                | Photo upload with 5MB validation (min 40 lines)     | VERIFIED  | 117 lines; generateUploadUrl → fetch POST → savePhoto; toast on error |
| `src/components/grabfoodMenu/SyncStatusBadge.tsx`            | Sync status badge (min 20 lines)                    | VERIFIED  | 81 lines; full status/color mapping + relative time |
| `src/hooks/convex/useGrabFoodMenu.ts`                        | useGrabFoodMenu hook                                | VERIFIED  | 50 lines; all 7 mutations + pushMenuChanges action + 2 queries bundled |
| `src/App.tsx`                                               | Route for /grabfood-menu                            | VERIFIED  | L310-314: lazy-loaded GrabFoodMenuSimulator with ProtectedRoute |

---

### Key Link Verification

| From                                        | To                                              | Via                                             | Status  | Details                                                        |
|---------------------------------------------|-------------------------------------------------|-------------------------------------------------|---------|----------------------------------------------------------------|
| `convex/grabfoodMenu/mutations.ts`          | `convex/schema.ts`                              | ctx.db.insert/patch/delete on grabfoodMenuItems | WIRED   | ctx.db.insert at L89; ctx.db.patch at L38, L134; ctx.db.delete at L154 |
| `convex/integrations/grabfood/webhooks.ts`  | `convex/grabfoodMenu/queries.ts`                | internal.grabfoodMenu.queries.listMenuItemsInternal | WIRED | webhooks.ts L173-175: ctx.runQuery(internal.grabfoodMenu.queries.listMenuItemsInternal) |
| `convex/integrations/grabfood/adapter.ts`   | `convex/integrations/grabfood/config.ts`        | GRABFOOD_CONFIG.endpoints                       | WIRED   | adapter.ts L375: GRABFOOD_CONFIG.endpoints.menuBatch; L390: .menuNotify |
| `src/pages/GrabFoodMenuSimulator.tsx`       | `src/hooks/convex/useGrabFoodMenu.ts`           | useGrabFoodMenu hook                            | WIRED   | GrabFoodMenuSimulator.tsx L21: import; L35-45: destructured and used |
| `src/hooks/convex/useGrabFoodMenu.ts`       | `convex/grabfoodMenu/queries.ts`                | useQuery(api.grabfoodMenu.queries.listMenuItems) | WIRED  | hook L16-17: both listMenuItems and getSyncStatus queried      |
| `src/pages/GrabFoodMenuSimulator.tsx`       | `convex/integrations/grabfood/adapter.ts`       | useAction for pushMenuChanges                   | WIRED   | hook L27: useAction(api.integrations.grabfood.adapter.pushMenuChanges); page L227: pushMenuChanges({...}) |
| `src/App.tsx`                               | `src/pages/GrabFoodMenuSimulator.tsx`           | React.lazy route at /grabfood-menu              | WIRED   | App.tsx L80-81: lazyWithPreload import; L310: Route path="grabfood-menu" |

---

### Requirements Coverage

Both plans declare `requirements: []`. The phase is explicitly described as a "gap closure / inserted phase" with no REQUIREMENTS.md IDs. REQUIREMENTS.md does not map any requirement IDs to Phase 27.2 specifically. No orphaned requirements were found.

For context: GF-08 (availability toggle via batch API) was completed in Phase 27 and is checked off. GF-09 (sync status tracking) is listed as future work — it is not assigned to this phase. Phase 27.2 provides the full-stack implementation beyond GF-08 but does not carry a formal requirement ID.

**Coverage: N/A — no requirement IDs declared or expected.**

---

### Anti-Patterns Found

| File | Pattern | Severity | Notes |
|------|---------|----------|-------|
| None found | — | — | No TODO/FIXME, no empty implementations, no stub handlers found across all 13 artifacts |

`placeholder` occurrences in frontend files are legitimate HTML input placeholder attributes, not stubs.

`return null` in `convex/integrations/grabfood/adapter.ts` (lines 73, 126) are guard clauses for missing credentials — correct behavior.

---

### Human Verification Required

All automated checks pass. The following items require visual/live-environment verification:

#### 1. Inline Editing Flow

**Test:** Navigate to `/grabfood-menu`, click a product name, change value, click away.
**Expected:** Value saves via onBlur; card immediately reflects the new name without page reload.
**Why human:** The onBlur commit pattern involves DOM focus events that cannot be verified statically.

#### 2. Availability Overlay Visual

**Test:** Toggle the availability switch on any card to off.
**Expected:** Card receives a semi-transparent gray overlay; switch reflects the saved state on page reload.
**Why human:** Visual overlay application and reactive state persistence require browser inspection.

#### 3. Photo Upload End-to-End

**Test:** Click the camera area on a card, select an image file under 5MB.
**Expected:** Upload progress spinner appears; photo displays in card after upload; `menuProducts` record gains `photoStorageId` in Convex dashboard.
**Why human:** Convex file storage `generateUploadUrl` + fetch POST requires a live Convex environment.

#### 4. Push to GrabFood with Diff Dialog

**Test:** Make at least one price change and one availability change, then click "Push to GrabFood."
**Expected:** Dialog shows correct diff sections (price changes table, availability changes table); confirming push clears the diff after success toast.
**Why human:** Real GrabFood API call and reactive diff clearing require a live environment with valid credentials.

#### 5. Dark Mode Visual Fidelity

**Test:** Toggle system or browser dark mode and review the simulator page.
**Expected:** All card elements, dialogs, and badges render correctly; no hardcoded light-mode colors visible.
**Why human:** Visual theme correctness requires browser inspection.

---

### Summary

Phase 27.2 goal is fully achieved. All 16 observable truths are verified against the codebase:

- **Backend (Plan 01):** `grabfoodMenuItems` table with all required fields and 3 indexes exists in schema. `convex/grabfoodMenu/` module has 3 queries and 8 mutations (7 public + 1 internal). `pushMenuChanges` action correctly orchestrates two-step batch API call + menu notification + push state tracking. Webhook handler prefers `grabfoodMenuItems` with proper fallback.

- **Frontend (Plan 02):** All 6 frontend components exist and are substantive (well above minimum line counts). `GrabFoodMenuSimulator.tsx` (375 lines) integrates all components correctly. Inline editing via onBlur commit pattern is implemented in both text and price variants. Push diff is computed from persisted `lastPushed*` DB fields — survives page reloads. Route registered at `/grabfood-menu` with admin protection. GrabFood green CSS variable `--color-grabfood` used throughout.

- **Wiring:** All 7 key links verified — no orphaned artifacts. The `api.grabfoodMenu` module appears in `convex/_generated/api.d.ts`, confirming Convex registration.

No anti-patterns, stubs, or missing implementations found. Human verification is flagged for visual fidelity, photo upload live flow, and the push-to-GrabFood live API call.

---

_Verified: 2026-02-28_
_Verifier: Claude (gsd-verifier)_
