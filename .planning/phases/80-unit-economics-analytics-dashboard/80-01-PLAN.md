# Phase 80 — Plan 01: Implementation Plan (canonical)

**This is a pointer to the canonical plan.** The full task-by-task implementation plan lives at:

- **PLAN:** [`docs/superpowers/plans/2026-04-13-unit-economics-analytics-dashboard.md`](../../../docs/superpowers/plans/2026-04-13-unit-economics-analytics-dashboard.md)

Contains 16 tasks across 3 waves, complete code blocks per step, and TDD checkpoints.

**Read this file FIRST**, then read [`80-02-PLAN.md`](./80-02-PLAN.md) for the staff-review patches that supersede specific sections.

## Wave Summary

| Wave | Tasks | Files | Agent |
|---|---|---|---|
| 1 — Backend | T1, T2, T3, T4, T5, T6, T7 | `convex/reports/*`, `convex/schema.ts`, `convex/dispatchPlanner/queries.ts` | `convex-backend` (sequential) |
| 2 — Frontend | T8, T9, T10, T11, T12, T13 | `src/contexts/AnalyticsFilterContext.tsx`, `src/components/analytics/*`, `src/hooks/convex/useAnalytics.ts`, `src/pages/AnalyticsDashboard.tsx`, `src/App.tsx`, `src/components/layout/{Header,MobileBottomNav}.tsx` | `react-ui-builder` (T10-T12 parallel) |
| 3 — Verify | T14, T15, T16 | `tests/convex/*`, type/lint/build/docs/PR | `tdd-test-architect`, `code-auditor`, Bash |

## Critical Rule (Pitfall #11 closure)
"Units sold" metrics MUST iterate every `componentTypes` row where `category === "production" AND unit === "pcs"`. Hazelnut + future production types are counted automatically. Hardcoded `BIG_BALL`/`MID_BALL` checks anywhere in the codebase are bugs.
