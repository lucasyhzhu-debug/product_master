---
phase: 21-kitchen-production-targets
plan: 03
type: execute
wave: 3
depends_on:
  - 21-01
  - 21-02
files_modified:
  - src/pages/KitchenViewV2.tsx
  - src/components/kitchen/ProductionTargetsBar.tsx
  - src/components/kitchen/EndOfShiftForm.tsx
  - src/components/kitchen/ShiftReviewModal.tsx
  - src/components/kitchen/ShiftSuccessScreen.tsx
  - src/hooks/convex/useKitchenTargets.ts
autonomous: true
requirements:
  - KIT-12
  - KIT-13
  - KIT-14
  - KIT-15

must_haves:
  truths:
    - "Kitchen view shows ball totals (Original/Jumbo) at top-center derived from dispatch plan or defaults"
    - "Kitchen view shows packaging breakdown below ball totals"
    - "Boxing/stickering columns are no longer visible in the kitchen view"
    - "End-of-shift form at middle-bottom accepts produced quantities per product type"
    - "End-of-shift form has optional waste section with QA/testing, spoilage, waste reasons"
    - "Two-step confirmation: review summary shows before commit, success screen shows after"
    - "Orders are hidden by default, accessible via collapsible toggle"
  artifacts:
    - path: "src/pages/KitchenViewV2.tsx"
      provides: "Redesigned kitchen page with 3-section layout"
    - path: "src/components/kitchen/ProductionTargetsBar.tsx"
      provides: "Ball totals + packaging breakdown display"
    - path: "src/components/kitchen/EndOfShiftForm.tsx"
      provides: "Produced quantities + waste inputs"
    - path: "src/components/kitchen/ShiftReviewModal.tsx"
      provides: "Review summary dialog before commit"
    - path: "src/components/kitchen/ShiftSuccessScreen.tsx"
      provides: "Post-submit success summary"
    - path: "src/hooks/convex/useKitchenTargets.ts"
      provides: "Hook for getKitchenTargetsForDate + getShiftRecordsByDate"
  key_links:
    - from: "src/hooks/convex/useKitchenTargets.ts"
      to: "convex/kitchenConfig/queries.ts"
      via: "useQuery(api.kitchenConfig.queries.getKitchenTargetsForDate)"
      pattern: "getKitchenTargetsForDate"
    - from: "src/components/kitchen/EndOfShiftForm.tsx"
      to: "convex/kitchenShiftRecords/mutations.ts"
      via: "useProtectedMutation(api.kitchenShiftRecords.mutations.submitShiftRecord)"
      pattern: "submitShiftRecord"
---

<objective>
Redesign the kitchen page from the current 4-panel boxing/stickering layout into a simplified production-focused UI. Top-center displays today's production targets (ball totals + packaging breakdown). Middle-bottom has the end-of-shift form with two-step confirmation. Orders are hidden behind a collapsible toggle.

Purpose: Kitchen staff sees only what matters — how much to produce and a way to record what was made.
Output: Redesigned KitchenViewV2.tsx, new ProductionTargetsBar, EndOfShiftForm with review/success flow, useKitchenTargets hook.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-kitchen-production-targets/21-RESEARCH.md
@.planning/phases/21-kitchen-production-targets/21-01-SUMMARY.md
@.planning/phases/21-kitchen-production-targets/21-02-SUMMARY.md
@src/pages/KitchenViewV2.tsx (current implementation — will be heavily restructured)
@src/hooks/convex/useKitchenProduction.ts (WIB date pattern)
@src/components/kitchen/DueDateOrderList.tsx (keep — used in collapsible orders toggle)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useKitchenTargets hook + ProductionTargetsBar + EndOfShiftForm components</name>
  <files>src/hooks/convex/useKitchenTargets.ts, src/components/kitchen/ProductionTargetsBar.tsx, src/components/kitchen/EndOfShiftForm.tsx, src/components/kitchen/ShiftReviewModal.tsx, src/components/kitchen/ShiftSuccessScreen.tsx</files>
  <action>
**A) Create `src/hooks/convex/useKitchenTargets.ts`:**

Hook that provides today's targets and shift records. Computes today's date in WIB timezone (same pattern as `useKitchenProduction.ts` lines 162-164: `new Date(now.getTime() + 7 * 60 * 60 * 1000).toISOString().slice(0, 10)`).

```typescript
export function useKitchenTargets() {
  const today = useMemo(() => {
    const now = new Date();
    const wibNow = new Date(now.getTime() + 7 * 60 * 60 * 1000);
    return wibNow.toISOString().slice(0, 10);
  }, []);

  const targets = useQuery(api.kitchenConfig.queries.getKitchenTargetsForDate, { date: today });
  const todayShiftRecords = useQuery(api.kitchenShiftRecords.queries.getShiftRecordsByDate, { date: today });

  return { today, targets, todayShiftRecords };
}
```

Also re-export from `src/hooks/convex/index.ts`.

**B) Create `src/components/kitchen/ProductionTargetsBar.tsx`:**

Displays ball totals + packaging breakdown at the top of the kitchen page.

Layout (Claude's discretion on exact visual, but follow these constraints):
- Two large stat cards side by side: "Original Balls: {midBalls}" and "Jumbo Balls: {bigBalls}". Use the existing `StatCard` pattern from `src/components/kitchen/StatCard.tsx` or shadcn Card. BIG_BALL = Jumbo (80g), MID_BALL = Original (45g) per CLAUDE.md rule #10.
- Below: packaging breakdown as a horizontal row of compact badges or cards: "{quantity}x {productName}" for each item in packagingBreakdown array.
- If targets are undefined (loading), show a Skeleton loader.
- If targets have zero for both ball types, still display the zeros (per user decision: "display zero, do not hide").
- No "source" label shown (per user decision: "no source label").

Props: `targets: KitchenTargets | undefined`

**C) Create `src/components/kitchen/EndOfShiftForm.tsx`:**

Form for recording produced quantities + optional waste. Always visible at middle-bottom of kitchen page.

State: `step: "input" | "review" | "success"` — controls which view to show.

**Input step:**
- For each product in the targets' `packagingBreakdown`, render a number input field labeled with the product name. Default value: 0.
- State: `produced: Map<string, number>` keyed by menuProductId.
- Expandable waste section: "Any waste to capture?" toggle. When expanded, shows waste fields for each product with a dropdown for reason (QA/testing, Spoilage, Waste). Multiple waste entries per product are allowed if different reasons.
- State: `wasteEntries: Array<{ menuProductId, reason, quantity }>`.
- "Review & Submit" button — transitions to review step.
- Validate: at least one produced quantity > 0. Each waste quantity must not exceed produced quantity for the same product.

**Review step:** Render `<ShiftReviewModal>` inline (not a dialog — per context "review summary screen").

**Success step:** Render `<ShiftSuccessScreen>` inline.

**D) Create `src/components/kitchen/ShiftReviewModal.tsx`:**

Summary display of what will be submitted:
- "Production Summary" heading
- List of produced items: "80x Original Singles, 25x Original Triples" etc.
- If waste entries exist: "Waste" section showing "5x Original Singles (QA/testing)" etc.
- "Inventory will be updated at Kitchen location" note
- "Confirm" button (calls submitShiftRecord mutation via `useProtectedMutation`) and "Back" button
- On submit success: callback to parent to transition to success step
- On submit error: show toast error, stay on review step

Props: `produced, waste, onConfirm: () => Promise<void>, onBack: () => void, isSubmitting: boolean`

**E) Create `src/components/kitchen/ShiftSuccessScreen.tsx`:**

Clean summary of what was recorded:
- Green checkmark icon
- "Shift Recorded" heading
- Summary: "Produced: 80 singles, 25 triples. Waste: 5 singles (QA)."
- "Done" button — resets form back to input step with cleared values

Props: `produced, waste, onDone: () => void`
  </action>
  <verify>Run `npm run type-check`. All new components and hook compile. Verify hook is exported from index.ts.</verify>
  <done>useKitchenTargets hook provides today's targets and shift records. ProductionTargetsBar shows ball totals + packaging breakdown. EndOfShiftForm implements 3-step flow (input → review → success) with ShiftReviewModal and ShiftSuccessScreen.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure KitchenViewV2.tsx — simplified 3-section layout with collapsible orders</name>
  <files>src/pages/KitchenViewV2.tsx</files>
  <action>
Heavily restructure `KitchenViewV2.tsx` (currently ~567 lines) into the new simplified layout. The old 4-panel tabbed/swipeable layout (Production, Boxing, Stickering, Orders) is replaced by a single-page flow.

**New page structure:**
```
KitchenViewV2
  ├── PageHeader ("Kitchen" + date display)
  ├── ProductionTargetsBar (from Task 1)
  ├── EndOfShiftForm (from Task 1)
  ├── [Collapsible] "View Today's Orders" toggle
  │   └── DueDateOrderList (existing component, reused)
  └── [Conditionally] Today's Shift Records (compact list of submissions today)
```

**Implementation steps:**

1. **Keep all hooks at the top** (React hooks rule — CLAUDE.md Pitfall #9). Call `useKitchenTargets()`, `useAuth()`, and any existing hooks needed for the orders toggle BEFORE any conditional returns.

2. **Remove old panel components** from the render tree:
   - Remove `BoxingPanel`, `StickeringPanel`, `SwipeableKitchenLayout`, `ProductionLogPanel` imports and usage
   - Remove the tab/swipe navigation state
   - Remove `BallTrayCounter` and `BatchConfirmDialog` usage
   - Do NOT delete the component files — Phase 24 handles cleanup

3. **New render structure:**
   - `<div className="flex flex-col gap-6 p-4 max-w-4xl mx-auto">` container
   - PageHeader with "Kitchen" title and today's date (WIB)
   - `<ProductionTargetsBar targets={targets} />`
   - `<EndOfShiftForm targets={targets} today={today} todayShiftRecords={todayShiftRecords} />`
   - Collapsible orders section using shadcn `Collapsible` (or a simple toggle with `useState`):
     ```
     <button onClick={toggle}>
       {isOpen ? "Hide" : "View"} Today's Orders ({orderCount})
     </button>
     {isOpen && <DueDateOrderList ... />}
     ```
     Hidden by default (per user decision).
   - Below the form: compact list of today's shift records (if any exist). Show "N submissions today" with a brief summary per entry (who, when, total units). This gives immediate feedback that the submission was recorded.

4. **Loading state:** If `targets === undefined`, show a loading skeleton. Use existing Skeleton component or a simple loading indicator.

5. **Access control:** Keep the existing auth check pattern from KitchenViewV2. The page is accessible to all roles (kitchen, order_staff, manager, admin).

6. **Simplify imports:** Remove unused imports from the old layout. Keep imports for `DueDateOrderList`, `KitchenOrderCard`, and any order-related hooks needed for the collapsible section.

The resulting file should be significantly shorter than the current 567 lines — target ~200-300 lines since most logic is now in child components.
  </action>
  <verify>Run `npm run build` — full build must pass. Load the kitchen page in browser: targets display at top, end-of-shift form in middle, orders hidden behind toggle.</verify>
  <done>KitchenViewV2 is a clean 3-section layout: targets top-center, end-of-shift middle-bottom, collapsible orders. No boxing/stickering visible. All hooks called before conditionals.</done>
</task>

</tasks>

<verification>
1. `npm run type-check` passes
2. `npm run build` succeeds
3. Kitchen page shows ball totals (Original/Jumbo) at top-center
4. Packaging breakdown visible below ball totals
5. No boxing/stickering columns visible anywhere on the page
6. End-of-shift form at middle-bottom with produced + waste inputs
7. Two-step confirm: review screen before submit, success screen after
8. Orders hidden by default, visible via collapsible toggle
9. Today's shift records shown in a compact list
</verification>

<success_criteria>
- [ ] `npm run type-check` passes
- [ ] `npm run build` succeeds
- [ ] ProductionTargetsBar renders ball totals and packaging breakdown
- [ ] EndOfShiftForm has input → review → success flow
- [ ] KitchenViewV2 is simplified: no boxing/stickering, targets at top, form at bottom
- [ ] Collapsible orders toggle works (hidden by default)
- [ ] useKitchenTargets hook exported and wired
</success_criteria>

<output>
After completion, create `.planning/phases/21-kitchen-production-targets/21-03-SUMMARY.md`
</output>
