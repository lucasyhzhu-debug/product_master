---
phase: 52-expense-system-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/expenses/analyticsQueries.ts
  - convex/expenses/mutations.ts
  - convex/expenses/helpers.ts
  - convex/lib/validation.ts
  - convex/bankAccounts/mutations.ts
  - convex/payroll/queries.ts
  - convex/payroll/mutations.ts
  - convex/reimbursements/queries.ts
  - convex/reimbursements/mutations.ts
autonomous: true
requirements: [F1, F2, F3, F4, F5]

must_haves:
  truths:
    - "getFraudFlags uses 4 DB queries (down from 10) with in-memory slicing for 7d/30d subsets"
    - "All sequential for...of + await ctx.db.get() loops in payroll/reimbursement files are replaced with Promise.all parallel fetches"
    - "rejectExpense and voidExpense use validateRequiredReason from convex/lib/validation.ts"
    - "EXPENSE_HIGH_VALUE_THRESHOLD = 500_000 is the single source with DOA_ADMIN_ONLY_THRESHOLD and COMMENT_REQUIRED_THRESHOLD as aliases"
    - "bankAccounts delete mutation runs both reimbursement batch queries in parallel via Promise.all"
    - "All existing tests pass unchanged"
  artifacts:
    - path: "convex/expenses/analyticsQueries.ts"
      provides: "Consolidated getFraudFlags with toExpenseForFraud helper"
      contains: "toExpenseForFraud"
    - path: "convex/lib/validation.ts"
      provides: "Parameterized validateRequiredReason with optional label"
      exports: ["validateRequiredReason"]
    - path: "convex/expenses/helpers.ts"
      provides: "Unified threshold constant with aliases"
      contains: "EXPENSE_HIGH_VALUE_THRESHOLD"
  key_links:
    - from: "convex/expenses/mutations.ts"
      to: "convex/lib/validation.ts"
      via: "import validateRequiredReason"
      pattern: "validateRequiredReason"
    - from: "convex/expenses/helpers.ts"
      to: "EXPENSE_HIGH_VALUE_THRESHOLD"
      via: "constant alias"
      pattern: "EXPENSE_HIGH_VALUE_THRESHOLD"
---

<objective>
Consolidate backend expense code: parallelize sequential DB reads (F1, F2, F5), use shared validators (F3), and unify threshold constants (F4-backend). Zero behavior changes.

Purpose: Reduce DB round-trips on analytics hot path (10 to 4), eliminate duplicated validation logic, and prevent threshold drift.
Output: 9 Convex backend files refactored with parallel fetches, shared validation, and unified constants.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-expense-system-simplification/52-CONTEXT.md
@.planning/phases/52-expense-system-simplification/52-RESEARCH.md

@convex/expenses/analyticsQueries.ts
@convex/expenses/mutations.ts
@convex/expenses/helpers.ts
@convex/lib/validation.ts
@convex/bankAccounts/mutations.ts
@convex/payroll/queries.ts
@convex/payroll/mutations.ts
@convex/reimbursements/queries.ts
@convex/reimbursements/mutations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate fraud flag queries and parallelize backend DB reads (F1, F2, F5)</name>
  <files>convex/expenses/analyticsQueries.ts, convex/payroll/queries.ts, convex/payroll/mutations.ts, convex/reimbursements/queries.ts, convex/reimbursements/mutations.ts, convex/bankAccounts/mutations.ts</files>
  <action>
**F1 -- getFraudFlags in convex/expenses/analyticsQueries.ts:**
1. Replace the 3 sequential `Promise.all` blocks (10 total queries across 7d/30d/90d windows) with a single `Promise.all` of 4 queries -- one per status (`submitted`, `approved`, `awaiting_payment`, `reimbursed`) using the 90-day window (superset of all).
2. Extract a `toExpenseForFraud(e: Doc<"expenses">): ExpenseForFraud` helper function that replaces the 3 identical `.map(e => ({...}))` casts. Place it as a module-level function in the same file.
3. After the single `Promise.all`, combine all results into `allFraud = [...submitted90, ...approved90, ...awaiting90, ...reimbursed90].map(toExpenseForFraud)`.
4. Filter in memory for each detector:
   - Split detection (7d): `allFraud.filter(e => e.expenseDate >= sevenDaysAgo && ["submitted", "approved", "awaiting_payment"].includes(e.status))`
   - Concentration detection (30d): `allFraud.filter(e => e.expenseDate >= thirtyDaysAgo && ["approved", "awaiting_payment", "reimbursed"].includes(e.status))`
   - Unfamiliar vendor (90d): use full `allFraud` set, split by 30d threshold for recent vs historical windows
5. Remove the now-unused `sevenDaysAgo` and `thirtyDaysAgo` query window variables if they were only used for DB queries (keep them if still used for in-memory filtering).

**F2 -- Sequential user/entity fetches to Promise.all (6 call sites):**
1. `convex/payroll/queries.ts` (list handler, ~lines 61-76): Replace `for (userId of userIds) { await ctx.db.get() }` with `Promise.all([...userIds].map(id => ctx.db.get(id as Id<"users">)))`, then build userMap from results.
2. `convex/reimbursements/queries.ts` (listAwaitingPayment, ~lines 60-68): Same pattern -- collect unique user IDs, `Promise.all`, build Map.
3. `convex/reimbursements/mutations.ts` (createBatch, ~lines 60-95): Parallelize the `ctx.db.get()` calls for expense fetching: `const expenses = await Promise.all(args.expenseIds.map(id => ctx.db.get(id)))`. Keep the validation loop sequential AFTER the parallel fetch. The double-batching checks can also be parallelized per expense.
4. `convex/reimbursements/mutations.ts` (confirmBatch, ~lines 219-238): Fetch all expenses via `Promise.all` first. Then loop sequentially for `ctx.db.patch()` and audit trail writes (writes MUST be sequential in Convex).
5. `convex/reimbursements/mutations.ts` (voidBatch, ~lines 301-318): Same pattern as confirmBatch -- parallel reads, sequential writes.
6. `convex/payroll/mutations.ts` (create, ~lines 60-68): Two sequential account lookups by code. Wrap in `Promise.all([debitQuery, creditQuery])`.

**F5 -- bankAccounts referential integrity check:**
In `convex/bankAccounts/mutations.ts` (~lines 106-118), wrap the two sequential reimbursement batch queries in `Promise.all`:
```typescript
const [pendingBatches, confirmedBatches] = await Promise.all([
  ctx.db.query("reimbursementBatches")...collect(),
  ctx.db.query("reimbursementBatches")...collect(),
]);
```

**IMPORTANT:** Only parallelize reads. All `ctx.db.patch()`, `ctx.db.insert()`, and audit trail writes must remain sequential.
  </action>
  <verify>
    <automated>cd "D:/Claude/Product Manager/product_master" && npx vitest run tests/convex/expenseAnalytics.test.ts tests/convex/payroll.test.ts tests/convex/reimbursementBatch.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>getFraudFlags uses exactly 4 DB queries in a single Promise.all (down from 10 in 3 blocks). toExpenseForFraud helper replaces 3 duplicate .map blocks. All 6 sequential for...of + await ctx.db.get() loops replaced with Promise.all. bankAccounts delete check runs queries in parallel. All existing tests pass unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Shared validation and threshold unification (F3, F4-backend)</name>
  <files>convex/lib/validation.ts, convex/expenses/mutations.ts, convex/expenses/helpers.ts</files>
  <action>
**F3 -- Parameterize validateRequiredReason:**
1. In `convex/lib/validation.ts`, add an optional `label` parameter to `validateRequiredReason`:
   ```typescript
   export function validateRequiredReason(reason: string, label = "Void reason"): void {
     if (!reason.trim()) {
       throw new Error(`${label} is required`);
     }
   }
   ```
   This is backward-compatible -- existing callers (payroll, reimbursements) pass no label and still get "Void reason is required".

2. In `convex/expenses/mutations.ts`, replace the inline trim+check in `rejectExpense` (~lines 449-453) with:
   ```typescript
   validateRequiredReason(args.reason, "Rejection reason");
   ```
   And in `voidExpense` (~lines 496-500) with:
   ```typescript
   validateRequiredReason(args.reason);
   ```
   Add the import at the top of the file.

**F4-backend -- Threshold constant unification:**
In `convex/expenses/helpers.ts` (~lines 13-14), replace:
```typescript
export const DOA_ADMIN_ONLY_THRESHOLD = 500_000;
export const COMMENT_REQUIRED_THRESHOLD = 500_000;
```
With:
```typescript
export const EXPENSE_HIGH_VALUE_THRESHOLD = 500_000;
export const DOA_ADMIN_ONLY_THRESHOLD = EXPENSE_HIGH_VALUE_THRESHOLD;
export const COMMENT_REQUIRED_THRESHOLD = EXPENSE_HIGH_VALUE_THRESHOLD;
```
This preserves all existing imports -- the aliases export the same values under the same names.

**Test impact check:** The test `convex/reimbursements/__tests__/helpers.test.ts:65` asserts "Void reason is required" -- unchanged since default label is "Void reason". The test `convex/expenses/__tests__/helpers.test.ts` imports both `DOA_ADMIN_ONLY_THRESHOLD` and `COMMENT_REQUIRED_THRESHOLD` -- unchanged since aliases export same values.
  </action>
  <verify>
    <automated>cd "D:/Claude/Product Manager/product_master" && npx vitest run convex/expenses/__tests__/helpers.test.ts convex/reimbursements/__tests__/helpers.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>validateRequiredReason accepts optional label parameter (default "Void reason"). rejectExpense uses "Rejection reason" label, voidExpense uses default. EXPENSE_HIGH_VALUE_THRESHOLD = 500_000 exists with DOA_ADMIN_ONLY_THRESHOLD and COMMENT_REQUIRED_THRESHOLD as aliases. All tests pass.</done>
</task>

</tasks>

<verification>
```bash
cd "D:/Claude/Product Manager/product_master" && npm run type-check && npm run test 2>&1 | tail -20
```
All existing tests must pass unchanged. Type check must pass.
</verification>

<success_criteria>
- getFraudFlags uses 4 DB queries in single Promise.all (down from 10)
- toExpenseForFraud helper eliminates 3 duplicate .map blocks
- All 6 sequential for...of loops replaced with Promise.all parallel fetches
- bankAccounts delete check runs both queries in parallel
- validateRequiredReason parameterized with optional label
- rejectExpense and voidExpense use shared validator
- EXPENSE_HIGH_VALUE_THRESHOLD is single source with aliases
- `npm run type-check` passes
- `npm run test` passes (zero behavior changes)
</success_criteria>

<output>
After completion, create `.planning/phases/52-expense-system-simplification/52-01-SUMMARY.md`
</output>
