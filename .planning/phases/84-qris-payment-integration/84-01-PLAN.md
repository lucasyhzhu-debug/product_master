---
phase: 84-qris-payment-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - convex/integrations/qris/__tests__/xendit.test.ts
  - convex/integrations/qris/__tests__/verifyToken.test.ts
  - convex/qrisPayments/__tests__/mutations.test.ts
  - convex/qrisPayments/__tests__/createInvoice.test.ts
  - convex/qrisPayments/__tests__/webhookTransition.test.ts
  - convex/qrisPayments/__tests__/_factory.ts
  - src/components/orders/__tests__/QrisChargeDialog.test.tsx
  - tests/e2e/qris-charge.spec.ts
autonomous: true
requirements: [R1, R2, R3, R4, R5, R6, R7]
must_haves:
  truths:
    - "qrcode.react@4.2.0 is installed and resolvable"
    - "vendor-*.js bundle cap is raised to 650 kB in the same commit that adds qrcode.react"
    - "All Wave 0 test files exist and fail RED (modules under test not implemented yet)"
    - "A qrisPayments factory + AwaitingPayment order factory exist for reuse by backend tests"
  artifacts:
    - path: "convex/integrations/qris/__tests__/xendit.test.ts"
      provides: "R1 buildCreateQrBody request-body shape test"
      contains: "buildCreateQrBody"
    - path: "convex/integrations/qris/__tests__/verifyToken.test.ts"
      provides: "R4a verifyCallbackToken constant-time compare test"
      contains: "verifyCallbackToken"
    - path: "convex/qrisPayments/__tests__/webhookTransition.test.ts"
      provides: "R4b/R4c idempotency replay + needsReview tests"
      contains: "decideWebhookOutcome"
    - path: "convex/qrisPayments/__tests__/_factory.ts"
      provides: "qrisPayments + AwaitingPayment order test factories"
      contains: "AwaitingPayment"
    - path: "vite.config.ts"
      provides: "Raised vendor bundle cap"
      contains: "650 kB"
  key_links:
    - from: "package.json"
      to: "qrcode.react"
      via: "dependencies entry"
      pattern: "qrcode.react"
    - from: "vite.config.ts"
      to: "vendor-*.js cap"
      via: "bundlesize limit"
      pattern: "650 kB"
---

<objective>
Stand up the Wave 0 test scaffold for the entire phase, install the `qrcode.react` dependency, and bump the vendor bundle cap in the SAME commit. This plan creates all failing (RED) test files and shared test factories so subsequent implementation plans turn them GREEN.

Purpose: Satisfy the Nyquist validation contract (84-VALIDATION.md) — every downstream requirement (R1–R7) has a pre-existing automated test. Avoid the recurring bundle-cap CI failure (pitfall #16) by raising the cap atomically with the dependency.
Output: `qrcode.react@4.2.0` installed, `vite.config.ts` cap at 650 kB, 6 RED test files + 1 factory helper.
</objective>

<execution_context>
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/workflows/execute-plan.md
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-qris-payment-integration/84-SPEC.md
@.planning/phases/84-qris-payment-integration/84-RESEARCH.md
@.planning/phases/84-qris-payment-integration/84-VALIDATION.md
@.planning/phases/84-qris-payment-integration/84-PATTERNS.md

<interfaces>
<!-- Pure-function signatures the tests assert against. These functions are implemented in later plans;
     the tests reference them and fail RED until then. Copy these signatures verbatim. -->

buildCreateQrBody (impl in convex/integrations/qris/xendit.ts — Plan 02):
```typescript
export function buildCreateQrBody(orderNumber: string, finalTotal: number): {
  reference_id: string; external_id: string; type: "DYNAMIC"; currency: "IDR"; amount: number;
}
```

verifyCallbackToken (impl in convex/integrations/qris/webhooks.ts — Plan 04):
```typescript
export function verifyCallbackToken(header: string | null, expected: string | undefined): boolean
```

decideWebhookOutcome (impl in convex/qrisPayments/mutations.ts — Plan 03):
```typescript
export function decideWebhookOutcome(
  order: { status: string },
  row: { amount: number; status: string },
  payload: { amount: number },
): { transition: boolean; recordPaid: true; needsReview: boolean; reason?: string }
```
</interfaces>

<test-model>
@convex/__tests__/hmac.test.ts
@convex/productInventory/__tests__/channelAudit.test.ts
</test-model>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install qrcode.react and bump the vendor bundle cap atomically</name>
  <read_first>
    - vite.config.ts (lines 15-44 — the bundlesize limits + manualChunks block; PATTERNS.md analog: vite.config.ts:22)
    - 84-RESEARCH.md (Pitfall 3 + Standard Stack — qrcode.react@4.2.0, named exports QRCodeSVG/QRCodeCanvas)
    - CLAUDE.md Common Pitfall #16 (vendor bundle cap)
  </read_first>
  <action>
Run `npm install qrcode.react@4.2.0`. This is a NEW dependency (verified absent). It exports NAMED components `QRCodeSVG` and `QRCodeCanvas` (no default export) — do not import a default.

In `vite.config.ts`, change the `assets/vendor-*.js` bundlesize limit from `'600 kB'` to `'650 kB'` (currently line 22). Update the explanatory comment block above it (lines 19-21) to append: qrcode.react (~tens of kB) added Phase 84. Keep the fallback note that overflow can be split via manualChunks.

Both changes (package.json/package-lock.json AND vite.config.ts) MUST land in the same commit — a green local Windows build can still break Vercel on the cap (pitfall #16). After install, verify `npm ls qrcode.react` resolves to 4.2.0.
  </action>
  <verify>
    <automated>npm ls qrcode.react</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls qrcode.react` output contains `qrcode.react@4.2.0`
    - `vite.config.ts` contains the literal string `650 kB` on the `assets/vendor-*.js` limit line
    - `vite.config.ts` no longer contains `assets/vendor-*.js'` paired with `600 kB`
    - `package.json` dependencies contains `"qrcode.react"`
  </acceptance_criteria>
  <done>qrcode.react@4.2.0 installed; vendor cap raised to 650 kB in the same change set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create pure-function unit test files (RED) for R1 + R4a</name>
  <read_first>
    - convex/__tests__/hmac.test.ts (the test model for verifyCallbackToken per PATTERNS.md + RESEARCH R4a)
    - 84-RESEARCH.md (Pattern 1 buildCreateQrBody, Pattern 2 verifyCallbackToken, the divergence note: missing token → false)
    - 84-PATTERNS.md (xendit.ts + webhooks.ts pure-fn blocks)
  </read_first>
  <behavior>
    buildCreateQrBody (R1):
    - returns `type: "DYNAMIC"`, `currency: "IDR"`
    - `external_id` === orderNumber, `reference_id` === orderNumber
    - `amount` === finalTotal (exact, no rounding)
    verifyCallbackToken (R4a):
    - returns false when header is null (missing header → 401)
    - returns false when expected is undefined (missing config → 401)
    - returns false on length mismatch
    - returns false on content mismatch (same length)
    - returns true on exact match
  </behavior>
  <action>
Create `convex/integrations/qris/__tests__/xendit.test.ts`. Import `{ buildCreateQrBody }` from `../xendit` and assert the body shape: given `buildCreateQrBody("0521-001", 35000)`, expect `{ reference_id: "0521-001", external_id: "0521-001", type: "DYNAMIC", currency: "IDR", amount: 35000 }`. Add a case with finalTotal `1500` (Xendit floor) asserting `amount === 1500`.

Create `convex/integrations/qris/__tests__/verifyToken.test.ts`, modelled on `convex/__tests__/hmac.test.ts`. Import `{ verifyCallbackToken }` from `../webhooks` and cover all 5 behavior cases above. Use a fixed expected token like `"whtok_test_abc123"`.

These import modules that do not exist yet (xendit.ts, webhooks.ts) — the suite MUST fail RED. That is correct for Wave 0. Do NOT create stub implementations.
  </action>
  <verify>
    <automated>npm run test -- convex/integrations/qris/__tests__/verifyToken.test.ts; if [ $? -ne 0 ]; then echo "RED-as-expected"; fi</automated>
  </verify>
  <acceptance_criteria>
    - File `convex/integrations/qris/__tests__/xendit.test.ts` contains `buildCreateQrBody` and `type: "DYNAMIC"` and `currency: "IDR"`
    - File `convex/integrations/qris/__tests__/verifyToken.test.ts` contains `verifyCallbackToken` and at least 5 `expect(` assertions
    - Running the verifyToken test fails to resolve `../webhooks` (RED — module not yet implemented)
  </acceptance_criteria>
  <done>Two pure-fn test files exist and fail RED because their target modules are not implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create backend integration test files (RED) + shared factory for R2/R3/R4b/R4c, plus frontend + E2E test stubs</name>
  <read_first>
    - convex/productInventory/__tests__/channelAudit.test.ts (convex-test t.run pattern; line ~212 documents the t.action(internal.*) resolver bug per RESEARCH Pitfall 5)
    - 84-RESEARCH.md (Validation Architecture → Test Map R2/R3/R4b/R4c; Testing strategy note: invoke mutations via t.run, NOT t.action(internal.*))
    - 84-PATTERNS.md (mutations.ts decideWebhookOutcome block; the schema block for qrisPayments field shapes)
    - 84-UI-SPEC.md (the 3 roles must mount; reactive paid flip; states active/paid/expired)
  </read_first>
  <behavior>
    decideWebhookOutcome (R4b/R4c, pure — testable now without runtime):
    - order AwaitingPayment + amount match + row pending → { transition: true, recordPaid: true, needsReview: false }
    - order PaymentReceived (replay) → { transition: false } (idempotency)
    - payload.amount !== row.amount → { needsReview: true, reason contains "amount" } AND recordPaid: true
    - row.status === "expired" (superseded) → { needsReview: true, reason contains "superseded" } AND recordPaid: true
    mutations/createInvoice (R2/R3, via t.run on internal mutations once implemented):
    - insertPending creates one pending row linked to orderId
    - expirePrior flips a prior pending row to expired
    - getActiveQrisPayment returns the latest non-expired row
    - create guard: order not AwaitingPayment → throws, writes nothing
    - create guard: finalTotal < 1500 → rejected
  </behavior>
  <action>
Create `convex/qrisPayments/__tests__/_factory.ts` exporting: (a) `makeAwaitingPaymentOrder(t)` that inserts a minimal `orders` row with `status: "AwaitingPayment"` and a `finalTotal` (default 35000) and returns its id; (b) `makeQrisPayment(t, overrides)` that inserts a `qrisPayments` row with sensible defaults matching the schema in 84-PATTERNS.md (`provider: "xendit"`, `externalId`, `xenditQrId`, `qrString`, `amount`, `status: "pending"`, `expiresAt: Date.now()+30*60*1000`). Use `convexTest`/`t.run(async (ctx) => ctx.db.insert(...))`.

Create `convex/qrisPayments/__tests__/webhookTransition.test.ts`: import `{ decideWebhookOutcome }` from `../mutations` and cover the 4 pure-fn cases above directly (no runtime needed — sidesteps the t.action resolver bug). Then add an integration block that, once `recordPaidAndTransition` exists, invokes it TWICE via `t.run`/`ctx.runMutation` against a factory order to assert: first call transitions to PaymentReceived + reserves stock once; second call (replay) is a no-op (status stays PaymentReceived, no double reservation). Mark the integration block so it fails RED until the mutation exists.

Create `convex/qrisPayments/__tests__/mutations.test.ts` (R2): assert insertPending/expirePrior/getActiveQrisPayment behavior via `t.run`/internal mutations. RED until Plan 03.

Create `convex/qrisPayments/__tests__/createInvoice.test.ts` (R3): assert the two guards (non-AwaitingPayment throws + writes nothing; finalTotal < 1500 rejected) by exercising the internal query/mutation pieces via `t.run` (NOT `t.action(internal.*)` — Pitfall 5). RED until Plan 03.

Create `src/components/orders/__tests__/QrisChargeDialog.test.tsx` (R5/R7): RTL stubs asserting the component mounts without crashing for all 3 roles (order_staff, manager, admin), that it renders the QR in active state, and flips to a paid panel when the subscription row.status === "paid". RED until Plan 05.

Create `tests/e2e/qris-charge.spec.ts` (R6): a Playwright happy-path spec for the flag-on button → dialog → simulated paid flip. Wrap the live Test-Mode loop in `test.skip(!process.env.XENDIT_API_KEY, "no Test key")` so CI skips it cleanly.

All files import not-yet-existing modules and MUST fail RED (except the pure decideWebhookOutcome cases which also stay RED until Plan 03 exports it). Do NOT stub implementations.
  </action>
  <verify>
    <automated>npm run test -- convex/qrisPayments/__tests__/webhookTransition.test.ts; if [ $? -ne 0 ]; then echo "RED-as-expected"; fi</automated>
  </verify>
  <acceptance_criteria>
    - `convex/qrisPayments/__tests__/_factory.ts` contains `AwaitingPayment` and `makeQrisPayment`
    - `convex/qrisPayments/__tests__/webhookTransition.test.ts` contains `decideWebhookOutcome` and references a replay (twice-invoked) assertion
    - `convex/qrisPayments/__tests__/mutations.test.ts` and `createInvoice.test.ts` both exist and reference `t.run`
    - `src/components/orders/__tests__/QrisChargeDialog.test.tsx` mounts all 3 role strings: `order_staff`, `manager`, `admin`
    - `tests/e2e/qris-charge.spec.ts` contains `test.skip(` gated on `XENDIT_API_KEY`
    - Running webhookTransition test fails RED (target module `../mutations` not implemented)
  </acceptance_criteria>
  <done>All 5 remaining Wave 0 test files + factory exist and fail RED; convex-test resolver bug avoided via pure fns + t.run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build pipeline → Vercel | bundle cap is the gate; a missed bump ships a broken prod frontend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-00 | Denial of Service | Vercel deploy (bundle cap) | mitigate | Bump vendor cap to 650 kB in the same commit as the qrcode.react install (pitfall #16); verify locally with the cap before merge |

No payment-path code is introduced in this plan (test scaffolds + dependency only); webhook/secret/role threats are addressed in the plans that implement those surfaces.
</threat_model>

<verification>
- `npm ls qrcode.react` resolves to 4.2.0
- `vite.config.ts` vendor cap == 650 kB
- All 8 Wave 0 artifacts exist (6 test files + factory + e2e spec)
- Test suites for the new files fail RED (targets unimplemented) — this is the expected Wave 0 state
</verification>

<success_criteria>
- qrcode.react@4.2.0 installed; vendor cap raised to 650 kB atomically
- Every requirement R1–R7 has a corresponding failing test file (Nyquist contract satisfied)
- Shared qrisPayments + order factory available for Plans 02–05
</success_criteria>

<output>
After completion, create `.planning/phases/84-qris-payment-integration/84-01-SUMMARY.md`
</output>
