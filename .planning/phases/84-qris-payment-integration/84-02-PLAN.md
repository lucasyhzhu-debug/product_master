---
phase: 84-qris-payment-integration
plan: 02
type: execute
wave: 2
depends_on: [84-01]
files_modified:
  - convex/schema.ts
  - convex/integrations/qris/provider.ts
  - convex/integrations/qris/xendit.ts
autonomous: true
requirements: [R1, R2, R7]
must_haves:
  truths:
    - "A qrisPayments table exists with by_order and by_externalId indexes plus an optional rawPayload field"
    - "businessSettings carries an optional qrisNmid field"
    - "buildCreateQrBody produces the exact Xendit DYNAMIC QR request body (R1 test green)"
    - "xenditProvider.createInvoice reads process.env/fetch INSIDE the method (no module-top-level side effects, no 'use node'), calls POST /qr_codes with Basic auth, and returns {xenditQrId, qrString, expiresAt}"
  artifacts:
    - path: "convex/schema.ts"
      provides: "qrisPayments table (+ optional rawPayload) + businessSettings.qrisNmid"
      contains: "qrisPayments"
    - path: "convex/integrations/qris/provider.ts"
      provides: "QrisProvider interface (createInvoice, getStatus)"
      contains: "QrisProvider"
    - path: "convex/integrations/qris/xendit.ts"
      provides: "xenditProvider impl + buildCreateQrBody pure fn"
      contains: "buildCreateQrBody"
  key_links:
    - from: "convex/integrations/qris/xendit.ts"
      to: "api.xendit.co/qr_codes"
      via: "fetch with Basic auth"
      pattern: "api.xendit.co/qr_codes"
    - from: "convex/integrations/qris/xendit.ts"
      to: "QrisProvider"
      via: "implements interface"
      pattern: "QrisProvider"
---

<objective>
Lay the data + adapter foundation: the `qrisPayments` table (incl. optional `rawPayload`), the optional `businessSettings.qrisNmid` field, the provider-agnostic `QrisProvider` interface, and the Xendit implementation that calls `POST /qr_codes`. Turns the R1 (`buildCreateQrBody`) test GREEN.

Purpose: Everything downstream (create action, webhook, dialog) reads/writes this table and calls this adapter. Defining the contracts first removes the scavenger hunt for later plans. The `rawPayload` field gives Plan 04's A1/A2 raw-payload mitigation a real, schema-backed home (staffreview R1 — Convex rejects unschemaed fields).
Output: schema additions (+ codegen), `provider.ts` interface, `xendit.ts` adapter (env/fetch read inside the method, no `"use node"`).
</objective>

<execution_context>
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/workflows/execute-plan.md
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-qris-payment-integration/84-SPEC.md
@.planning/phases/84-qris-payment-integration/84-RESEARCH.md
@.planning/phases/84-qris-payment-integration/84-PATTERNS.md
@.planning/phases/84-qris-payment-integration/84-CONTEXT.md

<interfaces>
<!-- The contracts this plan defines. Copy verbatim — downstream plans import these. -->

QrisProvider (provider.ts):
```typescript
export interface CreateInvoiceResult { xenditQrId: string; qrString: string; expiresAt: number; }
export interface QrisProvider {
  createInvoice(args: { orderNumber: string; finalTotal: number }): Promise<CreateInvoiceResult>;
  getStatus(xenditQrId: string): Promise<{ status: string }>;
}
```

qrisPayments schema (schema.ts — exact field shapes from 84-PATTERNS.md + the rawPayload addition R1):
```typescript
qrisPayments: defineTable({
  orderId: v.id("orders"),
  provider: v.string(),                 // "xendit"
  externalId: v.string(),               // = orderNumber (per-day match key, NOT globally unique)
  xenditQrId: v.string(),               // globally unique — primary match key in the webhook
  qrString: v.string(),
  amount: v.number(),
  status: v.union(v.literal("pending"), v.literal("paid"), v.literal("expired")),
  receiptId: v.optional(v.string()),
  source: v.optional(v.string()),
  expiresAt: v.number(),                // our own 30-min window, NOT Xendit's expires_at (staffreview R5)
  paidAt: v.optional(v.number()),
  needsReview: v.optional(v.boolean()),
  reviewReason: v.optional(v.string()),
  rawPayload: v.optional(v.string()),   // staffreview R1 — A1/A2 raw webhook body for early debugging
})
  .index("by_order", ["orderId"])
  .index("by_externalId", ["externalId"]),
```
</interfaces>

<analogs>
@convex/integrations/grabfood/adapter.ts
@convex/integrations/grabfood/config.ts
</analogs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add qrisPayments table (+ optional rawPayload) + businessSettings.qrisNmid to schema, then codegen</name>
  <read_first>
    - convex/schema.ts (the `orders` defineTable + its `.index(...)` calls ~lines 319-327, and the `businessSettings` defineTable ~lines 632-642 with `npwp: v.optional(v.string())` per PATTERNS.md)
    - 84-PATTERNS.md (the schema block — exact field list + indexes)
    - 84-RESEARCH.md (Project Constraints — run `npx convex codegen` after schema edits; recurring Phase 76/81 stale-api.d.ts lesson)
  </read_first>
  <action>
In `convex/schema.ts`, add a new `qrisPayments` table EXACTLY as the schema block in the interfaces section above: 14 fields including the optional `rawPayload: v.optional(v.string())` (staffreview R1), `status` as `v.union(v.literal("pending"), v.literal("paid"), v.literal("expired"))`, with two indexes `.index("by_order", ["orderId"])` and `.index("by_externalId", ["externalId"])`.

Add a single optional field to the existing `businessSettings` defineTable: `qrisNmid: v.optional(v.string())` — match the `v.optional(v.string())` style of the existing `npwp` field. Do NOT make it required.

After saving, run `npx convex codegen` so `convex/_generated/api.d.ts` and `dataModel.d.ts` reflect the new table. Stale generated files are a recurring failure (Phase 76/81). Then `npm run type-check`.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `convex/schema.ts` contains `qrisPayments: defineTable(`
    - `convex/schema.ts` contains `.index("by_order", ["orderId"])` and `.index("by_externalId", ["externalId"])`
    - `convex/schema.ts` contains `rawPayload: v.optional(v.string())`
    - `convex/schema.ts` contains `qrisNmid: v.optional(v.string())`
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>qrisPayments table + indexes + optional rawPayload + businessSettings.qrisNmid compile; generated API types regenerated.</done>
</task>

<task type="auto">
  <name>Task 2: Define the QrisProvider interface (provider.ts)</name>
  <read_first>
    - convex/integrations/grabfood/config.ts (named-export type/const module analog per PATTERNS.md)
    - 84-CONTEXT.md (D-04 — provider.ts holds the QrisProvider interface: createInvoice, getStatus)
  </read_first>
  <action>
Create `convex/integrations/qris/provider.ts` as a plain TS module (NO Convex registrations) so both the action and the tests can import it. Export `CreateInvoiceResult` and the `QrisProvider` interface EXACTLY as the interfaces block above (`createInvoice(args: { orderNumber, finalTotal }) => Promise<{ xenditQrId, qrString, expiresAt }>`, `getStatus(xenditQrId) => Promise<{ status }>`). Use static imports only (pitfall #8). Mirror the named-export style of `grabfood/config.ts`.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `convex/integrations/qris/provider.ts` contains `export interface QrisProvider`
    - File contains `createInvoice` and `getStatus`
    - File contains no `import(` dynamic-import calls
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>QrisProvider interface + CreateInvoiceResult exported as a pure TS module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement xenditProvider adapter + buildCreateQrBody (turns R1 GREEN)</name>
  <read_first>
    - convex/integrations/grabfood/adapter.ts (env-var read pattern + fetch + `!res.ok` throw shape ~lines 27-41 per PATTERNS.md)
    - convex/integrations/qris/__tests__/xendit.test.ts (the R1 contract this must satisfy — written in Plan 01; it imports ONLY buildCreateQrBody, so module init must have NO side effects)
    - 84-RESEARCH.md (Pattern 1 — btoa NOT Buffer; key as username + empty password; spike body shape)
    - 84-PATTERNS.md (xendit.ts block — buildCreateQrBody exact return + fetch shape)
  </read_first>
  <behavior>
    - buildCreateQrBody("0521-001", 35000) === { reference_id: "0521-001", external_id: "0521-001", type: "DYNAMIC", currency: "IDR", amount: 35000 }
    - buildCreateQrBody(_, 1500) → amount === 1500 (Xendit floor)
  </behavior>
  <action>
Create `convex/integrations/qris/xendit.ts`. Do NOT add `"use node"` (staffreview I1/R3 — the default Convex runtime already provides `btoa` and `fetch`; `"use node"` would drop them). Do NOT read `process.env` or call `fetch` at module top level — all such reads happen INSIDE `createInvoice`/`getStatus`, so importing the module (as the R1 unit test does) executes no env/fetch and cannot throw on import.

Export the pure fn `buildCreateQrBody(orderNumber, finalTotal)` returning EXACTLY `{ reference_id: orderNumber, external_id: orderNumber, type: "DYNAMIC" as const, currency: "IDR" as const, amount: finalTotal }` (sends BOTH reference_id and external_id — the spike does, harmless; A4 fallback safety).

Export `xenditProvider: QrisProvider` (import the type from `./provider`). `createInvoice({ orderNumber, finalTotal })`:
- INSIDE the method: `const apiKey = process.env.XENDIT_API_KEY` (never client-exposed, never at module scope)
- `const authHeader = "Basic " + btoa(\`${apiKey}:\`)` — key as username, EMPTY password. Use `btoa`, NOT `Buffer` (Convex runtime is Web-standard).
- `await fetch("https://api.xendit.co/qr_codes", { method: "POST", headers: { Authorization: authHeader, "Content-Type": "application/json" }, body: JSON.stringify(buildCreateQrBody(orderNumber, finalTotal)) })`
- on `!res.ok`: `throw new Error(\`Xendit ${res.status}: ${await res.text()}\`)` (mirrors grabfood adapter:33-36)
- parse `{ id, qr_string }` and return `{ xenditQrId: json.id, qrString: json.qr_string, expiresAt: Date.now() + 30 * 60 * 1000 }` — our own 30-min window, NOT Xendit's `expires_at` (staffreview R5).

Implement `getStatus(xenditQrId)` calling `GET https://api.xendit.co/qr_codes/{id}` with the same Basic auth (env read inside the method), returning `{ status: json.status }`. Static imports only (pitfall #8). Secret read only here, never returned to client.

Run the R1 test — it MUST now pass GREEN.
  </action>
  <verify>
    <automated>npm run test -- convex/integrations/qris/__tests__/xendit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `convex/integrations/qris/xendit.ts` contains `type: "DYNAMIC"` and `currency: "IDR"`
    - File contains `"Basic " + btoa(` and does NOT contain `Buffer`
    - File does NOT contain `"use node"` (staffreview R3)
    - File contains `https://api.xendit.co/qr_codes`
    - File contains `process.env.XENDIT_API_KEY` and that read occurs inside `createInvoice`/`getStatus` (no top-level `process.env`/`fetch` call — the line is inside a handler body, not at module scope)
    - `npm run test -- convex/integrations/qris/__tests__/xendit.test.ts` exits 0 (R1 GREEN)
  </acceptance_criteria>
  <done>xenditProvider implements QrisProvider, reads env/fetch inside the methods (no 'use node', no module-top-level side effects), calls /qr_codes with Basic auth via btoa; R1 test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Convex action → Xendit API | outbound HTTPS carrying the secret API key |
| secret store → code | `XENDIT_API_KEY` lives in Convex env vars only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01 | Information Disclosure | XENDIT_API_KEY | mitigate | Read only via `process.env.XENDIT_API_KEY` INSIDE the adapter method; never at module scope, never returned to client, never logged; Basic-auth header built server-side only |
| T-84-02 | Tampering | create-QR request body | mitigate | `buildCreateQrBody` hard-codes `type:"DYNAMIC"`/`currency:"IDR"` and sets `amount` = caller's finalTotal; amount validation/floor enforced by the action (Plan 03) |
| T-84-03 | Spoofing | Xendit endpoint | accept | Fixed `https://api.xendit.co` host over TLS; no user-controlled URL. Accepted (vendor-controlled endpoint) |
</threat_model>

<verification>
- `npm run type-check` passes (schema + provider compile)
- R1 test (`buildCreateQrBody`) is GREEN
- qrisPayments table + both indexes + optional rawPayload present in schema; businessSettings.qrisNmid present
- No `Buffer` usage; no `"use node"`; no module-top-level env/fetch; no dynamic `import()` (pitfall #8)
</verification>

<success_criteria>
- qrisPayments table (+ rawPayload) + businessSettings.qrisNmid defined and type-checked
- QrisProvider interface + xenditProvider implementation exist (env/fetch inside the methods)
- R1 request-body-shape test green; secret never client-exposed
</success_criteria>

<output>
After completion, create `.planning/phases/84-qris-payment-integration/84-02-SUMMARY.md`
</output>
</content>
