---
phase: 84-qris-payment-integration
plan: 04
type: execute
wave: 4
depends_on: [84-03]
files_modified:
  - convex/integrations/qris/webhooks.ts
  - convex/integrations/qris/__tests__/webhookHandler.test.ts
  - convex/http.ts
autonomous: true
requirements: [R4, R4a]
must_haves:
  truths:
    - "POST /api/xendit/qr-payment is registered in convex/http.ts"
    - "A missing or invalid x-callback-token returns HTTP 401 with no state change AND recordPaidAndTransition is NOT invoked"
    - "A COMPLETED payload with a valid token records payment and drives the transition exactly once"
    - "A valid + non-COMPLETED payload returns 200 with no transition"
    - "The runMutation call is wrapped in try/catch (mirroring grabfood) so a throw never escapes as a 500; an unmatchable COMPLETED returns 200 so Xendit stops retrying"
    - "The parser passes both xenditQrId (primary) and externalId (A4) to the mutation and stores the raw payload (A1/A2) via rawPayload"
  artifacts:
    - path: "convex/integrations/qris/webhooks.ts"
      provides: "verifyCallbackToken pure fn + handleXenditQrPayment httpAction"
      contains: "verifyCallbackToken"
    - path: "convex/integrations/qris/__tests__/webhookHandler.test.ts"
      provides: "Handler-level 401 / non-COMPLETED / COMPLETED tests (staffreview C6)"
      contains: "handleXenditQrPayment"
    - path: "convex/http.ts"
      provides: "POST /api/xendit/qr-payment route registration"
      contains: "/api/xendit/qr-payment"
  key_links:
    - from: "convex/integrations/qris/webhooks.ts"
      to: "recordPaidAndTransition"
      via: "ctx.runMutation(internal.qrisPayments.mutations.recordPaidAndTransition)"
      pattern: "recordPaidAndTransition"
    - from: "convex/http.ts"
      to: "handleXenditQrPayment"
      via: "http.route POST registration"
      pattern: "handleXenditQrPayment"
---

<objective>
Build the inbound Xendit webhook: the constant-time `x-callback-token` verifier (turns R4a GREEN), the `httpAction` handler that records payment and drives the idempotent transition (with a try/catch around the mutation so a throw never becomes a 500), a handler-level test suite, and its registration in `convex/http.ts` alongside the 6 GrabFood routes.

Purpose: This is the paid signal — the ONLY way an order reaches PaymentReceived via QRIS (the QR never reads "paid" on poll; spike-confirmed). Authentication (401 on bad token), idempotency, honor-always-flag-mismatch, and safe handling of unmatchable payloads all converge here. Retry semantics are documented explicitly (staffreview I3).
Output: `convex/integrations/qris/webhooks.ts`, a handler test file, modified `convex/http.ts`.
</objective>

<execution_context>
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/workflows/execute-plan.md
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-qris-payment-integration/84-SPEC.md
@.planning/phases/84-qris-payment-integration/84-RESEARCH.md
@.planning/phases/84-qris-payment-integration/84-PATTERNS.md

<interfaces>
<!-- verifyCallbackToken is asserted by the R4a test (Plan 01). The handler calls the Plan 03 mutation. -->

verifyCallbackToken (webhooks.ts — copy verbatim from 84-PATTERNS.md):
```typescript
export function verifyCallbackToken(header: string | null, expected: string | undefined): boolean {
  if (!expected || !header) return false;            // missing config OR header → 401
  if (header.length !== expected.length) return false;
  let mismatch = 0;
  for (let i = 0; i < header.length; i++) mismatch |= header.charCodeAt(i) ^ expected.charCodeAt(i);
  return mismatch === 0;
}
```

Consumes (from Plan 03):
- internal.qrisPayments.mutations.recordPaidAndTransition({ xenditQrId?, externalId, amount, receiptId?, source?, rawPayload? }) → { transitioned: boolean }

Spike-confirmed COMPLETED payload (A2 — confirm against a real callback early):
```jsonc
{ "status": "COMPLETED", "amount": 35000, "reference_id": "0521-001", "qr_id": "qr_...",
  "payment_details": { "receipt_id": "RRN...", "source": "DANA" } }
// Header: x-callback-token: <XENDIT_WEBHOOK_TOKEN>  (A1 — confirm exact header live)
// A1/A2 + C8: confirm which key carries the globally-unique QR id (qr_id vs id) and pass it as xenditQrId.
```
</interfaces>

<retry-semantics>
<!-- staffreview I3 — document explicitly so reviewers/ops don't second-guess: -->
- Bad/missing token → 401 (no state change). Xendit will redeliver; that is correct (transient misconfig should self-heal once the token is fixed).
- COMPLETED, matched, honored (incl. needsReview mismatch) → 200. We chose to honor + flag, so we ACK to stop retries.
- COMPLETED, UNMATCHABLE (no qrisPayments row for the qr id/externalId) → 200. An unmatchable payload will never match on retry either, so we ACK to stop an infinite retry loop; the mutation's no-match path logs it for manual inspection (staffreview C4).
- mutation throws unexpectedly → caught; we still return 200 (the paid row, if reached, is recorded durably by Plan 03 step 5). We deliberately do NOT force redelivery via non-2xx, because Plan 03 records the payment durably before any throwable step, so a redelivery would only re-run the idempotent guard.
</retry-semantics>

<analogs>
@convex/integrations/grabfood/webhooks.ts
@convex/http.ts
</analogs>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: webhooks.ts — verifyCallbackToken (R4a) + handleXenditQrPayment httpAction + handler-level tests (C6)</name>
  <read_first>
    - convex/integrations/grabfood/webhooks.ts (lines 1-3 imports, the constant-time loop, and lines 107-128 — the `try { payload = JSON.parse } catch {}` + the try/catch around `processPayload`/`runMutation` then `return new Response("OK", { status: 200 })`. PATTERNS.md flags the DIVERGENCE: grabfood returns 200 on missing secret/sig — Xendit MUST return false → 401)
    - convex/integrations/qris/__tests__/verifyToken.test.ts (the R4a pure-fn contract written in Plan 01)
    - 84-PATTERNS.md (webhooks.ts block — the full httpAction skeleton + the btoa-not-Buffer note)
    - 84-RESEARCH.md (Pattern 2 + Pitfall 4 missing-token-must-401; Open Questions A1/A2 — capture one real callback early, store raw payload)
  </read_first>
  <behavior>
    verifyCallbackToken (R4a):
    - null header → false; undefined expected → false; length mismatch → false; content mismatch → false; exact match → true
    handler (staffreview C6 — assert at the httpAction level, not just the pure fn):
    - invalid/missing token → Response 401 AND recordPaidAndTransition NOT invoked (spy/mock the mutation runner)
    - valid token + status !== COMPLETED → 200, no transition (mutation NOT invoked)
    - valid token + COMPLETED → 200, recordPaidAndTransition invoked exactly once with { xenditQrId, externalId, amount, receiptId, source, rawPayload }
    - valid token + COMPLETED that the mutation reports unmatched ({ transitioned:false }) → still 200, no throw
  </behavior>
  <action>
Create `convex/integrations/qris/webhooks.ts`. Import `{ httpAction }` from `../../_generated/server` and `{ internal }` from `../../_generated/api`.

Export the pure `verifyCallbackToken(header, expected)` EXACTLY as the interfaces block (constant-time XOR loop, missing config/header → false). CRITICAL DIVERGENCE from grabfood: grabfood returns 200 on missing secret/signature (skip) — do NOT copy that. For Xendit, missing token MUST → 401 (SPEC R4a, Pitfall 4). Never use `===` on tokens (timing attack).

Export `handleXenditQrPayment = httpAction(async (ctx, request) => { ... })`:
1. `const body = await request.text();`
2. `const token = request.headers.get("x-callback-token");` — A1: confirm against one real Test-Mode callback before go-live.
3. `if (!verifyCallbackToken(token, process.env.XENDIT_WEBHOOK_TOKEN)) return new Response("Unauthorized", { status: 401 });` — 401, NOT 200; NO mutation call, NO state change before this passes.
4. Parse: `let payload: any = {}; try { payload = JSON.parse(body); } catch {}`. A2: the real envelope may wrap as `{ event, data: {...} }` — defensively `const evt = payload.data ?? payload;` before reading fields.
5. `if (evt.status === "COMPLETED") { try { await ctx.runMutation(internal.qrisPayments.mutations.recordPaidAndTransition, { xenditQrId: evt.qr_id ?? evt.id, externalId: evt.reference_id ?? evt.external_id, amount: evt.amount, receiptId: evt?.payment_details?.receipt_id, source: evt?.payment_details?.source, rawPayload: body }); } catch (err) { console.log("QRIS webhook mutation error:", err); } }` — staffreview C4/C8/A1: pass the globally-unique QR id as `xenditQrId` (primary match key) AND `externalId` (A4 fallback); store the raw `body` as `rawPayload` (A1/A2, schema field added in Plan 02 R1); wrap the runMutation in try/catch like grabfood:111-115 so a throw never becomes a 500.
6. `return new Response("OK", { status: 200 });` — applies to non-COMPLETED, matched, unmatched, and caught-error paths (retry semantics block above; staffreview I3).

Use `process.env.XENDIT_WEBHOOK_TOKEN` read directly (env-var-only secret, no DB row — diverges from grabfood's resolveHmacSecret). btoa not Buffer; static imports only (pitfall #8). Secret never logged.

Create `convex/integrations/qris/__tests__/webhookHandler.test.ts` (staffreview C6): a handler-level suite. Since `httpAction` cannot be invoked through `t.action(internal.*)` (Pitfall 5), test the handler by either (a) the convex-test http harness `t.fetch("/api/xendit/qr-payment", { method:"POST", headers, body })` if available in this project's convex-test version, or (b) extracting the handler body into a pure `processWebhook(deps, request)` where `deps.runMutation` is injectable and asserting the spy. Cover the 4 handler behaviors above: missing/invalid token → 401 + runMutation spy NOT called; valid + non-COMPLETED → 200 + spy not called; valid + COMPLETED → 200 + spy called once with the xenditQrId/externalId/rawPayload args; valid + COMPLETED returning {transitioned:false} → 200, no throw. Read convex/integrations/grabfood/__tests__ (if present) for the project's preferred httpAction test harness.

Run the R4a + handler tests — they MUST go GREEN.
  </action>
  <verify>
    <automated>npm run test -- convex/integrations/qris/__tests__/verifyToken.test.ts convex/integrations/qris/__tests__/webhookHandler.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `convex/integrations/qris/webhooks.ts` contains `export function verifyCallbackToken`
    - File contains `status: 401` and `request.headers.get("x-callback-token")`
    - File calls `recordPaidAndTransition` only after the token check, passing both `xenditQrId` and `externalId` and `rawPayload: body`
    - File wraps the `ctx.runMutation(` call in a `try`/`catch` and returns `status: 200` on the COMPLETED path (no 500 escape)
    - File reads `process.env.XENDIT_WEBHOOK_TOKEN` and does NOT call `resolveHmacSecret`
    - File does NOT return 200 before the token check (no grabfood skip-on-missing copy)
    - `convex/integrations/qris/__tests__/webhookHandler.test.ts` asserts: 401-path runMutation NOT called, COMPLETED-path runMutation called once, non-COMPLETED → no call, unmatched → 200/no throw
    - `npm run test -- convex/integrations/qris/__tests__/verifyToken.test.ts convex/integrations/qris/__tests__/webhookHandler.test.ts` exits 0 (R4a + handler GREEN)
  </acceptance_criteria>
  <done>Token verifier + webhook handler implemented; missing token → 401 with no mutation; COMPLETED → recordPaidAndTransition once (xenditQrId primary, rawPayload stored); runMutation wrapped so no 500; handler-level 401/COMPLETED/unmatched tests green; R4a green.</done>
</task>

<task type="auto">
  <name>Task 2: Register POST /api/xendit/qr-payment in convex/http.ts</name>
  <read_first>
    - convex/http.ts (lines 4-11 grabfood import block, 64-102 the 6 grabfood http.route registrations — the exact registration template)
    - 84-PATTERNS.md (http.ts block — import + route registration verbatim)
  </read_first>
  <action>
In `convex/http.ts`, add `import { handleXenditQrPayment } from "./integrations/qris/webhooks";` near the grabfood import block (lines 4-11). Then register the route alongside the grabfood routes (after line 102, before `export default http;`):
```typescript
http.route({
  path: "/api/xendit/qr-payment",
  method: "POST",
  handler: handleXenditQrPayment,
});
```
Add a short comment header (mirroring the grabfood `// ─── GrabFood Webhooks ───` banner) noting this is the Xendit QRIS payment webhook. Then run `npm run type-check` and `npm run build` (build also exercises the 650 kB cap — pitfall #16).
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `convex/http.ts` contains `import { handleXenditQrPayment }`
    - `convex/http.ts` contains `path: "/api/xendit/qr-payment"` with `method: "POST"`
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>Webhook route registered; type-check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → POST /api/xendit/qr-payment | unauthenticated HTTP endpoint receiving the paid signal |
| webhook handler → recordPaidAndTransition | external input mutates order state + reserves stock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-09 | Spoofing | forged webhook | mitigate | `verifyCallbackToken` constant-time compare of `x-callback-token` vs `XENDIT_WEBHOOK_TOKEN`; missing/invalid → 401 with NO mutation call and NO state change (does NOT copy grabfood skip-on-missing); asserted by the handler-level 401 test (C6) |
| T-84-10 | Information Disclosure | timing attack on token | mitigate | Constant-time XOR loop, never `===` |
| T-84-11 | Tampering / replay | duplicate COMPLETED delivery | mitigate | Handler delegates to recordPaidAndTransition whose `status !== PaymentReceived` guard makes replay a no-op (Plan 03 T-84-05) |
| T-84-12 | Information Disclosure | XENDIT_WEBHOOK_TOKEN | mitigate | Read only from `process.env` in the httpAction runtime; never logged, never client-exposed |
| T-84-13 | Tampering | unverified payload envelope (A1/A2) | mitigate | Parse defensively (`payload.data ?? payload`); store raw `body` as `rawPayload`; match by globally-unique `xenditQrId` first then `externalId` (A4/C8); lock the parser to one real Test-Mode callback before go-live |
| T-84-19 | Denial of Service | unmatchable/error payload → 500 retry loop | mitigate | runMutation wrapped in try/catch; unmatchable COMPLETED returns 200 (no infinite Xendit retries); documented retry semantics (staffreview C4/I3) |
</threat_model>

<verification>
- R4a token-compare test GREEN; handler-level 401/COMPLETED/non-COMPLETED/unmatched tests GREEN (C6)
- `npm run type-check` + `npm run build` pass (build exercises the bundle cap)
- Route registered; missing token path returns 401 with no mutation; unmatchable COMPLETED returns 200 (no 500)
- Secret read only from env; no dynamic import; no Buffer
</verification>

<success_criteria>
- Webhook authenticates with constant-time token compare; 401 on missing/invalid (handler-level tested)
- COMPLETED drives recordPaidAndTransition exactly once (idempotency from Plan 03); xenditQrId-primary match; rawPayload stored
- Defensive envelope parsing; runMutation wrapped so no 500; documented retry semantics
</success_criteria>

<output>
After completion, create `.planning/phases/84-qris-payment-integration/84-04-SUMMARY.md`
</output>
</content>
