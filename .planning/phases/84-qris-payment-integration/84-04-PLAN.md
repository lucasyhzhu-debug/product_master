---
phase: 84-qris-payment-integration
plan: 04
type: execute
wave: 4
depends_on: [84-03]
files_modified:
  - convex/integrations/qris/webhooks.ts
  - convex/http.ts
autonomous: true
requirements: [R4, R4a]
must_haves:
  truths:
    - "POST /api/xendit/qr-payment is registered in convex/http.ts"
    - "A missing or invalid x-callback-token returns HTTP 401 with no state change"
    - "A COMPLETED payload with a valid token records payment and drives the transition exactly once"
    - "The parser tolerates either reference_id or xenditQrId as the match key (A4) and stores the raw payload for early debugging (A1/A2)"
  artifacts:
    - path: "convex/integrations/qris/webhooks.ts"
      provides: "verifyCallbackToken pure fn + handleXenditQrPayment httpAction"
      contains: "verifyCallbackToken"
    - path: "convex/http.ts"
      provides: "POST /api/xendit/qr-payment route registration"
      contains: "/api/xendit/qr-payment"
  key_links:
    - from: "convex/integrations/qris/webhooks.ts"
      to: "recordPaidAndTransition"
      via: "ctx.runMutation(internal.qrisPayments.mutations.recordPaidAndTransition)"
      pattern: "recordPaidAndTransition"
    - from: "convex/http.ts"
      to: "handleXenditQrPayment"
      via: "http.route POST registration"
      pattern: "handleXenditQrPayment"
---

<objective>
Build the inbound Xendit webhook: the constant-time `x-callback-token` verifier (turns R4a GREEN), the `httpAction` handler that records payment and drives the idempotent transition, and its registration in `convex/http.ts` alongside the 6 GrabFood routes.

Purpose: This is the paid signal — the ONLY way an order reaches PaymentReceived via QRIS (the QR never reads "paid" on poll; spike-confirmed). Authentication (401 on bad token), idempotency, and honor-always-flag-mismatch all converge here.
Output: `convex/integrations/qris/webhooks.ts`, modified `convex/http.ts`.
</objective>

<execution_context>
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/workflows/execute-plan.md
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-qris-payment-integration/84-SPEC.md
@.planning/phases/84-qris-payment-integration/84-RESEARCH.md
@.planning/phases/84-qris-payment-integration/84-PATTERNS.md

<interfaces>
<!-- verifyCallbackToken is asserted by the R4a test (Plan 01). The handler calls the Plan 03 mutation. -->

verifyCallbackToken (webhooks.ts — copy verbatim from 84-PATTERNS.md):
```typescript
export function verifyCallbackToken(header: string | null, expected: string | undefined): boolean {
  if (!expected || !header) return false;            // missing config OR header → 401
  if (header.length !== expected.length) return false;
  let mismatch = 0;
  for (let i = 0; i < header.length; i++) mismatch |= header.charCodeAt(i) ^ expected.charCodeAt(i);
  return mismatch === 0;
}
```

Consumes (from Plan 03):
- internal.qrisPayments.mutations.recordPaidAndTransition({ externalId, amount, receiptId?, source? })

Spike-confirmed COMPLETED payload (A2 — confirm against a real callback early):
```jsonc
{ "status": "COMPLETED", "amount": 35000, "reference_id": "0521-001",
  "payment_details": { "receipt_id": "RRN...", "source": "DANA" } }
// Header: x-callback-token: <XENDIT_WEBHOOK_TOKEN>  (A1 — confirm exact header live)
```
</interfaces>

<analogs>
@convex/integrations/grabfood/webhooks.ts
@convex/http.ts
</analogs>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: webhooks.ts — verifyCallbackToken (R4a) + handleXenditQrPayment httpAction</name>
  <read_first>
    - convex/integrations/grabfood/webhooks.ts (lines 1-3 imports, 18-62 constant-time loop, 82-90 handler shape; PATTERNS.md flags the DIVERGENCE: grabfood returns valid:true on missing secret/sig — Xendit MUST return false → 401)
    - convex/integrations/qris/__tests__/verifyToken.test.ts (the R4a contract written in Plan 01)
    - 84-PATTERNS.md (webhooks.ts block — the full httpAction skeleton + the btoa-not-Buffer note)
    - 84-RESEARCH.md (Pattern 2 + Pitfall 4 missing-token-must-401; Open Questions A1/A2 — capture one real callback early, store raw payload)
  </read_first>
  <behavior>
    verifyCallbackToken (R4a):
    - null header → false; undefined expected → false; length mismatch → false; content mismatch → false; exact match → true
    handler:
    - invalid/missing token → Response 401, no mutation called
    - status COMPLETED + valid token → calls recordPaidAndTransition once
    - non-COMPLETED status → 200, no transition
  </behavior>
  <action>
Create `convex/integrations/qris/webhooks.ts`. Import `{ httpAction }` from `../../_generated/server` and `{ internal }` from `../../_generated/api`.

Export the pure `verifyCallbackToken(header, expected)` EXACTLY as the interfaces block (constant-time XOR loop, missing config/header → false). CRITICAL DIVERGENCE from grabfood: grabfood treats missing secret/signature as `valid:true` (skip) — do NOT copy that. For Xendit, missing token MUST → 401 (SPEC acceptance criterion R4a, Pitfall 4). Never use `===` on tokens (timing attack).

Export `handleXenditQrPayment = httpAction(async (ctx, request) => { ... })`:
1. `const body = await request.text();`
2. `const token = request.headers.get("x-callback-token");` — A1: this is Xendit's standard header; confirm against one real Test-Mode callback before go-live.
3. `if (!verifyCallbackToken(token, process.env.XENDIT_WEBHOOK_TOKEN)) return new Response("Unauthorized", { status: 401 });` — 401, NOT 200; NO state change before this check passes.
4. Parse: `let payload: any = {}; try { payload = JSON.parse(body); } catch {}`. A2: the real envelope may wrap as `{ event, data: {...} }` — defensively unwrap `payload.data ?? payload` before reading fields, and (A1/A2 mitigation) log/store the raw `body` on a debug path initially so the parser can be locked to the real shape.
5. `const evt = payload.data ?? payload;` then `if (evt.status === "COMPLETED") { await ctx.runMutation(internal.qrisPayments.mutations.recordPaidAndTransition, { externalId: evt.reference_id ?? evt.external_id ?? evt.qr_id, amount: evt.amount, receiptId: evt?.payment_details?.receipt_id, source: evt?.payment_details?.source }); }` — A4: pass whichever match key the callback carries (reference_id | external_id | qr id); the mutation matches by externalId then falls back to xenditQrId.
6. `return new Response("OK", { status: 200 });`

Use `process.env.XENDIT_WEBHOOK_TOKEN` read directly (env-var-only secret, no DB row — diverges from grabfood's resolveHmacSecret). btoa not Buffer; static imports only (pitfall #8). Secret never logged.

Run the R4a test — it MUST go GREEN.
  </action>
  <verify>
    <automated>npm run test -- convex/integrations/qris/__tests__/verifyToken.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `convex/integrations/qris/webhooks.ts` contains `export function verifyCallbackToken`
    - File contains `status: 401` and `request.headers.get("x-callback-token")`
    - File contains `recordPaidAndTransition` called only after the token check
    - File reads `process.env.XENDIT_WEBHOOK_TOKEN` and does NOT call `resolveHmacSecret`
    - File does NOT return valid/200 on missing token (no grabfood skip-on-missing copy)
    - `npm run test -- convex/integrations/qris/__tests__/verifyToken.test.ts` exits 0 (R4a GREEN)
  </acceptance_criteria>
  <done>Token verifier + webhook handler implemented; missing token → 401; COMPLETED → recordPaidAndTransition once; R4a green.</done>
</task>

<task type="auto">
  <name>Task 2: Register POST /api/xendit/qr-payment in convex/http.ts</name>
  <read_first>
    - convex/http.ts (lines 4-11 grabfood import block, 64-102 the 6 grabfood http.route registrations — the exact registration template)
    - 84-PATTERNS.md (http.ts block — import + route registration verbatim)
  </read_first>
  <action>
In `convex/http.ts`, add `import { handleXenditQrPayment } from "./integrations/qris/webhooks";` near the grabfood import block (lines 4-11). Then register the route alongside the grabfood routes (after line 102, before `export default http;`):
```typescript
http.route({
  path: "/api/xendit/qr-payment",
  method: "POST",
  handler: handleXenditQrPayment,
});
```
Add a short comment header (mirroring the grabfood `// ─── GrabFood Webhooks ───` banner) noting this is the Xendit QRIS payment webhook. Then run `npm run type-check` and `npm run build` (build also exercises the 650 kB cap — pitfall #16).
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `convex/http.ts` contains `import { handleXenditQrPayment }`
    - `convex/http.ts` contains `path: "/api/xendit/qr-payment"` with `method: "POST"`
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>Webhook route registered; type-check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → POST /api/xendit/qr-payment | unauthenticated HTTP endpoint receiving the paid signal |
| webhook handler → recordPaidAndTransition | external input mutates order state + reserves stock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-09 | Spoofing | forged webhook | mitigate | `verifyCallbackToken` constant-time compare of `x-callback-token` vs `XENDIT_WEBHOOK_TOKEN`; missing/invalid → 401 with NO state change (does NOT copy grabfood skip-on-missing) |
| T-84-10 | Information Disclosure | timing attack on token | mitigate | Constant-time XOR loop, never `===` |
| T-84-11 | Tampering / replay | duplicate COMPLETED delivery | mitigate | Handler delegates to recordPaidAndTransition whose `status !== PaymentReceived` guard makes replay a no-op (Plan 03 T-84-05) |
| T-84-12 | Information Disclosure | XENDIT_WEBHOOK_TOKEN | mitigate | Read only from `process.env` in the httpAction runtime; never logged, never client-exposed |
| T-84-13 | Tampering | unverified payload envelope (A1/A2) | mitigate | Parse defensively (`payload.data ?? payload`); store raw payload on a debug field early and lock the parser to one real Test-Mode callback before go-live; match by reference_id OR xenditQrId (A4) |
</threat_model>

<verification>
- R4a token-compare test GREEN
- `npm run type-check` + `npm run build` pass (build exercises the bundle cap)
- Route registered; missing token path returns 401 with no mutation
- Secret read only from env; no dynamic import; no Buffer
</verification>

<success_criteria>
- Webhook authenticates with constant-time token compare; 401 on missing/invalid
- COMPLETED drives recordPaidAndTransition exactly once (idempotency from Plan 03)
- Defensive envelope parsing + A4 dual-key match
</success_criteria>

<output>
After completion, create `.planning/phases/84-qris-payment-integration/84-04-SUMMARY.md`
</output>
