---
phase: 84-qris-payment-integration
plan: 03
type: execute
wave: 3
depends_on: [84-02]
files_modified:
  - convex/qrisPayments/mutations.ts
  - convex/qrisPayments/queries.ts
  - convex/qrisPayments/actions.ts
autonomous: true
requirements: [R2, R3, R4b, R4c, R6, R7]
must_haves:
  truths:
    - "createQrisInvoice is a raw action with token: v.string() and gates auth via an internal query running requireRole(ctx, token, [order_staff,manager,admin]); there is NO roles: option and NO protectedAction"
    - "createQrisInvoice rejects non-AwaitingPayment orders and writes nothing"
    - "createQrisInvoice expires any prior pending row and inserts exactly one new pending row"
    - "createQrisInvoice rejects finalTotal < 1500 and re-checks QRIS_ENABLED server-side"
    - "recordPaidAndTransition ALWAYS writes the qrisPayments row to paid (durably, before reserve), transitions AwaitingPayment->PaymentReceived exactly once, reserves stock once, and writes logStatusTransition"
    - "If reserveStockForOrderInternal throws, the paid record is NOT lost: row stays paid + needsReview:true with reviewReason 'stock reservation failed; payment recorded'"
    - "An unmatched COMPLETED webhook returns { transitioned: false } and throws nothing"
    - "A replayed COMPLETED webhook does not re-transition or double-reserve (status guard)"
    - "amount-mismatch AND/OR superseded QR is recorded paid AND flagged needsReview; when both apply the reason contains both substrings"
    - "Matching is primarily on globally-unique xenditQrId; externalId matching is scoped to the active pending row, never a blind .first() over all history"
    - "getActiveQrisPayment + getQrisConfig are callable by order_staff without crashing"
  artifacts:
    - path: "convex/qrisPayments/mutations.ts"
      provides: "insertPending, expirePrior, recordPaidAndTransition, decideWebhookOutcome"
      contains: "decideWebhookOutcome"
    - path: "convex/qrisPayments/queries.ts"
      provides: "getActiveQrisPayment + getQrisConfig (order_staff-safe) + getOrderForCreate internal auth+state query"
      contains: "getQrisConfig"
    - path: "convex/qrisPayments/actions.ts"
      provides: "createQrisInvoice action (token-auth via requireRole, guarded, supersede-on-regenerate)"
      contains: "createQrisInvoice"
  key_links:
    - from: "convex/qrisPayments/mutations.ts"
      to: "reserveStockForOrderInternal"
      via: "import from ../orders/mutations/inventoryIntegration"
      pattern: "reserveStockForOrderInternal"
    - from: "convex/qrisPayments/mutations.ts"
      to: "logStatusTransition"
      via: "import from ../orders/helpers/statusTransitions"
      pattern: "logStatusTransition"
    - from: "convex/qrisPayments/actions.ts"
      to: "requireRole via internal getOrderForCreate"
      via: "ctx.runQuery(internal.qrisPayments.queries.getOrderForCreate, { orderId, token })"
      pattern: "getOrderForCreate"
    - from: "convex/qrisPayments/actions.ts"
      to: "xenditProvider.createInvoice"
      via: "import from ../integrations/qris/xendit"
      pattern: "xenditProvider"
    - from: "convex/qrisPayments/queries.ts"
      to: "process.env.QRIS_ENABLED"
      via: "getQrisConfig reads env flag"
      pattern: "QRIS_ENABLED"
---

<objective>
Build the `qrisPayments` feature module: the idempotent, payment-durable paid-transition mutation (the highest-risk surface), the supersede-on-regenerate insert/expire mutations, the order_staff-safe queries (active row + flag/NMID config + internal auth+state query), and the token-authenticated `createQrisInvoice` action. Turns R2, R3, R4b, R4c tests GREEN.

Purpose: This is the transactional core. Idempotency lives here (status guard, not webhook dedup). Authentication uses the project's `token` + `requireRole` convention (there is NO `protectedAction` — staffreview C1). Payment durability survives reserve failures (SPEC R4 "always honored" — staffreview C3). The role superset (pitfall #19) and the env-flag defense-in-depth (D-01) are enforced here.
Output: `mutations.ts`, `queries.ts`, `actions.ts` under `convex/qrisPayments/`.
</objective>

<execution_context>
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/workflows/execute-plan.md
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-qris-payment-integration/84-SPEC.md
@.planning/phases/84-qris-payment-integration/84-RESEARCH.md
@.planning/phases/84-qris-payment-integration/84-PATTERNS.md
@.planning/phases/84-qris-payment-integration/84-CONTEXT.md

<interfaces>
<!-- Contracts this module exposes. The webhook (Plan 04) and frontend (Plan 05) import these. -->

decideWebhookOutcome (pure, exported from mutations.ts — compositional reason per staffreview C7):
```typescript
export function decideWebhookOutcome(
  order: { status: string },
  row: { amount: number; status: string },
  payload: { amount: number },
): { transition: boolean; recordPaid: true; needsReview: boolean; reason?: string }
```

internal mutations (mutations.ts):
- internal.qrisPayments.mutations.insertPending({ orderId, externalId, xenditQrId, qrString, amount, expiresAt })
- internal.qrisPayments.mutations.expirePrior({ orderId })
- internal.qrisPayments.mutations.recordPaidAndTransition({ xenditQrId?, externalId, amount, receiptId?, source?, rawPayload? }) → { transitioned: boolean }

queries (queries.ts):
- api.qrisPayments.queries.getActiveQrisPayment({ orderId }) — protectedQuery roles [order_staff,manager,admin]
- api.qrisPayments.queries.getQrisConfig({}) — protectedQuery roles [order_staff,manager,admin] → { enabled, qrisNmid, merchantName }
- internal.qrisPayments.queries.getOrderForCreate({ orderId, token }) — runs requireRole, returns { status, finalTotal, orderNumber } (staffreview C1 + I4)

action (actions.ts):
- api.qrisPayments.actions.createQrisInvoice({ orderId, token }) — returns { xenditQrId, qrString, expiresAt }
  (raw `action` + `token: v.string()`; NO `roles:` option, NO protectedAction — staffreview C1)

reserveStockForOrderInternal (existing — import, do NOT reimplement):
```typescript
// convex/orders/mutations/inventoryIntegration.ts:219
export async function reserveStockForOrderInternal(
  ctx: MutationCtx, args: { orderId: Id<"orders">; locationId?: ...; skipStockCheck?: boolean }
): Promise<{ reserved: number; shortages: ReservationShortage[] }>
```

logStatusTransition (existing — import, do NOT reimplement):
```typescript
// convex/orders/helpers/statusTransitions.ts:149
export async function logStatusTransition(
  ctx: MutationCtx, orderId: Id<"orders">, fromStatus: string, toStatus: string,
  reason: string, triggeredBy?: "user" | "kitchen" | "system", userId?: Id<"users">
): Promise<void>
```

requireRole (existing — import, do NOT reimplement):
```typescript
// convex/lib/auth.ts:128
export async function requireRole(ctx: QueryCtx | MutationCtx, token: string, allowedRoles: UserRole[]): Promise<Doc<"users">>
```
</interfaces>

<analogs>
@convex/orders/mutations/statusUpdates.ts
@convex/businessSettings/queries.ts
@convex/bigsellerOrders/actions.ts
@convex/bigsellerOrders/queries.ts
</analogs>

<role-table>
canAccessOrders resolves to { order_staff, manager, admin } (src/lib/types.ts ~760/783/806).
EVERY query/mutation/action reachable from OrderDetail MUST authorize the set { order_staff, manager, admin }.
businessSettings.get is ["admin","manager"] — NOT safe; that is why getQrisConfig folds in qrisNmid.
The action has NO `roles:` option (no protectedAction exists) — it takes `token` and calls requireRole inside the internal getOrderForCreate query (mirror convex/bigsellerOrders/actions.ts:20-26 + queries.ts:240-246 requireAdminByToken).
</role-table>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mutations.ts — decideWebhookOutcome (compositional reason) + payment-durable idempotent recordPaidAndTransition + insertPending/expirePrior</name>
  <read_first>
    - convex/orders/mutations/statusUpdates.ts (lines 108-210 — the canonical pattern: confirmedAt write at :123, the try/catch around reserveStockForOrderInternal at :141-170 that REVERTS status to oldStatus on throw, and logStatusTransition at :202-210. This is the exact analog. Do NOT reuse updatePayment:230 or moveForward:410-431)
    - convex/orders/mutations/inventoryIntegration.ts (lines 219-249 — reserveStockForOrderInternal signature + the {reserved:0} short-circuits)
    - convex/orders/helpers/statusTransitions.ts (lines 149-157 — logStatusTransition signature; use triggeredBy "system" for the webhook actor)
    - convex/orders/helpers/customerResolution.ts (lines 55-97 — orderNumber MMDD-NNN resets DAILY → externalId is NOT globally unique; staffreview C8)
    - convex/qrisPayments/__tests__/webhookTransition.test.ts + mutations.test.ts (the R2/R4b/R4c contracts written in Plan 01 — reserved-qty-once replay, both-true reason, unmatched no-op)
    - 84-PATTERNS.md (mutations.ts block) + 84-RESEARCH.md (Pattern 3 + Blast Radius — guard before reserve; idempotency by status not dedup)
  </read_first>
  <behavior>
    decideWebhookOutcome (pure):
    - AwaitingPayment + amount match + row pending → { transition: true, recordPaid: true, needsReview: false }
    - order PaymentReceived → { transition: false } (caller treats as replay no-op)
    - payload.amount !== row.amount → needsReview: true, reason contains "amount", recordPaid: true
    - row.status === "expired" → needsReview: true, reason contains "superseded", recordPaid: true
    - BOTH (amount mismatch AND row expired) → needsReview: true, reason contains BOTH "amount" and "superseded" (staffreview C7)
    recordPaidAndTransition (integration, via t.run):
    - no row matches xenditQrId or externalId → return { transitioned: false }, throw nothing (staffreview C4)
    - first COMPLETED call: row → paid (durable), order → PaymentReceived, reserveStockForOrderInternal called once, logStatusTransition written
    - reserve throws → paid row PRESERVED + needsReview:true reviewReason "stock reservation failed; payment recorded" (staffreview C3)
    - replayed call: order stays PaymentReceived, NO second reservation
    insertPending: one pending row linked to orderId; expirePrior flips prior pending → expired
  </behavior>
  <action>
Create `convex/qrisPayments/mutations.ts` using `internalMutation` from `../_generated/server` (typed `MutationCtx` — never `ctx: { db: any }`). Import `reserveStockForOrderInternal` from `../orders/mutations/inventoryIntegration` and `logStatusTransition` from `../orders/helpers/statusTransitions` (static imports, pitfall #8).

Export the pure `decideWebhookOutcome(order, row, payload)`. Build the reason COMPOSITIONALLY so both signals survive when both apply (staffreview C7):
```typescript
export function decideWebhookOutcome(order, row, payload) {
  const amountMismatch = payload.amount !== row.amount;
  const superseded = row.status === "expired";
  const needsReview = amountMismatch || superseded;
  const parts: string[] = [];
  if (amountMismatch) parts.push(`amount ${payload.amount} != expected ${row.amount}`);
  if (superseded) parts.push("QR was superseded/expired before payment");
  const reason = parts.length ? parts.join("; ") : undefined;
  const transition = order.status === "AwaitingPayment";
  return { transition, recordPaid: true as const, needsReview, reason };
}
```

`insertPending({ orderId, externalId, xenditQrId, qrString, amount, expiresAt })`: insert one `qrisPayments` row with `provider: "xendit"`, `status: "pending"`.

`expirePrior({ orderId })`: query `by_order` index for the order; patch every `status: "pending"` row to `status: "expired"` (index-scan-then-patch shape from customerResolution.ts:62-68). Run this BEFORE inserting a new pending row.

`recordPaidAndTransition({ xenditQrId?, externalId, amount, receiptId?, source?, rawPayload? })`:
1. MATCH (staffreview C8 — externalId MMDD-NNN is per-day, not unique): if `xenditQrId` is provided, find the row by it FIRST (globally unique). Else (or if not found) query `by_externalId` and select the ACTIVE row — prefer the most recent `status: "pending"` (or otherwise the most recent by `_creationTime`); NEVER a blind `.first()` over all historical rows.
2. NO-MATCH (staffreview C4): if no row found, `console.log` a diagnostic (no secrets) and `return { transitioned: false }` — throw nothing.
3. Load the order via `row.orderId`.
4. Compute `const outcome = decideWebhookOutcome(order, row, { amount });`.
5. ALWAYS record paid on the row, durably, BEFORE the transition/reserve (SPEC R4 — staffreview C3):
   `await ctx.db.patch(row._id, { status: "paid", paidAt: Date.now(), receiptId, source, ...(rawPayload ? { rawPayload } : {}), ...(outcome.needsReview ? { needsReview: true, reviewReason: outcome.reason } : {}) });`
6. IDEMPOTENCY GUARD: `if (order.status === "PaymentReceived") return { transitioned: false };` (replay no-op — payment already recorded above).
7. `if (order.status !== "AwaitingPayment") return { transitioned: false };` (recorded, not transitioned).
8. `await ctx.db.patch(order._id, { status: "PaymentReceived", confirmedAt: Date.now() });` (mirror statusUpdates.ts:123). Do NOT set isKitchenVisible — `computeIsKitchenVisible("PaymentReceived")` is false, identical to AwaitingPayment (staffreview C3 note).
9. RESERVE WITH FAILURE DURABILITY (staffreview C3 — mirror statusUpdates.ts:141-170 try/catch revert, but keep the paid record):
```typescript
try {
  await reserveStockForOrderInternal(ctx, { orderId: order._id });
} catch (err) {
  // payment is already recorded paid (step 5); revert the order status so the
  // shortage is visible, but DO NOT lose the payment — flag it for review.
  await ctx.db.patch(order._id, { status: order.status /* oldStatus = AwaitingPayment */, confirmedAt: undefined });
  await ctx.db.patch(row._id, { needsReview: true, reviewReason: "stock reservation failed; payment recorded" });
  await logStatusTransition(ctx, order._id, "PaymentReceived", "AwaitingPayment", "QRIS payment received but stock reservation failed", "system", undefined);
  return { transitioned: false };
}
```
   (Capture `oldStatus = order.status` before step 8 and use it in the revert. The paid row from step 5 is never reverted.)
10. `await logStatusTransition(ctx, order._id, "AwaitingPayment", "PaymentReceived", "QRIS payment received", "system", undefined);` (mirror statusUpdates.ts:202-210).
11. `return { transitioned: true };`.

Run the R2 + R4b/R4c tests — they MUST go GREEN.
  </action>
  <verify>
    <automated>npm run test -- convex/qrisPayments/__tests__/webhookTransition.test.ts convex/qrisPayments/__tests__/mutations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `convex/qrisPayments/mutations.ts` contains `export function decideWebhookOutcome` and builds `reason` via `parts.join(` (compositional — both signals survive)
    - File patches the row to `status: "paid"` BEFORE the `order.status === "PaymentReceived"` guard (grep: the `paidAt:` patch line precedes the PaymentReceived-guard line)
    - File contains `order.status === "PaymentReceived"` guard returning before reserve
    - File wraps `reserveStockForOrderInternal(ctx,` in a `try`/`catch` and the catch contains `"stock reservation failed; payment recorded"`
    - File contains `logStatusTransition(` imported from `../orders/helpers/statusTransitions`
    - File returns `{ transitioned: false }` on the no-match path and does not throw (grep: a `return { transitioned: false }` exists before the order load)
    - File matches by `xenditQrId` first and scopes externalId matching to a pending/most-recent row (no bare `.first()` over `by_externalId` without a pending filter)
    - File does NOT contain `moveForward`, `updatePayment`, `isKitchenVisible`, or `ctx: { db: any }`
    - File contains `internalMutation`
    - `npm run test -- convex/qrisPayments/__tests__/webhookTransition.test.ts convex/qrisPayments/__tests__/mutations.test.ts` exits 0 (R2/R4b/R4c GREEN)
  </acceptance_criteria>
  <done>Payment-durable idempotent transition: row recorded paid before reserve, reserve-failure keeps the payment + flags review + reverts status + logs, replay is a no-op, unmatched is a safe no-op, both-true reason composes, match locked to xenditQrId; R2/R4b/R4c green.</done>
</task>

<task type="auto">
  <name>Task 2: queries.ts — getActiveQrisPayment + getQrisConfig (order_staff-safe) + getOrderForCreate internal auth+state query</name>
  <read_first>
    - convex/businessSettings/queries.ts (protectedQuery shape — NOTE its roles are ["admin","manager"], the UNSAFE set we must NOT copy)
    - convex/bigsellerOrders/queries.ts (lines 240-246 — requireAdminByToken internalQuery: the token→requireRole gate the action calls; staffreview C1)
    - 84-PATTERNS.md (queries.ts block — getActiveQrisPayment + getQrisConfig with roles ["order_staff","manager","admin"] and the env-flag read)
    - 84-CONTEXT.md (D-01 env-var flag; RESEARCH correction #2 — fold qrisNmid into getQrisConfig so the dialog never calls useBusinessSettings)
    - 84-RESEARCH.md (Open Question 2 — QRIS_ENABLED convention is `=== "true"`)
  </read_first>
  <action>
Create `convex/qrisPayments/queries.ts`.

`getActiveQrisPayment` (`protectedQuery` from `../lib/functions`): `roles: ["order_staff","manager","admin"]`, `args: { orderId: v.id("orders") }`. Query `by_order`; return the MOST RECENT row by `_creationTime` among `{ pending, paid }` (exclude only `expired`) so a freshly-paid row wins over an older expired one and the dialog flips (staffreview I2). Return null if none.

`getQrisConfig` (`protectedQuery`): `roles: ["order_staff","manager","admin"]`, `args: {}`. Return `{ enabled: process.env.QRIS_ENABLED === "true", qrisNmid: settings?.qrisNmid ?? null, merchantName: settings?.businessName ?? null }` where `settings = await ctx.db.query("businessSettings").first()`. Queries CAN read `process.env` (only `fetch` is action-restricted). This folds NMID into an order_staff-safe path so the dialog never calls `useBusinessSettings()` (pitfall #19, RESEARCH correction #2).

`getOrderForCreate` (`internalQuery` from `../_generated/server`): `args: { orderId: v.id("orders"), token: v.string() }`. FIRST `await requireRole(ctx, args.token, ["order_staff","manager","admin"])` (import from `../lib/auth`) — this is the action's auth gate (staffreview C1, mirror bigsellerOrders/queries.ts:240-246). Then load the order and return `{ status, finalTotal, orderNumber }` (staffreview I4 — the action needs orderNumber as the externalId match key and has no ctx.db).

The role superset is MANDATORY on the two protected queries — `useSessionQuery` subscribes on mount regardless of dialog state; a narrower set crashes order_staff (3rd recurrence, pitfall #19).
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `convex/qrisPayments/queries.ts` contains `getActiveQrisPayment`, `getQrisConfig`, and `getOrderForCreate`
    - Both protected queries contain `roles: ["order_staff", "manager", "admin"]` (NOT ["admin","manager"])
    - `getActiveQrisPayment` excludes only `expired` (returns most recent among pending/paid) — file does not filter to `pending` only
    - `getOrderForCreate` calls `requireRole(ctx, args.token, ["order_staff", "manager", "admin"])` and returns `orderNumber`
    - File contains `process.env.QRIS_ENABLED === "true"`
    - File contains `qrisNmid` and `businessName` reads from businessSettings
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>Order_staff-safe active-row (pending|paid most-recent) + config queries exist; flag read server-side; NMID folded in; getOrderForCreate gates auth via requireRole and returns orderNumber.</done>
</task>

<task type="auto">
  <name>Task 3: actions.ts — createQrisInvoice (token auth via requireRole, flag re-check, state guard, supersede, insert)</name>
  <read_first>
    - convex/bigsellerOrders/actions.ts (lines 20-26 — raw `action({ args: { token: v.string(), ... }, handler })` that gates auth by `ctx.runQuery(internal.*.requireAdminByToken, { token })`; actions cannot touch ctx.db and there is NO protectedAction; staffreview C1)
    - convex/qrisPayments/__tests__/createInvoice.test.ts (the R3 contract written in Plan 01)
    - 84-PATTERNS.md (actions.ts block) + 84-CONTEXT.md (D-01 defense-in-depth — action re-checks QRIS_ENABLED)
    - 84-SPEC.md (R3 — guard AwaitingPayment + finalTotal >= 1500; fresh call supersedes prior pending)
  </read_first>
  <action>
Create `convex/qrisPayments/actions.ts` using `action` from `../_generated/server` (a RAW action — there is NO `protectedAction`, so DO NOT pass a `roles:` option; staffreview C1). `createQrisInvoice` args: `{ orderId: v.id("orders"), token: v.string() }`. Handler:
1. Re-check the flag server-side (defense-in-depth, D-01): if `process.env.QRIS_ENABLED !== "true"` throw `new Error("QRIS is not enabled")`.
2. AUTH + state read in one internal query: `const order = await ctx.runQuery(internal.qrisPayments.queries.getOrderForCreate, { orderId, token });` — getOrderForCreate runs `requireRole(ctx, token, ["order_staff","manager","admin"])` and throws on unauthorized BEFORE returning state (staffreview C1; mirror bigsellerOrders/actions.ts:24). Actions have no ctx.db, so all auth+reads go through this query.
3. Guard: if `order.status !== "AwaitingPayment"` throw `new Error("Order is not awaiting payment")` (write nothing).
4. Guard: if `order.finalTotal < 1500` throw `new Error("Amount below QRIS minimum (1500 IDR)")`.
5. `const { xenditQrId, qrString, expiresAt } = await xenditProvider.createInvoice({ orderNumber: order.orderNumber, finalTotal: order.finalTotal })` (import `xenditProvider` from `../integrations/qris/xendit`, static — pitfall #8). Strip/never forward the `token` to this external call.
6. `await ctx.runMutation(internal.qrisPayments.mutations.expirePrior, { orderId })` (supersede any prior pending row).
7. `await ctx.runMutation(internal.qrisPayments.mutations.insertPending, { orderId, externalId: order.orderNumber, xenditQrId, qrString, amount: order.finalTotal, expiresAt })`.
8. return `{ xenditQrId, qrString, expiresAt }`.

Step ordering matters: a throw in steps 1-4 (incl. the requireRole throw inside getOrderForCreate) happens BEFORE any write (R3: "throws, writes nothing"). Test via the extracted internal mutation/query through `t.run`, NOT `t.action(internal.*)` (Pitfall 5). Run the R3 test — it MUST go GREEN.
  </action>
  <verify>
    <automated>npm run test -- convex/qrisPayments/__tests__/createInvoice.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `convex/qrisPayments/actions.ts` contains `createQrisInvoice` with `token: v.string()` in its args
    - File does NOT contain `roles:` and does NOT reference `protectedAction`
    - File calls `ctx.runQuery(internal.qrisPayments.queries.getOrderForCreate, { orderId, token })` (auth via requireRole inside that query)
    - File contains `process.env.QRIS_ENABLED !== "true"` re-check
    - File contains `order.status !== "AwaitingPayment"` and `< 1500` guards before any runMutation
    - File imports `xenditProvider` from `../integrations/qris/xendit`
    - File calls `expirePrior` before `insertPending`
    - `npm run test -- convex/qrisPayments/__tests__/createInvoice.test.ts` exits 0 (R3 GREEN)
  </acceptance_criteria>
  <done>Token-authenticated (requireRole via getOrderForCreate) guarded create action supersedes prior pending row, inserts one new pending row, re-checks the flag; no roles:/protectedAction; R3 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| order-detail client → createQrisInvoice action | authenticated staff request (token) to mint a QR |
| webhook (Plan 04) → recordPaidAndTransition | untrusted external paid signal mutates order state + reserves stock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-04 | Elevation of Privilege | createQrisInvoice | mitigate | The raw `action` takes `token: v.string()` and authorizes via `internal.qrisPayments.queries.getOrderForCreate` which runs `requireRole(ctx, token, ["order_staff","manager","admin"])` (no `protectedAction` exists; mirror bigsellerOrders/actions.ts:24) + server-side state guard (`status===AwaitingPayment`) + flag re-check (D-01). Token never forwarded to Xendit |
| T-84-05 | Tampering / replay | recordPaidAndTransition | mitigate | `order.status === "PaymentReceived"` guard BEFORE `reserveStockForOrderInternal` — replayed COMPLETED is a no-op; idempotency by status, not webhook dedup |
| T-84-06 | Tampering | amount integrity | mitigate | `decideWebhookOutcome` flags `needsReview` (compositional reason) when payload.amount != row.amount; payment honored but flagged, never silently trusted |
| T-84-07 | Denial of Service | order_staff page mount | mitigate | All queries reachable from OrderDetail use the role superset so manager/order_staff mounts don't throw ConvexError (pitfall #19, 3rd recurrence) |
| T-84-08 | Tampering | stock double-reservation | mitigate | Single `reserveStockForOrderInternal` call gated behind the status guard; verified by the R4b replay test asserting reserved qty decremented exactly once |
| T-84-17 | Repudiation / Data-loss | payment lost on reserve failure | mitigate | Row recorded `paid` durably BEFORE reserve; reserve-throw catch keeps the paid row + needsReview + reverts order status + logStatusTransition (SPEC R4 always-honored; staffreview C3) |
| T-84-18 | Data-correctness | wrong order matched | mitigate | Match primarily on globally-unique `xenditQrId`; externalId (per-day MMDD-NNN) matching scoped to the active pending row, never blind `.first()` (staffreview C8) |
</threat_model>

<verification>
- R2, R3, R4b, R4c tests GREEN
- `npm run type-check` passes
- Action authorizes via token + requireRole (no roles:/protectedAction); queries use roles ["order_staff","manager","admin"]
- Replay test proves no double-reservation (reserved qty decremented once); reserve-failure keeps the paid record; unmatched is a safe no-op
- No dynamic import; no Buffer; no ctx:{db:any}; no isKitchenVisible
</verification>

<success_criteria>
- Payment-durable, idempotent recordPaidAndTransition with needsReview flagging + audit log + reserve-failure handling
- Supersede-on-regenerate insert/expire; xenditQrId-primary matching
- Token-authenticated createQrisInvoice (requireRole) with flag defense-in-depth
- Order_staff-safe queries (R7 no-crash guarantee at the data layer)
</success_criteria>

<output>
After completion, create `.planning/phases/84-qris-payment-integration/84-03-SUMMARY.md`
</output>
</content>
