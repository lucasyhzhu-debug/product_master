---
phase: 84-qris-payment-integration
plan: 05
type: execute
wave: 4
depends_on: [84-03]
files_modified:
  - src/hooks/convex/useQris.ts
  - src/components/orders/QrisChargeDialog.tsx
  - src/pages/OrderDetail.tsx
autonomous: false
requirements: [R5, R6, R7]
must_haves:
  truths:
    - "The Charge via QRIS button appears ONLY when order.status === AwaitingPayment AND QRIS_ENABLED is on"
    - "useQrisConfig()?.enabled is read at the TOP of OrderDetail (unconditional hook call), not inside a conditional (pitfall #9)"
    - "The dialog renders a scannable QR + amount + optional NMID/merchant + 30-min countdown"
    - "The dialog flips to a paid state reactively (no manual refresh) when the row.status becomes paid"
    - "An expired QR offers Generate New QR, which supersedes the prior pending row"
    - "The page mounts for order_staff, manager, and admin without crashing (R7)"
    - "A needsReview badge shows inline next to payment status with the reason on hover"
    - "createQrisInvoice is invoked via useAction + an explicit token (NOT useSessionAction, which does not exist in this project)"
  artifacts:
    - path: "src/hooks/convex/useQris.ts"
      provides: "useQrisConfig, useActiveQrisPayment, useCreateQrisInvoice"
      contains: "useQrisConfig"
    - path: "src/components/orders/QrisChargeDialog.tsx"
      provides: "single derived-state-machine dialog (active/paid/expired/error/loading)"
      contains: "QRCodeSVG"
    - path: "src/pages/OrderDetail.tsx"
      provides: "Charge via QRIS button (gated) + needsReview badge"
      contains: "QrisChargeDialog"
  key_links:
    - from: "src/components/orders/QrisChargeDialog.tsx"
      to: "getActiveQrisPayment"
      via: "useActiveQrisPayment subscription"
      pattern: "useActiveQrisPayment"
    - from: "src/pages/OrderDetail.tsx"
      to: "useQrisConfig"
      via: "button visibility gate (read at page top)"
      pattern: "useQrisConfig"
    - from: "src/hooks/convex/useQris.ts"
      to: "createQrisInvoice"
      via: "useAction + explicit token"
      pattern: "useAction"
---

<objective>
Build the frontend: the `useQris` hooks, the single reactive `QrisChargeDialog` (derived state machine per D-03 + 84-UI-SPEC.md), and the OrderDetail wiring (gated button + needsReview badge). Turns R5/R7 RTL tests GREEN and completes the end-to-end loop.

Purpose: The reactive paid flip is the headline UX guarantee — staff watch the screen, the webhook confirms, the dialog transitions with no click. The role superset (Plan 03 queries) keeps order_staff mounts from crashing (R7, pitfall #19). The action is invoked the way every other action in this codebase is: `useAction` + an explicit auth `token` (there is NO `useSessionAction` wrapper — staffreview C2).
Output: `useQris.ts`, `QrisChargeDialog.tsx`, modified `OrderDetail.tsx`.
</objective>

<execution_context>
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/workflows/execute-plan.md
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-qris-payment-integration/84-SPEC.md
@.planning/phases/84-qris-payment-integration/84-UI-SPEC.md
@.planning/phases/84-qris-payment-integration/84-PATTERNS.md
@.planning/phases/84-qris-payment-integration/84-CONTEXT.md

<interfaces>
<!-- Backend contracts consumed (from Plan 03). qrcode.react v4 NAMED export. -->
<!-- staffreview C2: queries use useSessionQuery (convex-helpers); the ACTION uses useAction + an
     explicit token (like useBigSeller.ts:77/94, useGrabFood.ts:62). There is NO useSessionAction. -->

Hooks (useQris.ts):
```typescript
import { useSessionQuery } from "convex-helpers/react/sessions";
import { useAction } from "convex/react";
import { useAuth } from "...";   // however OrderDetail/useBigSeller obtains the session token

export function useQrisConfig() { return useSessionQuery(api.qrisPayments.queries.getQrisConfig, {}); }
// → { enabled: boolean; qrisNmid: string | null; merchantName: string | null } | undefined

export function useActiveQrisPayment(orderId) { return useSessionQuery(api.qrisPayments.queries.getActiveQrisPayment, { orderId }); }
// → qrisPayments row | null | undefined (undefined = loading)

export function useCreateQrisInvoice() {
  const action = useAction(api.qrisPayments.actions.createQrisInvoice);
  const { user } = useAuth();
  const token = user?.token ?? "";
  return (orderId) => action({ orderId, token });   // action takes { orderId, token }
}
// → (orderId) => Promise<{ xenditQrId, qrString, expiresAt }>
```

qrcode.react v4 (NAMED export, NOT default):
```tsx
import { QRCodeSVG } from "qrcode.react";
<QRCodeSVG value={row.qrString} size={256} level="M" />
```
</interfaces>

<analogs>
@src/components/orders/ConfirmationDialog.tsx
@src/components/orders/PaymentMethodButtons.tsx
@src/hooks/convex/useBusinessSettings.ts
@src/hooks/convex/useBigSeller.ts
@src/hooks/convex/useGrabFood.ts
</analogs>

<ui-contract>
84-UI-SPEC.md is the authoritative visual/copy contract (checker-approved). Key locks:
- 4 type sizes (28/20/16/14px), 2 weights (400/500). No text-xs.
- min-h-[44px] mobile touch targets (copy from PaymentMethodButtons:67).
- Paid = --color-status-success + CheckCircle2; needsReview/expired/countdown-low = --color-status-warning. No destructive token.
- QR: black-on-white, wrapped in a fixed white card (`bg-white p-4 rounded-xl`) mandatory even in dark mode. No brand tint.
- Copy strings verbatim from the Copywriting Contract table (titles, bodies, CTAs).
- Countdown = local setInterval from expiresAt; MUST clear on unmount.
- States derived from subscription, not local toggles.
- NOTE (staffreview R2): Convex queries do NOT re-run on env-var change — flipping QRIS_ENABLED only takes effect after a client re-subscribe/reload. Don't expect a live flip without reload during verification.
</ui-contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useQris.ts hooks (useAction + token for the action, NOT useSessionAction)</name>
  <read_first>
    - src/hooks/convex/useBusinessSettings.ts (lines 1-25 — useSessionQuery shape for the queries)
    - src/hooks/convex/useBigSeller.ts (lines 76-95 — useAction for an action) + src/hooks/convex/useGrabFood.ts (lines 58-67 — useAction + `const token = user?.token ?? ""` from useAuth)
    - 84-PATTERNS.md (useQris.ts block)
  </read_first>
  <action>
Create `src/hooks/convex/useQris.ts`. Import `{ useSessionQuery }` from `convex-helpers/react/sessions`, `{ useAction }` from `convex/react`, `{ api }` from `../../../convex/_generated/api`, and the session/auth hook the other hooks use to read the token (match `useGrabFood.ts:59-60` — `const { user } = useAuth(); const token = user?.token ?? "";`). Export:
- `useQrisConfig()` → `useSessionQuery(api.qrisPayments.queries.getQrisConfig, {})`
- `useActiveQrisPayment(orderId: Id<"orders">)` → `useSessionQuery(api.qrisPayments.queries.getActiveQrisPayment, { orderId })`
- `useCreateQrisInvoice()` → grabs `useAction(api.qrisPayments.actions.createQrisInvoice)` + the session token, and returns a callback `(orderId) => action({ orderId, token })`. Do NOT use `useSessionAction` — it does not exist in this project and is used nowhere in `src/` (staffreview C2); the action takes an explicit `token` arg (Plan 03).

`undefined` from useSessionQuery means loading (Convex pitfall #2) — consumers must handle it.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/convex/useQris.ts` contains `useQrisConfig`, `useActiveQrisPayment`, `useCreateQrisInvoice`
    - File uses `useAction(api.qrisPayments.actions.createQrisInvoice)` and passes a `token` to the action call
    - File does NOT contain `useSessionAction`
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>Three QRIS hooks exported; queries via useSessionQuery; the action via useAction + explicit token.</done>
</task>

<task type="auto">
  <name>Task 2: QrisChargeDialog.tsx — single derived-state-machine dialog</name>
  <read_first>
    - src/components/orders/ConfirmationDialog.tsx (lines 1-14 imports, 44-57 Dialog shell; text-sm font-medium label convention; PATTERNS.md analog)
    - src/components/orders/PaymentMethodButtons.tsx (line 67 — min-h-[44px] touch target to copy)
    - src/components/orders/__tests__/QrisChargeDialog.test.tsx (the R5/R7 contract written in Plan 01 — incl. the active-state render that exercises QRCodeSVG, staffreview I5)
    - 84-UI-SPEC.md (full visual + copy contract — authoritative)
    - 84-PATTERNS.md (QrisChargeDialog block — QRCodeSVG named import, white card, derived states)
  </read_first>
  <action>
Create `src/components/orders/QrisChargeDialog.tsx` as a SINGLE component with state DERIVED from `useActiveQrisPayment(orderId)` (D-03 — not local toggles). Import `{ QRCodeSVG }` from `qrcode.react` (v4 NAMED export, NOT default), `Dialog`/`DialogContent`/`DialogHeader`/`DialogTitle`/`DialogDescription`/`DialogFooter` from `@/components/ui/dialog`, `Button`, `CheckCircle2`/`QrCode` from `lucide-react`, and `useCreateQrisInvoice` from `../../hooks/convex/useQris`.

On open, if there is no active pending row, call `useCreateQrisInvoice()(orderId)` to mint one (show the loading/Generating state meanwhile). Derive state:
- **loading**: `row === undefined` or action in flight → Skeleton sized to 256px + "Generating QR code…"
- **active**: `row.status === "pending"` AND `Date.now() < row.expiresAt` → white card `<div className="bg-white p-4 rounded-xl">` wrapping `<QRCodeSVG value={row.qrString} size={256} level="M" />`, title "Scan to Pay with QRIS", "Amount due" label + IDR amount (28px `text-3xl font-medium`, `Rp` dot-thousands), optional NMID/merchant block (merchant name line + "NMID: {qrisNmid}" line, both `text-sm text-muted-foreground` centered) ONLY when `config.qrisNmid` is set, and "Expires in mm:ss" countdown (local `setInterval` from `row.expiresAt`, warning color ≤ 5:00, MUST `clearInterval` on unmount).
- **paid**: `row.status === "paid"` (reactive, NO manual refresh) → green success panel (`--color-status-success`) + `CheckCircle2`, title "Payment Received", body "Rp {amount} received via {source}. The order has moved to Payment Received." (omit "via {source}" if source absent). If `row.needsReview`, render a warning sub-panel: "This payment needs review — {reviewReason}. The payment was still recorded."
- **expired**: `row.status === "expired"` OR countdown hits 0 → warning state, title "QR Code Expired", body "This QR code is no longer valid. Generate a new one to charge the customer again.", primary "Generate New QR" button calling `useCreateQrisInvoice()(orderId)` (supersedes prior server-side; NON-destructive — primary variant, NOT AlertDialog/window.confirm).
- **error**: action throws / subscription error → "Couldn't generate QR code" + body + "Try Again".

"Close" outline button in DialogFooter in all states. Use 4 sizes / 2 weights, NO text-xs, min-h-[44px] mobile touch targets. QR black-on-white, no brand tint. All copy verbatim from 84-UI-SPEC Copywriting Contract.

Run the R5/R7 test — it MUST go GREEN (all 3 roles mount; active state renders the QR; reactive paid flip).
  </action>
  <verify>
    <automated>npm run test -- src/components/orders/__tests__/QrisChargeDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/orders/QrisChargeDialog.tsx` contains `import { QRCodeSVG } from "qrcode.react"` (named, not default)
    - File contains `bg-white p-4 rounded-xl` (white QR card) and `useActiveQrisPayment`
    - File contains `clearInterval` (countdown cleanup on unmount)
    - File contains the verbatim titles "Scan to Pay with QRIS", "Payment Received", "QR Code Expired", and CTA "Generate New QR"
    - File contains no `text-xs` and no `window.confirm`/`window.prompt`
    - `npm run test -- src/components/orders/__tests__/QrisChargeDialog.test.tsx` exits 0 (R5/R7 GREEN, incl. the active-state QRCodeSVG render)
  </acceptance_criteria>
  <done>Single reactive dialog with active/paid/expired/error/loading states matching the UI contract; tests green (active state exercises QRCodeSVG).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Wire button (gate read at page top) + needsReview badge into OrderDetail and verify end-to-end against Xendit Test Mode</name>
  <read_first>
    - src/pages/OrderDetail.tsx (the import block + the top-of-component hook section, and the AwaitingPayment-gated WhatsApp card ~lines 344-364 — the conditional-render pattern to mirror; PATTERNS.md analog)
    - 84-CONTEXT.md (D-02 — needsReview is an INDICATOR only, no list/filter/resolve flow; D-03 button placement near PaymentMethodButtons)
    - 84-VALIDATION.md (Manual-Only Verifications — Test-Mode loop, real callback envelope A1/A2)
    - CLAUDE.md pitfall #9 (hooks before conditional returns)
  </read_first>
  <what-built>
Claude wires `QrisChargeDialog` into `src/pages/OrderDetail.tsx`. CRITICAL (staffreview R4, pitfall #9): call `const qrisConfig = useQrisConfig();` at the TOP of the component with the other hooks — an UNCONDITIONAL hook call, never inside an `if`/after an early return. Then derive `const showQrisButton = order?.status === 'AwaitingPayment' && qrisConfig?.enabled === true;` and conditionally RENDER (not conditionally call the hook). The "Charge via QRIS" button (primary variant, `QrCode` leading icon, min-h-[44px]) mounts near the existing payment UI, rendered ONLY when `showQrisButton` (button ABSENT, not disabled, otherwise — same conditional-render pattern as the WhatsApp card at :345). A `needsReview` badge (D-02) mounts inline next to the order's payment status using `Badge` (warning palette) + `Tooltip` showing the full `reviewReason` — INDICATOR ONLY, no list/filter/resolve flow (deferred to Phase 77). Then `npm run type-check`, `npm run test` (full suite), and `npm run build` (exercises the 650 kB cap).

Claude will also: (a) set the dev Convex env vars `XENDIT_API_KEY` (Test Mode key), `XENDIT_WEBHOOK_TOKEN`, `QRIS_ENABLED=true`; (b) point the Xendit dashboard callback URL at the deployed dev httpAction; (c) capture ONE real Test-Mode webhook to confirm the exact header name (A1), the body envelope (A2), AND which payload key carries the globally-unique QR id that maps to `xenditQrId` (staffreview C8), then lock the parser/match key to it if it diverges from the spike's simulate shape.
  </what-built>
  <how-to-verify>
1. Open an order in `AwaitingPayment` in dev with `QRIS_ENABLED=true`. Confirm the "Charge via QRIS" button appears. Set `QRIS_ENABLED` unset/false, then RELOAD the page (queries don't re-run on env change without re-subscribe — staffreview R2) — confirm the button is ABSENT.
2. Click "Charge via QRIS" — confirm a scannable QR renders with the amount, the 30-min countdown, and (if `qrisNmid` set) the NMID/merchant block.
3. In the Xendit dashboard, simulate a Test-Mode payment for that QR. WITHOUT refreshing, confirm the dialog flips to the green "Payment Received" state and the order status becomes `PaymentReceived` (check the order moved + stock reserved once).
4. Simulate the SAME payment again (replay) — confirm the order stays `PaymentReceived` and stock is NOT reserved a second time.
5. Let a QR expire (or wait past expiresAt) — confirm the dialog offers "Generate New QR" and regenerating supersedes the prior row.
6. Log in as order_staff (not admin) and open the same order — confirm NO page crash (R7).
7. Confirm the captured real webhook's header name is `x-callback-token` (A1), the body envelope matches the parser (A2), and which key carries the globally-unique QR id mapped to `xenditQrId` (C8); note any divergence and lock the match key.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. wrong header name, envelope wrapping, button visible when flag off, no reactive flip, order_staff crash, wrong QR-id key).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → getQrisConfig / getActiveQrisPayment | order_staff/manager/admin read paths; must not crash on mount |
| browser → createQrisInvoice action | authenticated staff mint request (token passed; re-validated server-side via requireRole) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-14 | Denial of Service | order_staff page mount | mitigate | Dialog reads NMID via order_staff-safe `getQrisConfig` (Plan 03), NEVER `useBusinessSettings()` (admin/manager-only would crash order_staff — pitfall #19, 3rd recurrence). R7 RTL test mounts all 3 roles. useQrisConfig called unconditionally at page top (pitfall #9) |
| T-84-15 | Elevation of Privilege | button visibility | mitigate | Button gated on `QRIS_ENABLED` flag (UI gate) AND the create action takes a `token` and re-checks flag + role (requireRole) + state server-side (defense-in-depth, D-01) — the button is not the only gate |
| T-84-16 | Information Disclosure | secrets in client | mitigate | No secret reaches the browser; `getQrisConfig` returns only a boolean + NMID + merchant name; API key/webhook token stay in Convex env |
</threat_model>

<verification>
- R5/R7 RTL tests GREEN (incl. active-state QRCodeSVG render); full `npm run test` suite GREEN; `npm run build` passes (650 kB cap)
- useQrisConfig read unconditionally at page top (no hooks-order violation)
- Button absent when flag off (after reload) OR status != AwaitingPayment
- Reactive paid flip confirmed in dev Test-Mode loop (human checkpoint)
- order_staff mounts without crashing
- Real webhook header (A1) + envelope (A2) + QR-id key (C8) confirmed and match key locked
</verification>

<success_criteria>
- Gated "Charge via QRIS" button (gate hook at page top) + reactive dialog + needsReview indicator badge
- Action invoked via useAction + token (no useSessionAction)
- End-to-end dev Test-Mode loop: simulate-payment → order reaches PaymentReceived (no refresh)
- Replay does not double-reserve; expired offers regenerate; order_staff safe
</success_criteria>

<output>
After completion, create `.planning/phases/84-qris-payment-integration/84-05-SUMMARY.md`
</output>
</content>
