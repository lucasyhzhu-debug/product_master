---
phase: 17-unified-dispatch-planner-3rd-outlet
plan: 05
type: execute
wave: 4
depends_on: ["17-04"]
files_modified:
  - src/App.tsx
  - src/pages/index.ts
  - docs/CHANGELOG.md
  - docs/SCHEMA.md
  - docs/API_REFERENCE.md
autonomous: true

must_haves:
  truths:
    - "Dispatch Planner page is accessible via /dispatch-planner route with proper auth guard"
    - "Page is exported from pages barrel and imported in App.tsx"
    - "CHANGELOG, SCHEMA, and API_REFERENCE docs are updated"
    - "npm run build passes (full production build)"
  artifacts:
    - path: "src/App.tsx"
      provides: "Route for /dispatch-planner with ProtectedRoute"
      contains: "dispatch-planner"
    - path: "src/pages/index.ts"
      provides: "DispatchPlanner export"
      contains: "DispatchPlanner"
    - path: "docs/CHANGELOG.md"
      provides: "Phase 17 changelog entry"
      contains: "Dispatch Planner"
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/DispatchPlanner.tsx"
      via: "Route element with ProtectedRoute wrapper"
      pattern: "DispatchPlanner"
    - from: "src/pages/index.ts"
      to: "src/pages/DispatchPlanner.tsx"
      via: "barrel export"
      pattern: "DispatchPlanner"
---

<objective>
Wire the Dispatch Planner page into the app routing, add navigation, update page exports, and update documentation (CHANGELOG, SCHEMA, API_REFERENCE).

Purpose: Make the planner accessible in the app and document all changes.
Output: Routed page + updated docs.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/App.tsx
@src/pages/index.ts
@docs/CHANGELOG.md
@docs/SCHEMA.md
@docs/API_REFERENCE.md
@.planning/phases/17-unified-dispatch-planner-3rd-outlet/17-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire routing and page exports</name>
  <files>src/App.tsx, src/pages/index.ts</files>
  <action>
**1. src/pages/index.ts:**
Add export: `export { DispatchPlanner } from './DispatchPlanner';`

**2. src/App.tsx:**
- Import `DispatchPlanner` in the destructured import from `@/pages` (add to the existing import block at the top)
- Add route inside the `<Layout />` section (same area as K3Mart cockpit route):
```tsx
<Route
  path="dispatch-planner"
  element={
    <ProtectedRoute requiredPermission="canAccessDashboard">
      <DispatchPlanner />
    </ProtectedRoute>
  }
/>
```
- Place it near the k3mart-cockpit route (around line 258-262)
- Uses `canAccessDashboard` permission (Manager, Admin) -- same as K3Mart cockpit per access control table in CLAUDE.md

**3. Navigation (if applicable):**
Check if there's a sidebar/nav component that lists pages. If so, add "Dispatch Planner" link with a LayoutGrid or CalendarRange icon from lucide-react. If navigation is auto-generated from routes or if there's no explicit nav menu, skip this step.
  </action>
  <verify>npm run type-check passes. Route `/dispatch-planner` exists in App.tsx. DispatchPlanner is in pages/index.ts exports.</verify>
  <done>Dispatch Planner page accessible at /dispatch-planner with manager/admin auth guard.</done>
</task>

<task type="auto">
  <name>Task 2: Update documentation and run final build</name>
  <files>docs/CHANGELOG.md, docs/SCHEMA.md, docs/API_REFERENCE.md</files>
  <action>
**1. docs/CHANGELOG.md:**
Add entry at the top (below header):
```markdown
## [v1.2.0] - 2026-02-17

### Added
- **Unified Dispatch Planner** (`/dispatch-planner`): Multi-channel weekly production planning page combining Direct Sales, GoFood, K3Mart, and Other Consignment channels into a single rolling 7-day grid
  - Channel configuration with drag-to-reorder priorities and per-channel commission rates
  - Segmented capacity bar per day showing demand waterfall across channels (default 200 balls/day, configurable)
  - Direct orders auto-populate at due date with production window visualization (due date - 2 days)
  - Editable cells with auto-save on blur for future days; read-only past days with actual sales data
  - Collapsible channel groups with subtotals
  - Consignment outlet management with product mapping (name + price)
  - Inventory simulation: manual BOM walk checking packaging and production components against current stock
- **3rd GoFood outlet (Tamtem)**: Merchant ID G958262444 syncs automatically on existing cron schedule alongside Goldfinch and Crystal

### Schema Changes
- Added `dispatchPlans` table -- multi-channel dispatch plan cells
- Added `dispatchChannelConfig` table -- channel priority, commission rates, colors
- Added `dispatchConsignmentOutlets` table -- configurable consignment outlets with product mapping
- Added `dispatchPlannerSettings` table -- daily capacity and planner configuration

### Backend
- `convex/dispatchPlanner/queries.ts` -- 5 queries (unified weekly plan, channel config, settings, consignment outlets, inventory simulation)
- `convex/dispatchPlanner/mutations.ts` -- 8 mutations (seed, save cell, channel config CRUD, settings, consignment CRUD)
- `convex/dispatchPlanner/helpers.ts` -- Pure business logic (redistribution, pre-fill, date helpers)
```

**2. docs/SCHEMA.md:**
Add documentation for the 4 new tables (dispatchPlans, dispatchChannelConfig, dispatchConsignmentOutlets, dispatchPlannerSettings) with field descriptions, indexes, and relationships. Follow existing format in the file.

**3. docs/API_REFERENCE.md:**
Add documentation for the new queries and mutations under a "Dispatch Planner" section. List each function with args and return types. Follow existing format.

**4. Run final build:**
Execute `npm run build` to verify the complete build passes (TypeScript + Vite).
  </action>
  <verify>`npm run build` passes successfully. docs/CHANGELOG.md contains "Dispatch Planner" entry. docs/SCHEMA.md documents 4 new tables. docs/API_REFERENCE.md documents dispatch planner endpoints.</verify>
  <done>Full production build passes. All documentation updated with Phase 17 changes.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes (production build, not just type-check)
2. `/dispatch-planner` route exists with auth guard
3. CHANGELOG documents all new features and schema changes
4. SCHEMA.md documents 4 new tables
5. API_REFERENCE.md documents dispatch planner queries and mutations
</verification>

<success_criteria>
- [ ] `npm run build` passes
- [ ] Route `/dispatch-planner` accessible with canAccessDashboard permission
- [ ] CHANGELOG.md updated with v1.2.0 entry
- [ ] SCHEMA.md updated with 4 new tables
- [ ] API_REFERENCE.md updated with dispatch planner API
</success_criteria>

<output>
After completion, create `.planning/phases/17-unified-dispatch-planner-3rd-outlet/17-05-SUMMARY.md`
</output>
