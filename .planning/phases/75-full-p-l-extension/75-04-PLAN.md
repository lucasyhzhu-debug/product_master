---
phase: 75
plan: 04
type: execute
wave: 3
depends_on: [75-01, 75-02, 75-03]
files_modified:
  - docs/CHANGELOG.md
  - docs/ROADMAP.md
  - docs/API_REFERENCE.md
autonomous: false
requirements: [FIN-01, FIN-02]
tags: [verification, docs, phase-close]
must_haves:
  truths:
    - "npm run type-check passes"
    - "npm run lint passes"
    - "npm run build succeeds"
    - "npm run test passes (full suite)"
    - "All 14 Wave-0 tests are GREEN"
    - "docs/CHANGELOG.md has a new Phase 75 entry"
    - "docs/ROADMAP.md shows FIN-01 and FIN-02 as Complete"
    - "docs/API_REFERENCE.md documents new WeekData fields (capExAmount, freeCashFlow, opexExcludingDA, depreciationAmortization, fcfMarginPercent) and gapAnalysis.missingReversals"
    - "Human smoke-check on /financials confirms canonical EBITDA-first layout + zero-CapEx helper text + per-channel Contribution Margin label"
    - "Verification runs on gsd/phase-75-* branch (not main, not another phase branch)"
  artifacts:
    - path: "docs/CHANGELOG.md"
      provides: "Phase 75 delivery entry"
      contains: "Phase 75"
    - path: "docs/ROADMAP.md"
      provides: "FIN-01/FIN-02 marked complete"
      contains: "FIN-01"
    - path: "docs/API_REFERENCE.md"
      provides: "New WeekData fields documented"
      contains: "capExAmount"
  key_links:
    - from: "docs/CHANGELOG.md Phase 75 entry"
      to: "docs/ROADMAP.md FIN-01/FIN-02 status"
      via: "consistent dating + requirement ID references"
      pattern: "FIN-01"
---

<objective>
Gate + documentation close-out for Phase 75. Run the full verification suite
(type-check, lint, build, test), perform a human smoke-check on `/financials` per the
manual-only verifications in VALIDATION.md, and update the 3 mandatory docs (CHANGELOG,
ROADMAP, API_REFERENCE) before merging the phase branch to main.

Purpose: Lock phase delivery behind automated AND human gates, update project-level
traceability so FIN-01 and FIN-02 flip from Pending → Complete in REQUIREMENTS.md's
traceability table (via ROADMAP update).
Output: 3 doc files updated, full test suite green, human smoke sign-off.
</objective>

<execution_context>
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/workflows/execute-plan.md
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-full-p-l-extension/75-CONTEXT.md
@.planning/phases/75-full-p-l-extension/75-VALIDATION.md
@.planning/phases/75-full-p-l-extension/75-00-SUMMARY.md
@.planning/phases/75-full-p-l-extension/75-01-SUMMARY.md
@.planning/phases/75-full-p-l-extension/75-02-SUMMARY.md
@.planning/phases/75-full-p-l-extension/75-03-SUMMARY.md
@docs/CHANGELOG.md
@docs/ROADMAP.md
@docs/API_REFERENCE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full verification gate — branch check, type-check, lint, build, test</name>
  <files>(no files modified — verification commands only)</files>
  <read_first>
    - .planning/phases/75-full-p-l-extension/75-VALIDATION.md (sampling rules, full-suite expectation)
    - .planning/phases/75-full-p-l-extension/75-00-SUMMARY.md through 75-03-SUMMARY.md (confirm all prior plans reported pass)
  </read_first>
  <action>
Run each command in sequence. Each MUST exit 0. Capture output for the SUMMARY.

```bash
# 0. Branch verification (R4 / CLAUDE.md Rule 12) — must be on phase-75 branch
git branch --show-current | grep -q "gsd/phase-75" || { echo "ERROR: Not on phase-75 branch"; exit 1; }

# 1. Type-check
npm run type-check

# 2. Lint
npm run lint

# 3. Full unit test suite — all 14 Wave-0 tests GREEN + existing suite preserved
npm run test

# 4. Production build — Vite + tsc -b
npm run build

# 5. Regression guard: confirm no other UI reads opex expecting 6150/6160 to be inside
grep -rn "\.opex\b" src/pages src/components | grep -v "financials/" || echo "No other opex consumers — safe"
```

If any command fails:
- Identify the failing file from output
- Fix in-place (do not mark this task complete until ALL commands pass)
- Re-run the full sequence

Expected green state after Plans 00–03 shipped:
- `npm run test`: total test count = (existing pre-Phase-75 count) + 14 new (or +15 with R3 Direction-B test). No failures.
- `npm run build`: bundle completes, no size regression beyond vite-plugin-bundlesize tolerance
  (NB per MEMORY.md `feedback_vendor_bundle_cap.md`: if a heavy vendor dep was added, bump cap
  in vite.config.ts — unlikely here since no new deps)

Record counts + duration in 75-04-SUMMARY.md.
  </action>
  <acceptance_criteria>
    - Runs on `gsd/phase-75-*` branch (not main, not another phase branch)
    - `npm run type-check` exits 0
    - `npm run lint` exits 0
    - `npm run test` exits 0; total test count increased by at least 14 compared to pre-Phase-75 baseline (+15 if R3 Direction-B test applied)
    - `npm run build` exits 0; dist/ produced; no vite-plugin-bundlesize errors
    - Regression grep confirms no non-financials consumer of `data.opex` that would break on the 6150/6160 filter
    - All commands succeed WITHOUT `--no-verify` or other bypass flags
  </acceptance_criteria>
  <verify>
    <automated>git branch --show-current | grep -q "gsd/phase-75" && npm run type-check && npm run lint && npm run test && npm run build</automated>
  </verify>
  <done>Branch verified. All 4 automated gates green. Test count +14 (or +15 with R3). Build dist/ produced. No unexpected opex consumers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human smoke-check on /financials</name>
  <what-built>
    Full Phase 75 delivery: canonical EBITDA-first P&L layout with CapEx and Free Cash Flow rows,
    per-channel rows stopping at Contribution Margin (renamed from Gross Margin), D/A extracted
    into its own row between EBITDA and EBIT, DataQualityPanel surfacing missingReversals gap,
    CSV export matching on-screen layout.
  </what-built>
  <how-to-verify>
    Dev server is running (`npm run dev` in one terminal + `npx convex dev` in another).

    0. **Pre-verification — deployment order (I3):**
       In normal CI (push to `main` → GitHub Action deploys Convex → Vercel rebuilds), the
       order is automatic. For manual deploys: run `npx convex logs --prod | head -20` and
       confirm the latest deployment includes Phase 75 functions BEFORE triggering any
       Vercel rebuild. If frontend ships before backend, `data.current.capExAmount` is
       `undefined` and the CapEx row renders "Rp NaN".

    1. **Canonical layout (D-07):**
       - Navigate to http://localhost:5173/financials (login as manager or admin)
       - Select a period with known activity (e.g. a recent week with at least 1 fixed asset acquisition and some D/A posted)
       - Verify row order top-to-bottom: Revenue → (deductions) → Net Revenue → COGS → Contribution Margin / Gross Profit → (OpEx items) → Total Operating Expenses (excl. D/A) → EBITDA → EBITDA Margin % → Depreciation & Amortization → EBIT (Operating Profit) → EBIT Margin % → (Other) → Net Income → Net Margin % → CapEx (Fixed Asset Acquisitions) → FREE CASH FLOW → FCF Margin %
       - D/A appears EXACTLY ONCE (not duplicated above EBIT and below EBITDA)

    2. **D/A tooltip (D-08, D-09):**
       - Hover on "Depreciation & Amortization" label
       - Expected tooltip: "Depreciation: Rp X | Amortization: Rp Y" (only appears if both amounts are non-zero)

    3. **Zero-CapEx rendering (D-14):**
       - Switch to a period with NO asset acquisitions (e.g. pre-Phase-60 period or a quiet week)
       - Verify CapEx row STILL renders with `Rp 0` and muted helper text "No asset acquisitions this period"
       - **R5 sanity check:** Confirm rendered text is `Rp 0` (not `-Rp 0` or `(Rp 0)`) — PLRow formatting at zero with `isNegative invertColor` flags set should produce a clean zero, not a signed-or-parenthesised zero.
       - Verify FCF row = Net Income + D/A (no CapEx subtraction visible)

    4. **FCF formula tooltip (D-13):**
       - Hover on "FREE CASH FLOW" label
       - Expected tooltip: "Free Cash Flow = Net Income + Depreciation & Amortization − CapEx"

    5. **Per-channel scope (D-10, D-11):**
       - Click any channel row in the Revenue section to expand
       - Verify the sub-row shows label "Contribution Margin" (NOT "Gross Margin")
       - Verify NO OpEx, D/A, CapEx, or FCF appears in the expanded channel panel
       - Verify the COGS breakdown sub-row still appears unchanged

    6. **DataQualityPanel (D-15):**
       - If the period has no missingReversals: verify the panel reports "All clear" or shows only pre-existing checks
       - If testing with seeded data: verify the "N converted expense(s) missing reversal JE" section renders with an error-tinted icon and a "Review in Expenses" link

    7. **CSV export (D-16):**
       - Click the "Download CSV" button on /financials
       - Open the downloaded CSV in Excel/Sheets
       - Verify row order matches step 1 (EBITDA appears above D/A, above EBIT, above NI, above CapEx, above Free Cash Flow)
       - Verify channel column is "All" for all rows below "Gross Profit / Contribution Margin"
       - Verify no row labeled just "Gross Profit" (it must be renamed)
       - Verify no row labeled just "Total Operating Expenses" without "(excl. D/A)" suffix

    Report PASS / FAIL for each of the 7 checkpoints (steps 1–7, step 0 is a pre-verification gate). If any FAIL, identify which Plan (01/02/03) owns the fix.
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or list failing checkpoint numbers with descriptions.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update docs/CHANGELOG.md, docs/ROADMAP.md, docs/API_REFERENCE.md</name>
  <files>docs/CHANGELOG.md, docs/ROADMAP.md, docs/API_REFERENCE.md</files>
  <read_first>
    - docs/CHANGELOG.md (latest entries — follow the existing format; typically `## [Phase N] — YYYY-MM-DD` headers with sub-sections Added/Changed/Fixed/Docs)
    - docs/ROADMAP.md (find Phase 75 section — mark requirements complete)
    - docs/API_REFERENCE.md (find Income Statement query section if present; otherwise add one for the new fields)
    - .planning/phases/75-full-p-l-extension/75-00-SUMMARY.md through 75-03-SUMMARY.md (for accurate change descriptions)
    - .planning/REQUIREMENTS.md (FIN-01 / FIN-02 definitions — copy language for docs)
  </read_first>
  <action>
1. **docs/CHANGELOG.md** — add a new entry at the top (below any active unreleased section, or as a new phase entry per repo convention):

```markdown
## Phase 75 — Full P&L Extension — 2026-04-XX

### Added
- Income Statement extends from Net Income through Depreciation & Amortization, CapEx,
  to Free Cash Flow, with FCF = Net Income + D/A − CapEx formula surfaced via tooltip (FIN-01)
- CapEx (Fixed Asset Acquisitions) row always renders — zero-period shows "No asset
  acquisitions this period" helper text to preserve bridge visibility (D-14)
- DataQualityPanel surfaces converted-expense reversal JE gaps
  (`gapAnalysis.missingReversals`) to catch potential P&L double-counts (D-15)
- New `helperText` prop on PLRow for muted below-label notes
- Wave 0 test coverage: 14 new tests across 4 files
  (incomeStatement-capex, incomeStatement-gap-missingReversals, csvExport, ChannelRow)

### Changed
- P&L layout reorganized to canonical EBITDA-first order:
  Revenue → Net Revenue → COGS → Contribution Margin → OpEx (excl. D/A) → EBITDA → D/A → EBIT → Other → Net Income → CapEx → FCF (D-07)
- OpEx row split into "Operating Expenses (excl. D/A)" + dedicated
  "Depreciation & Amortization" row; D/A appears exactly once (D-08, D-09)
- Per-channel row label renamed from "Gross Margin" to "Contribution Margin";
  per-channel rows explicitly stop at Contribution Margin — no OpEx/D/A/CapEx/FCF
  allocation (D-10, D-11)
- Company-level "Gross Profit" row label updated to reflect Contribution Margin naming
- `generateIncomeStatementCSV` extended with new rows matching on-screen layout
  (D-16); channel column is "All" for all rows below Contribution Margin

### Technical
- `WeekData` query response gains: `opexExcludingDA`, `depreciationAmortization`,
  `capExAmount`, `freeCashFlow`, `fcfMarginPercent`
- `gapAnalysis` gains `missingReversals` array
- `opex` array in response now excludes codes 6150 and 6160 (D/A lives in its own fields)
- `totalOpEx` preserved unchanged (inclusive of D/A) for back-compat with consumers
- No schema migration; no new indexes; `fixedAssets` scanned in-memory at current scale
- `convex/reports/incomeStatement.ts` extended with 2 parallel queries in the main Promise.all

### Requirements Closed
- FIN-01: Income Statement extends through D/A, CapEx, to FCF — delivered
- FIN-02: Per-channel breakdown flows through Contribution Margin — delivered
```

Update the date placeholder to the actual merge date.

2. **docs/ROADMAP.md** — find Phase 75 section (likely labeled `### Phase 75: Full P&L Extension`). Update:
   - Add `- [x]` prefix on each plan's line (00, 01, 02, 03, 04)
   - Update `**Plans**: X/X plans complete` (should be `5/5`)
   - Add a Status: Complete note with date (e.g. `**Completed:** 2026-04-XX`)
   - Update the main progress table at the bottom:
     ```
     | 75. Full P&L Extension | v2.0 | 5/5 | Complete | 2026-04-XX |
     ```

3. **docs/API_REFERENCE.md** — find the `getIncomeStatement` / `getWeeklyIncomeStatement` query section. Add new response field documentation:

```markdown
## getIncomeStatement (Phase 75 extension)

Response fields added in Phase 75:

| Field | Type | Description |
|-------|------|-------------|
| `current.opexExcludingDA` | number | Operating expenses excluding depreciation and amortization (IDR) |
| `current.depreciationAmortization` | number | Sum of depreciation (code 6150) and amortization (code 6160) OpEx lines (IDR) |
| `current.capExAmount` | number | Sum of `fixedAssets.cost` where `acquisitionDate` falls in `[periodStart, periodEnd)` (IDR) |
| `current.freeCashFlow` | number | `netIncome + depreciationAmortization − capExAmount` (IDR) |
| `current.fcfMarginPercent` | number \| null | `freeCashFlow / totalGross × 100`, or null if totalGross is zero |
| `current.opex[]` | array | Operating expense accounts. **Changed in Phase 75:** codes 6150 and 6160 excluded (D/A rendered separately) |
| `current.gapAnalysis.missingReversals[]` | array | Converted expenses (Phase 71) whose linked journal entry is not marked `isReversed: true` — indicates potential P&L double-count |

Deltas gain: `opexExcludingDA`, `depreciationAmortization`, `capExAmount`, `freeCashFlow`, `fcfMarginPp`.

Example:
\`\`\`typescript
const stmt = useQuery(api.reports.incomeStatement.getIncomeStatement, {
  periodStart, periodEnd,
});
// stmt.current.capExAmount === sum of fixedAssets.cost in period
// stmt.current.freeCashFlow === stmt.current.netIncome + stmt.current.depreciationAmortization - stmt.current.capExAmount
\`\`\`
```

4. Verify doc writes with `git diff docs/`. Confirm all 3 files updated.
  </action>
  <acceptance_criteria>
    - `docs/CHANGELOG.md` contains a new entry with heading matching `## Phase 75` (grep-verifiable)
    - `docs/CHANGELOG.md` entry mentions `FIN-01` and `FIN-02` (grep-verifiable)
    - `docs/CHANGELOG.md` entry mentions `Free Cash Flow` and `CapEx` (grep-verifiable)
    - `docs/ROADMAP.md` Phase 75 section has plans marked `- [x]` for 00, 01, 02, 03, 04 (grep-verifiable with `grep -A 20 "Phase 75" docs/ROADMAP.md | grep -c "- \[x\]"` should return ≥ 5)
    - `docs/ROADMAP.md` main progress table row for Phase 75 shows `5/5` and `Complete` (grep-verifiable)
    - `docs/API_REFERENCE.md` contains new field names: `capExAmount`, `freeCashFlow`, `opexExcludingDA`, `depreciationAmortization`, `fcfMarginPercent`, `missingReversals` (grep-verifiable)
    - `git diff --stat docs/` shows exactly 3 files modified (or newly created sections)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Phase 75" docs/CHANGELOG.md && grep -c "capExAmount\|freeCashFlow\|missingReversals" docs/API_REFERENCE.md && grep -A 30 "Phase 75:" docs/ROADMAP.md | grep -c "\[x\]"</automated>
  </verify>
  <done>3 docs updated. CHANGELOG has Phase 75 entry. ROADMAP has 5/5 marked complete. API_REFERENCE documents new fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification commands → CI | `npm run build` is the mandatory merge gate per CLAUDE.md. No `--no-verify` or bypass allowed. |
| Docs → repository | Docs are plain markdown, no code execution path. |
| Human smoke-check → user judgment | Sign-off is captured in the checkpoint `resume-signal`; no automatic auth gate. |
| CI pipeline ordering (Convex deploy → Vercel rebuild) → production parity | Normal CI handles ordering automatically. Manual deploys (e.g. `npx convex deploy` + `vercel --prod`) can race; the Task 2 pre-verification step 0 documents the check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-04-01 | Tampering | CHANGELOG entry could omit breaking changes | mitigate | Acceptance criteria require explicit mention of: `opex` array no longer contains 6150/6160 (downstream consumers must be aware); `gapAnalysis` gains new field (consumer-side destructuring may need updates). CHANGELOG draft in Task 3 includes these explicitly. |
| T-75-04-02 | Repudiation | Phase 75 completion status recorded only in ROADMAP, not in REQUIREMENTS.md | accept | REQUIREMENTS.md's traceability table is authoritative but manually maintained. A future automation phase could auto-sync, but for now ROADMAP change + CHANGELOG entry provide paper trail. |
| T-75-04-03 | Tampering | Frontend ships before backend in a manual deploy — UI reads `data.current.capExAmount === undefined` → "Rp NaN" | mitigate | Task 2 `<how-to-verify>` step 0 documents the deployment-order check via `npx convex logs --prod`. Normal CI pipeline (`.github/workflows/convex-deploy.yml` triggers Vercel only after Convex deploy succeeds) handles this automatically. Manual deploys follow the documented step 0. |

No high-severity threats. Documentation gap mitigated by explicit acceptance criteria listing breaking changes.
</threat_model>

<verification>
- All 4 automated commands from Task 1 exit 0 (branch check + type-check + lint + test + build)
- Human sign-off on Task 2 (7 checkpoints + step 0 pre-verification reported)
- `git diff docs/` shows exactly the 3 expected files modified
- Phase SUMMARYs (00-04) all exist in `.planning/phases/75-full-p-l-extension/`
</verification>

<success_criteria>
- [ ] `git branch --show-current` matches `gsd/phase-75-*`
- [ ] `npm run type-check` + `npm run lint` + `npm run test` + `npm run build` all green
- [ ] Human smoke-check on /financials reported PASS for all 7 checkpoints (plus step 0 pre-verification)
- [ ] `docs/CHANGELOG.md` updated with Phase 75 entry (dated)
- [ ] `docs/ROADMAP.md` Phase 75 marked Complete (5/5 plans) and main progress table updated
- [ ] `docs/API_REFERENCE.md` documents new WeekData fields + missingReversals
- [ ] Phase 75 branch ready for merge to main
</success_criteria>

<output>
After completion, create `.planning/phases/75-full-p-l-extension/75-04-SUMMARY.md` documenting:
- Verification command outputs (test count, build size, lint warnings if any)
- Human smoke-check result per checkpoint
- Docs updated (file names + line counts of changes)
- Notes on any deferred items (e.g. Claude's Discretion items from CONTEXT.md that were explicitly chosen one way or the other)
- Merge readiness checklist: all items must be checked before branching to main merge
</output>

---

## Plan Update Log

**2026-04-17 — Staff review revision:**
- Applied I3: added a new "step 0" pre-verification in Task 2's `<how-to-verify>` block documenting deployment-order dependency (Convex backend must deploy BEFORE Vercel rebuild). Normal CI handles automatically; manual deploys follow the documented `npx convex logs --prod | head -20` check. Added a matching T-75-04-03 entry to the STRIDE threat register and a `Trust Boundaries` entry.
- Applied R4: added a branch verification command at the start of Task 1 (`git branch --show-current | grep -q "gsd/phase-75"`). Added a must-have truth "Verification runs on gsd/phase-75-* branch (not main, not another phase branch)" and a matching acceptance criterion. The `<verify>` automated command leads with the branch check.
- Applied R5: added a sanity-check sub-bullet in Task 2 step 3 (zero-CapEx) — "Confirm rendered text is `Rp 0` (not `-Rp 0` or `(Rp 0)`) — sanity-check PLRow formatting at zero with `isNegative invertColor` flags set."
- Added the Task 1 regression grep (no non-financials consumer of `data.opex`) from the staff review's section 10 suggestion.
