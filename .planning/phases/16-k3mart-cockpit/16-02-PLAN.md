---
phase: 16-k3mart-cockpit
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/components/k3martCockpit/WeeklyPlannerGrid.tsx
  - src/components/k3martCockpit/PlannerGridHeader.tsx
  - src/components/k3martCockpit/OutletPlannerRow.tsx
  - src/components/k3martCockpit/EditablePlannerCell.tsx
  - src/components/k3martCockpit/PlannerActionBar.tsx
  - src/components/k3martCockpit/WeekNavigator.tsx
  - src/pages/K3MartCockpit.tsx
autonomous: true

must_haves:
  truths:
    - "Weekly planner grid is outlet-first: primary rows are outlets, sub-rows are products per outlet"
    - "All outlets are always expanded (no collapse/expand per user decision)"
    - "Per-outlet subtotal row shown when outlet has more than 1 product"
    - "Column headers have 3 rows: day name, date, event name (holiday/commercial date)"
    - "Weekends, holidays, and sales dates have distinct color highlights in column headers"
    - "Current stock column appears before day columns showing K3Mart stock per product per outlet"
    - "Week navigation with big arrow buttons and prominent date display, default to current week via useState(getCurrentWeekNumber())"
    - "Color difference between current week and other weeks in navigation display"
    - "Copy last week button in PlannerActionBar (above/below grid) copies all saved values from previous week"
    - "Row totals per product, column totals (daily production targets), and grand total row"
    - "Auto-save on blur for editable cells (no batch save button)"
    - "Auto-suggest quantities pre-fill cells based on weekday/weekend/holiday patterns"
    - "Draft vs confirmed days have visual distinction in the grid"
    - "Per-day confirm buttons are in PlannerGridHeader (per column, not in PlannerActionBar)"
    - "Editing a confirmed day changes button to 'Update Kitchen'"
    - "Unsaved changes warning on navigation away"
    - "Only K3Mart-mapped, non-hidden products appear in the grid (hidden products filtered)"
  artifacts:
    - path: "src/components/k3martCockpit/WeeklyPlannerGrid.tsx"
      provides: "Outlet-first grid orchestrator with week nav state, copy-last-week, totals"
      min_lines: 150
    - path: "src/components/k3martCockpit/PlannerGridHeader.tsx"
      provides: "3-row column headers with day/date/event, color coding, and per-day confirm buttons"
      min_lines: 60
    - path: "src/components/k3martCockpit/WeekNavigator.tsx"
      provides: "Week navigation with prev/next arrows and prominent date range display"
      min_lines: 40
  key_links:
    - from: "src/components/k3martCockpit/WeeklyPlannerGrid.tsx"
      to: "src/hooks/convex/useK3MartCockpit.ts (useConvexWeeklyDispatchPlans)"
      via: "Query with dynamic weekNumber from navigation state"
      pattern: "useConvexWeeklyDispatchPlans"
    - from: "src/components/k3martCockpit/WeeklyPlannerGrid.tsx"
      to: "src/hooks/convex/useK3MartCockpit.ts (useConvexCopyLastWeek)"
      via: "Copy last week mutation on button click in PlannerActionBar"
      pattern: "useConvexCopyLastWeek"
    - from: "src/components/k3martCockpit/EditablePlannerCell.tsx"
      to: "convex/k3martCockpit/mutations.ts (saveWeeklyDispatchPlan)"
      via: "Auto-save on blur"
      pattern: "saveWeeklyDispatchPlan"
    - from: "src/components/k3martCockpit/PlannerGridHeader.tsx"
      to: "src/hooks/convex/useK3MartCockpit.ts (useConvexConfirmDayPlan)"
      via: "Per-day confirm buttons in header columns"
      pattern: "useConvexConfirmDayPlan"
    - from: "src/components/k3martCockpit/WeeklyPlannerGrid.tsx"
      to: "convex/k3martCockpit/helpers.ts (calculateAutoSuggest)"
      via: "Auto-suggest uses backend helper via Plan 01 query response"
      pattern: "suggestedQty|calculateAutoSuggest"
    - from: "src/components/k3martCockpit/WeeklyPlannerGrid.tsx (hidden product filter)"
      to: "convex/k3martCockpit/queries.ts (getWeeklyDispatchPlans isHidden filter)"
      via: "Backend already filters hidden products; frontend receives only visible products"
      pattern: "isHidden"
---

<objective>
Rewrite the weekly planner grid from product-first to outlet-first layout with 3-row headers, week navigation, copy-last-week, auto-suggest, current stock column, per-day confirm, totals, and auto-save on blur.

Purpose: Transform the planning grid to match user decisions -- outlet-first structure, holiday-aware headers, day-by-day confirm flow, and production target totals.
Output: Completely rebuilt weekly planner grid with all user-decided features.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-k3mart-cockpit/16-CONTEXT.md
@.planning/phases/16-k3mart-cockpit/16-01-SUMMARY.md

@src/components/k3martCockpit/WeeklyPlannerGrid.tsx
@src/components/k3martCockpit/PlannerGridHeader.tsx
@src/components/k3martCockpit/OutletPlannerRow.tsx
@src/components/k3martCockpit/EditablePlannerCell.tsx
@src/components/k3martCockpit/PlannerActionBar.tsx
@src/pages/K3MartCockpit.tsx
@src/lib/indonesianHolidays.ts
@docs/UI_BRAND_REFERENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite core grid components (WeeklyPlannerGrid, OutletPlannerRow, EditablePlannerCell)</name>
  <files>
    src/components/k3martCockpit/WeeklyPlannerGrid.tsx
    src/components/k3martCockpit/OutletPlannerRow.tsx
    src/components/k3martCockpit/EditablePlannerCell.tsx
  </files>
  <action>
    **IMPORTANT -- Use existing shadcn/ui components (NO custom primitives):**
    Available in `src/components/ui/`: Card, Badge, Button, Sheet, AlertDialog, Dialog, Skeleton, ScrollArea, Switch, Input, Label, Textarea, Tooltip, Separator, DropdownMenu, Tabs, Alert, Select, Checkbox, Table, Progress.

    **Rewrite WeeklyPlannerGrid.tsx:**
    Major rewrite to orchestrate outlet-first layout:
    - **Week state initialization:** `const [weekOffset, setWeekOffset] = useState(0)` and compute `currentWeekNumber` using `getWeekNumber()` from `convex/k3martCockpit/helpers.ts`. Explicitly:
      ```typescript
      import { getWeekNumber, getWeekDates, getTodayJakarta } from "@/../../convex/k3martCockpit/helpers";
      const [weekOffset, setWeekOffset] = useState(0);
      const currentWeekNumber = useMemo(() => {
        const today = getTodayJakarta();
        const d = new Date(today + "T00:00:00+07:00");
        d.setDate(d.getDate() + weekOffset * 7);
        return getWeekNumber(d.toLocaleDateString("en-CA", { timeZone: "Asia/Jakarta" }));
      }, [weekOffset]);
      const isCurrentWeek = weekOffset === 0;
      ```
    - Accept outlet-first data from Plan 01 backend via `useConvexWeeklyDispatchPlans(currentWeekNumber)`
    - **Hidden product filtering:** The backend already filters `isHidden` products from the response. The frontend receives only visible, K3Mart-mapped products. No additional frontend filtering needed (per user decision: "only K3Mart-mapped, non-hidden products appear").
    - Render: WeekNavigator -> PlannerGridHeader -> [OutletPlannerRow per outlet] -> Totals row -> PlannerActionBar
    - No product tabs (removed -- outlet-first structure replaces product tabs)
    - Track unsaved changes for beforeunload warning (per user decision: prompt on navigate away):
      ```typescript
      const [hasUnsavedChanges, setHasUnsavedChanges] = useState(false);
      useEffect(() => {
        if (!hasUnsavedChanges) return;
        const handler = (e: BeforeUnloadEvent) => { e.preventDefault(); };
        window.addEventListener("beforeunload", handler);
        return () => window.removeEventListener("beforeunload", handler);
      }, [hasUnsavedChanges]);
      ```
    - Pass week navigation handlers (prev/next/today) to WeekNavigator
    - Pass `weekDates` (array of 7 YYYY-MM-DD strings from `getWeekDates`) and day statuses to PlannerGridHeader
    - Compute and pass daily column totals and grand total to totals row

    **Rewrite OutletPlannerRow.tsx:**
    Change from single outlet row to outlet group with product sub-rows:
    - **Outlet header row:** Outlet name (bold, sticky left), then empty cells for the 7 days (or outlet subtotals)
    - **Product sub-rows:** One row per product for this outlet:
      - Sticky left: Product name (indented under outlet)
      - Stock column: Current stock number (from snapshot data), colored red if below avgDailySales * 2
      - 7 editable cells (one per day) -- uses EditablePlannerCell
      - Row total column on the right (sum of 7 days)
    - **Outlet subtotal row:** Only shown when outlet has >1 product (per user decision). Sum of all product sub-rows per day.
    - Visually group outlets with subtle borders between outlet groups
    - All outlets always expanded (no collapse/expand per user decision)

    **Rewrite EditablePlannerCell.tsx:**
    - Single editable number input per cell (per user decision)
    - **Auto-save on blur:** When user taps away from cell, immediately save via `saveWeeklyDispatchPlan` mutation. No batch save button needed (per user decision).
    - Debounce saves by 300ms to handle rapid tab-through scenarios (per research pitfall #2).
    - Pre-fill with auto-suggested quantity if cell is empty (from backend `suggestedQty` in PlanCell). Show suggested value as placeholder/ghost text.
    - Confirmed cells: show value with green background indicator. Still editable (per user decision: edit after confirm allowed).
    - Submitted cells: show value with blue background, not editable.
    - Draft cells: show value with no special background (default).
    - Tap to edit inline: focus shows a border ring. Show `Input` from shadcn/ui.
    - Cell minimum width for touch targets.
    - Call `setHasUnsavedChanges(true)` on local edit, clear on successful save.
  </action>
  <verify>
    `npm run type-check` passes. `grep "weekOffset\|getWeekNumber\|OutletPlannerRow\|auto-save\|onBlur" src/components/k3martCockpit/WeeklyPlannerGrid.tsx src/components/k3martCockpit/EditablePlannerCell.tsx` returns matches. No product tabs remain.
  </verify>
  <done>
    Core grid is outlet-first with product sub-rows, auto-save on blur with 300ms debounce, auto-suggest placeholders, week state initialized to current week, unsaved changes warning, and proper hidden product handling (backend-filtered).
  </done>
</task>

<task type="auto">
  <name>Task 2: Build navigation, headers, action bar, and wire into K3MartCockpit page</name>
  <files>
    src/components/k3martCockpit/WeekNavigator.tsx
    src/components/k3martCockpit/PlannerGridHeader.tsx
    src/components/k3martCockpit/PlannerActionBar.tsx
    src/pages/K3MartCockpit.tsx
  </files>
  <action>
    **IMPORTANT -- Use existing shadcn/ui components.**

    **Create WeekNavigator.tsx:**
    Week navigation component:
    - Big left/right arrow buttons (shadcn `Button variant="outline"` with ChevronLeft/ChevronRight icons, size="lg")
    - Center: prominent date range display showing "17 Feb - 23 Feb 2026" (Monday to Sunday of selected week)
    - Below date range: week number label "Week 8, 2026"
    - Current week indicator: different background color. Use `bg-primary/10` for current week, `bg-muted` for other weeks (per user decision: color difference between current week and other weeks)
    - "Today" button to jump back to current week (only shown when navigated away)
    - Props: `weekNumber: string, weekDates: string[], onPrev: () => void, onNext: () => void, onToday: () => void, isCurrentWeek: boolean`

    **Rewrite PlannerGridHeader.tsx:**
    Three-row column headers per user decision:
    - Row 1: Day name ("Monday", "Tuesday", etc.) -- full name, not abbreviated
    - Row 2: Date ("17 Feb")
    - Row 3: Event name if applicable (holiday name, "Valentine's Day", "2.2", etc.) -- empty row if no event
    - Color coding (Claude's discretion on exact palette):
      - Weekday: default `bg-card` background
      - Weekend: `bg-blue-50` with `text-blue-700` day name
      - Holiday: `bg-red-50` with `text-red-700` day name, event name in red
      - Sales date: `bg-purple-50` with `text-purple-700` day name, event name in purple
    - "Today" column: ring highlight `ring-2 ring-primary`
    - First column (sticky left): "Outlet / Product" label
    - Second column (before days): "Stock" label for current stock column
    - Last column: "Total" label for row totals
    - **Per-day confirm buttons row at bottom of header (NOT in PlannerActionBar):**
      - Each day column shows confirm state: "Draft" badge (amber) or "Confirmed" badge (green) or "Submitted" badge (blue)
      - If day has draft plans, show small "Confirm" button below badge
      - If day is confirmed and has unsaved edits, show "Update Kitchen" button
    - Use `getDayType()` and `getEventName()` from `src/lib/indonesianHolidays.ts`
    - Props: `weekDates: string[], dayStatuses: Record<string, "draft"|"confirmed"|"submitted">, onConfirmDay: (date: string) => void, hasEditsForDay: Record<string, boolean>`

    **Rewrite PlannerActionBar.tsx:**
    Contains (per clarification):
    - **"Copy Last Week" button** (prominent, uses `copyLastWeek` mutation from Plan 01). Disabled with tooltip "No plans from previous week" when previous week is empty.
    - **Grand totals summary row** at the bottom: daily column totals (sum of all outlet totals per day) -- these are the daily production targets per user decision ("column totals -- most important"). Grand total at bottom right (sum of all days).
    - Per-day confirm buttons are NOT here -- they are in PlannerGridHeader per column.
    - No batch "Save" button (auto-save on blur replaces it).
    - Props: `onCopyLastWeek: () => void, hasPreviousWeek: boolean, dailyTotals: Record<string, number>, grandTotal: number, weekDates: string[]`

    **Update K3MartCockpit.tsx:**
    - Replace `weeklyPlannerOpen` toggle with always-visible weekly planner (remove collapsible behavior -- the planner is the main feature of the cockpit)
    - Remove BACKLOG comments K3MART-01 through K3MART-05 and wire real data:
      - K3MART-01: Production readiness already wired (deficit calculation)
      - K3MART-02: Outlet stock query already returns needed data
      - K3MART-03: Dispatch plan data now comes from outlet-first query
      - K3MART-04: Sources/destinations from inventory sources query
      - K3MART-05: Stock movements from movement history query
    - K3MART-06: Leave as-is for now (Plan 04 handles kitchen wiring)
    - Pass new props to WeeklyPlannerGrid (outlet-first data, onCopyLastWeek, week navigation handlers)
  </action>
  <verify>
    `npm run build` passes. `grep "WeekNavigator\|Copy Last Week\|PlannerGridHeader\|onConfirmDay" src/components/k3martCockpit/WeekNavigator.tsx src/components/k3martCockpit/PlannerGridHeader.tsx src/components/k3martCockpit/PlannerActionBar.tsx` returns matches. Per-day confirm buttons exist in PlannerGridHeader, NOT in PlannerActionBar.
  </verify>
  <done>
    Week navigation with current week highlighting and Today button. 3-row color-coded headers with per-day confirm buttons in header columns. PlannerActionBar has Copy Last Week + grand totals row. K3MartCockpit page wired with always-visible planner and BACKLOG stubs resolved (K3MART-01 through K3MART-05). Collapsible toggle removed.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes
- Grid layout is outlet-first with product sub-rows
- Column headers show day name, date, and event name (3 rows)
- Week navigation works (prev/next arrows, today button), defaults to current week
- Copy last week populates grid with previous week's values (button in PlannerActionBar)
- Auto-save fires on cell blur with debounce
- Per-day confirm buttons in PlannerGridHeader (not PlannerActionBar)
- Daily column totals visible as production targets in PlannerActionBar
- Hidden products not shown (filtered by backend)
- No old product-tab UI remains
</verification>

<success_criteria>
- Outlet-first grid with product sub-rows renders correctly
- 3-row headers with holiday/commercial date color coding
- Week navigation with current week highlighting, initialized to current week
- Copy-last-week functional (in PlannerActionBar)
- Auto-save on blur (no batch save) with 300ms debounce
- Per-day confirm in header columns with "Update Kitchen" on re-edit
- Column totals = daily production targets (in PlannerActionBar totals row)
- Grand total at bottom
- Hidden products excluded from grid
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/16-k3mart-cockpit/16-02-SUMMARY.md`
</output>
