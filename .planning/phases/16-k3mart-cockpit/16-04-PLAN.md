---
phase: 16-k3mart-cockpit
plan: 04
type: execute
wave: 3
depends_on: ["16-02", "16-03"]
files_modified:
  - convex/k3martCockpit/mutations.ts
  - src/pages/K3MartCockpit.tsx
  - src/components/k3martCockpit/index.ts
  - docs/CHANGELOG.md
  - docs/API_REFERENCE.md
autonomous: false

must_haves:
  truths:
    - "Confirmed dispatch plans create/update synthetic kitchen orders (stub with TODO for Phase 15 KIT-06 dependency)"
    - "All BACKLOG comments (K3MART-01 through K3MART-06) in K3MartCockpit.tsx are resolved"
    - "Hidden products not shown in planning grid (only K3Mart-mapped, non-hidden products appear)"
    - "npm run build succeeds with zero errors"
    - "CHANGELOG.md updated with Phase 16 changes"
    - "API_REFERENCE.md updated with new queries/mutations"
    - "Visual verification confirms outlet-first grid, stock flow, rotation, settings modal"
  artifacts:
    - path: "docs/CHANGELOG.md"
      provides: "Updated changelog with Phase 16 entry"
      contains: "Phase 16"
    - path: "docs/API_REFERENCE.md"
      provides: "Updated API reference with K3Mart cockpit queries/mutations"
      contains: "copyLastWeek"
  key_links:
    - from: "convex/k3martCockpit/mutations.ts (confirmDayPlan)"
      to: "convex/orders/mutations/orderCrud.ts (create)"
      via: "Synthetic kitchen order creation on confirm (Phase 15 KIT-06 stub)"
      pattern: "syntheticKitchenOrder|KIT-06"
---

<objective>
Wire synthetic kitchen order creation from confirmed plans, resolve all BACKLOG stubs, update documentation, and visually verify the complete cockpit.

Purpose: Complete Phase 16 by connecting the confirm -> kitchen pipeline, cleaning up all stubs, and ensuring the cockpit is production-ready with documentation.
Output: Fully functional K3Mart cockpit with documentation and visual verification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-k3mart-cockpit/16-CONTEXT.md
@.planning/phases/16-k3mart-cockpit/16-01-SUMMARY.md
@.planning/phases/16-k3mart-cockpit/16-02-SUMMARY.md
@.planning/phases/16-k3mart-cockpit/16-03-SUMMARY.md

@src/pages/K3MartCockpit.tsx
@convex/k3martCockpit/mutations.ts
@docs/CHANGELOG.md
@docs/API_REFERENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire synthetic kitchen orders, resolve BACKLOG stubs, and update docs</name>
  <files>
    convex/k3martCockpit/mutations.ts
    src/pages/K3MartCockpit.tsx
    src/components/k3martCockpit/index.ts
    docs/CHANGELOG.md
    docs/API_REFERENCE.md
  </files>
  <action>
    **Update confirmDayPlan in mutations.ts:**
    After confirming plans and computing kitchen deltas, add synthetic kitchen order creation stub:
    ```typescript
    // Synthetic kitchen order creation -- Phase 15 KIT-06 dependency
    // When Phase 15 is implemented, this will:
    // 1. Check if a synthetic K3Mart kitchen order exists for this date
    // 2. If yes, update its items with new kitchen delta quantities
    // 3. If no, create a new synthetic order with:
    //    - customer: "K3Mart Dispatch" (system customer)
    //    - status: PaymentReceived (skip payment, go directly to kitchen)
    //    - items: one per product with kitchenOrderQty from deltas
    //    - dueDate: the confirmed date
    //    - isK3MartSynthetic: true (flag for kitchen view to distinguish)
    // For now, log the deltas for visibility:
    console.log("[K3Mart] Kitchen deltas for", args.date, ":", kitchenDeltas);
    ```
    This is a conscious stub -- Phase 15 KIT-06 will receive demand from K3M-05.

    **Resolve ALL BACKLOG comments in K3MartCockpit.tsx:**
    Search for every `// BACKLOG: K3MART-` comment and either:
    - Wire real data (most cases, since Plan 01 backend now provides the data)
    - Remove the comment if the data is already wired

    Specific resolutions:
    - `K3MART-01`: Production readiness targets -- use `productionReadinessData.products` with proper deficit calculation (already partially wired)
    - `K3MART-02`: Outlet stock query enhancement -- now provided by Plan 01's enhanced `getOutletStockSummary`
    - `K3MART-03`: Dispatch plan data for outlet product cards -- now comes from `getWeeklyDispatchPlans` outlet-first response
    - `K3MART-04`: Inventory sources/destinations -- use `getInventorySources` query (already exists)
    - `K3MART-05`: Stock movements query -- use `getStockMovementHistory` query per outlet
    - `K3MART-06`: Production bump approval -- leave as toast.info (future feature, not in this phase scope)

    **Ensure product visibility:**
    In the planning grid data flow, filter out products marked as `hidden: true` in outlet settings. Per user decision: "Option to hide specific product rows." Only K3Mart-mapped, non-hidden products appear in grid.

    **Update index.ts:**
    Export new components: `WeekNavigator`, `StockFlowConfirmDialog`, `OutletSettingsModal`.

    **Update CHANGELOG.md:**
    Add Phase 16 entry:
    ```markdown
    ## Phase 16: K3Mart Cockpit (2026-02-XX)

    ### Added
    - Outlet-first weekly planning grid with product sub-rows per outlet
    - Three-row column headers showing day name, date, and holiday/commercial event name
    - Week navigation with prev/next arrows and "Today" button
    - Copy-last-week button duplicates previous week's plan as draft
    - Auto-suggest quantities based on weekday/weekend/holiday demand patterns
    - Current stock column showing K3Mart stock per product per outlet
    - Per-day confirm buttons (day-by-day granularity, not whole week)
    - "Update Kitchen" button when editing already-confirmed plans
    - Rotation stock shortcut (stock-out remaining + stock-in fresh in one action)
    - Confirmation dialog before every K3Mart API call with price sanity check
    - Stock movement history from K3Mart API with status badges
    - Outlet settings modal: active/inactive toggle, per-outlet product selection, custom pricing
    - Commercial/sales dates (Valentine's, 11/11, sequential dates) in holiday system
    - Daily column totals as production targets

    ### Changed
    - Weekly planner reorganized from product-first to outlet-first layout
    - Planner always visible (removed collapsible toggle)
    - Outlet cards show average daily sales from last week
    - Active outlets only shown in planning grid

    ### Technical
    - Extended indonesianHolidays.ts with commercial dates and day-type classifier
    - Auto-save on blur replaces batch save for planning cells
    - Synthetic kitchen order stub (pending Phase 15 KIT-06)
    ```

    **Update API_REFERENCE.md:**
    Document new/changed endpoints:
    - `k3martCockpit.queries.getWeeklyDispatchPlans` -- now returns outlet-first structure
    - `k3martCockpit.queries.getOutletSettings` -- outlet configs with product visibility and pricing
    - `k3martCockpit.mutations.copyLastWeek` -- duplicate previous week as drafts
    - `k3martCockpit.mutations.saveOutletSettings` -- upsert outlet product configs
  </action>
  <verify>
    `npm run build` passes with zero errors. `grep -r "BACKLOG: K3MART" src/pages/K3MartCockpit.tsx` returns empty (all stubs resolved). CHANGELOG.md contains "Phase 16". API_REFERENCE.md contains "copyLastWeek".
  </verify>
  <done>
    Synthetic kitchen order stub in place for Phase 15 dependency. All BACKLOG stubs resolved. Hidden products filtered. Documentation updated. Build passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete K3Mart Cockpit</name>
  <files>src/pages/K3MartCockpit.tsx</files>
  <action>
    Human verifies the complete K3Mart cockpit visually:
    1. Weekly planning grid layout
    2. Outlet cards and stock flow
    3. Settings modal
    4. All features from user decisions
  </action>
  <verify>
    1. Navigate to K3Mart Cockpit page
    2. **Weekly planner:** Verify outlet-first layout (outlets as primary rows, products as sub-rows)
    3. **Column headers:** Verify 3 rows (day name, date, event name for holidays/commercial dates)
    4. **Color coding:** Weekend columns have blue tint, holiday columns have red tint, sales date columns have purple tint
    5. **Week navigation:** Click prev/next arrows to navigate weeks. Verify current week has different color. Click "Today" to return.
    6. **Current stock column:** Verify stock numbers appear before day columns
    7. **Editable cells:** Tap a cell, enter number, tap away. Verify auto-save (no save button needed)
    8. **Auto-suggest:** Verify empty cells show ghost/placeholder suggested quantities
    9. **Copy last week:** Click button. Verify previous week's values populate as drafts.
    10. **Confirm day:** Click confirm button on a day column. Verify cells turn green. Edit a confirmed cell -- verify "Update Kitchen" button appears.
    11. **Totals:** Verify column totals at bottom (daily production targets), row totals (per product), and grand total
    12. **Outlet cards:** Tap an outlet card. Verify expansion shows stock detail + stock flow form + history
    13. **Rotation:** In expanded outlet, click "Rotate Stock". Verify auto-fills from current stock. Verify confirmation dialog appears.
    14. **Confirmation dialog:** Verify price sanity check (price shown, would block if zero)
    15. **Settings (admin):** Click gear icon. Verify outlet active/inactive toggles. Verify per-outlet product selection and custom pricing.
    16. **Mobile:** Check responsive behavior
  </verify>
  <done>User approves complete K3Mart cockpit visual verification</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- All BACKLOG stubs resolved
- CHANGELOG.md has Phase 16 entry
- API_REFERENCE.md updated
- Synthetic kitchen order stub in place
- Visual verification confirms all user decisions implemented
</verification>

<success_criteria>
- All 5 K3M requirements (K3M-01 through K3M-05) implemented
- All phase success criteria met:
  1. Stubs show real data
  2. Outlet-first weekly view
  3. Holiday/weekend highlighting with adjusted quantities
  4. Manual stock in/out with rotation
  5. Confirmed plans have synthetic order stub (Phase 15 dependency)
- Documentation updated
- `npm run build` succeeds
- User approves visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/16-k3mart-cockpit/16-04-SUMMARY.md`
</output>
