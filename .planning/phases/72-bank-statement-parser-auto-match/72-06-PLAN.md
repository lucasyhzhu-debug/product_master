---
phase: 72
plan: 06
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - docs/CHANGELOG.md
  - docs/SCHEMA.md
  - docs/API_REFERENCE.md
  - CLAUDE.md
autonomous: true
requirements: [BANK-01, BANK-02]
must_haves:
  truths:
    - "Full test suite passes: npm run test (no skips)"
    - "npm run build passes"
    - "npm run type-check passes"
    - "npm run lint passes"
    - "Zero occurrences of npm-registry xlsx anywhere (only CDN tarball reference)"
    - "CHANGELOG, SCHEMA, API_REFERENCE updated"
    - "CLAUDE.md documents the xlsx CDN install pitfall"
    - "No JE posting code exists for bank statements (P72 invariant)"
    - "No P73 scope accidentally shipped (split-view, manual match, inline create, revenue aggregation)"
  artifacts:
    - path: "docs/CHANGELOG.md"
      provides: "Phase 72 entry"
      contains: "Phase 72"
    - path: "docs/SCHEMA.md"
      provides: "3 new tables + sourceType extension + accounts.by_name index documented"
      contains: "bankStatements"
    - path: "docs/API_REFERENCE.md"
      provides: "bankStatements.* + bankKeywordRules.* endpoints documented"
      contains: "createFromParsedStatement"
    - path: "CLAUDE.md"
      provides: "New Common Pitfall: xlsx must be installed from SheetJS CDN tarball, not npm"
      contains: "cdn.sheetjs.com"
  key_links:
    - from: "docs/CHANGELOG.md"
      to: ".planning/phases/72-bank-statement-parser-auto-match/"
      via: "Phase 72 summary links back to plan SUMMARYs"
      pattern: "Phase 72"
---

<objective>
Phase 72 verification + docs rollup + boundary audit. Ensures all gates green and no P73 scope leaked in.

Purpose: Final quality gate before the phase ships. This plan does NOT implement new behavior — it verifies, documents, and audits.
Output: Updated docs; full green test suite; explicit no-P73-scope audit report.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/72-bank-statement-parser-auto-match/72-CONTEXT.md
@.planning/phases/72-bank-statement-parser-auto-match/72-01-SUMMARY.md
@.planning/phases/72-bank-statement-parser-auto-match/72-02-SUMMARY.md
@.planning/phases/72-bank-statement-parser-auto-match/72-03-SUMMARY.md
@.planning/phases/72-bank-statement-parser-auto-match/72-04-SUMMARY.md
@.planning/phases/72-bank-statement-parser-auto-match/72-05-SUMMARY.md
@docs/CHANGELOG.md
@docs/SCHEMA.md
@docs/API_REFERENCE.md
@CLAUDE.md
</context>

## Git Workflow
**Branch:** `gsd/phase-72-bank-statement-parser-auto-match`
**Checkpoints:** None

## Implementation Waves

### Wave 3: Verification & Documentation [SEQUENTIAL]
| Agent | Task | Files |
|-------|------|-------|
| Bash | Task 1: Run full test suite + build + lint; capture output | none (verification only) |
| Bash | Task 2: Boundary audit — ensure no P73 scope leaked; ensure no JE posting code | none (verification only) |
| code-auditor | Task 3: Update CHANGELOG + SCHEMA + API_REFERENCE + CLAUDE.md | docs/CHANGELOG.md, docs/SCHEMA.md, docs/API_REFERENCE.md, CLAUDE.md |

## Documentation Updates
- [x] `docs/CHANGELOG.md` — Phase 72 entry with plan-by-plan summary
- [x] `docs/SCHEMA.md` — 3 new tables + journalEntries.sourceType extension + accounts.by_name index
- [x] `docs/API_REFERENCE.md` — bankStatements + bankKeywordRules queries & mutations
- [x] `CLAUDE.md` — add Pitfall #15 for xlsx CDN install

## Success Criteria
- [ ] `npm run test` exits 0 (full suite)
- [ ] `npm run type-check` exits 0
- [ ] `npm run build` exits 0
- [ ] `npm run lint` exits 0 with no new warnings
- [ ] `grep -r "createJournalEntryWithLines" convex/bankStatements/` returns 0 lines (NO JE posting)
- [ ] `grep -rn "split-view\|splitView\|manualMatch\|manualUnmatch\|createExpenseFromLine\|aggregateRevenue" src/components/bankReconciliation/ src/pages/BankReconciliationPage.tsx` returns 0 (no P73 scope)
- [ ] CHANGELOG.md contains "Phase 72" entry
- [ ] SCHEMA.md documents all 3 new tables
- [ ] API_REFERENCE.md documents all new endpoints
- [ ] CLAUDE.md Common Pitfalls section includes xlsx CDN install note

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Full verification run</name>
  <files></files>
  <read_first>
    - package.json (scripts)
    - .planning/phases/72-bank-statement-parser-auto-match/72-VALIDATION.md (per-task map — spot check all rows map to passing tests)
  </read_first>
  <action>
Run each of these in order. All must exit 0. If any fails, STOP and fix before continuing:

```
npm run type-check
npm run lint
npm run test
npm run build
```

Capture the output of `npm run test` and note the total test count. Compare against VALIDATION.md — spot-check that files listed as Wave 0 required now exist:
- `src/lib/bankStatement/__tests__/parseBcaXlsx.test.ts` ✓
- `src/lib/bankStatement/__tests__/parseBcaCsv.test.ts` ✓
- `src/lib/bankStatement/__tests__/reconciliation.test.ts` ✓
- `src/lib/bankStatement/__tests__/yearRollover.test.ts` ✓
- `src/lib/bankStatement/__tests__/fuzzyMatch.test.ts` ✓
- `convex/bankStatements/__tests__/matchEngine.test.ts` ✓
- `convex/bankStatements/__tests__/mutations.test.ts` ✓
- `convex/bankKeywordRules/__tests__/seed.test.ts` ✓
- `tests/fixtures/bca-sample-synthetic.ts` ✓

Confirm no `.skip(...)` or `expect(true).toBe(true)` placeholders in any new test file:
```
grep -rn "\.skip\(\|test\.skip\|describe\.skip\|expect\(true\)\.toBe\(true\)" src/lib/bankStatement/__tests__/ convex/bankStatements/__tests__/ convex/bankKeywordRules/__tests__/
```
Expected: zero matches.
  </action>
  <verify>
    <automated>npm run type-check && npm run lint && npm run test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All 4 commands exit 0
    - 9 test files + 1 fixture present
    - Zero skip markers / trivial assertions in new tests
  </acceptance_criteria>
  <done>Phase gate green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: P72/P73 boundary audit — ensure no P73 scope leaked</name>
  <files></files>
  <read_first>
    - .planning/phases/72-bank-statement-parser-auto-match/72-CONTEXT.md §Phase Boundary, §Phase 72 UI Scope (D-25, D-26)
    - The planner's phase_boundary_summary (see planning input): "Phase 72 ships data + pipeline + minimal read-only review. Explicitly OUT OF SCOPE → P73: split-view, manual match/unmatch, inline expense/JE creation, revenue aggregation dashboard, learn-from-override, JE posting"
  </read_first>
  <action>
Run these targeted audits. All must pass. If any fails, file an immediate regression fix ticket and correct before phase merge.

**Audit 1 — No JE posting for bank statements (D-20 invariant):**
```
grep -rn "createJournalEntryWithLines" convex/bankStatements/ convex/bankKeywordRules/
```
Expected: ZERO matches.

**Audit 2 — No split-view / manual match UI:**
```
grep -rn -E "split.?view|splitView|manualMatch|manualUnmatch|unmatch" src/components/bankReconciliation/ src/pages/BankReconciliationPage.tsx src/pages/BankRulesManager.tsx
```
Expected: ZERO matches (or only comments referencing "P73" / future work).

**Audit 3 — No inline expense creation from bank line:**
```
grep -rn -E "createExpenseFromLine|createExpenseFromBank|bankLineToExpense" src/ convex/
```
Expected: ZERO matches.

**Audit 4 — No revenue aggregation dashboard (P73 scope):**
```
grep -rn -E "revenueAggregation|channelDiff|bankRevenueChannel" src/pages/ src/components/
```
Expected: ZERO matches.

**Audit 5 — No learn-from-override:**
```
grep -rn -E "learnFromOverride|saveAsRule|ruleFromOverride" src/ convex/
```
Expected: ZERO matches.

**Audit 6 — Line-level state mutation (P73): status change mutations not yet present:**
```
grep -rn -E "confirmLine|matchLine|unmatchLine" convex/bankStatements/
```
Expected: ZERO matches (line.status is set on INSERT in plan 04; no mutation exists yet to change it).

**Audit 7 — xlsx install source:**
```
grep -c "cdn.sheetjs.com/xlsx-0.20.3" package.json
```
Expected: 1.

```
npm ls xlsx | grep "0.20.3"
```
Expected: 1 match (or more — same version across tree).

**Audit 8 — Admin-gate coverage on queries:**
```
grep -c "requireRole" convex/bankStatements/queries.ts convex/bankKeywordRules/queries.ts
```
Expected: ≥5 (3 in bankStatements/queries + 2 in bankKeywordRules/queries).

If any audit fails, document which plan introduced the violation, revert that part, re-run tests. Do NOT merge with any audit failing.
  </action>
  <verify>
    <automated>bash -c 'fails=0; for cmd in "grep -rn createJournalEntryWithLines convex/bankStatements/ convex/bankKeywordRules/" "grep -rn -E manualMatch src/components/bankReconciliation/ src/pages/BankReconciliationPage.tsx" "grep -rn createExpenseFromLine src/ convex/" "grep -rn revenueAggregation src/pages/ src/components/"; do out=$(eval "$cmd" 2>/dev/null); if [ -n "$out" ]; then echo "AUDIT FAIL: $cmd -> $out"; fails=$((fails+1)); fi; done; exit $fails'</automated>
  </verify>
  <acceptance_criteria>
    - All 8 audits pass with expected zero-match results
    - `grep -c "cdn.sheetjs.com/xlsx-0.20.3" package.json` returns 1
    - `grep -c "requireRole" convex/bankStatements/queries.ts` returns ≥3
  </acceptance_criteria>
  <done>Phase boundary preserved; no P73 scope leaked.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update docs (CHANGELOG + SCHEMA + API_REFERENCE + CLAUDE.md)</name>
  <files>
    docs/CHANGELOG.md,
    docs/SCHEMA.md,
    docs/API_REFERENCE.md,
    CLAUDE.md
  </files>
  <read_first>
    - docs/CHANGELOG.md (format — read last 3 entries)
    - docs/SCHEMA.md (format — find where other table definitions live)
    - docs/API_REFERENCE.md (format — find how queries/mutations are documented)
    - CLAUDE.md (Common Pitfalls section — current highest number, so we can add the next one)
    - The 5 SUMMARY files in .planning/phases/72-bank-statement-parser-auto-match/ — condense into changelog entry
  </read_first>
  <action>
**docs/CHANGELOG.md:** Prepend a new entry (follow existing format — date-stamped, phase-numbered, plan-by-plan bullets):

```markdown
## [Phase 72] Bank Statement Parser & Auto-Match — 2026-04-XX

**Requirements delivered:** BANK-01, BANK-02

- **Plan 01 (Foundation):** Installed `xlsx@0.20.3` from SheetJS CDN (secure); `fastest-levenshtein@1.0.16`. Added schema tables `bankStatements`, `bankStatementLines`, `bankKeywordRules`. Extended `journalEntries.sourceType` with `"bank_statement"` literal. Added `accounts.by_name` index. Appended 20 new accounts to `DEFAULT_ACCOUNTS` in support of the rule seed.
- **Plan 02 (Parser + libs):** BCA XLSX parser with reconciliation checksum (abort-on-mismatch), Indonesian date parser with year-rollover handling, fuzzy similarity wrapper, SHA-256 file hash helper. CSV fallback.
- **Plan 03 (Match engine):** Two-layer matcher — Layer A keyword/counterparty classification with catch-all segregation; Layer B record linkage against expenses/revenue/reimbursements/payroll with fuzzy threshold ≥0.8 and ±3 day window. 26 rule positive fixtures + direction + hint + ambiguity test cases.
- **Plan 04 (Convex API):** `bankKeywordRules:seedDefaults` (idempotent, fails loudly on unresolved account refs), admin CRUD, admin-gated `bankStatements:createFromParsedStatement` (dedup by fileHash + by accountNumber+period; atomic parent-child insert; runs match engine inline; does NOT post JEs). All queries admin-gated.
- **Plan 05 (UI):** `/bank-reconciliation` upload wizard + read-only review + statement history. `/bank-rules` admin CRUD page. Admin-gated via ProtectedRoute.
- **Plan 06 (Verification):** Full suite green; P72/P73 boundary audit clean; docs updated.

**Out of scope (P73):** Split-view UI, manual match/unmatch, inline expense/JE creation, revenue aggregation dashboard, learn-from-override, JE posting.
```

**docs/SCHEMA.md:** Add three new table definitions in the appropriate section (probably near other financial tables). Document each field exactly as defined in plan 01 Task 2, including indexes. Mention:
- `bankStatements` header is IMMUTABLE after insert (except `matchedCount`)
- `matchedType`+`matchedId` is polymorphic (first use in codebase)
- `journalEntries.sourceType` gained `"bank_statement"` literal
- `accounts` gained `by_name` index

**docs/API_REFERENCE.md:** Document every new query and mutation with signature + admin-only note:

Under a new "Bank Reconciliation" section:
- `bankStatements.createFromParsedStatement(token, header, lines) → { statementId, lineCount, matchedCount }` — admin
- `bankStatements.listStatements(token) → BankStatement[]` — admin
- `bankStatements.getStatement(token, id) → BankStatement | null` — admin
- `bankStatements.listLines(token, statementId, statusFilter?) → BankStatementLine[]` — admin
- `bankKeywordRules.seedDefaults(token?) → { ruleCode, action }[]` — admin (dashboard-callable)
- `bankKeywordRules.list(token, includeInactive?) → BankKeywordRule[]` — admin
- `bankKeywordRules.getById(token, id) → BankKeywordRule | null` — admin
- `bankKeywordRules.create(token, ...fields) → Id` — admin
- `bankKeywordRules.update(token, id, patch) → null` — admin
- `bankKeywordRules.deactivate(token, id) → null` — admin

**CLAUDE.md:** Add a new entry to §Common Pitfalls (find the next sequential number — currently #14 is "Keep phase directory names short", so add #15):

```
15. **Install `xlsx` from SheetJS CDN, not npm** — The npm registry version (`xlsx@0.18.5`) is frozen and known-vulnerable (CVE-2023-30533 prototype pollution, CVE-2024-22363 ReDoS). SheetJS publishes patched versions only via their CDN. Correct install: `npm install --save https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz`. Do NOT run `npm audit fix` on this dependency — it will attempt to downgrade to the vulnerable registry version. Phase 72 established this pattern.
```

Also in CLAUDE.md §Quick File Finder, append a new row for bank reconciliation:

| **Bank reconciliation** | `convex/bankStatements/`, `convex/bankKeywordRules/`, `convex/lib/indonesianDate.ts` | `src/pages/BankReconciliationPage.tsx`, `src/pages/BankRulesManager.tsx`, `src/hooks/convex/useBankReconciliation.ts`, `src/components/bankReconciliation/`, `src/lib/bankStatement/` |
  </action>
  <verify>
    <automated>bash -c 'grep -c "Phase 72" docs/CHANGELOG.md >/dev/null && grep -c "bankStatements" docs/SCHEMA.md >/dev/null && grep -c "createFromParsedStatement" docs/API_REFERENCE.md >/dev/null && grep -c "cdn.sheetjs.com" CLAUDE.md >/dev/null && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md contains "Phase 72" entry
    - SCHEMA.md documents `bankStatements`, `bankStatementLines`, `bankKeywordRules`
    - API_REFERENCE.md documents all 10 new endpoints
    - CLAUDE.md Common Pitfalls gained a new entry mentioning `cdn.sheetjs.com`
    - CLAUDE.md Quick File Finder contains a row for bank reconciliation
  </acceptance_criteria>
  <done>All docs current; phase fully documented for future reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new runtime trust boundaries — this plan is documentation + verification only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-32 | Tampering (scope) | P73 scope leaked into P72 | mitigate | Task 2 boundary audit greps for 8 specific P73 patterns. Phase merge blocked on any audit failure. |
| T-72-33 | Repudiation | Undocumented schema/API changes | mitigate | Task 3 requires SCHEMA.md + API_REFERENCE.md updates. CHANGELOG.md mandatory per CLAUDE.md §Git Workflow. |
</threat_model>

<verification>
- Test suite green
- Boundary audit passes all 8 checks
- Docs updated
- Branch still `gsd/phase-72-bank-statement-parser-auto-match`
</verification>

<success_criteria>
- All Task acceptance criteria met
- Ready to merge: `npm run build` passes, tests green, docs current, audits clean
- After this plan, follow CLAUDE.md merge flow: PR review → merge to main → CHANGELOG final sweep → next phase branch
</success_criteria>

<output>
Create `.planning/phases/72-bank-statement-parser-auto-match/72-06-SUMMARY.md` documenting:
- Full test suite pass count + runtime
- Build output size delta (if measurable)
- Audit results (all 8 zero-match confirmed)
- Docs diff summary (line counts added to each file)
- Link to Phase 73 intake: which P73 behaviors unblocked by this phase's data model (split-view target, manual match, etc.)
</output>
