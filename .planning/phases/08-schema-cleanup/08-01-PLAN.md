---
phase: 08-schema-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/SCHEMA_AUDIT.md
  - convex/schema.ts
  - docs/SCHEMA.md
autonomous: true

must_haves:
  truths:
    - "docs/SCHEMA_AUDIT.md exists and categorizes all ~215 v.optional() fields into Category A (legitimately optional), Category B (candidate for required), Category C (deprecated/remove), and Category D (table-level)"
    - "Every denormalized field in schema.ts has an inline comment with formal category (SNAPSHOT, CACHE, or DERIVED), source of truth reference, and timing description"
    - "docs/SCHEMA.md has a Denormalization Patterns summary section listing all SNAPSHOT, CACHE, and DERIVED patterns"
    - "Audit document includes a removal log section listing all fields/tables to be removed and why"
  artifacts:
    - path: "docs/SCHEMA_AUDIT.md"
      provides: "Comprehensive field audit with categories A/B/C/D, removal log, permanent onboarding reference"
      contains: "Category A"
    - path: "convex/schema.ts"
      provides: "Inline denormalization comments on all ~50 denormalized fields"
      contains: "SNAPSHOT:"
    - path: "docs/SCHEMA.md"
      provides: "Denormalization Patterns summary section"
      contains: "Denormalization Patterns"
  key_links:
    - from: "docs/SCHEMA_AUDIT.md"
      to: "convex/schema.ts"
      via: "field names and table names match exactly"
      pattern: "v\\.optional"
    - from: "convex/schema.ts"
      to: "docs/SCHEMA.md"
      via: "inline comments reference same patterns as SCHEMA.md summary"
      pattern: "SNAPSHOT|CACHE|DERIVED"
---

<objective>
Create the comprehensive field audit document and add denormalization documentation to schema.ts and SCHEMA.md.

Purpose: Fulfill SCHEMA-01 (audit all optional fields) and SCHEMA-04 (document denormalization patterns). This is the reference document that guides all subsequent schema changes and serves as a permanent onboarding resource.

Output: `docs/SCHEMA_AUDIT.md` with complete field categorization, inline denormalization comments in `schema.ts`, denormalization summary in `docs/SCHEMA.md`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-schema-cleanup/08-RESEARCH.md

@convex/schema.ts
@docs/SCHEMA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SCHEMA_AUDIT.md field audit document</name>
  <files>docs/SCHEMA_AUDIT.md</files>
  <action>
Create `docs/SCHEMA_AUDIT.md` as a permanent onboarding reference document with these sections:

**1. Overview**
- State: "215 v.optional() fields across 37+ tables audited on [date]"
- Explain the four categories: A (legitimately optional), B (candidate for required after backfill), C (deprecated - remove), D (table-level assessment)

**2. Category A: Legitimately Optional**
Table-by-table listing of all fields that are correctly optional because not all documents should have them. Use the research data from 08-RESEARCH.md Category A section. Include the field name and WHY it's optional. Example format:

```markdown
### ingredients
| Field | Reason |
|-------|--------|
| brand | Not all ingredients have a brand |
| procurementSource | Not always tracked |
```

Cover ALL tables: ingredients, packagingMaterials, customers, orders (many fields), orderItems, orderItemProduction, recipeVersions, packagingVersions, productVersions, recipeComponents, vouchers, componentTypes, storageLocations, inventoryBatches, componentStock, componentTransactions, orderComponentReservations, feedback, users, sessions, whatsappTemplates, orderEvents, productionTargets, productionCounts, productionLog, externalOutlets, externalStockSnapshots, externalRevenue, externalRevenueItems, externalSyncLogs, externalProductMappings, platformCredentials, gofoodDepotStock, k3martDispatchPlans, k3martStockMovements, menuProductComponents, orderMessages.

**3. Category B: Candidates for Required**
List fields that are optional in schema but should always have values. For each field include: table, field name, why it should be required, default value if missing documents need backfilling. Use the research data:

| Table | Field | Why Likely Required | Default If Missing |
|-------|-------|--------------------|--------------------|
| ingredients | costPerBaseUnit | Calculated on create/update | Recalculate from price/volume |
| ingredients | baseUnit | Calculated on create/update | Derive from unitType |
| packagingMaterials | costPerBaseUnit | Same pattern | Same |
| packagingMaterials | baseUnit | Same pattern | Same |
| menuProducts | unitCost | COGS from BOM | 0 |
| menuProducts | cachedProductionSummary | BOM operations | "" |
| menuProducts | productType | Set during BOM refactor | "food" |
| orders | isKitchenVisible | Added Phase 7 | Compute from status |
| orders | finalTotal | totalAmount - discount | Compute from totalAmount |

**NOTE:** `orders.completedAt` was initially considered Category B but is actually Category A (legitimately optional). Only terminal orders (CompleteShipped, PickedUp, Cancelled) have this value; active orders correctly have `undefined`. The backfill in Plan 03 fills it for historical terminal orders for data completeness, but the field stays `v.optional()`. Do NOT list it in Category B.
| kitchenInventory | totalProducedOriginal | Added later | 0 |
| kitchenInventory | totalProducedBiteSized | Added later | 0 |
| kitchenInventory | updatedBy | Tracking | "system" |
| productionUnitTypes | color | Display | "#93C572" |

**4. Category C: DEPRECATED - Remove Entirely**
List fields to remove with code references that need cleanup:

| Table | Field | Status | Code References |
|-------|-------|--------|-----------------|
| menuProducts | productionType | DEPRECATED Phase 6 | Seed data (mutations.ts) |
| menuProducts | productionUnits | DEPRECATED Phase 6 | Seed data |
| menuProducts | isFixed | Replaceable by posSlot/packagingPosSlot check | mutations.ts, MenuProductsManager.tsx, useMenuProducts.ts |
| orderItems | productionType | DEPRECATED Phase 6 | Fallback in queries.ts (6 locs), packaging.ts (2 locs), whatsapp.ts (1 loc) |
| orderItems | productionUnits | DEPRECATED Phase 6 | Same locations |

**5. Category D: Table-Level Assessment**
| Table | Status | Notes |
|-------|--------|-------|
| kitchenInventory | KEEP | Actively used by Kitchen V2/V3 ball tray system |
| productionUnitTypes | KEEP | FK from orderItemProduction.productionUnitTypeId |

**6. Removal Log**
Document what will be removed and why:
- menuProducts.productionType: Deprecated in Phase 6, BOM is source of truth
- menuProducts.productionUnits: Deprecated in Phase 6, BOM is source of truth
- menuProducts.isFixed: Replaced by posSlot/packagingPosSlot deletion-protection check
- orderItems.productionType: Deprecated in Phase 6, orderItemProduction records are source of truth
- orderItems.productionUnits: Deprecated in Phase 6, orderItemProduction records are source of truth
- useConvexFixedProducts hook: Exported but never imported (dead code)

**7. Denormalization Inventory**
Three subsections (SNAPSHOT, CACHE, DERIVED) listing all denormalized fields. Copy from 08-RESEARCH.md denormalization inventory section.
  </action>
  <verify>
Verify file exists and contains all four categories. Check that all tables from schema.ts are represented. Run `git diff --stat` to confirm the file was created.
  </verify>
  <done>
`docs/SCHEMA_AUDIT.md` exists with complete categorization of all ~215 v.optional() fields into categories A/B/C/D, a removal log, and a denormalization inventory. The document is structured as a permanent onboarding reference.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add denormalization comments to schema.ts and SCHEMA.md</name>
  <files>convex/schema.ts, docs/SCHEMA.md</files>
  <action>
**Part A: Add inline denormalization comments to `convex/schema.ts`**

For every denormalized field, add a comment directly above or on the same line using the formal category format from user decisions:

SNAPSHOT format: `// SNAPSHOT: Copied from {source} at {event}. Never updated after.`
CACHE format: `// CACHE: {what is cached}. Source: {source table.field}. Updated: {when/how}. Stale indicator: {if any}.`
DERIVED format: `// DERIVED: Computed as {formula}. Updated: {when}.`

**Fields to annotate (comprehensive list):**

**orders table:**
- `customerName` -> `// SNAPSHOT: Copied from customers.name at order creation. Never updated after.`
- `customerPhone` -> `// SNAPSHOT: Copied from customers.phone at order creation. Never updated after.`
- `voucherCode` -> `// SNAPSHOT: Copied from vouchers.code at order creation. Never updated after.`
- `voucherDiscountValue` -> `// SNAPSHOT: Calculated discount at order creation. Never updated after.`
- `totalAmount` -> `// DERIVED: Sum of orderItems.lineTotal. Updated on item add/remove/edit.`
- `totalCost` -> `// DERIVED: Sum of orderItems.lineCost. Updated on item add/remove/edit.`
- `totalMargin` -> `// DERIVED: totalAmount - totalCost. Updated on item add/remove/edit.`
- `finalTotal` -> `// DERIVED: totalAmount - orderLevelDiscount. Updated on discount change.`
- `itemCount` -> `// DERIVED: Count of orderItems for this order. Updated on item add/remove.`
- `isKitchenVisible` -> `// DERIVED: Computed from status. Set on every status transition. Source: statusTransitions.ts computeKitchenVisibility().`
- `completedAt` -> `// DERIVED: Set when order reaches terminal status (CompleteShipped/PickedUp/Cancelled). Cleared on revert.`

**orderItems table:**
- `productName` -> `// SNAPSHOT: From manual entry or menuProducts.name at item creation. Never updated after.`
- `unitPrice` -> `// SNAPSHOT: From manual entry or menuProducts.defaultPrice at item creation. Never updated after.`
- `unitCost` -> `// SNAPSHOT: From menuProducts.unitCost at item creation. Never updated after.`
- `lineTotal` -> `// DERIVED: quantity * unitPrice - discountAmount. Computed at creation/update.`
- `lineCost` -> `// DERIVED: quantity * unitCost. Computed at creation/update.`
- `lineMargin` -> `// DERIVED: lineTotal - lineCost. Computed at creation/update.`

**orderItemProduction table:**
- `productionUnitCode` -> `// SNAPSHOT: From productionUnitTypes.code at order confirmation. Never updated after.`
- `productionUnitName` -> `// SNAPSHOT: From productionUnitTypes.name at order confirmation. Never updated after.`
- `unitsRemaining` -> `// DERIVED: unitsRequired - unitsCompleted. Updated on each ball completion.`

**componentIngredients table:**
- `ingredientName` -> `// SNAPSHOT: From ingredients.name at creation. Never updated after.`
- `cachedLineCost` -> `// CACHE: ingredientCost * quantity. Source: ingredients.costPerBaseUnit. Updated: on cost invalidation.`

**packagingComponentMaterials table:**
- `materialName` -> `// SNAPSHOT: From packagingMaterials.name at creation. Never updated after.`
- `cachedLineCost` -> `// CACHE: materialCost * quantity. Source: packagingMaterials.costPerBaseUnit. Updated: on cost invalidation.`

**recipeVersions table:**
- `cachedTotalCost` -> `// CACHE: Sum of component costs. Source: componentIngredients. Updated: on cost invalidation.`
- `cachedCostPerGram` -> `// CACHE: cachedTotalCost / estimatedYieldGrams. Source: componentIngredients. Updated: on cost invalidation.`
- `costCacheUpdatedAt` -> `// CACHE: Timestamp of last cost recalculation.`

**recipeComponents table:**
- `cachedSubtotalCost` -> `// CACHE: Sum of ingredient line costs. Source: componentIngredients. Updated: on cost invalidation.`

**packagingVersions table:**
- `cachedTotalCost` -> `// CACHE: Sum of material costs. Source: packagingComponentMaterials. Updated: on cost invalidation.`
- `costCacheUpdatedAt` -> `// CACHE: Timestamp of last cost recalculation.`

**packagingComponents table:**
- `cachedSubtotalCost` -> `// CACHE: Sum of material line costs. Source: packagingComponentMaterials. Updated: on cost invalidation.`

**productVersions table:**
- `recipeName` -> `// SNAPSHOT: From recipes.name at version creation. Never updated after.`
- `recipeVersionName` -> `// SNAPSHOT: From recipeVersions.versionName at version creation. Never updated after.`
- `packagingName` -> `// SNAPSHOT: From packagingRecipes.name at version creation. Never updated after.`
- `packagingVersionName` -> `// SNAPSHOT: From packagingVersions.versionName at version creation. Never updated after.`
- `cachedCogs` -> `// CACHE: COGS breakdown. Source: recipeVersions + packagingVersions costs. Updated: on cost invalidation.`
- `cogsCacheUpdatedAt` -> `// CACHE: Timestamp of last COGS recalculation.`

**menuProducts table:**
- `unitCost` -> `// CACHE: Production COGS from BOM. Source: componentTypes.unitCostIdr via menuProductComponents. Updated: by recalculateAllCosts or invalidateMenuProductCosts.`
- `unitCostStaleAt` -> `// CACHE: Timestamp when unitCost was marked stale. Cleared after recalculation. Source: set on componentType cost change.`
- `cachedProductionSummary` -> `// CACHE: Human-readable BOM summary (e.g., "1 Big, 2 Mid"). Source: menuProductComponents + componentTypes. Updated: on BOM change.`

**componentStock table:**
- `totalStock` -> `// CACHE: Sum of inventoryBatches.quantityRemaining. Updated: on batch change.`
- `totalReserved` -> `// CACHE: Sum of inventoryBatches.quantityReserved. Updated: on reservation change.`
- `weightedUnitCostIdr` -> `// CACHE: Weighted average cost from active batches. Updated: on batch change.`
- `latestSupplierName` -> `// CACHE: From most recent inventoryBatch. Updated: on new batch.`
- `latestPurchaseUrl` -> `// CACHE: From most recent inventoryBatch. Updated: on new batch.`
- `latestUnitCostIdr` -> `// CACHE: From most recent inventoryBatch. Updated: on new batch.`
- `lastRestockTotalStock` -> `// CACHE: Snapshot of totalStock at last restock. Updated: on restock.`

**ingredients table:**
- `costPerBaseUnit` -> `// CACHE: Calculated from price/volume/shipping. Updated: on ingredient edit.`
- `baseUnit` -> `// CACHE: Derived from unitType (kg->g, l->ml). Updated: on ingredient edit.`

**packagingMaterials table:**
- `costPerBaseUnit` -> `// CACHE: Calculated from price/volume/shipping. Updated: on material edit.`
- `baseUnit` -> `// CACHE: Derived from unitType. Updated: on material edit.`

**inventoryBatches table:**
- `unitCostIdr` -> `// DERIVED: totalCostIdr / quantityPurchased. Computed at batch creation.`

**externalStockSnapshots table:**
- `productName` -> `// SNAPSHOT: From external API at snapshot time. Never updated after.`

**k3martStockMovements table:**
- `priceAtSubmission` -> `// SNAPSHOT: Price at K3Mart submission time. Never updated after.`
- `currentStockAtSubmission` -> `// SNAPSHOT: Stock level at K3Mart submission time. Never updated after.`

Replace existing generic "Denormalized" and "CACHED" comments with the formal annotations. Do NOT remove existing functional comments (like PRD references or DEPRECATED markers).

**Part B: Add Denormalization Patterns section to `docs/SCHEMA.md`**

At the end of `docs/SCHEMA.md`, add a new section:

```markdown
## Denormalization Patterns

This project uses three categories of denormalized data. All patterns are annotated inline in `convex/schema.ts`.

### SNAPSHOT (frozen at creation, never updated)
Data copied from a source record at creation time. The snapshot preserves the value as it was when the event occurred, even if the source record changes later.

| Table | Field | Source | Captured At |
|-------|-------|--------|-------------|
[List all ~18 SNAPSHOT fields from research]

### CACHE (refreshable/invalidatable)
Computed values stored for query performance. These can be recalculated from their source data at any time.

| Table | Field | Source | Updated When |
|-------|-------|--------|--------------|
[List all ~20 CACHE fields from research]

### DERIVED (computed from other fields)
Values computed from other fields in the same or related records.

| Table | Field | Derivation | Updated When |
|-------|-------|------------|--------------|
[List all ~12 DERIVED fields from research]
```
  </action>
  <verify>
Run `npm run type-check` to ensure schema.ts comments didn't break anything. Verify inline comments exist by searching: `grep -c "SNAPSHOT:\|CACHE:\|DERIVED:" convex/schema.ts` (should be ~50+). Verify SCHEMA.md has the new section.
  </verify>
  <done>
`convex/schema.ts` has inline denormalization comments on all ~50 denormalized fields using formal SNAPSHOT/CACHE/DERIVED categories. `docs/SCHEMA.md` has a Denormalization Patterns summary section. `npm run type-check` passes.
  </done>
</task>

</tasks>

<verification>
1. `docs/SCHEMA_AUDIT.md` exists and covers all 37+ tables
2. Every Category B field has a proposed default value for backfilling
3. Category C lists exactly 5 deprecated fields for removal
4. `convex/schema.ts` has SNAPSHOT/CACHE/DERIVED comments on all denormalized fields
5. `docs/SCHEMA.md` has a Denormalization Patterns section
6. `npm run type-check` passes
7. `npm run build` passes
</verification>

<success_criteria>
- Audit document categorizes all ~215 v.optional() fields into A/B/C/D categories
- Removal log documents all fields/tables to be removed with justification
- All denormalized fields in schema.ts have formal inline annotations (SNAPSHOT/CACHE/DERIVED)
- docs/SCHEMA.md has denormalization summary section
- npm run build passes (comments-only changes to schema.ts)
</success_criteria>

<output>
After completion, create `.planning/phases/08-schema-cleanup/08-01-SUMMARY.md`
</output>
