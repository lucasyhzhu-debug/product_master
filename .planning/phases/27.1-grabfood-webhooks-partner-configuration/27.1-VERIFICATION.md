---
phase: 27.1-grabfood-webhooks-partner-configuration
verified: 2026-02-26T12:30:00Z
status: passed
score: 14/14 must-haves verified
re_verification: false
---

# Phase 27.1: GrabFood Webhooks & Partner Configuration Verification Report

**Phase Goal:** Implement all 6 GrabFood inbound webhook endpoints (GET menu, submit order, order state, menu sync, integration status, menu push) with DB-stored HMAC secret, so the GrabFood App Simulator test cases work end-to-end. Frontend gets Webhooks tab with HMAC secret field + copyable webhook URLs, and Settings tab gains per-mapping GrabFood price and availability controls.
**Verified:** 2026-02-26T12:30:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (Plan 01)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | All 6 GrabFood webhook routes return HTTP 200 when called | VERIFIED | All 6 httpAction handlers in webhooks.ts return `new Response("OK", { status: 200 })`. All 6 routes registered in http.ts at correct paths. |
| 2 | HMAC validation reads secret from platformCredentials table, not env vars | VERIFIED | `resolveHmacSecret()` calls `ctx.runQuery(internal.platformCredentials.queries.getHmacSecret, { platformId: "grabfood" })`. No env var references in HMAC path. |
| 3 | Webhook events are logged to externalSyncLogs with syncType 'webhook' | VERIFIED | `handleWebhookCommon` calls `ctx.runMutation(internal.externalData.mutations.createSyncLog, { syncType: "webhook", ... })` after every event. GET /menu handler also writes syncLog. |
| 4 | GET /menu returns valid GrabFood Section-based menu JSON built from externalProductMappings | VERIFIED | `handleGetMenuWebhook` queries `listProductMappingsInternal`, filters `isAvailable !== false`, builds `{ sections: [{ categories: [{ items }] }] }` JSON with IDR currency and all-day service hours. Returns `Content-Type: application/json`. |
| 5 | Order webhook logs to syncLogs only — no writes to grabfoodOrders table | VERIFIED | `handleOrderWebhook` processPayload only calls `console.log`. No reference to `upsertOrder` or `grabfoodOrders` in webhooks.ts. |
| 6 | HMAC failures are logged as syncLog entries with status 'error' | VERIFIED | `handleWebhookCommon` writes `{ syncType: "webhook", status: "error", errorMessage: "HMAC failed: ${hmac.reason}" }` before returning 200 on HMAC failure. Same pattern in GET menu handler. |
| 7 | Shared syncType validator in schema.ts keeps schema, createSyncLog, and SyncLogEntry type in sync | VERIFIED | `export const syncType = v.union(...)` at line 31 of schema.ts; imported by `externalData/mutations.ts` at line 4; `SyncLogEntry` type in `platformCredentials/queries.ts` at line 178 includes `"webhook"`. |
| 8 | validateHmacSignature has unit tests covering valid signature, invalid signature, and missing secret cases | VERIFIED | `convex/__tests__/hmac.test.ts` has 5 test cases: valid sig, invalid sig, missing secret, missing header, empty body (GET scenario). |

### Observable Truths (Plan 02)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 9 | Admin can enter HMAC secret in Webhooks tab and save it to platformCredentials | VERIFIED | `WebhooksTab` component calls `useProtectedMutation(api.platformCredentials.mutations.saveHmacSecret)` with `{ platformId: "grabfood", hmacSecret }` on save. `saveHmacSecret` mutation does admin auth + db patch/insert. |
| 10 | Webhooks tab displays 6 copyable webhook URLs derived from Convex deployment URL | VERIFIED | `CONVEX_SITE_URL = import.meta.env.VITE_CONVEX_URL?.replace(".cloud", ".site") ?? ""`. 6 endpoints rendered with Copy button. All 6 paths match http.ts routes. |
| 11 | Admin can set GrabFood-specific price per product mapping in Settings tab | VERIFIED | Settings tab renders `GrabFood Price (IDR)` column. `handlePriceCommit` calls `updateMappingFields({ mappingId, grabfoodPrice: price })` which maps to `api.externalData.mutations.updateProductMappingFields`. |
| 12 | Admin can toggle product availability per mapping in Settings tab | VERIFIED | Settings tab renders `Available` column with Switch. `handleAvailabilityToggle` calls `updateMappingFields({ mappingId, isAvailable })`. |
| 13 | Sync error banner appears when last menu sync webhook had errors | VERIFIED | `useQuery(api.externalData.queries.getLatestWebhookError, { source: "grabfood" })` powers amber banner. Banner shows `webhookError.errorMessage` with dismiss X button via `errorDismissed` state. |
| 14 | Clipboard copy has try/catch fallback for non-HTTPS contexts | VERIFIED | `handleCopy` wraps `navigator.clipboard.writeText(url)` in try/catch. Catch block shows `toast.error("Copy failed -- use HTTPS or copy manually")`. |

**Score:** 14/14 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `convex/integrations/grabfood/webhooks.ts` | All 6 webhook handlers + shared handleWebhookCommon + HMAC resolution | VERIFIED | 324 lines. Exports: `validateHmacSignature`, `handleGetMenuWebhook`, `handleOrderWebhook`, `handleOrderStateWebhook`, `handleMenuSyncWebhook`, `handleIntegrationStatusWebhook`, `handleMenuPushWebhook`. `handleWebhookCommon` private helper wires 5 POST handlers. |
| `convex/http.ts` | 6 registered GrabFood webhook routes | VERIFIED | 6 routes registered at `/api/grabfood/menu` (GET), `/api/grabfood/order` (POST), `/api/grabfood/order/state` (POST), `/api/grabfood/menu-sync` (POST), `/api/grabfood/integration-status` (POST), `/api/grabfood/menu/push` (POST). |
| `convex/platformCredentials/queries.ts` | getHmacSecret internalQuery + SyncLogEntry type with 'webhook' | VERIFIED | `getHmacSecret` at line 155. `SyncLogEntry.syncType` union includes `"webhook"` at line 178. |
| `convex/schema.ts` | Shared syncType validator exported; hmacSecret field; grabfoodPrice/isAvailable fields | VERIFIED | `export const syncType` at line 31. `hmacSecret: v.optional(v.string())` at line 1182. `grabfoodPrice: v.optional(v.number())` at line 1128. `isAvailable: v.optional(v.boolean())` at line 1129. |
| `convex/__tests__/hmac.test.ts` | 5 unit tests for HMAC validation | VERIFIED | 68 lines, 5 `it()` cases covering all code paths including empty-body GET scenario. |
| `convex/platformCredentials/mutations.ts` | saveHmacSecret mutation with admin auth | VERIFIED | `saveHmacSecret` mutation at line 183. Uses `const user = await requireRole(ctx, args.token, ["admin"])`. Stores `user.name` as `updatedBy`. |
| `convex/externalData/mutations.ts` | syncType imported from schema; updateProductMappingFields mutation | VERIFIED | `import { externalSource, syncType } from "../schema"` at line 4. `createSyncLog` uses `syncType: syncType` at line 110. `updateProductMappingFields` mutation at line 522. |
| `convex/externalData/queries.ts` | getLatestWebhookError query | VERIFIED | At line 290. Queries `externalSyncLogs` by source, finds most recent webhook error within 24h, returns `{ timestamp, errorMessage }` or null. |
| `src/pages/GrabFoodManager.tsx` | Webhooks tab with HMAC + URLs; enhanced Settings tab with price/availability | VERIFIED | 5 tabs: Orders, Store Status, Menu, Settings, Webhooks. `WebhooksTab` component at line 1201. Settings tab has GrabFood Price and Available columns per mapping row. |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `webhooks.ts` | `platformCredentials/queries.ts` | `ctx.runQuery(internal.platformCredentials.queries.getHmacSecret)` | WIRED | Pattern found at line 71 of webhooks.ts |
| `webhooks.ts` | `externalData/mutations.ts` | `ctx.runMutation(internal.externalData.mutations.createSyncLog)` | WIRED | Called in `handleWebhookCommon` (line 95, 118) and GET menu handler (line 156, 233) |
| `http.ts` | `webhooks.ts` | Import + route registration for all 6 handlers | WIRED | All 6 handlers imported at lines 5-11 of http.ts; all 6 routes registered at lines 68-102 |
| `externalData/mutations.ts` | `schema.ts` | `import { externalSource, syncType } from "../schema"` | WIRED | Line 4 of externalData/mutations.ts |
| `GrabFoodManager.tsx` | `platformCredentials/mutations.ts` | `useProtectedMutation(api.platformCredentials.mutations.saveHmacSecret)` | WIRED | Line 1212-1213 of GrabFoodManager.tsx |
| `GrabFoodManager.tsx` | `externalData/mutations.ts` | `useMutation(api.externalData.mutations.updateProductMappingFields)` | WIRED | Line 982 of GrabFoodManager.tsx |
| `GrabFoodManager.tsx` | `externalData/queries.ts` | `useQuery(api.externalData.queries.getLatestWebhookError)` | WIRED | Line 1203 of GrabFoodManager.tsx |
| `webhooks.ts GET /menu` | `externalData/queries.ts` | `ctx.runQuery(internal.externalData.queries.listProductMappingsInternal)` | WIRED | Line 173 of webhooks.ts; `listProductMappingsInternal` spreads all mapping fields including `grabfoodPrice` and `isAvailable` |

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| WH-01 | 27.1-01 | All 6 endpoints return HTTP 200 and log to externalSyncLogs with syncType "webhook" | SATISFIED | 6 httpAction handlers confirmed, all return 200, all call createSyncLog with syncType "webhook" |
| WH-02 | 27.1-01 | HMAC-SHA256 reads from platformCredentials; failures logged with status "error" | SATISFIED | `resolveHmacSecret` uses `getHmacSecret` internalQuery; HMAC failure path writes `{ status: "error" }` syncLog |
| WH-03 | 27.1-01 | GET /menu builds Section-based JSON from externalProductMappings with price override and availability toggle | SATISFIED | `handleGetMenuWebhook` filters `isAvailable !== false`, uses `grabfoodPrice ?? menuProduct?.defaultPrice ?? 0`, returns valid section structure |
| WH-04 | 27.1-02 | Admin can enter HMAC secret in Webhooks tab and view/copy all 6 webhook URLs | SATISFIED | WebhooksTab component with show/hide input, save button, 6 endpoint rows with Copy buttons |
| WH-05 | 27.1-02 | Admin can set GrabFood-specific price and toggle availability per product mapping | SATISFIED | Settings tab with GrabFood Price (IDR) input column and Available Switch column, both backed by `updateProductMappingFields` mutation |

**Orphaned requirements:** None. All 5 WH-0x requirements declared in plans match REQUIREMENTS.md entries for Phase 27.1.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | — | — | No TODO/FIXME/placeholder stubs found in implementation files. No empty returns. No console-only handlers. |

---

## Human Verification Required

### 1. GrabFood App Simulator end-to-end test

**Test:** Start Convex dev, navigate to GrabFood Manager > Webhooks tab. Enter a test HMAC secret, save it. Point GrabFood App Simulator at the deployed Convex site URL endpoints (copy each URL). Run all 6 test cases in the App Simulator.
**Expected:** All 6 test cases return success in the App Simulator. Sync logs appear in the database with syncType "webhook" and status "success".
**Why human:** Cannot simulate GrabFood's App Simulator programmatically. Requires real HTTP calls to the deployed Convex httpAction endpoint with proper GrabFood HMAC signatures.

### 2. Clipboard copy behaviour in HTTPS vs non-HTTPS

**Test:** Navigate to Webhooks tab on localhost (non-HTTPS). Click a Copy button.
**Expected:** Error toast appears: "Copy failed -- use HTTPS or copy manually". No JS exception thrown.
**Why human:** `navigator.clipboard.writeText` behaviour in non-HTTPS is browser-dependent. The try/catch code path must be verified live.

### 3. GET /menu response with real product mappings

**Test:** Ensure at least one product mapping exists for source "grabfood" with a `grabfoodPrice` set. Call `GET /api/grabfood/menu` directly.
**Expected:** JSON response contains the mapped product in the Snacks category with the custom GrabFood price (not the default menu product price).
**Why human:** Requires live Convex database with seeded grabfood product mappings to verify price priority logic.

---

## Gaps Summary

None. All 14 must-haves verified. All 5 requirement IDs satisfied. No anti-patterns found. No missing artifacts, no stubs, no unwired connections.

---

_Verified: 2026-02-26T12:30:00Z_
_Verifier: Claude (gsd-verifier)_
