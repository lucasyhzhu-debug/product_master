---
phase: 19-gofood-depot-management-and-kitchen-production-targets
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/GoFoodDepotManager.tsx
  - src/components/gofoodDepot/DepotCockpitTable.tsx
  - src/components/gofoodDepot/DepotStockTransferDialog.tsx
autonomous: true
requirements:
  - GF-02
  - GF-03
gap_closure: true

must_haves:
  truths:
    - "Build passes with no TypeScript errors (no Id type mismatch in GoFoodDepotManager.tsx)"
    - "Stock transfer dialog opens and allows stock movement for all outlets that have a linked storage location"
    - "destinationLocationId flows from selectedOutlet.linkedStorageLocationId through the component chain to the dialog"
  artifacts:
    - path: "src/pages/GoFoodDepotManager.tsx"
      provides: "Passes destinationLocationId prop to DepotCockpitTable and casts depotStock as any[]"
    - path: "src/components/gofoodDepot/DepotCockpitTable.tsx"
      provides: "Accepts and threads destinationLocationId into setTransferDialogProduct and DepotStockTransferDialog"
    - path: "src/components/gofoodDepot/DepotStockTransferDialog.tsx"
      provides: "Renders correctly when destinationLocationId is provided"
  key_links:
    - from: "src/pages/GoFoodDepotManager.tsx"
      to: "src/components/gofoodDepot/DepotCockpitTable.tsx"
      via: "destinationLocationId prop = selectedOutlet?.linkedStorageLocationId"
      pattern: "destinationLocationId"
    - from: "src/components/gofoodDepot/DepotCockpitTable.tsx"
      to: "src/components/gofoodDepot/DepotStockTransferDialog.tsx"
      via: "transferDialogProduct.destinationLocationId"
      pattern: "destinationLocationId.*transferDialogProduct"
---

<objective>
Fix the build-breaking type mismatch and wire the missing destinationLocationId through the depot transfer component chain so stock transfers work for all outlets.

Purpose: Two diagnosed bugs prevent the transfer dialog from functioning — a TypeScript build blocker (union type mismatch) and a runtime wire gap (destinationLocationId never passed to the dialog).
Output: Build passes, transfer dialog opens successfully for any outlet with a linked storage location.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-gofood-depot-management-and-kitchen-production-targets/19-UAT.md
@src/pages/GoFoodDepotManager.tsx
@src/components/gofoodDepot/DepotCockpitTable.tsx
@src/components/gofoodDepot/DepotStockTransferDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix build type mismatch — cast depotStock as any[] in GoFoodDepotManager</name>
  <files>src/pages/GoFoodDepotManager.tsx</files>
  <action>
    Line 198 of GoFoodDepotManager.tsx passes `depotStock={depotStock ?? []}` to DepotCockpitTable. The `getDepotStock` query returns a union type `Id<"productInventory"> | Id<"gofoodDepotStock">` depending on code path, but DepotCockpitTable only accepts `Id<"productInventory">`. This causes the TypeScript build to fail.

    Fix: Change line 198 from:
      `depotStock={depotStock ?? []}`
    to:
      `depotStock={(depotStock ?? []) as any[]}`

    This is identical to the pattern already used on lines 200-201 for stockGrouped and storageLocations. No logic change — pure type cast.
  </action>
  <verify>Run `npm run type-check` — must pass with no errors in GoFoodDepotManager.tsx</verify>
  <done>No TypeScript error on line 198 of GoFoodDepotManager.tsx; type-check exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Wire destinationLocationId from GoFoodDepotManager through DepotCockpitTable to DepotStockTransferDialog</name>
  <files>
    src/pages/GoFoodDepotManager.tsx
    src/components/gofoodDepot/DepotCockpitTable.tsx
    src/components/gofoodDepot/DepotStockTransferDialog.tsx
  </files>
  <action>
    Root cause: `selectedOutlet?.linkedStorageLocationId` exists in the DB and is resolved in GoFoodDepotManager, but it is never passed down. The dialog shows "This outlet does not have a linked storage location" for ALL outlets because destinationLocationId is always undefined.

    **Step 1 — GoFoodDepotManager.tsx:**
    Add `destinationLocationId` prop to the `<DepotCockpitTable>` call:
    ```tsx
    <DepotCockpitTable
      outletId={resolvedOutletId}
      depotStock={(depotStock ?? []) as any[]}
      restockMap={restockMap}
      stockGrouped={(stockGrouped ?? []) as any[]}
      storageLocations={(storageLocations ?? []) as any[]}
      destinationLocationId={selectedOutlet?.linkedStorageLocationId}
    />
    ```

    **Step 2 — DepotCockpitTable.tsx:**
    a) Add `destinationLocationId?: Id<"storageLocations">` to the `DepotCockpitTableProps` interface.
    b) Destructure `destinationLocationId` in the component function signature.
    c) In the `transferDialogProduct` state type, add `destinationLocationId?: Id<"storageLocations">`.
    d) In the `setTransferDialogProduct` call (lines 312-318), include `destinationLocationId`:
    ```tsx
    setTransferDialogProduct({
      menuProductId: row.menuProductId,
      menuProductName: row.menuProductName,
      destinationLocationId,
    })
    ```
    e) Pass `destinationLocationId={transferDialogProduct.destinationLocationId}` to `<DepotStockTransferDialog>`.

    **Step 3 — DepotStockTransferDialog.tsx (verification only):**
    Confirm that the existing `destinationLocationId` prop is already accepted by the dialog component (it is — the amber warning at line 197 fires when `!destinationLocationId`, so the prop is already wired, just never supplied). No changes needed to DepotStockTransferDialog.tsx.

    Do NOT change any mutation logic, validation, or business rules.
  </action>
  <verify>
    1. `npm run type-check` passes
    2. Navigate to /gofood-depot, select any outlet with a linked storage location (e.g. Tamtem Depot)
    3. Click "Move here" on any product row
    4. Dialog opens — amber warning "This outlet does not have a linked storage location" must NOT appear
    5. Source location dropdown shows available locations; entering a quantity and clicking "Move Stock" completes without error
  </verify>
  <done>Transfer dialog opens with no amber warning for outlets that have linkedStorageLocationId set in DB; stock transfer completes successfully</done>
</task>

</tasks>

<verification>
`npm run build` passes (no TypeScript errors).
Transfer dialog functional for all linked outlets — amber warning only shown for truly unlinked outlets.
</verification>

<success_criteria>
- [ ] `npm run type-check` exits 0
- [ ] `npm run build` succeeds
- [ ] Transfer dialog opens for Tamtem Depot without the "no linked storage location" warning
- [ ] Stock transfer completes and stock count updates
</success_criteria>

<output>
After completion, create `.planning/phases/19-gofood-depot-management-and-kitchen-production-targets/19-06-SUMMARY.md`
</output>
