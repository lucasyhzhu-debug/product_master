---
phase: 14-order-qol
plan: 07
type: execute
wave: 1
depends_on: ["14-06"]
gap_closure: true
files_modified:
  - convex/orders/mutations/orderCrud.ts
  - convex/orders/mutations/statusUpdates.ts
  - convex/orders/queries.ts
autonomous: true

must_haves:
  truths:
    - "Order creation (Draft) logs an orderEvent with type 'created' including userId"
    - "getAuditTrail returns the creation event as the earliest entry in the timeline"
    - "Slide-over order query resolves createdByUserId to user name, not raw createdBy string"
    - "moveForward auto-sets expedited=true when transitioning to PaymentReceived and dueDate is today or tomorrow"
    - "Auto-expedited orders also auto-enter kitchen (PaymentReceived → BeingPrepared) like manual expedite"
    - "npm run build passes"
  artifacts:
    - path: "convex/orders/mutations/orderCrud.ts"
      provides: "Draft creation logs audit event"
      contains: "logOrderEvent"
    - path: "convex/orders/mutations/statusUpdates.ts"
      provides: "Auto-expedite logic on PaymentReceived transition"
      contains: "auto.*expedit|dueDate.*today"
---

<objective>
Fix backend gaps from Phase 14 UAT: creator name resolution, audit trail Draft event, and auto-expedite for today/tomorrow orders.

Purpose: Correct 3 backend issues — GAP-01 (creator name), GAP-02 (missing Draft audit event), GAP-03 (auto-expedite).
Output: Backend mutations and queries properly log creation events, resolve user names, and auto-expedite urgent orders.
</objective>

## Git Workflow
**Branch:** `feature/order-qol` (existing)
**Checkpoints:** None (autonomous)

## Implementation Waves
### Wave 1: Backend fixes [SEQUENTIAL]
| Agent | Task | Files |
|-------|------|-------|
| convex-backend | All 3 backend fixes | convex/orders/mutations/orderCrud.ts, convex/orders/mutations/statusUpdates.ts, convex/orders/queries.ts |

### Wave 2: Verification [SEQUENTIAL]
| Agent | Task |
|-------|------|
| code-auditor | Type check + pattern compliance |
| Bash | npm run build |

## Documentation Updates
- [ ] CHANGELOG.md (append to Phase 14 section)

## Success Criteria
- [ ] `npm run build` passes
- [ ] New Draft orders create an audit event
- [ ] Slide-over shows actual user name, not "admin"
- [ ] Orders due today/tomorrow auto-expedite on payment

<tasks>

<task type="auto">
  <name>Task 1: Backend gap fixes (creator, audit trail, auto-expedite)</name>
  <files>
    convex/orders/mutations/orderCrud.ts
    convex/orders/mutations/statusUpdates.ts
    convex/orders/queries.ts
  </files>
  <action>
    **GAP-01: Creator name in slide-over**

    In `convex/orders/queries.ts`, find the query used by OrderSlideOver (likely `getById` or similar).
    Add `createdByUserId` → user name resolution, same pattern as `listForKanban` lines 1102-1107:
    ```
    let creatorName = order.createdBy;
    if (order.createdByUserId) {
      const user = await ctx.db.get(order.createdByUserId);
      if (user) creatorName = user.name;
    }
    ```
    Return `creatorName` in the response.

    **GAP-02: Draft creation audit event**

    In `convex/orders/mutations/orderCrud.ts`, after `ctx.db.insert("orders", ...)`:
    - Import `logOrderEvent` from the helpers
    - Call `logOrderEvent(ctx, orderId, "created", { toStatus: "Draft", triggeredBy: "user", userId })` where userId comes from the session token
    - This ensures the audit trail starts from order creation

    Also in `getAuditTrail` query: ensure "created" event type renders correctly (it's already generic enough — just verify the frontend AuditTrail component handles event.type === "created").

    **GAP-03: Auto-expedite on PaymentReceived for today/tomorrow**

    In `convex/orders/mutations/statusUpdates.ts`, in `moveForward` handler:
    After computing `nextStatus`, before the `ctx.db.patch`:
    - If `nextStatus === "PaymentReceived"`:
      - Check order.dueDate: if dueDate is today or tomorrow (use WIB timezone via convex/lib/wibDate.ts helpers)
      - If yes: set `updates.expedited = true` and change `nextStatus` to `"BeingPrepared"` (skip PaymentReceived, go straight to kitchen)
      - Also trigger kitchen entry logic (consume materials, set kitchenEnteredAt)
      - Log an "auto_expedited" event in audit trail
    - This mirrors `expediteOrder` mutation logic but triggers automatically
  </action>
  <verify>
    `npm run build` passes. `grep "logOrderEvent" convex/orders/mutations/orderCrud.ts` returns match. `grep "auto.*expedit\|dueDate" convex/orders/mutations/statusUpdates.ts` returns match.
  </verify>
  <done>
    Draft creation logs audit event. Creator name resolves from users table. Today/tomorrow orders auto-expedite on payment.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes
- New orders show creator name (not "admin") in slide-over
- Audit trail includes "Order created" as first entry
- Order with today due date auto-expedites when payment received
</verification>
