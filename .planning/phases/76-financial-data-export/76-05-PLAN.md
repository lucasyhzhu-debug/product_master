---
phase: 76-financial-data-export
plan: 05
type: execute
wave: 4
depends_on:
  - "76-04"
files_modified:
  - tests/e2e/financial-data-export.spec.ts
  - .planning/phases/76-financial-data-export/76-UAT.md
  - docs/CHANGELOG.md
  - docs/API_REFERENCE.md
  - docs/ROADMAP.md
  - docs/FILE_MAP.md
autonomous: false
requirements:
  - FIN-03
  - FIN-04
tags: [e2e, playwright, uat, docs]
must_haves:
  truths:
    - "Playwright happy-path test passes: navigate from /financials → click Export range → set range → click Generate → both files download with correct filename patterns"
    - "Playwright role-gate test passes: kitchen user redirected away from /financials/export — uses loginAsRole(page, 'kitchen') directly (Improvement 10), NO test.skip fallback"
    - "Playwright filename-WIB-date test passes: date input range → assert downloaded filename matches selected dates exactly (M6 — Pitfall #4 mitigation)"
    - "Manual UAT signed-off: CSV opens in Excel + Google Sheets without formula execution, IDR renders as integer, filename WIB-correct"
    - "Wave ordering: Playwright (5.1) → UAT (5.2) → triple-review (NEW Wave 3) → docs sweep (5.3) → merge (5.4) — G3"
    - "Documentation updated post-merge: CHANGELOG, API_REFERENCE, ROADMAP, FILE_MAP"
  artifacts:
    - path: "tests/e2e/financial-data-export.spec.ts"
      provides: "Playwright happy-path + role-gate redirect tests + filename-WIB-date assertion (M6)"
    - path: ".planning/phases/76-financial-data-export/76-UAT.md"
      provides: "Manual verification checklist (formula injection visual confirm, IDR integer, multi-file download)"
    - path: "docs/CHANGELOG.md"
      provides: "v2.0 entry for Phase 76 — Financial Data Export"
    - path: "docs/API_REFERENCE.md"
      provides: "New `convex/reports/financialExport` queries documented"
    - path: "docs/ROADMAP.md"
      provides: "FIN-03 + FIN-04 marked complete"
    - path: "docs/FILE_MAP.md"
      provides: "/financials/export route added to permission table"
  key_links:
    - from: "tests/e2e/financial-data-export.spec.ts"
      to: "FinancialExportPage at /financials/export"
      via: "page.goto + page.getByRole interactions"
      pattern: "financials/export"
    - from: "tests/e2e/financial-data-export.spec.ts"
      to: "tests/e2e/helpers.ts:loginAsRole"
      via: "import { loginAsRole } from \"./helpers\""
      pattern: "loginAsRole\\(page,\\s*\"kitchen\"\\)"
---

## Git Workflow

**Branch:** `gsd/phase-76-financial-data-export` (this plan finalizes the feature branch; merge to main triggers post-merge docs sweep).

**Checkpoints:**
- Wave 2 task 5.2: `checkpoint:human-verify` — Manual UAT in Excel + Google Sheets (cannot be automated; opens spreadsheets in user's environment).
- Wave 3 task 5.3: `checkpoint:human-verify` — Triple-review approval (per CLAUDE.md MEMORY: "NEVER skip triple-review in execute-phase"). Triple-review runs BEFORE docs sweep + merge per G3.
- Wave 4 task 5.5: `checkpoint:human-verify` — Approval to merge feature branch to main.

## Implementation Waves

### Wave 1: Playwright E2E [SEQUENTIAL]

| Agent | Task | Files |
|-------|------|-------|
| code-author | Task 5.1: Implement Playwright happy-path + role-gate redirect (loginAsRole) + filename-WIB-date specs | tests/e2e/financial-data-export.spec.ts |

### Wave 2: Manual UAT [BLOCKING CHECKPOINT]

| Agent | Task | Files |
|-------|------|-------|
| code-author | Task 5.2: Author UAT.md checklist + execute manual checks | .planning/phases/76-financial-data-export/76-UAT.md |

### Wave 3: Triple-review [BLOCKING CHECKPOINT — runs BEFORE docs/merge per G3]

| Agent | Task |
|-------|------|
| user (checkpoint) | Task 5.3: Run triple-review against feature branch diff vs main; resolve Critical/Important findings before proceeding |

### Wave 4: Post-merge docs + merge [SEQUENTIAL]

| Agent | Task | Files |
|-------|------|-------|
| code-author | Task 5.4: Update CHANGELOG, API_REFERENCE, ROADMAP, FILE_MAP | docs/CHANGELOG.md, docs/API_REFERENCE.md, docs/ROADMAP.md, docs/FILE_MAP.md |
| user (checkpoint) | Task 5.5: Approve merge to main | gsd/phase-76-financial-data-export → main |

**Wave reordering rationale (G3):** Per CLAUDE.md MEMORY `feedback_triple_review_mandatory.md` — triple-review must run on the diff between feature branch and main BEFORE the docs commits land. This catches issues that would otherwise pollute the docs PR. New order: E2E pass → UAT signed → triple-review (Critical/Important resolved) → docs sweep → merge.

## Documentation Updates

- [x] `docs/CHANGELOG.md` — Phase 76 entry under v2.0 (Required by CLAUDE.md after every merge)
- [x] `docs/API_REFERENCE.md` — Document `getRawTransactionsExport`, `getMultiPeriodPLExport`, `getExportPreflight`
- [x] `docs/ROADMAP.md` — Mark FIN-03, FIN-04 complete; mark Phase 76 complete
- [x] `docs/FILE_MAP.md` — Add `/financials/export` route to "Full Role → Route Permission Table" with manager+admin access

## Success Criteria

- [ ] `npx playwright test tests/e2e/financial-data-export.spec.ts` exits 0 (happy-path + role-gate redirect using loginAsRole + filename-WIB-date)
- [ ] UAT.md checklist signed (all manual checks: Excel opens cleanly, Google Sheets opens cleanly, formula NOT executed, IDR integer, filename WIB-correct)
- [ ] Triple-review run BEFORE docs sweep (G3); all Critical and Important findings resolved
- [ ] All 4 docs updated and committed in post-merge sweep
- [ ] No `test.skip` in role-gate test (Improvement 10 — `loginAsRole(page, "kitchen")` works directly)

---

<objective>
Provide end-to-end automated coverage and the user-facing manual UAT for Phase 76. Playwright covers the happy-path (navigation → form → Generate → download), role-gate redirect (using `loginAsRole(page, "kitchen")` per Improvement 10 — no skip fallback), and filename-WIB-date correctness (M6 — Pitfall #4 mitigation). UAT.md covers spreadsheet rendering quality and timezone correctness that automated tests cannot verify. Triple-review runs BEFORE docs/merge (G3). Post-merge documentation sweep closes out the v2.0 milestone progress for FIN-03 + FIN-04.

Purpose: This is the verification gate. Plans 01-04 deliver code + unit tests; this plan exercises the integrated system from a real browser, confirms the deliverable matches the roadmap success criteria, and surfaces any cross-cutting issues via triple-review BEFORE the merge commit lands. Without this gate, the phase cannot be marked complete.

Output: Playwright spec + UAT.md + triple-review checkpoint + 4 docs updates + merge.
</objective>

<execution_context>
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/workflows/execute-plan.md
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/76-financial-data-export/76-CONTEXT.md
@.planning/phases/76-financial-data-export/76-RESEARCH.md
@.planning/phases/76-financial-data-export/76-VALIDATION.md
@.planning/phases/76-financial-data-export/76-UI-SPEC.md
@.planning/phases/76-financial-data-export/76-04-PLAN.md

@tests/e2e/income-statement-uat.spec.ts
@tests/e2e/helpers.ts
@docs/CHANGELOG.md
@docs/API_REFERENCE.md
@docs/ROADMAP.md
@docs/FILE_MAP.md

<interfaces>
<!-- Playwright helpers (existing — verified by direct read of tests/e2e/helpers.ts:31) -->
From `tests/e2e/helpers.ts`:
- `loginAsRole(page: Page, role: TestRole): Promise<void>` — line 31. `TestRole = "admin" | "manager" | "kitchen" | "order_staff"`. Logs in via avatar grid + PIN pad against the E2E-{Role} test user. **Use this directly for the role-gate test (Improvement 10) — no skip fallback needed.**
- `loginAsManager(page)` — line 115 (backward-compat wrapper).
- `waitForDataLoad(page)`, `screenshot(page, name)` — utility helpers.

<!-- Filename patterns for download assertions (D-11) -->
- Raw: `frollie-transactions-{8-digit-start}-{8-digit-end}.csv`
- P&L: `frollie-pl-summary-{8-digit-start}-{8-digit-end}-{weekly|monthly|custom}.csv`

<!-- Manual journal entry path (verified) -->
The manual journal entry route is `/journal` (NOT `/manual-journal`). Verified by direct read of `src/App.tsx:457` — `<Route path="journal" ...>`. UAT step 1 (formula-injection seed) must use `/journal`.
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 5.1: Playwright happy-path + role-gate redirect (loginAsRole) + filename-WIB-date specs</name>
  <files>tests/e2e/financial-data-export.spec.ts</files>
  <read_first>
    - tests/e2e/income-statement-uat.spec.ts (analog — same shape: loginAsManager, navigate, interact, screenshot)
    - tests/e2e/helpers.ts (full — confirm `loginAsRole`, `loginAsManager`, `waitForDataLoad`, `screenshot` exports; `TestRole` type accepts `"kitchen"`)
    - .planning/phases/76-financial-data-export/76-PATTERNS.md §"tests/e2e/financial-data-export.spec.ts (NEW — Playwright E2E)" (lines 699-740)
    - .planning/phases/76-financial-data-export/76-VALIDATION.md (rows 76-04-01, 76-04-02)
    - .planning/phases/76-financial-data-export/76-UI-SPEC.md (copy strings used in role/text matchers)
  </read_first>
  <behavior>
    - Test "happy path: navigate → set range → click Generate → assert downloads": Login as manager, go to `/financials`, click "Export range…", click "Last week" preset, wait for preflight, click Generate, assert two files downloaded with the verbatim filename pattern.
    - Test "only Raw transactions selected: single file download with frollie-transactions-..." Variant of happy-path.
    - Test "disabled state: Generate disabled when no type selected": Uncheck both; assert button disabled with the verbatim tooltip.
    - Test "role gate: kitchen role redirects away from /financials/export" — uses `loginAsRole(page, "kitchen")` directly (Improvement 10 — NO skip fallback, NO try/catch).
    - Test "filename WIB date matches selected range (M6 — Pitfall #4 mitigation)": Login as manager, navigate to `/financials/export`, manually enter From=`2026-04-13` and To=`2026-04-19` in the date inputs, click Generate, assert the downloaded filenames are exactly `frollie-transactions-20260413-20260419.csv` (and matching P&L). Confirms timezone-correct mapping with no off-by-one.
  </behavior>
  <action>
Create `tests/e2e/financial-data-export.spec.ts`:

```typescript
import { test, expect } from "@playwright/test";
import { loginAsManager, loginAsRole, waitForDataLoad, screenshot } from "./helpers";

test.describe("Phase 76 UAT: Financial Data Export", () => {
  test("happy path: navigate to export page and trigger downloads", async ({ page }) => {
    await loginAsManager(page);
    await page.goto("/financials");
    await waitForDataLoad(page);

    // Navigate via the new "Export range…" button on /financials
    await page.getByRole("link", { name: /export range/i }).click();
    await page.waitForURL("**/financials/export");

    // Both export types are checked by default per UI-SPEC
    await expect(page.getByLabel(/Raw transactions/i)).toBeChecked();
    await expect(page.getByLabel(/P&L summary/i)).toBeChecked();

    // Apply "Last week" preset (prior ISO week per Improvement 9)
    await page.getByRole("button", { name: /^Last week$/i }).click();

    // Wait for preflight to populate (matches the verbatim copy "Range covers ... journal entries")
    await expect(page.locator('text=/Range covers \\d+ journal entries/')).toBeVisible({ timeout: 10_000 });

    // Click Generate; assert TWO file downloads
    const [download1, download2] = await Promise.all([
      page.waitForEvent("download"),
      page.waitForEvent("download"),
      page.getByRole("button", { name: /generate exports/i }).click(),
    ]);

    // D-11 — verbatim filename templates
    const fn1 = download1.suggestedFilename();
    const fn2 = download2.suggestedFilename();
    const filenames = [fn1, fn2];
    expect(filenames.some(f => /^frollie-transactions-\d{8}-\d{8}\.csv$/.test(f))).toBe(true);
    expect(filenames.some(f => /^frollie-pl-summary-\d{8}-\d{8}-(weekly|monthly|custom)\.csv$/.test(f))).toBe(true);

    await screenshot(page, "uat-76-01-happy-path");
  });

  test("happy path: only Raw transactions selected", async ({ page }) => {
    await loginAsManager(page);
    await page.goto("/financials/export");

    // Uncheck P&L
    await page.getByLabel(/P&L summary/i).uncheck();

    // Granularity radio should disappear
    await expect(page.getByText(/Granularity/i)).toHaveCount(0);

    // Apply "Last month" preset (Last week was already covered above)
    await page.getByRole("button", { name: /^Last month$/i }).click();
    await expect(page.locator('text=/Range covers \\d+ journal entries/')).toBeVisible({ timeout: 10_000 });

    // Click Generate; assert ONE file download
    const [download] = await Promise.all([
      page.waitForEvent("download"),
      page.getByRole("button", { name: /generate exports/i }).click(),
    ]);
    expect(download.suggestedFilename()).toMatch(/^frollie-transactions-\d{8}-\d{8}\.csv$/);
  });

  test("disabled state: Generate disabled when no type selected", async ({ page }) => {
    await loginAsManager(page);
    await page.goto("/financials/export");
    await page.getByLabel(/Raw transactions/i).uncheck();
    await page.getByLabel(/P&L summary/i).uncheck();
    const btn = page.getByRole("button", { name: /generate exports/i });
    await expect(btn).toBeDisabled();
    await expect(btn).toHaveAttribute("title", "Select at least one export type.");
  });

  // Improvement 10 — use loginAsRole directly. No skip fallback. tests/e2e/helpers.ts:31 supports "kitchen".
  test("role gate: kitchen role redirects away from /financials/export", async ({ page }) => {
    await loginAsRole(page, "kitchen");
    await page.goto("/financials/export");
    // Expect redirect — kitchen should NOT remain on /financials/export.
    await expect(page).not.toHaveURL(/\/financials\/export/, { timeout: 5_000 });
  });

  test("role gate: order_staff role redirects away from /financials/export", async ({ page }) => {
    await loginAsRole(page, "order_staff");
    await page.goto("/financials/export");
    await expect(page).not.toHaveURL(/\/financials\/export/, { timeout: 5_000 });
  });

  // M6 — filename WIB date matches user selection. Pitfall #4 mitigation.
  test("filename WIB date matches user-selected date range (M6, Pitfall #4)", async ({ page }) => {
    await loginAsManager(page);
    await page.goto("/financials/export");

    // Manually enter a known date range: 2026-04-13 (Mon) to 2026-04-19 (Sun, inclusive label).
    // The "To" input shows the inclusive end date; periodEnd is stored as next-day midnight.
    await page.getByLabel(/^From$/i).fill("2026-04-13");
    await page.getByLabel(/^To$/i).fill("2026-04-19");

    // Wait for preflight to update (debounced 300ms)
    await page.waitForTimeout(500);
    await expect(page.locator('text=/Range covers \\d+ journal entries/')).toBeVisible({ timeout: 10_000 });

    // Click Generate; both files
    const [download1, download2] = await Promise.all([
      page.waitForEvent("download"),
      page.waitForEvent("download"),
      page.getByRole("button", { name: /generate exports/i }).click(),
    ]);

    const filenames = [download1.suggestedFilename(), download2.suggestedFilename()];

    // Filename must encode the user-selected dates exactly:
    //   transactions: frollie-transactions-20260413-20260419.csv
    //   pl: frollie-pl-summary-20260413-20260419-{granularity}.csv
    expect(filenames).toEqual(expect.arrayContaining([
      expect.stringMatching(/^frollie-transactions-20260413-20260419\.csv$/),
    ]));
    expect(filenames).toEqual(expect.arrayContaining([
      expect.stringMatching(/^frollie-pl-summary-20260413-20260419-(weekly|monthly|custom)\.csv$/),
    ]));
  });
});
```

**Key assertions:**
- Happy path covers full UI flow + multi-file download.
- Filename regex matches D-11 verbatim templates.
- Granularity hidden when P&L unchecked (cross-check of plan 04 RTL test against real DOM).
- Role-gate test uses `loginAsRole(page, "kitchen")` directly (Improvement 10) — no skip pattern.
- M6 filename-WIB-date test confirms timezone correctness (Pitfall #4).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/financial-data-export.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/e2e/financial-data-export.spec.ts`
    - At least 6 `test("..."` cases (added M6 + order_staff): `[ "$(grep -cE 'test\\(\"' tests/e2e/financial-data-export.spec.ts)" -ge 6 ]`
    - Imports loginAsRole (Improvement 10): `grep -q "import.*loginAsRole.*from.*helpers" tests/e2e/financial-data-export.spec.ts`
    - Uses loginAsRole(page, "kitchen") directly — NO test.skip fallback (Improvement 10): `grep -q "loginAsRole(page, \"kitchen\")" tests/e2e/financial-data-export.spec.ts && ! grep -E "test\\.skip\\(.*loginAsKitchen|loginAsKitchen.*test\\.skip" tests/e2e/financial-data-export.spec.ts`
    - Filename pattern assertions present: `grep -q "frollie-transactions-\\\\d{8}-\\\\d{8}" tests/e2e/financial-data-export.spec.ts && grep -q "frollie-pl-summary-\\\\d{8}-\\\\d{8}-" tests/e2e/financial-data-export.spec.ts`
    - Filename-WIB-date test (M6) — exact-match `20260413-20260419` literal: `grep -q "20260413-20260419" tests/e2e/financial-data-export.spec.ts`
    - Role-gate test present: `grep -q "role gate" tests/e2e/financial-data-export.spec.ts`
    - Disabled-state assertion present: `grep -q "Select at least one export type" tests/e2e/financial-data-export.spec.ts`
    - All non-skipped tests pass: `npx playwright test tests/e2e/financial-data-export.spec.ts 2>&1 | grep -E "(failed|FAIL)" | (! grep .)`
  </acceptance_criteria>
  <done>
    Playwright happy-path + disabled-state + role-gate (no skip — Improvement 10) + filename-WIB-date (M6) tests pass. Screenshots captured for happy path.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5.2: Manual UAT — Excel + Google Sheets verification</name>
  <files>.planning/phases/76-financial-data-export/76-UAT.md</files>
  <what-built>
    Phase 76 financial data export UI and CSV serializers.
  </what-built>
  <how-to-verify>
    Author `.planning/phases/76-financial-data-export/76-UAT.md` with the following checklist, then execute each step:

    ```markdown
    # Phase 76 UAT — Financial Data Export

    **Tested by:** _______________
    **Date:** _______________
    **Environment:** _______________

    ## Prerequisites
    - Manager or admin account
    - Production data has at least 1 confirmed order in the last week
    - Browsers available: Chrome (current), Firefox (current), Safari (if Mac/iOS)

    ## Setup — formula injection seed (D-14)
    Before UAT, seed a manual journal entry with the description literal `=SUM(A1:A10)` (use `/journal` page — verified at src/App.tsx:457). Note the JE entry date so it falls inside the test range.

    ## Checks

    ### 1. Navigation + role gate
    - [ ] Login as manager. Visit `/financials`. Confirm "Export range…" button visible next to "Export CSV" in the header.
    - [ ] Click "Export range…". Confirm URL is `/financials/export` and form is visible.
    - [ ] Logout. Login as kitchen role. Try to visit `/financials/export` directly. Confirm redirect to kitchen landing page (NOT the export page).
    - [ ] Logout. Login as order_staff. Confirm same redirect.

    ### 2. Form interaction (UI-SPEC)
    - [ ] Both export-type checkboxes default to checked.
    - [ ] Granularity section visible by default (P&L checked).
    - [ ] Uncheck "P&L summary". Granularity section disappears.
    - [ ] Re-check "P&L summary". Granularity reappears, "Weekly" selected by default.
    - [ ] Click "Last week" preset. Both date pickers update — From=Monday, To=Sunday (prior ISO week per Improvement 9). Active preset chip shows ring highlight.
    - [ ] Click "Custom" preset. Manually type a different date. Verify preflight refreshes within ~1s (300ms debounce — Improvement 4).
    - [ ] Set start > end. Generate button disabled with tooltip "End date must be on or after start date."
    - [ ] Uncheck both checkboxes. Generate button disabled with tooltip "Select at least one export type."

    ### 3. Preflight stats (D-12, D-16)
    - [ ] Set range with known data. Confirm "Range covers N journal entries, M revenue rows, X periods." appears.
    - [ ] Numbers render in tabular-nums (re-render doesn't shift digits visually).
    - [ ] Set a very large range (e.g., entire year of data). Confirm `Large range` warning Alert appears with amber surface and AlertTriangle icon (only if seeded data has >10k JE lines; skip if not).

    ### 4. Generate + multi-file download (D-11)
    - [ ] With both types checked + "Last week" preset, click "Generate exports".
    - [ ] Two file downloads triggered. Filenames match `frollie-transactions-YYYYMMDD-YYYYMMDD.csv` and `frollie-pl-summary-YYYYMMDD-YYYYMMDD-weekly.csv`.
    - [ ] Filename dates correspond to WIB-interpreted prior-ISO-week range (NOT off-by-one from UTC).
    - [ ] Toast "Downloaded transactions and P&L summary CSVs." appears.

    ### 5. Single-file + granular toast (Improvement 7)
    - [ ] Uncheck "Raw transactions". Click "Generate exports".
    - [ ] Only ONE file downloaded (`frollie-pl-summary-...`).
    - [ ] Toast "Downloaded frollie-pl-summary-...csv." appears.
    - [ ] (If possible — pick a date range that has zero raw transactions but has P&L data, e.g., a future-dated range that has revenue but no JE postings yet.) With BOTH types checked, click Generate. Confirm toast "P&L downloaded; no raw transactions in range." appears (NOT the generic "Downloaded both CSVs" toast).

    ### 6. CSV correctness — Excel
    Open `frollie-transactions-*.csv` in Microsoft Excel:
    - [ ] First row contains 12 column headers in D-01 order: `entry_date, je_id, je_number, je_type, account_code, account_name, debit_idr, credit_idr, description, source_doc_type, source_doc_id, created_by`.
    - [ ] Find the JE we seeded with `=SUM(A1:A10)` description. Confirm cell renders the LITERAL string (prefixed with apostrophe `'=SUM(A1:A10)`), NOT a formula result. Excel should NOT compute or display a number.
    - [ ] `debit_idr` and `credit_idr` columns: every row has exactly one populated, the other is `0`. Numbers render as integers (no decimals).
    - [ ] Dates render as YYYY-MM-DD; pasting back into a date column verifies WIB correctness.

    ### 7. CSV correctness — Google Sheets
    Upload the same `frollie-transactions-*.csv` to Google Sheets:
    - [ ] Same 12 column headers.
    - [ ] `=SUM(A1:A10)` description cell renders the literal string (apostrophe prefix); Sheets does not execute the formula.
    - [ ] Integer rendering same as Excel.

    ### 8. Multi-period P&L
    Open `frollie-pl-summary-*.csv` in Excel:
    - [ ] First row = 8-column header: `period, section, channel, line_item, amount_idr, confidence, prev_period_idr, delta_pct`.
    - [ ] Second row = annotation `# Multi-period export — prev_period_idr compares against the immediately prior period within the file.`
    - [ ] First period's body rows have empty `prev_period_idr` and `delta_pct` cells.
    - [ ] Second period's body rows have non-empty `prev_period_idr` (matches first period's `amount_idr`).
    - [ ] Bottom of file: single block starting with `# Data Quality Notes (range-aggregated)` — NOT repeated per period.
    - [ ] Footer shows real product names + counts (NOT all zeros — Critical 3 follow-through). If unmapped products exist in seeded data, names appear in `# Unmapped products: ...` line.

    ### 9. Browser compatibility (D-11 multi-file download)
    - [ ] Repeat step 4 in Chrome — both files download.
    - [ ] Repeat step 4 in Firefox — both files download (may prompt for permission on first attempt; grant + retry).
    - [ ] Repeat step 4 in Safari (if available) — both files download.

    ## Sign-off

    - [ ] All checks pass: ☐ yes ☐ no
    - [ ] Issues found: __________________________________________________
    - [ ] Approved for merge: ☐ yes ☐ no — Signed: _________________
    ```

    Then execute every check and record results inline. Stop and report any failure to the operator before merge.
  </how-to-verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/76-financial-data-export/76-UAT.md`
    - All 9 check sections present: `[ "$(grep -cE '^### [0-9]' .planning/phases/76-financial-data-export/76-UAT.md)" -ge 9 ]`
    - Sign-off section present: `grep -q "Sign-off" .planning/phases/76-financial-data-export/76-UAT.md`
    - Manual journal path verified — uses `/journal` (R6): `grep -q "/journal" .planning/phases/76-financial-data-export/76-UAT.md && ! grep -q "/manual-journal" .planning/phases/76-financial-data-export/76-UAT.md`
    - All checkboxes checked (after manual run): `! grep -E "^- \\[ \\]" .planning/phases/76-financial-data-export/76-UAT.md` (only after sign-off)
  </acceptance_criteria>
  <resume-signal>Type "approved" once all UAT checks pass, or describe failures to fix.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5.3: Triple-review feature branch BEFORE docs sweep (G3)</name>
  <what-built>
    Plans 01-04 complete: refactor, backend queries, frontend helpers, page, route, RTL+E2E tests, manual UAT.
  </what-built>
  <how-to-verify>
    Per CLAUDE.md MEMORY ("NEVER skip triple-review in execute-phase") and staffreview G3: run `/triple-review` against the diff between `gsd/phase-76-financial-data-export` and `main`. This must happen BEFORE the docs sweep (Wave 4 task 5.4) and BEFORE merge (Wave 4 task 5.5).

    Address every Critical and Important finding in a follow-up commit on the same branch. Re-run triple-review until all Critical and Important findings are resolved or accepted with documented justification.

    Output: triple-review report saved to `docs/reviews/triple-review-phase-76-{date}.md`.

    **Why before docs:** Triple-review may surface code issues that require follow-up commits. If those commits land AFTER the docs PR, the docs PR commit-list is incomplete and CHANGELOG drifts. Running triple-review FIRST keeps the docs PR honest.
  </how-to-verify>
  <acceptance_criteria>
    - Triple-review report committed: `test -f docs/reviews/triple-review-phase-76-*.md`
    - All Critical findings resolved (or explicitly documented as accepted): manual sign-off
    - All Important findings resolved (or explicitly documented as accepted): manual sign-off
    - No `git status` modifications outstanding before proceeding to Wave 4
  </acceptance_criteria>
  <resume-signal>Type "review-clean" once triple-review findings are resolved, or describe blockers.</resume-signal>
</task>

<task type="auto">
  <name>Task 5.4: Post-merge documentation sweep</name>
  <files>docs/CHANGELOG.md, docs/API_REFERENCE.md, docs/ROADMAP.md, docs/FILE_MAP.md</files>
  <read_first>
    - docs/CHANGELOG.md (top of file — find current v2.0 section to append into)
    - docs/API_REFERENCE.md (find query documentation section — match existing format)
    - docs/ROADMAP.md (find Phase 76 + FIN-03/FIN-04 entries)
    - docs/FILE_MAP.md (find "Full Role → Route Permission Table")
    - .planning/phases/76-financial-data-export/76-CONTEXT.md (D-13 role gate for FILE_MAP entry)
    - CLAUDE.md "After every merge to main" section
  </read_first>
  <behavior>
    - Behavior 1: CHANGELOG.md gets a new Phase 76 entry under v2.0 with bullet points for FIN-03 + FIN-04 deliverables.
    - Behavior 2: API_REFERENCE.md gets a new "Financial Export" section with the 3 queries documented (args, return shape, role gate).
    - Behavior 3: ROADMAP.md marks `[ ] Phase 76` → `[x] Phase 76` with completion date; marks FIN-03 + FIN-04 in REQUIREMENTS.md traceability table from `Pending` → `Complete`.
    - Behavior 4: FILE_MAP.md adds a row for `/financials/export` to the role permission table with manager+admin access.
  </behavior>
  <action>
**docs/CHANGELOG.md** — Add a new entry at the top of the v2.0 section:

```markdown
## [Phase 76 — Financial Data Export] — 2026-MM-DD (merge `<commit-sha>`)

### Added (FIN-03, FIN-04)
- New `/financials/export` page (manager + admin) for raw GL transaction export and multi-period P&L summary export
- Backend queries `convex/reports/financialExport.ts` — `getRawTransactionsExport`, `getMultiPeriodPLExport`, `getExportPreflight`, all role-gated to manager+admin
- Shared `convex/lib/periodBuckets.ts` — single source of truth for weekly/monthly/custom period bucketing (imported by both backend and frontend)
- Frontend helpers `src/lib/financialExportHelpers.ts` — `buildExportFilenames`, `presetToRange`, `formatWeekLabel`/`formatMonthLabel`, `generateRawTransactionsCSV`, `generateMultiPeriodPLCSV`
- New `useDebouncedValue` hook (`src/hooks/useDebouncedValue.ts`) — real 300ms debounce for preflight subscription
- "Export range…" entry button on `/financials` PageHeader
- Pre-flight stats panel showing journal-line / revenue-row / period counts with >10k soft warning (D-12, D-16)
- Filename templates: `frollie-transactions-{YYYYMMDD}-{YYYYMMDD}.csv` and `frollie-pl-summary-{YYYYMMDD}-{YYYYMMDD}-{granularity}.csv` (WIB)
- "Last week" preset semantics: prior ISO week (Mon-Sun) in WIB

### Changed
- `src/lib/csvExport.ts` — `generateIncomeStatementCSV` now delegates body row construction to a new exported `buildIncomeStatementRows` helper; helper computes deltas internally from `(current, previous)` (zero behavior change for existing callers)
- `convex/reports/incomeStatement.ts` — `fetchAndAggregate` now exported with optional `includePrevious: boolean = true` parameter (preserves existing call sites; multi-period export passes `false`); `WeekData` and `GapAnalysis` interfaces also exported
- `convex/lib/periodRange.ts` — `WIB_OFFSET_MS` now exported

### Tests
- `convex/reports/__tests__/financialExport.test.ts` — convex-test integration coverage for D-01..D-04, D-08 (real rangeGap aggregation), D-12, D-13, D-14, D-16 (concrete >10k seeding) + Phase 70 COGS-override regression
- `src/lib/__tests__/financialExportHelpers.test.ts` — Vitest helper coverage for D-05, D-06, D-08, D-11, D-14, D-15 + UI-SPEC preset ranges + path-traversal mitigation + year-boundary weekly bucket + prior-ISO-week semantics + format-label partial suffix
- `src/pages/__tests__/FinancialExportPage.test.tsx` — RTL component coverage (granularity hidden, validation tooltips, filename preview deterministic via vi.setSystemTime, loading state)
- `tests/e2e/financial-data-export.spec.ts` — Playwright happy-path + role-gate redirect (kitchen + order_staff via loginAsRole) + filename-WIB-date (Pitfall #4)

### Security
- T-76-01 mitigated: server-side `requireRole(["manager","admin"])` on every new query + frontend `<ProtectedRoute allowedRoles={["manager","admin"]}>`
- T-76-02 mitigated: every CSV cell (header + body) flows through `escapeCell` (CWE-1236 formula injection)
- T-76-03 mitigated: filenames built from numeric epoch + literal granularity union; defensive `^\d{8}$` check

### Schema
- No schema changes. No new indexes. No new dependencies.
```

**docs/API_REFERENCE.md** — Add a new section (locate near other report query docs, e.g., near `incomeStatement` queries):

```markdown
### Financial Export (Phase 76)

**Module:** `convex/reports/financialExport.ts`
**Role gate:** `manager`, `admin`

#### `getRawTransactionsExport`
Returns one row per `journalEntryLines` entry in the date range, denormalized with parent JE + account fields.

| Arg | Type | Description |
|-----|------|-------------|
| periodStart | number | Epoch ms — inclusive start |
| periodEnd | number | Epoch ms — exclusive end |
| token | string | Session token |

Returns: `RawTransactionRow[]` with `{ entryDate, journalEntryId, entryNumber, sourceType, accountCode, accountName, debitAmount, creditAmount, description, sourceId, createdByName, _creationTime }` per row. Sorted by entryDate ASC, entryNumber ASC, _creationTime ASC. Reversal rows (`sourceType` ending in `_void`) are included.

#### `getMultiPeriodPLExport`
Loops `fetchAndAggregate` over weekly/monthly/custom buckets within the range; returns one period record per bucket plus a range-aggregated gap analysis (real `WeekData.gapAnalysis` shapes).

| Arg | Type | Description |
|-----|------|-------------|
| periodStart | number | Epoch ms — inclusive start |
| periodEnd | number | Epoch ms — exclusive end |
| granularity | "weekly" \| "monthly" \| "custom" | Bucket size |
| token | string | Session token |

Returns: `{ periods: Array<{ bucketStart, bucketEnd, label, current: WeekData }>, rangeGap: { totalProducts, totalMappedProducts, unmappedProducts: Array<{name,count,revenue}>, missingChannels: Array<{source,displayName,reason}>, zeroCostComponents: Array<{name,code}> } }`.

#### `getExportPreflight`
Cheap parallel index-bound counts for the UI preflight panel.

| Arg | Type | Description |
|-----|------|-------------|
| periodStart | number | Epoch ms |
| periodEnd | number | Epoch ms |
| granularity | "weekly" \| "monthly" \| "custom" | For periodCount |
| token | string | Session token |

Returns: `{ journalLineCount, revenueRowCount, periodCount, isLargeRange }`. `isLargeRange === journalLineCount > 10000` (D-16 soft warning threshold).
```

**docs/ROADMAP.md** — Update Phase 76 + traceability:

```markdown
- [x] **Phase 76: Financial Data Export** - Raw transaction and P&L summary CSV export with date range picker (completed 2026-MM-DD)
```

In the requirements traceability section (or `.planning/REQUIREMENTS.md` if that's where it lives — read the file first):

```markdown
| FIN-03 | Phase 76 | Complete |
| FIN-04 | Phase 76 | Complete |
```

Also flip the bullets at the top of REQUIREMENTS.md:
```
- [x] **FIN-03**: User can export raw financial transactions (revenue + journal entries) as CSV for a date range
- [x] **FIN-04**: User can export P&L summary as CSV for weekly/monthly/custom range
```

**docs/FILE_MAP.md** — Add to the "Full Role → Route Permission Table":

```markdown
| `/financials/export` | manager, admin | manager, admin | Phase 76 — Financial Data Export | Raw GL + P&L summary CSV exports for accountant handoff |
```

(Adapt column structure to match the existing table format — read FILE_MAP.md first.)

Commit each doc change with the canonical message format `docs(76): <change>` and reference the merge commit SHA from main.
  </action>
  <verify>
    <automated>grep -q "Phase 76" docs/CHANGELOG.md && grep -q "getRawTransactionsExport" docs/API_REFERENCE.md && grep -q "Phase 76.*completed\\|\\[x\\] \\*\\*Phase 76" docs/ROADMAP.md && grep -q "/financials/export" docs/FILE_MAP.md</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG entry: `grep -q "Phase 76" docs/CHANGELOG.md && grep -q "FIN-03" docs/CHANGELOG.md && grep -q "FIN-04" docs/CHANGELOG.md`
    - API_REFERENCE entries: `grep -q "getRawTransactionsExport" docs/API_REFERENCE.md && grep -q "getMultiPeriodPLExport" docs/API_REFERENCE.md && grep -q "getExportPreflight" docs/API_REFERENCE.md`
    - ROADMAP marks Phase 76 complete: `grep -E "^\\- \\[x\\] \\*\\*Phase 76:" docs/ROADMAP.md`
    - REQUIREMENTS.md traceability: FIN-03 + FIN-04 marked Complete (read .planning/REQUIREMENTS.md after edit)
    - FILE_MAP.md has new route: `grep -q "/financials/export" docs/FILE_MAP.md`
  </acceptance_criteria>
  <done>
    All 4 docs updated. Phase complete from documentation perspective.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5.5: Approval to merge feature branch to main</name>
  <what-built>
    Plans 01-05 complete: refactor, backend queries, frontend helpers, page, route, RTL+E2E tests, manual UAT, triple-review, docs sweep.
  </what-built>
  <how-to-verify>
    Triple-review (Wave 3 task 5.3) and docs sweep (task 5.4) are complete. Time to merge.

    1. Switch to main: `git switch main && git pull`
    2. Merge: `git merge --no-ff gsd/phase-76-financial-data-export -m "Phase 76: Financial Data Export"`
    3. Push: `git push origin main`
    4. Confirm CI green: `gh run list --limit 5`
    5. Update `.planning/STATE.md` to mark Phase 76 complete and increment completed_phases counter.

    Per CLAUDE.md "Branch from main before starting a new phase": after this merge, the next phase MUST branch from main (NOT from another feature branch).
  </how-to-verify>
  <acceptance_criteria>
    - Merge commit on main: `git log origin/main --oneline -5 | grep -i "Phase 76"`
    - CI green post-merge: `gh run list --branch main --limit 1` reports success
    - `.planning/STATE.md` updated: completed_phases incremented and `last_activity` updated
    - Doc commits landed on main (CHANGELOG, API_REFERENCE, ROADMAP, FILE_MAP)
  </acceptance_criteria>
  <resume-signal>Type "merged" once feature branch is merged and CI is green, or describe blockers.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test environment ↔ production | Playwright runs against dev/staging only — no production interaction. UAT runs against production data with manager+admin role (read-only export, no mutation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01 (E2E layer) | Spoofing / Elevation of Privilege | Playwright `role gate redirect` test | mitigate | Test confirms kitchen role is redirected — uses `loginAsRole(page, "kitchen")` directly (Improvement 10). No skip fallback; the test cannot falsely report green. |
| T-76-02 (UAT layer) | Tampering | Manual Excel/Sheets formula-injection test | mitigate | UAT step 6 + 7 require seeding `=SUM(A1:A10)` and visually confirming non-execution in both Excel and Google Sheets. Sign-off blocks merge until verified. |
| T-76-PL05-01 | Information Disclosure | Documentation could leak sensitive query details | accept | Public docs (CHANGELOG, API_REFERENCE) describe query shapes — same level of detail as other phases' docs. No secrets or PII exposed. |
</threat_model>

<verification>
After all 5 tasks complete:
1. `npx playwright test tests/e2e/financial-data-export.spec.ts` — passes (>= 6 tests, including kitchen + order_staff role-gate via `loginAsRole`, plus filename-WIB-date M6 test)
2. `.planning/phases/76-financial-data-export/76-UAT.md` exists, signed off
3. Triple-review run BEFORE docs sweep (G3); findings resolved
4. All 4 docs (CHANGELOG, API_REFERENCE, ROADMAP, FILE_MAP) updated
5. Merged to main; CI green
6. STATE.md updated to reflect Phase 76 complete
</verification>

<success_criteria>
- [ ] Playwright E2E spec passes (>= 6 tests, including happy-path with multi-file download assertion + role-gate redirect via `loginAsRole(page, "kitchen")` + filename-WIB-date M6)
- [ ] No `test.skip` fallback in role-gate test (Improvement 10)
- [ ] UAT.md authored AND signed off (all checks pass) — uses `/journal` not `/manual-journal` (R6)
- [ ] Triple-review run BEFORE docs sweep + merge (G3); all Critical/Important findings resolved
- [ ] CHANGELOG, API_REFERENCE, ROADMAP, FILE_MAP all updated
- [ ] gsd/phase-76-financial-data-export merged to main with CI green
- [ ] STATE.md updated: Phase 76 marked complete
- [ ] FIN-03 + FIN-04 marked complete in REQUIREMENTS.md traceability table
</success_criteria>

<output>
After completion, create `.planning/phases/76-financial-data-export/76-05-SUMMARY.md` documenting:
- Playwright test count + filename pattern assertion result + filename-WIB-date assertion (M6)
- UAT.md sign-off status + any caveats discovered
- Triple-review findings summary (count by severity, all resolved) — ran BEFORE docs/merge per G3
- Merge commit SHA + CI status
- v2.0 milestone progress: FIN-03 + FIN-04 → 2 more requirements complete (now X/Y in v2.0)
</output>
