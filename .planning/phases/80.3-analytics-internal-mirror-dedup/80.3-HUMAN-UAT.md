# Phase 80.3 — Human UAT Checklist

**Plan:** `.planning/phases/80.3-analytics-internal-mirror-dedup/80.3-01-PLAN.md`
**Branch:** `gsd/phase-80.3-analytics-internal-mirror-dedup`
**Dev environment:** `dev:exciting-fennec-671` (run `npx convex dev` in one terminal, `npm run dev` in another)
**Prod environment (post-merge only):** `prod:decisive-wombat-7`

---

## Pre-flight (run these before manual UAT)

- [x] `npm run type-check` passes (Wave 3.2)
- [x] `npm run test` passes — 1633 tests including 13 new in `unitEconomics.test.ts` and 2 updated in `unitEconomics-unlinked.test.ts` (Wave 3.4)
- [x] `npm run build` succeeds — exit 0 (Wave 3.5)
- [ ] On dev branch: `npm run dev` running, `/analytics` and `/k3mart-cockpit` both load without console errors

---

## Method 1 — Headline KPI cross-check (dev parity, ~2 min)

Open both pages with **All Time** filter, all channels, all products.

| # | Item | Pre-fix expected | Post-fix expected | Pass? |
|---|------|-------------------|--------------------|-------|
| 1 | `/analytics` loads in ≤2s with All Time filter | — | — | [ ] |
| 2 | `/k3mart-cockpit` Overview tab loads in ≤2s with All Time filter | — | — | [ ] |
| 3 | Analytics Revenue (Net) vs Sales Net Sales — delta within 1% (discount/commission accounting wedge) | ~Rp 130M apart | ≤1% apart | [ ] |
| 4 | Analytics Units Sold vs Sales Balls Sold — exact match (both BOM-derived) | 9,493 vs 8,876 (delta 617) | exact match | [ ] |
| 5 | Analytics Orders vs Sales Lifetime Transactions — exact match | 2,629 vs 2,364 (delta 265) | exact match | [ ] |
| 6 | Analytics Channel Breakdown: Direct channel no longer 2x larger than (whatsapp + instagram) sum from orders table | Direct 2x inflated | Direct ~ correct | [ ] |
| 7 | 7d filter on `/analytics`: numbers look plausible for the recent week | — | — | [ ] |
| 8 | Channel Economics widget shows GoFood with non-zero units + non-zero net (negative regression sanity check) | non-zero | non-zero | [ ] |

If row 4 or 5 fails: R5 skip is not effective. Stop and inspect `convex/reports/unitEconomics.ts:163`.
If row 8 fails: R5 widened too far (gobiz being skipped). Stop and inspect — Test 12b should have caught this.

---

## Method 2 — Production parity check (post-deploy only)

After Wave 4 merge → main → Vercel/Convex deploy:

- [ ] Open prod `/analytics` All Time
- [ ] Revenue dropped from ~Rp 517M → ~Rp 387M (matches debug session math: -Rp 129,936,000)
- [ ] Units dropped 9,493 → 8,876 (delta 617 BOM-expanded balls)
- [ ] Orders dropped 2,629 → 2,364 (delta 265 duplicate Direct orders)
- [ ] Open prod `/k3mart-cockpit` Overview All Time and confirm Analytics now matches it on Units + Orders
- [ ] No console errors on either page
- [ ] Mobile responsive view of `/analytics` still renders correctly

---

## Notes for the human reviewer

- The Revenue delta between Analytics (Net) and Sales (Gross) of ~20% is an **expected commission/discount accounting wedge** — both pipelines should agree on Gross, but Net deducts platform commissions which Sales aggregates differently. Only investigate if this gap is much larger than 20%.
- Unit + Order counts MUST match exactly (both are BOM-derived from the same `externalRevenue` source for non-Direct channels, and from the same `orders` table for Direct).
- If a target was set from inflated pre-fix values, the new (correct) numbers are the real baseline. CHANGELOG documents the magnitude of the change for stakeholders.

---

## Sign-off

- [ ] All Method 1 rows pass on dev — initials + date: __________
- [ ] All Method 2 rows pass on prod — initials + date: __________
- [ ] Phase 80.3 ready for `/gsd-verify-work 80.3` and ROADMAP.md mark-complete
