---
phase: 20-production-ingredient-tracking-and-cogs
plan: 06
type: execute
wave: 4
depends_on: ["20-04", "20-05"]
files_modified:
  - src/pages/DispatchPlanner.tsx
  - src/components/dispatchPlanner/MaterialsCheckPanel.tsx
  - src/hooks/convex/index.ts
  - docs/CHANGELOG.md
  - docs/SCHEMA.md
  - docs/API_REFERENCE.md
autonomous: true

must_haves:
  truths:
    - "Materials Check panel in dispatch planner shows both packaging AND ingredient sufficiency"
    - "Simulate button triggers combined check showing projected resupply dates"
    - "npm run build succeeds with zero errors"
    - "All new hooks are exported from hooks/convex/index.ts"
    - "Documentation updated with schema changes and new API endpoints"
  artifacts:
    - path: "src/components/dispatchPlanner/MaterialsCheckPanel.tsx"
      provides: "Combined materials check panel for dispatch planner"
    - path: "docs/CHANGELOG.md"
      provides: "Phase 20 changelog entry"
    - path: "docs/SCHEMA.md"
      provides: "Updated with new tables and field extensions"
  key_links:
    - from: "src/components/dispatchPlanner/MaterialsCheckPanel.tsx"
      to: "convex/dispatchPlanner/queries.ts"
      via: "useQuery for simulateInventory"
      pattern: "api\\.dispatchPlanner\\.simulateInventory"
    - from: "src/pages/DispatchPlanner.tsx"
      to: "src/components/dispatchPlanner/MaterialsCheckPanel.tsx"
      via: "renders MaterialsCheckPanel"
      pattern: "MaterialsCheckPanel"
---

<objective>
Wire the dispatch planner Materials Check panel to show combined packaging + ingredient simulation, verify full build, and update documentation.

Purpose: Final integration and verification -- ensures all pieces connect and the build passes.
Output: Working Materials Check panel + clean build + updated docs
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-production-ingredient-tracking-and-cogs/20-04-SUMMARY.md
@.planning/phases/20-production-ingredient-tracking-and-cogs/20-05-SUMMARY.md
@.planning/phases/20-production-ingredient-tracking-and-cogs/20-RESEARCH.md
@src/pages/DispatchPlanner.tsx
@convex/dispatchPlanner/queries.ts
@docs/CHANGELOG.md
@docs/SCHEMA.md
@docs/API_REFERENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Materials Check panel + Dispatch Planner wiring</name>
  <files>src/components/dispatchPlanner/MaterialsCheckPanel.tsx, src/pages/DispatchPlanner.tsx</files>
  <action>
**MaterialsCheckPanel.tsx** -- Create a new component for the combined materials check:

Props: `startDate: string` (the current planner week start date)

Layout:
1. Header: "Materials Check" with a "Simulate" button (Lucide `Calculator` icon)
2. When Simulate is clicked, call useQuery for `api.dispatchPlanner.simulateInventory` with the startDate. Before click, show placeholder text: "Click Simulate to check material sufficiency for the planned week."
3. After simulation loads, show two sections:

**Packaging Materials** section:
- List each day with status indicator (green check / amber warning / red X)
- For days with shortages: list component name, required vs available, deficit
- This matches the existing simulation output format

**Ingredient Materials** section (NEW):
- Same day-by-day view as packaging
- For each ingredient shortage: name, required vs available, deficit
- Below the day grid: "Ingredient Resupply Forecast" table showing:
  - Ingredient name | Current Stock | 7-Day Requirement | Runs Out By
  - "Runs Out By" shows the day name (e.g., "Wednesday") in red if within the window, "OK" in green if sufficient
  - Format: projected resupply date from ingredientStatus in simulation response

Styling:
- Card-based layout matching existing dispatch planner components
- Use shadcn Table for the resupply forecast
- Red/amber/green color coding for status
- Collapsible sections (default expanded)

**DispatchPlanner.tsx** -- Wire the MaterialsCheckPanel:
1. Find where the existing simulation/inventory check is rendered (may be inline or a simple status)
2. Replace or augment with `<MaterialsCheckPanel startDate={currentWeekStart} />`
3. The panel should be at the bottom of the planner page (below the main grid) or in a collapsible section
4. Ensure the Simulate button is clearly visible and labeled

**Hook exports** -- Ensure `src/hooks/convex/index.ts` exports all new hooks from useProductionRecipes.ts:
- useProductionRecipe, useProductionCogs, useProductionComponentsWithTiers
- useAddSubComponent, useRemoveSubComponent, useAddIngredient, useRemoveIngredient
- useUpdateIngredientQuantity, useUpdateSubComponentQuantity
  </action>
  <verify>Run `npm run type-check`. Verify MaterialsCheckPanel renders both packaging and ingredient sections. Verify DispatchPlanner includes the panel.</verify>
  <done>Materials Check panel shows combined packaging + ingredient simulation. Simulate button triggers 7-day check. Resupply forecast shows "runs out by" dates. All hooks exported from index.</done>
</task>

<task type="auto">
  <name>Task 2: Full build verification + documentation updates</name>
  <files>docs/CHANGELOG.md, docs/SCHEMA.md, docs/API_REFERENCE.md</files>
  <action>
**Build verification:**
1. Run `npm run type-check` -- must pass with zero errors
2. Run `npm run build` -- must succeed (tsc + vite build)
3. If any errors, fix them before proceeding to documentation

**docs/CHANGELOG.md** -- Add Phase 20 entry at the top:
```markdown
## Phase 20: Production Ingredient Tracking & COGS (2026-02-17)

### Added
- Hierarchical production component recipes (sub-components + direct ingredients, up to 3 tiers deep)
- Recipe editor modal on ProductionComponentsManager (click row to open)
- COGS toggle per-component: Manual vs Calculated (with manual value preserved as fallback)
- Live COGS preview in recipe editor with partial calculation warnings
- COGS breakdown tooltip showing full ingredient hierarchy
- Food ingredient FIFO inventory tracking (same as packaging -- batches, receive dialog, low-stock alerts)
- Ingredient deduction on order fulfillment (full hierarchy trace, warn-but-allow for insufficient stock)
- Negative stock display with red highlight and warning icon
- Combined Materials Check panel in dispatch planner (packaging + ingredients)
- 7-day ingredient resupply forecast with projected depletion dates

### Changed
- ProductionComponentsManager: tier-based sorting (highest first), batchSize and cogsMode fields in create/edit dialogs
- Inventory Production tab: includes ingredient rows with type badges
- Dispatch planner: simulateInventory returns both packaging and ingredient sufficiency data
- componentTypes: relaxed production+trackInventory restriction for ingredient tracking
- Cost invalidation: cascades ingredient price changes through production component hierarchy to menu products

### Schema
- New table: productionComponentLinks (hierarchical sub-component links)
- New table: productionComponentIngredients (direct ingredient links)
- Extended: componentTypes (+batchSize, batchSizeUnit, cogsMode, manualUnitCostIdr, cachedCalculatedCogs, cogsCacheUpdatedAt, cogsMissingCount)
- Extended: ingredients (+ingredientComponentTypeId for inventory tracking link)
```

**docs/SCHEMA.md** -- Add the two new tables with field descriptions. Update componentTypes section with new fields. Update ingredients section with new field.

**docs/API_REFERENCE.md** -- Add the new productionRecipes module:
- Mutations: addSubComponent, removeSubComponent, addIngredient, removeIngredient, updateIngredientQuantity, updateSubComponentQuantity
- Queries: getRecipeForComponent, calculateCogs, getComponentsWithTiers
- Internal: recalculateComponentCogs, invalidateProductionComponentCosts
- Extended: simulateInventory (now includes ingredient data)
  </action>
  <verify>Run `npm run build` -- must pass. Verify CHANGELOG, SCHEMA, and API_REFERENCE docs are updated.</verify>
  <done>Build passes with zero errors. Documentation fully updated with Phase 20 changes. All new tables, fields, and API endpoints documented.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- MaterialsCheckPanel shows both packaging and ingredient simulation
- Simulate button works in dispatch planner
- All hooks exported from index.ts
- CHANGELOG.md has Phase 20 entry
- SCHEMA.md has new tables and field extensions
- API_REFERENCE.md has new productionRecipes module
</verification>

<success_criteria>
- [ ] `npm run build` succeeds
- [ ] Materials Check panel shows combined packaging + ingredient simulation
- [ ] Ingredient resupply forecast with "runs out by" dates
- [ ] All hooks properly exported
- [ ] CHANGELOG.md updated
- [ ] SCHEMA.md updated
- [ ] API_REFERENCE.md updated
</success_criteria>

<output>
After completion, create `.planning/phases/20-production-ingredient-tracking-and-cogs/20-06-SUMMARY.md`
</output>
