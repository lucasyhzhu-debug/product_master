---
phase: 25-codebase-cleanup
plan: 06
type: execute
wave: 4
depends_on: ["25-01", "25-05"]
files_modified: []
autonomous: false
requirements:
  - CLEANUP-DARK-MODE
  - CLEANUP-HOOK-RENAME
  - CLEANUP-PROTECTED-MUTATION
  - CLEANUP-QUERY-FACTORY

must_haves:
  truths:
    - "npm run type-check passes with zero errors"
    - "npm run build succeeds with no errors"
    - "npm run test passes"
    - "Zero useConvex-prefixed hook names exist anywhere in src/"
    - "No hardcoded bg-white/bg-gray-N/text-gray-N without dark: counterparts in the 18 target files"
    - "protectedMutation wrapper used in all role-protected order and productionRecipes mutations"
  artifacts:
    - path: ".planning/phases/25-codebase-cleanup/25-06-SUMMARY.md"
      provides: "Final verification report"
  key_links: []
---

<objective>
Final verification sweep for Phase 25. Runs automated checks, performs grep sweeps to confirm all 4 streams completed correctly, and pauses for human visual verification of dark mode in the browser.

Purpose: Phase 25 made systemic changes across many files — hook renames, mutation wrappers, color classes. A final sweep catches any stragglers before merge to main.

Output: Green type-check, green build, green tests, human-verified dark mode. Phase ready to merge.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25-codebase-cleanup/25-01-SUMMARY.md
@.planning/phases/25-codebase-cleanup/25-02-SUMMARY.md
@.planning/phases/25-codebase-cleanup/25-03-SUMMARY.md
@.planning/phases/25-codebase-cleanup/25-04-SUMMARY.md
@.planning/phases/25-codebase-cleanup/25-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification sweeps</name>
  <files></files>
  <action>
Run the following checks in order. Report results. Fix any issues found.

CHECK 1 — Type check:
npm run type-check
Must exit 0. If errors, fix them.

CHECK 2 — Build:
npm run build
Must succeed. If errors, fix them.

CHECK 3 — Tests:
npm run test
Must pass. If failures, fix them.

CHECK 4 — Hook rename completeness:
grep -r "useConvex" src/ --include="*.tsx" --include="*.ts"
Expect: ZERO matches. If any found, these are stragglers from the rename — fix them.

CHECK 5 — Dark mode completeness (hardcoded colors sweep):
grep -rn "className=.*bg-white\|className=.*bg-gray-[0-9]\|className=.*text-gray-[0-9]\|className=.*border-gray-[0-9]" src/ --include="*.tsx"
Inspect each hit. Any occurrence in the 18 target files that lacks a dark: counterpart on the same element = straggler to fix.
Note: some gray classes may be on valid colored-badge elements — review context before "fixing."

CHECK 6 — protectedMutation coverage:
grep -n "export const.*= mutation(" convex/orders/mutations/orderCrud.ts convex/orders/mutations/statusUpdates.ts convex/orders/mutations/itemCrud.ts convex/orders/mutations/packaging.ts convex/orders/mutations/inventoryIntegration.ts convex/productionRecipes/mutations.ts
Inspect hits — each bare mutation() in these files should be a public/internal mutation. If any have requireRole inside them, they should have been migrated.

CHECK 7 — Query factory (informational):
grep -l "listAll\|queryHelpers" convex/menuProducts/queries.ts convex/whatsappTemplates/queries.ts convex/kitchenConfig/queries.ts
Confirm at least 2 of 3 files show queryHelpers adoption.

Report all check results. Fix any stragglers before proceeding to checkpoint.
  </action>
  <verify>
All 7 checks above pass with expected results. No stragglers found (or all found stragglers fixed and checks re-run to confirm).
  </verify>
  <done>All automated checks pass. type-check green. build green. tests green. Zero useConvex prefix. Dark mode straggler audit complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 25 Codebase Cleanup is complete:
- Stream 1: Dark mode fixed in 18 src/ files including WhatsApp preview bubble dark mode
- Stream 2: 161 hooks renamed (useConvex prefix removed) across 15 hook files
- Stream 3: protectedMutation applied to orders/ and productionRecipes/ mutations; frontend hooks updated to useSessionMutation
- Stream 4: queryHelpers applied to 2-3 candidate query files
  </what-built>
  <how-to-verify>
1. Start the dev servers: npx convex dev (Terminal 1) + npm run dev (Terminal 2)
2. Toggle dark mode in the app (check Header or Settings for theme toggle)
3. Verify these pages look correct in dark mode — no white/gray boxes that don't adapt:
   - Order Manager (kanban board, voucher input area)
   - Order Create page
   - Inventory Manager (TransferStockDialog, StatCard)
   - Menu Products (POS Preview panel)
   - WhatsApp Templates Manager — specifically the LIVE PREVIEW bubble:
     * Should show dark chat background (very dark, near-black like WhatsApp's own dark mode)
     * Sent message bubble should be dark teal (#005c4b) not light green
     * NOT a generic card/muted background
   - Ingredients Manager
   - Kitchen View (ShiftSuccessScreen, KitchenOrderCard)
4. Create or open an order — confirm order creation still works (protectedMutation migration)
5. No console errors about "missing sessionId" or "Unauthorized"
  </how-to-verify>
  <resume-signal>Type "approved" if dark mode looks correct and orders work, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 25 is complete when:
- [ ] npm run type-check passes
- [ ] npm run build succeeds
- [ ] npm run test passes
- [ ] grep -r "useConvex" src/ returns zero matches
- [ ] Human approved dark mode visual verification
- [ ] Order creation still works (no runtime auth errors)
</verification>

<success_criteria>
- All 4 streams fully implemented and verified
- Zero useConvex prefix remaining in src/
- Dark mode correct in all 18 fixed files (including WhatsApp preview)
- protectedMutation in orders/ and productionRecipes/ working at runtime
- npm run type-check + build + test all pass
- Human approved visual verification
</success_criteria>

<output>
After completion, create .planning/phases/25-codebase-cleanup/25-06-SUMMARY.md with:
- Results of each automated check (pass/fail + any stragglers fixed)
- Human verification result
- Final state confirmation
- Ready for merge to main
</output>
