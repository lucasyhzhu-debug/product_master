---
phase: 24-ingredient-simulation-id-linking
plan: "03"
type: execute
wave: 3
depends_on: ["24-01", "24-02"]
files_modified:
  - src/components/dispatchPlanner/ChannelSettingsDialog.tsx
  - src/components/dispatchPlanner/MaterialsCheckPanel.tsx
  - src/components/kitchen/ProductionTargetsBar.tsx
  - src/components/kitchen/ManagerTargetSettings.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Capacity tab is gone from ChannelSettingsDialog (2-tab dialog: Channels, Outlets)"
    - "MaterialsCheckPanel shows amber banner when simulateInventory returns unlinkedIngredients.length > 0"
    - "ProductionTargetsBar shows 'from Restock Planner' badge when overrideSource === 'restock_planner'"
    - "ManagerTargetSettings passes source: 'manual' explicitly when calling setDailyOverride"
  artifacts:
    - path: "src/components/dispatchPlanner/ChannelSettingsDialog.tsx"
      provides: "2-tab settings dialog (Channels, Outlets only)"
    - path: "src/components/dispatchPlanner/MaterialsCheckPanel.tsx"
      provides: "Unlinked ingredients amber warning banner"
    - path: "src/components/kitchen/ProductionTargetsBar.tsx"
      provides: "Source badge for restock_planner overrides"
    - path: "src/components/kitchen/ManagerTargetSettings.tsx"
      provides: "Explicit source: 'manual' on setDailyOverride calls"
  key_links:
    - from: "src/components/dispatchPlanner/MaterialsCheckPanel.tsx"
      to: "useDispatchSimulateInventory hook"
      via: "reads simulation.unlinkedIngredients to render amber banner"
      pattern: "unlinkedIngredients"
    - from: "src/components/kitchen/ProductionTargetsBar.tsx"
      to: "KitchenTargets interface"
      via: "reads targets.overrideSource === 'restock_planner' for badge"
      pattern: "overrideSource"
---

<objective>
Frontend cleanup for Phase 24 — three parallel concerns that each touch separate component files:
1. Remove Capacity tab from ChannelSettingsDialog
2. Show unlinked ingredients warning banner in MaterialsCheckPanel
3. Add "from Restock Planner" source badge to ProductionTargetsBar + update ManagerTargetSettings

Purpose: Complete the UI side of the backend changes shipped in Plans 01-02.
Output: Updated ChannelSettingsDialog (2 tabs), MaterialsCheckPanel with warning, ProductionTargetsBar with badge.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-ingredient-simulation-id-linking/24-CONTEXT.md
@.planning/phases/24-ingredient-simulation-id-linking/24-RESEARCH.md
@.planning/phases/24-ingredient-simulation-id-linking/24-01-SUMMARY.md
@.planning/phases/24-ingredient-simulation-id-linking/24-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Capacity tab from ChannelSettingsDialog</name>
  <files>src/components/dispatchPlanner/ChannelSettingsDialog.tsx</files>
  <action>
    In `src/components/dispatchPlanner/ChannelSettingsDialog.tsx`:

    1. Remove the `TabsTrigger` for "Capacity" from the tabs list (the one with value "capacity").
    2. Remove the `TabsContent` block for the Capacity tab (the one containing `DailyCapacityEditor`).
    3. Remove the `DailyCapacityEditor` sub-component function (defined at line ~753 in the file) — it will no longer be used.
    4. Remove the `useDispatchPlannerSettings` and `useDispatchUpdateSettings` hook usages from the component body if they are ONLY used by the Capacity tab. Check: if `settings` or `updateSettings` are referenced only inside the Capacity tab, remove those hook calls too.
    5. Remove unused imports that become dead after removing the Capacity tab: `useDispatchPlannerSettings`, `useDispatchUpdateSettings` (from hooks), `Label`, `Input` if they were only used by `DailyCapacityEditor`.
    6. The JSDoc comment at the top of the file mentions "3. Capacity" — update it to remove that line.

    The dialog should now have 2 tabs: "Channels" and "Outlets". All other tab content stays unchanged.

    Follow the React hooks rule: hooks must be at the top of the component body before any conditional returns. If the hooks were placed correctly before, removing them is fine — just remove the call sites and imports.
  </action>
  <verify>
    `npm run type-check` passes.
    `npm run build` passes.
    Grep `src/components/dispatchPlanner/ChannelSettingsDialog.tsx` for "capacity" (case-insensitive) — should have 0 results for the tab UI (the word may still appear in comments if you leave a note, but the JSX/TabsTrigger should be gone).
    Grep for `DailyCapacityEditor` — should return 0 results.
  </verify>
  <done>ChannelSettingsDialog has 2 tabs (Channels, Outlets). No Capacity tab, no DailyCapacityEditor. Type-check and build pass.</done>
</task>

<task type="auto">
  <name>Task 2: MaterialsCheckPanel unlinked warning + ProductionTargetsBar source badge + ManagerTargetSettings source field</name>
  <files>src/components/dispatchPlanner/MaterialsCheckPanel.tsx, src/components/kitchen/ProductionTargetsBar.tsx, src/components/kitchen/ManagerTargetSettings.tsx</files>
  <action>
    **MaterialsCheckPanel.tsx:**

    The `useDispatchSimulateInventory` hook (in `src/hooks/convex/useDispatchPlanner.ts`) returns the raw simulation result. Read the SUMMARY from plan 02 to confirm the hook already passes through `unlinkedIngredients: string[]` from the backend return.

    In `MaterialsCheckPanel.tsx`, after the simulation result is available and before the day-by-day results table, add an amber warning banner:

    ```tsx
    {simulation?.unlinkedIngredients && simulation.unlinkedIngredients.length > 0 && (
      <div className="flex items-start gap-2 rounded-md border border-amber-300 bg-amber-50 dark:bg-amber-950/20 dark:border-amber-700 p-3 text-sm text-amber-800 dark:text-amber-400">
        <AlertTriangle className="h-4 w-4 mt-0.5 shrink-0" />
        <span>
          {simulation.unlinkedIngredients.length} ingredient{simulation.unlinkedIngredients.length > 1 ? "s" : ""} not linked to inventory tracking — forecasts may be incomplete.{" "}
          <span className="font-medium">
            {simulation.unlinkedIngredients.join(", ")}
          </span>
        </span>
      </div>
    )}
    ```

    Place this banner between the simulate button row and the day results. `AlertTriangle` is already imported in the file.

    Also update the local type `SimulationResult` (or whatever interface captures the backend shape in this file) to include `unlinkedIngredients: string[]`. Check the existing `DayResult` / return type — add `unlinkedIngredients?: string[]` to the top-level response interface.

    **ProductionTargetsBar.tsx:**

    1. Update the `KitchenTargets` interface to add:
       ```typescript
       overrideSource?: "manual" | "restock_planner";
       ```

    2. Remove the JSDoc comment `// No source label shown (per user decision).` — this decision is now superseded.

    3. In the component body, in the section that displays the targets (likely near the top/header area), add a badge when `targets.source === "override" && targets.overrideSource === "restock_planner"`:
       ```tsx
       {targets.source === "override" && targets.overrideSource === "restock_planner" && (
         <Badge variant="outline" className="text-xs border-blue-300 text-blue-700 dark:text-blue-400">
           from Restock Planner
         </Badge>
       )}
       ```
       Import `Badge` from `@/components/ui/badge` if not already imported.

    **ManagerTargetSettings.tsx:**

    Find all calls to the `setDailyOverride` mutation (search for `setDailyOverride` in the file). For each call, add `source: "manual"` explicitly to the args object. This makes the intent clear — manager-set overrides are always "manual". No behavioral change (the mutation defaults to "manual"), but explicit is better.
  </action>
  <verify>
    `npm run type-check` passes.
    `npm run build` passes.
    Grep `src/components/dispatchPlanner/MaterialsCheckPanel.tsx` for `unlinkedIngredients` — should appear.
    Grep `src/components/kitchen/ProductionTargetsBar.tsx` for `overrideSource` — should appear in interface and render.
    Grep `src/components/kitchen/ManagerTargetSettings.tsx` for `source.*manual` — should appear in setDailyOverride call.
  </verify>
  <done>
    MaterialsCheckPanel shows amber banner listing unlinked ingredient names when unlinkedIngredients.length > 0.
    ProductionTargetsBar KitchenTargets interface has overrideSource field; badge renders for restock_planner overrides.
    ManagerTargetSettings explicitly passes source: "manual".
    `npm run type-check` and `npm run build` both pass.
  </done>
</task>

</tasks>

<verification>
- `npm run type-check` passes
- `npm run build` passes (no Vite warnings about missing imports)
- ChannelSettingsDialog has 2 tabs only
- MaterialsCheckPanel renders amber warning when simulation returns unlinked ingredients
- ProductionTargetsBar KitchenTargets interface has `overrideSource?: "manual" | "restock_planner"`
- ManagerTargetSettings passes `source: "manual"` to setDailyOverride
</verification>

<success_criteria>
- No Capacity tab in ChannelSettingsDialog
- Unlinked ingredient warning visible in Materials Check results when ingredients are unlinked
- Kitchen view shows "from Restock Planner" badge when target comes from a restock_planner override
- All type checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/24-ingredient-simulation-id-linking/24-03-SUMMARY.md`
</output>
