---
phase: 24-ingredient-simulation-id-linking
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/dispatchPlanner/mutations.ts
  - convex/dispatchPlanner/queries.ts
  - src/pages/DispatchPlanner.tsx
  - src/components/dispatchPlanner/PlannerCell.tsx
  - src/components/dispatchPlanner/PlannerGrid.tsx
  - src/components/dispatchPlanner/WeekNav.tsx
  - src/components/layout/Header.tsx
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "Editing a Direct Sales 'Planned (Manual)' cell saves successfully without error"
    - "Cells only save on explicit action (Enter key or Save button), not on blur"
    - "Page is labeled 'Planner' in nav and header, not 'Restock' or 'Restock Planner'"
    - "Today is always the second column in the grid (startDate = yesterday)"
    - "Arrow navigation shifts +/-7 days keeping the same day alignment"
    - "Save to Kitchen buttons are aligned at the top of each date column inside the grid"
  artifacts:
    - path: "convex/dispatchPlanner/mutations.ts"
      provides: "savePlanCell handles direct-manual outletId correctly"
      contains: "direct-manual"
    - path: "src/pages/DispatchPlanner.tsx"
      provides: "getYesterday() replaces getCurrentMonday(), page title 'Planner'"
      contains: "Planner"
    - path: "src/components/dispatchPlanner/PlannerCell.tsx"
      provides: "No auto-save on blur"
    - path: "src/components/layout/Header.tsx"
      provides: "Nav label 'Planner'"
      contains: "Planner"
  key_links:
    - from: "src/pages/DispatchPlanner.tsx"
      to: "convex/dispatchPlanner/mutations.ts"
      via: "handleSaveCell passes outletId=undefined for direct-manual"
      pattern: "direct-manual"
    - from: "src/components/dispatchPlanner/WeekNav.tsx"
      to: "src/pages/DispatchPlanner.tsx"
      via: "Back to Today uses getYesterday()"
      pattern: "getYesterday"
---

<objective>
Fix 6 Planner grid UX issues identified in Phase 24 UAT Test 4.

Purpose: The Planner page (formerly Restock Planner) has a critical save bug (direct-manual cells fail Convex validator), unwanted auto-save on blur, wrong page naming, Monday-anchored date grid instead of yesterday-anchored, and misaligned Save to Kitchen buttons.

Output: Working Planner page where cells save correctly, saves are explicit (not on blur), dates are yesterday-anchored, and Save to Kitchen buttons sit atop each date column.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/phase-24-test4-planner-grid.md

@convex/dispatchPlanner/mutations.ts
@convex/dispatchPlanner/queries.ts
@src/pages/DispatchPlanner.tsx
@src/components/dispatchPlanner/PlannerCell.tsx
@src/components/dispatchPlanner/PlannerGrid.tsx
@src/components/dispatchPlanner/WeekNav.tsx
@src/components/layout/Header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix direct-manual save bug + remove blur-save + rename to Planner</name>
  <files>
    convex/dispatchPlanner/mutations.ts
    src/pages/DispatchPlanner.tsx
    src/components/dispatchPlanner/PlannerCell.tsx
    src/components/layout/Header.tsx
  </files>
  <action>
**Critical save bug (Issue 1):**
In `src/pages/DispatchPlanner.tsx` `handleSaveCell`: detect when `outletId === "direct-manual"` and pass `outletId: undefined` to the `savePlanCell` mutation. The "direct-manual" string is a display-only sentinel from `assembleDirectChannel` in queries.ts — it is NOT a valid Convex document ID and must never reach the mutation validator.

In `convex/dispatchPlanner/mutations.ts` `savePlanCell` handler: update the direct channel lookup branch to handle `outletId === undefined` AND `orderId === undefined` for manual direct plans. The lookup should match existing dispatchPlans rows by `channel === "direct"` AND `outletId === undefined` AND `orderId === undefined` AND `menuProductId === args.menuProductId` AND `date === args.date`. If no existing row found, insert a new one. If found, update plannedQty (or delete if qty is 0).

**Remove blur-save (Issue 2):**
In `src/components/dispatchPlanner/PlannerCell.tsx`: remove the `performSave(editValue)` call from `handleBlur` (lines ~86-91). The blur handler should only reset the cell to the last-saved value (revert if dirty). Keep the Enter key handler that calls `performSave`. This means cells save ONLY when the user presses Enter — no save on focus loss.

After removing blur-save, also add visual feedback: when a cell has unsaved changes (isDirty), show a subtle border color change (e.g., `ring-2 ring-amber-400`) so the user knows to press Enter to save.

**Rename to Planner (Issue 3):**
- `src/components/layout/Header.tsx` line ~88: change `label: 'Restock'` to `label: 'Planner'`
- `src/pages/DispatchPlanner.tsx`: change all instances of `"Restock Planner"` to `"Planner"` (useDocumentTitle, PageHeader title in both loading and main render paths)
  </action>
  <verify>
    npm run type-check
    Grep for "Restock Planner" in src/ — should return 0 matches (only "Planner" remains)
    Grep for "direct-manual" in mutations.ts — should NOT appear as a passed value (only in detection logic if needed)
  </verify>
  <done>
    Direct-manual cells save without "Failed to save plan" error; cells do not auto-save on blur; page labeled "Planner" everywhere
  </done>
</task>

<task type="auto">
  <name>Task 2: Yesterday-anchored date grid + Save to Kitchen button placement</name>
  <files>
    src/pages/DispatchPlanner.tsx
    src/components/dispatchPlanner/PlannerGrid.tsx
    src/components/dispatchPlanner/WeekNav.tsx
  </files>
  <action>
**Yesterday-anchored dates (Issue 4 + 5):**
In `src/pages/DispatchPlanner.tsx`: replace `getCurrentMonday()` with a new `getYesterday()` helper. This helper should return yesterday's date (today minus 1 day) in Jakarta timezone (Asia/Jakarta, UTC+7). The 7-day window becomes [yesterday, yesterday+6], making today always the second column.

Update the `useState` initializer for `startDate` to use `getYesterday()`.

In `src/components/dispatchPlanner/WeekNav.tsx`: the "Back to Today" button currently calls `getCurrentMonday()` — change it to call the same `getYesterday()` logic (import from DispatchPlanner or extract to a shared util). The `handlePrev` and `handleNext` already shift by exactly 7 days which will preserve the yesterday-alignment automatically. Update the `isCurrentWeek` check to compare against `getYesterday()` instead of `getCurrentMonday()`.

Export `getYesterday` from `DispatchPlanner.tsx` or move it to a shared location that both `DispatchPlanner.tsx` and `WeekNav.tsx` can import.

**Save to Kitchen button placement (Issue 6):**
In `src/pages/DispatchPlanner.tsx`: remove the Save to Kitchen button row that is currently rendered BELOW `<PlannerGrid>` (lines ~241-251).

In `src/components/dispatchPlanner/PlannerGrid.tsx`: add a new prop `renderColumnAction?: (date: string) => React.ReactNode`. Render a header row inside the grid (above the channel rows, below the date headers) that maps over `dates` and calls `renderColumnAction(date)` for each column. The first cell in this row should be empty (matching the label column width). Each action cell should be centered in the column.

In `src/pages/DispatchPlanner.tsx`: pass `renderColumnAction` to `PlannerGrid` that renders the Save to Kitchen button per date. The button should be compact (small variant, icon + abbreviated text or just icon with tooltip).
  </action>
  <verify>
    npm run type-check
    npm run build
  </verify>
  <done>
    Today is always the second column; arrows navigate +/-7 days keeping alignment; "Back to Today" returns to yesterday-anchored view; Save to Kitchen buttons appear at top of each date column inside the grid, aligned with their respective columns
  </done>
</task>

</tasks>

<verification>
- `npm run type-check` passes
- `npm run build` succeeds
- Grep for "getCurrentMonday" in src/ returns 0 matches (fully replaced by getYesterday)
- Grep for "Restock Planner" in src/ returns 0 matches
</verification>

<success_criteria>
- Direct-manual Planner cells save without error
- Cells save only on Enter key press, not on blur
- Page labeled "Planner" in nav and header
- Today is always the second column
- Save to Kitchen buttons aligned at top of each date column in the grid
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/24-ingredient-simulation-id-linking/24-05-SUMMARY.md`
</output>
