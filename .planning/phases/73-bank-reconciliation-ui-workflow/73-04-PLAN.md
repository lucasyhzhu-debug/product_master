---
phase: 73
plan: 04
type: execute
wave: "2b"
depends_on: ["73-01", "73-02", "73-03"]
files_modified:
  - src/components/bankReconciliation/BatchConfirmDialog.tsx
  - src/components/bankReconciliation/LearnFromOverrideDialog.tsx
  - src/components/bankReconciliation/InlineExpenseDialog.tsx
  - src/components/bankReconciliation/InlineRevenueDialog.tsx
  - src/components/bankReconciliation/InlineReimbursementDialog.tsx
  - src/components/bankReconciliation/SearchAllRecordsDialog.tsx
  - src/components/bankReconciliation/ReconciliationActionBar.tsx
  - src/components/bankReconciliation/SplitViewWorkspace.tsx
  - src/components/bankReconciliation/RuleFormDialog.tsx
  - src/pages/AssetRegister.tsx
  - src/pages/ExpenseSubmit.tsx
  - tests/e2e/bank-reconciliation-inline-expense.spec.ts
  - tests/e2e/bank-reconciliation-batch-confirm.spec.ts
  - tests/e2e/bank-reconciliation-capex-roundtrip.spec.ts
autonomous: false
requirements: [BANK-03]
must_haves:
  truths:
    - "Batch Confirm preview modal shows N + grouped DR/CR + grand totals + balance gate; posts via batchConfirmExactTier"
    - "Learn-from-override dialog opens when user edits overrideCategoryAccountId; saves via createFromOverride"
    - "Inline expense dialog reuses ExpenseSubmit form body; status='submitted', requires receiptFile + submittedBy"
    - "Inline revenue + reimbursement dialogs delegate to existing creation flows; auto-match line"
    - "Search all records dialog widens candidate filter to whole-table substring"
    - "CapEx-flagged line clicks [Route to Asset Register] navigating to /asset-register/new?fromBankLine={lineId} with prefills"
    - "AssetRegister.tsx accepts ?fromBankLine= URL param + on save calls markAssetLinked + navigates back to /bank-reconciliation?tab=review&statementId=…&lineId=…"
  artifacts:
    - path: "src/components/bankReconciliation/BatchConfirmDialog.tsx"
      provides: "Preview modal with DR/CR sanity gate"
    - path: "src/components/bankReconciliation/LearnFromOverrideDialog.tsx"
      provides: "Wraps RuleFormDialog body with override pre-fill"
    - path: "src/components/bankReconciliation/InlineExpenseDialog.tsx"
      provides: "Standard expense submission in dialog (D-17 critical)"
      contains: "Submit for approval"
    - path: "src/pages/AssetRegister.tsx"
      provides: "URL-param prefill + bank line backlink + duplicate detection (D-22)"
      contains: "fromBankLine"
  key_links:
    - from: "src/components/bankReconciliation/BatchConfirmDialog.tsx"
      to: "useBatchConfirmExactTier hook"
      via: "preview → confirm → mutation"
      pattern: "useBatchConfirmExactTier"
    - from: "src/components/bankReconciliation/InlineExpenseDialog.tsx"
      to: "useInlineCreateExpense hook + ExpenseSubmit form body"
      via: "Dialog wraps ExpenseSubmitForm; mutation passes through inlineCreateExpense (D-17)"
      pattern: "Submit for approval"
    - from: "src/pages/AssetRegister.tsx (save handler)"
      to: "useMarkAssetLinked + navigate('/bank-reconciliation?…')"
      via: "URL param fromBankLine triggers post-save link + redirect"
      pattern: "markAssetLinked|fromBankLine"
---

<objective>
Phase 73 dialogs + handoffs: Batch Confirm, Learn-from-Override, Inline Create (expense / revenue / reimbursement), Search All Records, and the CapEx → Asset Register round-trip. Wires the placeholder callbacks left in Plan 03's ReconciliationActionBar / SplitViewWorkspace.

Purpose: Complete the BANK-03 interactive surface — every action a reviewer needs is now reachable. Honors D-17 (standard expense submission, never auto-approved), D-08 (batch preview with balance gate), D-09 (unmatch confirmation copy), D-10/D-11/D-12 (override-rule creation), D-20/D-21/D-22 (CapEx round-trip).
Output: All 6 dialogs functional + AssetRegister round-trip + ExpenseSubmit form-body extraction (if needed per A2 RESEARCH assumption) + 3 Playwright E2E specs covering the inline-expense / batch-confirm / capex-roundtrip flows.
</objective>

<execution_context>
@.planning/phases/73-bank-reconciliation-ui-workflow/73-CONTEXT.md
@.planning/phases/73-bank-reconciliation-ui-workflow/73-RESEARCH.md
@.planning/phases/73-bank-reconciliation-ui-workflow/73-UI-SPEC.md
@.planning/phases/73-bank-reconciliation-ui-workflow/73-VALIDATION.md
@CLAUDE.md
</execution_context>

<context>
@src/components/bankReconciliation/RuleFormDialog.tsx
@src/components/bankReconciliation/SplitViewWorkspace.tsx
@src/components/bankReconciliation/ReconciliationActionBar.tsx
@src/pages/ExpenseSubmit.tsx
@src/pages/AssetRegister.tsx
@src/hooks/convex/useBankReconciliation.ts
@src/components/ui/dialog.tsx
@src/components/ui/alert.tsx
@src/components/ui/table.tsx
@src/components/ui/popover.tsx
@src/lib/dateUtils.ts
@src/lib/utils.ts

<interfaces>
From convex/bankStatements/mutations.ts (Plans 01+02 outputs):
- batchConfirmExactTier({ token, statementId }): { posted, skipped, totalAmountIdr }
- inlineCreateExpense({ token, bankLineId, submittedBy, receiptStorageId, accountId, date, amountIdr, vendorName, description })
- inlineCreateRevenue({ token, bankLineId, transactionDate, revenueGross, source, periodStart?, periodEnd? })
- inlineCreateReimbursement({ token, bankLineId, totalAmount, createdAt, items })
- markAssetLinked({ token, bankLineId, expenseId })

From convex/bankKeywordRules/mutations.ts (Plan 02 output):
- createFromOverride({ token, ruleCode, plSection, direction, matchType, descriptionPatternsMode, counterpartyPatterns, descriptionPatterns, categoryAccountId, jeDebitAccountId, jeCreditAccountId, confidence, priority, subCategory, flags })

From src/pages/ExpenseSubmit.tsx (REUSE per D-17):
- The form body must be extractable into a reusable `<ExpenseSubmitForm>` component for embedding in `<Dialog>` (RESEARCH A2 assumption — verify and refactor if needed).
</interfaces>
</context>

## Git Workflow

**Branch:** `feature/phase-73-bank-reconciliation-ui` (shared)
**Pre-flight:** Plans 01 (Wave 1a) + 02 (Wave 1b) + 03 (Wave 2a) complete. ExpenseSubmit form-body extractability check at Task 2.
**Checkpoints:** 1 human-verify at end (full action coverage smoke test).

## Implementation Waves

### Wave 2b: Frontend dialogs + CapEx round-trip [PARALLEL with Plan 05 (also Wave 2b) — both depend on Plan 03 (Wave 2a)]
Plan 04 and Plan 05 are both Wave 2b. They are parallel to each other (no shared files) but both depend on Plan 03's Wave 2a shell (SplitViewWorkspace dialog-open state hooks, hook facade extensions, ReconciliationActionBar stub callbacks).

| Agent | Task | Files |
|-------|------|-------|
| react-ui-builder | Wave 0 Playwright E2E stubs (RED) | tests/e2e/bank-reconciliation-inline-expense.spec.ts, bank-reconciliation-batch-confirm.spec.ts, bank-reconciliation-capex-roundtrip.spec.ts |
| react-ui-builder | ExpenseSubmit form-body extraction (if needed) + InlineExpenseDialog + Inline revenue/reimbursement dialogs | src/pages/ExpenseSubmit.tsx, src/components/bankReconciliation/InlineExpenseDialog.tsx, InlineRevenueDialog.tsx, InlineReimbursementDialog.tsx |
| react-ui-builder | BatchConfirmDialog + LearnFromOverrideDialog + SearchAllRecordsDialog | src/components/bankReconciliation/BatchConfirmDialog.tsx, LearnFromOverrideDialog.tsx, SearchAllRecordsDialog.tsx, RuleFormDialog.tsx |
| react-ui-builder | AssetRegister round-trip + wire all dialog callbacks in ReconciliationActionBar/SplitViewWorkspace | src/pages/AssetRegister.tsx, src/components/bankReconciliation/ReconciliationActionBar.tsx, SplitViewWorkspace.tsx |

## Documentation Updates
- [ ] `docs/CHANGELOG.md` — entry for "Phase 73 — Reconciliation dialogs + CapEx round-trip"

## Success Criteria
- [ ] `npm run type-check` passes
- [ ] `npm run build` succeeds
- [ ] All 6 dialog files exist and export their named React components
- [ ] `npx playwright test tests/e2e/bank-reconciliation-inline-expense.spec.ts tests/e2e/bank-reconciliation-batch-confirm.spec.ts tests/e2e/bank-reconciliation-capex-roundtrip.spec.ts` exits 0
- [ ] grep `src/components/bankReconciliation/InlineExpenseDialog.tsx` for `Submit for approval` returns ≥1 match
- [ ] grep `src/components/bankReconciliation/InlineExpenseDialog.tsx` for `Approve now\|approveNow` returns 0 matches (D-17 critical — never auto-approve)
- [ ] grep `src/pages/AssetRegister.tsx` for `fromBankLine` returns ≥1 match
- [ ] grep `src/pages/AssetRegister.tsx` for `markAssetLinked` returns ≥1 match
- [ ] Manual: Batch Confirm preview shows DR/CR balance + posts; Inline expense dialog requires receipt + submitter; CapEx button routes to Asset Register and round-trips back

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 Playwright E2E stubs (RED) — inline-expense, batch-confirm, capex-roundtrip</name>
  <files>tests/e2e/bank-reconciliation-inline-expense.spec.ts, tests/e2e/bank-reconciliation-batch-confirm.spec.ts, tests/e2e/bank-reconciliation-capex-roundtrip.spec.ts</files>
  <read_first>
    - playwright.config.ts (auth helpers, baseURL, projects)
    - tests/e2e/ (existing E2E specs — find role-login pattern, e.g. tests/e2e/expense-approval.spec.ts)
    - tests/helpers/ (fixture builders e.g. seedReconcileFixture if present)
    - .planning/phases/73-bank-reconciliation-ui-workflow/73-CONTEXT.md (D-08, D-17, D-20, D-21, D-22)
    - .planning/phases/73-bank-reconciliation-ui-workflow/73-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <behavior>
    **bank-reconciliation-inline-expense.spec.ts (D-17 invariant):**
    - Test 1 (`inline expense dialog blocks Approve now`): the dialog MUST NOT render any "Approve now" button; only `Submit for approval` is present.
    - Test 2 (`requires receipt upload`): submitting without receipt surfaces a validation error; expense is NOT created.
    - Test 3 (`created expense has status submitted, not approved`): after submission, the expense row persists with `status === "submitted"` (D-17 critical). Verify via UI or Convex query — test reads the Expense Approval queue and finds the new row with submitted status.
    - Test 4 (`bank line is patched to suggested, not confirmed`): after inline-create, bank line status becomes `"suggested"` (not `"confirmed"`) — must wait for approval + explicit Confirm.

    **bank-reconciliation-batch-confirm.spec.ts (D-08 balance gate):**
    - Test 1 (`preview shows N + grouped DR/CR + grand totals`): the preview modal renders account-pair groups with line counts and totals.
    - Test 2 (`balance gate blocks post when DR ≠ CR`): seed a scenario where sum(DR) ≠ sum(CR); the `[Post N journal entries]` button is disabled and the `<Alert>` copy "Ledger imbalance detected" is visible.
    - Test 3 (`posting exits the dialog and updates confirmed count`): click Post with a balanced preview; toast appears; progress header confirmed count increments.
    - Test 4 (`skipped count surfaced in preview`): lines missing jeDebit/jeCreditAccountId appear in a "skipped" tally.

    **bank-reconciliation-capex-roundtrip.spec.ts (D-21 / D-22):**
    - Test 1 (`capex-flagged line shows Route to Asset Register button`): a bank line with `flags.includes("capex_needs_asset_register")` shows the CapEx button instead of `[Confirm]`.
    - Test 2 (`clicking routes to AssetRegister with prefills`): navigation lands on `/asset-register/new?fromBankLine={id}` with vendor/cost/date prefilled.
    - Test 3 (`duplicate detection prompt appears (D-22)`): saving an asset whose vendor + cost + purchaseDate (±3d) matches an existing asset surfaces the duplicate prompt with `[Link to existing]` / `[Create new anyway]` buttons.
    - Test 4 (`save routes back with markAssetLinked applied`): choosing `[Create new anyway]` saves the asset, calls `markAssetLinked`, and navigates back to `/bank-reconciliation?tab=review&statementId=…&lineId=…` with the line now auto-matched.
  </behavior>
  <action>
    Create the 3 Playwright spec files following the existing `tests/e2e/` conventions. Use the `loginAs(page, 'manager')` helper (verify exact helper path in tests/helpers).

    For fixtures:
    - inline-expense: seed a bank statement with at least one unmatched debit line + active accounts for the dialog account picker.
    - batch-confirm: seed a statement with at least 2 exact-tier auto-matched lines AND one deliberately-unbalanced pair (mock a jeDebitAccountId/jeCreditAccountId combination where account postings don't balance) to exercise the gate.
    - capex-roundtrip: seed a bank line with `flags: ["capex_needs_asset_register"]` (B01 rule) AND a pre-existing asset matching vendor+cost+date for the duplicate detection test.

    All tests MUST be RED at this step — behavior they assert is implemented in Tasks 2/3/4.

    Plan 06 Task 1 originally duplicated some of these specs. Plan 06 will run these 3 specs in its full suite (no duplicate creation — Plan 06's `bank-reconciliation-capex.spec.ts` is consolidated into this plan's `bank-reconciliation-capex-roundtrip.spec.ts`).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/bank-reconciliation-inline-expense.spec.ts tests/e2e/bank-reconciliation-batch-confirm.spec.ts tests/e2e/bank-reconciliation-capex-roundtrip.spec.ts 2>&1 | tee /tmp/p73-04-wave0.log; echo "Wave 0 E2E stubs created (RED expected)"</automated>
  </verify>
  <acceptance_criteria>
    - 3 E2E spec files exist at the paths in `<files>`
    - grep `tests/e2e/bank-reconciliation-inline-expense.spec.ts` for `Approve now` returns 0 matches (D-17 — never renders)
    - grep `tests/e2e/bank-reconciliation-inline-expense.spec.ts` for `status.*submitted\|Submit for approval` returns ≥1 match
    - grep `tests/e2e/bank-reconciliation-batch-confirm.spec.ts` for `Ledger imbalance\|balance gate` returns ≥1 match
    - grep `tests/e2e/bank-reconciliation-capex-roundtrip.spec.ts` for `fromBankLine` returns ≥1 match
    - grep `tests/e2e/bank-reconciliation-capex-roundtrip.spec.ts` for `Link to existing\|Create new anyway\|duplicate` returns ≥1 match
    - Tests are RED (Playwright exits non-zero) — expected until Tasks 2/3/4 implement the behaviors
  </acceptance_criteria>
  <done>3 Playwright stub specs created; tests RED; E2E contract for D-08, D-17, D-21, D-22 established before dialog implementation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ExpenseSubmit form-body extraction (if needed) + 3 inline-create dialogs (turns inline-expense E2E partially GREEN)</name>
  <files>src/pages/ExpenseSubmit.tsx, src/components/bankReconciliation/InlineExpenseDialog.tsx, src/components/bankReconciliation/InlineRevenueDialog.tsx, src/components/bankReconciliation/InlineReimbursementDialog.tsx</files>
  <read_first>
    - src/pages/ExpenseSubmit.tsx (full — assess RESEARCH A2 assumption: is the form body extractable into a reusable component without major refactor?)
    - convex/expenses/mutations.ts (existing submitExpense flow + required fields)
    - src/components/ui/dialog.tsx, src/components/ui/alert.tsx, src/components/ui/input.tsx, src/components/ui/select.tsx
    - src/hooks/convex/useBankReconciliation.ts (Plan 03 — useInlineCreateExpense, useInlineCreateRevenue, useInlineCreateReimbursement)
    - .planning/phases/73-bank-reconciliation-ui-workflow/73-UI-SPEC.md (§6.7 — inline-create dialogs, Inline Expense critical D-17 banner copy)
    - .planning/phases/73-bank-reconciliation-ui-workflow/73-CONTEXT.md (D-16, D-17, D-18, D-19)
    - .planning/phases/73-bank-reconciliation-ui-workflow/73-RESEARCH.md (Pitfall 4 — file input in nested Dialog focus-trap)
  </read_first>
  <action>
    **A) Inspect `src/pages/ExpenseSubmit.tsx`.** Determine if its form body (the JSX block from "submittedBy picker" through "Submit button") is already a separate component or could be cleanly extracted. Two paths:
    - **Path 1 (form body extractable):** Refactor `ExpenseSubmit.tsx` so the form body becomes `<ExpenseSubmitForm />` exported alongside the page. Page wrapper is a thin layout that hosts the form. The InlineExpenseDialog can then import `<ExpenseSubmitForm>` directly.
    - **Path 2 (form body too coupled):** Build `InlineExpenseDialog.tsx` to replicate the form fields directly (date, amount, vendorName, description, accountId, submittedBy picker, receiptStorageId upload). Use `useInlineCreateExpense` from Plan 02. This duplicates form code but is faster.

    **CHOOSE Path 1 if extraction takes <60 lines of refactor.** Otherwise Path 2. Document the choice in the SUMMARY.

    **B) `InlineExpenseDialog.tsx` (NEW)** per UI-SPEC §6.7:
    - Wraps `<Dialog>` `max-w-3xl`. Title: "Create expense from bank line".
    - Banner `<Alert>` at top per D-17: "This expense uses the standard submission flow. Receipt and owner are required even though money has already left the bank."
    - Form (extracted body OR replicated): all standard expense fields.
    - Pre-filled (highlighted with `bg-[var(--invoice-field-auto)]` + border `var(--invoice-field-auto-border)`): date (from line.date), amount (line.amountIdr), description (line.rawDescription), vendorName (line.parsedCounterparty || empty).
    - Required-but-not-pre-filled (highlighted with `bg-[var(--invoice-field-input)]`): submittedBy (user picker over active users), receiptStorageId (file upload), accountId (account picker; pre-filled if line.suggestedCategoryAccountId present).
    - Footer: `[Cancel]` ghost · `[Submit for approval]` primary. **NEVER `[Approve now]`** (D-17 critical).
    - On submit: call `useInlineCreateExpense({ bankLineId, submittedBy, receiptStorageId, accountId, date, amountIdr, vendorName, description })`. Show toast per UI-SPEC §8: "Expense submitted — approve in Expense Approval to confirm match".
    - File input pattern: follow ExpenseSubmit's existing receipt upload exactly (same `<input type="file">` placement) per RESEARCH Pitfall 4 — do NOT introduce a custom focus-trap-evading pattern.

    **C) `InlineRevenueDialog.tsx` (NEW)** per D-18 + UI-SPEC §6.7:
    - `<Dialog>` `max-w-xl`. Title: "Create revenue record from bank line".
    - Form fields: transactionDate (pre-filled from line.date), revenueGross (pre-filled line.amountIdr), source (pre-filled from line.linkedChannel if set, else picker over known channels), periodStart/periodEnd (default to month containing transactionDate using `getWibMonthStart/End`).
    - Footer: `[Cancel]` ghost · `[Create revenue record]` primary.
    - On submit: call `useInlineCreateRevenue`. Toast: "Revenue record created and linked to line".

    **D) `InlineReimbursementDialog.tsx` (NEW)** per D-19 + UI-SPEC §6.7:
    - `<Dialog>` `max-w-xl`. Title: "Create reimbursement batch from bank line".
    - Form fields: totalAmount (pre-filled line.amountIdr), createdAt (pre-filled line.date). Per CONTEXT: "User adds individual items per existing reimbursement flow". For P73, simplest is to create the batch shell and link, then deep-link to the existing reimbursement-batch-detail page where items are added. Render batch-shell form here + helper text: "Items will be added on the next screen".
    - Footer: `[Cancel]` ghost · `[Create reimbursement batch]` primary.
    - On submit: call `useInlineCreateReimbursement`. Navigate to `/reimbursements/{batchId}` (or whatever existing reimbursement detail route is — verify in App.tsx). Toast: "Reimbursement batch created and linked to line. Add expense items on the next screen."

    **All 3 dialogs** receive `{ open, onOpenChange, line }` props from SplitViewWorkspace (wired in Task 4).
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - 3 dialog files exist and export the named components
    - grep `src/components/bankReconciliation/InlineExpenseDialog.tsx` for `Submit for approval` returns ≥1 match
    - grep `src/components/bankReconciliation/InlineExpenseDialog.tsx` for `Approve now` returns 0 matches
    - grep `src/components/bankReconciliation/InlineExpenseDialog.tsx` for `useInlineCreateExpense` returns ≥1 match
    - grep `src/components/bankReconciliation/InlineExpenseDialog.tsx` for `receiptStorageId\|receipt` returns ≥1 match
    - grep `src/components/bankReconciliation/InlineRevenueDialog.tsx` for `useInlineCreateRevenue` returns ≥1 match
    - grep `src/components/bankReconciliation/InlineReimbursementDialog.tsx` for `useInlineCreateReimbursement` returns ≥1 match
    - If Path 1: grep `src/pages/ExpenseSubmit.tsx` for `export.*ExpenseSubmitForm` returns ≥1 match
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>3 inline-create dialogs ready; D-17 invariant enforced (Submit for approval, never Approve now); ExpenseSubmit form-body decision documented.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: BatchConfirmDialog + LearnFromOverrideDialog + SearchAllRecordsDialog (turns batch-confirm E2E partially GREEN)</name>
  <files>src/components/bankReconciliation/BatchConfirmDialog.tsx, src/components/bankReconciliation/LearnFromOverrideDialog.tsx, src/components/bankReconciliation/SearchAllRecordsDialog.tsx, src/components/bankReconciliation/RuleFormDialog.tsx</files>
  <read_first>
    - src/components/bankReconciliation/RuleFormDialog.tsx (full — current rule CRUD form; extract shared form body for reuse in LearnFromOverrideDialog)
    - src/components/ui/dialog.tsx, src/components/ui/table.tsx, src/components/ui/alert.tsx, src/components/ui/separator.tsx
    - src/hooks/convex/useBankReconciliation.ts (Plan 03 — useBatchConfirmExactTier, useCreateRuleFromOverride, useSearchExpenses/Revenue/Reimbursements/Payroll)
    - .planning/phases/73-bank-reconciliation-ui-workflow/73-UI-SPEC.md (§6.4 batch dialog, §6.5 override dialog, §8 copy)
    - .planning/phases/73-bank-reconciliation-ui-workflow/73-CONTEXT.md (D-06, D-07, D-08, D-10, D-11, D-12)
  </read_first>
  <action>
    **A) `BatchConfirmDialog.tsx` (NEW)** per UI-SPEC §6.4:
    - Props: `{ open, onOpenChange, statementId, exactTierLines: BankStatementLineDoc[], onSuccess: (result) => void }`.
    - `<Dialog>` `max-w-2xl`. Title: "Confirm {N} matched lines".
    - Body:
      1. Summary line: "You are about to post {N} journal entries totaling {formatCurrency(total)} IDR."
      2. `<Table>` grouped by `(jeDebitAccountId, jeCreditAccountId)` pair: columns `Debit Account | Credit Account | Lines | Total`. Group with reduce; render account names by joining against accounts query (use existing useAccountsList hook if present; else fetch ad-hoc).
      3. `<Separator>` + grand total row: `Grand Total DR: X · Grand Total CR: Y`.
      4. **Balance gate:** if computed `totalDR !== totalCR`, render `<Alert variant="destructive">` "Ledger imbalance detected (DR ≠ CR). Posting blocked. Review the selected lines or contact an admin." and DISABLE the [Post] button.
    - Footer: `[Cancel]` ghost · `[Post N journal entries]` primary (label dynamically replaces N).
    - On Post: call `useBatchConfirmExactTier({ statementId })`. On success: toast "{N} journal entries posted"; onSuccess callback closes dialog.
    - Pre-filter logic per RESEARCH Open Q #1: surface `skipped` count in the preview if any included lines were filtered ("3 lines skipped — no JE accounts assigned").

    **B) `LearnFromOverrideDialog.tsx` (NEW)** per UI-SPEC §6.5 + D-10/D-11/D-12:
    - Props: `{ open, onOpenChange, line: BankStatementLineDoc, overrideAccountId: Id<"accounts">, onSaved: () => void }`.
    - `<Dialog>` (NOT Sheet — keyboard focus). Title: "Save as a matching rule?". Subhead: "Future bank lines matching this pattern will be classified automatically."
    - Pre-filled fields (per D-11):
      - `counterpartyPatterns`: `[line.parsedCounterparty]` if present
      - `descriptionPatterns`: extracted keyword substrings from `line.rawDescription` (split by whitespace, filter ≥4 chars, take top 3 by length)
      - `direction`: line.direction
      - `matchType`: default `"description_contains"`
      - `descriptionPatternsMode`: default `"any"`
      - `categoryAccountId`: overrideAccountId
      - `jeDebitAccountId`/`jeCreditAccountId`: derive from override or leave for user to fill
      - `confidence`: default `"strong"`
      - `priority`: default 50
      - `plSection`: derive from override account's section, else default `"OpEx"`
      - `ruleCode`: auto-generate next available (e.g., `O{NN}` for OpEx) by querying existing rules; OR let user enter
      - `flags`: empty
    - All fields editable (per D-11).
    - Reuse `RuleFormDialog.tsx` body — extract its form fields into a shared `<RuleFormFields>` component used by both the original RuleFormDialog (admin CRUD) and LearnFromOverrideDialog (manager+admin learn-from-override). If the existing RuleFormDialog is a tight monolith, refactor in this task.
    - Footer: `[Don't save]` ghost · `[Save rule]` primary.
    - On save: call `useCreateRuleFromOverride(args)`. On success: toast "Rule saved — future lines will auto-classify".

    **C) `SearchAllRecordsDialog.tsx` (NEW)** per D-06 + UI-SPEC:
    - Props: `{ open, onOpenChange, line: BankStatementLineDoc, defaultType: MatchedType, onCandidateSelected: (type, id) => void }`.
    - `<Dialog>` `max-w-3xl`. Title: "Search all records".
    - Tab bar inside dialog (Tabs primitive): one tab per matched type (Expense / Revenue / Reimbursement / Payroll). Default to defaultType.
    - Each tab body: search input (search term) + amount filter (default = line.amountIdr) + date range filter (defaults blank — widens beyond ±3d window). Live results table calling the corresponding `useSearchExpenses` / etc. hook (each tab composes its own useQuery).
    - Click a row → calls onCandidateSelected(type, id) and closes dialog. SplitViewWorkspace then sets that as the selectedCandidate.
    - Per UI-SPEC §11 — does not mutate the default candidate filter.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - 3 dialog files exist
    - grep `src/components/bankReconciliation/BatchConfirmDialog.tsx` for `useBatchConfirmExactTier` returns ≥1 match
    - grep `src/components/bankReconciliation/BatchConfirmDialog.tsx` for `Ledger imbalance` returns ≥1 match (balance gate copy)
    - grep `src/components/bankReconciliation/LearnFromOverrideDialog.tsx` for `useCreateRuleFromOverride` returns ≥1 match
    - grep `src/components/bankReconciliation/LearnFromOverrideDialog.tsx` for `Save rule` returns ≥1 match
    - grep `src/components/bankReconciliation/SearchAllRecordsDialog.tsx` for `searchExpenses\|useSearchExpenses` returns ≥1 match
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>3 dialogs ready; balance gate enforced in Batch Confirm; LearnFromOverride reuses RuleFormDialog body; SearchAllRecords tabs over 4 typed search queries.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Wire dialogs into SplitViewWorkspace + AssetRegister round-trip + LearnFromOverride trigger on overrideCategoryAccountId edit (turns capex-roundtrip E2E GREEN)</name>
  <files>src/components/bankReconciliation/SplitViewWorkspace.tsx, src/components/bankReconciliation/ReconciliationActionBar.tsx, src/pages/AssetRegister.tsx</files>
  <read_first>
    - src/components/bankReconciliation/SplitViewWorkspace.tsx (Plan 03 output — has dialog-open state hooks stubbed + TODO callbacks)
    - src/components/bankReconciliation/ReconciliationActionBar.tsx (Plan 03 — has [Search all records], [Confirm all exact-tier], [Route to Asset Register] callbacks stubbed)
    - src/components/bankReconciliation/StatementReviewTable.tsx (existing P72 — find where overrideCategoryAccountId is edited; identify trigger point for LearnFromOverrideDialog)
    - src/pages/AssetRegister.tsx (full — intake form, asset save handler)
    - src/hooks/convex/useFixedAssets.ts (existing asset creation hook)
    - src/hooks/convex/useBankReconciliation.ts (useMarkAssetLinked from Plan 03)
    - .planning/phases/73-bank-reconciliation-ui-workflow/73-CONTEXT.md (D-10, D-20, D-21, D-22)
    - .planning/phases/73-bank-reconciliation-ui-workflow/73-RESEARCH.md (Pitfall 6 — CapEx returns to wrong tab without intentional redirect)
  </read_first>
  <action>
    **A) Wire dialogs into SplitViewWorkspace.tsx:**
    - Plan 03 already declared the 6 dialog open-state hooks. In this task, render all 6 dialogs at end of SplitViewWorkspace JSX, controlled by those states.
    - Wire ReconciliationActionBar callbacks:
      - `onBatchConfirm` → setBatchConfirmOpen(true)
      - `onSearchAll` → setSearchAllOpen(true)
      - `onRouteToAssetRegister` → `navigate('/asset-register/new?fromBankLine=' + selectedLine._id)` per D-20 (use react-router useNavigate)
      - `onInlineCreateExpense/Revenue/Reimbursement` → toggle corresponding dialog state
    - Wire SearchAllRecordsDialog `onCandidateSelected` → setSelectedCandidate(...) + setSearchAllOpen(false).
    - Wire BatchConfirmDialog `onSuccess` → close dialog; toast already in dialog; Convex reactivity refreshes the UI.

    **B) LearnFromOverride trigger:**
    - Find where reviewer can edit `overrideCategoryAccountId` on a line — likely a select dropdown in the bank line row OR in StatementReviewTable.
    - On override change handler, after the override mutation succeeds (existing P72 mutation), open `<LearnFromOverrideDialog>` with the changed line + selected accountId.
    - If the override-edit affordance doesn't exist yet on the new BankLineRow (Plan 03), add a small inline `<Select>` for `overrideCategoryAccountId` on the row's expanded view (or context menu). Use existing `useUpdateLineOverride` hook from P72 (verify name in useBankReconciliation.ts; if absent, add a thin wrapper around the existing P72 mutation).
    - Per D-10 trigger semantics: dialog appears AFTER successful override save, NOT before.

    **C) AssetRegister round-trip per D-20/D-21/D-22 + RESEARCH Pitfall 6:**
    - `src/pages/AssetRegister.tsx`:
      1. On mount, parse `?fromBankLine={id}` from `useSearchParams`. If present, fetch the bank line via `useGetLine(lineId)` (P72 query — or add ad-hoc), pre-fill the new-asset form with `purchaseDate = line.date`, `cost = line.amountIdr`, `vendor = line.parsedCounterparty`, `description = line.rawDescription`, plus a hidden `sourceBankLineId = line._id` field.
      2. Surface a banner `<Alert>`: "Creating asset from bank line {date} — {amount}". Include "Cancel and return to reconciliation" link back to `/bank-reconciliation?tab=review&statementId={line.statementId}&lineId={line._id}`.
      3. **Duplicate detection per D-22:** Before allowing save, query existing assets by `vendor + cost + ±3 day purchaseDate`. If matches found, show prompt: "An asset matching this vendor, amount, and date already exists. Link to existing asset or create a new one?" Two buttons: `[Link to existing]` (passes the existing asset's expenseId to markAssetLinked) / `[Create new anyway]`. Implement as inline alert above the form with action buttons.
      4. **Save handler:**
         - Standard path: create asset via existing fixedAssets create mutation. The asset register's existing flow creates an associated expense (Phase 60+ pattern); capture that newExpenseId.
         - If `sourceBankLineId` present: call `useMarkAssetLinked({ bankLineId: sourceBankLineId, expenseId: newExpenseId })`.
         - Then `navigate('/bank-reconciliation?tab=review&statementId=' + line.statementId + '&lineId=' + line._id)` per Pitfall 6.
         - Toast: "Asset registered. Click Confirm to post the acquisition JE."

    **D) ReconciliationActionBar.tsx wiring confirmation:**
    - Plan 03 already declared the [Route to Asset Register] button and inline-create buttons. Verify callbacks now resolve via SplitViewWorkspace state wiring in step A above.
  </action>
  <verify>
    <automated>npm run type-check && npm run build && npx playwright test tests/e2e/bank-reconciliation-inline-expense.spec.ts tests/e2e/bank-reconciliation-batch-confirm.spec.ts tests/e2e/bank-reconciliation-capex-roundtrip.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep `src/components/bankReconciliation/SplitViewWorkspace.tsx` for `BatchConfirmDialog` returns ≥1 match
    - grep `src/components/bankReconciliation/SplitViewWorkspace.tsx` for `LearnFromOverrideDialog` returns ≥1 match
    - grep `src/components/bankReconciliation/SplitViewWorkspace.tsx` for `InlineExpenseDialog` returns ≥1 match
    - grep `src/components/bankReconciliation/SplitViewWorkspace.tsx` for `SearchAllRecordsDialog` returns ≥1 match
    - grep `src/components/bankReconciliation/SplitViewWorkspace.tsx` for `/asset-register/new\?fromBankLine` returns ≥1 match
    - grep `src/pages/AssetRegister.tsx` for `fromBankLine` returns ≥1 match
    - grep `src/pages/AssetRegister.tsx` for `markAssetLinked` returns ≥1 match
    - grep `src/pages/AssetRegister.tsx` for `/bank-reconciliation\?tab=review` returns ≥1 match (round-trip return)
    - All 3 Wave 0 Playwright E2E specs pass (GREEN)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 6 dialogs wired into SplitViewWorkspace; CapEx round-trip functional with duplicate detection; all 3 Wave 0 E2E specs GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human verification — full action coverage smoke test</name>
  <what-built>
    All Phase 73 BANK-03 actions now reachable from the split-view:
    - Match / Unmatch / Confirm (Plan 03) — re-verified
    - Batch Confirm preview modal with DR/CR balance gate
    - Learn-from-Override dialog when editing a line's category
    - Inline create: expense (standard submission, receipt required) / revenue / reimbursement
    - Search all records dialog (4 typed tabs)
    - CapEx [Route to Asset Register] → AssetRegister page with prefills + duplicate detection → on save, navigate back to /bank-reconciliation with line auto-matched
  </what-built>
  <how-to-verify>
    1. `npm run dev` + `npx convex dev`. Log in as manager.
    2. Open `/bank-reconciliation` Statements tab. Click a statement with `confidence="exact"` lines → Review tab.
    3. **Batch Confirm:** Click `[Confirm all exact-tier]`. Modal opens. Verify count, DR/CR groups, grand totals, balance check. Click `[Post N journal entries]`. Toast: "{N} journal entries posted". Lines move to confirmed.
    4. **Inline expense:** Pick an unmatched debit line. Click `[+ Create expense]`. Dialog opens with banner. Try to submit without receipt — error. Upload receipt + pick submitter + accountId. Submit. Toast: "Expense submitted". Verify expense appears in Expense Approval queue with status="submitted" (NOT approved).
    5. **Inline revenue:** Pick an unmatched credit line. Click `[+ Create revenue]`. Dialog opens with prefills. Submit. Toast. Verify externalRevenue row exists.
    6. **Inline reimbursement:** Pick an unmatched debit line. Click `[+ Create reimbursement]`. Dialog opens. Submit. Navigates to reimbursement detail page.
    7. **Learn from override:** On a matched-but-wrong-category line, change overrideCategoryAccountId via the inline select. After save, LearnFromOverride dialog appears with pre-fills. Edit ruleCode/keywords. Save. Toast: "Rule saved". Verify rule appears in `/bank-rules` (admin-only — ask admin to verify or use admin login).
    8. **Search all records:** Click `[Search all records]`. Dialog opens. Switch tabs. Type partial vendor name. Results widen beyond ±3d window. Click a row → returns to split-view with that candidate selected.
    9. **CapEx round-trip:** Find a line flagged `capex_needs_asset_register` (B01 rule). `[Route to Asset Register]` button visible instead of [Confirm]. Click it. Asset Register opens with prefills + banner. Try to save with vendor/cost/date matching an existing asset — duplicate prompt appears. Choose `[Create new anyway]`. Save. Navigates back to /bank-reconciliation with line auto-matched and lineId in URL.
    10. **Unmatch a confirmed line:** Click `[Unmatch auto]` on a confirmed line. Confirm dialog appears with copy "Unmatch and reverse this line? The original journal entry stays in the ledger. A reversal journal entry will be created…". Confirm. Line shows ReversedIndicator pill. Verify reversal JE in Convex dashboard.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Plan 06. If issues found, describe them.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dialog→backend mutation | All dialogs call hooks from useBankReconciliation which inject session token; backend re-validates roles |
| AssetRegister round-trip | URL params (fromBankLine) trusted only after backend `useGetLine` resolves (validates lineId exists + user has access) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-21 | Tampering | InlineExpenseDialog auto-approve via dev-tools mutation arg manipulation | mitigate | Hard-coded `status: "submitted"` in inlineCreateExpense backend (Plan 02 acceptance criteria); dialog doesn't accept a status arg. Backend test verifies. |
| T-73-22 | Tampering | BatchConfirm posts unbalanced JEs by manipulating client preview | mitigate | `createJournalEntryWithLines` (Plan 01) re-validates balance server-side. UI gate is convenience only. |
| T-73-23 | Tampering | LearnFromOverride creates rule with malicious ruleCode | mitigate | createFromOverride (Plan 02) regex-validates ruleCode `/^[A-Z]\d{2}$/` and uniqueness-checks. |
| T-73-24 | Information Disclosure | URL param fromBankLine leaks lineId | accept | lineId is opaque Convex Id; backend `markAssetLinked` re-validates user has manager+admin role + line exists. |
| T-73-25 | Tampering | AssetRegister duplicate detection bypassed by client | accept | Reviewer can choose "Create new anyway"; this is a user-facing UX prompt, not a hard security gate. Audit trail (asset.createdBy) preserved. |
| T-73-26 | Elevation | Manager opens admin-only RuleFormDialog body via LearnFromOverride | mitigate | LearnFromOverrideDialog calls createFromOverride (manager+admin gate) NOT the admin-only plain create mutation. The shared form body is rendering-only; mutation routing differs. |
</threat_model>

<verification>
- `npm run type-check` and `npm run build` pass
- Manual smoke test via Task 5 covers all 6 dialog flows + CapEx round-trip
- All 3 Wave 0 Playwright E2E specs GREEN
- All grep acceptance criteria pass
</verification>

<success_criteria>
- All 5 tasks complete including human-verify
- Every BANK-03 action reachable from the UI
- D-17 invariant verified end-to-end (inline expense lands in approval queue, NOT approved)
- D-22 duplicate asset detection prompt visible
- Reversal JE created on unmatch confirms; ReversedIndicator pill renders
</success_criteria>

<output>
After completion, create `.planning/phases/73-bank-reconciliation-ui-workflow/73-04-SUMMARY.md` covering: 6 dialogs created, ExpenseSubmit form-body decision (Path 1 or 2), AssetRegister round-trip diff, 3 E2E specs GREEN, any deviations.
</output>
