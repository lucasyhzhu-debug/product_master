---
phase: 73
plan: 05
type: execute
wave: "2b"
depends_on: ["73-01", "73-02", "73-03"]
files_modified:
  - src/components/bankReconciliation/RevenueGapTab.tsx
  - src/pages/BankReconciliationPage.tsx
  - src/components/bankReconciliation/SplitViewWorkspace.tsx
  - src/components/bankReconciliation/BankLinesPane.tsx
autonomous: true
requirements: [BANK-04]
must_haves:
  truths:
    - "Revenue Gap tab shows a per-period table of channel/bankCr/extRev/diff/diff%"
    - "Period picker defaults to current WIB month with last-12-months dropdown + Custom range option"
    - "(unallocated) row appears for credits with linkedChannel=null, with extRev='—'"
    - "Diff% infinity case (extRev=0, bankCr>0) renders '—' + warning icon + tooltip"
    - "Row click navigates to /bank-reconciliation?tab=review&channelFilter={channel}&period={YYYY-MM} and Review tab applies the filter"
  artifacts:
    - path: "src/components/bankReconciliation/RevenueGapTab.tsx"
      provides: "Revenue Gap dashboard tab"
      contains: "useRevenueGap"
    - path: "src/components/bankReconciliation/BankLinesPane.tsx"
      provides: "Filter chips honor channelFilter + period URL params from drill-down"
      contains: "channelFilter"
  key_links:
    - from: "src/components/bankReconciliation/RevenueGapTab.tsx"
      to: "useRevenueGap hook → revenueGapByPeriod query"
      via: "Convex useQuery with WIB month bounds"
      pattern: "useRevenueGap"
    - from: "Revenue Gap row click"
      to: "Review tab with pre-applied channelFilter + period"
      via: "useNavigate setSearchParams"
      pattern: "channelFilter|tab=review"
---

<objective>
Phase 73 Revenue Gap dashboard tab (D-13/D-14/D-15) + drill-down filter on the Review tab. Smallest of the Wave 2 plans — single new tab + lines pane filter extension.

Purpose: Surface the cross-channel "where is bank money we haven't recorded as revenue" diagnostic that Phase 70 (DA-01/DA-02) made possible. Drill-down keeps the reviewer in the same workspace.
Output: New tab functional; clicking a row filters the Review tab.
</objective>

<execution_context>
@.planning/phases/73-bank-reconciliation-ui-workflow/73-CONTEXT.md
@.planning/phases/73-bank-reconciliation-ui-workflow/73-RESEARCH.md
@.planning/phases/73-bank-reconciliation-ui-workflow/73-UI-SPEC.md
@CLAUDE.md
</execution_context>

<context>
@src/pages/BankReconciliationPage.tsx
@src/components/bankReconciliation/BankReconciliationTabs.tsx
@src/components/bankReconciliation/SplitViewWorkspace.tsx
@src/components/bankReconciliation/BankLinesPane.tsx
@src/hooks/convex/useBankReconciliation.ts
@src/components/ui/popover.tsx
@src/components/ui/select.tsx
@src/components/ui/table.tsx
@src/components/ui/alert.tsx
@src/lib/dateUtils.ts
@src/lib/utils.ts
@src/lib/platformColors.ts

<interfaces>
From convex/bankStatements/queries.ts (Plan 02 output):
```typescript
revenueGapByPeriod({ token, periodStart, periodEnd }): Array<{
  channel: string;       // e.g. "gopay", "tokopedia", "(unallocated)"
  bankCr: number;        // sum of credit lines (IDR integer)
  extRev: number | null; // null for "(unallocated)" row
  diff: number;          // bankCr - (extRev ?? 0)
  diffPct: number | null; // null when extRev=0 OR null
}>
```

From convex/lib/periodRange.ts (use, do not reimplement):
- `getWibMonthStart(year, monthIdx0)`, `getWibMonthEnd(year, monthIdx0)`, `getWibComponents(epochMs)`

From src/lib/platformColors.ts:
- color per channel name for row label tinting
</interfaces>
</context>

## Git Workflow

**Branch:** `feature/phase-73-bank-reconciliation-ui` (shared)
**Pre-flight:** Plans 01 (Wave 1a) + 02 (Wave 1b) + 03 (Wave 2a) complete (uses Plan 02 query and Plan 03 BankLinesPane + hook facade).
**Checkpoints:** None (visual verification rolled into Plan 06's E2E suite).

## Implementation Waves

### Wave 2b: Frontend revenue gap [PARALLEL with Plan 04 (also Wave 2b) — both depend on Plan 03 (Wave 2a)]
Plan 05 and Plan 04 are both Wave 2b and run in parallel (no file overlap between them). Both depend on Plan 03's Wave 2a outputs (hook facade, BankLinesPane shell).

| Agent | Task | Files |
|-------|------|-------|
| react-ui-builder | RevenueGapTab + BankLinesPane drill-down filter extension | All files in `files_modified` |

## Documentation Updates
- [ ] `docs/CHANGELOG.md` — entry for "Phase 73 — Revenue Gap dashboard tab"

## Success Criteria
- [ ] `npm run type-check` passes
- [ ] `npm run build` succeeds
- [ ] grep `src/components/bankReconciliation/RevenueGapTab.tsx` for `useRevenueGap` returns ≥1 match
- [ ] grep `src/components/bankReconciliation/RevenueGapTab.tsx` for `(unallocated)` returns ≥1 match
- [ ] grep `src/components/bankReconciliation/BankLinesPane.tsx` for `channelFilter` returns ≥1 match
- [ ] Manual: open `/bank-reconciliation?tab=revenue-gap`, see table, click a row, lands on Review tab with filter applied

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: RevenueGapTab.tsx with period picker, table, drill-down navigation</name>
  <files>src/components/bankReconciliation/RevenueGapTab.tsx, src/pages/BankReconciliationPage.tsx</files>
  <read_first>
    - src/lib/dateUtils.ts (WIB formatting + month bounds; verify presence of getWibMonthStart-equivalents on frontend OR compute from getWibComponents pattern in convex/lib/periodRange.ts and replicate)
    - src/components/ui/popover.tsx, src/components/ui/select.tsx, src/components/ui/table.tsx, src/components/ui/alert.tsx
    - src/lib/platformColors.ts (channel color tints)
    - src/lib/utils.ts (formatCurrency)
    - .planning/phases/73-bank-reconciliation-ui-workflow/73-UI-SPEC.md (§6.6 Revenue Gap tab full layout + copy)
    - .planning/phases/73-bank-reconciliation-ui-workflow/73-CONTEXT.md (D-13, D-14, D-15)
  </read_first>
  <action>
    **A) `RevenueGapTab.tsx` (NEW)** per UI-SPEC §6.6:
    - State: `const [period, setPeriod] = useState<{ start: number; end: number; label: string }>(currentWibMonth())` where `currentWibMonth()` returns the current WIB month bounds + label "Nov 2025".
    - Period picker: shadcn `<Popover>` containing a `<Select>` (or simple list) with last 12 WIB months + "Custom…" option. On select, compute month bounds via WIB helpers and update state.
    - Custom range: opens secondary popover with two date inputs (start/end). Preserve via URL query if needed (`?period=2025-11` or `?periodStart=…&periodEnd=…`).
    - Call `const rows = useRevenueGap(period.start, period.end);` (hook from Plan 03's hook facade extension).
    - Loading: show `<Skeleton>` rows. Error/empty: copy from UI-SPEC §8.
    - Render `<Table>`:
      ```
      Channel | Bank Credits | External Revenue | Diff | Diff %
      ```
      All numeric columns right-aligned with `tabular-nums font-mono`. Per row:
      - Channel cell: row.channel name + colored dot from `platformColors[row.channel]` (or muted dot for "(unallocated)")
      - Bank Credits: `formatCurrency(row.bankCr)`
      - External Revenue: `row.extRev === null ? "—" : formatCurrency(row.extRev)`
      - Diff: `formatCurrency(row.diff)` with color: positive → `text-status-warning`, negative → `text-status-info`, zero → `text-muted-foreground` (rendered as "—")
      - Diff %: if `row.diffPct === null` AND `row.bankCr > 0` AND `row.extRev === 0` → render `—` + `<AlertTriangle>` + tooltip "No external revenue recorded — bank shows money we haven't captured." (UI-SPEC §6.6). Else `${row.diffPct.toFixed(1)}%` colored same as Diff.
    - "(unallocated)" row: italic label, warning badge "Needs channel mapping".
    - Row click handler (D-15): `navigate('/bank-reconciliation?tab=review&channelFilter=' + row.channel + '&period=' + periodLabelYYYYMM + (statementId ? '&statementId=' + statementId : ''))`. Hover style: `bg-muted/50 cursor-pointer`.

    **B) `BankReconciliationPage.tsx` (EXTEND)** — wire the Revenue Gap tab content (replaced earlier placeholder from Plan 03):
    ```tsx
    <TabsContent value="revenue-gap"><RevenueGapTab /></TabsContent>
    ```
  </action>
  <verify>
    <automated>npm run type-check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/bankReconciliation/RevenueGapTab.tsx` exists
    - grep `src/components/bankReconciliation/RevenueGapTab.tsx` for `useRevenueGap` returns ≥1 match
    - grep `src/components/bankReconciliation/RevenueGapTab.tsx` for `(unallocated)` returns ≥1 match
    - grep `src/components/bankReconciliation/RevenueGapTab.tsx` for `channelFilter=` returns ≥1 match (drill-down nav)
    - grep `src/components/bankReconciliation/RevenueGapTab.tsx` for `AlertTriangle` returns ≥1 match (infinity case icon)
    - grep `src/pages/BankReconciliationPage.tsx` for `RevenueGapTab` returns ≥1 match
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Revenue Gap tab functional with period picker + diff table + infinity edge case + row drill-down navigation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: BankLinesPane drill-down filter — channelFilter + period URL params</name>
  <files>src/components/bankReconciliation/BankLinesPane.tsx, src/components/bankReconciliation/SplitViewWorkspace.tsx</files>
  <read_first>
    - src/components/bankReconciliation/BankLinesPane.tsx (Plan 03 — current filter chips)
    - src/components/bankReconciliation/SplitViewWorkspace.tsx (Plan 03 — currently passes statementId to BankLinesPane)
    - convex/bankStatements/queries.ts (existing listLines — verify it accepts a `linkedChannel?: string` filter; if not, this task ALSO extends listLines OR filters client-side)
    - .planning/phases/73-bank-reconciliation-ui-workflow/73-CONTEXT.md (D-15)
  </read_first>
  <action>
    **A) `BankLinesPane.tsx` extension:**
    - Read URL params via `useSearchParams`: `channelFilter` + `period` (YYYY-MM).
    - When `channelFilter` set: filter the line list to credits with `linkedChannel === channelFilter`. If `period` set: filter to lines whose date falls within that WIB month.
    - Render an active-filter chip at top of the lines pane: `Filtered by: gopay · Nov 2025  [×]` where × clears the URL params (and the filter).
    - **Filter implementation choice:** extend `listLines` query in `convex/bankStatements/queries.ts` to accept `linkedChannel?: v.optional(v.string())` + `dateStart?: v.optional(v.number())` + `dateEnd?: v.optional(v.number())` args (preferred — server-side). OR client-side filter on the existing query result (fallback if backend extension is heavier than expected). Choose server-side if it's <20 LOC; else client-side. If extending the query, ADD this filter to the existing P72 query without changing its current behavior when the new args are absent.
    - Update `useListLines` hook to pass through the new args.

    **B) `SplitViewWorkspace.tsx` extension:**
    - Read same URL params; pass them down to BankLinesPane (or BankLinesPane can read URL itself, simpler).
    - Display a callout banner above the split view if filter is active: "Showing only {channel} lines for {period}. [Clear filter]".
  </action>
  <verify>
    <automated>npm run type-check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep `src/components/bankReconciliation/BankLinesPane.tsx` for `channelFilter` returns ≥1 match
    - grep `src/components/bankReconciliation/BankLinesPane.tsx` for `useSearchParams` returns ≥1 match
    - grep `src/components/bankReconciliation/SplitViewWorkspace.tsx` for `Filtered by\|Clear filter\|channelFilter` returns ≥1 match
    - If listLines was extended: grep `convex/bankStatements/queries.ts` for `linkedChannel: v.optional` returns ≥1 match
    - `npm run build` exits 0
    - Manual: navigate `/bank-reconciliation?tab=review&statementId=…&channelFilter=gopay&period=2025-11` → only gopay credits in Nov 2025 visible; clear filter chip restores full list
  </acceptance_criteria>
  <done>Drill-down from Revenue Gap row → Review tab works end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→revenueGapByPeriod query | Backend (Plan 02) gates manager+admin |
| URL params (channelFilter / period) | Untrusted strings; used only as filter args (no SQL/eval); backend re-validates user role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-27 | Tampering | URL channelFilter set to malicious string | accept | String passed as filter arg only; Convex query validators reject non-string. Worst case: empty result set. |
| T-73-28 | Information Disclosure | Revenue Gap surfaces aggregate revenue across channels | accept | Manager+admin already see this data via Sales Analytics + Income Statement; no new disclosure. |
| T-73-29 | Tampering | Period picker ranges set to year-spanning custom range causing slow query | mitigate | revenueGapByPeriod uses `by_date` and `by_period` indexes; ≤365 days bounded. UI cap: prevent > 12-month range with a validation toast. |
</threat_model>

<verification>
- `npm run type-check` and `npm run build` pass
- Manual: tab loads, drill-down navigation applies filter, infinity case visible
</verification>

<success_criteria>
- Both tasks complete; revenue gap table renders correctly for current month
- Drill-down navigation applies filter on Review tab
- BANK-04 covered for revenue-gap aggregation derived metric (D-14)
</success_criteria>

<output>
After completion, create `.planning/phases/73-bank-reconciliation-ui-workflow/73-05-SUMMARY.md` covering: tab + filter extension, listLines query extension (if any), screenshot/notes.
</output>
