---
phase: 73
plan: 06
type: execute
wave: 3
depends_on: ["73-01", "73-02", "73-03", "73-04", "73-05"]
files_modified:
  - tests/e2e/bank-reconciliation-split-view.spec.ts
  - tests/e2e/bank-rules-learn-from-override.spec.ts
  - tests/e2e/bank-rules-perms.spec.ts
  - docs/CHANGELOG.md
  - docs/SCHEMA.md
  - docs/API_REFERENCE.md
  - CLAUDE.md
autonomous: false
requirements: [BANK-03, BANK-04]
must_haves:
  truths:
    - "All Phase 73 backend tests GREEN (manualMatch, unmatch, confirmLine, batchConfirm, progress, revenueGap, listCandidates, createFromOverride)"
    - "All Phase 73 frontend component tests GREEN (StatementHistoryList, StatementProgressHeader, ReconciliationActionBar from Plan 03)"
    - "All Phase 73 Playwright E2E specs GREEN: inline-expense + batch-confirm + capex-roundtrip (from Plan 04) + split-view + learn-from-override + perms (this plan)"
    - "npm run type-check + npm run build + npm run test pass with no warnings/errors"
    - "code-auditor sweep clean (no untracked files left, no console.log left in production paths)"
    - "All docs updated: CHANGELOG, SCHEMA (D-25/D-26), API_REFERENCE (12 new exports), CLAUDE.md (file finder for new components if route changes)"
  artifacts:
    - path: "tests/e2e/bank-reconciliation-split-view.spec.ts"
      provides: "Click-to-match flow E2E coverage"
    - path: "docs/CHANGELOG.md"
      provides: "Phase 73 entry under v2.0"
      contains: "Phase 73"
    - path: "docs/SCHEMA.md"
      provides: "D-25 audit fields + D-26 sourceType update"
      contains: "bank_statement_reversal"
  key_links:
    - from: "tests/e2e/bank-reconciliation-split-view.spec.ts"
      to: "running app at localhost via Playwright"
      via: "page.goto + role login + actions + expect"
      pattern: "bank-reconciliation"
---

<objective>
Phase 73 verification, E2E coverage completion, and docs. Adds the 3 remaining E2E specs NOT already created by Plan 04 (split-view match flow, learn-from-override rule creation, role perms), then runs the full suite and updates all required documentation per CLAUDE.md "after every merge" rule.

Purpose: Gate the merge of Phase 73 to main. Nothing ships without passing the full suite + docs.
Output: 3 additional E2E specs + updated CHANGELOG/SCHEMA/API_REFERENCE/CLAUDE.md + verification of complete test suite passing.

**Note:** The 3 E2E specs for inline-expense, batch-confirm, and capex-roundtrip were created and turned GREEN in Plan 04 (Wave 2b Task 1). The StatementHistoryList component test was created and turned GREEN in Plan 03 (Wave 2a Task 1). This plan only runs the full suite (including those specs) and adds the 3 remaining specs.
</objective>

<execution_context>
@.planning/phases/73-bank-reconciliation-ui-workflow/73-CONTEXT.md
@.planning/phases/73-bank-reconciliation-ui-workflow/73-RESEARCH.md
@.planning/phases/73-bank-reconciliation-ui-workflow/73-VALIDATION.md
@CLAUDE.md
</execution_context>

<context>
@playwright.config.ts
@tests/e2e/
@vitest.config.ts
@src/components/bankReconciliation/StatementHistoryList.tsx
@docs/CHANGELOG.md
@docs/SCHEMA.md
@docs/API_REFERENCE.md
@CLAUDE.md
</context>

## Git Workflow

**Branch:** `feature/phase-73-bank-reconciliation-ui` (shared)
**Pre-flight:** Plans 01–05 complete. Working tree clean except this plan's outputs.
**Checkpoints:** 1 final human-verify before merge to main.

## Implementation Waves

### Wave 3: Verification [SEQUENTIAL]
| Agent | Task |
|-------|------|
| react-ui-builder | Write 3 additional Playwright E2E specs (split-view, learn-from-override, perms) |
| Bash | Run full suite: type-check + build + unit + E2E (including Plan 03/04 specs) |
| code-auditor | Sweep for untracked files / console.log / leftover TODOs in P73 paths |
| react-ui-builder + convex-backend | Update CHANGELOG / SCHEMA / API_REFERENCE / CLAUDE.md |

## Documentation Updates
- [x] `docs/CHANGELOG.md` — Phase 73 entry under v2.0
- [x] `docs/SCHEMA.md` — D-25 audit fields on bankStatementLines + D-26 bank_statement_reversal sourceType literal
- [x] `docs/API_REFERENCE.md` — 12 new mutations/queries (manualMatch, unmatch, confirmLine, batchConfirmExactTier, inlineCreateExpense, inlineCreateRevenue, inlineCreateReimbursement, markAssetLinked, createFromOverride, getStatementProgress, getStatementProgressBulk, listCandidatesForLine, search* x4, revenueGapByPeriod) + permission widening on 4 P72 queries
- [x] `CLAUDE.md` — bank reconciliation file finder row updated with new component file paths

## Success Criteria
- [ ] `npm run type-check` exits 0
- [ ] `npm run build` exits 0
- [ ] `npm run test -- --run convex/bankStatements convex/bankKeywordRules src/components/bankReconciliation` exits 0
- [ ] `npx playwright test tests/e2e/bank*.spec.ts` exits 0 (includes Plan 04's 3 specs + this plan's 3 specs)
- [ ] All Phase 73 grep guards from Plans 01–05 still pass (no regression)
- [ ] CHANGELOG entry visible at top of `docs/CHANGELOG.md`
- [ ] SCHEMA.md has D-25 + D-26 entries
- [ ] No untracked files in `convex/bankStatements/`, `convex/bankKeywordRules/`, `src/components/bankReconciliation/`, `src/pages/BankReconciliationPage.tsx`, `src/pages/AssetRegister.tsx`, `tests/e2e/bank*`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 3 additional Playwright E2E specs (split-view, learn-from-override, perms)</name>
  <files>tests/e2e/bank-reconciliation-split-view.spec.ts, tests/e2e/bank-rules-learn-from-override.spec.ts, tests/e2e/bank-rules-perms.spec.ts</files>
  <read_first>
    - playwright.config.ts (auth helpers, baseURL, projects)
    - tests/e2e/ (existing E2E specs — find an example like tests/e2e/expense-approval.spec.ts for the role-login pattern)
    - tests/e2e/bank-reconciliation-inline-expense.spec.ts (Plan 04 — fixture pattern to reuse)
    - tests/helpers/ (fixture builders e.g. seedReconcileFixture)
    - src/components/bankReconciliation/StatementHistoryList.tsx (Plan 03 — extended with progress column)
    - .planning/phases/73-bank-reconciliation-ui-workflow/73-VALIDATION.md (Phase Requirements → Test Map for E2E specs)
  </read_first>
  <behavior>
    **bank-reconciliation-split-view.spec.ts (BANK-03):**
    - Test 1: manager logs in, opens /bank-reconciliation, picks a statement, sees split view with progress header
    - Test 2: manager selects a line → candidates pane refreshes; selects a candidate → [Match selected] enabled
    - Test 3: manager clicks [Match selected] → toast appears, line status changes to "suggested"
    - Test 4: manager clicks [Confirm] on a matched line → toast "Journal entry posted"; progress header confirmed count increments
    - Test 5: manager clicks [Unmatch auto] on a confirmed line → confirm dialog appears with reversal copy; confirm; line shows ReversedIndicator pill

    **bank-rules-learn-from-override.spec.ts (D-10/D-11/D-12):**
    - Test 1: manager edits a line's overrideCategoryAccountId → LearnFromOverride dialog appears with pre-filled pattern fields
    - Test 2: manager edits the ruleCode + descriptionPatterns + clicks [Save rule] → toast "Rule saved"
    - Test 3: future bank line matching the new rule's keywords classifies automatically (re-import a small fixture or call the classifier)

    **bank-rules-perms.spec.ts (D-23):**
    - Test 1: kitchen role gets blocked / redirected from /bank-reconciliation
    - Test 2: order_staff role gets blocked
    - Test 3: manager role accesses /bank-reconciliation but gets blocked from /bank-rules
    - Test 4: admin role accesses both

    **StatementHistoryList component test:** already created in Plan 03 Task 1 (`src/components/bankReconciliation/__tests__/StatementHistoryList.test.tsx`). No duplicate creation here — this plan's verification step runs the existing suite.

    **CapEx round-trip test:** already created in Plan 04 Task 1 (`tests/e2e/bank-reconciliation-capex-roundtrip.spec.ts`). No duplicate creation here.
  </behavior>
  <action>
    Build the 3 Playwright spec files following the existing E2E pattern in `tests/e2e/`. Use the helper to log in as different roles (likely a `loginAs(page, 'manager')` helper exists; verify in tests/helpers/).

    For deterministic fixtures, seed a known bank statement + lines via convex-test or a setup script invoked in `beforeAll`. Reuse Plan 04's fixture pattern (`tests/e2e/bank-reconciliation-inline-expense.spec.ts`) — that plan established the `seedBankStatement` / `seedReconcileFixture` helper if one didn't previously exist.

    Do NOT create `tests/e2e/bank-reconciliation-capex.spec.ts` — Plan 04 already created `tests/e2e/bank-reconciliation-capex-roundtrip.spec.ts` covering that flow.

    Do NOT create `src/components/bankReconciliation/__tests__/StatementHistoryList.test.tsx` — Plan 03 already created it.

    Mark all tests un-skipped and ensure they pass when run after Plans 01–05 are complete.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/bank-reconciliation-split-view.spec.ts tests/e2e/bank-rules-learn-from-override.spec.ts tests/e2e/bank-rules-perms.spec.ts && npm run test -- --run src/components/bankReconciliation/__tests__/StatementHistoryList.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 3 E2E spec files exist at the paths in `<files>`
    - grep `tests/e2e/bank-reconciliation-split-view.spec.ts` for `Match selected` returns ≥1 match
    - grep `tests/e2e/bank-rules-learn-from-override.spec.ts` for `Save rule` returns ≥1 match
    - grep `tests/e2e/bank-rules-perms.spec.ts` for `kitchen\|order_staff` returns ≥2 matches
    - `src/components/bankReconciliation/__tests__/StatementHistoryList.test.tsx` exists (from Plan 03) and passes
    - `tests/e2e/bank-reconciliation-capex-roundtrip.spec.ts` exists (from Plan 04) and is NOT re-created here
    - All test commands exit 0
  </acceptance_criteria>
  <done>3 additional Playwright specs created; combined with Plan 03/04 output, all Phase 73 E2E coverage passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Full suite verification + code-auditor sweep</name>
  <files>(no file changes — verification only)</files>
  <read_first>
    - .planning/phases/73-bank-reconciliation-ui-workflow/73-VALIDATION.md (Sampling Rate + Wave 0 Gaps — confirm all GREEN)
  </read_first>
  <action>
    Run the full verification suite in order. Capture output for the SUMMARY:

    1. `npm run type-check` — must exit 0. Capture line count of any warnings.
    2. `npm run lint` — must exit 0 (or with only pre-existing warnings unrelated to P73).
    3. `npm run build` — must exit 0. Capture bundle size delta for the new components (baseline from main if available).
    4. `npm run test -- --run convex/bankStatements convex/bankKeywordRules src/components/bankReconciliation` — must exit 0 (includes StatementHistoryList.test.tsx + StatementProgressHeader.test.tsx + ReconciliationActionBar.test.tsx from Plan 03).
    5. `npx playwright test tests/e2e/bank*.spec.ts` — must exit 0 (all 6 P73 specs: split-view, inline-expense, batch-confirm, capex-roundtrip, learn-from-override, perms).

    Then code-auditor sweep:
    6. `git status --short` — verify no untracked files left in P73 directories. If any remain, either stage them or remove them with explicit reasoning.
    7. `grep -rn "console.log\|console.error\|debugger\|TODO Plan 04\|TODO Plan 05" src/components/bankReconciliation/ convex/bankStatements/ convex/bankKeywordRules/` — must return 0 matches (or only legitimate logging that should ship).
    8. `grep -rn "createReversalEntry" convex/bankStatements/` — must return 0 matches (RESEARCH Pitfall 1).
    9. `grep -rn "status: \"approved\"" convex/bankStatements/mutations.ts` (anywhere inside inlineCreateExpense block) — must return 0 matches (D-17 critical).
    10. `grep -rn "matchedCount" convex/bankStatements/mutations.ts` (in P73-added mutations) — must return 0 matches (anti-pattern).

    If any check fails, fix the issue and re-run from step 1. Do NOT proceed to docs (Task 3) until all 10 checks pass.
  </action>
  <verify>
    <automated>npm run type-check && npm run build && npm run test -- --run convex/bankStatements convex/bankKeywordRules src/components/bankReconciliation && npx playwright test tests/e2e/bank*.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 5 verification commands exit 0
    - All 5 code-auditor greps return expected counts (0 forbidden, ≥1 expected)
    - `git status --short` shows no untracked files in P73 directories
  </acceptance_criteria>
  <done>Full suite green; no leftover TODOs or forbidden patterns; ready for docs update.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Documentation updates (CHANGELOG, SCHEMA, API_REFERENCE, CLAUDE.md)</name>
  <files>docs/CHANGELOG.md, docs/SCHEMA.md, docs/API_REFERENCE.md, CLAUDE.md</files>
  <read_first>
    - docs/CHANGELOG.md (current top entry — match formatting style)
    - docs/SCHEMA.md (find bankStatementLines section — extend with D-25; find journalEntries.sourceType section — extend with D-26)
    - docs/API_REFERENCE.md (find bankStatements section — list new exports)
    - CLAUDE.md (find Quick File Finder table — confirm bank reconciliation row points to right files; update with any new ones)
    - .planning/phases/73-bank-reconciliation-ui-workflow/73-CONTEXT.md (D-25, D-26 — exact field names)
    - .planning/phases/73-bank-reconciliation-ui-workflow/73-01-PLAN.md, 73-02-PLAN.md (canonical mutation/query lists)
  </read_first>
  <action>
    **A) `docs/CHANGELOG.md`** — prepend new entry under v2.0:
    ```markdown
    ## [Unreleased] — Phase 73: Bank Reconciliation UI & Workflow

    ### Added
    - Split-view bank reconciliation workspace at `/bank-reconciliation` (Review tab) with click-to-select line + candidate, Match / Unmatch / Confirm actions
    - Live per-statement reconciliation progress (matched / suggested / unmatched / confirmed counts + percentage)
    - Batch Confirm preview modal with DR/CR balance gate; posts N journal entries all-or-nothing
    - Learn-from-Override dialog when reviewer changes a line's category — saves a `bankKeywordRules` row via `createFromOverride` (manager + admin gated, diverges from admin-only CRUD)
    - Inline record creation from unmatched bank lines: expense (standard submission flow per D-17 — receipt + submitter required, status=submitted, NOT auto-approved), revenue (externalRevenue), reimbursement (batch shell + deep-link to detail page)
    - Search all records dialog with 4 typed tabs (expense/revenue/reimbursement/payroll) for widening beyond the default ±3-day window
    - CapEx round-trip: bank lines flagged `capex_needs_asset_register` show [Route to Asset Register]; AssetRegister intake accepts URL prefills + duplicate detection (D-22) + auto-link back to bank line on save
    - Revenue Gap dashboard tab: per-period table of channel × bank credits × external revenue × diff × diff%; row click drills into Review tab with channel + period filter applied
    - 9 audit fields on `bankStatementLines` (D-25): confirmedAt/By/JournalEntryId, reversedAt/By/JournalEntryId, createdExpenseId/RevenueId/ReimbursementId
    - New journalEntries.sourceType literal `"bank_statement_reversal"` (D-26) for unmatch reversals — bypasses NON_REVERSIBLE_TYPES guard via direct `createJournalEntryWithLines` call
    - Permission widening: `/bank-reconciliation` route + sidebar entry now manager + admin (D-23); `/bank-rules` stays admin-only

    ### Backend mutations (convex/bankStatements/mutations.ts)
    `manualMatch`, `unmatch`, `confirmLine`, `batchConfirmExactTier`, `inlineCreateExpense`, `inlineCreateRevenue`, `inlineCreateReimbursement`, `markAssetLinked`

    ### Backend mutations (convex/bankKeywordRules/mutations.ts)
    `createFromOverride` (manager + admin gated)

    ### Backend queries (convex/bankStatements/queries.ts)
    `getStatementProgress`, `getStatementProgressBulk`, `listCandidatesForLine`, `searchExpenses`, `searchRevenue`, `searchReimbursements`, `searchPayroll`, `revenueGapByPeriod`. Existing P72 queries (`listStatements`, `getStatement`, `findByFileHash`, `listLines`) widened from admin-only to manager + admin.

    ### Tests
    - 8 backend test files (convex-test) covering all new mutations + queries (including progress Tests 7–8 for bulk cap + mixed statuses)
    - 3 frontend component test files (StatementHistoryList, StatementProgressHeader, ReconciliationActionBar)
    - 6 Playwright E2E specs covering split-view match flow, inline-expense submission, batch-confirm balance gate, CapEx round-trip + duplicate detection, learn-from-override, role gating
    ```

    **B) `docs/SCHEMA.md`** — find bankStatementLines section, add subsection:
    ```markdown
    ### Phase 73 audit fields (D-25)
    All optional. Populated when reviewer confirms / unmatches / inline-creates from a bank line.

    | Field | Type | Set when |
    |-------|------|----------|
    | confirmedAt | number | Reviewer clicked Confirm |
    | confirmedBy | Id<"users"> | Reviewer clicked Confirm |
    | confirmedJournalEntryId | Id<"journalEntries"> | Confirm posted JE |
    | reversedAt | number | Reviewer Unmatched a previously-confirmed line |
    | reversedBy | Id<"users"> | Same |
    | reversalJournalEntryId | Id<"journalEntries"> | Unmatch reversal JE |
    | createdExpenseId | Id<"expenses"> | inlineCreateExpense or markAssetLinked called |
    | createdRevenueId | Id<"externalRevenue"> | inlineCreateRevenue called |
    | createdReimbursementId | Id<"reimbursementBatches"> | inlineCreateReimbursement called |
    ```
    Then find journalEntries.sourceType section, add note:
    ```markdown
    ### Phase 73 sourceType addition (D-26)
    `"bank_statement_reversal"` added to the union for unmatch-reversal journal entries. NOT added to `VALID_VOID_PAIRS` in `convex/lib/journalEngine.ts` — reversals are posted as fresh JEs via direct `createJournalEntryWithLines` call (the `"bank_statement"` literal stays in `NON_REVERSIBLE_TYPES` so the generic void path still blocks bank statement reversals from non-reconciliation flows).
    ```

    **C) `docs/API_REFERENCE.md`** — find bankStatements section, append the 12 new exports with signatures (one-line each pointing to the source file). Same for bankKeywordRules (createFromOverride). Note the role widening on 4 P72 queries.

    **D) `CLAUDE.md`** — find the Quick File Finder row for "Bank reconciliation". Verify it lists:
    - `convex/bankStatements/` (mutations.ts, queries.ts, matchEngine.ts)
    - `convex/bankKeywordRules/` (mutations.ts, defaultRules.ts)
    - `convex/lib/journalEngine.ts` (linked from JE work)
    - `src/pages/BankReconciliationPage.tsx`
    - `src/components/bankReconciliation/` (now includes 17 components)
    - `src/hooks/convex/useBankReconciliation.ts`

    Update if the bank reconciliation row is stale or absent.
  </action>
  <verify>
    <automated>grep -c "Phase 73" docs/CHANGELOG.md && grep -c "bank_statement_reversal" docs/SCHEMA.md && grep -c "createFromOverride\|getStatementProgress" docs/API_REFERENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - grep `docs/CHANGELOG.md` for `Phase 73` returns ≥1 match
    - grep `docs/SCHEMA.md` for `bank_statement_reversal` returns ≥1 match
    - grep `docs/SCHEMA.md` for `confirmedJournalEntryId` returns ≥1 match
    - grep `docs/API_REFERENCE.md` for `getStatementProgress` returns ≥1 match
    - grep `docs/API_REFERENCE.md` for `createFromOverride` returns ≥1 match
    - grep `CLAUDE.md` for `bank-reconciliation\|BankReconciliation` returns ≥2 matches
  </acceptance_criteria>
  <done>All 4 docs updated; CHANGELOG entry visible at top.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Final pre-merge verification</name>
  <what-built>
    Phase 73 complete:
    - All 6 plans landed
    - Backend: 9 schema fields + 1 sourceType literal + 5 mutations + 1 rule mutation + 8 queries + 4 widened queries
    - Frontend: 17 new components + tab shell + extended StatementHistoryList + AssetRegister round-trip
    - Tests: 8 backend specs + 3 component tests + 6 E2E specs, all GREEN
    - Docs: CHANGELOG / SCHEMA / API_REFERENCE / CLAUDE.md updated
  </what-built>
  <how-to-verify>
    1. Run `npm run type-check && npm run build && npm run test && npx playwright test tests/e2e/bank*.spec.ts` one final time. All exit 0.
    2. `git status` — only the expected P73 files are modified or new.
    3. `git log --oneline` — review commit history; no surprises.
    4. Visit `/bank-reconciliation` as manager. Re-run the smoke checks from Plan 03 Task 5 + Plan 04 Task 5 in one pass.
    5. Visit `/bank-reconciliation?tab=revenue-gap` as manager. Verify table renders + drill-down works (Plan 05).
    6. Verify `docs/CHANGELOG.md` top entry matches the work delivered.
    7. If everything looks good, type "ship it" — Claude will then run the merge process: `git switch main && git pull && git merge feature/phase-73-bank-reconciliation-ui --no-ff` (or PR-based if the project uses PRs).

    If anything is off, type details. Claude will fix and re-run.
  </how-to-verify>
  <resume-signal>"ship it" to merge OR describe issues for fix-loop.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test→running app | Tests log in via real session token flow; backend re-validates roles |
| docs/* | No security boundary; documentation only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-30 | Information Disclosure | E2E specs commit fixture data with PII (account numbers) | mitigate | Use synthetic fixture builder (P72 pattern); never seed production-like account numbers. |
| T-73-31 | Tampering | Skip CHANGELOG update | mitigate | CLAUDE.md mandates "CHANGELOG ALWAYS required after merging"; this plan's Task 3 grep guard catches absence before merge. |
</threat_model>

<verification>
- All 4 tasks complete including final human-verify checkpoint
- Full suite green
- Docs updated
- No leftover TODOs / console.log / forbidden patterns
</verification>

<success_criteria>
- Phase 73 ready to merge to main
- BANK-03 + BANK-04 fully delivered with backend + UI + tests + docs coverage
- Audit trail clean (commit history reflects per-plan progression; tests document contracts; docs match implementation)
</success_criteria>

<output>
After completion, create `.planning/phases/73-bank-reconciliation-ui-workflow/73-06-SUMMARY.md` covering: full test pass output (paste tail of vitest + playwright), bundle size delta, docs touched, merge outcome.
</output>
