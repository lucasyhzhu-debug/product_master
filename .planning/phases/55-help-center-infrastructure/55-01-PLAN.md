---
phase: 55-help-center-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/helpGuides.ts
  - src/lib/__tests__/helpGuides.test.ts
  - src/components/help/RoleTag.tsx
  - src/components/help/CalloutBox.tsx
  - src/components/help/StepCard.tsx
  - src/components/help/GuideSection.tsx
  - src/components/help/FaqAccordion.tsx
  - src/components/help/index.ts
autonomous: true
requirements:
  - HELP-04
  - HCMP-02
  - HCMP-03
  - HCMP-04
  - HCMP-05
  - HCMP-06

must_haves:
  truths:
    - "Guide registry defines 6 guides (all coming-soon until Phase 56 wires ExpenseGuide component) with GuideConfig interface"
    - "RoleTag renders colored badges for 'all', 'manager', 'admin' roles using CSS variable tokens for dark mode"
    - "CalloutBox renders tip (green), warning (amber), important (orange) callouts using CSS variable tokens for dark mode"
    - "StepCard renders numbered steps with optional tip/warning and vertical dotted connector"
    - "GuideSection wraps content with anchor ID and scroll-margin-top for deep linking"
    - "FaqAccordion renders grouped collapsible Q&A using shadcn Accordion"
    - "searchGuides() has unit tests covering guide title, section title, and FAQ question matching"
  artifacts:
    - path: "src/lib/helpGuides.ts"
      provides: "GuideConfig and GuideSection interfaces, HELP_GUIDES registry array"
      exports: ["GuideConfig", "GuideSection", "HELP_GUIDES", "searchGuides", "SearchResult", "POPULAR_QUESTIONS"]
    - path: "src/lib/__tests__/helpGuides.test.ts"
      provides: "Unit tests for searchGuides() pure function"
    - path: "src/components/help/RoleTag.tsx"
      provides: "Role badge component"
      exports: ["RoleTag"]
    - path: "src/components/help/CalloutBox.tsx"
      provides: "Styled callout box component"
      exports: ["CalloutBox"]
    - path: "src/components/help/StepCard.tsx"
      provides: "Numbered step card component"
      exports: ["StepCard"]
    - path: "src/components/help/GuideSection.tsx"
      provides: "Section wrapper with anchor ID"
      exports: ["GuideSection"]
    - path: "src/components/help/FaqAccordion.tsx"
      provides: "Grouped FAQ accordion component"
      exports: ["FaqAccordion"]
    - path: "src/components/help/index.ts"
      provides: "Barrel export for all help components"
  key_links:
    - from: "src/lib/helpGuides.ts"
      to: "src/pages/guides/ExpenseGuide.tsx"
      via: "component field in GuideConfig (set in Phase 56 when guide is created)"
      pattern: "component.*ExpenseGuide"
    - from: "src/components/help/FaqAccordion.tsx"
      to: "src/components/ui/accordion.tsx"
      via: "shadcn Accordion import"
      pattern: "from.*@/components/ui/accordion"
---

<objective>
Create the Help Center data registry and 5 reusable help components that form the foundation for all guide pages, plus unit tests for the searchGuides pure function.

Purpose: Establish the GuideConfig interface and HELP_GUIDES registry that drives the landing page, router, and search. Build the 5 simpler reusable components (RoleTag, CalloutBox, StepCard, GuideSection, FaqAccordion) plus barrel export. WorkflowDiagram and GuideLayout are in plan 02 due to their complexity.

Output: `src/lib/helpGuides.ts` (registry), 5 component files in `src/components/help/`, barrel export, unit test file
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-03-16-help-center-design.md
@.planning/phases/55-help-center-infrastructure/55-CONTEXT.md

<interfaces>
<!-- Key types the executor needs from existing codebase -->

From src/components/ui/accordion.tsx (shadcn):
Uses Radix Accordion: Accordion, AccordionItem, AccordionTrigger, AccordionContent

From lucide-react (already in project):
CreditCard, ChefHat, Package, Wallet, UtensilsCrossed, BarChart3, Lightbulb, AlertTriangle, Info

Dark mode CSS variable tokens available in src/index.css:
- Status success: `--color-status-success` (text), `--color-status-success-bg` (background)
- Status warning: `--color-status-warning` (text), `--color-status-warning-bg` (background)
- Status error: `--color-status-error` (text), `--color-status-error-bg` (background)
- Status info: `--color-status-info` (text), `--color-status-info-bg` (background)
- Role tokens: `--color-role-admin` / `--color-role-admin-bg`, `--color-role-manager` / `--color-role-manager-bg`
- Semantic: `--color-muted`, `--color-muted-foreground`, `--color-secondary`, `--color-foreground`

Per CODE_STYLE.md: Do NOT use raw Tailwind dark: classes for semantic backgrounds.
Use CSS variable tokens that auto-switch in .dark {} block.
</interfaces>
</context>

## Git Workflow
**Branch:** `feature/help-center-infrastructure`
**Checkpoints:** None (autonomous)

## Implementation Waves
### Wave 1: Foundation [PARALLEL]
| Agent | Task | Files |
|-------|------|-------|
| react-ui-builder | Registry + 5 components + tests | helpGuides.ts, 5 component files, barrel, test file |

### Wave 2: Verification [SEQUENTIAL]
| Agent | Task |
|-------|------|
| code-auditor | Type check + pattern compliance |
| Bash | npm run build |
| Bash | npm run test |

## Documentation Updates
- [ ] CHANGELOG.md (after full phase merge)

## Success Criteria
- [ ] `npm run type-check` passes
- [ ] `npm run build` succeeds
- [ ] `npm run test` passes (including new searchGuides tests)
- [ ] All 5 components export correctly from barrel
- [ ] No `dark:` Tailwind classes in CalloutBox or RoleTag (uses CSS variable tokens)

<tasks>

<task type="auto">
  <name>Task 1: Guide registry and type definitions</name>
  <files>src/lib/helpGuides.ts</files>
  <action>
Create `src/lib/helpGuides.ts` with:

1. **GuideSection interface:**
```typescript
export interface GuideSection {
  id: string;
  title: string;
  role?: "all" | "manager" | "admin";
}
```

2. **GuideConfig interface:**
```typescript
export interface GuideConfig {
  id: string;
  title: string;
  description: string;
  icon: ComponentType<{ className?: string }>;
  accentColor: string;       // Tailwind color name (e.g., "orange")
  sections: GuideSection[];
  readTimeMinutes: number;
  status: "live" | "coming-soon";
  isNew?: boolean;
  component?: ComponentType;  // eagerly imported, undefined for coming-soon
}
```

3. **HELP_GUIDES array** with 6 entries:
- `expenses` (status: "coming-soon", isNew: true, accentColor: "orange", icon: CreditCard, 8 sections matching spec exactly). Add comment: `// status changed to "live" in Phase 56 when ExpenseGuide component is wired`.
- `kitchen` (status: "coming-soon", accentColor: "green", icon: ChefHat)
- `orders` (status: "coming-soon", accentColor: "blue", icon: Package)
- `inventory` (status: "coming-soon", accentColor: "purple", icon: Warehouse)
- `recipes` (status: "coming-soon", accentColor: "rose", icon: UtensilsCrossed)
- `analytics` (status: "coming-soon", accentColor: "amber", icon: BarChart3)

Coming-soon entries (including expenses for now): empty sections array (except expenses which has its 8 sections pre-populated for search), readTimeMinutes: 0 for non-expenses, no component.

Import icons from `lucide-react`. Import `ComponentType` from `react`.

4. **SearchResult type and helper function** for search (used by HelpCenter page):
```typescript
export interface SearchResult {
  guideId: string;
  guideTitle: string;
  type: "guide" | "section" | "faq";
  label: string;
  anchor?: string;
}

export function searchGuides(query: string): SearchResult[] { ... }
```
This searches guide titles, section titles, and FAQ question text (from `POPULAR_QUESTIONS`). Case-insensitive `String.includes`. Returns empty array for empty/whitespace query.

5. **POPULAR_QUESTIONS array** (for landing page):
```typescript
export const POPULAR_QUESTIONS: { question: string; guideId: string; anchor: string }[] = [
  { question: "How do I submit an expense?", guideId: "expenses", anchor: "submitting" },
  { question: "Who can approve expenses?", guideId: "expenses", anchor: "approving" },
  { question: "How does payroll work?", guideId: "expenses", anchor: "payroll" },
  { question: "Understanding the P&L", guideId: "expenses", anchor: "pnl" },
];
```
  </action>
  <verify>
    <automated>cd "D:/Claude/Product Manager/product_master" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>helpGuides.ts exports GuideConfig, GuideSection, SearchResult, HELP_GUIDES (6 entries, all coming-soon), POPULAR_QUESTIONS, and searchGuides function. TypeScript compiles without errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for searchGuides pure function</name>
  <files>src/lib/__tests__/helpGuides.test.ts</files>
  <behavior>
    - Test 1: Empty query returns empty array
    - Test 2: Whitespace-only query returns empty array
    - Test 3: Query matching a guide title returns a "guide" result
    - Test 4: Query matching a section title returns a "section" result with anchor
    - Test 5: Query matching a FAQ question returns a "faq" result with guideId and anchor
    - Test 6: Search is case-insensitive (uppercase query matches lowercase title)
    - Test 7: Query matching nothing returns empty array
    - Test 8: Query matching multiple items returns all matches
  </behavior>
  <action>
Create `src/lib/__tests__/helpGuides.test.ts` using Vitest (matching existing test pattern in the project).

```typescript
import { describe, it, expect } from 'vitest';
import { searchGuides, HELP_GUIDES, POPULAR_QUESTIONS } from '../helpGuides';
```

Write tests for the 8 behaviors listed above. Use the actual HELP_GUIDES data (expenses has sections, other guides have titles). Verify SearchResult shape: `{ guideId, guideTitle, type, label, anchor? }`.
  </action>
  <verify>
    <automated>cd "D:/Claude/Product Manager/product_master" && npx vitest run src/lib/__tests__/helpGuides.test.ts 2>&1</automated>
  </verify>
  <done>All 8 test cases pass. searchGuides correctly filters guide titles, section titles, and FAQ questions case-insensitively.</done>
</task>

<task type="auto">
  <name>Task 3: Five reusable help components + barrel export</name>
  <files>src/components/help/RoleTag.tsx, src/components/help/CalloutBox.tsx, src/components/help/StepCard.tsx, src/components/help/GuideSection.tsx, src/components/help/FaqAccordion.tsx, src/components/help/index.ts</files>
  <action>
Create `src/components/help/` directory with 5 components and a barrel export.

**RoleTag.tsx:**
- Props: `{ role: "all" | "manager" | "admin" }`
- Renders a small inline Badge-like span
- Colors use CSS variable tokens from index.css (NO `dark:` Tailwind classes):
  - "all": `bg-[var(--color-muted)] text-[var(--color-muted-foreground)]` ("All Staff")
  - "manager": `bg-[var(--color-role-manager-bg)] text-[var(--color-role-manager)]` ("Manager+")
  - "admin": `bg-[var(--color-role-admin-bg)] text-[var(--color-role-admin)]` ("Admin Only")
- Style: `rounded-full px-2 py-0.5 text-xs font-medium inline-flex items-center`

**CalloutBox.tsx:**
- Props: `{ type: "tip" | "warning" | "important"; children: React.ReactNode }`
- Renders a box with left border (4px), icon on top-left, children content
- Icons from lucide-react: tip = Lightbulb, warning = AlertTriangle, important = Info
- Colors use CSS variable tokens (NO `dark:` Tailwind classes):
  - tip: `border-l-[var(--color-status-success)] bg-[var(--color-status-success-bg)] text-[var(--color-status-success)]` (icon color)
  - warning: `border-l-[var(--color-status-warning)] bg-[var(--color-status-warning-bg)] text-[var(--color-status-warning)]` (icon color)
  - important: `border-l-[var(--color-status-error)] bg-[var(--color-status-error-bg)] text-[var(--color-status-error)]` (icon color)
- Use `border-l-4` with inline style `borderLeftColor: 'var(--color-status-success)'` (or the appropriate token) since Tailwind arbitrary border-color in `border-l-[var(...)]` can be tricky. Background via `style={{ backgroundColor: 'var(--color-status-success-bg)' }}`. Icon color via `style={{ color: 'var(--color-status-success)' }}`.
- Text content (children) should use default foreground color, not the status color. Only the icon and left border use the status color.
- Padding: p-4, gap between icon and content

**StepCard.tsx:**
- Props: `{ step: number; title: string; description: string; icon?: ComponentType<{ className?: string }>; tip?: string; warning?: string; isLast?: boolean }`
- Layout: numbered circle (w-8 h-8, bg-primary, text-primary-foreground, rounded-full, flex center) on left, title (font-semibold) + description (text-muted-foreground) on right
- If `tip` provided: render a CalloutBox type="tip" below the description
- If `warning` provided: render a CalloutBox type="warning" below the description
- If `isLast` is NOT true: render a vertical dotted line (border-l-2 border-dashed border-muted-foreground/30) connecting to the next step. The line extends from below the number circle. Use relative positioning on the container and absolute on the line.
- Import CalloutBox from same directory

**GuideSection.tsx:**
- Props: `{ id: string; title: string; role?: "all" | "manager" | "admin"; children: React.ReactNode }`
- Renders a `<section>` with `id={id}` and `style={{ scrollMarginTop: '80px' }}`
- Title as h2 (text-xl font-bold) with RoleTag inline if role is provided
- Children below the title with mt-4 spacing
- Import RoleTag from same directory

**FaqAccordion.tsx:**
- Types: `FaqItem { question: string; answer: string | React.ReactNode }`, `FaqGroup { title: string; items: FaqItem[] }`
- Props: `{ groups: FaqGroup[] }`
- Export FaqItem and FaqGroup types
- Renders each group with a title (text-lg font-semibold, mb-3) followed by a shadcn Accordion (type="multiple")
- Each FaqItem is an AccordionItem with AccordionTrigger (question) and AccordionContent (answer)
- Use unique value keys: `${groupIndex}-${itemIndex}`
- Import from `@/components/ui/accordion`

**index.ts (barrel):**
```typescript
export { RoleTag } from './RoleTag';
export { CalloutBox } from './CalloutBox';
export { StepCard } from './StepCard';
export { GuideSection } from './GuideSection';
export { FaqAccordion } from './FaqAccordion';
export type { FaqItem, FaqGroup } from './FaqAccordion';
// WorkflowDiagram and GuideLayout added in plan 02
```
  </action>
  <verify>
    <automated>cd "D:/Claude/Product Manager/product_master" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>All 5 components render correctly with proper TypeScript types. CalloutBox and RoleTag use CSS variable tokens (no dark: Tailwind classes). Barrel exports all components and FAQ types. No type errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run build` succeeds
- `npm run test` passes (searchGuides tests green)
- All exports from `src/components/help/index.ts` resolve
- `src/lib/helpGuides.ts` exports `HELP_GUIDES` with 6 entries (all coming-soon, expenses has isNew: true)
- No `dark:` Tailwind classes in CalloutBox.tsx or RoleTag.tsx
</verification>

<success_criteria>
- GuideConfig interface matches the design spec exactly
- HELP_GUIDES has 6 entries with correct accent colors, icons, and statuses (expenses is coming-soon with isNew until Phase 56)
- searchGuides function performs case-insensitive search across guide titles, section titles, and FAQ questions
- searchGuides has 8 passing unit tests
- All 5 components compile and export cleanly
- StepCard uses CalloutBox internally (not duplicating callout rendering)
- GuideSection uses RoleTag internally
- FaqAccordion uses shadcn Accordion primitives
- CalloutBox uses --color-status-* CSS variable tokens for dark mode (no dark: classes)
- RoleTag uses --color-role-* and --color-muted CSS variable tokens for dark mode (no dark: classes)
</success_criteria>

<output>
After completion, create `.planning/phases/55-help-center-infrastructure/55-01-SUMMARY.md`
</output>
