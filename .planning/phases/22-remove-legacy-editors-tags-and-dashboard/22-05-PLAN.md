---
phase: 22-remove-legacy-editors-tags-and-dashboard
plan: 05
type: execute
wave: 4
depends_on: ["22-03", "22-04"]
files_modified: []
autonomous: true
requirements:
  - SC-5
  - SC-6
  - SC-7

must_haves:
  truths:
    - "npm run type-check passes"
    - "npm run build succeeds"
    - "No dead imports or references to removed tables/pages"
    - "No orphaned files referencing deleted modules"
  artifacts: []
  key_links: []
---

<objective>
Final verification sweep — type-check, build, lint, and dead code grep to confirm all removals are clean.

Purpose: Catch any stray references, broken imports, or orphaned code that slipped through the previous plans. This is the quality gate before merging.
Output: Clean build with zero references to legacy code.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-remove-legacy-editors-tags-and-dashboard/22-03-SUMMARY.md
@.planning/phases/22-remove-legacy-editors-tags-and-dashboard/22-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full verification sweep and fix any remaining issues</name>
  <files></files>
  <action>
    Run the following verification commands and fix any issues found:

    1. **Type check:**
       ```
       npm run type-check
       ```
       If errors: fix each one. Common issues will be stray imports referencing deleted files.

    2. **Build:**
       ```
       npm run build
       ```
       Must succeed (tsc + vite build).

    3. **Dead reference grep — legacy table names:**
       ```
       grep -rn "recipeVersions\|recipeComponents\|componentIngredients\|packagingVersions\|packagingComponents\|packagingComponentMaterials\|productVersions" src/ convex/ --include="*.ts" --include="*.tsx" | grep -v "_generated/" | grep -v ".planning/" | grep -v "node_modules/"
       ```
       Should return zero matches. If any found, fix.

    4. **Dead reference grep — deleted page names:**
       ```
       grep -rn "RecipeEditor\|PackagingEditor\|ProductEditor\|TagsManager\|MaterialsManager\|Dashboard" src/ --include="*.ts" --include="*.tsx" | grep -v "HubPage\|// " | grep -v ".planning/"
       ```
       Should return zero matches (excluding comments). If any found, fix.

    5. **Dead reference grep — deleted hook names:**
       ```
       grep -rn "useRecipes\|usePackaging\b\|useProducts\b\|useTags\b\|useDashboard\b\|useMaterials\b" src/ --include="*.ts" --include="*.tsx"
       ```
       Should return zero matches. If any found, fix.

    6. **Dead reference grep — deleted permissions:**
       ```
       grep -rn "canAccessRecipes\|canAccessProducts\|canAccessMaterials" src/ --include="*.ts" --include="*.tsx"
       ```
       Should return zero matches. If any found, fix.

    7. **Dead reference grep — branding:**
       ```
       grep -rn "Frollie Recipe Master\|Frollie Master" src/ index.html --include="*.ts" --include="*.tsx" --include="*.html"
       ```
       Should return zero matches. If any found, fix.

    8. **Orphan check — empty directories:**
       Check if any component directories are now empty after file deletions:
       - `src/components/recipes/`
       - `src/components/packaging/`
       - `src/components/products/`
       - `src/components/materials/`
       - `src/components/dashboard/`
       - `src/components/onboarding/`
       Delete any empty directories.

    9. **Lint (optional but recommended):**
       ```
       npm run lint
       ```
       Fix any lint errors related to the changes.

    For each issue found, make the minimal fix. Do not make unrelated changes.
  </action>
  <verify>
    `npm run type-check` passes (exit code 0).
    `npm run build` succeeds (exit code 0).
    All grep checks return zero matches.
  </verify>
  <done>Full codebase clean: type-check passes, build succeeds, no dead references to legacy tables/pages/hooks/permissions, no orphaned files</done>
</task>

</tasks>

<verification>
- `npm run type-check` passes
- `npm run build` succeeds
- Zero grep matches for legacy table names in src/ and convex/ (excluding _generated/)
- Zero grep matches for deleted page/hook/permission names
- Zero grep matches for old branding
- No empty component directories remain
</verification>

<success_criteria>
- `npm run type-check` passes
- `npm run build` succeeds
- No dead imports or references to removed tables/pages/hooks
- No orphaned files or empty directories
- Branding consistently "Frollie Pro"
</success_criteria>

<output>
After completion, create `.planning/phases/22-remove-legacy-editors-tags-and-dashboard/22-05-SUMMARY.md`
</output>
