---
phase: 22-remove-legacy-editors-tags-and-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - src/pages/HubPage.tsx
  - src/pages/index.ts
  - src/App.tsx
autonomous: true
requirements:
  - SC-3
  - SC-6

must_haves:
  truths:
    - "Manager and admin users see a role-filtered hub page at /"
    - "Kitchen role is redirected to /kitchen, order_staff to /orders"
    - "Hub page shows static navigation cards organized by functional area"
    - "Cards are completely hidden for roles without access"
    - "Hub page has Frollie Pro branding with user greeting"
    - "No live data queries on the hub page"
  artifacts:
    - path: "src/pages/HubPage.tsx"
      provides: "Role-filtered hub page with navigation cards"
      min_lines: 80
    - path: "src/App.tsx"
      provides: "Updated routing with hub page for manager/admin"
  key_links:
    - from: "src/pages/HubPage.tsx"
      to: "src/contexts/AuthContext.tsx"
      via: "useAuth hook"
      pattern: "useAuth"
    - from: "src/App.tsx"
      to: "src/pages/HubPage.tsx"
      via: "route element"
      pattern: "HubPage"
---

<objective>
Build the "Frollie Pro" hub page at `/` — a static, role-filtered navigation page replacing the deleted Dashboard.

Purpose: Provide managers and admins with a polished landing page organized by functional area. Kitchen and order_staff keep their direct role-based redirects. This is the user-facing replacement for the removed Dashboard.
Output: New HubPage component wired into App.tsx routing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-remove-legacy-editors-tags-and-dashboard/22-02-SUMMARY.md
@.planning/phases/22-remove-legacy-editors-tags-and-dashboard/22-CONTEXT.md
@src/App.tsx
@src/contexts/AuthContext.tsx
@src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invoke frontend-design skill to produce HubPage design spec</name>
  <files>
    src/pages/HubPage.tsx
  </files>
  <action>
    **Per locked user decision:** "The frontend-designer skill should handle the entire hub page design."

    Invoke the `/frontend-design` skill with the following design brief:

    **Page:** HubPage — role-filtered navigation hub replacing the deleted Dashboard
    **Route:** `/home`
    **Requirements:**
    - "Frollie Pro" branded header with user greeting ("Welcome back, {user.name}" via `useAuth()`)
    - Static navigation cards (NO live data queries / no `useQuery` calls)
    - Use shadcn/ui `Card` component, Lucide icons, React Router `<Link>` for navigation
    - Responsive grid: 1 col mobile, 2 cols md, 3 cols lg
    - Cards completely hidden (not rendered) for roles without access to ANY page in that area
    - Fresh, polished, modern visual look within existing Tailwind design system
    - Subtle hover effects on cards
    - Sub-section buttons as pills/chips or small outlined buttons
    - English language throughout

    **Functional area groupings** (skill may refine grouping/ordering):
    1. **Operations** — Orders, Kitchen, Packaging View (canAccessOrders, canAccessKitchen)
    2. **Inventory & Supply** — Inventory, Locations, Restock Planner, Ingredients (canAccessInventory)
    3. **Sales & Distribution** — GoFood Depot, Sales Analytics, K3Mart Cockpit (canAccessDashboard)
    4. **Configuration** — Production Components, Customers, WhatsApp Templates (mixed permissions)
    5. **Admin** — Menu Products, Vouchers, Users (admin only)

    **Role visibility:** Use `hasPermission()` from `useAuth()`. Card shown if user has access to at least one page in the area.

    **Context files for the skill:**
    - `src/contexts/AuthContext.tsx` (useAuth hook, hasPermission)
    - `src/lib/types.ts` (Permission type)
    - `src/components/ui/card.tsx` (shadcn Card component)
    - `src/App.tsx` (existing routes for reference)
    - `src/index.css` (theme variables)

    The skill will produce the full `HubPage.tsx` implementation. After the skill completes, verify its output meets all requirements above.
  </action>
  <verify>
    File exists at `src/pages/HubPage.tsx`.
    Component imports `useAuth` from AuthContext.
    Component uses `Card` from shadcn/ui.
    No `useQuery` or `useMutation` imports present.
    Has responsive grid layout with Tailwind classes.
    Has "Frollie Pro" branding and user greeting.
    Cards are role-filtered using hasPermission().
  </verify>
  <done>HubPage.tsx created via frontend-design skill with role-filtered navigation cards, Frollie Pro branding, and user greeting; no live data queries</done>
</task>

<task type="auto">
  <name>Task 2: Wire HubPage into App.tsx routing and update barrel export</name>
  <files>
    src/App.tsx
    src/pages/index.ts
  </files>
  <action>
    1. **Add `HubPage` to `src/pages/index.ts`:**
       Add export: `export { HubPage } from './HubPage';`

    2. **Update `src/App.tsx`:**
       - Import `HubPage` from `@/pages`
       - Update the `RoleBasedRedirect` component:
         - Kitchen role → redirect to `/kitchen` (unchanged)
         - Order staff role → redirect to `/orders` (unchanged)
         - Manager and Admin → render `<HubPage />` directly (instead of redirecting to `/orders`)

       The approach: Replace the current `<Route index element={<RoleBasedRedirect />} />` logic. Two options:

       **Option A (preferred):** Keep `RoleBasedRedirect` but for manager/admin, redirect to a `/home` route where HubPage is rendered inside Layout. Then:
       - Add route: `<Route path="home" element={<ProtectedRoute requiredPermission="canAccessDashboard"><HubPage /></ProtectedRoute>} />` inside the Layout block
       - RoleBasedRedirect: manager/admin → `<Navigate to="/home" />`

       **Actually, simpler Option B:** Render HubPage directly at the index route wrapped in Layout, and keep RoleBasedRedirect for kitchen/order_staff only:
       - Change the index route to: `<Route index element={<RoleBasedLanding />} />`
       - `RoleBasedLanding` checks role: kitchen → Navigate to /kitchen, order_staff → Navigate to /orders, manager/admin → wraps in Layout and renders HubPage

       **Best approach:** Use the existing pattern. Add HubPage as a standard route inside `<Route element={<Layout />}>`:
       ```
       <Route path="home" element={
         <ProtectedRoute requiredPermission="canAccessDashboard">
           <HubPage />
         </ProtectedRoute>
       } />
       ```
       Then update `RoleBasedRedirect`:
       - kitchen → `/kitchen`
       - order_staff → `/orders`
       - manager/admin → `/home`

       This keeps the index route (`/`) as the redirect dispatcher, and `/home` as the actual hub page route with proper Layout wrapping.

    3. **Remove the bandwidth conservation comment** on the RoleBasedRedirect that says "was /sales, re-enable after March 1st" — the hub page is the permanent landing now.
  </action>
  <verify>
    `npm run type-check` passes.
    `npm run build` succeeds.
    In App.tsx: `/home` route renders HubPage inside Layout.
    RoleBasedRedirect sends manager/admin to `/home`.
  </verify>
  <done>HubPage wired at /home route; RoleBasedRedirect sends manager/admin to /home; kitchen→/kitchen and order_staff→/orders unchanged; build passes</done>
</task>

</tasks>

<verification>
- `npm run type-check` passes
- `npm run build` succeeds
- HubPage.tsx exists with role-filtered cards and no data queries
- `/home` route renders HubPage for manager/admin
- Kitchen and order_staff still redirect to their role-specific pages
</verification>

<success_criteria>
- New HubPage component with Frollie Pro branding and role-filtered navigation cards
- HubPage wired into App.tsx at `/home` route
- RoleBasedRedirect updated: manager/admin → /home
- No live data queries on hub page (zero Convex bandwidth)
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/22-remove-legacy-editors-tags-and-dashboard/22-03-SUMMARY.md`
</output>
