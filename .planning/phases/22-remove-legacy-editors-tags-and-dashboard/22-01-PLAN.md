---
phase: 22-remove-legacy-editors-tags-and-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/legacy-export/
  - convex/schema.ts
  - convex/recipes/queries.ts
  - convex/recipes/mutations.ts
  - convex/packaging/queries.ts
  - convex/packaging/mutations.ts
  - convex/products/queries.ts
  - convex/products/mutations.ts
  - convex/tags/queries.ts
  - convex/tags/mutations.ts
  - convex/dashboard/queries.ts
  - convex/lib/costInvalidation.ts
  - convex/ingredients/mutations.ts
  - convex/materials/mutations.ts
autonomous: true
requirements:
  - SC-1
  - SC-2
  - SC-4

must_haves:
  truths:
    - "Production table data checked and exported to docs/legacy-export/ if non-empty"
    - "11 legacy schema tables are removed from schema.ts"
    - "No backend query or mutation references the dropped tables"
    - "costInvalidation.ts only contains menu product and production component functions"
    - "ingredients/mutations.ts no longer schedules invalidateRecipeCosts"
    - "materials/mutations.ts no longer schedules invalidatePackagingCosts"
  artifacts:
    - path: "convex/schema.ts"
      provides: "Schema without 11 legacy tables"
      contains: "defineSchema"
    - path: "convex/lib/costInvalidation.ts"
      provides: "Only invalidateMenuProductCosts and invalidateProductionComponentCosts"
      exports: ["invalidateMenuProductCosts", "invalidateProductionComponentCosts"]
  key_links:
    - from: "convex/ingredients/mutations.ts"
      to: "convex/lib/costInvalidation.ts"
      via: "ctx.scheduler.runAfter"
      pattern: "invalidateProductionComponentCosts"
    - from: "convex/componentTypes/mutations.ts"
      to: "convex/lib/costInvalidation.ts"
      via: "ctx.scheduler.runAfter"
      pattern: "invalidateMenuProductCosts"
---

<objective>
Drop 11 legacy schema tables, delete 5 backend modules, and strip costInvalidation.ts to only surviving functions.

Purpose: Remove all backend dead code for the legacy recipe/packaging/product/tags/dashboard system. This is the foundation that must complete before frontend cleanup can begin.
Output: Clean backend with no references to dropped tables; costInvalidation.ts reduced to 2 functions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@convex/schema.ts
@convex/lib/costInvalidation.ts
@convex/ingredients/mutations.ts
@convex/materials/mutations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check production table data and export if non-empty</name>
  <files>
    docs/legacy-export/
  </files>
  <action>
    **Data preservation gate (per locked user decision):** Before dropping any tables, verify whether the 11 legacy tables contain data in production.

    1. Run `npx convex export --path /tmp/convex-export` to get a full database export (or use `npx convex run` to query row counts).
       Alternatively, for each of the 11 tables, run a quick count query via the Convex dashboard Functions tab or CLI:
       ```
       npx convex run --prod "SELECT COUNT(*) FROM recipes"
       ```
       Tables to check: `recipes`, `recipeVersions`, `recipeComponents`, `componentIngredients`, `packagingRecipes`, `packagingVersions`, `packagingComponents`, `packagingComponentMaterials`, `products`, `productVersions`, `tags`.

    2. If ALL 11 tables have zero rows → skip to Task 2. No export needed.

    3. If ANY table has data:
       - Create directory `docs/legacy-export/`
       - For each non-empty table, extract its data from the Convex export and save as `docs/legacy-export/{tableName}.jsonl`
       - Commit the exports: `git add docs/legacy-export/ && git commit -m "docs(22): export legacy table data before dropping"`

    NOTE: The Convex export produces JSONL files per table. If using `npx convex export`, simply copy the relevant table files to `docs/legacy-export/`.
  </action>
  <verify>
    If tables were empty: log output confirms zero rows for all 11 tables.
    If tables had data: `docs/legacy-export/` directory exists with JSONL files for each non-empty table, committed to repo.
  </verify>
  <done>Production data for all 11 legacy tables has been checked; non-empty tables exported to docs/legacy-export/ and committed (or confirmed empty)</done>
</task>

<task type="auto">
  <name>Task 2: Drop 11 legacy tables from schema.ts and delete 5 backend modules</name>
  <files>
    convex/schema.ts
    convex/recipes/queries.ts
    convex/recipes/mutations.ts
    convex/packaging/queries.ts
    convex/packaging/mutations.ts
    convex/products/queries.ts
    convex/products/mutations.ts
    convex/tags/queries.ts
    convex/tags/mutations.ts
    convex/dashboard/queries.ts
  </files>
  <action>
    1. Remove these 11 table definitions from `convex/schema.ts`:
       - `recipes` (and its indexes)
       - `recipeVersions` (and its indexes)
       - `recipeComponents` (and its indexes)
       - `componentIngredients` (and its indexes)
       - `packagingRecipes` (and its indexes)
       - `packagingVersions` (and its indexes)
       - `packagingComponents` (and its indexes)
       - `packagingComponentMaterials` (and its indexes)
       - `products` (and its indexes)
       - `productVersions` (and its indexes)
       - `tags` (and its indexes)

       IMPORTANT: Remove ALL 11 atomically in one edit. The `tagIds` fields on recipes/packagingRecipes/products reference the tags table — since all are being removed together, there are no dangling FK references.

       Do NOT remove: `ingredients`, `packagingMaterials`, `menuProducts`, `menuProductComponents`, `componentTypes`, `productionComponentIngredients`, `productionComponentLinks`, or any other active table.

    2. Delete entire directories:
       - `convex/recipes/` (both queries.ts and mutations.ts)
       - `convex/packaging/` (both queries.ts and mutations.ts) — this is the legacy packaging recipe module, NOT the packaging view orders query
       - `convex/products/` (both queries.ts and mutations.ts)
       - `convex/tags/` (both queries.ts and mutations.ts)
       - `convex/dashboard/` (queries.ts — the getSummary query does an unbounded orders scan)

       WARNING: Do NOT delete `convex/orders/queries.ts` which contains `getPackagingOrders` — that is an active operational query. The `convex/packaging/` directory is the legacy packaging RECIPE module, completely separate from the orders packaging view.
  </action>
  <verify>
    Run `grep -r "componentIngredients\|recipeVersions\|recipeComponents\|packagingVersions\|packagingComponents\|packagingComponentMaterials\|productVersions" convex/ --include="*.ts" -l` — should return only `convex/lib/costInvalidation.ts` (cleaned in Task 2) and `convex/_generated/` files.
    Verify `convex/recipes/`, `convex/packaging/`, `convex/products/`, `convex/tags/`, `convex/dashboard/` directories are deleted.
  </verify>
  <done>11 tables removed from schema.ts; 5 backend module directories deleted; no remaining backend code references dropped tables except costInvalidation.ts (handled in Task 2)</done>
</task>

<task type="auto">
  <name>Task 3: Strip costInvalidation.ts and update callers</name>
  <files>
    convex/lib/costInvalidation.ts
    convex/ingredients/mutations.ts
    convex/materials/mutations.ts
  </files>
  <action>
    1. In `convex/lib/costInvalidation.ts`:
       - DELETE the `invalidateRecipeCosts` function (lines ~20-99) — it references `componentIngredients` and `recipeComponents` tables (being dropped)
       - DELETE the `invalidatePackagingCosts` function (lines ~108-171) — it references `packagingComponentMaterials` and `packagingComponents` tables (being dropped)
       - KEEP `invalidateMenuProductCosts` (lines ~184-226) — references only `menuProductComponents` and `menuProducts` (both active)
       - KEEP `invalidateProductionComponentCosts` (lines ~237-313) — references only `productionComponentIngredients`, `componentTypes`, `productionComponentLinks` (all active)
       - Remove the `calculateLineCost` import if no longer needed by surviving functions. Check: `invalidateProductionComponentCosts` uses `calculateLineCost` so the import stays.

    2. In `convex/ingredients/mutations.ts`:
       - Find the line (~101): `await ctx.scheduler.runAfter(0, internal.lib.costInvalidation.invalidateRecipeCosts, { ingredientId: ... })`
       - DELETE this scheduler call entirely. The legacy recipe cost invalidation is no longer needed.
       - KEEP the `invalidateProductionComponentCosts` scheduler call (~106) — this is active.

    3. In `convex/materials/mutations.ts`:
       - Find the line (~100): `await ctx.scheduler.runAfter(0, internal.lib.costInvalidation.invalidatePackagingCosts, { materialId: ... })`
       - DELETE this scheduler call entirely. The legacy packaging cost invalidation is no longer needed.
       - Check if any other code in this file references dropped tables. If so, remove those references too.

    4. Verify that `convex/componentTypes/mutations.ts:203` and `convex/productionRecipes/mutations.ts:363` still correctly import `internal.lib.costInvalidation.invalidateMenuProductCosts` — these calls MUST remain unchanged.
  </action>
  <verify>
    Run `grep -rn "invalidateRecipeCosts\|invalidatePackagingCosts" convex/ --include="*.ts"` — should return zero results (excluding _generated/).
    Run `grep -rn "invalidateMenuProductCosts\|invalidateProductionComponentCosts" convex/ --include="*.ts"` — should show the 2 surviving functions in costInvalidation.ts plus their callers.
    Run `npm run type-check` to confirm no TypeScript errors from removed references.
  </verify>
  <done>costInvalidation.ts contains only 2 surviving functions; ingredients/mutations.ts no longer schedules recipe cost invalidation; materials/mutations.ts no longer schedules packaging cost invalidation; type-check passes</done>
</task>

</tasks>

<verification>
- `npm run type-check` passes (no references to dropped tables)
- `convex/schema.ts` contains 48 tables (59 - 11)
- `convex/lib/costInvalidation.ts` exports only `invalidateMenuProductCosts` and `invalidateProductionComponentCosts`
- No TypeScript files in `convex/` (excluding `_generated/`) reference any of the 11 dropped table names
</verification>

<success_criteria>
- All 11 legacy schema tables removed from convex/schema.ts
- 5 backend module directories deleted (recipes, packaging, products, tags, dashboard)
- costInvalidation.ts stripped to 2 surviving functions
- Caller references in ingredients/mutations.ts and materials/mutations.ts cleaned
- `npm run type-check` passes
</success_criteria>

<output>
After completion, create `.planning/phases/22-remove-legacy-editors-tags-and-dashboard/22-01-SUMMARY.md`
</output>
