---
phase: 22-remove-legacy-editors-tags-and-dashboard
plan: 04
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - src/components/layout/Header.tsx
  - src/components/layout/Footer.tsx
  - src/components/layout/MobileBottomNav.tsx
  - src/pages/Login.tsx
  - src/hooks/useDocumentTitle.ts
  - index.html
autonomous: true
requirements:
  - SC-3
  - SC-7

must_haves:
  truths:
    - "All instances of 'Frollie Recipe Master' are replaced with 'Frollie Pro'"
    - "Header nav has a Home link for manager/admin pointing to /home"
    - "Tags item removed from MobileBottomNav"
    - "Materials nav item removed if present"
    - "index.html title tag updated"
  artifacts:
    - path: "src/components/layout/Header.tsx"
      provides: "Updated brand name + Home nav link"
      contains: "Frollie Pro"
    - path: "src/pages/Login.tsx"
      provides: "Updated brand name on login page"
      contains: "Frollie Pro"
    - path: "src/hooks/useDocumentTitle.ts"
      provides: "Updated document title suffix"
      contains: "Frollie Pro"
    - path: "index.html"
      provides: "Updated page title"
      contains: "Frollie Pro"
  key_links:
    - from: "src/components/layout/Header.tsx"
      to: "/home"
      via: "nav link"
      pattern: "Home.*home"
---

<objective>
Rebrand to "Frollie Pro" and reorganize navigation — update brand name everywhere, add Home nav link, remove dead nav items.

Purpose: Complete the rebranding from "Frollie Recipe Master" to "Frollie Pro" across all user-facing surfaces, and clean navigation to reflect the removed pages. Runs in parallel with Plan 03 (no file overlap).
Output: Consistent "Frollie Pro" branding; clean navigation with Home link.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-remove-legacy-editors-tags-and-dashboard/22-CONTEXT.md
@src/components/layout/Header.tsx
@src/components/layout/Footer.tsx
@src/pages/Login.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand to Frollie Pro and update navigation</name>
  <files>
    src/components/layout/Header.tsx
    src/components/layout/Footer.tsx
    src/components/layout/MobileBottomNav.tsx
    src/pages/Login.tsx
    index.html
  </files>
  <action>
    **Branding updates — replace "Frollie Recipe Master" with "Frollie Pro":**

    1. `src/components/layout/Header.tsx` line ~308:
       Change `"Frollie Recipe Master"` → `"Frollie Pro"`

    2. `src/components/layout/Footer.tsx` line ~21:
       Change `"Frollie Recipe Master"` → `"Frollie Pro"`

    3. `src/pages/Login.tsx` line ~79:
       Change `"Frollie Recipe Master"` → `"Frollie Pro"` (h1 heading)

    4. `src/pages/Login.tsx` line ~152:
       Change `"Frollie Recipe Master"` → `"Frollie Pro"` (footer copyright)

    5. `index.html` line ~7:
       Change `<title>Frollie Master</title>` → `<title>Frollie Pro</title>`

    6. `src/hooks/useDocumentTitle.ts` — 3 occurrences of "Frollie Master":
       - Line ~5 JSDoc: `Automatically appends " | Frollie Master" suffix.` → `Automatically appends " | Frollie Pro" suffix.`
       - Line ~6 JSDoc: `Resets to "Frollie Master" on unmount.` → `Resets to "Frollie Pro" on unmount.`
       - Line ~11 runtime: `` `${title} | Frollie Master` : 'Frollie Master' `` → `` `${title} | Frollie Pro` : 'Frollie Pro' ``

    7. `src/components/shared/FormBuilder.example.tsx` line 5:
       Change "Frollie Recipe Master" → "Frollie Pro" in comment (low priority but completeness)

    **Navigation updates:**

    8. `src/components/layout/Header.tsx` — Add "Home" nav link:
       - Add a new item to `mainNavItems` (or equivalent nav array): `{ path: '/home', icon: Home, label: 'Home', permission: 'canAccessDashboard' }`
       - Import `Home` icon from lucide-react
       - Position as the FIRST item in the nav list
       - Only visible to manager/admin (canAccessDashboard permission)

    9. `src/components/layout/MobileBottomNav.tsx` — Remove Tags nav item:
       - Find the item `{ path: '/tags', icon: Tags, label: 'Tags', permission: 'canAccessRecipes' }` (approximately line 54)
       - DELETE this entire nav item
       - Note: `canAccessRecipes` permission is being removed in Plan 02 — this nav item would break without this cleanup

    10. `src/components/layout/Header.tsx` — Remove any nav references to removed pages:
        - Check `configItems` array for any recipe/packaging/product/tags/materials entries and remove them
        - Check for any `/materials` or `/tags` references in nav arrays
        - Verify no nav item uses `canAccessRecipes`, `canAccessProducts`, or `canAccessMaterials` permissions (these are being removed in Plan 02's types.ts cleanup)

    11. `src/components/layout/MobileBottomNav.tsx` — Add Home to mobile nav:
        - Add `{ path: '/home', icon: Home, label: 'Home', permission: 'canAccessDashboard' }` as the first mobile nav item for manager/admin
  </action>
  <verify>
    Run `grep -rn "Frollie Recipe Master\|Frollie Master" src/ index.html` — should return zero matches (including src/hooks/useDocumentTitle.ts).
    Run `grep -rn "Frollie Pro" src/ index.html` — should show all updated locations.
    Run `grep -rn "/tags\|canAccessRecipes\|canAccessProducts\|canAccessMaterials" src/components/layout/ --include="*.tsx"` — should return zero matches.
    Run `npm run type-check` to confirm no broken permission references.
  </verify>
  <done>All branding updated to "Frollie Pro"; Home nav link added for manager/admin; Tags nav item removed; no nav references to deleted pages; type-check passes</done>
</task>

</tasks>

<verification>
- `npm run type-check` passes
- `npm run build` succeeds
- Zero occurrences of "Frollie Recipe Master" or "Frollie Master" in codebase (excluding .planning/, .claude/, .agent/ directories)
- Home nav link exists pointing to /home for manager/admin
- No nav item references /tags, /materials, /recipes/:id, /packaging/:id, /products/:id
</verification>

<success_criteria>
- "Frollie Pro" branding in Header, Footer, Login page, index.html
- Home nav link added to desktop and mobile navigation
- Tags nav item removed from MobileBottomNav
- No navigation references to deleted pages
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/22-remove-legacy-editors-tags-and-dashboard/22-04-SUMMARY.md`
</output>
