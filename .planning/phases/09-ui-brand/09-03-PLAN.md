---
phase: 09-ui-brand
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - src/pages/IngredientsManager.tsx
  - src/pages/MaterialsManager.tsx
  - src/pages/MenuProductsManager.tsx
  - src/pages/RecipeEditor.tsx
  - src/pages/PackagingEditor.tsx
  - src/pages/ProductEditor.tsx
  - src/pages/PackagingView.tsx
  - src/pages/LocationsManager.tsx
  - src/pages/ProductionComponentsManager.tsx
  - src/pages/UsersManager.tsx
  - src/pages/VouchersManager.tsx
  - src/pages/WhatsAppTemplatesManager.tsx
  - src/pages/InventoryManager.tsx
  - src/components/layout/PageHeader.tsx
autonomous: true

must_haves:
  truths:
    - "All 13 standard pages use PageHeader component (no custom inline headers)"
    - "No page has its own outer padding wrapper (PageContainer handles it)"
    - "Hardcoded gray/white colors replaced with semantic tokens in all 13 pages"
    - "All pages use space-y-6 for content spacing (not space-y-4 or space-y-8)"
  artifacts:
    - path: "src/components/layout/PageHeader.tsx"
      provides: "Enhanced PageHeader with badge and search slots"
      exports: ["PageHeader"]
  key_links:
    - from: "src/pages/IngredientsManager.tsx"
      to: "src/components/layout/PageHeader.tsx"
      via: "PageHeader component import"
      pattern: "PageHeader"
    - from: "src/pages/MaterialsManager.tsx"
      to: "src/components/layout/PageHeader.tsx"
      via: "PageHeader component import"
      pattern: "PageHeader"
    - from: "src/pages/MenuProductsManager.tsx"
      to: "src/components/layout/PageHeader.tsx"
      via: "PageHeader component import"
      pattern: "PageHeader"
---

<objective>
Audit and fix 13 standard pages (non-complex) for brand consistency: migrate custom headers to PageHeader, remove per-page padding, replace hardcoded colors with semantic tokens, standardize spacing.

Purpose: Establish visual consistency across the majority of pages. These 13 pages are simpler and share common patterns, making them efficient to batch-fix.
Output: 13 updated page files, enhanced PageHeader component
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ui-brand/09-RESEARCH.md
@.planning/phases/09-ui-brand/09-01-SUMMARY.md
@.planning/phases/09-ui-brand/09-02-SUMMARY.md
@src/components/layout/PageHeader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance PageHeader and fix 6 pages with custom headers</name>
  <files>
    src/components/layout/PageHeader.tsx
    src/pages/IngredientsManager.tsx
    src/pages/MaterialsManager.tsx
    src/pages/MenuProductsManager.tsx
    src/pages/RecipeEditor.tsx
    src/pages/PackagingEditor.tsx
    src/pages/ProductEditor.tsx
  </files>
  <action>
**1. Enhance `src/components/layout/PageHeader.tsx`:**

Add new optional props to support all page header patterns:
```tsx
interface PageHeaderProps {
  title: string;
  description?: string;
  backTo?: string;
  backLabel?: string;
  action?: ReactNode;       // existing
  children?: ReactNode;     // existing
  badge?: ReactNode;        // NEW: status badge next to title
}
```

Keep existing styling but ensure consistency:
- Title: `text-2xl font-bold tracking-tight` (already correct)
- Description: `text-sm text-muted-foreground mt-1` (already correct)
- Container: `flex items-center justify-between mb-6` (already correct)

Add the `badge` rendering — inline next to title:
```tsx
<div className="flex items-center gap-3">
  <h1 className="text-2xl font-bold tracking-tight">{title}</h1>
  {badge}
</div>
```

**2. Fix `src/pages/IngredientsManager.tsx`:**

Current: Custom `<h1>` with `text-3xl font-bold` + back button inline.

Changes:
- Replace custom header with `<PageHeader title="Ingredients" backTo="/menu-products" backLabel="Back to Products" action={...} />`
- Remove outer `space-y-6` div wrapper if present (PageContainer handles outer padding)
- Keep inner content with `space-y-6` for section spacing
- Replace any `text-gray-*` with `text-foreground` or `text-muted-foreground`
- Replace any `bg-gray-*` with `bg-muted` or `bg-muted/50`
- Replace any `bg-white` with `bg-card`

**3. Fix `src/pages/MaterialsManager.tsx`:**

Current: Custom `<h1>` with `text-3xl font-bold` + back button inline.

Same fix pattern as IngredientsManager:
- Replace custom header with `<PageHeader title="Materials" backTo="/menu-products" backLabel="Back to Products" action={...} />`
- Remove outer wrapper padding
- Replace hardcoded colors with semantic tokens

**4. Fix `src/pages/MenuProductsManager.tsx`:**

Current: Custom `<h1>` with `text-2xl sm:text-3xl` + responsive layout.

Changes:
- Replace custom header section with `<PageHeader title="Menu Products" description="Manage your product catalog" action={...} />`
- Move any search/filter bar to below PageHeader (between header and list)
- Remove outer wrapper padding
- Replace hardcoded colors

**5. Fix `src/pages/RecipeEditor.tsx`, `PackagingEditor.tsx`, `ProductEditor.tsx`:**

These already use PageHeader. Check and fix:
- Remove any outer `space-y-6` wrapper (PageContainer provides padding)
- But KEEP inner `space-y-6` for content section spacing
- Replace hardcoded `text-gray-*`, `bg-gray-*`, `bg-white` with semantic tokens
- Ensure consistent spacing: `space-y-6` for main content sections

**For each page, the general fix pattern is:**

a) Read the page file carefully
b) If it has a custom header (not PageHeader), replace with PageHeader
c) Remove any outer padding div (`p-6`, `space-y-8`, etc.) — PageContainer provides this
d) Keep internal spacing at `space-y-6` for sections
e) Search and replace in the file:
   - `text-gray-900` / `text-gray-800` -> `text-foreground`
   - `text-gray-700` / `text-gray-600` -> `text-foreground/80` or `text-muted-foreground`
   - `text-gray-500` / `text-gray-400` -> `text-muted-foreground`
   - `bg-gray-50` / `bg-gray-100` -> `bg-muted` or `bg-muted/50`
   - `bg-gray-200` -> `bg-muted`
   - `bg-white` -> `bg-card` or `bg-background`
   - `border-gray-200` / `border-gray-300` -> `border-border`
   - `divide-gray-200` -> `divide-border`
   - `ring-gray-*` -> `ring-ring` or `ring-border`

f) Replace any `text-terracotta` or `bg-terracotta*` references with `text-brand` or `bg-brand*` equivalents (the CSS classes were renamed in Plan 01)
g) Replace any `order-heading` class usage with `font-bold` (Playfair Display was removed)

**IMPORTANT:**
- Do NOT change component logic, only styling/structure
- Do NOT change how data is fetched or displayed
- Read each page first before modifying to understand its structure
- Preserve all existing functionality (buttons, modals, forms, etc.)
  </action>
  <verify>
1. `npm run type-check` passes
2. `grep -r "text-3xl" src/pages/IngredientsManager.tsx src/pages/MaterialsManager.tsx` returns 0 (custom headers removed)
3. All 6 page files import PageHeader
4. `grep -c "text-gray-" src/pages/IngredientsManager.tsx src/pages/MaterialsManager.tsx src/pages/MenuProductsManager.tsx` returns 0 or minimal
  </verify>
  <done>
PageHeader enhanced with badge slot. IngredientsManager, MaterialsManager, MenuProductsManager migrated from custom headers to PageHeader. RecipeEditor, PackagingEditor, ProductEditor cleaned up. All 6 pages have consistent spacing and semantic color tokens.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix remaining 7 standard pages for consistency</name>
  <files>
    src/pages/PackagingView.tsx
    src/pages/LocationsManager.tsx
    src/pages/ProductionComponentsManager.tsx
    src/pages/UsersManager.tsx
    src/pages/VouchersManager.tsx
    src/pages/WhatsAppTemplatesManager.tsx
    src/pages/InventoryManager.tsx
  </files>
  <action>
These 7 pages already use PageHeader but need consistency fixes.

**For each page, apply the same fix pattern:**

a) Read the page file first
b) Remove any outer padding wrapper (`p-6`, `space-y-8`, etc.) since PageContainer provides uniform padding
c) Ensure content uses `space-y-6` for section spacing
d) Replace all hardcoded color classes with semantic tokens:
   - `text-gray-*` -> `text-foreground` / `text-muted-foreground`
   - `bg-gray-*` / `bg-white` -> `bg-muted` / `bg-card` / `bg-background`
   - `border-gray-*` -> `border-border`
   - `divide-gray-*` -> `divide-border`
   - Any terracotta references -> brand equivalents
e) Check that page doesn't add its own container/padding (redundant with PageContainer)
f) Replace `order-heading` class if present with `font-bold`

**Page-specific notes:**

1. **PackagingView.tsx** — Has `space-y-6` wrapper. Remove outer wrapper (keep inner spacing). Replace hardcoded grays.

2. **LocationsManager.tsx** — Has `space-y-6` wrapper. Same treatment. Replace hardcoded grays.

3. **ProductionComponentsManager.tsx** — Also known as ComponentTypesManager. Check actual filename in pages directory. Relies on Layout container currently. Remove any redundant padding. Replace hardcoded grays.

4. **UsersManager.tsx** — Relies on Layout container. No outer wrapper to remove. Replace hardcoded grays.

5. **VouchersManager.tsx** — Relies on Layout container. Replace hardcoded grays.

6. **WhatsAppTemplatesManager.tsx** — Has `p-6` wrapper. Remove it (PageContainer provides padding). Replace hardcoded grays.

7. **InventoryManager.tsx** — Relies on Layout container. Replace hardcoded grays. Check for any custom padding.

**For SalesAnalytics and K3MartCockpit:** These are standard layout pages. Check if they have `p-6` wrappers to remove and hardcoded colors to fix. If so, include them here too.

Wait — SalesAnalytics and K3MartCockpit are included in the full 21-page audit but should be in this batch since they use standard layout (not full-width). Let me include them:

8. **SalesAnalytics.tsx** — Has `p-6` wrapper. Remove it. Replace hardcoded grays.

9. **K3MartCockpit.tsx** — Has `space-y-6` wrapper. Keep inner spacing. Replace hardcoded grays.

So this task covers 9 pages total (7 + SalesAnalytics + K3MartCockpit).

**IMPORTANT:**
- Do NOT change component logic
- Preserve all existing functionality
- Only modify styling/structure for consistency
- Read each page before modifying
  </action>
  <verify>
1. `npm run type-check` passes
2. `grep -c "p-6" src/pages/WhatsAppTemplatesManager.tsx` returns 0 (outer padding removed)
3. `grep -rn "bg-white" src/pages/PackagingView.tsx src/pages/LocationsManager.tsx src/pages/UsersManager.tsx` returns 0 or minimal
4. All 9 pages have consistent `space-y-6` for content sections
  </verify>
  <done>
All 9 standard pages (PackagingView, LocationsManager, ProductionComponentsManager, UsersManager, VouchersManager, WhatsAppTemplatesManager, InventoryManager, SalesAnalytics, K3MartCockpit) have consistent padding (from PageContainer), semantic color tokens (no hardcoded grays), and uniform spacing (space-y-6).
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. All 15 standard pages use PageHeader (no custom inline headers)
3. No page has its own outer `p-6` or `container` wrapper (PageContainer handles it)
4. `grep -rn "text-gray-900\|bg-white\|border-gray-200" src/pages/IngredientsManager.tsx src/pages/MaterialsManager.tsx src/pages/MenuProductsManager.tsx src/pages/PackagingView.tsx src/pages/LocationsManager.tsx src/pages/ProductionComponentsManager.tsx src/pages/UsersManager.tsx src/pages/VouchersManager.tsx src/pages/WhatsAppTemplatesManager.tsx src/pages/InventoryManager.tsx` returns 0 or minimal
5. All pages use `space-y-6` for content spacing
</verification>

<success_criteria>
- IngredientsManager, MaterialsManager, MenuProductsManager migrated to PageHeader
- All 15 standard pages have consistent padding, colors, spacing
- Hardcoded gray/white/terracotta colors replaced with semantic tokens
- PageHeader enhanced with badge slot
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-brand/09-03-SUMMARY.md`
</output>
