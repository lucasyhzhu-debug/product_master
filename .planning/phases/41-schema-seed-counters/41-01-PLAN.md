---
phase: 41-schema-seed-counters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/schema.ts
  - convex/accounts/mutations.ts
  - convex/accounts/__tests__/seed.test.ts
autonomous: true
requirements:
  - COA-04
  - COA-05
  - JE-04
user_setup: []

must_haves:
  truths:
    - "Running accounts:seedDefaults creates 36 GL accounts with correct codes, names, types, categories"
    - "Running accounts:seedDefaults a second time produces no duplicates (idempotent)"
    - "All 36 default accounts have isSystem: true and isActive: true"
    - "journalEntryLines table has entryDate field with by_account_entryDate compound index"
    - "All 10 new tables and users modification pass type-check"
  artifacts:
    - path: "convex/schema.ts"
      provides: "10 new tables (accounts, expenses, expenseStatusHistory, reimbursementBatches, reimbursementBatchItems, journalEntries, journalEntryLines, bankAccounts, payrollEntries, counters) + users bank fields"
      contains: "accounts: defineTable"
    - path: "convex/accounts/mutations.ts"
      provides: "seedDefaults mutation with 36 PSAK-aligned accounts"
      exports: ["seedDefaults"]
    - path: "convex/accounts/__tests__/seed.test.ts"
      provides: "Seed idempotency and isSystem flag tests"
      min_lines: 30
  key_links:
    - from: "convex/accounts/mutations.ts"
      to: "convex/schema.ts"
      via: "ctx.db.insert('accounts', ...)"
      pattern: "ctx\\.db\\.insert\\(\"accounts\""
    - from: "convex/accounts/mutations.ts"
      to: "convex/schema.ts"
      via: "ctx.db.query('accounts').withIndex('by_code', ...)"
      pattern: "withIndex\\(\"by_code\""
---

<objective>
Add 10 new accounting tables to the Convex schema, modify the users table with bank detail fields, and create the Chart of Accounts seed function with 36 PSAK-aligned default accounts.

Purpose: This is the foundational data layer for the v1.7 Expense & Accounting system. All downstream phases (42-50) depend on these table definitions existing.
Output: Updated schema.ts with 75 tables (65 existing + 10 new), accounts/mutations.ts with seedDefaults, and tests proving seed idempotency.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-schema-seed-counters/41-CONTEXT.md
@.planning/phases/41-schema-seed-counters/41-RESEARCH.md

@convex/schema.ts
@convex/productionUnitTypes/mutations.ts
@convex/lib/periodRange.ts
</context>

## Git Workflow
**Branch:** `feature/41-schema-seed-counters`
**Checkpoints:** None (fully autonomous)

## Implementation Waves
### Wave 1: Backend [SEQUENTIAL]
| Agent | Task | Files |
|-------|------|-------|
| convex-backend | Schema + Seed + Tests | convex/schema.ts, convex/accounts/mutations.ts, convex/accounts/__tests__/seed.test.ts |

### Wave 2: Verification [SEQUENTIAL]
| Agent | Task |
|-------|------|
| code-auditor | Type check + pattern compliance |
| Bash | npm run build |

## Documentation Updates
- [ ] CHANGELOG.md (after merge to main)

## Success Criteria
- [ ] `npm run type-check` passes
- [ ] `npm run build` succeeds
- [ ] `npx vitest run convex/accounts/__tests__/seed.test.ts` passes

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 10 accounting tables to schema and modify users</name>
  <files>convex/schema.ts</files>
  <behavior>
    - npm run type-check passes with all 10 new tables + users modification
    - journalEntryLines has entryDate (v.number()) field and by_account_entryDate compound index
    - All union types use v.union(v.literal(...)) pattern matching existing externalSource pattern
    - Users table has bankAccountNumber and bankName as v.optional(v.string())
  </behavior>
  <action>
Add 10 new tables to convex/schema.ts BEFORE the closing `});` of defineSchema. Add them in a new section after the GrabFood Menu Simulator section. Use the section header pattern: `// ============================================ // ACCOUNTING & EXPENSE TABLES // ============================================`

**accounts table:**
```
code: v.string()
name: v.string()
type: v.union(v.literal("asset"), v.literal("liability"), v.literal("equity"), v.literal("revenue"), v.literal("cogs"), v.literal("opex"), v.literal("other"))
category: v.string()
isActive: v.boolean()
isSystem: v.boolean()
description: v.optional(v.string())
```
Indexes: by_code ["code"], by_type ["type"], by_active_type ["isActive", "type"]

**expenses table:**
```
expenseNumber: v.string()
submittedBy: v.id("users")
amount: v.number()
accountId: v.id("accounts")
expenseDate: v.number()
description: v.string()
vendorName: v.string()  // PRD: required (validated on submission)
paymentMethod: v.union(v.literal("personal_cash"), v.literal("personal_transfer"), v.literal("company_card"))  // PRD Section 3 line 101
receiptFileId: v.optional(v.id("_storage"))
receiptImageHash: v.optional(v.string())
status: v.union(v.literal("draft"), v.literal("submitted"), v.literal("approved"), v.literal("rejected"), v.literal("awaiting_payment"), v.literal("reimbursed"), v.literal("voided"))  // PRD Section 3 line 104
lateSubmission: v.boolean()  // PRD: required boolean, set on submission (default false for drafts)
duplicateWarning: v.optional(v.string())  // PRD: optional string — stores reference to similar expense ID
submittedAt: v.optional(v.number())  // PRD Section 3 line 107 — needed for late-submission calc and approval time metrics
approvedBy: v.optional(v.id("users"))
approvedAt: v.optional(v.number())
approverComment: v.optional(v.string())
rejectedBy: v.optional(v.id("users"))
rejectedAt: v.optional(v.number())
rejectionReason: v.optional(v.string())
previousExpenseId: v.optional(v.id("expenses"))
// rejectionCount omitted — derivable from previousExpenseId chain via expenseStatusHistory
voidedBy: v.optional(v.id("users"))  // addendum per research: avoids reverse lookup on void
voidedAt: v.optional(v.number())     // addendum per research
voidReason: v.optional(v.string())   // addendum per research
journalEntryId: v.optional(v.id("journalEntries"))  // addendum per research: forward reference avoids reverse lookup
createdAt: v.number()
```
Indexes: by_submitter_status ["submittedBy", "status"], by_status ["status"], by_amount_date_submitter ["amount", "expenseDate", "submittedBy"], by_receipt_hash ["receiptImageHash"], by_expense_number ["expenseNumber"]

**expenseStatusHistory table:**
```
expenseId: v.id("expenses")
fromStatus: v.optional(v.string())  // optional for initial creation (no prior status); PRD says required but first transition has no fromStatus
toStatus: v.string()
changedBy: v.id("users")
changedAt: v.number()
comment: v.optional(v.string())
```
Indexes: by_expense ["expenseId"]

**reimbursementBatches table:**
```
batchNumber: v.string()
employeeUserId: v.id("users")
totalAmount: v.number()
status: v.union(v.literal("pending"), v.literal("confirmed"), v.literal("voided"))
bankAccountId: v.optional(v.id("bankAccounts"))
bankReference: v.optional(v.string())  // PRD Section 3 line 136 — generic name, not bank-specific
transferDate: v.optional(v.number())
confirmedBy: v.optional(v.id("users"))
confirmedAt: v.optional(v.number())
voidedBy: v.optional(v.id("users"))
voidedAt: v.optional(v.number())
voidReason: v.optional(v.string())
journalEntryId: v.optional(v.id("journalEntries"))
createdBy: v.id("users")
createdAt: v.number()
```
Indexes: by_batch_number ["batchNumber"], by_employee_status ["employeeUserId", "status"], by_status ["status"]

**reimbursementBatchItems table:**
```
batchId: v.id("reimbursementBatches")
expenseId: v.id("expenses")
```
Indexes: by_batch ["batchId"], by_expense ["expenseId"]

**journalEntries table:**
```
entryNumber: v.string()
date: v.number()
description: v.string()
sourceType: v.union(v.literal("expense_approval"), v.literal("expense_void"), v.literal("reimbursement"), v.literal("reimbursement_void"), v.literal("payroll"), v.literal("payroll_void"), v.literal("manual"))  // PRD has 6 (no payroll_void); payroll_void added proactively per research; manual from PRD Section 3 line 158
sourceId: v.optional(v.string())
isReversed: v.boolean()
reversedByEntryId: v.optional(v.id("journalEntries"))
createdBy: v.id("users")
createdAt: v.number()
```
Indexes: by_entry_number ["entryNumber"], by_source ["sourceType", "sourceId"], by_date ["date"]

**journalEntryLines table** (JE-04 requirement -- entryDate denormalized from parent):
```
journalEntryId: v.id("journalEntries")
accountId: v.id("accounts")
entryDate: v.number()
debitAmount: v.number()
creditAmount: v.number()
description: v.optional(v.string())
```
Indexes: by_journal_entry ["journalEntryId"], by_account_entryDate ["accountId", "entryDate"], by_entryDate ["entryDate"]  // by_entryDate added per prior staff review (PNL-04) — enables single-index period queries for P&L aggregation

**bankAccounts table:**
```
name: v.string()
bankName: v.string()
accountNumber: v.string()
isActive: v.boolean()
createdBy: v.id("users")   // required — new table, all records will have a creator
createdAt: v.number()      // required — new table, always set on creation
```
Indexes: by_active ["isActive"]

**payrollEntries table** (includes void fields per research recommendation to avoid schema migration in Phase 47):
```
employeeType: v.union(v.literal("contractor"), v.literal("staff"))
frequency: v.union(v.literal("weekly"), v.literal("monthly"))
amount: v.number()
periodStart: v.number()
periodEnd: v.number()
description: v.string()  // PRD Section 3 line 196: required (e.g., "March 2026 — Production Staff")
attachmentFileId: v.optional(v.id("_storage"))
status: v.optional(v.union(v.literal("active"), v.literal("voided")))
voidedBy: v.optional(v.id("users"))
voidedAt: v.optional(v.number())
voidReason: v.optional(v.string())
journalEntryId: v.optional(v.id("journalEntries"))
createdBy: v.id("users")
createdAt: v.number()
```
Indexes: by_period ["periodStart"], by_employee_type ["employeeType"]

**counters table:**
```
prefix: v.string()
date: v.string()
lastSequence: v.number()
```
Indexes: by_prefix_date ["prefix", "date"]

**Users table modification:** Add two optional fields after the existing `createdAt` field:
```
bankAccountNumber: v.optional(v.string())
bankName: v.optional(v.string())
```
Do NOT change existing fields or indexes on users table.

Ensure the schema compiles cleanly. Run `npm run type-check` to verify.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>All 10 new tables and users modification compile cleanly. journalEntryLines has entryDate field with by_account_entryDate index (JE-04). Schema goes from 65 to 75 tables.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Chart of Accounts seed function with tests</name>
  <files>convex/accounts/mutations.ts, convex/accounts/__tests__/seed.test.ts</files>
  <behavior>
    - seedDefaults creates 36 accounts when run on empty database
    - seedDefaults is idempotent: running twice produces same 36 accounts, no duplicates
    - All 36 accounts have isSystem: true and isActive: true
    - Account codes cover: 7 Revenue (4100-4700), 4 COGS (5100-5400), 11 OpEx (6100-6990), 3 Other (7100-7900), 6 Assets (1100-1600), 5 Liabilities (2100-2500), 3 Equity (3100-3300)
    - Seed function has no auth requirement (args: {}) matching productionUnitTypes:seedDefaults pattern
  </behavior>
  <action>
**Create `convex/accounts/mutations.ts`:**

Create the `seedDefaults` mutation following the exact `productionUnitTypes:seedDefaults` pattern from `convex/productionUnitTypes/mutations.ts`:
- `args: {}` (no auth -- seed functions run from Dashboard Functions tab)
- Loop through 36 default accounts
- For each account, query `by_code` index to check existence
- If exists: `ctx.db.patch(existing._id, { ...fields })` to update with current values (upsert pattern for name/type/category corrections). This follows the `productionUnitTypes:seedDefaults` pattern which does a real patch on re-run — NOT skip-if-exists.
- If not exists: `ctx.db.insert("accounts", { ...fields })` to create new record
- Return array of `{ code, action: "created" | "updated", id }` results
- IMPORTANT: The research RESEARCH.md example shows a simplified "skip-if-exists" pattern — do NOT follow that. The plan requires upsert (patch on re-run) matching `productionUnitTypes:seedDefaults`.

The 36 default accounts (all with `isSystem: true`, `isActive: true`):

Revenue (4xxx):
- 4100 "Direct Sales" type:"revenue" category:"Revenue"
- 4200 "GoFood Revenue" type:"revenue" category:"Revenue"
- 4300 "Shopee Revenue" type:"revenue" category:"Revenue"
- 4400 "TikTok Revenue" type:"revenue" category:"Revenue"
- 4500 "K3Mart Revenue" type:"revenue" category:"Revenue"
- 4600 "Consignment Revenue" type:"revenue" category:"Revenue"
- 4700 "GrabFood Revenue" type:"revenue" category:"Revenue"

COGS (5xxx):
- 5100 "Production COGS" type:"cogs" category:"Cost of Goods Sold"
- 5200 "Packaging COGS" type:"cogs" category:"Cost of Goods Sold"
- 5300 "Commissions & Fees" type:"cogs" category:"Cost of Goods Sold"
- 5400 "Platform Ad Burn" type:"cogs" category:"Cost of Goods Sold"

OpEx (6xxx):
- 6100 "Salaries & Wages" type:"opex" category:"Operating Expenses"
- 6200 "Rent & Utilities" type:"opex" category:"Operating Expenses"
- 6300 "Transportation (Local)" type:"opex" category:"Operating Expenses"
- 6350 "Travel & Visa" type:"opex" category:"Operating Expenses"
- 6400 "Marketing & Promotion" type:"opex" category:"Operating Expenses"
- 6500 "Office & Supplies" type:"opex" category:"Operating Expenses"
- 6600 "Equipment & Maintenance" type:"opex" category:"Operating Expenses"
- 6700 "Software & Subscriptions" type:"opex" category:"Operating Expenses"
- 6800 "Professional Services" type:"opex" category:"Operating Expenses"
- 6900 "Meals & Entertainment" type:"opex" category:"Operating Expenses"
- 6990 "Miscellaneous OpEx" type:"opex" category:"Operating Expenses"

Other (7xxx):
- 7100 "Interest Income" type:"other" category:"Other Income/Expense"
- 7200 "Interest Expense" type:"other" category:"Other Income/Expense"
- 7900 "Other Non-Operating" type:"other" category:"Other Income/Expense"

Assets (1xxx):
- 1100 "Cash (Bank Accounts)" type:"asset" category:"Assets"
- 1200 "Accounts Receivable" type:"asset" category:"Assets"
- 1300 "Inventory (Raw Materials)" type:"asset" category:"Assets"
- 1400 "Prepaid Expenses" type:"asset" category:"Assets"
- 1500 "Fixed Assets" type:"asset" category:"Assets"
- 1600 "Accumulated Depreciation" type:"asset" category:"Assets"  // NOTE: contra-asset (credit-normal). Stored as type:"asset" per PRD; balance sheet logic in Phase 48+ MUST subtract this from total assets

Liabilities (2xxx):
- 2100 "Accounts Payable" type:"liability" category:"Liabilities"
- 2200 "Employee Reimbursements Payable" type:"liability" category:"Liabilities"
- 2300 "Accrued Expenses" type:"liability" category:"Liabilities"
- 2400 "Tax Payable" type:"liability" category:"Liabilities"
- 2500 "Loans Payable" type:"liability" category:"Liabilities"

Equity (3xxx):
- 3100 "Owner's Capital" type:"equity" category:"Equity"
- 3200 "Retained Earnings" type:"equity" category:"Equity"
- 3300 "Current Period P&L" type:"equity" category:"Equity"

**Create `convex/accounts/__tests__/seed.test.ts`:**

Use the `convex-test` framework following patterns from existing tests (e.g., `convex/bigsellerOrders/__tests__/mutations.test.ts`). If convex-test is too heavy for pure data validation, use Vitest with extracted constants:

1. Extract the DEFAULT_ACCOUNTS array as a named export from mutations.ts so tests can reference it
2. Test: DEFAULT_ACCOUNTS array has exactly 36 entries
3. Test: All entries have isSystem: true and isActive: true
4. Test: All account codes are unique (no duplicates in the array)
5. Test: Account codes follow PSAK ranges (1xxx Assets, 2xxx Liabilities, 3xxx Equity, 4xxx Revenue, 5xxx COGS, 6xxx OpEx, 7xxx Other)
6. Test: Revenue count is 7, COGS count is 4, OpEx count is 11, Other count is 3, Assets count is 6, Liabilities count is 5, Equity count is 3
7. Test: Verify specific codes exist: "1100", "2200", "4100", "5100", "6100", "7100", "3100"

For the seed function itself, if convex-test can mock ctx.db, add:
8. Test: seedDefaults inserts all 36 on empty DB
9. Test: seedDefaults on second run returns "updated" for all 36 (idempotent)

If convex-test setup is too complex for this phase, tests 1-7 (pure data validation on the exported array) are sufficient. The mutation behavior will be verified manually via Dashboard.
  </action>
  <verify>
    <automated>npx vitest run convex/accounts/__tests__/seed.test.ts -x</automated>
  </verify>
  <done>seedDefaults mutation creates 36 PSAK-aligned accounts (COA-04). All defaults have isSystem: true (COA-05). Tests prove data correctness and idempotency of the account array. Seed can be run from Convex Dashboard Functions tab.</done>
</task>

</tasks>

<verification>
1. `npm run type-check` passes (schema compiles with 10 new tables)
2. `npm run build` succeeds (no frontend breakage from schema changes)
3. `npx vitest run convex/accounts/__tests__/seed.test.ts` passes (seed data validated)
4. Manual: Run `accounts:seedDefaults` from Convex Dashboard Functions tab -- returns 36 "created" entries
5. Manual: Run `accounts:seedDefaults` again -- returns 36 "updated" entries (idempotent)
</verification>

<success_criteria>
- Schema has 75 tables (65 + 10 new) and all type-check cleanly
- Users table has bankAccountNumber and bankName optional fields
- journalEntryLines has entryDate field with by_account_entryDate index (JE-04)
- seedDefaults creates exactly 36 accounts with correct PSAK codes (COA-04)
- All 36 defaults have isSystem: true (COA-05)
- Seed is idempotent on re-run
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/41-schema-seed-counters/41-01-SUMMARY.md`
</output>
