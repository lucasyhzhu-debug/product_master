---
phase: 10-frontend-factories
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/convex/createMutationHook.ts
  - src/hooks/convex/useIngredients.ts
  - src/hooks/convex/useMaterials.ts
  - src/hooks/convex/useTags.ts
  - src/hooks/convex/useCustomers.ts
  - src/hooks/convex/useStorageLocations.ts
  - src/hooks/convex/index.ts
autonomous: true

must_haves:
  truths:
    - "createMutationHook factory produces typed hooks with toast notifications"
    - "All 5 entity hook files use the factory for mutation hooks"
    - "Existing hook consumers (pages, order forms) work without any code changes"
    - "Barrel export (index.ts) maintains identical public API"
  artifacts:
    - path: "src/hooks/convex/createMutationHook.ts"
      provides: "Generic mutation hook factory with toast wrappers"
      exports: ["createMutationHook", "MutationHookConfig"]
      min_lines: 25
    - path: "src/hooks/convex/useIngredients.ts"
      provides: "Ingredient hooks using factory for mutations"
      exports: ["useConvexCreateIngredient", "useConvexUpdateIngredient", "useConvexDeleteIngredient"]
    - path: "src/hooks/convex/useMaterials.ts"
      provides: "Material hooks using factory for mutations"
    - path: "src/hooks/convex/useTags.ts"
      provides: "Tag hooks using factory for mutations + standalone useConvexSeedTags"
    - path: "src/hooks/convex/useCustomers.ts"
      provides: "Customer hooks using factory, transform layer removed"
    - path: "src/hooks/convex/useStorageLocations.ts"
      provides: "Storage location hooks using factory (adds toast wrappers it previously lacked)"
  key_links:
    - from: "src/hooks/convex/useIngredients.ts"
      to: "src/hooks/convex/createMutationHook.ts"
      via: "import createMutationHook"
      pattern: "createMutationHook\\("
    - from: "src/hooks/convex/index.ts"
      to: "all 5 entity hook files"
      via: "barrel re-exports"
      pattern: "export.*useConvex(Create|Update|Delete)"
---

<objective>
Create the `createMutationHook` factory and migrate all 5 target entity hooks to use it (FHOOK-01, FHOOK-02).

Purpose: Eliminate ~400 lines of duplicated try/catch/toast mutation boilerplate across 5 hook files by extracting a shared factory function. Per user decision: explicit configuration pattern (pass mutation ref + toast messages), not schema-driven magic.

Output: 1 new factory file, 5 simplified hook files, barrel export unchanged.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-frontend-factories/10-RESEARCH.md
@src/hooks/convex/useIngredients.ts
@src/hooks/convex/useMaterials.ts
@src/hooks/convex/useTags.ts
@src/hooks/convex/useCustomers.ts
@src/hooks/convex/useStorageLocations.ts
@src/hooks/convex/index.ts
@src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create createMutationHook factory + migrate Ingredients and Materials hooks</name>
  <files>
    src/hooks/convex/createMutationHook.ts
    src/hooks/convex/useIngredients.ts
    src/hooks/convex/useMaterials.ts
  </files>
  <action>
Create `src/hooks/convex/createMutationHook.ts`:

```typescript
import { useSessionMutation } from "convex-helpers/react/sessions";
import type { FunctionReference, FunctionReturnType } from "convex/server";
import { toast } from "sonner";
import { getErrorMessage } from "@/lib/utils";

export interface MutationHookConfig {
  successMessage: string;
  errorMessage: string;
}

export function createMutationHook<
  Mutation extends FunctionReference<"mutation">
>(
  mutationRef: Mutation,
  config: MutationHookConfig,
) {
  return function useMutationWithToast() {
    const mutation = useSessionMutation(mutationRef);
    const execute = async (
      ...args: Parameters<typeof mutation>
    ): Promise<FunctionReturnType<Mutation>> => {
      try {
        const result = await mutation(...args);
        toast.success(config.successMessage);
        return result;
      } catch (error: unknown) {
        toast.error(getErrorMessage(error, config.errorMessage));
        throw error;
      }
    };
    return { mutate: execute, mutateAsync: execute };
  };
}
```

Key design points:
- Uses `Parameters<typeof mutation>` to inherit the exact args that `useSessionMutation` provides (sessionId already stripped). This avoids complex generic type math.
- Returns `{ mutate, mutateAsync }` matching the existing hook return shape exactly.
- Uses `getErrorMessage` from `@/lib/utils` (already used by all existing hooks).

Migrate `useIngredients.ts`:
- Keep all query hooks unchanged (useConvexIngredients, useConvexIngredient, useConvexIngredientSearch).
- Keep the `ConvexIngredient` and `IngredientCreateInput` type exports for backward compatibility.
- Replace the 3 mutation hook functions (useConvexCreateIngredient, useConvexUpdateIngredient, useConvexDeleteIngredient) with `createMutationHook` calls using `export const`.
- Preserve exact same toast messages as before.

Migrate `useMaterials.ts`:
- Identical pattern to ingredients. Keep query hooks and type exports.
- Replace 3 mutation hooks with factory calls.
- Preserve exact same toast messages.

IMPORTANT: The factory-generated hooks use `export const` (not `export function`). This is fine because the barrel file exports by name, not by declaration type. The hooks are called at component scope, not module scope -- the factory creates a new hook function each time at module load.
  </action>
  <verify>
Run `npm run type-check` -- should pass with no errors related to hook types.
Grep for `useSessionMutation` in `useIngredients.ts` and `useMaterials.ts` -- should only appear as import in `createMutationHook.ts`, NOT in the migrated files.
Verify barrel file `index.ts` still exports all the same names (no changes needed to index.ts for this task).
  </verify>
  <done>
`createMutationHook.ts` exists and exports the factory. `useIngredients.ts` and `useMaterials.ts` each use the factory for their 3 mutation hooks. Both files are ~30-40 lines (down from ~115). `npm run type-check` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate Tags, Customers, StorageLocations hooks + update barrel export</name>
  <files>
    src/hooks/convex/useTags.ts
    src/hooks/convex/useCustomers.ts
    src/hooks/convex/useStorageLocations.ts
    src/hooks/convex/index.ts
  </files>
  <action>
Migrate `useTags.ts`:
- Keep query hooks unchanged (useConvexTags, useConvexTag, useConvexTagsMany).
- Keep `ConvexTag` and `TagCreateInput` type exports.
- Replace 3 mutation hooks with factory calls.
- Keep `useConvexSeedTags` as a standalone hook (uses `useMutation` not `useSessionMutation`, does NOT fit the factory pattern -- it's a public mutation).
- Preserve exact toast messages.

Migrate `useCustomers.ts`:
- REMOVE the `transformCustomer` function and the legacy `Customer` type import. The transform converts Convex `_id` to numeric `id` which is a legacy artifact.
- REMOVE `import type { Customer } from "@/lib/types"`.
- Rewrite query hooks to return raw Convex data directly (like all other hooks do):
  - `useConvexCustomers(limit?)` returns `useQuery(api.customers.queries.list, { limit })` directly (no transform wrapper, no `{ data, isLoading }` shape).
  - `useConvexCustomer(id?)` returns `useQuery(...)` directly.
  - `useConvexCustomerSearch(query, limit?)` returns `useQuery(...)` directly.
  - `useConvexCustomerByPhone(phone?)` returns `useQuery(...)` directly.
- Replace 3 mutation hooks with factory calls.
- Export `CustomerCreateInput` and `CustomerUpdateInput` types for backward compatibility.

CRITICAL: The order form components (`OrderForm.tsx`, `OrderFormPOS.tsx`) use `useConvexCustomerSearch` and access `.data` property and `customer.id` (numeric, from transform). After removing the transform:
- The hook will return the raw array (or undefined) instead of `{ data, isLoading }`.
- Customer objects will have `_id` (string) not `id` (number).
- Update BOTH `src/components/orders/OrderForm.tsx` and `src/components/orders/OrderFormPOS.tsx`:
  - Change `const { data: customers } = useConvexCustomerSearch(...)` to `const customers = useConvexCustomerSearch(...)`.
  - Change `customer.id` to `customer._id` in `key` props.
  - Update `handleCustomerSelect` to use `customer._id` directly (remove the `_id ?? id` fallback).
  - Update the type annotation: `(customer: { _id: string; name: string; phone?: string | null })`.

Migrate `useStorageLocations.ts`:
- Keep query hooks unchanged (useConvexStorageLocations, useConvexStorageLocation, useConvexDefaultLocation).
- Keep type exports (StorageLocationCreateInput, StorageLocationUpdateInput, StorageLocation).
- Replace 3 mutation hooks (which currently return raw `useSessionMutation` without toast wrappers) with factory calls. This ADDS toast notifications that the hooks previously lacked.
- NOTE: LocationsManager.tsx currently calls these mutations directly (no `.mutate()` wrapper) AND has its own inline toast calls. The page migration in Plan 03 will handle removing those inline toasts. For now, the hook migration means LocationsManager.tsx will need minor updates to call `.mutate()` instead of calling the raw function. Update LocationsManager.tsx:
  - Change `await createLocation({...})` to `await createLocation.mutate({...})`
  - Change `await updateLocation({...})` to `await updateLocation.mutate({...})`
  - Change `await deleteLocation({...})` to `await deleteLocation.mutate({...})`
  - Remove ALL inline `toast.success(...)` and `toast.error(...)` calls in LocationsManager since the factory now handles toasts.
  - Remove the `import { toast } from "sonner"` line if it becomes unused.

Update barrel export `index.ts`:
- Add `createMutationHook` and `MutationHookConfig` exports from `./createMutationHook`.
- No other changes needed -- all hook export names remain identical.
  </action>
  <verify>
Run `npm run build` -- must pass completely (not just type-check, full build). This validates:
1. All barrel exports resolve correctly.
2. Order form components work with updated customer hook return types.
3. LocationsManager works with `.mutate()` call pattern.
4. No double-toast issues (factory toasts only, inline toasts removed).

Grep verification:
- `useSessionMutation` should only appear in `createMutationHook.ts` and hooks that don't fit the factory (useConvexSeedTags, complex hooks in other files).
- `transformCustomer` should not appear anywhere.
- `toast.success` and `toast.error` should not appear in `LocationsManager.tsx`.
  </verify>
  <done>
All 5 hook files (ingredients, materials, tags, customers, storageLocations) use `createMutationHook` for their CRUD mutations. Customer transform layer removed; order forms updated to use raw Convex types. StorageLocations hooks now have toast notifications via factory. LocationsManager inline toasts removed. Barrel export updated with factory re-export. `npm run build` passes.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors.
2. Each of the 5 hook files uses `createMutationHook` for create/update/delete mutations.
3. `createMutationHook.ts` exists in `src/hooks/convex/`.
4. Barrel export `index.ts` exports `createMutationHook` and all existing hook names.
5. No references to `transformCustomer` remain.
6. `useSessionMutation` only appears in `createMutationHook.ts` and non-target hooks.
7. Each migrated hook file is ~25-45 lines (down from 100-170).
</verification>

<success_criteria>
- FHOOK-01: `createMutationHook` factory exists and produces typed hooks with toast wrappers.
- FHOOK-02: All 5 target entity hooks use the factory. Each hook file shrinks from ~100-170 lines to ~25-45 lines.
- Zero regressions: existing consumers (IngredientsManager, MaterialsManager, LocationsManager, OrderForm, OrderFormPOS) work without behavioral changes.
- `npm run build` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/10-frontend-factories/10-01-SUMMARY.md`
</output>
