---
phase: 19-gofood-depot-kitchen-targets
plan: 05
type: execute
wave: 3
depends_on: ["19-02", "19-03"]
files_modified:
  - src/pages/RestockPlanner.tsx
  - src/components/restockPlanner/GoFoodRestockSection.tsx
autonomous: true
requirements:
  - GF-04

must_haves:
  truths:
    - "Dispatch Planner page shows a GoFood depot restock section with per-outlet per-product restock suggestions"
    - "Restock data matches the same numbers shown on the GoFood depot cockpit table"
    - "GoFood restock section is read-only on the Dispatch Planner page (view only, no mutations)"
  artifacts:
    - path: "src/pages/RestockPlanner.tsx"
      provides: "Dispatch Planner page extended with GoFood restock section"
    - path: "src/components/restockPlanner/GoFoodRestockSection.tsx"
      provides: "GoFood restock data display component for Dispatch Planner"
      min_lines: 40
  key_links:
    - from: "src/components/restockPlanner/GoFoodRestockSection.tsx"
      to: "convex/gofoodDepot/queries.ts"
      via: "useQuery for getRestockSuggestions per outlet"
      pattern: "useQuery.*getRestockSuggestions"
---

<objective>
Extend the Dispatch Planner page to show GoFood depot restock suggestions.

Purpose: The context requires "extend existing Dispatch Planner page to show GoFood depot restock data" as an additional Phase 19 deliverable. This gives dispatch planners a single view of what needs to go where.

Output: New GoFoodRestockSection component added to RestockPlanner page, showing per-outlet restock suggestions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-gofood-depot-kitchen-targets/19-02-SUMMARY.md
@.planning/phases/19-gofood-depot-kitchen-targets/19-03-SUMMARY.md

@src/pages/RestockPlanner.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: GoFood restock section on Dispatch Planner page</name>
  <files>src/pages/RestockPlanner.tsx, src/components/restockPlanner/GoFoodRestockSection.tsx</files>
  <action>
  **A. Create `src/components/restockPlanner/GoFoodRestockSection.tsx`:**
  Read-only section showing GoFood depot restock suggestions:

  - Query all gobiz `externalOutlets`
  - For each outlet, call `getRestockSuggestions` query (from plan 02)
  - Display as a section with outlet name heading + table:
    | Product | Current Stock | Restock Tomorrow | Breakdown |
    |---------|---------------|------------------|-----------|
    | Singles | 12 | 9 | 3-day avg: 7.3 -> +1 (weekday) = 9 |

  - Show current stock from `productInventory` at the outlet's `linkedStorageLocationId`
  - "Breakdown" column shows the calculation detail (same as tooltip on depot page, but here displayed inline since this is a planning view)
  - If no sales data exists for an outlet, show "No sales data" message
  - Section header: "GoFood Depot Restock" with a truck icon (Lucide: Truck)
  - Collapsible section (default expanded)

  **B. Update `src/pages/RestockPlanner.tsx`:**
  - Import and render `GoFoodRestockSection` component
  - Place it as a new section on the page (below or above existing content, follow existing page structure)
  - The section is purely additive -- do NOT modify existing Dispatch Planner logic or layout
  - Wrap in a conditional: only render if gobiz outlets exist (they always do in this project, but defensive coding)
  </action>
  <verify>Run `npm run type-check` and `npm run build`. Navigate to the Dispatch Planner page -- should see a new "GoFood Depot Restock" section with per-outlet suggestions.</verify>
  <done>Dispatch Planner page shows GoFood depot restock suggestions per outlet with current stock, restock amounts, and calculation breakdown. Data matches depot cockpit table numbers.</done>
</task>

</tasks>

<verification>
- `npm run type-check` passes
- `npm run build` passes
- Dispatch Planner page renders GoFood restock section
- Restock numbers match the depot page cockpit table
- Section is read-only (no mutations)
- Existing Dispatch Planner functionality unchanged
</verification>

<success_criteria>
- GoFood depot restock data visible on Dispatch Planner page
- Per-outlet per-product restock suggestions shown with calculation breakdown
- Consistent numbers between depot page and Dispatch Planner
- Existing Dispatch Planner unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/19-gofood-depot-kitchen-targets/19-05-SUMMARY.md`
</output>
