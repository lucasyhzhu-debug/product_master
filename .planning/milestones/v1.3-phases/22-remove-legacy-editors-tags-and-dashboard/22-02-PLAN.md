---
phase: 22-remove-legacy-editors-tags-and-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/pages/Dashboard.tsx
  - src/pages/RecipeEditor.tsx
  - src/pages/PackagingEditor.tsx
  - src/pages/ProductEditor.tsx
  - src/pages/TagsManager.tsx
  - src/pages/MaterialsManager.tsx
  - src/pages/PackagingComponentsManager.tsx
  - src/pages/index.ts
  - src/hooks/convex/useRecipes.ts
  - src/hooks/convex/usePackaging.ts
  - src/hooks/convex/useProducts.ts
  - src/hooks/convex/useTags.ts
  - src/hooks/convex/useDashboard.ts
  - src/hooks/convex/useMaterials.ts
  - src/hooks/convex/index.ts
  - src/components/dashboard/index.ts
  - src/components/dashboard/LowStockAlert.tsx
  - src/components/dashboard/OrderStatsCards.tsx
  - src/components/dashboard/ProductionQueueTable.tsx
  - src/components/dashboard/SalesWidget.tsx
  - src/components/dashboard/SyncHealthBanner.tsx
  - src/components/recipes/RecipeCard.tsx
  - src/components/packaging/PackagingCard.tsx
  - src/components/products/ProductCard.tsx
  - src/components/ingredients/IngredientCard.tsx
  - src/components/materials/MaterialCard.tsx
  - src/components/shared/TagFilterBar.tsx
  - src/components/shared/Carousel.tsx
  - src/components/shared/index.ts
  - src/components/onboarding/index.ts
  - src/components/onboarding/OnboardingTour.tsx
  - src/components/onboarding/tour-steps.ts
  - src/hooks/useOnboardingTour.ts
  - src/App.tsx
  - src/lib/types.ts
autonomous: true
requirements:
  - SC-1
  - SC-3
  - SC-5
  - SC-7

must_haves:
  truths:
    - "Legacy editor pages (Recipe, Packaging, Product, Tags, Materials) are deleted"
    - "Dashboard.tsx is deleted"
    - "All orphaned hooks, components, and barrel exports are cleaned"
    - "Routes for deleted pages are removed from App.tsx"
    - "Permissions canAccessRecipes, canAccessProducts, canAccessMaterials removed from types.ts"
    - "npm run type-check passes with no dead references"
  artifacts:
    - path: "src/pages/index.ts"
      provides: "Barrel export without legacy pages"
    - path: "src/hooks/convex/index.ts"
      provides: "Barrel export without legacy hooks"
    - path: "src/App.tsx"
      provides: "Routes without legacy editor pages"
    - path: "src/lib/types.ts"
      provides: "ROLE_PERMISSIONS without canAccessRecipes/Products/Materials"
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/index.ts"
      via: "import"
      pattern: "from.*@/pages"
    - from: "src/hooks/convex/index.ts"
      to: "hook files"
      via: "barrel export"
      pattern: "export.*from"
---

<objective>
Delete all frontend dead code: 7 page files, 6 hook files, 12+ component files, clean barrel exports, remove legacy routes and permissions.

Purpose: Remove all frontend references to the legacy recipe/packaging/product/tags/dashboard system. Depends on Plan 01 (backend removal) completing first, since hooks reference backend queries that are being deleted.
Output: Clean frontend with no references to removed backend modules; App.tsx has no legacy routes; barrel exports are clean.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-remove-legacy-editors-tags-and-dashboard/22-01-SUMMARY.md
@src/App.tsx
@src/pages/index.ts
@src/hooks/convex/index.ts
@src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy page files, hook files, and orphaned components</name>
  <files>
    src/pages/Dashboard.tsx
    src/pages/RecipeEditor.tsx
    src/pages/PackagingEditor.tsx
    src/pages/ProductEditor.tsx
    src/pages/TagsManager.tsx
    src/pages/MaterialsManager.tsx
    src/pages/PackagingComponentsManager.tsx
    src/hooks/convex/useRecipes.ts
    src/hooks/convex/usePackaging.ts
    src/hooks/convex/useProducts.ts
    src/hooks/convex/useTags.ts
    src/hooks/convex/useDashboard.ts
    src/hooks/convex/useMaterials.ts
    src/components/dashboard/index.ts
    src/components/dashboard/LowStockAlert.tsx
    src/components/dashboard/OrderStatsCards.tsx
    src/components/dashboard/ProductionQueueTable.tsx
    src/components/dashboard/SalesWidget.tsx
    src/components/dashboard/SyncHealthBanner.tsx
    src/components/recipes/RecipeCard.tsx
    src/components/packaging/PackagingCard.tsx
    src/components/products/ProductCard.tsx
    src/components/ingredients/IngredientCard.tsx
    src/components/materials/MaterialCard.tsx
    src/components/shared/TagFilterBar.tsx
    src/components/shared/Carousel.tsx
    src/components/onboarding/index.ts
    src/components/onboarding/OnboardingTour.tsx
    src/components/onboarding/tour-steps.ts
    src/hooks/useOnboardingTour.ts
  </files>
  <action>
    DELETE these files (grouped by category):

    **Pages (7 files):**
    - `src/pages/Dashboard.tsx` — replaced by hub page in Plan 03
    - `src/pages/RecipeEditor.tsx` — legacy recipe editor
    - `src/pages/PackagingEditor.tsx` — legacy packaging editor (NOT PackagingView.tsx — that's operational)
    - `src/pages/ProductEditor.tsx` — legacy product editor
    - `src/pages/TagsManager.tsx` — tags system
    - `src/pages/MaterialsManager.tsx` — packaging materials manager (no active references after packagingComponentMaterials dropped)
    - `src/pages/PackagingComponentsManager.tsx` — already dead (not routed in App.tsx, not exported in pages/index.ts)

    **Hooks (6 files):**
    - `src/hooks/convex/useRecipes.ts`
    - `src/hooks/convex/usePackaging.ts`
    - `src/hooks/convex/useProducts.ts`
    - `src/hooks/convex/useTags.ts`
    - `src/hooks/convex/useDashboard.ts`
    - `src/hooks/convex/useMaterials.ts`

    **Dashboard components (entire directory — 6 files):**
    - `src/components/dashboard/` — delete the entire directory (index.ts, LowStockAlert.tsx, OrderStatsCards.tsx, ProductionQueueTable.tsx, SalesWidget.tsx, SyncHealthBanner.tsx). Note: `LowStockAlertsBanner` in `src/components/inventory/` is a SEPARATE component and must NOT be deleted.

    **Card components (5 files):**
    - `src/components/recipes/RecipeCard.tsx` — only used in Dashboard.tsx
    - `src/components/packaging/PackagingCard.tsx` — only used in Dashboard.tsx
    - `src/components/products/ProductCard.tsx` — only used in Dashboard.tsx (the `ProductCard` in StickeringPanel.tsx is a local component, not this file)
    - `src/components/ingredients/IngredientCard.tsx` — only used in Dashboard.tsx
    - `src/components/materials/MaterialCard.tsx` — only used in Dashboard.tsx

    **Shared components (2 files):**
    - `src/components/shared/TagFilterBar.tsx` — only used in Dashboard.tsx
    - `src/components/shared/Carousel.tsx` — only used in Dashboard.tsx (LoadingCards in LoadingState.tsx is separate)

    **Onboarding (4 files):**
    - `src/components/onboarding/OnboardingTour.tsx` — only used in Dashboard.tsx
    - `src/components/onboarding/tour-steps.ts` — only used by OnboardingTour
    - `src/components/onboarding/index.ts` — barrel export for onboarding
    - `src/hooks/useOnboardingTour.ts` — only used in Dashboard.tsx

    After deleting, check if these directories are now empty and can be deleted:
    - `src/components/dashboard/` — should be empty, delete directory
    - `src/components/onboarding/` — should be empty, delete directory
    - `src/components/recipes/` — check if RecipeCard.tsx was the only file; if empty, delete directory
    - `src/components/packaging/` — check if PackagingCard.tsx was the only file; if empty, delete directory
    - `src/components/products/` — check if ProductCard.tsx was the only file; if empty, delete directory
    - `src/components/materials/` — check if MaterialCard.tsx was the only file; if empty, delete directory

    DO NOT delete:
    - `src/pages/IngredientsManager.tsx` — KEEP (ingredients used by production components/COGS)
    - `src/pages/PackagingView.tsx` — KEEP (operational order-packaging view for kitchen staff)
    - `src/components/shared/LoadingState.tsx` — KEEP (exports LoadingCards used by other pages)
    - `src/components/inventory/LowStockAlertsBanner.tsx` — KEEP (separate from dashboard LowStockAlert)
  </action>
  <verify>
    Confirm all listed files are deleted.
    Confirm `src/components/dashboard/` directory is deleted.
    Run `grep -r "useRecipes\|usePackaging\|useProducts\|useTags\|useDashboard\|useMaterials" src/hooks/convex/ --include="*.ts" -l` — should return only `index.ts` (cleaned in Task 2).
  </verify>
  <done>All 30+ legacy files deleted; orphaned directories cleaned</done>
</task>

<task type="auto">
  <name>Task 2: Clean barrel exports, remove legacy routes, and strip dead permissions</name>
  <files>
    src/pages/index.ts
    src/hooks/convex/index.ts
    src/components/shared/index.ts
    src/App.tsx
    src/lib/types.ts
  </files>
  <action>
    1. **Update `src/pages/index.ts`:**
       Remove exports for:
       - `Dashboard`
       - `RecipeEditor`
       - `PackagingEditor`
       - `ProductEditor`
       - `TagsManager`
       - `MaterialsManager`
       Keep all other exports (OrderManager, KitchenViewV2, IngredientsManager, PackagingView, etc.)

    2. **Update `src/hooks/convex/index.ts`:**
       Remove the entire export blocks for:
       - `// Tags` section (useConvexTags, etc. from "./useTags")
       - `// Recipes` section (useConvexRecipes, etc. from "./useRecipes")
       - `// Packaging` section (useConvexPackagingList, etc. from "./usePackaging")
       - `// Products` section (useConvexProducts, etc. from "./useProducts")
       - `// Dashboard` section (useConvexDashboardSummary, etc. from "./useDashboard")
       - `// Packaging Materials` section (useConvexMaterials, etc. from "./useMaterials")
       Keep all other export blocks (Ingredients, Orders, Kitchen, Inventory, etc.)

    3. **Update `src/components/shared/index.ts`:**
       Remove exports for:
       - `Carousel` (from './Carousel')
       - `TagFilterBar` (from './TagFilterBar') if exported
       Keep all other shared component exports (LoadingCards, ConfirmDialog, etc.)

    4. **Update `src/App.tsx`:**
       - Remove imports for: `RecipeEditor`, `PackagingEditor`, `ProductEditor`, `TagsManager`, `MaterialsManager` from `@/pages`
       - Remove these route definitions:
         - `/recipes/:id` → RecipeEditor
         - `/packaging/:id` → PackagingEditor
         - `/products/:id` → ProductEditor
         - `/tags` → TagsManager
         - `/materials` → MaterialsManager
       - Keep the `RoleBasedRedirect` component for now (will be updated in Plan 03 for hub page)
       - Keep all other routes (kitchen, orders, packaging, ingredients, menu-products, etc.)

    5. **Update `src/lib/types.ts`:**
       - Remove `canAccessRecipes: boolean` from `RolePermissions` interface
       - Remove `canAccessProducts: boolean` from `RolePermissions` interface
       - Remove `canAccessMaterials: boolean` from `RolePermissions` interface
       - Remove the corresponding entries from ALL role objects in `ROLE_PERMISSIONS`:
         - kitchen: remove `canAccessRecipes: false`, `canAccessProducts: false`, `canAccessMaterials: false`
         - order_staff: remove same three
         - manager: remove same three
         - admin: remove same three
       - KEEP `canAccessIngredients` — IngredientsManager is still active
       - KEEP `canAccessPackaging` — PackagingView is still active

    6. **Verify no remaining imports reference deleted files:**
       Run grep across `src/` for imports from deleted modules. Fix any discovered broken imports.
  </action>
  <verify>
    Run `npm run type-check` — must pass with no errors.
    Run `grep -r "RecipeEditor\|PackagingEditor\|ProductEditor\|TagsManager\|MaterialsManager\|Dashboard" src/App.tsx` — should return zero matches (except comments if any).
    Run `grep -r "canAccessRecipes\|canAccessProducts\|canAccessMaterials" src/ --include="*.ts" --include="*.tsx"` — should return zero matches.
  </verify>
  <done>All barrel exports clean; App.tsx has no legacy routes; types.ts has no dead permissions; npm run type-check passes</done>
</task>

</tasks>

<verification>
- `npm run type-check` passes
- `npm run build` succeeds
- No file in `src/` imports from deleted hook or page files
- `src/pages/index.ts` does not export Dashboard, RecipeEditor, PackagingEditor, ProductEditor, TagsManager, or MaterialsManager
- `src/hooks/convex/index.ts` does not export from useRecipes, usePackaging, useProducts, useTags, useDashboard, or useMaterials
- `canAccessRecipes`, `canAccessProducts`, `canAccessMaterials` do not appear in any .ts/.tsx file
</verification>

<success_criteria>
- 30+ legacy frontend files deleted
- Barrel exports (pages/index.ts, hooks/convex/index.ts, shared/index.ts) cleaned
- 5 legacy routes removed from App.tsx
- 3 dead permission keys removed from types.ts ROLE_PERMISSIONS
- `npm run type-check` passes
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/22-remove-legacy-editors-tags-and-dashboard/22-02-SUMMARY.md`
</output>
