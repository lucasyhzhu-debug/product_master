---
phase: 25-codebase-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/convex/useMenuProductComponents.ts
  - src/hooks/convex/useSalesAnalytics.ts
  - src/hooks/convex/useProductionUnitTypes.ts
  - src/hooks/convex/useWhatsAppTemplates.ts
  - src/hooks/convex/useStorageLocations.ts
  - src/hooks/convex/useCustomers.ts
  - src/hooks/convex/useKitchenStats.ts
  - src/hooks/convex/index.ts
autonomous: true
requirements:
  - CLEANUP-HOOK-RENAME

must_haves:
  truths:
    - "All hooks in Batches 1-3 (useMenuProductComponents, useSalesAnalytics, useProductionUnitTypes, useWhatsAppTemplates, useStorageLocations, useCustomers, useKitchenStats) have useConvex prefix removed from exported function names"
    - "All import sites for these 7 hook files are updated to use the new unprefixed names"
    - "The barrel export index.ts exports the new unprefixed names"
    - "npm run type-check passes after each batch"
  artifacts:
    - path: "src/hooks/convex/index.ts"
      provides: "Barrel export with all renamed hook names"
      contains: "useStorageLocations"
  key_links:
    - from: "src/hooks/convex/useCustomers.ts"
      to: "src/pages/CustomersManager.tsx"
      via: "renamed imports"
      pattern: "useCustomers|useCreateCustomer|useUpdateCustomer"
---

<objective>
Remove the useConvex prefix from hooks in 7 files (Batches 1-3: smallest-to-medium hooks). Rename exported function names, update barrel export, and update all import sites across pages and components.

Purpose: The useConvex prefix is redundant (hooks already live in src/hooks/convex/). Removing it reduces verbosity at all call sites and aligns with hooks in other files (useVouchers, useGoFoodDepot, etc.) that already lack the prefix.

Output: 7 hook files and index.ts updated. All consumer files updated. type-check passes after each batch.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-codebase-cleanup/25-RESEARCH.md

## Hook Rename Rules

Batch breakdown (from RESEARCH.md):
- Batch 1: useMenuProductComponents (2 hooks) + useSalesAnalytics (3) + useProductionUnitTypes (3) + useWhatsAppTemplates (5) = 13 renames
- Batch 2: useStorageLocations (6) + useCustomers (7) = 13 renames
- Batch 3: useKitchenStats (8) = 8 renames

Process per batch:
1. Read the hook file
2. Rename all useConvex-prefixed exports (e.g., useConvexStorageLocations → useStorageLocations)
3. Update src/hooks/convex/index.ts barrel export for these hooks
4. Grep for old names in src/ to find all consumer files
5. Update all consumer files
6. Run npm run type-check — MUST pass before moving to next batch

Collision audit (from RESEARCH.md): CLEAN. No useOrders, useCustomers, etc. defined outside src/hooks/convex/.

Files to keep as-is (already clean, NOT in this plan):
- useVouchers.ts (0 prefixed), useGoFoodDepot.ts (0 prefixed), useProductInventory.ts (0 prefixed)
- useKitchenProduction.ts (0 prefixed), useKitchenTargets.ts (0 prefixed)
- useDispatchPlanner.ts (0 prefixed), useProductionRecipes.ts (0 prefixed)
- useIngredients.ts (PARTIAL — handled in plan 25-03 with useOrders.ts batch)
- useOrders.ts (PARTIAL — handled in plan 25-03)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename hooks in Batch 1 (useMenuProductComponents + useSalesAnalytics + useProductionUnitTypes + useWhatsAppTemplates)</name>
  <files>
    src/hooks/convex/useMenuProductComponents.ts
    src/hooks/convex/useSalesAnalytics.ts
    src/hooks/convex/useProductionUnitTypes.ts
    src/hooks/convex/useWhatsAppTemplates.ts
    src/hooks/convex/index.ts
  </files>
  <action>
Step 1 — Read each of the 4 hook files. Identify every exported function with the useConvex prefix. Rename by stripping useConvex:
- useConvexMenuProductComponents → useMenuProductComponents (and all sub-hooks in that file)
- useConvexSalesAnalytics → useSalesAnalytics (3 hooks)
- useConvexProductionUnitTypes → useProductionUnitTypes (and any sub-hooks)
- useConvexWhatsAppTemplates → useWhatsAppTemplates (and sub-hooks like useConvexWhatsAppTemplatesMutations → useWhatsAppTemplatesMutations)

Step 2 — Update src/hooks/convex/index.ts: find all re-exports of the old useConvex names from these 4 files; update to export the new names.

Step 3 — Find all consumer files:
Run grep for each old name across src/:
- grep -r "useConvexMenuProductComponents\|useConvexSalesAnalytics\|useConvexProductionUnitTypes\|useConvexWhatsAppTemplates" src/ --include="*.tsx" --include="*.ts" -l
Update each consumer file to import and use the new unprefixed names.

Step 4 — npm run type-check. Fix any errors before proceeding.
  </action>
  <verify>
1. grep -r "useConvexMenuProductComponents\|useConvexSalesAnalytics\|useConvexProductionUnitTypes\|useConvexWhatsAppTemplates" src/ --include="*.tsx" --include="*.ts"
   Expect: zero matches
2. npm run type-check — must exit 0
  </verify>
  <done>13 hooks renamed across 4 files. index.ts updated. All consumer imports updated. type-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rename hooks in Batch 2 (useStorageLocations + useCustomers) and Batch 3 (useKitchenStats)</name>
  <files>
    src/hooks/convex/useStorageLocations.ts
    src/hooks/convex/useCustomers.ts
    src/hooks/convex/useKitchenStats.ts
    src/hooks/convex/index.ts
  </files>
  <action>
Process Batch 2 first, then Batch 3. For each batch: rename → update index.ts → update consumers → type-check.

BATCH 2 — useStorageLocations (6 hooks) + useCustomers (7 hooks):
Step 1 — Read useStorageLocations.ts. Rename all useConvex-prefixed exports (e.g., useConvexStorageLocations → useStorageLocations, useConvexCreateStorageLocation → useCreateStorageLocation, etc.).
Step 2 — Read useCustomers.ts. Rename all 7 useConvex-prefixed exports (e.g., useConvexCustomers → useCustomers, useConvexCreateCustomer → useCreateCustomer, etc.).
Step 3 — Update src/hooks/convex/index.ts for both files.
Step 4 — Find and update all consumer files:
grep -r "useConvexStorageLocations\|useConvexCreateStorageLocation\|useConvexCustomers\|useConvexCreateCustomer" src/ --include="*.tsx" --include="*.ts" -l
Update each file.
Step 5 — npm run type-check. Fix errors.

BATCH 3 — useKitchenStats (8 hooks):
Step 1 — Read useKitchenStats.ts. Rename all 8 useConvex-prefixed exports.
Step 2 — Update src/hooks/convex/index.ts.
Step 3 — Find and update consumer files:
grep -r "useConvexKitchen" src/ --include="*.tsx" --include="*.ts" -l
(Note: useKitchenStats is the file name but hook names may be useConvexKitchenStats, useConvexSubmitShift, useConvexUpdateShift, etc. — read the file first to see exact names)
Update each consumer.
Step 4 — npm run type-check. Fix errors.
  </action>
  <verify>
1. grep -r "useConvexStorageLocations\|useConvexCustomers\|useConvexKitchenStats\|useConvexSubmitShift\|useConvexUpdateShift" src/ --include="*.tsx" --include="*.ts"
   Expect: zero matches
2. npm run type-check — must exit 0
  </verify>
  <done>21 hooks renamed across 3 files (6 + 7 + 8). index.ts updated. All consumers updated. type-check passes after each sub-batch.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. npm run type-check — must pass with zero errors
2. Confirm old names gone from 7 hook files:
   grep -r "useConvexMenuProductComponents\|useConvexSalesAnalytics\|useConvexProductionUnitTypes\|useConvexWhatsAppTemplates\|useConvexStorageLocations\|useConvexCustomers\|useConvexKitchenStats" src/ --include="*.tsx" --include="*.ts"
   Expect: zero matches
3. Confirm new names are exported from index.ts:
   grep "useMenuProductComponents\|useSalesAnalytics\|useProductionUnitTypes\|useWhatsAppTemplates\|useStorageLocations\|useCustomers\|useKitchenStats" src/hooks/convex/index.ts
   Expect: all new names present
</verification>

<success_criteria>
- 34 hooks renamed across 7 hook files (Batches 1-3)
- index.ts exports all new unprefixed names
- Zero occurrences of old useConvex-prefixed names for these 7 files in entire src/
- npm run type-check passes
</success_criteria>

<output>
After completion, create .planning/phases/25-codebase-cleanup/25-02-SUMMARY.md with:
- Complete list of renamed hooks (old name → new name)
- Consumer files updated per batch
- Confirmation that type-check passed after each batch
</output>
