---
phase: 25-codebase-cleanup
plan: 04
type: execute
wave: 2
depends_on: ["25-02"]
files_modified:
  - src/hooks/convex/useComponentTypes.ts
  - src/hooks/convex/useInventory.ts
  - src/hooks/convex/useFeedback.ts
  - src/hooks/convex/useMenuProducts.ts
  - src/hooks/convex/useIngredients.ts
  - src/hooks/convex/useOrders.ts
  - src/hooks/convex/useExternalData.ts
  - src/hooks/convex/useK3MartCockpit.ts
  - src/hooks/convex/index.ts
  - src/hooks/__tests__/useConvexHooks.test.tsx
autonomous: true
requirements:
  - CLEANUP-HOOK-RENAME

must_haves:
  truths:
    - "All remaining useConvex-prefixed hooks in Batches 4-5 (useComponentTypes, useInventory, useFeedback, useMenuProducts, useIngredients partial, useOrders partial, useExternalData, useK3MartCockpit) are renamed to unprefixed versions"
    - "The test file src/hooks/__tests__/useConvexHooks.test.tsx is updated to use new unprefixed names"
    - "Zero occurrences of any useConvex-prefixed hook name remain anywhere in src/"
    - "npm run type-check passes"
    - "npm run test passes (test file updated to match renamed hooks)"
  artifacts:
    - path: "src/hooks/convex/useOrders.ts"
      provides: "Orders hooks with clean unprefixed names"
      contains: "export function useOrders"
    - path: "src/hooks/__tests__/useConvexHooks.test.tsx"
      provides: "Updated test file with new hook names"
  key_links:
    - from: "src/hooks/convex/index.ts"
      to: "all consumer pages"
      via: "barrel export of all renamed hooks"
      pattern: "export.*useOrders|useInventory|useMenuProducts"
---

<objective>
Complete the hook rename by processing the larger remaining batches (Batches 4-5: 88 renames across useComponentTypes, useInventory, useFeedback, useMenuProducts, partial useIngredients, partial useOrders, useExternalData, useK3MartCockpit). Also update the test file.

Purpose: Finishes the hook rename started in plan 25-02. Must run after 25-02 to avoid index.ts merge conflicts. After this plan, no useConvex-prefixed hook names exist anywhere in the codebase.

Output: 8 more hook files renamed, index.ts fully updated, test file updated. Zero useConvex prefix names remain in src/.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-codebase-cleanup/25-RESEARCH.md
@.planning/phases/25-codebase-cleanup/25-02-SUMMARY.md

## Batch breakdown for this plan

Batch 4 (41 renames):
- useComponentTypes.ts — 10 hooks (all prefixed)
- useIngredients.ts — 6 of 8 hooks prefixed (keep useAuth-adjacent ones, rename useConvex-prefixed only)
- useOrders.ts — 24 of 27 hooks prefixed (the 3 already-clean hooks: useKanbanOrders, useCreateDraft, useUpdateDraft — leave as-is)

Batch 5 (46 renames):
- useInventory.ts — 13 hooks (all prefixed)
- useFeedback.ts — 12 hooks (all prefixed)
- useMenuProducts.ts — 16 hooks (all prefixed)
- useExternalData.ts — 24 hooks (all prefixed)
- useK3MartCockpit.ts — 22 hooks (all prefixed)

Process per batch:
1. Read hook files, identify ALL useConvex-prefixed exports
2. Rename in hook files
3. Update index.ts barrel export for renamed hooks
4. Grep for old names in src/ (pages, components, test files)
5. Update all consumer files
6. npm run type-check after each batch — MUST pass before next batch

Test file note: src/hooks/__tests__/useConvexHooks.test.tsx imports useConvex-prefixed names.
Update it in the same step as useOrders.ts (Batch 4).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Batch 4 — Rename useComponentTypes, useIngredients (partial), useOrders (partial)</name>
  <files>
    src/hooks/convex/useComponentTypes.ts
    src/hooks/convex/useIngredients.ts
    src/hooks/convex/useOrders.ts
    src/hooks/convex/index.ts
    src/hooks/__tests__/useConvexHooks.test.tsx
  </files>
  <action>
Step 1 — Read useComponentTypes.ts. Rename all 10 useConvex-prefixed exports:
e.g., useConvexComponentTypes → useComponentTypes, useConvexCreateComponentType → useCreateComponentType, etc.

Step 2 — Read useIngredients.ts. Identify the 6 of 8 hooks with useConvex prefix.
The 2 hooks to KEEP as-is: useLinkIngredientToComponentType, useUnlinkIngredientFromComponentType (already clean).
Rename only the 6 useConvex-prefixed ones (e.g., useConvexIngredients → useIngredients, etc.).

Step 3 — Read useOrders.ts. Identify the 24 of 27 hooks with useConvex prefix.
The 3 hooks to KEEP as-is: useKanbanOrders, useCreateDraft, useUpdateDraft (already clean names).
Rename all 24 useConvex-prefixed hooks (e.g., useConvexOrders → useOrders, useConvexCreateOrder → useCreateOrder, etc.).
Note: useOrders.ts also currently uses raw useMutation — do NOT change mutation hooks to useSessionMutation here. That happens in plan 25-05. Only rename the exported function names.

Step 4 — Update src/hooks/convex/index.ts: update all re-exports for useComponentTypes, useIngredients (the 6 renamed ones), and useOrders (the 24 renamed ones). Keep existing clean exports (useKanbanOrders, useCreateDraft, useUpdateDraft, useLinkIngredientToComponentType, useUnlinkIngredientFromComponentType) unchanged.

Step 5 — Update test file: read src/hooks/__tests__/useConvexHooks.test.tsx.
Find all useConvex-prefixed imports and usages. Update to new names.

Step 6 — Find all consumer files for useComponentTypes, useIngredients, useOrders:
grep -r "useConvexComponentTypes\|useConvexIngredients\|useConvexOrders\|useConvexCreateOrder\|useConvexUpdateOrder\|useConvexDeleteOrder" src/ --include="*.tsx" --include="*.ts" -l
Update each file.

Step 7 — npm run type-check. Fix errors.
Step 8 — npm run test. Fix test failures.
  </action>
  <verify>
1. grep -r "useConvexComponentTypes\|useConvexIngredients\|useConvexOrders" src/ --include="*.tsx" --include="*.ts"
   Expect: zero matches
2. grep "useConvexOrders\|useConvexCreateOrder" src/hooks/__tests__/useConvexHooks.test.tsx
   Expect: zero matches
3. npm run type-check — must exit 0
4. npm run test — must pass
  </verify>
  <done>41 hooks renamed across 3 files. Test file updated. index.ts updated. All consumers updated. type-check + tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Batch 5 — Rename useInventory, useFeedback, useMenuProducts, useExternalData, useK3MartCockpit</name>
  <files>
    src/hooks/convex/useInventory.ts
    src/hooks/convex/useFeedback.ts
    src/hooks/convex/useMenuProducts.ts
    src/hooks/convex/useExternalData.ts
    src/hooks/convex/useK3MartCockpit.ts
    src/hooks/convex/index.ts
  </files>
  <action>
Process each of the 5 hook files. All are 100% useConvex-prefixed (no partial cases):

Step 1 — Read and rename each file:
- useInventory.ts: 13 hooks (e.g., useConvexInventory → useInventory, useConvexInventoryBatches → useInventoryBatches, etc.)
- useFeedback.ts: 12 hooks (e.g., useConvexFeedback → useFeedback, useConvexCaptureFeedback → useCaptureFeedback, etc.)
- useMenuProducts.ts: 16 hooks (e.g., useConvexMenuProducts → useMenuProducts, useConvexCreateMenuProduct → useCreateMenuProduct, etc.)
- useExternalData.ts: 24 hooks (e.g., useConvexExternalRevenue → useExternalRevenue, useConvexGoBizSync → useGoBizSync, etc.)
- useK3MartCockpit.ts: 22 hooks (e.g., useConvexK3MartCockpit → useK3MartCockpit, useConvexK3MartOrders → useK3MartOrders, etc.)

Step 2 — Update src/hooks/convex/index.ts for all 5 files' exports.

Step 3 — Find and update ALL consumer files for all 5 file groups:
grep -r "useConvexInventory\|useConvexFeedback\|useConvexMenuProducts\|useConvexExternalRevenue\|useConvexK3Mart" src/ --include="*.tsx" --include="*.ts" -l
Update each consumer file. The K3Mart hooks and ExternalData hooks are primarily used in K3MartCockpit.tsx, SalesAnalytics.tsx, and similar dashboard pages. The inventory hooks span InventoryManager.tsx, LocationsManager.tsx, and related components.

Step 4 — npm run type-check. Fix any errors.
Step 5 — npm run test. Fix any test failures.

After this task: ZERO useConvex-prefixed function names should exist anywhere in src/.
  </action>
  <verify>
1. Final sweep — no useConvex prefix anywhere:
   grep -r "useConvex" src/ --include="*.tsx" --include="*.ts"
   Expect: ZERO matches (this is the definitive check)
2. npm run type-check — must exit 0
3. npm run test — must pass
  </verify>
  <done>47 hooks renamed across 5 files. index.ts fully updated. All consumers updated. Zero useConvex-prefixed names remain in entire src/. type-check + tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete — DEFINITIVE CHECKS:
1. grep -r "useConvex" src/ --include="*.tsx" --include="*.ts"
   Expect: ZERO matches (the prefix is completely eliminated)
2. npm run type-check — must pass with zero errors
3. npm run test — must pass
4. Confirm index.ts exports all expected clean names:
   grep -c "^export" src/hooks/convex/index.ts
   (Count should be similar to before — same number of exports, just renamed)
</verification>

<success_criteria>
- Zero useConvex-prefixed function names anywhere in src/
- All 161 hooks that needed renaming are renamed (88 in this plan, 34 in plan 25-02)
- index.ts barrel export uses only clean unprefixed names
- Test file src/hooks/__tests__/useConvexHooks.test.tsx updated and passing
- npm run type-check passes
- npm run test passes
</success_criteria>

<output>
After completion, create .planning/phases/25-codebase-cleanup/25-04-SUMMARY.md with:
- Complete list of renamed hooks per file (Batch 4 + Batch 5)
- Consumer files updated
- Confirmation that the final grep shows ZERO useConvex matches
- Confirmation that type-check + tests pass
</output>
