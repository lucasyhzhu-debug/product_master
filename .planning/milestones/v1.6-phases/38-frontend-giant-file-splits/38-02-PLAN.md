---
phase: 38-frontend-giant-file-splits
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/salesAnalytics/OrdersTab.tsx
  - src/components/salesAnalytics/StoreStatusTab.tsx
  - src/components/salesAnalytics/MenuTab.tsx
  - src/components/salesAnalytics/GrabFoodSettingsTab.tsx
  - src/components/salesAnalytics/WebhooksTab.tsx
  - src/components/salesAnalytics/OutletDialog.tsx
  - src/pages/GrabFoodManager.tsx
autonomous: true
requirements: [FFS-02]

must_haves:
  truths:
    - "GrabFoodManager.tsx is under 600 LOC after extraction"
    - "All 5 tab components and OutletDialog render identically to original inline versions"
    - "formatCurrencyIDR is replaced with formatCurrency from utils.ts (or formatCurrency(amount ?? 0) at call sites)"
    - "npm run build passes with zero type errors"
  artifacts:
    - path: "src/components/salesAnalytics/OrdersTab.tsx"
      provides: "GrabFood orders tab with sync history and revenue table"
      exports: ["OrdersTab"]
    - path: "src/components/salesAnalytics/StoreStatusTab.tsx"
      provides: "GrabFood store status with pause/unpause controls"
      exports: ["StoreStatusTab"]
    - path: "src/components/salesAnalytics/MenuTab.tsx"
      provides: "GrabFood menu item availability toggles"
      exports: ["MenuTab"]
    - path: "src/components/salesAnalytics/GrabFoodSettingsTab.tsx"
      provides: "GrabFood settings with MerchantID management and OAuth"
      exports: ["GrabFoodSettingsTab"]
    - path: "src/components/salesAnalytics/WebhooksTab.tsx"
      provides: "GrabFood webhook endpoints and error display"
      exports: ["WebhooksTab"]
    - path: "src/components/salesAnalytics/OutletDialog.tsx"
      provides: "Add/edit outlet dialog"
      exports: ["OutletDialog"]
    - path: "src/pages/GrabFoodManager.tsx"
      provides: "Slim orchestrator with tab layout importing extracted tab components"
      exports: ["GrabFoodManager"]
      max_lines: 600
  key_links:
    - from: "src/pages/GrabFoodManager.tsx"
      to: "src/components/salesAnalytics/OrdersTab.tsx"
      via: "import { OrdersTab }"
      pattern: "import.*OrdersTab"
    - from: "src/pages/GrabFoodManager.tsx"
      to: "src/components/salesAnalytics/GrabFoodSettingsTab.tsx"
      via: "import { GrabFoodSettingsTab }"
      pattern: "import.*GrabFoodSettingsTab"
    - from: "src/pages/GrabFoodManager.tsx"
      to: "src/components/salesAnalytics/OutletDialog.tsx"
      via: "import { OutletDialog }"
      pattern: "import.*OutletDialog"
---

<objective>
Split GrabFoodManager.tsx (1,486 LOC) into focused tab components, achieving under 600 LOC in the main file.

Purpose: GrabFoodManager is a tab-based page with 5 clearly delineated sections. Each tab is a natural extraction boundary with its own UI, hooks, and state. The main component is just a tab container (~122 LOC) so after extraction the result will be well under 600.

Output: 6 new files (5 tab components + OutletDialog) + slimmed GrabFoodManager.tsx (~250-290 LOC)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/GrabFoodManager.tsx
@src/lib/utils.ts
@src/lib/formatters.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/utils.ts:
```typescript
export function formatCurrency(amount: number | null | undefined): string;
// Returns "-" for null — at GrabFood call sites use formatCurrency(amount ?? 0) to get "Rp 0" behavior
```

From src/lib/formatters.ts:
```typescript
export function formatRelativeTime(timestamp: number): string;
// Returns "Just now", "5m ago", "2h ago", "3d ago"
```

From src/lib/dateUtils.ts (created by plan 38-01, or create inline if running in parallel):
```typescript
export function formatDateTimeId(timestamp: number): string;
// Indonesian locale datetime
```

From src/hooks/convex/useGrabFood.ts:
```typescript
export function useGrabFoodOrders(outletId?: Id<"externalOutlets">);
export function useGrabFoodOrderStats(outletId?: Id<"externalOutlets">);
export function useGrabFoodActions();
export function useGrabFoodOutlets();
```

GrabFoodManager main component (lines 122-243) uses:
- useSearchParams, useAuth, useDocumentTitle
- useGrabFoodOutlets, useGrabFoodOrders, useGrabFoodOrderStats, useGrabFoodActions
- Tab layout with Tabs/TabsList/TabsTrigger/TabsContent
- OutletDialog for add/edit
</interfaces>

**Note:** This plan does NOT depend on `src/lib/dateUtils.ts` from plan 38-01. GrabFoodManager's `formatDateTime` takes an ISO string (from GrabFood API), which is different from dateUtils.ts's number-based helpers. The `formatDateTime` function stays local to OrdersTab.tsx. The `formatRelativeTime` replacement uses `@/lib/formatters` (already exists). No cross-plan file conflicts.
</context>

## Git Workflow
**Branch:** `gsd/phase-38-frontend-giant-file-splits`
**Checkpoints:** None (autonomous)

## Implementation Waves
### Wave 1: Extractions [SEQUENTIAL]
| Agent | Task | Files |
|-------|------|-------|
| executor | Extract all 6 sub-components, replace formatCurrencyIDR, slim GrabFoodManager | All files_modified |

### Wave 2: Verification [SEQUENTIAL]
| Agent | Task |
|-------|------|
| Bash | `wc -l src/pages/GrabFoodManager.tsx` (must be <600) |
| Bash | `npm run type-check` |
| Bash | `npm run build` |

## Documentation Updates
- [ ] CHANGELOG.md (LOC numbers TBD after `wc -l`)

## Success Criteria
- [ ] `wc -l src/pages/GrabFoodManager.tsx` < 600
- [ ] `npm run type-check` passes
- [ ] `npm run build` succeeds
- [ ] No `formatCurrencyIDR` function in the codebase (replaced with formatCurrency)

<tasks>

<task type="auto">
  <name>Task 1: Extract GrabFoodManager tab components and replace formatCurrencyIDR</name>
  <files>
    src/components/salesAnalytics/OrdersTab.tsx,
    src/components/salesAnalytics/StoreStatusTab.tsx,
    src/components/salesAnalytics/MenuTab.tsx,
    src/components/salesAnalytics/GrabFoodSettingsTab.tsx,
    src/components/salesAnalytics/WebhooksTab.tsx,
    src/components/salesAnalytics/OutletDialog.tsx,
    src/pages/GrabFoodManager.tsx
  </files>
  <action>
**Step 1: Handle formatCurrencyIDR deduplication**

The local `formatCurrencyIDR` (lines 87-90) returns "Rp 0" for null while the shared `formatCurrency` returns "-". Per CONTEXT.md ("Deduplicate obvious identical helpers"), replace all call sites:
- Where "Rp 0" for null is acceptable: use `formatCurrency(amount ?? 0)`
- Where "-" for null is fine: use `formatCurrency(amount)` directly

The `formatDateTime` function (lines 92-105) accepts a string ISO date and formats it. If `src/lib/dateUtils.ts` already exists from plan 01, check if `formatDateTimeId` covers this use case. If not (formatDateTimeId takes a number, formatDateTime takes a string), keep a local `formatDateTime` in OrdersTab.tsx since it's GrabFood-specific (takes ISO string from GrabFood API, uses "en-ID" locale with Asia/Jakarta timezone).

The `formatRelativeTime` function (lines 107-116) is nearly identical to `src/lib/formatters.ts`. Replace with import from `@/lib/formatters`. Note the small behavior difference: GrabFoodManager's version accepts `number | null | undefined` and returns "Never" for falsy, while formatters.ts accepts `number` only. Add a null guard at call sites: `ts ? formatRelativeTime(ts) : "Never"`.

**Step 2: Extract 5 tab components** (each as a separate .tsx file with named export)

Each file goes in `src/components/salesAnalytics/` per CONTEXT.md.

1. **OrdersTab.tsx** (~235 LOC) — Lines 249-483 (including OrdersTabProps interface at line 249).
   - Props: `OrdersTabProps` — merchantID, outletId, orders, isLoading, stats, onSync, isSyncing, formatDateTime (pass as prop or keep local), onNavigateToMapping.
   - Import `formatCurrency` from `@/lib/utils` (replaces formatCurrencyIDR calls).
   - Import `formatRelativeTime` from `@/lib/formatters` (with null guard at call sites).
   - Import UI components: Card, CardContent, CardHeader, CardTitle, Badge, Button, Table components, Skeleton.
   - Import icons: RefreshCw, Clock, ChevronDown, ChevronUp from lucide-react.
   - Keep the `formatDateTime` function local to this file (it takes an ISO string, specific to GrabFood API response format).

2. **StoreStatusTab.tsx** (~232 LOC) — Lines 490-721 (including StoreStatusTabProps interface at line 490).
   - Props: `StoreStatusTabProps` — merchantID, outletName.
   - This component calls `useGrabFoodActions()` internally to get store status actions. Move the hook call inside.
   - Import `formatRelativeTime` from `@/lib/formatters`.

3. **MenuTab.tsx** (~226 LOC) — Lines 727-952 (including MenuTabProps interface and MenuItem type).
   - Props: `MenuTabProps` — merchantID, isAdmin.
   - Include the `MenuItem` type definition (lines 732-740) in this file.
   - Uses `useGrabFoodActions()` internally for menu sync.

4. **GrabFoodSettingsTab.tsx** (~229 LOC) — Lines 958-1186 (including SettingsTabProps interface).
   - Named `GrabFoodSettingsTab` (not just `SettingsTab` — there's already a `SettingsTab` in the salesAnalytics barrel for the product mapping settings tab).
   - Props: `GrabFoodSettingsTabProps` — outlets array, onAddOutlet, onEditOutlet.
   - This component has its OWN hooks (useQuery for product mappings, useProtectedMutation) — keep them inside.
   - Import `GrabFoodCredentialsDialog` from `./GrabFoodCredentialsDialog` (existing sibling).

5. **WebhooksTab.tsx** (~179 LOC) — Lines 1192-1370 (including WEBHOOK_ENDPOINTS constant).
   - No props needed — fully self-contained with own hooks.
   - Move `WEBHOOK_ENDPOINTS` constant (lines 1192-1199) into this file.
   - Uses useQuery and useProtectedMutation internally.

**Step 3: Extract OutletDialog**

6. **OutletDialog.tsx** (~111 LOC) — Lines 1376-1486 (including OutletDialogProps interface).
   - Props: `OutletDialogProps` — open, onOpenChange, editingOutlet.
   - Uses useProtectedMutation internally.

**Step 4: Slim GrabFoodManager.tsx**

Remove all extracted code (lines 87-116 helpers, lines 249-1486 sub-components). The remaining file should contain ONLY:
- Import statements for extracted components, hooks, and UI primitives
- The `export function GrabFoodManager()` body:
  - Hook calls (useDocumentTitle, useAuth, useSearchParams, useGrabFoodOutlets, useGrabFoodOrders, useGrabFoodOrderStats, useGrabFoodActions)
  - Outlet state management (selectedOutlet, outletDialog state)
  - Tab layout JSX composing: outlet selector, Tabs with TabsContent for each extracted tab, OutletDialog

Do NOT add the extracted components to the salesAnalytics barrel index.ts — they are internal (only consumed by GrabFoodManager page).

**LOC math verification:**
- Starting: 1,486 LOC
- Extracting: formatCurrencyIDR (~4) + formatDateTime (~14) + formatRelativeTime (~10) + OrdersTab (~235) + StoreStatusTab (~232) + MenuTab (~226) + GrabFoodSettingsTab (~229) + WebhooksTab (~179) + OutletDialog (~111) = ~1,240 LOC
- Remaining: 1,486 - 1,240 = ~246 LOC
- Add back: ~25 new import lines = ~271 LOC
- Remove: ~10 LOC now-unused imports = ~261 LOC
- **Expected final: ~261 LOC (well under 600 target)**
  </action>
  <verify>
    <automated>cd "D:/Claude/Product Manager/product_master" && wc -l src/pages/GrabFoodManager.tsx && grep -c "formatCurrencyIDR" src/pages/GrabFoodManager.tsx src/components/salesAnalytics/OrdersTab.tsx 2>/dev/null; npm run type-check && npm run build</automated>
  </verify>
  <done>
    - GrabFoodManager.tsx is under 600 LOC (target ~261)
    - All 6 new files exist with named exports
    - formatCurrencyIDR is completely removed (replaced with formatCurrency)
    - formatRelativeTime uses shared import from @/lib/formatters
    - npm run build passes with zero errors
  </done>
</task>

</tasks>

<verification>
1. `wc -l src/pages/GrabFoodManager.tsx` — must show < 600
2. `npm run type-check` — zero TypeScript errors
3. `npm run build` — successful build
4. `grep -r "formatCurrencyIDR" src/` — should return empty (function removed)
5. Verify all 6 new files exist: `ls src/components/salesAnalytics/{OrdersTab,StoreStatusTab,MenuTab,GrabFoodSettingsTab,WebhooksTab,OutletDialog}.tsx`
</verification>

<success_criteria>
- GrabFoodManager.tsx under 600 LOC (target ~261)
- All tab components extracted to individual files in salesAnalytics/
- formatCurrencyIDR eliminated, using formatCurrency instead
- No type errors, build passes
</success_criteria>

<output>
After completion, create `.planning/phases/38-frontend-giant-file-splits/38-02-SUMMARY.md`
</output>
