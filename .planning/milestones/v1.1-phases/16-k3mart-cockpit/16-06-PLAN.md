---
phase: 16-k3mart-cockpit
plan: 06
type: execute
wave: 2
depends_on: ["16-05"]
gap_closure: true
files_modified:
  - src/pages/K3MartCockpit.tsx
  - src/components/k3martCockpit/WeeklyPlannerGrid.tsx
  - src/components/k3martCockpit/OutletPlannerRow.tsx
  - src/components/k3martCockpit/EditablePlannerCell.tsx
  - src/components/k3martCockpit/PlannerGridHeader.tsx
  - src/components/k3martCockpit/PlannerActionBar.tsx
  - src/components/k3martCockpit/OutletSettingsModal.tsx
  - src/components/k3martCockpit/OutletCard.tsx
autonomous: true

must_haves:
  truths:
    - "Today's Dispatch section appears ABOVE the Weekly Planner in page layout"
    - "Weekly Planner is collapsible with a toggle in the section header (default expanded)"
    - "Past days (before today) in the weekly grid are greyed out and non-editable, showing planned qty as static text"
    - "Today's column is editable and highlighted (not greyed out)"
    - "Dark mode: all grid cells, headers, and backgrounds use proper dark mode tokens (bg-card, text-foreground, border-border)"
    - "Outlet Settings modal shows K3Mart product name (e.g., 'Dubai Chewy Cookie') and POS name (e.g., 'Original - Single (45g)') side by side"
    - "Outlet Settings default price shows the actual K3Mart price from product mappings (e.g., Rp 45,000), not Rp 0"
    - "Product names in the weekly grid show POS product name, not K3Mart codes"
  artifacts:
    - path: "src/pages/K3MartCockpit.tsx"
      provides: "Reordered layout (dispatch above planner) with collapsible planner"
      min_lines: 400
    - path: "src/components/k3martCockpit/OutletSettingsModal.tsx"
      provides: "Product names from K3Mart mapping with real default prices"
      contains: "externalProductName"
---

<objective>
Fix frontend UX issues found during Phase 16 verification: layout ordering, collapsibility, dark mode, past day greying, and product name/price display.

Issues from user feedback:
1. Weekly Planner should be BELOW Today's Dispatch (user wants to see outlet performance first, then plan)
2. Weekly Planner should be collapsible (was incorrectly made always-visible; user wants toggle)
3. Past days (e.g., Monday when it's Tuesday) should be greyed out and non-editable, showing planned qty as read-only
4. Dark mode styling needs work — grid has white/light backgrounds that don't adapt
5. Outlet Settings shows raw K3Mart codes and Rp 0 instead of product names and real prices
6. Product names in the planning grid should use POS names from menu products, not K3Mart codes
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-k3mart-cockpit/16-CONTEXT.md
@.planning/phases/16-k3mart-cockpit/16-05-SUMMARY.md

@src/pages/K3MartCockpit.tsx
@src/components/k3martCockpit/WeeklyPlannerGrid.tsx
@src/components/k3martCockpit/OutletPlannerRow.tsx
@src/components/k3martCockpit/EditablePlannerCell.tsx
@src/components/k3martCockpit/PlannerGridHeader.tsx
@src/components/k3martCockpit/PlannerActionBar.tsx
@src/components/k3martCockpit/OutletSettingsModal.tsx
@src/components/k3martCockpit/OutletCard.tsx
@docs/UI_BRAND_REFERENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Layout reorder, collapsibility, past day greying, and dark mode fixes</name>
  <files>
    src/pages/K3MartCockpit.tsx
    src/components/k3martCockpit/WeeklyPlannerGrid.tsx
    src/components/k3martCockpit/OutletPlannerRow.tsx
    src/components/k3martCockpit/EditablePlannerCell.tsx
    src/components/k3martCockpit/PlannerGridHeader.tsx
    src/components/k3martCockpit/PlannerActionBar.tsx
  </files>
  <action>
    **IMPORTANT — Use existing shadcn/ui components and follow docs/UI_BRAND_REFERENCE.md for dark mode tokens.**

    **1. Layout reorder in K3MartCockpit.tsx (lines 425-550):**

    Move Today's Dispatch section (lines 492-526) ABOVE Weekly Planner (lines 483-490). New order:
    1. Page Header
    2. ProductionReadinessBar (alert zone)
    3. InventorySourcePanel
    4. **TODAY'S DISPATCH** ← moved up
    5. **WEEKLY PLANNER** ← moved down

    **2. Make Weekly Planner collapsible:**

    Add collapsible toggle to Weekly Planner section header:
    ```tsx
    const [plannerExpanded, setPlannerExpanded] = useState(true);

    <div className="space-y-2">
      <button
        onClick={() => setPlannerExpanded(!plannerExpanded)}
        className="flex items-center gap-2 px-1 w-full text-left"
      >
        <Calendar className="h-5 w-5 text-primary" />
        <h2 className="text-lg font-semibold text-foreground">Weekly Planner</h2>
        <ChevronDown className={cn(
          "h-4 w-4 text-muted-foreground transition-transform",
          !plannerExpanded && "-rotate-90"
        )} />
      </button>
      {plannerExpanded && <WeeklyPlannerGrid />}
    </div>
    ```

    **3. Past days greyed out in the weekly grid:**

    Pass `todayStr` from WeeklyPlannerGrid to OutletPlannerRow and EditablePlannerCell:

    In **WeeklyPlannerGrid.tsx**: Already has `todayStr`. Pass it to OutletPlannerRow.

    In **OutletPlannerRow.tsx**: Add `todayStr` prop. For each day cell, compute:
    ```typescript
    const isPastDay = date < todayStr;  // string comparison works for YYYY-MM-DD
    ```
    Pass `isPastDay` to EditablePlannerCell. When isPastDay=true, cell is non-editable.

    In **EditablePlannerCell.tsx**: Add `isPastDay` prop. When true:
    - `isEditable` forced to false
    - Background: `bg-muted/50` (greyed out)
    - Show planned qty as static text (no input), dimmed color `text-muted-foreground`

    In **PlannerGridHeader.tsx**: Past day column headers also get muted styling:
    - Day name, date, event name all use `text-muted-foreground`
    - No confirm/draft buttons shown for past days
    - Background: `bg-muted/30`

    **4. Dark mode fixes across all grid components:**

    Audit and fix all hardcoded light-mode colors. Replace with dark-mode-aware tokens:

    - `bg-white` → `bg-card` or `bg-background`
    - `text-black` or `text-gray-900` → `text-foreground`
    - `border-gray-200` → `border-border`
    - `bg-gray-50` → `bg-muted`
    - `bg-gray-100` → `bg-muted`
    - `bg-blue-50` (weekend) → `bg-blue-950/30 dark:bg-blue-950/30` or use `bg-blue-50 dark:bg-blue-900/20`
    - `bg-red-50` (holiday) → `bg-red-50 dark:bg-red-900/20`
    - `bg-purple-50` (sales date) → `bg-purple-50 dark:bg-purple-900/20`
    - `bg-green-50` (confirmed) → `bg-green-50 dark:bg-green-900/20`
    - Status badges should use dark-mode-aware variants

    Check ALL files in the k3martCockpit components directory. Common offenders:
    - WeeklyPlannerGrid.tsx — grid wrapper backgrounds
    - OutletPlannerRow.tsx — row backgrounds, outlet header
    - EditablePlannerCell.tsx — cell backgrounds per status
    - PlannerGridHeader.tsx — header row backgrounds
    - PlannerActionBar.tsx — totals row background
    - OutletCard.tsx — card backgrounds

    Also check for:
    - Input fields need `bg-transparent` or `bg-background` in dark mode
    - Focus rings should use `ring-primary` not hardcoded colors
    - Tooltip backgrounds should use dark mode tokens
  </action>
  <verify>
    `npm run build` passes. Today's Dispatch appears above Weekly Planner. Planner has collapse toggle. Past days are greyed out and non-editable. Dark mode shows no white/light backgrounds in the grid area.
  </verify>
  <done>
    Layout reordered (dispatch first, planner second). Planner is collapsible. Past days greyed out with read-only planned values. Dark mode properly uses theme tokens throughout all grid components.
  </done>
</task>

<task type="auto">
  <name>Task 2: Product name and price display in outlet settings and grid</name>
  <files>
    src/components/k3martCockpit/OutletSettingsModal.tsx
    src/components/k3martCockpit/OutletPlannerRow.tsx
    src/components/k3martCockpit/WeeklyPlannerGrid.tsx
    src/components/k3martCockpit/OutletCard.tsx
  </files>
  <action>
    **IMPORTANT — Use existing shadcn/ui components.**

    **1. Update OutletSettingsModal.tsx to show proper product names and prices:**

    The backend (Plan 05) now returns `externalProductName` and `defaultPrice` in outlet settings. Update the modal to show:

    For each product row in the "Product Settings" tab:
    ```
    ✅ Dubai Chewy Cookie → Original - Single (45g)
       Default: Rp 45,000    [Custom pricing toggle]  [Price input if custom]
    ```

    Instead of the current:
    ```
    ✅ F03131-P00002
       Default: Rp 0    [Custom pricing toggle]
    ```

    Show:
    - Line 1: K3Mart product name (externalProductName) → POS product name (productName), with arrow icon between them
    - Line 2: "Default: Rp {formatCurrency(defaultPrice)}" — use the real default price from backend
    - If defaultPrice is 0, show a warning: "⚠ Price not mapped" in amber

    **2. Update product names in the weekly grid:**

    In **OutletPlannerRow.tsx**, the product name column currently shows the POS menu product name. Ensure it uses `productName` from the backend response (which Plan 05 now resolves from externalProductMappings → menuProducts). If truncated, show full name on hover via shadcn Tooltip.

    In **WeeklyPlannerGrid.tsx**, ensure the outlet data being passed uses the enriched product names from the updated query.

    **3. Update OutletCard.tsx product names:**

    In the expanded outlet panel, product names should also use resolved names, not K3Mart codes.
  </action>
  <verify>
    `npm run build` passes. Outlet settings shows "Dubai Chewy Cookie → Original - Single (45g)" with "Default: Rp 45,000". Grid product names are human-readable POS names. No K3Mart product codes visible in the UI.
  </verify>
  <done>
    Outlet settings shows K3Mart name → POS name with real default price. Grid uses resolved POS product names. Tooltips on truncated names. Price warnings when unmapped.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes
- Layout: Today's Dispatch above Weekly Planner
- Weekly Planner is collapsible (default expanded)
- Past days greyed out and non-editable
- Dark mode: no white backgrounds in grid
- Product names: K3Mart name + POS name shown, not codes
- Default prices: real mapped prices, not Rp 0
</verification>

<success_criteria>
- Today's Dispatch section renders before Weekly Planner
- Weekly Planner collapses/expands via header toggle
- Past days show planned qty as greyed-out static text
- All grid components render correctly in dark mode
- Outlet Settings shows "Dubai Chewy Cookie → Original - Single (45g)" with Rp 45,000
- No raw K3Mart product codes visible anywhere in the cockpit UI
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/16-k3mart-cockpit/16-06-SUMMARY.md`
</output>
