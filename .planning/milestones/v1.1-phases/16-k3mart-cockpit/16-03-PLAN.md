---
phase: 16-k3mart-cockpit
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/components/k3martCockpit/OutletCardGrid.tsx
  - src/components/k3martCockpit/OutletCard.tsx
  - src/components/k3martCockpit/ExpandedOutletPanel.tsx
  - src/components/k3martCockpit/StockFlowForm.tsx
  - src/components/k3martCockpit/StockFlowConfirmDialog.tsx
  - src/components/k3martCockpit/StockMovementHistory.tsx
  - src/components/k3martCockpit/OutletSettingsModal.tsx
  - src/pages/K3MartCockpit.tsx
autonomous: true

must_haves:
  truths:
    - "Outlet info cards show current stock, number sold today, average sales/day from last week"
    - "Tapping outlet card expands to show stock-in/stock-out form + history log"
    - "Rotation shortcut button does stock-out of remaining + stock-in of new quantity in one action"
    - "Rotation auto-fills stock-out quantity from current K3Mart stock and auto-generates 'rotation stock-out/stock-in' comment"
    - "Confirmation step always shown before sending to K3Mart API with summary (outlet, product, qty, price)"
    - "Price sanity check prevents zero/null price in any API call"
    - "K3Mart API error messages shown with retry button; no local save on API failure"
    - "History log shows API-pulled list of all movements with statuses"
    - "Outlet settings modal supports active/inactive toggle, per-outlet product selection, and per-outlet pricing"
    - "Default pricing from menu product with 'Custom pricing' toggle for per-outlet overrides"
  artifacts:
    - path: "src/components/k3martCockpit/StockFlowConfirmDialog.tsx"
      provides: "Confirmation dialog before K3Mart API call with summary display"
      min_lines: 40
    - path: "src/components/k3martCockpit/OutletSettingsModal.tsx"
      provides: "Bulk outlet management modal with product selection and pricing"
      min_lines: 80
    - path: "src/components/k3martCockpit/StockFlowForm.tsx"
      provides: "Enhanced stock form with rotation shortcut and price validation"
      min_lines: 100
  key_links:
    - from: "src/components/k3martCockpit/StockFlowForm.tsx"
      to: "src/components/k3martCockpit/StockFlowConfirmDialog.tsx"
      via: "Opens confirm dialog before API submission"
      pattern: "StockFlowConfirmDialog"
    - from: "src/components/k3martCockpit/OutletSettingsModal.tsx"
      to: "src/hooks/convex/useK3MartCockpit.ts (useConvexSaveOutletSettings)"
      via: "Save outlet product selection and pricing"
      pattern: "useConvexSaveOutletSettings"
    - from: "src/components/k3martCockpit/StockMovementHistory.tsx"
      to: "src/hooks/convex/useK3MartCockpit.ts (useConvexFetchStockFlowHistory)"
      via: "Fetch movements from K3Mart API"
      pattern: "useConvexFetchStockFlowHistory"
---

<objective>
Enhance outlet cards with stock info display, add rotation shortcut to stock flow, add confirmation dialogs before API calls, wire movement history from API, and build outlet settings modal for product selection and pricing.

Purpose: Complete the stock movement recording workflow (K3M-04) and outlet management (K3M-01) per user decisions -- rotation is a key daily workflow, confirmation prevents accidental submissions, and per-outlet settings control what appears in the planning grid.
Output: Fully functional outlet cards, stock flow with rotation, confirmation, history, and outlet settings.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-k3mart-cockpit/16-CONTEXT.md
@.planning/phases/16-k3mart-cockpit/16-01-SUMMARY.md

@src/components/k3martCockpit/OutletCardGrid.tsx
@src/components/k3martCockpit/OutletCard.tsx
@src/components/k3martCockpit/ExpandedOutletPanel.tsx
@src/components/k3martCockpit/StockFlowForm.tsx
@src/components/k3martCockpit/StockMovementHistory.tsx
@src/hooks/convex/useK3MartCockpit.ts
@convex/integrations/k3mart/adapter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stock flow with rotation shortcut, confirmation dialog, and price validation</name>
  <files>
    src/components/k3martCockpit/StockFlowForm.tsx
    src/components/k3martCockpit/StockFlowConfirmDialog.tsx
    src/components/k3martCockpit/StockMovementHistory.tsx
    src/components/k3martCockpit/ExpandedOutletPanel.tsx
  </files>
  <action>
    **IMPORTANT -- Use existing shadcn/ui components:**
    Available: Card, Badge, Button, AlertDialog, Dialog, Input, Label, Textarea, Tooltip, Separator, Select, Tabs, Alert.

    **Create StockFlowConfirmDialog.tsx:**
    Confirmation dialog using shadcn `AlertDialog`:
    - `AlertDialogTitle`: "Confirm Stock {In/Out}" or "Confirm Rotation"
    - `AlertDialogDescription`: Summary of what will be sent to K3Mart API
    - Content shows:
      - Outlet name (bold)
      - Product name
      - Direction: "Stock In" / "Stock Out" / "Rotation (Stock Out + Stock In)"
      - Quantity (large number)
      - Price per unit (from outlet product pricing)
      - Total value (qty * price)
      - Note (if any)
    - **Price sanity check:** If price is 0 or null, show a shadcn `Alert variant="destructive"` warning: "Price is missing! Cannot submit without price." and disable the confirm button. Per user decision: NEVER send a request without price.
    - `AlertDialogAction` (confirm, disabled if price is 0/null) + `AlertDialogCancel`
    - Props: `open, onConfirm, onCancel, data: { outlet, product, direction, qty, price, note, isRotation }`

    **Enhance StockFlowForm.tsx:**
    Add rotation shortcut per user decisions:
    1. Add a "Rotate" button (shadcn `Button`) alongside stock-in/stock-out tabs or as a third option:
       - Prominent button with `RefreshCw` icon, amber color
       - Labeled "Rotate Stock"
    2. When "Rotate" is clicked:
       - Auto-fill stock-out quantity from current K3Mart stock for selected product (`selectedProduct.currentStock`)
       - Show stock-in quantity input (pre-filled with 30 as common default or empty for user to enter)
       - Auto-generate note: "rotation stock-out/stock-in"
    3. Rotation submit flow:
       - First call: stock-out of `currentStock` with note "rotation stock-out"
       - On success: stock-in of user-entered quantity with note "rotation stock-in"
       - Both calls go through `StockFlowConfirmDialog` (show combined summary)
    4. **Price validation:** Before showing `StockFlowConfirmDialog`, check that price exists and is > 0. If not, show toast error: "Price not configured for this product at this outlet. Configure in Outlet Settings."
    5. Replace direct `onSubmit` call with opening `StockFlowConfirmDialog` first (per user decision: always confirm before sending to API)
    6. **Error handling:** On K3Mart API error, show the error message in a toast + a "Retry" button. Do NOT save locally if API fails (per user decision).

    **Enhance StockMovementHistory.tsx:**
    Wire to real K3Mart API data:
    1. Use `useConvexFetchStockFlowHistory` action to pull history from K3Mart API for this outlet
    2. Also show local `k3martStockMovements` records from `useConvexStockMovementHistory` query
    3. Merge both sources, sorted by timestamp descending
    4. Each entry shows:
       - Direction icon (green arrow down for stock-in, amber arrow up for stock-out)
       - Product name
       - Quantity
       - Status badge: pending (amber), approved (green), rejected (red), canceled (gray) -- use shadcn `Badge`
       - Timestamp (relative time with absolute on hover via shadcn `Tooltip`)
       - Note text (if any)
    5. Tap a log entry to see full details (expand inline or use a small Dialog showing all fields)
    6. Add pagination: show 10 entries initially, "Load more" button

    **Update ExpandedOutletPanel.tsx:**
    - Pass `outletName` and product `price` data to `StockFlowForm` for confirm dialog
    - Pass outlet products with current stock for rotation auto-fill
  </action>
  <verify>
    `npm run build` passes. `grep "Rotate\|StockFlowConfirmDialog\|rotation" src/components/k3martCockpit/StockFlowForm.tsx` returns matches. Price sanity check prevents zero-price submissions.
  </verify>
  <done>
    Stock flow has rotation shortcut (auto-fills from current stock, generates rotation notes), confirmation dialog before every API call with price sanity check, error display with retry, and history log from K3Mart API with status badges and expandable details.
  </done>
</task>

<task type="auto">
  <name>Task 2: Outlet settings modal and outlet card enhancements</name>
  <files>
    src/components/k3martCockpit/OutletSettingsModal.tsx
    src/components/k3martCockpit/OutletCard.tsx
    src/components/k3martCockpit/OutletCardGrid.tsx
    src/pages/K3MartCockpit.tsx
  </files>
  <action>
    **IMPORTANT -- Use existing shadcn/ui components:**
    Available: Card, Badge, Button, Dialog, Switch, Input, Label, Separator, Tabs, Table.

    **Create OutletSettingsModal.tsx:**
    Modal for bulk outlet management per user decisions:
    - Use shadcn `Dialog` (`DialogContent`, `DialogHeader`, `DialogTitle`)
    - Two sections (use shadcn `Tabs`):

    **Tab 1: "Outlets"**
    - List all K3Mart outlets (active and inactive)
    - Each row: outlet name + shadcn `Switch` for active/inactive toggle
    - Uses `useConvexToggleOutletActive` mutation on toggle
    - Active outlets shown with green indicator, inactive grayed out

    **Tab 2: "Product Settings"**
    - Select outlet from dropdown (shadcn `Select`)
    - For selected outlet, show all K3Mart-mapped products:
      - Each product row:
        - Product name
        - shadcn `Checkbox` to include/exclude from planning grid (maps to `restockTargets.isHidden` -- per-outlet product selection)
        - "Custom pricing" shadcn `Switch` toggle
        - If custom pricing enabled: shadcn `Input type="number"` for price override (formatted as Rp) -- maps to `restockTargets.customPrice`
        - Default price shown as muted reference text (from `K3MART_CONFIG.productMap` default)
      - Price sanity: prevent saving 0 or negative price
    - Save button calls `useConvexSaveOutletSettings` mutation
    - Per user decision: default pricing from menu product, "Custom pricing" toggle enables override

    **Trigger:** "Settings" button (gear icon) in the K3MartCockpit.tsx page header, next to the "Refresh Data" button. Admin-only (hide if role is not admin, per user decision: admin-only configuration).

    **Enhance OutletCard.tsx:**
    Per user decisions, outlet info cards should show:
    - Current stock (total across products) -- already shown
    - Number sold today -- already shown
    - Average sales/day from last week -- ADD this. Calculate from existing `products[].avgDailySales` sum.
    - Keep existing plan status badge, low stock warning, last sync time

    **Update OutletCardGrid.tsx:**
    - Active outlets only in the grid (per user decision). Filter `outlets.filter(o => o.isActive)` if not already done by backend.
    - Ensure the grid still works with the expanded panel for stock flow forms.

    **Update K3MartCockpit.tsx:**
    - Add OutletSettingsModal with open/close state
    - Add "Settings" button in header (admin-only, next to "Refresh Data")
    - Wire `useConvexOutletSettings` to pass pricing data to OutletCardGrid
  </action>
  <verify>
    `npm run build` passes. `grep "OutletSettingsModal\|Custom pricing\|isHidden" src/components/k3martCockpit/OutletSettingsModal.tsx` returns matches. Settings button exists in K3MartCockpit page header.
  </verify>
  <done>
    Outlet settings modal with active/inactive toggles, per-outlet product selection (isHidden), and per-outlet pricing (customPrice) with custom pricing toggle. Outlet cards show current stock, sold today, and avg sales/day. Active outlets only in grid. Settings accessible via admin-only gear button.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes
- Rotation shortcut in stock flow form
- Confirmation dialog before every K3Mart API call
- Price sanity check prevents zero-price submissions
- Movement history shows API-pulled data with statuses
- Outlet settings modal with product selection and pricing
- Admin-only settings access
- Outlet cards show avg daily sales
</verification>

<success_criteria>
- Rotation button auto-fills from current stock + generates rotation notes
- Confirmation dialog shows summary with price sanity check
- Error handling shows API errors with retry
- History log merges API + local data with status badges
- Outlet settings supports active/inactive, product selection (isHidden), custom pricing (customPrice)
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/16-k3mart-cockpit/16-03-SUMMARY.md`
</output>
