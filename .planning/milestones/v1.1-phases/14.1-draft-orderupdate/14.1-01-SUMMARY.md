---
phase: 14.1-draft-orderupdate
plan: "01"
subsystem: api
tags: [convex, mutations, orders, draft, kanban]

requires:
  - phase: 14-order-qol
    provides: "7-status order model, Kanban board, orderCrud mutations"
provides:
  - "createDraft mutation for minimal Draft order creation (customer only, no items)"
  - "updateDraft mutation for full Draft field editing (customer, dueDate, delivery, voucher, notes)"
  - "Draft Kanban column sorted by creation date descending (newest first)"
affects: [14.1-02-PLAN, frontend-order-creation]

tech-stack:
  added: []
  patterns: ["minimal-create-then-update pattern for Draft orders"]

key-files:
  created: []
  modified:
    - convex/orders/mutations/orderCrud.ts
    - convex/orders/queries.ts

key-decisions:
  - "createDraft is intentionally minimal (no items, no price validation) to support instant persistence on customer selection"
  - "updateDraft handles voucher lifecycle (validate, apply, release) independently from item management"
  - "WhatsApp backend already handles language parameter correctly; fix is frontend-only (Plan 02)"

patterns-established:
  - "Minimal-create-then-update: createDraft inserts with zeroed totals, updateDraft patches fields, replaceItems handles items separately"

duration: 3min
completed: 2026-02-16
---

# Phase 14.1 Plan 01: Draft Backend Mutations Summary

**createDraft + updateDraft mutations for persistent Draft orders with Kanban sort fix (newest first)**

## Performance

- **Duration:** 3 min
- **Started:** 2026-02-16T02:41:53Z
- **Completed:** 2026-02-16T02:44:23Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- createDraft mutation creates minimal Draft order with just a customer (zero items, zero totals)
- updateDraft mutation updates all editable fields on Draft orders including customer change and voucher handling
- Draft Kanban column now sorts by _creationTime descending (newest first)
- Confirmed WhatsApp backend already supports language parameter correctly (no backend change needed)

## Task Commits

Each task was committed atomically:

1. **Task 1: createDraft and updateDraft mutations** - `829b38c` (feat)
2. **Task 2: Draft column sort fix + WhatsApp verification** - `4fb1d54` (fix)

## Files Created/Modified
- `convex/orders/mutations/orderCrud.ts` - Added createDraft (minimal Draft creation) and updateDraft (full Draft field editing with voucher lifecycle)
- `convex/orders/queries.ts` - Fixed listForKanban Draft column sort to use _creationTime descending

## Decisions Made
- createDraft is deliberately minimal: no items, no price validation, zeroed totals. Frontend calls replaceItems separately for item management.
- updateDraft handles voucher change/clear independently, including release of old voucher usage and application of new voucher.
- WhatsApp getMessage already accepts and uses the language parameter. The translation bug is frontend-only (StepWhatsAppTemplate.tsx does not pass language to the query). Fix deferred to Plan 02.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Backend mutations ready for Plan 02 (frontend integration)
- Frontend needs: wire createDraft on customer selection, open edit modal for Draft cards, pass language to WhatsApp query

---
*Phase: 14.1-draft-orderupdate*
*Completed: 2026-02-16*
