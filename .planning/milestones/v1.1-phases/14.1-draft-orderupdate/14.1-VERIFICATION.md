---
phase: 14.1-draft-orderupdate
verified: 2026-02-16T03:15:00Z
status: passed
score: 10/10 must-haves verified
---

# Phase 14.1: Draft Order Fixes Verification Report

**Phase Goal:** Fix Draft order persistence and editing -- auto-create Draft on customer select, enable full Draft editing from Kanban, fix WhatsApp translation toggle

**Verified:** 2026-02-16T03:15:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A Draft order is created in the database the moment a customer is selected, even with zero items | VERIFIED | createDraft mutation exists, called on customer select (OrderCreate.tsx:196, 219) |
| 2 | A Draft order can be fully updated via a single updateDraft mutation | VERIFIED | updateDraft mutation exists with all fields (orderCrud.ts:723-823), called at OrderCreate.tsx:353 |
| 3 | Draft column in Kanban is sorted by creation date descending (newest first) | VERIFIED | queries.ts:1094-1098 implements _creationTime descending sort for draft column |
| 4 | Any order_staff can delete a Draft order via the existing remove mutation | VERIFIED | remove mutation exists (orderCrud.ts:466-503), restricted to Draft status only (line 475) |
| 5 | WhatsApp getMessage query respects the language parameter | VERIFIED | Backend already supported (whatsapp.ts), frontend now passes language param (StepWhatsAppTemplate:63) |
| 6 | Selecting a customer in the new order form auto-creates a Draft order immediately | VERIFIED | createDraft called on customer select (OrderCreate.tsx:196, 219), sets draftOrderId state |
| 7 | Clicking a Draft card in Kanban opens OrderCreate in edit mode with all fields pre-filled | VERIFIED | Draft routing (OrderManager.tsx:37), edit mode detection (OrderCreate.tsx:46), data loading (99-149) |
| 8 | Draft cards in Kanban open the edit form, not the read-only slide-over | VERIFIED | Status-aware routing in OrderManager.tsx:34-42, Draft cards navigate to /orders/new?draft=ID |
| 9 | Editing a Draft updates the existing order (mutation, not create-then-delete) | VERIFIED | updateDraft + replaceItems called on submit (OrderCreate.tsx:353-372), no delete+create |
| 10 | WhatsApp translate toggle switches both the title and the full message body content | VERIFIED | language state passed to getMessage query (StepWhatsAppTemplate:63), templateName derived from language |

**Score:** 10/10 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| convex/orders/mutations/orderCrud.ts | createDraft and updateDraft mutations | VERIFIED | createDraft (645-715), updateDraft (723-823), both exported |
| convex/orders/queries.ts | listForKanban with Draft column sorted newest-first | VERIFIED | Draft column sort logic at lines 1094-1098 uses _creationTime descending |
| src/pages/OrderCreate.tsx | Order creation page with Draft auto-save and edit mode | VERIFIED | Edit mode (46-149), auto-save on customer select (196, 219), pre-fill from existing Draft (100-149) |
| src/pages/OrderManager.tsx | Kanban board with Draft column routing to edit mode | VERIFIED | handleCardClick routes Draft to edit form (34-42), status-aware navigation |
| src/components/orders/StepWhatsAppTemplate.tsx | WhatsApp template with language parameter passed to getMessage | VERIFIED | language state (56), passed to useQuery (63), toggle button (115-123) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| OrderCreate.tsx (customer select) | convex/orders/mutations/orderCrud.ts (createDraft) | useMutation called when customer is selected | WIRED | Line 164 imports mutation, lines 196 and 219 call it on customer select |
| OrderCreate.tsx (submit) | convex/orders/mutations/orderCrud.ts (updateDraft) | useMutation called to save Draft field changes | WIRED | Line 165 imports mutation, line 353 calls it to save changes before submit |
| OrderManager.tsx (Draft card click) | OrderCreate.tsx (edit mode) | navigate with draft param | WIRED | Line 37 navigates with draft param, OrderCreate reads it at line 46 |
| StepWhatsAppTemplate.tsx (language toggle) | convex/orders/whatsapp.ts (getMessage) | useQuery passes language parameter | WIRED | Language state at line 56, passed to query at line 63 |
| Draft column (queries.ts) | _creationTime sort | Draft orders sorted by creation date descending | WIRED | Lines 1094-1098 implement sort logic for draft column |

### Requirements Coverage

Phase 14.1 is an inserted phase, not mapped to specific requirements. Core goal: fix Draft persistence and editing bugs.

### Anti-Patterns Found

None detected.

### Human Verification Required

#### 1. Draft Auto-Save Flow

**Test:** Navigate to /orders/new, select a customer, verify createDraft mutation is called immediately in DevTools Network tab, add products, refresh page, verify draft persists and appears in Kanban Draft column.

**Expected:** Draft is created on customer selection, persists across refresh, appears in Kanban Draft column sorted newest-first

**Why human:** Need to verify real-time behavior and user flow, confirm Draft appears in Kanban immediately after creation

#### 2. Draft Editing from Kanban

**Test:** Create a Draft order with customer + products, navigate to /orders Kanban view, locate the Draft in the Draft column (at top, newest first), click the Draft card, verify it opens /orders/new?draft={orderId}, verify all fields are pre-filled, change quantity, add notes, submit, verify Draft moves to AwaitingPayment column.

**Expected:** Draft card opens edit form with pre-filled data, submit transitions status correctly

**Why human:** Need to verify full UX flow, confirm routing behavior matches user expectations

#### 3. WhatsApp Language Toggle

**Test:** Create an order with at least 1 item, navigate to order detail slide-over, click WhatsApp section, verify payment request message displays in Indonesian (default), click the language toggle button (ID/EN), verify both the template title AND the full message body switch to English, toggle back to Indonesian, verify content switches back.

**Expected:** Both title and message body switch languages when toggle is clicked

**Why human:** Need to verify visual content changes, confirm DB templates exist for both languages (or fallback is acceptable)

#### 4. Draft Column Sort Order

**Test:** Create 3 Draft orders in sequence (Draft A, B, C), navigate to /orders Kanban view, verify Draft column shows: Draft C (top), Draft B (middle), Draft A (bottom), create a new Draft (Draft D), refresh Kanban, verify Draft D is now at the top of the Draft column.

**Expected:** Newest Drafts appear at the top, sort order is creation time descending

**Why human:** Need to verify visual sort order matches expectations, confirm real-time updates work

---

## Summary

**All must-haves verified.** Phase 14.1 successfully implements:

1. **Draft auto-creation** on customer selection (backend: createDraft mutation, frontend: auto-save on customer select)
2. **Full Draft editing** from Kanban (status-aware routing, edit mode with pre-fill, updateDraft + replaceItems on submit)
3. **WhatsApp language toggle** fix (language parameter now passed to getMessage query)
4. **Draft column sort** fix (newest first via _creationTime descending)

**Build status:** PASSED (npm run build succeeds with zero errors)

**Code quality:** No anti-patterns detected, all wiring verified, mutations properly exported

**Ready for:** User Acceptance Testing (UAT) with the 4 human verification scenarios above

---

_Verified: 2026-02-16T03:15:00Z_
_Verifier: Claude (gsd-verifier)_
