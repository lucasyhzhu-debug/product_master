---
phase: 14-order-qol
plan: 06
type: execute
wave: 4
depends_on: ["14-04", "14-05"]
files_modified:
  - src/components/orders/AuditTrail.tsx
  - src/components/orders/OrderSlideOver.tsx
  - src/components/orders/KanbanCard.tsx
  - src/pages/OrderDetail.tsx
  - src/pages/KitchenViewV2.tsx
  - src/components/kitchen/OrderCard.tsx
  - src/lib/orderConstants.ts
  - docs/CHANGELOG.md
autonomous: false

must_haves:
  truths:
    - "Audit trail vertical timeline appears at the bottom of the order details slide-over"
    - "Audit trail shows who changed the status, when, and which transition (with revert reason if applicable)"
    - "OrderDetail page (if navigated directly) shows updated statuses and audit trail"
    - "KitchenViewV2 uses new BeingPrepared status instead of old InProduction/Boxed/Labeled"
    - "Kitchen has 'Complete Order' button that moves BeingPrepared -> AwaitingDelivery"
    - "Kitchen has 'Send back to order desk' button on BeingPrepared orders"
    - "Copy to new order button on cancelled orders creates new Draft"
    - "WhatsApp modal appears after Submit Order action"
    - "npm run build succeeds with zero errors"
    - "CHANGELOG.md updated with Phase 14 changes"
  artifacts:
    - path: "src/components/orders/AuditTrail.tsx"
      provides: "Vertical timeline component showing status change history"
      min_lines: 50
    - path: "docs/CHANGELOG.md"
      provides: "Updated changelog with Phase 14 entry"
      contains: "Phase 14"
  key_links:
    - from: "src/components/orders/AuditTrail.tsx"
      to: "convex/orders/queries.ts (getAuditTrail)"
      via: "useQuery for audit events"
      pattern: "getAuditTrail"
    - from: "src/pages/KitchenViewV2.tsx"
      to: "convex/orders/mutations/statusUpdates.ts"
      via: "Kitchen uses moveForward for complete, moveBackward for send-back"
      pattern: "moveForward|moveBackward"
---

<objective>
Build the audit trail timeline, wire up remaining UI integrations (kitchen status updates, WhatsApp modal, copy-to-new-order), update KitchenView for new statuses, and finalize documentation.

Purpose: Complete the phase by connecting all remaining pieces -- audit trail visibility, kitchen integration with new statuses, and edge case UX (cancelled order copy, WhatsApp flow).
Output: Fully functional order QoL system with audit trail, kitchen integration, and documentation.
</objective>

## Git Workflow
**Branch:** `feature/order-qol`
**Checkpoints:** Visual verification of Kanban board + slide-over + audit trail

## Implementation Waves
### Wave 1: Audit Trail + Kitchen Updates [PARALLEL]
| Agent | Task | Files |
|-------|------|-------|
| react-ui-builder | Audit trail + slide-over completion | src/components/orders/AuditTrail.tsx, src/components/orders/OrderSlideOver.tsx |
| react-ui-builder | Kitchen view status updates | src/pages/KitchenViewV2.tsx, src/components/kitchen/OrderCard.tsx |

### Wave 2: Integration + Polish [SEQUENTIAL]
| Agent | Task | Files |
|-------|------|-------|
| react-ui-builder | Copy-to-new-order + WhatsApp modal + OrderDetail | src/components/orders/KanbanCard.tsx, src/pages/OrderDetail.tsx |

### Wave 3: Verification [SEQUENTIAL]
| Agent | Task |
|-------|------|
| code-auditor | Type check + pattern compliance |
| Bash | npm run build |

## Documentation Updates
- [ ] CHANGELOG.md (required)
- [ ] API_REFERENCE.md (new queries/mutations)
- [ ] SCHEMA.md (status change)

## Success Criteria
- [ ] `npm run type-check` passes
- [ ] `npm run build` succeeds
- [ ] Audit trail renders in slide-over
- [ ] Kitchen view works with new statuses
- [ ] CHANGELOG.md updated

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-order-qol/14-CONTEXT.md
@.planning/phases/14-order-qol/14-01-SUMMARY.md
@.planning/phases/14-order-qol/14-03-SUMMARY.md
@.planning/phases/14-order-qol/14-04-SUMMARY.md
@.planning/phases/14-order-qol/14-05-SUMMARY.md

@src/components/orders/OrderSlideOver.tsx
@src/pages/KitchenViewV2.tsx
@src/components/kitchen/
@src/pages/OrderDetail.tsx
@docs/CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit trail timeline + kitchen view updates</name>
  <files>
    src/components/orders/AuditTrail.tsx
    src/components/orders/OrderSlideOver.tsx
    src/pages/KitchenViewV2.tsx
    src/components/kitchen/OrderCard.tsx
  </files>
  <action>
    **Create AuditTrail.tsx:**
    Vertical timeline component at the bottom of the order details slide-over:
    - Uses `useQuery(api.orders.queries.getAuditTrail, { orderId })` to fetch events
    - Timeline design (Claude's discretion on visual design):
      - Vertical line on the left
      - Each event is a node on the timeline with:
        - Circle icon on the line (colored by event type: green for forward, amber for backward, red for cancel)
        - Status transition text: "{fromStatus} -> {toStatus}"
        - Who: "{userName}" or "System" if no user
        - When: relative time ("2 hours ago") + absolute timestamp on hover
        - Reason: if present, shown as italic text below the transition
    - Most recent event at top (descending order)
    - Collapsible section: "Status History ({count})" header, expanded by default
    - Loading state: skeleton timeline with 3 placeholder nodes
    - Empty state: "No status changes recorded" with info icon

    **Update OrderSlideOver.tsx:**
    Replace the audit trail placeholder with the real `<AuditTrail orderId={orderId} />` component.
    Also add:
    - "Copy to New Order" button visible only when order status is "Cancelled"
      - Calls `copyFromCancelled` mutation
      - On success: navigate to `/orders/new?orderId={newDraftId}` or open the new draft in slide-over
      - Show toast "Draft order created from cancelled order"
    - WhatsApp section: after Submit Order (when order transitions from Draft to AwaitingPayment), show a WhatsApp payment template modal/section. Reuse existing `<OrderWhatsAppPanel />` component or `<StepWhatsAppTemplate />` component for the WhatsApp template display. The modal should show the payment request template with order details pre-filled.

    **Update KitchenViewV2.tsx:**
    1. Replace all old status references:
       - `InProduction`/`Boxed`/`Labeled` -> `BeingPrepared`
       - `Confirmed` -> `PaymentReceived`
       - `WaitingShipment`/`WaitingPickup` -> `AwaitingDelivery`
       - `CompleteShipped`/`PickedUp` -> `Complete`
    2. Kitchen visibility: show orders with status `BeingPrepared` (was InProduction/Boxed/Labeled)
    3. Kitchen "Complete Order" action: calls `moveForward` mutation (BeingPrepared -> AwaitingDelivery)
    4. Kitchen "Send back to order desk": calls `moveBackward` mutation with targetStatus "PaymentReceived" and a reason dialog
    5. Package tracking (empty/filling/filled/packed) STAYS on orderItems -- this is visual progress within BeingPrepared, unchanged

    **Update kitchen OrderCard component (if exists in src/components/kitchen/):**
    Update status badge colors and labels to match new statuses.
    Add "EXPEDITED" badge display for orders with `expedited: true`.
  </action>
  <verify>
    `npm run build` passes. `grep "AuditTrail" src/components/orders/OrderSlideOver.tsx` returns a match. `grep "BeingPrepared" src/pages/KitchenViewV2.tsx` returns matches (no old statuses remain).
  </verify>
  <done>
    Audit trail timeline renders in slide-over with status change history including who/when/reason. Kitchen view uses new statuses. Kitchen has Complete Order and Send Back buttons. Copy-to-new-order works for cancelled orders. WhatsApp modal shows after submit.
  </done>
</task>

<task type="auto">
  <name>Task 2: OrderDetail page update + documentation</name>
  <files>
    src/pages/OrderDetail.tsx
    src/lib/orderConstants.ts
    docs/CHANGELOG.md
    docs/API_REFERENCE.md
    docs/SCHEMA.md
  </files>
  <action>
    **Update OrderDetail.tsx:**
    1. Replace all old status references with new 7-status model
    2. Remove the accordion-based status workflow (was step-by-step: AwaitingPayment -> Confirmed -> InProduction -> Boxed -> Labeled -> WaitingShipment -> Complete)
    3. Replace with simple status badge + forward/backward buttons (same as slide-over StatusActionButtons)
    4. Add `<AuditTrail orderId={orderId} />` at the bottom
    5. Remove channel selection (removed per user decision)
    6. Remove payment method buttons (removed per user decision)
    7. Keep: shipping info, delivery info, items, voucher display, notes, WhatsApp templates
    8. Show creator name ("Created by {name}")

    **Final cleanup of orderConstants.ts:**
    Ensure no old status references remain. Clean up any TODO comments from Plan 01.

    **Update CHANGELOG.md:**
    Add Phase 14 entry:
    ```markdown
    ## Phase 14: Order QoL (2026-02-XX)

    ### Added
    - Kanban board with 6 columns (Draft, Awaiting Payment, Payment Received, Being Prepared, Awaiting Delivery, Complete)
    - Dedicated order creation page (/orders/new) with customer-first layout
    - Quick-tap due date pills (next 7 days with day names)
    - Customer search with autocomplete
    - Crystal and Goldfinch quick-add address buttons
    - Order audit trail (status change timeline with who/when/reason)
    - Creator attribution on all order cards
    - Expedited order badges and manual expedite action
    - Copy-to-new-order from cancelled orders
    - Forward/backward status transition buttons with validation

    ### Changed
    - Order statuses simplified from 12 to 7 (Draft, AwaitingPayment, PaymentReceived, BeingPrepared, AwaitingDelivery, Complete, Cancelled)
    - Order creation starts as Draft (was AwaitingPayment)
    - Kitchen view uses BeingPrepared status (replaces InProduction/Boxed/Labeled)
    - Sales channel and payment method fields removed from order creation

    ### Migration
    - Existing orders migrated: Confirmed->PaymentReceived, InProduction/Boxed/Labeled->BeingPrepared, WaitingShipment/WaitingPickup->AwaitingDelivery, CompleteShipped/PickedUp->Complete
    ```

    **Update API_REFERENCE.md:**
    Document new queries and mutations: listForKanban, getAuditTrail, moveForward, moveBackward, submitOrder, expediteOrder, copyFromCancelled, searchCustomers.

    **Update SCHEMA.md:**
    Document the new 7-status model, new fields (createdByUserId, expedited, kitchenEnteredAt), and orderEvents.userId.
  </action>
  <verify>
    `npm run build` passes with zero errors. `grep -r "InProduction\|Boxed\|Labeled\|WaitingShipment\|CompleteShipped\|WaitingPickup\|PickedUp" src/ --include="*.tsx" --include="*.ts" | grep -v node_modules | grep -v "\.test\."` returns empty (no old statuses in frontend source).
  </verify>
  <done>
    OrderDetail page uses new statuses with audit trail. All documentation updated. No old status references in codebase. `npm run build` succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of Kanban board + order creation + audit trail</name>
  <files>src/pages/OrderManager.tsx</files>
  <action>
    Human verifies the complete Order QoL system visually:
    1. Kanban board at /orders with 6 columns
    2. Order creation at /orders/new with customer search, due date pills, POS grid
    3. Slide-over panel with order details + audit trail
    4. Forward/backward status transitions
    5. Kitchen view with new statuses
  </action>
  <verify>
    1. Navigate to /orders -- verify 6-column Kanban board renders
    2. Click "+ New Order" -- verify redirects to /orders/new
    3. On /orders/new: type a customer name, verify autocomplete works
    4. Select due date pill -- verify day names show correctly
    5. Add items via POS grid -- verify running total updates
    6. Click "Submit Order" -- verify order moves to AwaitingPayment column, WhatsApp modal appears
    7. On Kanban: click an order card -- verify slide-over opens from right
    8. In slide-over: verify audit trail shows at bottom with status history
    9. Click forward button on AwaitingPayment order -- verify moves to PaymentReceived
    10. Click backward arrow -- verify confirmation modal with reason field
    11. Check /kitchen -- verify orders show with BeingPrepared status
    12. On mobile: verify horizontal scroll with snap scrolling
  </verify>
  <done>User approves complete Order QoL visual verification</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- No old status references in src/ or convex/ (except migration file)
- CHANGELOG.md has Phase 14 entry
- Audit trail renders in slide-over
- Kitchen view works with new statuses
- All 8 ORD requirements implemented
</verification>

<success_criteria>
- Kanban board with 6 columns displays correctly
- Order creation is a separate page with customer-first layout
- Due date pills show next 7 days
- Audit trail shows status change history
- Kitchen view updated for new statuses
- All documentation updated
- `npm run build` and `npm run type-check` pass
- User approves visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/14-order-qol/14-06-SUMMARY.md`
</output>
