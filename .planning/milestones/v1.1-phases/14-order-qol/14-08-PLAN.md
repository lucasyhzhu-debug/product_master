---
phase: 14-order-qol
plan: 08
type: execute
wave: 2
depends_on: ["14-07"]
gap_closure: true
files_modified:
  - src/components/orders/StatusActionButtons.tsx
  - src/components/orders/OrderSlideOver.tsx
  - src/components/orders/KanbanBoard.tsx
  - src/components/orders/KanbanColumn.tsx
  - src/components/orders/AuditTrail.tsx
  - src/components/orders/DueDatePills.tsx
  - src/pages/OrderCreate.tsx
  - src/pages/OrderManager.tsx
autonomous: false

must_haves:
  truths:
    - "StatusActionButtons catches ConvexError for insufficient stock and shows override dialog with reason input"
    - "Override dialog calls moveForward with skipStockCheck=true and overrideReason"
    - "Slide-over shows discounted total prominently with discount badge and struck-through full price"
    - "Slide-over header shows resolved creator name (from creatorName field), not 'admin'"
    - "AuditTrail renders 'created' event type as 'Order created' with creator name"
    - "DueDatePills shows expedite warning when Today or Tomorrow selected"
    - "OrderCreate delivery section: single text input with Crystal/Goldfinch quick-tap buttons, no Pickup/Delivery toggle"
    - "KanbanBoard columns fit iPhone 14 Pro (430px) viewport without cutoff"
    - "Page header is compact on mobile (no overflow)"
    - "WhatsApp modal auto-opens after order submit (Draft → AwaitingPayment)"
    - "npm run build passes"
  artifacts:
    - path: "src/components/orders/StatusActionButtons.tsx"
      provides: "Stock override dialog"
      contains: "override"
---

<objective>
Fix all frontend gaps from Phase 14 UAT: stock override dialog, discounted pricing in slide-over, expedite warning on date pills, delivery simplification, mobile responsiveness, WhatsApp auto-trigger, and audit trail creation event display.

Purpose: Resolve 6 frontend issues — GAP-04 (expedite warning), GAP-05 (delivery simplification), GAP-06 (mobile responsive), GAP-07 (discount display), GAP-08 (stock override UI), GAP-09 (WhatsApp auto-trigger) + wire GAP-01/02 frontend changes.
Output: Polished order management UX matching user expectations from visual verification.
</objective>

## Git Workflow
**Branch:** `feature/order-qol` (existing)
**Checkpoints:** Visual re-verification after all fixes

## Implementation Waves
### Wave 1: Frontend fixes [PARALLEL]
| Agent | Task | Files |
|-------|------|-------|
| react-ui-builder | Stock override dialog + discount display + audit trail | StatusActionButtons.tsx, OrderSlideOver.tsx, AuditTrail.tsx |
| react-ui-builder | Order creation UX + mobile responsive | OrderCreate.tsx, DueDatePills.tsx, KanbanBoard.tsx, KanbanColumn.tsx, OrderManager.tsx |

### Wave 2: Verification [SEQUENTIAL]
| Agent | Task |
|-------|------|
| code-auditor | Type check + pattern compliance |
| Bash | npm run build |

### Wave 3: Visual re-verification [CHECKPOINT]
| Agent | Task |
|-------|------|
| human | Re-verify all 9 gaps are resolved |

## Documentation Updates
- [ ] CHANGELOG.md (append gap fixes to Phase 14 section)

## Success Criteria
- [ ] `npm run build` passes
- [ ] All 9 UAT gaps resolved
- [ ] User approves visual re-verification

<tasks>

<task type="auto">
  <name>Task 1: Stock override dialog + discount display + audit trail + WhatsApp + slide-over creator</name>
  <files>
    src/components/orders/StatusActionButtons.tsx
    src/components/orders/OrderSlideOver.tsx
    src/components/orders/AuditTrail.tsx
  </files>
  <action>
    **IMPORTANT -- Use existing shadcn/ui components (NO custom primitives):**
    Available in `src/components/ui/`: Card, Badge, Button, Sheet, AlertDialog, Dialog, Skeleton, ScrollArea, Switch, Input, Label, Textarea, Tooltip, Separator, DropdownMenu, Tabs, Alert, Select, Checkbox, Table, Progress.

    **GAP-08: Stock override dialog in StatusActionButtons.tsx**

    When `moveForward` throws a ConvexError containing "Insufficient packaging stock":
    1. Catch the error, parse the shortage message
    2. Show a shadcn `AlertDialog` instead of a raw toast:
       - Title: "Insufficient Packaging Stock"
       - Description: parsed shortage details (line by line)
       - Textarea for override reason (required)
       - "Override & Continue" button (destructive variant) that calls `moveForward` with `skipStockCheck: true`, `overrideReason: reason`
       - "Cancel" button
    3. Only show override option if user has manager/admin role (check via useAuth)

    **GAP-07: Discounted price in OrderSlideOver.tsx**

    Find the pricing section in the slide-over. Mirror the Kanban card pattern (KanbanCard.tsx lines 128-141):
    - Calculate discount from `order.orderLevelDiscount` + `order.orderLevelDiscountType`
    - Show discounted total prominently in brand color
    - If discount > 0: show discount badge + struck-through full price
    - Replace the current `totalAmount` display

    **GAP-01 (frontend): Creator name in slide-over header**

    Change line 144 from:
    ```
    {order.createdBy && ` by ${order.createdBy}`}
    ```
    To use the resolved `creatorName` field returned by the backend (from Plan 07 fix):
    ```
    {order.creatorName && ` by ${order.creatorName}`}
    ```
    If the query doesn't return `creatorName`, fall back to `createdBy`.

    **GAP-02 (frontend): Audit trail "created" event in AuditTrail.tsx**

    Handle `event.type === "created"` in the timeline rendering:
    - Show "Order created" label (green badge like forward transitions)
    - Show creator name (event.userName) and timestamp
    - This should be the bottom-most (oldest) entry in the timeline

    **GAP-09: WhatsApp modal auto-trigger**

    In OrderCreate.tsx (or wherever submit is handled):
    After successful order creation (Draft → AwaitingPayment):
    - Navigate to OrderManager with a query param like `?whatsapp={orderId}`
    - In OrderManager.tsx, read the query param and auto-open the slide-over with WhatsApp modal shown
    - OR: show a success dialog with "Send WhatsApp" button that opens the template modal
    Choose the simpler approach that works with existing components.
  </action>
  <verify>
    `npm run build` passes. Override dialog appears in StatusActionButtons. Discount shows in slide-over. Audit trail shows creation event.
  </verify>
  <done>
    Stock override has proper dialog with reason. Slide-over shows discounted pricing and resolved creator name. Audit trail starts from Draft creation. WhatsApp modal triggered after submit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Order creation UX + mobile responsive</name>
  <files>
    src/pages/OrderCreate.tsx
    src/components/orders/DueDatePills.tsx
    src/components/orders/KanbanBoard.tsx
    src/components/orders/KanbanColumn.tsx
    src/pages/OrderManager.tsx
  </files>
  <action>
    **IMPORTANT -- Use existing shadcn/ui components (NO custom primitives).**

    **GAP-04: Expedite warning on DueDatePills**

    In `DueDatePills.tsx` (or wherever date pills are rendered in OrderCreate):
    - When Today or Tomorrow pill is selected, show a small text below the pills:
      `"Orders due today/tomorrow are auto-expedited when payment is received"`
    - Use muted text style (`text-xs text-amber-600` or similar)
    - No confirmation needed — just informational

    **GAP-05: Delivery section simplification in OrderCreate.tsx**

    Replace the current delivery section (DeliveryToggle + conditional address):
    1. Remove `DeliveryToggle` import and usage
    2. Remove `deliveryType` state
    3. Single section: label "Delivery / Pickup" with a text input
    4. Below the input: two quick-tap buttons:
       - "Crystal" → fills address with "Crystal - self pickup"
       - "Goldfinch" → fills address with "Goldfinch - self pickup"
    5. Buttons styled as small outline badges/buttons (like date pills)
    6. Remove the `deliveryType` from the mutation call args (just pass `deliveryAddress`)

    **GAP-06: Mobile responsive Kanban**

    In `KanbanBoard.tsx` and `KanbanColumn.tsx`:
    1. Change column `min-w-[85vw]` to `min-w-[calc(100vw-2rem)]` for mobile (ensures column fits with padding)
    2. Ensure `h-[calc(100vh-140px)]` accounts for page header — may need adjustment
    3. In `OrderManager.tsx` or wherever the page header is:
       - Make header compact on mobile: title + button on same line, smaller text
       - Ensure no horizontal overflow at 430px width
    4. Test that snap scrolling still works after width changes
  </action>
  <verify>
    `npm run build` passes. No DeliveryToggle in OrderCreate. DueDatePills shows expedite warning. Kanban columns don't overflow at 430px viewport.
  </verify>
  <done>
    Expedite warning on Today/Tomorrow pills. Delivery simplified to text input + quick buttons. Kanban fits iPhone 14 Pro viewport. Page header compact on mobile.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual re-verification of all 9 gap fixes</name>
  <files>src/pages/OrderManager.tsx</files>
  <action>
    Human re-verifies all 9 gaps are resolved:
    1. GAP-01: Slide-over shows actual user name (not "admin")
    2. GAP-02: Audit trail starts with "Order created" event
    3. GAP-03: Today/tomorrow order auto-expedites on payment
    4. GAP-04: Expedite warning shown when selecting Today/Tomorrow pill
    5. GAP-05: Delivery section is single text input with Crystal/Goldfinch buttons
    6. GAP-06: Kanban fits iPhone 14 Pro (430px) without cutoff
    7. GAP-07: Slide-over shows discounted total with struck-through price
    8. GAP-08: Stock override shows dialog (not raw toast) with reason input
    9. GAP-09: WhatsApp modal auto-appears after order submit
  </action>
  <verify>
    All 9 items checked and approved by user.
  </verify>
  <done>User approves all gap fixes — Phase 14 ready for merge.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- All 9 UAT gaps resolved per user visual verification
- No regressions in existing Kanban/order functionality
</verification>
