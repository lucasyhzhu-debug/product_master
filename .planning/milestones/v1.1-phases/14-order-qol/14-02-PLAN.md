---
phase: 14-order-qol
plan: 02
type: tdd
wave: 2
depends_on: ["14-01"]
files_modified:
  - convex/orders/__tests__/statusTransitions.test.ts
  - convex/orders/helpers/statusTransitions.ts
  - convex/orders/helpers/autoEntry.ts
autonomous: true

must_haves:
  truths:
    - "Forward transitions only allow valid next-status moves (Draft->AwaitingPayment->PaymentReceived->BeingPrepared->AwaitingDelivery->Complete)"
    - "Backward transitions are validated (AwaitingPayment->Draft, PaymentReceived->AwaitingPayment, BeingPrepared->PaymentReceived, AwaitingDelivery->BeingPrepared, Complete->AwaitingDelivery)"
    - "Auto-entry logic correctly identifies orders crossing the 2-day threshold"
    - "Cancelled orders cannot be revived"
    - "Expedited orders enter BeingPrepared immediately regardless of due date"
    - "Orders sent back from kitchen while ≤2 days do NOT auto-re-enter (kitchenEnteredAt consumed)"
  artifacts:
    - path: "convex/orders/__tests__/statusTransitions.test.ts"
      provides: "Comprehensive test suite for status transitions, backward moves, cancellation, auto-entry"
      min_lines: 150
    - path: "convex/orders/helpers/autoEntry.ts"
      provides: "Auto-entry logic: shouldAutoEnterKitchen(order, now) pure function"
      exports: ["shouldAutoEnterKitchen"]
  key_links:
    - from: "convex/orders/__tests__/statusTransitions.test.ts"
      to: "convex/orders/helpers/statusTransitions.ts"
      via: "Tests import and validate transition maps"
      pattern: "isValidForwardTransition|isValidBackwardTransition"
    - from: "convex/orders/helpers/autoEntry.ts"
      to: "convex/orders/helpers/statusTransitions.ts"
      via: "Auto-entry uses status constants"
      pattern: "ALL_ORDER_STATUSES|PaymentReceived|BeingPrepared"
---

<objective>
TDD plan: Build and test the status transition state machine and auto-entry logic using RED-GREEN-REFACTOR cycles.

Purpose: Status transitions are the core business logic -- forward moves, backward reversals, cancellation rules, and auto-entry threshold all have edge cases that must be covered by tests before the UI depends on them.
Output: Tested, correct status transition helpers and auto-entry pure function.
</objective>

## Git Workflow
**Branch:** `feature/order-qol`
**Checkpoints:** None -- fully autonomous TDD

## Implementation Waves
### Wave 1: TDD Cycles [SEQUENTIAL]
| Agent | Task | Files |
|-------|------|-------|
| tdd-test-architect | RED: Write failing tests | convex/orders/__tests__/statusTransitions.test.ts |
| tdd-test-architect | GREEN: Implement auto-entry + fix transitions | convex/orders/helpers/autoEntry.ts, convex/orders/helpers/statusTransitions.ts |

## Documentation Updates
- [ ] CHANGELOG.md (tests added)

## Success Criteria
- [ ] `npm run test` passes all new tests
- [ ] Forward transition validation covers all 5 valid moves
- [ ] Backward transition validation covers all 5 valid reversals
- [ ] Auto-entry threshold logic tested with edge cases

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-order-qol/14-CONTEXT.md
@.planning/phases/14-order-qol/14-01-SUMMARY.md

@convex/orders/helpers/statusTransitions.ts
@convex/orders/__tests__/statusTransitions.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED - Write failing tests for status transitions and auto-entry</name>
  <files>
    convex/orders/__tests__/statusTransitions.test.ts
  </files>
  <action>
    Create comprehensive test file covering:

    **Forward transition tests:**
    - `isValidForwardTransition("Draft", "AwaitingPayment")` -> true
    - `isValidForwardTransition("AwaitingPayment", "PaymentReceived")` -> true
    - `isValidForwardTransition("PaymentReceived", "BeingPrepared")` -> true
    - `isValidForwardTransition("BeingPrepared", "AwaitingDelivery")` -> true
    - `isValidForwardTransition("AwaitingDelivery", "Complete")` -> true
    - `isValidForwardTransition("Draft", "PaymentReceived")` -> false (skip not allowed)
    - `isValidForwardTransition("Complete", "Draft")` -> false (terminal)
    - `isValidForwardTransition("Cancelled", "Draft")` -> false (terminal)

    **Backward transition tests:**
    - `isValidBackwardTransition("AwaitingPayment", "Draft")` -> true
    - `isValidBackwardTransition("PaymentReceived", "AwaitingPayment")` -> true
    - `isValidBackwardTransition("PaymentReceived", "Draft")` -> true (allowed per user decision)
    - `isValidBackwardTransition("BeingPrepared", "PaymentReceived")` -> true
    - `isValidBackwardTransition("AwaitingDelivery", "BeingPrepared")` -> true
    - `isValidBackwardTransition("Complete", "AwaitingDelivery")` -> true
    - `isValidBackwardTransition("Draft", "AwaitingPayment")` -> false (wrong direction)
    - `isValidBackwardTransition("Cancelled", "Draft")` -> false (cancelled cannot revert)

    **Cancellation tests:**
    - `canCancelOrder({ status: "Draft" })` -> true
    - `canCancelOrder({ status: "AwaitingPayment" })` -> true
    - `canCancelOrder({ status: "PaymentReceived" })` -> true
    - `canCancelOrder({ status: "BeingPrepared" })` -> true
    - `canCancelOrder({ status: "AwaitingDelivery" })` -> true
    - `canCancelOrder({ status: "Complete" })` -> false (terminal)
    - `canCancelOrder({ status: "Cancelled" })` -> false (already cancelled)

    **Kitchen visibility tests:**
    - `computeIsKitchenVisible("BeingPrepared")` -> true
    - `computeIsKitchenVisible("Draft")` -> false
    - `computeIsKitchenVisible("PaymentReceived")` -> false
    - `computeIsKitchenVisible("Complete")` -> false

    **Auto-entry threshold tests** (import `shouldAutoEnterKitchen` from autoEntry.ts):
    - Order due in 3 days, status PaymentReceived, no kitchenEnteredAt -> false (>2 days)
    - Order due in 2 days, status PaymentReceived, no kitchenEnteredAt -> true (<=2 days, threshold crossed)
    - Order due in 1 day, status PaymentReceived, no kitchenEnteredAt -> true
    - Order due today, status PaymentReceived, no kitchenEnteredAt -> true
    - Order due in 2 days, status PaymentReceived, HAS kitchenEnteredAt -> false (threshold already consumed)
    - Order due in 2 days, status AwaitingPayment -> false (not in PaymentReceived)
    - Order due in 2 days, status BeingPrepared -> false (already in kitchen)
    - Order with no dueDate -> false
    - Expedited order (expedited=true, status PaymentReceived) -> true regardless of due date

    Use `describe`/`it` blocks organized by category. Import from `../helpers/statusTransitions` and `../helpers/autoEntry`.
  </action>
  <verify>
    `npm run test -- convex/orders/__tests__/statusTransitions.test.ts 2>&1 | tail -20` should show failures (RED phase).
  </verify>
  <done>
    Test file exists with 25+ test cases covering forward, backward, cancellation, kitchen visibility, and auto-entry. All tests fail because autoEntry.ts doesn't exist yet and transition maps may be incomplete.
  </done>
</task>

<task type="auto">
  <name>Task 2: GREEN - Implement auto-entry logic and fix transition helpers</name>
  <files>
    convex/orders/helpers/autoEntry.ts
    convex/orders/helpers/statusTransitions.ts
    convex/orders/helpers/index.ts
  </files>
  <action>
    **Create autoEntry.ts:**
    Pure function (no Convex ctx dependency) for testability:

    ```typescript
    interface AutoEntryOrder {
      status: string;
      dueDate?: number;
      kitchenEnteredAt?: number;
      expedited?: boolean;
    }

    export function shouldAutoEnterKitchen(order: AutoEntryOrder, now: number): boolean {
      // 1. Must be in PaymentReceived status
      if (order.status !== "PaymentReceived") return false;
      // 2. Expedited orders always enter
      if (order.expedited) return true;
      // 3. Must have a due date
      if (!order.dueDate) return false;
      // 4. If kitchenEnteredAt is set, threshold already consumed (sent back from kitchen)
      if (order.kitchenEnteredAt) return false;
      // 5. Check if ≤2 days before due date
      const twoDaysMs = 2 * 24 * 60 * 60 * 1000;
      const timeUntilDue = order.dueDate - now;
      return timeUntilDue <= twoDaysMs;
    }
    ```

    **Update statusTransitions.ts if needed:**
    Ensure `FORWARD_TRANSITIONS`, `BACKWARD_TRANSITIONS`, `isValidForwardTransition`, `isValidBackwardTransition` are correctly implemented per Plan 01. If Plan 01 already created these, this task validates they pass all test cases.

    Key rules:
    - PaymentReceived can go backward to BOTH AwaitingPayment AND Draft (user decision)
    - Cancelled is terminal -- no forward or backward from Cancelled
    - Complete is terminal for forward, but can go backward to AwaitingDelivery

    **Update helpers/index.ts:**
    Add re-export: `export { shouldAutoEnterKitchen } from "./autoEntry";`

    Run tests until ALL pass. If any test reveals a bug in the transition maps from Plan 01, fix the transition maps.
  </action>
  <verify>
    `npm run test -- convex/orders/__tests__/statusTransitions.test.ts` -- ALL tests pass (GREEN phase).
  </verify>
  <done>
    All 25+ tests pass. Auto-entry logic correctly handles threshold crossing, consumed thresholds, expedited orders, and wrong-status orders. Forward/backward transition validation is complete and tested.
  </done>
</task>

</tasks>

<verification>
- `npm run test -- convex/orders/__tests__/statusTransitions.test.ts` passes with 0 failures
- autoEntry.ts exports `shouldAutoEnterKitchen` pure function
- statusTransitions.ts exports `isValidForwardTransition` and `isValidBackwardTransition`
</verification>

<success_criteria>
- All status transition tests pass
- Auto-entry pure function handles all edge cases
- No Convex ctx dependency in auto-entry (pure function, fully testable)
</success_criteria>

<output>
After completion, create `.planning/phases/14-order-qol/14-02-SUMMARY.md`
</output>
