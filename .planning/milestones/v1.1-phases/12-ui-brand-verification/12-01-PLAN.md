---
phase: 12-ui-brand-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/UI_BRAND_REFERENCE.md
autonomous: true

must_haves:
  truths:
    - "Brand reference doc covers Kanban board pattern with column layout, card styling, and drag-drop affordance guidance"
    - "Brand reference doc covers dashboard summary header pattern with stat card grid, icon usage, and responsive stacking"
    - "Brand reference doc covers calendar grid pattern with day cell styling, holiday/weekend highlights, and outlet-first navigation"
    - "Brand reference doc confirms existing guidance (teal brand, Inter typography, dark mode) applies unchanged to v1.1 pages"
  artifacts:
    - path: "docs/UI_BRAND_REFERENCE.md"
      provides: "Complete v1.1 brand guidance"
      contains: "Kanban Board"
  key_links: []
---

<objective>
Review and update the UI Brand Reference doc to cover three new UI patterns introduced in v1.1: Kanban board (Phase 14), dashboard summary header (Phase 15), and calendar grid (Phase 16).

Purpose: Ensure all v1.1 implementers have clear visual guidance for new page types, preventing ad-hoc styling decisions that drift from the teal brand.
Output: Updated `docs/UI_BRAND_REFERENCE.md` with three new component pattern sections.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/UI_BRAND_REFERENCE.md
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit existing brand doc and add v1.1 component patterns</name>
  <files>docs/UI_BRAND_REFERENCE.md</files>
  <action>
Read `docs/UI_BRAND_REFERENCE.md` in full. Confirm all existing guidance (color palette, typography, spacing, dark mode, etc.) remains current and accurate for v1.1. No existing sections need changes -- v1.1 is additive.

Then add three new subsections inside the `## Component Patterns` section, AFTER the existing "Icons" subsection and BEFORE the `## Dark Mode` section:

**1. Kanban Board (for Phase 14 Order QoL)**

Add a `### Kanban Board` subsection with guidance for:
- **Container:** Horizontal-scrolling wrapper (`overflow-x-auto`) with `snap-x snap-mandatory` on mobile, standard horizontal scroll on desktop
- **Columns:** Each column is a vertical card stack. Header uses `text-sm font-semibold text-muted-foreground` with a count badge. Column width: `min-w-[280px] w-[320px]` on desktop, `min-w-[85vw] snap-center` on mobile
- **Column background:** `bg-muted/30` with `rounded-lg` and `p-3`
- **Cards:** Use standard `<Card>` with `shadow-sm`, `rounded-md`. Clickable cards get `hover:shadow-md cursor-pointer` transition. Card content uses standard spacing (`p-3 space-y-2`)
- **Status indicators:** Use the existing domain-specific status colors (not new colors). Each column header can have a subtle left border accent in the status color
- **Empty columns:** Show `text-muted-foreground text-sm` centered message, no empty state icon (too heavy for a column)
- **Dark mode:** All works automatically via CSS variables. Column bg uses `bg-muted/30` which resolves correctly in both modes
- **Responsive:** On mobile, columns snap-scroll horizontally. On `md:+`, show 3-4 columns visible with horizontal scroll for overflow

**2. Dashboard Summary Header (for Phase 15 Kitchen Overhaul)**

Add a `### Dashboard Summary Header` subsection with guidance for:
- **Container:** Full-width bar above main content. Uses `bg-card border-b border-border` with `p-4`
- **Layout:** CSS grid: `grid grid-cols-2 md:grid-cols-4 gap-3` for stat cards
- **Stat cards:** Each stat is a mini-card with `bg-background rounded-md p-3 border border-border`. Contains: icon (Lucide, `h-4 w-4 text-brand` or `text-muted-foreground`), label (`text-xs text-muted-foreground font-medium`), value (`text-lg font-bold text-foreground`), optional delta/subtext (`text-xs text-muted-foreground`)
- **Emphasis:** The primary KPI (e.g., "remaining balls") uses `text-brand` for its value instead of `text-foreground`
- **Dark mode:** Standard -- all via CSS variables
- **Responsive:** 2 columns on mobile, 4 on `md:+`. Values use `text-lg` not larger to stay compact

**3. Calendar Grid (for Phase 16 K3Mart Cockpit)**

Add a `### Calendar Grid` subsection with guidance for:
- **Container:** Standard `<Card>` wrapper with `overflow-x-auto` for narrow screens
- **Layout:** 7-column CSS grid: `grid grid-cols-7 gap-px bg-border` (gap-px + bg-border creates 1px grid lines)
- **Day headers:** `text-xs font-medium text-muted-foreground text-center py-2 bg-muted/50`
- **Day cells:** `bg-card p-2 min-h-[80px]` for content area. Date number in `text-xs font-medium` top-left corner
- **Today highlight:** `ring-2 ring-brand/50` on the cell, date number gets `bg-brand text-white rounded-full w-6 h-6 flex items-center justify-center`
- **Weekend cells:** `bg-muted/20` subtle background tint
- **Holiday cells:** `bg-amber-50 dark:bg-amber-950/20` with a small holiday indicator dot
- **Cell content:** Product quantities shown as compact pills: `text-xs bg-muted rounded-sm px-1.5 py-0.5`
- **Outlet tabs:** Use standard shadcn `Tabs` component above the grid. Active tab uses brand accent
- **Dark mode:** Weekend/holiday cell colors use dark-mode-aware values (as specified). Grid lines from `bg-border` resolve automatically
- **Responsive:** On mobile, show 3-day rolling view or horizontal scroll. Full 7-day grid on `md:+`

Finally, update the `*Last updated*` footer to `2026-02-15` and `*Phase: 12-ui-brand-verification, Plan 01*`.
  </action>
  <verify>
Verify the updated doc by checking:
1. `grep -c "Kanban Board" docs/UI_BRAND_REFERENCE.md` returns 1+
2. `grep -c "Dashboard Summary Header" docs/UI_BRAND_REFERENCE.md` returns 1+
3. `grep -c "Calendar Grid" docs/UI_BRAND_REFERENCE.md` returns 1+
4. `grep "Last updated" docs/UI_BRAND_REFERENCE.md` shows 2026-02-15
5. No hardcoded colors (gray-*, white, black) introduced in new sections -- all use semantic tokens
  </verify>
  <done>
UI_BRAND_REFERENCE.md contains three new component pattern subsections (Kanban Board, Dashboard Summary Header, Calendar Grid) with specific Tailwind classes, responsive guidance, and dark mode notes. Existing sections are unchanged. Doc footer updated to Phase 12.
  </done>
</task>

</tasks>

<verification>
1. `docs/UI_BRAND_REFERENCE.md` has new sections for Kanban Board, Dashboard Summary Header, Calendar Grid
2. All new sections use semantic color tokens (text-foreground, bg-muted, etc.) -- no hardcoded gray/white/black
3. Each new pattern includes: layout spec, responsive behavior, dark mode notes
4. Existing content is unmodified (teal palette, Inter typography, spacing scale, etc.)
5. Document footer updated to reflect Phase 12
</verification>

<success_criteria>
- Brand reference doc covers all three v1.1 UI patterns with actionable Tailwind guidance
- No hardcoded colors in new sections
- Each pattern has responsive and dark mode guidance
- Existing sections remain unchanged
- `npm run build` still passes (no code changes, but verify no accidental file modifications)
</success_criteria>

<output>
After completion, create `.planning/phases/12-ui-brand-verification/12-01-SUMMARY.md`
</output>
