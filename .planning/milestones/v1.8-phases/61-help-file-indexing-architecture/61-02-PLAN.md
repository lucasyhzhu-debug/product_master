---
phase: 61-help-file-indexing-architecture
plan: 02
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - .agent/skills/check-docs/SKILL.md
  - .agent/skills/update-docs/SKILL.md
autonomous: true
requirements:
  - HIDX-04
  - HIDX-05

must_haves:
  truths:
    - "/gsd:check-docs reads docs-manifest.json and reports stale vs up-to-date sections with commit summaries"
    - "/gsd:update-docs reads stale section file + git diff and proposes targeted edits"
    - "/gsd:update-docs --ack bumps lastReviewedCommit without editing section content"
    - "Both skills use existing gsd-tools.cjs for git commit operations"
  artifacts:
    - path: ".agent/skills/check-docs/SKILL.md"
      provides: "GSD skill for detecting stale tutorial sections"
      min_lines: 60
      contains: "lastReviewedCommit"
    - path: ".agent/skills/update-docs/SKILL.md"
      provides: "GSD skill for updating stale tutorial sections"
      min_lines: 80
      contains: "--ack"
  key_links:
    - from: ".agent/skills/check-docs/SKILL.md"
      to: ".planning/docs-manifest.json"
      via: "reads manifest to get sourceGlobs and lastReviewedCommit per mapping"
      pattern: "docs-manifest.json"
    - from: ".agent/skills/update-docs/SKILL.md"
      to: ".planning/docs-manifest.json"
      via: "reads manifest for stale sections, writes back updated lastReviewedCommit"
      pattern: "docs-manifest.json"
    - from: ".agent/skills/update-docs/SKILL.md"
      to: ".agent/skills/check-docs/SKILL.md"
      via: "reuses check-docs staleness detection as prerequisite step"
      pattern: "check-docs"
---

<objective>
Create two GSD slash-command skills: `/gsd:check-docs` to detect stale tutorial sections by comparing git history against the manifest, and `/gsd:update-docs` to propose and apply section-level edits with an `--ack` flag for acknowledge-without-change.

Purpose: These skills close the documentation drift loop. check-docs answers "what's stale?" and update-docs answers "how to fix it?" -- both operating at section granularity (~60 lines) instead of whole-guide granularity (~800 lines), keeping context cost low.

Output: Two SKILL.md files in `.agent/skills/` following established skill conventions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-help-file-indexing-architecture/61-CONTEXT.md
@.planning/phases/61-help-file-indexing-architecture/61-01-SUMMARY.md

@.agent/skills/handover/SKILL.md
@.agent/skills/validate-plan/SKILL.md

<interfaces>
<!-- docs-manifest.json schema (created by Plan 01) -->
```json
{
  "version": "1.0",
  "description": "Maps source file globs to tutorial section files for documentation drift detection",
  "guides": {
    "expenses": {
      "sections": ["overview", "submitting", "approving", "reimbursement", "payroll", "analytics", "pnl", "faq"]
    }
  },
  "mappings": [
    {
      "id": "expenses-overview",
      "sourceGlobs": ["convex/expenses/*.ts"],
      "docFile": "src/pages/guides/ExpenseGuide/OverviewSection.tsx",
      "guide": "expenses",
      "section": "overview",
      "lastReviewedCommit": "abc12345"
    }
  ]
}
```

<!-- Existing GSD skill pattern from handover/SKILL.md and validate-plan/SKILL.md -->
Skills use YAML frontmatter (name, description), markdown sections for Purpose, When to Use, Workflow steps with bash code blocks, and output formatting templates.

<!-- gsd-tools.cjs commit interface -->
```bash
node "./.claude/get-shit-done/bin/gsd-tools.cjs" commit "message" --files file1 file2
```
</interfaces>
</context>

## Git Workflow
**Branch:** `feature/help-file-indexing` (same branch as Plan 01)
**Checkpoints:** None (fully autonomous)

## Implementation Waves
### Wave 1: Skills [PARALLEL]
| Agent | Task | Files |
|-------|------|-------|
| executor | Task 1: Create /gsd:check-docs skill | `.agent/skills/check-docs/SKILL.md` |
| executor | Task 2: Create /gsd:update-docs skill | `.agent/skills/update-docs/SKILL.md` |

### Wave 2: Verification [SEQUENTIAL]
| Agent | Task |
|-------|------|
| code-auditor | Verify skill files follow established SKILL.md conventions |
| Bash | `grep -q "lastReviewedCommit" .agent/skills/check-docs/SKILL.md && grep -q "gsd-tools" .agent/skills/update-docs/SKILL.md` |

## Documentation Updates
- [ ] CHANGELOG.md (new /gsd:check-docs and /gsd:update-docs skills)

## Success Criteria
- [ ] Both SKILL.md files exist and follow established conventions
- [ ] check-docs skill references manifest, git log, staleness report format
- [ ] update-docs skill references --ack flag, manifest update, gsd-tools.cjs commit
- [ ] Commit messages with `#` characters are properly shell-quoted in skill instructions
- [ ] Skills are self-contained -- a Claude instance can execute them without clarification

<tasks>

<task type="auto">
  <name>Task 1: Create /gsd:check-docs skill</name>
  <files>.agent/skills/check-docs/SKILL.md</files>
  <action>
Create `.agent/skills/check-docs/SKILL.md` following the established skill pattern (see handover/SKILL.md, validate-plan/SKILL.md for conventions).

**Frontmatter:**
```yaml
---
name: check-docs
description: Detect stale tutorial sections by comparing git history against the docs manifest
---
```

**Sections to include:**

1. **Purpose**: Detect which tutorial sections have become stale because the source code they document has changed since last review.

2. **Usage**:
   ```
   /gsd:check-docs                    # Check all mapped sections
   /gsd:check-docs --guide expenses   # Check only one guide
   ```

3. **Workflow** (numbered steps with bash code blocks):

   **Step 1: Load manifest**
   ```bash
   cat .planning/docs-manifest.json
   ```
   Parse the JSON. Extract all mapping entries (or filter by `--guide` if provided).

   **Step 2: Check staleness per mapping**
   For each mapping entry:
   ```bash
   git log --oneline {lastReviewedCommit}..HEAD -- {sourceGlob1} {sourceGlob2} ...
   ```
   If output is non-empty, the section is STALE. Count commits and capture one-line summaries.

   Handle edge cases:
   - If `lastReviewedCommit` is not a valid commit (e.g., repo was rebased), warn and treat as stale
   - If source glob matches no files, warn as "no source files found for glob"
   - If `git log` returns nothing, section is UP TO DATE

   **Step 3: Generate report**
   Format output clearly:

   ```
   === Documentation Staleness Report ===

   Guide: Expenses & Reimbursement

   [STALE] overview (3 commits since last review)
     - abc1234 feat: add new expense status field
     - def5678 fix: receipt validation edge case
     - ghi9012 refactor: extract expense helpers
     Source: convex/expenses/*.ts
     Doc: src/pages/guides/ExpenseGuide/OverviewSection.tsx

   [OK] submitting (0 commits since last review)

   [STALE] approving (1 commit since last review)
     - jkl3456 feat: add DoA threshold for contractors
     Source: convex/expenses/mutations.ts, convex/expenses/fraudHelpers.ts
     Doc: src/pages/guides/ExpenseGuide/ApprovingSection.tsx

   ---
   Summary: 2 stale, 6 up-to-date (out of 8 sections)
   ```

   Claude's discretion: whether to include impact assessment (HIGH/MEDIUM/LOW) per stale section based on commit count and diff size.

   **Future:** Consider adding `--json` output mode for CI integration (not required in this phase).

4. **When to Use**:
   - After completing a phase that touched feature code documented in tutorials
   - Before starting a new milestone (check all guides)
   - When a user reports that a tutorial seems outdated
   - Periodically as part of documentation maintenance

5. **Notes**:
   - This is a READ-ONLY skill -- it does not modify any files
   - To fix stale sections, use `/gsd:update-docs`
   - Manifest location: `.planning/docs-manifest.json`
   - To validate manifest integrity: `npm run validate:docs-manifest`
  </action>
  <verify>
    <automated>cd "D:/Claude/Product Manager/product_master" && test -f .agent/skills/check-docs/SKILL.md && grep -q "lastReviewedCommit" .agent/skills/check-docs/SKILL.md && grep -q "git log" .agent/skills/check-docs/SKILL.md && echo "PASS"</automated>
  </verify>
  <done>
    - `.agent/skills/check-docs/SKILL.md` exists with frontmatter + 5 sections
    - Workflow describes loading manifest, running git log per mapping, generating report
    - Edge cases documented (invalid commit, empty glob, no changes)
    - Output format clearly distinguishes STALE vs OK sections with commit summaries
  </done>
</task>

<task type="auto">
  <name>Task 2: Create /gsd:update-docs skill</name>
  <files>.agent/skills/update-docs/SKILL.md</files>
  <action>
Create `.agent/skills/update-docs/SKILL.md` following established skill conventions.

**Frontmatter:**
```yaml
---
name: update-docs
description: Propose and apply section-level edits to stale tutorial sections, or acknowledge them as reviewed
---
```

**Sections to include:**

1. **Purpose**: Update stale tutorial sections by reading the affected section file + git diff of mapped source files, proposing targeted edits, and auto-committing after approval.

2. **Usage**:
   ```
   /gsd:update-docs                          # Interactive: show stale sections, pick which to update
   /gsd:update-docs --section expenses#overview   # Update a specific section directly
   /gsd:update-docs --ack expenses#overview       # Acknowledge as reviewed without changes
   /gsd:update-docs --ack-all                     # Acknowledge ALL stale sections (e.g., after pure refactor)
   ```

3. **Workflow** (numbered steps):

   **Step 1: Detect stale sections**
   Run the same staleness detection as `/gsd:check-docs`:
   ```bash
   cat .planning/docs-manifest.json
   ```
   For each mapping, check `git log {lastReviewedCommit}..HEAD -- {sourceGlobs}`.
   Filter to only stale sections.

   **Step 2: Select section (if no --section flag)**
   If no specific section provided, present numbered list of stale sections:
   ```
   Stale sections found:
   1. expenses#overview (3 commits)
   2. expenses#approving (1 commit)

   Which section to update? (number, or 'all' for batch):
   ```
   Wait for user input.

   **Step 3a: If --ack or --ack-all flag**
   Skip content editing. Just bump `lastReviewedCommit` to current HEAD:
   ```bash
   git rev-parse HEAD
   ```
   Update the mapping entry's `lastReviewedCommit` in `.planning/docs-manifest.json`.

   Commit with properly shell-quoted message (the `#` in `guide#section` must be quoted):
   ```bash
   node "./.claude/get-shit-done/bin/gsd-tools.cjs" commit 'docs(help): ack expenses#overview as reviewed (no content changes)' --files .planning/docs-manifest.json
   ```
   Use case: after a pure refactor phase that changed source code structure but not user-facing behavior.

   **Step 3b: If updating content**
   Read the affected section file (typically ~50-80 lines):
   ```bash
   cat {docFile}
   ```
   Read the git diff of source files since last review:
   ```bash
   git diff {lastReviewedCommit}..HEAD -- {sourceGlob1} {sourceGlob2}
   ```
   If diff is very large (>500 lines), use `git log --stat` instead to get a summary overview, then read only the most relevant changed files individually.

   Analyze what changed in the code and propose specific edits to the tutorial section:
   - New fields/features added -> add new StepCard or CalloutBox
   - Removed features -> remove or update references
   - Changed behavior -> update descriptions
   - Renamed concepts -> find-and-replace in section text

   Present proposed changes to user for review. Apply edits after approval.

   **Step 4: Update manifest + commit**
   After section file is updated (or acknowledged):
   ```bash
   git rev-parse HEAD
   ```
   Update `lastReviewedCommit` for the affected mapping entry in `.planning/docs-manifest.json`.

   Commit both the section file change and manifest update together (shell-quote the `#`):
   ```bash
   node "./.claude/get-shit-done/bin/gsd-tools.cjs" commit 'docs(help): update expenses#overview for new status field' --files {docFile} .planning/docs-manifest.json
   ```

   **Step 5: Repeat or finish**
   If more stale sections remain, offer to continue:
   ```
   Updated expenses#overview. 1 stale section remaining.
   Continue with expenses#approving? (y/n)
   ```

4. **Edge Cases**:
   - **Deleted source files**: If a source glob no longer matches any files, warn "source files may have been renamed or deleted -- manual review needed"
   - **Renamed sections**: If manifest references a docFile that doesn't exist, suggest running `npm run validate:docs-manifest` first
   - **Large diffs**: If git diff > 500 lines, switch to `git log --stat` summary + targeted file reads
   - **No stale sections**: Report "All sections up to date!" and exit

5. **When to Use**:
   - After `/gsd:check-docs` reports stale sections
   - After completing a feature phase that changes documented behavior
   - Use `--ack` after refactor-only phases (no behavior change = no content edits needed)
   - Use `--ack-all` after major refactors affecting many source files without behavior changes

6. **Notes**:
   - Each section file is ~50-80 lines -- small enough for precise, targeted edits
   - The git diff provides exact context of what changed -- no guessing
   - Commit message format: `docs(help): update {guide}#{section} for {brief reason}` -- always use single quotes around the message to prevent shell interpretation of `#`
   - Ack commit format: `docs(help): ack {guide}#{section} as reviewed (no content changes)` -- same quoting rule
  </action>
  <verify>
    <automated>cd "D:/Claude/Product Manager/product_master" && test -f .agent/skills/update-docs/SKILL.md && grep -q "\-\-ack" .agent/skills/update-docs/SKILL.md && grep -q "lastReviewedCommit" .agent/skills/update-docs/SKILL.md && grep -q "gsd-tools" .agent/skills/update-docs/SKILL.md && echo "PASS"</automated>
  </verify>
  <done>
    - `.agent/skills/update-docs/SKILL.md` exists with frontmatter + 6 sections
    - Workflow covers: stale detection, interactive picker, --ack flag, content editing, manifest bump, auto-commit
    - Edge cases documented (deleted files, renamed sections, large diffs)
    - Uses gsd-tools.cjs for commit operations (consistent with project conventions)
    - --ack and --ack-all flags documented for refactor-only scenarios
    - All commit message examples use single quotes to properly shell-quote the `#` in `guide#section`
  </done>
</task>

</tasks>

## Testing

### Automated Tests
- File existence and content grep checks in `<verify>` blocks
- Both SKILL.md files contain required keywords: `lastReviewedCommit`, `git log`, `gsd-tools`, `--ack`

### Manual Verification
- [ ] Read each SKILL.md and confirm a Claude instance could execute the workflow without ambiguity
- [ ] Verify bash code blocks in skills use correct shell quoting (single quotes around messages with `#`)
- [ ] Confirm skill structure matches existing skills (handover/SKILL.md, validate-plan/SKILL.md pattern)

<verification>
1. Both SKILL.md files exist in `.agent/skills/check-docs/` and `.agent/skills/update-docs/`
2. check-docs skill references `docs-manifest.json`, `git log`, staleness report format
3. update-docs skill references `--ack` flag, manifest update, `gsd-tools.cjs` commit
4. Both skills follow established SKILL.md conventions (frontmatter, Purpose, Usage, Workflow, When to Use)
5. Skills are self-contained instructions -- a Claude instance can execute them without clarification
6. Commit messages with `#` are single-quoted in all bash examples
</verification>

<success_criteria>
- /gsd:check-docs skill detects stale sections via git log against lastReviewedCommit
- /gsd:update-docs skill reads section file + git diff, proposes edits, commits with manifest bump
- --ack flag allows acknowledging sections as reviewed without content changes
- Both skills use existing gsd-tools.cjs for git operations
- Skills follow project's established SKILL.md pattern
- All bash examples properly shell-quote commit messages containing `#`
</success_criteria>

<output>
After completion, create `.planning/phases/61-help-file-indexing-architecture/61-02-SUMMARY.md`
</output>
