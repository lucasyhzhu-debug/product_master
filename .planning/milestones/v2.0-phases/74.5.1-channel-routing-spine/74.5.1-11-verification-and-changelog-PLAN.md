---
phase: 74.5.1
plan: 11
type: execute
wave: 4
title: Full verification gate + CHANGELOG
depends_on: [00, 01, 02, 03, 04, 05, 06, 07, 08, 09, 10]
files_modified:
  - docs/CHANGELOG.md
  - tests/e2e/channel-routing.spec.ts
  - tests/e2e/channel-audit.spec.ts
requirements: [R1, R2, R3, R4, R6, R7, R9]
autonomous: false

must_haves:
  truths:
    - "Every test file in Wave 0 is now GREEN (unit + integration + E2E)."
    - "`npm run type-check` passes."
    - "`npm run build` succeeds (no bundle cap breaches)."
    - "`npm run lint` + `npm run lint:convex` pass (no dynamic imports)."
    - "`npx playwright test tests/e2e/channel-*.spec.ts` PASS (E2E specs un-skipped)."
    - "`grep -r processGofoodSales` returns >=1 result (STILL present — retires in 74.5.2)."
    - "`grep -r gofood_sale convex/` returns >=1 result (STILL present — retires in 74.5.2)."
    - "CHANGELOG.md updated with full 74.5.1 entry per CLAUDE.md §Documentation Updates rule."
    - "Production-smoke gate: admin pages render + flag toggles work + schema pushed."
  artifacts:
    - path: "docs/CHANGELOG.md"
      provides: "74.5.1 release notes"
      contains: "74.5.1"
  key_links:
    - from: "docs/CHANGELOG.md"
      to: "all prior 74.5.1-*-SUMMARY.md files"
      via: "summary content aggregation"
      pattern: "74.5.1"
---

## Git Workflow

**Branch:** `feature/74.5.1-channel-routing-spine`
**Checkpoints:** `checkpoint:human-verify` gate at end — merge approval.

<objective>
Run the full verification battery. Confirm every requirement (R1, R2, R3, R4, R6, R7, R9) has green tests. Update CHANGELOG. Gate merge on human approval.

**Purpose:** Close the phase. Ship.

**Output:** CHANGELOG entry + green test battery + merge-ready branch.
</objective>

<execution_context>
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/workflows/execute-plan.md
@D:/Claude/Product Manager/product_master/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74.5.1-channel-routing-spine/74.5.1-SPEC.md
@CLAUDE.md
@docs/CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 11.1: Un-skip E2E specs + run full test suite</name>
  <files>
    tests/e2e/channel-routing.spec.ts,
    tests/e2e/channel-audit.spec.ts
  </files>
  <read_first>
    tests/e2e/channel-routing.spec.ts (Wave 0 scaffolds — currently skipped),
    tests/e2e/channel-audit.spec.ts,
    src/pages/ChannelRoutingManager.tsx (Plan 09 output),
    src/pages/ChannelAuditWorkbench.tsx (Plan 10 output)
  </read_first>
  <action>
**Step 1:** Flip `test.describe.skip(...)` to `test.describe.serial(...)` (or `test.describe(...)` if no serialization needed) in both E2E files. Complete the test bodies to actually exercise the pages built in Plans 09 + 10.

For `channel-routing.spec.ts`, actual test bodies:
- T-R7.1 CRUD: admin login → navigate → create rule → assert row appears → edit → assert update → delete → assert removal.
- T-R7.2 Duplicate validation: create rule → attempt duplicate → assert inline error.
- T-R7.3 Admin gate: visit as unauth → assert redirect.

For `channel-audit.spec.ts`:
- T-R6.E2E.1: seed one item per type → navigate → assert 5 tab counts ≥ 1.
- T-R6.E2E.2: resolve unmapped_sku → count decrements.
- T-R6.E2E.3: click Run full audit → toast appears.
- T-R6.E2E.4: assert tab shows audit-blocked status when issues exist.

Use Playwright fixtures + admin-login helper that already exists in `tests/e2e/` (pattern-match from existing admin specs — likely something like `loginAsAdmin(page)` or similar).

**Step 2:** Run the full battery:
```
npm run type-check
npm run lint
npm run lint:convex  # custom rule blocking dynamic imports
npm run build
npm run test          # vitest unit + integration
npx playwright test tests/e2e/channel-routing.spec.ts tests/e2e/channel-audit.spec.ts
```

All must PASS. If any fail, stop and fix before proceeding to Task 11.2.

**Step 3:** Confirm retirement markers are ABSENT (74.5.1 does NOT retire these — retires in 74.5.2). Verify they still exist:
```
grep -c "processGofoodSales" convex/productInventory/mutations.ts  # expect >= 1
grep -c "gofood_sale" convex/schema.ts  # expect 1
```
  </action>
  <acceptance_criteria>
    - `grep -c "test.describe.skip" tests/e2e/channel-routing.spec.ts` returns 0 (un-skipped).
    - `grep -c "test.describe.skip" tests/e2e/channel-audit.spec.ts` returns 0.
    - `npm run type-check` exits 0.
    - `npm run lint` exits 0.
    - `npm run lint:convex` exits 0 (if the custom rule exists — check package.json).
    - `npm run build` exits 0.
    - `npm run test` exits 0 (all vitest tests pass).
    - `npx playwright test tests/e2e/channel-routing.spec.ts tests/e2e/channel-audit.spec.ts` exits 0.
    - `grep -c "processGofoodSales" convex/productInventory/mutations.ts` returns >= 1 (NOT retired — 74.5.2 concern).
    - `grep -c 'v.literal("gofood_sale")' convex/schema.ts` returns 1 (NOT dropped — 74.5.2 concern).
    - `grep -c 'v.literal("channel_sale")' convex/schema.ts` returns 1 (new — added).
  </acceptance_criteria>
  <verify>
    <automated>npm run type-check && npm run build && npm run test && npx playwright test tests/e2e/channel-routing.spec.ts tests/e2e/channel-audit.spec.ts</automated>
  </verify>
  <done>
    Full battery green. All Wave 0 tests PASS (unit/integration/E2E).
  </done>
</task>

<task type="auto">
  <name>Task 11.2: Update CHANGELOG.md with phase 74.5.1 entry</name>
  <files>docs/CHANGELOG.md</files>
  <read_first>
    docs/CHANGELOG.md (existing format — follow top-of-file pattern),
    .planning/phases/74.5.1-channel-routing-spine/74.5.1-01-SUMMARY.md (if exists),
    ... all 74.5.1-*-SUMMARY.md files produced by prior plans
  </read_first>
  <action>
Prepend a new section to `docs/CHANGELOG.md` under the most recent unreleased/in-progress header. Format matches existing entries — terse bullet list per milestone sub-phase.

```markdown
## Phase 74.5.1 — Channel Routing Spine + Admin UI (2026-04-20)

Additive architectural spine for unified channel integration. All code lands behind 8-key
feature-flag map (`productInventorySettings.channelDeductionEnabled`, all default OFF).
No prod behavior change — ship-dark contract per CONTEXT D-10. Cutover + flag flips are 74.5.2.

### Schema additions
- 3 new tables: `channelRouting`, `channelAuditReports`, `channelAuditIssues`
- `externalRevenueItems.inventoryDeductedAt` (optional idempotency key)
- `productInventoryTransactions`: new `channel_sale` literal + `source` + `externalRef` fields + `by_source_externalRef` index
- `productInventoryTransactions.gofood_sale` literal PRESERVED (retires in 74.5.2)
- `productInventorySettings.channelDeductionEnabled` (optional object, 8 source keys)
- `externalSyncLogs.itemsDeducted` + `itemsSkipped` (R9)

### New helpers (convex/productInventory/)
- `channelRouting.ts` — 5-tier `resolveChannelRoute` + admin CRUD (R2)
- `channelSale.ts` — `processChannelSaleInternal` + `buildEventFromRow` (reuses Phase 78 substitution + stockTracker) (R4)
- `channelAudit.ts` — `detectAuditIssuesForItem` (pure, cheap) + `runFullAudit` (internal action, all 5 types) (R6)
- `channelAuditMutations.ts` — resolveAuditIssue / dismissAuditIssue / triggerFullAudit + list queries
- `channelFlags.ts` — `getChannelDeductionFlags` + `setChannelDeductionFlag` admin mutations

### saveRevenueItems — atomic dispatch hook (R3)
- `saveRevenueItemsImpl` now reads `channelDeductionEnabled[source]`; dispatches `processChannelSaleInternal` for eligible items when flag ON.
- Public `saveRevenueItems` return shape (`Id[]`) preserved for backward compat (`bigsellerOrders/mutations.ts:288-292`).
- Atomicity contract: deduction throw rolls back revenue + deduction together.

### Adapter refactors (R1, R9)
- `gobizAdapter`, `bigsellerAdapter`, `internalAdapter`, `k3martAdapter`, `grabfoodAdapter` all export `ChannelAdapter` — satisfying the unified contract.
- `convex/integrations/_shared/channelAdapter.ts` + `channelSaleEvent.ts` — new canonical types.
- GoFood `processGofoodSales` coexists in parallel (retires in 74.5.2).
- BigSeller preserves `platform === "shopee" || "tiktok"` emit gate at `sync.ts:804` (Pitfall 3).
- Internal adapter preserves Phase 80.2 existence-based guard at `adapter.ts:150-156` verbatim.
- K3Mart transitions from parent-only to parent+child shape (RESEARCH Caveat 1). Analytics reconciled in `convex/reports/unitEconomics.ts`.
- Grabfood ships as stub (`normalize()` throws `Not implemented` per D74.5.1-L5).
- Consignment `createSettlement` signature grows optional `items` arg (preserves `collapseRevenuePeriod` helper).

### Admin UIs
- `/admin/channel-routing` → `ChannelRoutingManager.tsx` — CRUD routing rules + Resolution Preview panel + Seed-from-outlets button.
- `/admin/channel-audit` → `ChannelAuditWorkbench.tsx` — 5 issue-type tabs + resolve/dismiss flows.
- `/admin/product-inventory-settings` → `ProductInventorySettings.tsx` — 8-key flag map. `bigseller`/`grabfood`/`internal` permanently disabled per D74.5.1-L1/L2.
- Shared components: `SourceBadge`, `ResolutionPreviewPanel`, `ChannelFlagRow`, `AuditIssueTypeBadge`.
- All routes `<ProtectedRoute roles={["admin"]}>`; all mutations `requireRole(ctx, token, ["admin"])`.

### Migrations
- `convex/migrations/channelRoutingSeed.ts` — one-shot idempotent seed from `externalOutlets.linkedStorageLocationId`. Admin-triggerable via ChannelRoutingManager button.

### Tests
- Wave 0 TDD: 11 new test files scaffold all R1-R7 requirements.
- All unit + integration tests green post-implementation.
- E2E specs: `tests/e2e/channel-routing.spec.ts` (R7), `tests/e2e/channel-audit.spec.ts` (R6) — full CRUD + resolution round-trip.
- Regression fixtures directory: `tests/fixtures/channel-regression/` (prod payload capture deferred to 74.5.2 per CONTEXT D-08).

### Deferred to 74.5.2
- Per-source backfill buttons in `/admin/unlinked-products-backfill`.
- Staged flag-flip rollout (Shopee → TikTok → K3Mart → GoFood last).
- `gofood_sale → channel_sale + source` transaction migration.
- `processGofoodSales` retirement.
- Schema cleanup (drop `gofood_sale` literal).
- Consignment per-product breakdown UI.
- `docs/CHANNEL_INTEGRATION.md` runbook.
- Historical backfill actions.

### Known limitations (74.5.1)
- `grep` confirms `processGofoodSales` + `gofood_sale` literal STILL present — 74.5.2 removes them.
- `channelDeductionEnabled.internal` permanent OFF per D74.5.1-L2 (reserveStockForOrderInternal remains authoritative internal stock path).
- Grabfood adapter stub — OAuth `orders:read` scope blocker unchanged.

### Files changed
See plan SUMMARYs under `.planning/phases/74.5.1-channel-routing-spine/*-SUMMARY.md`.
```

Do NOT touch any other CHANGELOG section.
  </action>
  <acceptance_criteria>
    - `grep -c "Phase 74.5.1" docs/CHANGELOG.md` returns >= 1.
    - `grep -q "Ship-dark contract\|ship-dark\|all default OFF" docs/CHANGELOG.md` passes.
    - `grep -q "channelDeductionEnabled" docs/CHANGELOG.md` passes.
    - `grep -q "Phase 80.2" docs/CHANGELOG.md` passes (retains prior history).
    - File length grows but no existing content deleted.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Phase 74.5.1" docs/CHANGELOG.md</automated>
  </verify>
  <done>
    CHANGELOG reflects the 74.5.1 release. Downstream 74.5.2 planner reads from here.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 74.5.1 complete: schema + helpers + atomic dispatch hook + 5 adapter refactors + 2 admin pages + 1 settings page + seed migration + CHANGELOG.
  </what-built>
  <how-to-verify>
    Merge-approval checklist — run through in order before merging to main:

    1. **All tests green:**
       ```
       npm run type-check && npm run lint && npm run build && npm run test && npx playwright test tests/e2e/channel-*.spec.ts
       ```
       All exit 0.

    2. **Prod-behavior preservation — flag-OFF verification:**
       - Start `npx convex dev` + `npm run dev`.
       - Run an existing E2E or manual sanity against a non-74.5 flow (e.g. `/k3mart-cockpit` sync trigger, `/order-history`).
       - Confirm no regressions in existing flows.

    3. **New admin surfaces render:**
       - `/admin/channel-routing` loads + CRUD works (Plan 09 checkpoint).
       - `/admin/channel-audit` loads + run audit + resolve works (Plan 10 checkpoint).
       - `/admin/product-inventory-settings` loads + 8 rows render with correct disabled states.

    4. **Schema pushed:**
       - `npx convex dashboard` — Tables tab shows `channelRouting`, `channelAuditReports`, `channelAuditIssues` with 4/2/4 indexes respectively.
       - `productInventoryTransactions` has both `channel_sale` and `gofood_sale` literals visible in schema.
       - `productInventorySettings` shows `channelDeductionEnabled` optional object field.

    5. **Retirement markers absent (74.5.2 scope):**
       - `grep -r "processGofoodSales" convex/` returns >=1 (still present).
       - `grep -r "gofood_sale" convex/schema.ts` returns 1 (still present).

    6. **CHANGELOG updated:**
       - `docs/CHANGELOG.md` has a new "Phase 74.5.1" section.

    7. **Branch status:**
       - `git status` — clean or only expected plan SUMMARY files staged.
       - `git log --oneline -20` — commits tell the story of the phase progression.

    8. **Final gate — merge approval:**
       - If all 7 checks pass AND `/triple-review` (separate invocation, not part of this plan) also approved, merge to main:
         ```
         git switch main && git pull
         git merge feature/74.5.1-channel-routing-spine --no-ff
         git push origin main
         ```
  </how-to-verify>
  <resume-signal>
    Type "approved — merge complete" once merged to main.
    Type "approved — hold merge" if approved but user wants to hold for /triple-review first.
    Otherwise describe failures.
  </resume-signal>
</task>

</tasks>

<verification>
```
npm run type-check
npm run lint
npm run build
npm run test
npx playwright test tests/e2e/channel-*.spec.ts
grep -c "Phase 74.5.1" docs/CHANGELOG.md
grep -c "processGofoodSales" convex/productInventory/mutations.ts
grep -c 'v.literal("gofood_sale")' convex/schema.ts
grep -c 'v.literal("channel_sale")' convex/schema.ts
```
</verification>

<success_criteria>
- [ ] All 7 R-IDs (R1, R2, R3, R4, R6, R7, R9) covered by passing tests
- [ ] E2E specs un-skipped + passing
- [ ] `npm run type-check` + `npm run build` + `npm run test` + `npx playwright test` all exit 0
- [ ] CHANGELOG.md has 74.5.1 entry
- [ ] Retirement markers still present (74.5.2 scope verified UNCHANGED)
- [ ] Human checkpoint passed (merge approval)
</success_criteria>

## Documentation Updates

- [x] CHANGELOG.md — THIS PLAN (mandatory per CLAUDE.md post-merge checklist)
- [ ] SCHEMA.md — deferred to 74.5.2 (per CONTEXT §Documentation to update in 74.5.2)
- [ ] API_REFERENCE.md — deferred to 74.5.2
- [ ] CHANNEL_INTEGRATION.md — new doc, deferred to 74.5.2

<output>
After completion, create `.planning/phases/74.5.1-channel-routing-spine/74.5.1-11-SUMMARY.md` + a top-level `.planning/phases/74.5.1-channel-routing-spine/74.5.1-PHASE-COMPLETE.md` that aggregates all 12 plan SUMMARYs into a single phase recap.
</output>
