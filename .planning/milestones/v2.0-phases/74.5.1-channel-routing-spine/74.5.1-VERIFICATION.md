---
phase: 74.5.1-channel-routing-spine
verified: 2026-04-20T17:00:00Z
status: human_needed
score: 11/11 roadmap truths verified (7 requirements + 4 contract truths)
overrides_applied: 2
overrides:
  - must_have: "channelAudit.test.ts all tests GREEN"
    reason: "Plan 00 TDD tests wrote shape {type, triggeredBy} expecting runFullAudit to take triggeredBy via action ctx; plan 04 impl uses {issueType} and runMutation ctx. This is TDD-scaffolding drift between plans 00 and 04 — documented as known deferral in verification_focus. Impl is correct per SPEC; tests need post-merge shape reconciliation. 7 of 7 failures are identical ctx-shape mismatches in one test file."
    accepted_by: "lucas"
    accepted_at: "2026-04-20T17:00:00Z"
  - must_have: "E2E specs un-skipped and running green on dev server"
    reason: "Plan 11 per-test `test.fixme` pending human testing on running dev server is intended state — describe blocks un-skipped (.describe.serial), bodies complete, individual tests marked fixme awaiting manual smoke. This is the plan-11 intended checkpoint:human-verify gate."
    accepted_by: "lucas"
    accepted_at: "2026-04-20T17:00:00Z"
human_verification:
  - test: "Admin navigates to /admin/channel-routing, sees empty routing table + Resolution Preview panel, clicks Create Rule, picks source=shopee + storage location, saves. Rule appears in table; Resolution Preview resolves (shopee, no outlet, no product) to that location."
    expected: "CRUD round-trip works; duplicate-combination produces inline validation error; delete removes row."
    why_human: "E2E test.fixme awaits manual smoke against a running dev server + seeded admin account; cannot be verified programmatically without spinning up Convex dev + playwright against live app."
  - test: "Admin opens /admin/product-inventory-settings, sees 8 flag rows (bigseller + grabfood DISABLED with tooltip; internal DISABLED per D74.5.1-L2). Flips shopee ON. Audit trail appears. Toggles back to OFF. Read-back shows shopee=false."
    expected: "8-key flag UI renders per UI-SPEC; toggles call setChannelDeductionFlag; persisted to productInventorySettings.channelDeductionEnabled."
    why_human: "Flag state UX + tooltip copy + 3-disabled-row semantics need visual confirmation; backend wiring is type-verified but visual regression is a human check."
  - test: "Admin navigates to /admin/channel-audit, clicks Run full audit. Summary grid shows 5-type counts. Clicks each tab, sees issue badges with legend tooltips. Selects 2 rows of same type, clicks bulk Resolve. Count decrements; resolution recorded."
    expected: "5-tab workbench renders; run-full-audit action returns counts; per-type resolution flows complete; bulk actions work; Dismiss AlertDialog with reason Textarea blocks confirm until reason typed."
    why_human: "E2E test.fixme awaits manual smoke; complex UI flow with bulk state + destructive confirmations needs visual verification."
  - test: "Admin runs seed migration (seedChannelRoutingFromOutlets action) via Convex dashboard or admin UI. Verifies channelRouting rows appear for every externalOutlet with linkedStorageLocationId. Re-runs: no-op (idempotent)."
    expected: "Seed populates channelRouting from externalOutlets; second run produces zero new rows."
    why_human: "Action must execute against live dev DB to verify idempotency + row creation; requires admin credentials + Convex dashboard access."
  - test: "Confirm flag-OFF = zero behavior change in prod. Run a Shopee/TikTok sync against dev env with no flag flipped. Verify externalRevenueItems rows are created WITH inventoryDeductedAt = undefined AND NO productInventoryTransactions channel_sale rows appear."
    expected: "Ship-dark contract (D-10): saveRevenueItems writes revenue items but does NOT dispatch processChannelSaleInternal when flag is absent/false."
    why_human: "Requires running an actual platform sync against dev and inspecting DB rows — type-check proves code compiles, runtime behavior verification needs live sync."
---

# Phase 74.5.1: Channel Routing Spine + Admin UI — Verification Report

**Phase Goal:** Additive architectural spine for unified channel integration. All code ships behind an 8-key feature-flag map (`productInventorySettings.channelDeductionEnabled`, all default OFF). Ship-dark contract per CONTEXT D-10 — no prod behavior change.

**Verified:** 2026-04-20T17:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (Roadmap + SPEC Acceptance Criteria)

| #   | Truth                                                                                  | Status       | Evidence                                                                                                                                                                                  |
| --- | -------------------------------------------------------------------------------------- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | R1 — ChannelAdapter contract + ChannelSaleEvent type; all 5 adapters implement          | VERIFIED     | `convex/integrations/_shared/channelAdapter.ts` (32 lines), `channelSaleEvent.ts` (50 lines). gobizAdapter, bigsellerAdapter, internalAdapter, k3martAdapter, grabfoodAdapter all exported with `ChannelAdapter<...>` type annotation. Stub path throws per D74.5.1-L5. |
| 2   | R2 — channelRouting table + resolveChannelRoute 5-tier precedence                      | VERIFIED     | `convex/productInventory/channelRouting.ts:38` exports CHANNEL_ROUTING_NOT_CONFIGURED. Lines 59/73/85/97/111 implement Tier 1/2/3/4/5. Schema has 4 indexes (by_source, by_source_outlet, by_source_product, by_source_outlet_product). All CRUD mutations gate `requireRole(..., ["admin"])`. |
| 3   | R3 — saveRevenueItems atomic revenue + deduction dispatch (flag-gated OFF)             | VERIFIED     | `convex/externalData/mutations.ts:801-930` reads `productInventorySettings.channelDeductionEnabled[source]`, coerces absent to false. `processChannelSaleInternal` + `buildEventFromRow` imported; `inventoryDeductedAt` patched on success. Entire mutation rolls back on throw (atomic internalMutation). `saveRevenueItemsWithCounts` wrapper exported for R9. |
| 4   | R4 — processChannelSaleInternal Layer-4 core reuses Phase 78 substitution + stockTracker | VERIFIED     | `convex/productInventory/channelSale.ts:19-20` imports `resolveSubstitutionPlan` + `createStockTracker`. `createdAt: event.occurredAt` (line 93, 111) preserves historical timestamps. `externalRef = externalTransactionId + (externalItemId ?? "")` (line 76). `transactionType: "channel_sale"` + `source: event.source` set. |
| 5   | R6 — Audit detection: 5 issue types + ChannelAuditWorkbench admin UI                   | VERIFIED (partial override) | `convex/productInventory/channelAudit.ts` emits all 5 literals (unmapped_sku, stale_mapping, malformed_item, duplicate_transaction, orphan_item) at lines 24-28 + issuesByType counters at 102-106. `detectAuditIssuesForItem` (cheap) + `runFullAudit` action (expensive) both exist. Schema has channelAuditReports + channelAuditIssues tables with 6 total indexes. Tests are red due to TDD scaffolding drift (override). UI: `src/pages/ChannelAuditWorkbench.tsx` (799 lines). |
| 6   | R7 — ChannelRoutingManager admin UI + CRUD                                             | VERIFIED (human pending) | `src/pages/ChannelRoutingManager.tsx` (669 lines) + `src/pages/ProductInventorySettings.tsx` (301 lines) + `src/hooks/convex/useChannelRouting.ts`. Routes registered in `src/App.tsx:496-527` with `<ProtectedRoute allowedRoles={["admin"]}>`. E2E specs exist but `test.fixme` (override #2 + human verification). |
| 7   | R9 — Sync logs gain deduction counters                                                 | VERIFIED     | Schema `externalSyncLogs.itemsDeducted` + `itemsSkipped` both optional (lines 1210-1211). Adapters (gobiz, bigseller, internal, k3mart) wire the values: gobiz:864, bigseller/sync:958, internal:284, k3mart emits through saveRevenueItems return values. |
| 8   | R8 — Feature-flag gate (channelFlags backend)                                          | VERIFIED     | `convex/productInventory/channelFlags.ts` (110 lines). `getChannelDeductionFlags` query + `setChannelDeductionFlag` mutation, both admin-gated. DEFAULT_FLAGS all-false 8-key object (lines 30-39). Absent row / absent field coerce to false. |
| 9   | D-01 honored: additive schema — `gofood_sale` preserved                                | VERIFIED     | `grep -c "gofood_sale" convex/schema.ts` returns 1. `grep -c "channel_sale"` returns 2. `grep -c "processGofoodSales" convex/productInventory/mutations.ts` returns 4 (not retired — per D-03). |
| 10  | D-07 honored: consignment uses collapseRevenuePeriod helper                            | VERIFIED     | `convex/consignment/mutations.ts:18` imports, lines 236/350/407 use. Per-item saveRevenueItems emit added at line 247 (optional items arg, line 183). |
| 11  | `npm run type-check` + `npm run build` pass                                            | VERIFIED     | Both run successfully. Type-check exit 0 (no output). Build: `✓ 4203 modules transformed. ✓ built in 24.47s`. All chunks within 150KB cap. |

**Score:** 11/11 roadmap truths verified (2 via override, 5 pending human smoke on running dev server).

### CONTEXT D-01 through D-10 Decisions

| Decision | Description                                              | Status    | Evidence                                                                                                 |
| -------- | -------------------------------------------------------- | --------- | -------------------------------------------------------------------------------------------------------- |
| D-01     | Additive schema, gofood_sale preserved                   | VERIFIED  | `gofood_sale` literal count in schema = 1; `processGofoodSales` occurrences in mutations.ts = 4.         |
| D-02     | Existing adapters behavior-preserving refactor           | VERIFIED  | BigSeller `saveRevenueItems` emit branch preserved (sync.ts:739); Internal Phase 80.2 existence-guard preserved; GoFood `processGofoodSales` coexists with new spine. |
| D-03     | Admin-only mutations + ProtectedRoute admin gates        | VERIFIED  | All routing CRUD mutations gate on `requireRole(..., ["admin"])`. All 3 admin routes in App.tsx use `<ProtectedRoute allowedRoles={["admin"]}>`. |
| D-04     | 5 audit issue types                                      | VERIFIED  | channelAudit.ts literals 24-28: unmapped_sku, stale_mapping, malformed_item, duplicate_transaction, orphan_item. Schema audit issues table matches. |
| D-05     | Triple-review required before merge                      | DEFERRED  | Per verification_focus note: triple-review deferred to post-merge (documented deviation).                |
| D-06     | K3Mart both paths through spine                          | VERIFIED  | k3martAdapter in `convex/integrations/k3mart/adapter.ts` + consignment `createSettlement` with optional items arg (mutations.ts:183+247) both routed through saveRevenueItems. |
| D-07     | Consignment collapsed-period via collapseRevenuePeriod   | VERIFIED  | collapseRevenuePeriod used at 3 sites in consignment/mutations.ts (236, 350, 407); period fields never set individually. |
| D-08     | K3Mart regression byte-identical on happy path           | VERIFIED (test) | `convex/productInventory/__tests__/k3martRegression.test.ts` exists (Wave 0 scaffold). Plan 08 confirmed childItems.length > 0 branch is byte-equivalent to else-branch synthesis on happy path. |
| D-09     | Consignment per-product breakdown UI                     | DEFERRED  | Stretch, explicitly 74.5.2 scope.                                                                         |
| D-10     | Ship-dark: all 8 flags default OFF                       | VERIFIED  | `channelDeductionEnabled` optional on schema; absent = false. saveRevenueItems reads flag with `flagMap?.[source] === true`. `DEFAULT_FLAGS` in channelFlags.ts = all false. |

### Required Artifacts

| Artifact                                                         | Expected                                       | Status     | Details                                                  |
| ---------------------------------------------------------------- | ---------------------------------------------- | ---------- | -------------------------------------------------------- |
| `convex/integrations/_shared/channelAdapter.ts`                  | R1 ChannelAdapter interface                    | VERIFIED   | 32 LOC; exports interface ChannelAdapter                 |
| `convex/integrations/_shared/channelSaleEvent.ts`                | R1 ChannelSaleEvent canonical type             | VERIFIED   | 50 LOC; exports ChannelSaleEvent                         |
| `convex/productInventory/channelRouting.ts`                      | R2 resolveChannelRoute + CRUD                  | VERIFIED   | 262 LOC; 5-tier + CHANNEL_ROUTING_NOT_CONFIGURED + admin-gated CRUD |
| `convex/productInventory/channelRoutingQueries.ts`               | R2 admin UI list/preview queries               | VERIFIED   | 156 LOC                                                  |
| `convex/productInventory/channelSale.ts`                         | R4 Layer-4 deduction core                      | VERIFIED   | 157 LOC; reuses Phase 78 substitution + stockTracker     |
| `convex/productInventory/channelAudit.ts`                        | R6 detectAuditIssuesForItem + runFullAudit     | VERIFIED   | 311 LOC; all 5 issue types emitted                       |
| `convex/productInventory/channelAuditMutations.ts`               | R6 audit CRUD mutations                        | VERIFIED   | 222 LOC; admin-gated resolve/dismiss/list                |
| `convex/productInventory/channelFlags.ts`                        | R8 flag get/set                                | VERIFIED   | 110 LOC; DEFAULT_FLAGS all false; admin-gated            |
| `convex/externalData/mutations.ts` (saveRevenueItems hook)       | R3 atomic dispatch                             | VERIFIED   | Lines 801-930 + saveRevenueItemsWithCounts wrapper       |
| `convex/integrations/gobiz/adapter.ts`                           | R1 gobizAdapter + normalize                    | VERIFIED   | `gobizAdapter: ChannelAdapter<GobizNormalizedBatch>` at line 90 |
| `convex/integrations/bigseller/adapter.ts`                       | R1 bigsellerAdapter + normalize                | VERIFIED   | `bigsellerAdapter` exported; normalize co-located         |
| `convex/integrations/internal/adapter.ts`                        | R1 internalAdapter + normalize + Phase 80.2 guard | VERIFIED | Line 74 `internalAdapter: ChannelAdapter<InternalRawBatch>` + lines 150-156 existence-based guard preserved |
| `convex/integrations/k3mart/adapter.ts`                          | R1 k3martAdapter + emit saveRevenueItems       | VERIFIED   | Line 1214 k3martAdapter; saveRevenueItems call at :660   |
| `convex/integrations/grabfood/adapter.ts`                        | R1 grabfoodAdapter stub                        | VERIFIED   | Line 859 grabfoodAdapter; normalize throws "Not implemented — requires orders:read OAuth scope" |
| `convex/consignment/mutations.ts`                                | D-06/D-07 consignment refactor                 | VERIFIED   | Optional items arg; saveRevenueItems emit; collapseRevenuePeriod preserved |
| `convex/migrations/channelRoutingSeed.ts`                        | Seed migration                                 | VERIFIED   | 174 LOC; seedChannelRoutingFromOutlets admin action; idempotent |
| `convex/schema.ts`                                               | Additive schema                                | VERIFIED   | 3 new tables, 3 modified tables; gofood_sale preserved   |
| `src/pages/ChannelRoutingManager.tsx`                            | R7 routing admin UI                            | VERIFIED (human smoke pending) | 669 LOC; route `/admin/channel-routing` with admin gate |
| `src/pages/ProductInventorySettings.tsx`                         | R7/R8 8-key flag UI                            | VERIFIED (human smoke pending) | 301 LOC; route `/admin/product-inventory-settings` |
| `src/pages/ChannelAuditWorkbench.tsx`                            | R6 audit workbench UI                          | VERIFIED (human smoke pending) | 799 LOC; route `/admin/channel-audit`   |

### Key Link Verification

| From                                                     | To                                                      | Via                                                 | Status   | Details                                                |
| -------------------------------------------------------- | ------------------------------------------------------- | --------------------------------------------------- | -------- | ------------------------------------------------------ |
| `convex/productInventory/channelSale.ts`                 | `convex/productInventory/substitution.ts`               | import resolveSubstitutionPlan                      | WIRED    | Line 19                                                |
| `convex/productInventory/channelSale.ts`                 | `convex/productInventory/stockTracker.ts`               | import createStockTracker                           | WIRED    | Line 20                                                |
| `convex/productInventory/channelSale.ts`                 | `convex/productInventory/channelRouting.ts`             | import resolveChannelRoute                          | WIRED    | Line 18                                                |
| `convex/externalData/mutations.ts`                       | `convex/productInventory/channelSale.ts`                | import processChannelSaleInternal + buildEventFromRow | WIRED  | Lines 11-12                                            |
| `convex/externalData/mutations.ts`                       | `convex/productInventory/channelAudit.ts`               | import detectAuditIssuesForItem                     | WIRED    | Line 14                                                |
| `convex/consignment/mutations.ts`                        | `convex/externalData/mutations.ts`                      | runMutation saveRevenueItems with items arg         | WIRED    | Lines 247-249                                          |
| `convex/integrations/k3mart/adapter.ts`                  | `convex/externalData/mutations.ts`                      | runMutation saveRevenueItems single-item per txn    | WIRED    | Line 660                                               |
| `src/pages/ChannelRoutingManager.tsx`                    | `convex/productInventory/channelRoutingQueries.ts`      | useQuery listRoutingRules                           | WIRED    | via useChannelRouting hook                             |
| `src/pages/ProductInventorySettings.tsx`                 | `convex/productInventory/channelFlags.ts`               | useQuery getChannelDeductionFlags                   | WIRED    | via useChannelRouting hook                             |
| `src/pages/ChannelAuditWorkbench.tsx`                    | `convex/productInventory/channelAuditMutations.ts`      | useMutation bindings                                | WIRED    | via useChannelAudit hook                               |
| `src/App.tsx`                                            | All 3 admin pages                                       | ProtectedRoute allowedRoles=["admin"]               | WIRED    | Lines 496-527                                          |

### Data-Flow Trace (Level 4)

| Artifact                                   | Data Variable                | Source                                             | Produces Real Data | Status                                 |
| ------------------------------------------ | ---------------------------- | -------------------------------------------------- | ------------------ | -------------------------------------- |
| `ChannelRoutingManager.tsx`                | routing rules                | `useQuery(listRoutingRules)` → DB                  | Yes (live DB)      | FLOWING                                |
| `ProductInventorySettings.tsx`             | 8 flag rows                  | `useQuery(getChannelDeductionFlags)` → DB+defaults | Yes (live DB)      | FLOWING (empty row = all-false default)|
| `ChannelAuditWorkbench.tsx`                | audit issues, reports        | `useQuery(listAuditIssues + listAuditReports)`     | Yes (live DB)      | FLOWING (may be empty until first runFullAudit) |
| `saveRevenueItems` (revenue + deduct)      | flag state                   | `ctx.db.query("productInventorySettings").first()` | Yes                | FLOWING                                |
| `processChannelSaleInternal`               | storage location             | `resolveChannelRoute` 5-tier                       | Yes (or throws)    | FLOWING                                |

### Behavioral Spot-Checks

| Behavior                                                 | Command                                                                | Result                    | Status |
| -------------------------------------------------------- | ---------------------------------------------------------------------- | ------------------------- | ------ |
| TypeScript compiles cleanly                              | `npm run type-check`                                                   | exit 0, no errors         | PASS   |
| Production build succeeds                                | `npm run build`                                                        | ✓ 4203 modules, 24.47s    | PASS   |
| Schema preserves gofood_sale                             | `grep -c "gofood_sale" convex/schema.ts`                               | 1                         | PASS   |
| Schema adds channel_sale                                 | `grep -c "channel_sale" convex/schema.ts`                              | 2                         | PASS   |
| processGofoodSales still present (retires in 74.5.2)     | `grep -c "processGofoodSales" convex/productInventory/mutations.ts`    | 4                         | PASS   |
| 8 channel flag keys present                              | Schema lines 1057-1068                                                 | all 8 keys present        | PASS   |
| 5 audit issue types in schema + impl                     | Schema 2329-2349 + channelAudit.ts literals                            | all 5 present             | PASS   |
| 4 routing table indexes                                  | Schema 2306-2309                                                       | by_source, by_source_outlet, by_source_product, by_source_outlet_product | PASS |
| Admin routes ProtectedRoute wired                        | App.tsx:496-527                                                        | 3 routes with allowedRoles=["admin"] | PASS |
| Vitest unit + integration tests (excluding deferral)     | `npm run test -- --run`                                                | 1682 pass / 2 skipped / 7 fail (all in channelAudit.test.ts TDD drift — override) | PASS (with override) |
| CHANGELOG entry present                                  | `grep -c "Phase 74.5.1" docs/CHANGELOG.md`                             | 1                         | PASS   |

### Requirements Coverage (REQ-IDs from PLAN frontmatter)

| Requirement | Source Plans                | Description                                              | Status    | Evidence                                                                                      |
| ----------- | --------------------------- | -------------------------------------------------------- | --------- | --------------------------------------------------------------------------------------------- |
| R1          | 00, 02, 06, 07, 11          | ChannelAdapter contract + ChannelSaleEvent type          | SATISFIED | 5/5 adapters (gobiz, bigseller, internal, k3mart, grabfood) implement interface              |
| R2          | 00, 01, 03, 08, 09, 11      | channelRouting table + 5-tier resolveChannelRoute        | SATISFIED | Schema + channelRouting.ts impl + admin UI CRUD                                              |
| R3          | 00, 01, 05, 11              | saveRevenueItems atomic revenue + deduction              | SATISFIED | mutations.ts:801-930 flag-gated dispatch; all-OFF default                                    |
| R4          | 00, 04, 11                  | processChannelSaleInternal Layer-4 core                  | SATISFIED | channelSale.ts reuses Phase 78; historical createdAt + externalRef set                       |
| R5          | (out of scope)              | processGofoodSales retirement                            | DEFERRED  | Explicitly 74.5.2 per CONTEXT D-03                                                            |
| R6          | 00, 01, 04, 10, 11          | Audit detection (5 types) + ChannelAuditWorkbench UI     | SATISFIED | 5 types emitted; UI page + hook wired; tests red (override)                                  |
| R7          | 00, 03, 09, 11              | ChannelRoutingManager admin UI                           | SATISFIED (human smoke pending) | Page + route; E2E test.fixme (override + human_needed)                  |
| R8          | (out of scope for explicit REQ list but delivered as D-10) | Historical backfill                   | DEFERRED  | Explicitly 74.5.2 per CONTEXT D-03 (backfill buttons on /admin/unlinked-products-backfill)   |
| R9          | 01, 06, 07, 11              | Sync logs deduction counters                             | SATISFIED | itemsDeducted + itemsSkipped on externalSyncLogs; wired in 4 adapters                        |

**No orphaned requirements.** R5 + R8 deferred to 74.5.2 per explicit scope.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `convex/productInventory/__tests__/channelAudit.test.ts` | multiple | TDD-scaffolding shape drift: `{type, triggeredBy}` vs impl `{issueType}` + runMutation ctx | Warning | 7 test failures — documented deferral, override applied |
| `convex/integrations/bigseller/__tests__/normalize.test.ts` | platform literal | `platform: string` vs union literal | Info | Latent drift per plan 11 SUMMARY; does not currently block build |
| `src/components/channelIntegration/AuditIssueTypeBadge.tsx` | react-refresh | only-export-components lint warning | Info | Polish-grade, non-blocking |
| `src/pages/ChannelRoutingManager.tsx` | useMemo deps | React hooks deps warning | Info | Polish-grade, non-blocking |
| `tests/e2e/channel-routing.spec.ts`, `channel-audit.spec.ts` | test.fixme | 8 fixmes pending UI smoke | Info | Intended per plan 11 — override applied |

No blockers. All phase-specific issues are either documented deferrals or non-blocking polish.

### Human Verification Required

5 items need human testing on a running dev server with admin credentials. See frontmatter `human_verification` array for detailed steps.

1. **CRUD round-trip on /admin/channel-routing** — Admin creates/edits/deletes routing rules; duplicate-combination validation fires.
2. **8-key flag UI on /admin/product-inventory-settings** — 8 rows render; 3 disabled-with-tooltip; toggle persists.
3. **Full audit workflow on /admin/channel-audit** — Run full audit, navigate 5 tabs, bulk resolve, dismiss-with-reason.
4. **Seed migration** — seedChannelRoutingFromOutlets populates channelRouting; re-run idempotent.
5. **Ship-dark contract** — Run Shopee/TikTok sync with flag OFF; verify zero productInventoryTransactions(channel_sale) rows + externalRevenueItems rows have inventoryDeductedAt=undefined.

### Gaps Summary

No blocking gaps. Two intentional deviations accepted via overrides:

1. **channelAudit.test.ts 7 failures** — TDD scaffolding shape drift between plan 00 tests and plan 04 impl. Impl is correct per SPEC R6 (all 5 issue types emitted, cheap/expensive split honored). Tests were authored against an earlier ctx/field-name shape. Override applied; reconciliation tracked as post-merge triage item.

2. **E2E specs test.fixme pending human smoke** — Plan 11 intended state per `checkpoint:human-verify` contract. describe blocks un-skipped, bodies complete, individual cases fixme-deferred to manual smoke against a running dev server.

R5 (processGofoodSales retirement) and R8 (historical backfill) are explicitly 74.5.2 scope per CONTEXT D-01/D-03 — not gaps.

Triple-review (D-05) was deferred to post-merge per documented deviation in verification_focus.

---

_Verified: 2026-04-20T17:00:00Z_
_Verifier: Claude (gsd-verifier, Opus 4.7 1M)_
