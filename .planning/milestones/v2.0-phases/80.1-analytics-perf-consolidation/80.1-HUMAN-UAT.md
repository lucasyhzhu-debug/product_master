---
status: pending
phase: 80.1-analytics-perf-consolidation
started: 2026-04-18
note: "Pre-merge items 1-5 run against dev environment. Post-deploy items 6-10 run on production after merge."
---

# Phase 80.1 HUMAN-UAT Checklist

## Pre-merge UAT (run against dev environment before PR merge)

### 1. Filter latency (dev)
expected: Open `/analytics` on local dev. Change the date range (30d → 7d). All 13 widgets refresh with no visible flicker. DevTools > Network shows exactly 3 Convex subscription names (kpiAndChannelSnapshot, timeSeriesSnapshot, skuSnapshot).
result: [pending]

### 2. R1 — axis label readability (dev)
expected: On every chart with a string X-axis (SkuParetoChart, UnitsByTypeStackedBars, TypeMixOverTime, WeekdayDualAxisChart, UnitsPerTxnByChannel, AovByChannel), hover every rotated label. Truncated labels (ending in `…`) reveal full text via Recharts tooltip title. NO silent truncation without recovery.
result: [pending]

### 3. R2 — tooltip contrast (dev)
expected: Hover every data point on every chart. Every tooltip has dark background (popover token) and light text. All values legible. Category colors appear ONLY as small colored squares next to value names.
result: [pending]

### 4. DayHourHeatmap render (dev)
expected: Heatmap shows WIB hour axis + day axis. Every cell shows a tooltip on hover with currency value (e.g. "Rp 1,2jt"). Cell text is legible against all cell colors (labelTextColor modifier auto-darkens).
result: [pending]

### 5. SkuChannelHeatmap render (dev)
expected: Heatmap shows SKU names on Y-axis (long SKU names truncated with `…`), channel names on X-axis. Hovering a cell reveals the FULL SKU name in the tooltip title line.
result: [pending]

## Post-deploy UAT (run on production after merge + deploy)

### 6. Filter latency on production data
expected: Open `/analytics` on production. Change date range (30d → 7d). All 13 widgets refresh smoothly. Network shows exactly 3 Convex queries.
result: [pending]

### 7. Channel filter
expected: Toggle GoFood off. Dashboard re-renders within 1 second. Still exactly 3 snapshot subscriptions observed.
result: [pending]

### 8. Granularity toggle
expected: Toggle volume-by-type granularity (day ↔ week). Only time-series widgets re-render; KPI + SKU widgets stay stable.
result: [pending]

### 9. Lazy-load verification (production)
expected: DevTools Network filtered to `.js`. Hard-reload `/dashboard`. NO `@nivo/*` or `vendor-nivo-*.js` downloaded. Navigate to `/analytics`. Nivo chunk NOW appears.
result: [pending]

### 10. Mobile / narrow viewport
expected: Narrow the browser window to ~375px width. Every chart either scrolls horizontally OR adapts labels gracefully. No chart silently drops ticks or clips labels against the plot edge.
result: [pending]
