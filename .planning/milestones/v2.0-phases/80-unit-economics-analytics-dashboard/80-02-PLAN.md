# Phase 80 — Plan 02: Staff Review Addendum (supersedes parent)

**This is a pointer to the canonical addendum.** The patches that supersede specific sections of `80-01-PLAN.md` live at:

- **ADDENDUM:** [`docs/superpowers/plans/2026-04-13-unit-economics-analytics-dashboard-ADDENDUM.md`](../../../docs/superpowers/plans/2026-04-13-unit-economics-analytics-dashboard-ADDENDUM.md)

**Read this AFTER** `80-01-PLAN.md`. Where the two disagree, the addendum wins.

## What the addendum delivers

### Critical fixes
1. **T1.5 (new)** — Add `by_completed_at` + `by_order_date` indexes on orders. Rewrites `loadFilteredData` to use `.withIndex()` bounded scans + per-order `by_order` fetch. Eliminates the 11× full-table-scan footprint.
2. **T1.6 (new)** — Migrate `convex/dispatchPlanner/queries.ts` off hardcoded `BIG_BALL`/`MID_BALL` to use `getProductionUnitsByTypePerProduct`. Backward-compat shape preserved (`bigBalls`/`midBalls` still returned) plus new `unitsByType` record.
3. **Revenue helpers** — New `convex/reports/revenueHelpers.ts` (`itemNetRevenue`/`itemGrossRevenue`/`itemDiscount`) using denormalized `lineTotal`. Global patch replaces all manual `quantity * unitPrice - discountAmount` math across T2–T7.
4. **T14.5 (new)** — 3 frontend smoke tests: `KpiRow`, `AnalyticsFilterBar`, `WeekdayDualAxisChart`.

### Improvements
- Extend `src/lib/platformColors.ts` with display-channel aggregates → all chart colors via `getPlatformPalette(channel).hex`
- Reuse `getWibComponents` from `convex/lib/periodRange.ts` (no inline `getJakartaDate`)
- Adaptive `pickBucketCount(spanMs)` for `channelMomentum` (7/13/12 by span)
- T13 nav must edit BOTH `Header.tsx` AND `MobileBottomNav.tsx`
- `DayHourHeatmap` fragment-key fix (`React.Fragment key={row}`)
- T16 Step 8 explicit squash-merge PR step

### Expanded backend tests (5 new)
- `volumeByType` Hazelnut series regression
- `channelEconomics` take-rate math
- `skuPareto` Other bucket + monotonic cumulativePct
- `rollingTrend` 7d windowing
- `dispatchPlanner` Hazelnut regression

## Total test coverage after addendum
- 10 backend integration tests
- 3 frontend component smoke tests
