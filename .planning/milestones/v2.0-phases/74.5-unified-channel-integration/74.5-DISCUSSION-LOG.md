# Phase 74.5: Unified Channel Integration Architecture - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-19
**Phase:** 74.5-unified-channel-integration
**Mode:** discuss (interactive, AskUserQuestion)
**Areas discussed:** Phase decomposition, K3Mart coexistence, Rollout sequencing, Backfill execution control (+ 4 follow-up questions)

---

## Round 1 — Gray area selection

Claude presented four gray areas grounded in spec + codebase state. User was offered all four as multi-select; answered each as single-select in Round 2 (AskUserQuestion permission was initially denied, then re-enabled; plain-text summary shown first, then AskUserQuestion re-offered).

---

## Phase decomposition

| Option | Description | Selected |
|--------|-------------|----------|
| Split into 74.5.1 / .2 / .3 | Routing+UI → Adapter cutover → Backfill+retirement. Mirrors 80 → 80.1/80.2/80.3. (Recommended) | |
| Single 12-task phase | Ship all 12 tasks from existing superpowers plan in one phase. | |
| Two phases | 74.5.1 schema+adapters, 74.5.2 backfill+retirement. Middle ground. | ✓ |

**User's choice:** Two phases
**Notes:** User accepted the two-phase split but explicitly delegated the split-line decision to Claude ("you can decide how these two should work architecturally and execution wise the best risk and balance of effort so that it's balanced").

**Claude's discretion applied:** Chose "additive 74.5.1, cutover 74.5.2" split:
- 74.5.1 = all code lands behind OFF flags (100% additive, zero behavior change in prod)
- 74.5.2 = flag flips + per-source backfill + retirement + schema cleanup + reporting extension

Rationale: 74.5.1 is code-heavy but reviewable as one artifact because nothing in prod changes. 74.5.2 is ops-heavy but reversible per-channel, localizing regression risk.

---

## K3Mart coexistence

| Option | Description | Selected |
|--------|-------------|----------|
| Hybrid: reroute deduction only | Keep consignment write path untouched; only stock deduction through new layer. (Recommended) | |
| Full K3Mart refactor | Rewrite K3Mart adapter to emit ChannelSaleEvent; regression snapshot tests. Cleaner but higher risk. | ✓ |
| Exclude K3Mart from 74.5 | Handle separately in 74.5.4. Reduces scope but leaves K3Mart stock-drifting. | |

**User's choice:** Full K3Mart refactor
**Follow-up refinement:** Both sync adapter AND consignment settlement mutation route through saveRevenueItems + processChannelSaleInternal. User added optional stretch: extend consignment reporting to include breakdown of products sold + # transactions.

**Notes:** User went beyond the recommended "hybrid" — chose maximum unification on K3Mart. This raises regression risk (consignment settlements are weekly manager workflow), mitigated by mandatory snapshot regression tests pre/post refactor for both paths.

---

## Rollout sequencing

| Option | Description | Selected |
|--------|-------------|----------|
| Per-channel feature flag | Shopee → TikTok → BigSeller → K3Mart → GoFood last. Flag lets verify each before next cutover. (Recommended) | ✓ |
| Big-bang cutover | All 5 adapters flip simultaneously after audit passes. Simpler, higher blast radius. | |
| Shadow-write dual-path | Dual-write old + new for 7 days with diff reports. Safest but doubles write cost. | |

**User's choice:** Per-channel feature flag
**Notes:** Matches recommendation. GoFood last because it's the only channel with working legacy deduction; processGofoodSales retires atomically when GoFood flag flips ON.

---

## Backfill execution control

| Option | Description | Selected |
|--------|-------------|----------|
| Per-source buttons | `Backfill shopee` / `Backfill tiktok` / etc. Matches per-channel flag rollout. (Recommended) | ✓ |
| Single "backfill all" button | Simplest UI; admin loses incremental verification. | |
| Date-range + source picker | Most flexible; more UI surface to build. | |

**User's choice:** Per-source buttons
**Notes:** Matches recommendation.

---

## Round 2 — Follow-up questions

## Two-phase split line

| Option | Description | Selected |
|--------|-------------|----------|
| 74.5.1 full refactor + flags OFF; 74.5.2 flips + backfill | All code lands behind OFF flags in 74.5.1; 74.5.2 flips + runs backfill + retires. (Recommended) | |
| 74.5.1 schema+routing only; 74.5.2 adapters+backfill | Keeps 74.5.1 pure additive, no adapter changes. | |
| 74.5.1 adapters+cutover; 74.5.2 audit+backfill | Ships live path first, data cleanup second. | |

**User's choice:** Claude's discretion
**User's rationale:** "you can decide how these two should work architecturally and execution wise the best risk and balance of effort so that it's balanced"
**Claude's decision:** Option A (full refactor + flags OFF in 74.5.1; cutover + backfill in 74.5.2). Captured in CONTEXT D-01 through D-05.

---

## Full K3Mart refactor scope

| Option | Description | Selected |
|--------|-------------|----------|
| K3Mart sync adapter only | Consignment settlement path untouched. (Recommended) | |
| K3Mart sync + consignment both | Unifies all K3Mart revenue writes. Higher risk. | ✓ |
| Define during planning | Defer to planner / research phase. | |

**User's choice:** Both sync + consignment
**User's note:** "yes do both and we can extend the consignment reporting to also include breakdown of products sold and # transactions (optional)"
**Claude's response:** Added consignment reporting extension as stretch goal in 74.5.2 (D-09). Mandatory snapshot regression tests for both K3Mart paths (D-08).

---

## Feature flag implementation

| Option | Description | Selected |
|--------|-------------|----------|
| `productInventorySettings` table fields | Reactive, UI-togglable, cleanup = remove fields. (Recommended) | ✓ |
| Convex env var | JSON string; requires `npx convex env set` to flip. | |
| Hardcoded const in adapter files | Simplest cleanup but no runtime flexibility. | |

**User's choice:** `productInventorySettings` table fields
**Notes:** Matches recommendation. New optional object field `channelDeductionEnabled`.

---

## Backfill UI location

| Option | Description | Selected |
|--------|-------------|----------|
| Inside `/admin/channel-audit` workbench footer | One admin page for the full data-hygiene workflow. (Recommended) | |
| Dedicated `/admin/channel-backfill` page | Separate admin page for backfill. | |
| Inside existing `/admin/unlinked-products-backfill` | Extends Phase 80.2 admin page. Consolidates all backfill actions. | ✓ |

**User's choice:** Inside existing `/admin/unlinked-products-backfill`
**Notes:** User chose the consolidation option (matches the `feedback_ui_for_db_ops.md` memory note about preferring admin UI for DB ops). Extends the Phase 80.2 page with a new "Channel deduction backfill (Phase 74.5)" section.

---

## Claude's Discretion

- Two-phase split line — delegated by user; chose "additive 74.5.1, cutover 74.5.2".
- Exact layout of the new section inside `/admin/unlinked-products-backfill` (banner copy, button grouping, confirm dialogs).
- Placement of feature-flag toggle UI inside Channel Routing Manager.
- Internal structure of shared helpers.
- Error-message wording for `CHANNEL_ROUTING_NOT_CONFIGURED`.

## Deferred Ideas

- Pricing consolidation (999.6), SKU resolver service (999.7), channel onboarding doc (999.8) — already deferred in spec; carried into CONTEXT deferred section.
- Global audit gate (from spec §8.3) — downgraded to per-source gate to match per-channel rollout philosophy.
- Direct-order reservation/fulfillment lifecycle — out of scope.
- Layer 5 journal engine changes — out of scope.
- `externalOutlets.linkedStorageLocationId` drop — deprecated in 74.5.1, dropped in 999.6 cleanup.
