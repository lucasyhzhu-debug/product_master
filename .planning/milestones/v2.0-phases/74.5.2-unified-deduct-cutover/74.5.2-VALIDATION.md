---
phase: 74.5.2
slug: unified-deduct-cutover
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-20
---

# Phase 74.5.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Derived from RESEARCH.md §Validation Architecture and CONTEXT D74.5.2-L1..L15.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.0.18 + convex-test (backend integration), Vitest + @testing-library/react (frontend), Playwright (E2E) |
| **Config file** | `vitest.config.ts` (backend + component tests); `playwright.config.ts` (E2E) |
| **Quick run command** | `npm run test -- --run <touched-path>` |
| **Full suite command** | `npm run test -- --run` (Vitest) + `npm run build` (tsc + vite) |
| **Estimated runtime** | ~25s full vitest; ~25s full build; ~3–5 min full playwright E2E |

---

## Sampling Rate

- **After every task commit:** `npm run type-check && npm run test -- --run <touched-file-tests>` — must be green before commit
- **After every plan wave:** `npm run test -- --run` (full Vitest) + `npm run build` — green before wave merge
- **Before `/gsd-verify-work 74.5.2`:** Full Vitest + full build + grep gates (processGofoodSales==0, gofood_sale==0 post-migration) + manual post-flip parity check (SC4 query)
- **Max feedback latency:** ~50s (full vitest + type-check + build)

---

## Per-Task Verification Map

> **Note:** Plan IDs are placeholders until the planner emits PLAN.md files. This table lists the verification expected per deliverable.

| Deliverable | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|-------------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| `channelAudit.test.ts` 4 `t.action()` tests go green | 01 | 0 | Test infra | — | N/A | unit | `npm run test -- --run convex/productInventory/__tests__/channelAudit.test.ts` | ✅ | ⬜ |
| BigSeller `normalize.test.ts` platform literal tightened to `ExternalSource` | 01 | 0 | D74.5.2-L2 | — | N/A | unit | `npm run test -- --run convex/integrations/bigseller/__tests__/normalize.test.ts` | ✅ | ⬜ |
| `backfill.test.ts` idempotency test (run twice, 0 double-deductions) | 02 | 1 | R8, SC3 | Double-deduction via replay | Idempotent: `inventoryDeductedAt IS NULL` existence check | integration | `npm run test -- --run convex/productInventory/__tests__/backfill.test.ts` | ❌ W0 | ⬜ |
| `backfill.test.ts` timestamp preservation test | 02 | 1 | R8 | — | `createdAt === revenue.transactionDate`, not `Date.now()` | integration | same as above | ❌ W0 | ⬜ |
| `backfill.test.ts` linkedMenuProductId=null skipped (no inventoryDeductedAt patch) | 02 | 1 | R8 | Silent mapping drops | `inventoryDeductedAt` only patched on `result.deducted === true` | integration | same as above | ❌ W0 | ⬜ |
| `backfill.test.ts` admin-only gate | 02 | 1 | — | Spoofed-token backfill trigger | `requireRole(..., ["admin"])` | integration | same as above | ❌ W0 | ⬜ |
| `by_source_deductedAt` index added to schema | 02 | 1 | D74.5.2-L12 | — | N/A | schema | `grep -c "by_source_deductedAt" convex/schema.ts` == 1 | — | ⬜ |
| `gofoodSaleToChannelSale.test.ts` chunked migration completes | 03 | 2 | D74.5.2-L6 | Time-limit exhaustion | `paginationOpts` + `isDone`/`continueCursor` loop | integration | `npm run test -- --run convex/migrations/__tests__/gofoodSaleToChannelSale.test.ts` | ❌ W0 | ⬜ |
| Migration writes `source: "gobiz"` NOT `"gofood"` (type-check) | 03 | 2 | Landmine #1 | Type-unsafe literal injection | Compile-time enforcement | type-check | `npm run type-check` | — | ⬜ |
| Migration preserves `gofoodOrderRef` on migrated rows | 03 | 2 | D74.5.2-L6 | Legacy UI display regression | Back-compat with `TransactionLogPanel` | integration | gofoodSaleToChannelSale.test.ts | ❌ W0 | ⬜ |
| Migration self-heals on re-run (partial completion recovers) | 03 | 2 | D74.5.2-L6 | Re-entrancy | `by_type` index narrows to `gofood_sale` only; re-run picks up where left off | integration | gofoodSaleToChannelSale.test.ts | ❌ W0 | ⬜ |
| `getSettlementItems` query admin-only | 04 | 3 | D74.5.2-L7 | Non-admin data leak | `requireRole(..., ["admin","manager"])` | integration | `npm run test -- --run convex/consignment/__tests__/getSettlementItems.test.ts` | ❌ W0 | ⬜ |
| `getSettlementItems` returns items with enriched menuProduct name | 04 | 3 | D74.5.2-L7 | — | `Promise.all` enrichment does not drop null menuProduct | integration | same as above | ❌ W0 | ⬜ |
| `SettlementFormDialog` item-row inputs validate `sum(items.totalPrice) === revenue.totalAmount` | 04 | 3 | D74.5.2-L7 | Silent amount drift | Existing `assertItemsMatchTotalRevenue` backend check | unit | component test TBD | — | ⬜ |
| `processGofoodSales` function body deleted | 05 | 4 | R5, SC2 | — | N/A | grep | `grep -c "processGofoodSales" convex/` == 0 | — | ⬜ |
| Both gobiz adapter call sites (`:840, :1105`) removed | 05 | 4 | R5 | — | N/A | grep | `grep -c "processGofoodSales" convex/integrations/gobiz/` == 0 | — | ⬜ |
| Post-migration: zero `gofood_sale` rows in `productInventoryTransactions` | 05 | 4 | SC2 | — | N/A | DB query | Admin runs `SELECT count(*) WHERE transactionType="gofood_sale"` → 0 (post-migration) | — | ⬜ |
| Runbook `docs/CHANNEL_INTEGRATION.md` exists with required sections | 06 | 5 | D74.5.2-L9 | — | N/A | grep | `grep -c "^## " docs/CHANNEL_INTEGRATION.md` covers (onboarding, runbook, triage, backfill ops, rollback) | ❌ NEW | ⬜ |
| `AuditIssueTypeBadge` react-refresh lint clean | 06 | 5 | D74.5.2-L3 | — | N/A | lint | `npm run lint` exits 0 for that file | — | ⬜ |
| `ChannelRoutingManager` useMemo deps lint clean | 06 | 5 | D74.5.2-L3 | — | N/A | lint | `npm run lint` exits 0 for that file | — | ⬜ |
| Full build + type-check + test suite green | all | — | SC6 | — | N/A | gate | `npm run build && npm run test -- --run` | — | ⬜ |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Plan 01 (Wave 0) must create these test scaffolds BEFORE any feature plans ship:

- [ ] **Fix** `convex/productInventory/__tests__/channelAudit.test.ts` — 4 failing `t.action(internal.*)` calls. Diagnose module-resolution glob/loader bug per research hypotheses (order: glob suffix, eager flag, version parity, fall-back to direct-handler invocation).
- [ ] **Create** `convex/productInventory/__tests__/backfill.test.ts` — idempotency, timestamp preservation, null-menuProduct skip, admin-only gate.
- [ ] **Create** `convex/migrations/__tests__/gofoodSaleToChannelSale.test.ts` — chunked migration, self-heal on re-run, `source: "gobiz"` literal, `gofoodOrderRef` preservation.
- [ ] **Create** `convex/consignment/__tests__/getSettlementItems.test.ts` — query correctness, admin/manager gate, enrichment.
- [ ] **Tighten** `convex/integrations/bigseller/__tests__/normalize.test.ts` — `platform: string → ExternalSource` union (D74.5.2-L2).

Component tests (`SettlementFormDialog` item-row validation, `UnlinkedProductsBackfill` section rendering) scaffolded in Wave 0 OR deferred to the plan that creates the component, planner's discretion per existing pattern.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Per-channel post-flip parity: `SUM(externalRevenueItems.quantity WHERE source=X) == -SUM(productInventoryTransactions.quantity WHERE source=X AND transactionType="channel_sale")` | SC4 | Requires running actual platform sync against dev/prod environment with admin credentials; not reproducible in unit tests without extensive fixtures | Admin runs Convex dashboard queries post-flip per `docs/CHANNEL_INTEGRATION.md` verification script; snapshot before flip + 24h after; compare |
| Post-flip inventory delta matches physical stock-outcome | D-13 | Physical inventory check cannot be automated | Admin physically counts stock at start/end of soak period, compares to digital ledger delta |
| Admin UI: "Backfill Shopee" button enabled despite K3Mart audit issues | D-17 | UI state check against live DB with seeded audit issues | Admin seeds K3Mart audit issue via `runFullAudit` → visits `/admin/unlinked-products-backfill` → confirms Shopee button NOT greyed |
| Admin UI: K3Mart bundle flip toggles both `k3mart` + `consignment` | D74.5.2-L14 | Requires clicking UI affordance and inspecting DB state | Admin clicks "Flip K3Mart (both paths)" → `channelDeductionEnabled.k3mart === true && channelDeductionEnabled.consignment === true` |
| GoFood post-deploy flip window: zero under-deduction lingers after admin clicks flag ON | D74.5.2-L5 | Deploy-flip timing is operational | Admin watches sync log timestamps pre/post-flip for any missing deductions in window |
| Consignment settlement form: admin can add item rows, validation prevents grandtotal mismatch | D74.5.2-L7 | Form UX verification | Admin creates new settlement with 3 items where sum ≠ total → form shows inline error; fixes sum → submits |
| Consignment settlement breakdown UI: expandable "Products sold" subsection renders items + transaction count | D74.5.2-L7 | UI visual verification | Admin opens OutletCard for a post-74.5.2 settlement; confirms Products sold expand shows rows |
| Runbook rollback procedure tested against a non-prod channel | D74.5.2-L10 | Destructive rollback on live data not unit-testable | Admin flips Shopee OFF on dev → confirms runbook cases A/B/C steps resolve correctly |
| 74.5.1 HUMAN-UAT: 5 items naturally resolved during cutover | D74.5.2-L11 | Inherits from 74.5.1 HUMAN-UAT | Tracked via `/gsd-verify-work 74.5.1` post-74.5.2-merge |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify OR Wave 0 dependency OR documented manual-only entry
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (enforced via planner task ordering)
- [ ] Wave 0 covers all ❌ W0 references in Per-Task Map
- [ ] No watch-mode flags in test commands (all use `--run`)
- [ ] Feedback latency < 60s per commit (vitest + type-check target)
- [ ] `nyquist_compliant: true` set in frontmatter after Wave 0 merge + green baseline confirmed

**Approval:** pending
