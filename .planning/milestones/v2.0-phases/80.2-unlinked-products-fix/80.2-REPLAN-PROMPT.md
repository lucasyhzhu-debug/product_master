# Phase 80.2 Replan Prompt

**Use:** Paste the block below into a fresh Claude Code session (after `/clear`) to trigger a full GSD v1.36.0 replan of Phase 80.2 using graphify + pattern-mapper + staffreview capabilities.

**Why this exists:** v2 of the PLAN was drafted before the 1.34.2 → 1.36.0 GSD upgrade. The new version ships `gsd-pattern-mapper`, `graphify` integration, and the consolidated `quad_review` gate — we want the replan to exercise all three instead of settling for v2's manual-review-only rigor.

---

## Prompt (copy from here, including the code block fence)

```
/gsd:plan-phase 80.2 --reviews

Phase 80.2 (Unlinked Products Fix — K3Mart + Direct) is a gap-closure for Phase 80 addressing two independent bugs that route K3Mart and Direct revenue into an "(Unlinked)" bucket in Unit Economics reports. Scaffold + v2 staff-reviewed PLAN already exist — **replan with full v1.36.0 GSD rigor** (graphify + gsd-pattern-mapper + plan-checker + staffreview). Do not discard v2 structural decisions.

**Source artifacts (READ FIRST — treat as ground truth):**
- `.planning/phases/80.2-unlinked-products-fix/80.2-CONTEXT.md` — phase boundary, root-cause decisions, fix architecture, canonical refs
- `.planning/phases/80.2-unlinked-products-fix/80.2-01-PLAN.md` — v2 staff-reviewed single-file plan (baseline — refine, don't rewrite)
- `.planning/debug/unlinked-products-k3mart-direct.md` — debug investigation / PRD with evidence table and confirmed root cause
- `graphify-out/GRAPH_REPORT.md` + `graphify-out/wiki/` — authoritative code map (use for coupling claims instead of raw greps; flag any touched file that appears on the god-node list)
- `.planning/phases/80-unit-economics-analytics-dashboard/` — parent phase (builds `convex/reports/unitEconomics.ts` loader, the common funnel both bugs flow through)
- `.planning/phases/79-shopee-item-level-revenue/` — closest neighbor (same `externalRevenueItems` pattern for a different source; good analog for backfill design)

**Replan mandates:**
1. Preserve v2 structural decisions: additive return type for `applyRetroactiveProductMappingImpl` (no call-site breakage), paginated-primary backfill, shared helpers (`getK3MartMappingBySku`, `hasExternalRevenueItems`), Wave 4.4 user-checkpoint before prod mutations.
2. Spawn `gsd-pattern-mapper` → produce `PATTERNS.md` mapping every new file to its closest existing analog in the codebase, with the analog named and the match reasoning:
   - `convex/integrations/k3mart/helpers.ts`
   - `convex/externalData/helpers.ts`
   - `backfillInternalRevenueItems` admin mutation
   - 5 new test files under `__tests__/`
3. Verify line-number references in v2 plan (`mutations.ts:458-596`, `adapter.ts:557-576`, `internal/adapter.ts:126`, etc.) are still accurate post-GSD-1.36.0. Flag drift.
4. Decide: keep as single PLAN or split into 4 per-wave plan files (`80.2-01` through `80.2-04`) with independent `wave: N` frontmatter and `depends_on` cross-wave links. Default to splitting unless pattern-mapper finds cross-wave helpers that make split messier than unified.
5. Run through full gate chain — plan-checker → plan-bounce (if configured) → staffreview (step 12.6) → requirements-coverage gate. No skip flags.
6. Ensure every file in the plan has a corresponding `must_haves.key_links` entry so plan-checker can verify referenced artifacts exist.

**Output:** refined PLAN file(s), `PATTERNS.md`, `RESEARCH.md` (if graph analysis surfaces novel constraints). Planning only — zero code changes.
```

---

## Notes for the next session

- The v2 PLAN is already committed (`4822f592 plan(80.2): insert Unlinked Products Fix (K3Mart + Direct)`). No risk of losing it during replan — worst case the planner creates a `*.pre-bounce.md` backup.
- The quad_review consolidation lives in `.claude/get-shit-done/workflows/execute-phase.md` and `.claude/commands/triple-review.md` — when it's time to execute 80.2 after the replan, `/gsd:execute-phase 80.2` will use the new consolidated review gate automatically.
- Branch strategy per project convention: code work runs on `fix/unlinked-products-k3mart-direct` (as the v2 PLAN specifies). Planning artifacts commit directly to main.
- Configs to verify before execute (optional but recommended):
  - `workflow.triple_review=true` (enables the 4th-reviewer synthesis)
  - `workflow.simplify=true` (post-review cleanup pass)
  - `workflow.staffreview=true` (already default true)
