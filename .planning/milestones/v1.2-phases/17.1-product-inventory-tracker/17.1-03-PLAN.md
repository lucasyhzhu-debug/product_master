---
phase: 17.1-product-inventory-tracker
plan: 03
type: execute
wave: 3
depends_on: ["17.1-01", "17.1-02"]
files_modified:
  - src/pages/InventoryManager.tsx
  - src/components/inventory/FinishedGoodsTab.tsx
  - src/components/inventory/AddStockDialog.tsx
  - src/components/inventory/AdjustStockDialog.tsx
  - src/components/inventory/ProductStockCard.tsx
  - src/components/inventory/TransactionLogPanel.tsx
  - src/hooks/convex/useProductInventory.ts
  - src/hooks/convex/index.ts
autonomous: true
requirements:
  - FGI-01
  - FGI-02
  - FGI-03
  - FGI-04
  - FGI-07
  - FGI-08

must_haves:
  truths:
    - "InventoryManager page has a 'Finished Goods' tab alongside Packaging and Ingredients"
    - "Finished Goods tab shows product stock cards with quantity per location and low-stock indicators"
    - "Staff can add stock via a simple dialog: pick product, enter quantity, location defaults to Crystal"
    - "Manager can adjust stock (add/subtract) with a required reason field"
    - "Transaction history is visible per product showing additions, drawdowns, adjustments, and GoFood sales"
    - "Low-stock products show red visual indicators and alert banner"
    - "Settings panel allows configuring global low-stock threshold and default location"
  artifacts:
    - path: "src/components/inventory/FinishedGoodsTab.tsx"
      provides: "Main tab content for finished goods inventory view"
      min_lines: 100
    - path: "src/components/inventory/AddStockDialog.tsx"
      provides: "Simple dialog for adding stock (product + quantity + location)"
      min_lines: 50
    - path: "src/components/inventory/AdjustStockDialog.tsx"
      provides: "Dialog for manager stock adjustments with reason"
      min_lines: 50
    - path: "src/components/inventory/ProductStockCard.tsx"
      provides: "Card component showing product stock with per-location breakdown"
      min_lines: 40
    - path: "src/hooks/convex/useProductInventory.ts"
      provides: "React hooks for product inventory queries and mutations"
      exports: ["useProductInventory"]
  key_links:
    - from: "src/pages/InventoryManager.tsx"
      to: "src/components/inventory/FinishedGoodsTab.tsx"
      via: "Tabs component rendering FinishedGoodsTab in 'finished_goods' TabsContent"
      pattern: "FinishedGoodsTab"
    - from: "src/hooks/convex/useProductInventory.ts"
      to: "convex/productInventory/queries.ts"
      via: "useQuery(api.productInventory.queries.*)"
      pattern: "api\\.productInventory"
    - from: "src/components/inventory/AddStockDialog.tsx"
      to: "convex/productInventory/mutations.ts"
      via: "useMutation(api.productInventory.mutations.addStock)"
      pattern: "addStock"
---

<objective>
Build the Finished Goods tab in InventoryManager and all supporting UI components — stock cards, add-stock dialog, adjust-stock dialog, transaction log, and settings panel.

Purpose: This is the primary user-facing interface for finished goods tracking. Kitchen staff add stock here, managers adjust and configure thresholds, and everyone sees real-time stock levels with low-stock warnings.

Output: Complete FinishedGoodsTab UI integrated into InventoryManager, with hooks connecting to Plan 01/02 backend.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17.1-product-inventory-tracker/17.1-CONTEXT.md
@.planning/phases/17.1-product-inventory-tracker/17.1-RESEARCH.md
@.planning/phases/17.1-product-inventory-tracker/17.1-01-SUMMARY.md
@.planning/phases/17.1-product-inventory-tracker/17.1-02-SUMMARY.md
@src/pages/InventoryManager.tsx
@src/hooks/convex/useInventory.ts (pattern reference)
@src/components/inventory/ReceiveStockDialog.tsx (pattern reference)
@src/components/inventory/ComponentRow.tsx (pattern reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: React hooks for product inventory + InventoryManager tab integration</name>
  <files>src/hooks/convex/useProductInventory.ts, src/hooks/convex/index.ts, src/pages/InventoryManager.tsx</files>
  <action>
**Create `src/hooks/convex/useProductInventory.ts`:**

Follow the pattern from `useInventory.ts`. Create a hook that wraps all product inventory queries and mutations:

```typescript
export function useProductInventory() {
  // Queries
  const stockOverview = useQuery(api.productInventory.queries.getStockOverview);
  const lowStockAlerts = useQuery(api.productInventory.queries.getLowStockAlerts);
  const settings = useQuery(api.productInventory.queries.getSettings);

  // Mutations
  const addStock = useMutation(api.productInventory.mutations.addStock);
  const adjustStock = useMutation(api.productInventory.mutations.adjustStock);
  const updateSettings = useMutation(api.productInventory.mutations.updateSettings);

  return { stockOverview, lowStockAlerts, settings, addStock, adjustStock, updateSettings };
}
```

Also export a separate hook for transaction log with pagination:
```typescript
export function useProductInventoryTransactions(filters: {
  menuProductId?: Id<"menuProducts">;
  locationId?: Id<"storageLocations">;
  transactionType?: string;
}) {
  return usePaginatedQuery(
    api.productInventory.queries.getTransactions,
    filters,
    { initialNumItems: 20 }
  );
}
```

**Export from `src/hooks/convex/index.ts`:** Add `useProductInventory` and `useProductInventoryTransactions` to the barrel export.

**Update `src/pages/InventoryManager.tsx`:**

The existing page has Tabs with "all", "production", "packaging" categories. Transform this into a 3-tab layout:

1. Change the Tabs value from the current category filter to a top-level tab approach:
   - **"packaging"** — Existing packaging inventory (current default view, all existing components)
   - **"ingredients"** — Existing production/ingredient inventory (Phase 20's view)
   - **"finished_goods"** — New FinishedGoodsTab component

2. Keep existing filtering logic inside the packaging and ingredients tabs.

3. For the "finished_goods" tab, render `<FinishedGoodsTab />` component (created in Task 2).

4. The categoryFilter state can stay for filtering within packaging/ingredients, but the top-level tab determines which section is shown.

5. Move the existing `<Tabs>` for category into a parent `<Tabs>` for the 3 sections. The existing category filter tabs become sub-content of the packaging/ingredients views.

**Important UX per CONTEXT.md:**
- "Retail staff friendly" — the Finished Goods tab should be the simplest view
- Keep existing Packaging and Ingredients behavior unchanged
  </action>
  <verify>Run `npm run type-check`. Verify InventoryManager renders with 3 tabs. Existing packaging/ingredients views still work.</verify>
  <done>useProductInventory hook connects to all backend queries/mutations. InventoryManager has 3 top-level tabs: Packaging, Ingredients, Finished Goods.</done>
</task>

<task type="auto">
  <name>Task 2: FinishedGoodsTab, stock cards, add/adjust dialogs, and transaction log</name>
  <files>src/components/inventory/FinishedGoodsTab.tsx, src/components/inventory/AddStockDialog.tsx, src/components/inventory/AdjustStockDialog.tsx, src/components/inventory/ProductStockCard.tsx, src/components/inventory/TransactionLogPanel.tsx</files>
  <action>
**Create `src/components/inventory/FinishedGoodsTab.tsx`:**

Main layout:
- **Low-stock alert banner** at top (red bg) if any low-stock alerts exist. Show count and product names. Use AlertTriangle icon.
- **Action bar**: "Add Stock" button (Plus icon) — opens AddStockDialog. "Settings" button (Settings icon) — opens a simple settings section/dialog.
- **Product stock grid**: Grid of ProductStockCard components (2 cols on desktop, 1 on mobile). Each card = one menu product showing total stock across all locations plus per-location breakdown.
- **Transaction history section**: Collapsible section at bottom showing recent transactions. Uses TransactionLogPanel.

Props: none (fetches own data via useProductInventory hook)

Data flow:
- Load stockOverview from hook. Group by menuProductId to get total + per-location breakdown.
- Load lowStockAlerts for the banner.
- Load settings for threshold display.

**Settings inline section** (per CONTEXT.md — Claude's discretion on placement):
- Place settings as a collapsible Card within the FinishedGoodsTab (not a separate page).
- Fields: Global low-stock threshold (number input), Default add-stock location (Select dropdown of active locations), Auto-advance on drawdown (toggle/checkbox), Alert mode (select: toast / toast+badge).
- On change, call updateSettings mutation.
- Only visible to admin/manager roles (check `useAuth()` for role).

**Create `src/components/inventory/AddStockDialog.tsx`:**

"Retail staff friendly" — extremely simple:
- **Product selector**: shadcn Select dropdown showing active menu products (load from useQuery(api.menuProducts.queries.list) or similar). Show product name.
- **Quantity**: Number input. Must be > 0.
- **Location**: shadcn Select dropdown defaulting to Crystal/Office/default location. Show all active storageLocations.
- **Submit**: Calls addStock mutation with token from useAuth().
- **Success**: Toast "Added {qty} x {product} to {location}" via sonner.
- No technical jargon. Labels: "Product", "How many boxes?", "Add to location".

**Create `src/components/inventory/AdjustStockDialog.tsx`:**

Manager-only dialog (button visible only to manager/admin):
- **Product**: Pre-selected (opened from a specific ProductStockCard) or selector.
- **Location**: Pre-selected or selector.
- **Type**: Radio/toggle — "Add" or "Remove".
- **Quantity**: Number input (always positive, sign determined by type).
- **Reason**: Required text input (e.g., "Spoilage", "Transfer to Goldfinch", "Count correction").
- Submit calls adjustStock with positive or negative quantity based on type.
- Success toast with adjustment details.

**Create `src/components/inventory/ProductStockCard.tsx`:**

Card component for each menu product:
- **Header**: Product name + total stock badge.
- **Per-location rows**: Each location with its stock count. Use small text/badges.
- **Low-stock indicator**: If any location is below threshold, show red AlertTriangle icon + red text. If stock is negative, show "Negative stock" warning.
- **Actions**: "Add Stock" quick button (opens AddStockDialog pre-filled with this product). "Adjust" button (manager/admin only, opens AdjustStockDialog). "History" button (expands/shows TransactionLogPanel filtered to this product).
- Visual style: Match existing ComponentRow card style from packaging inventory. Use Card + CardContent from shadcn/ui.

**Create `src/components/inventory/TransactionLogPanel.tsx`:**

Scrollable list of transactions:
- Props: `menuProductId?: Id<"menuProducts">` for filtering.
- Uses useProductInventoryTransactions hook with pagination.
- Each row shows: timestamp (formatted with date-fns), transaction type badge (color-coded: green for add, red for drawdown/gofood_sale, yellow for adjust), quantity (+/-), location name, performed by, reason (if adjust), order link (if drawdown).
- "Load more" button at bottom using Convex pagination loadMore.
- Transaction type badges: "Added" (green), "Order" (blue), "GoFood" (purple), "Adjusted" (yellow).
  </action>
  <verify>Run `npm run type-check`. Run `npm run build`. Verify all 5 component files compile. Open InventoryManager in browser, click Finished Goods tab — should render (even if empty data). Test AddStockDialog opens and submits.</verify>
  <done>Complete Finished Goods UI: stock cards with per-location breakdown, simple add-stock dialog for kitchen staff, manager adjust dialog with reason, transaction log with pagination, low-stock alerts with red indicators, inline settings panel.</done>
</task>

</tasks>

<verification>
- `npm run type-check` passes
- `npm run build` succeeds
- InventoryManager page loads with 3 tabs (Packaging, Ingredients, Finished Goods)
- Finished Goods tab shows product stock cards (or empty state if no data)
- AddStockDialog opens with product/quantity/location fields, submits successfully
- AdjustStockDialog shows for manager/admin only with reason field
- Transaction log shows formatted entries with type badges
- Low-stock alert banner appears when stock is below threshold
- Settings section visible to admin/manager, saves changes
</verification>

<success_criteria>
- [ ] `npm run type-check` passes
- [ ] `npm run build` succeeds
- [ ] InventoryManager has "Finished Goods" tab
- [ ] AddStockDialog works end-to-end (product -> quantity -> location -> submit -> toast)
- [ ] AdjustStockDialog works with reason field (manager/admin only)
- [ ] ProductStockCard shows per-location stock with low-stock indicators
- [ ] TransactionLogPanel shows paginated history with type badges
- [ ] Settings panel configurable (threshold, default location, alert mode)
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-product-inventory-tracker/17.1-03-SUMMARY.md`
</output>
