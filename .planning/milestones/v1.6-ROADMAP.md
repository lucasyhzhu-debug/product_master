# Milestone v1.6: Tech Debt & Resilience

**Status:** SHIPPED 2026-03-09
**Phases:** 35-40
**Total Plans:** 16

## Overview

Comprehensive tech debt reduction milestone: schema audit with index cleanup, backend helper extraction reducing 5 major files by 31.3% LOC, frontend giant file splits reducing 4 components by 74% LOC, E2E Playwright test foundation for critical paths, and Tamtem depot auto-seed resilience fix. Pure refactoring and testing — no new feature tables or API path changes.

## Phases

### Phase 35: Schema Review & Audit

**Goal**: Expert audit of all 65 Convex tables to identify data duplication, unused/redundant tables, denormalization waste, missing indexes, and inefficient patterns.
**Depends on**: None
**Plans**: 2 plans

Plans:
- [x] 35-01: Schema audit report — 42 findings (1 critical, 20 moderate, 21 low), 22 unused indexes identified, 6 range bound anti-patterns
- [x] 35-02: Execute quick-wins — 20 unused indexes removed, 5 compound indexes added, 10 range bound fixes, critical session cleanup (MIS-01)

**Details:**
- Schema audit methodology: `defineTable` count + index cross-reference + `withIndex` grep
- Agent: `schema-architect` — Convex schema specialist
- Compound index pattern: `source+isActive`, `batch+outlet`, `order+status` for eliminating post-scan filters
- Net result: 150 indexes (was 166: 21 removed, 5 compound added)

---

### Phase 36: Sales & Analytics Backend Simplification

**Goal**: Extract shared helpers (confidence, WIB timezone, sourceToPlatform) and split the largest analytics query files into slim orchestrators + pure helper modules.
**Depends on**: Phase 35 (cleaned indexes)
**Plans**: 3 plans

Plans:
- [x] 36-01: Shared helpers — confidence.ts, periodRange.ts WIB helpers, externalSource.ts sourceToPlatform()
- [x] 36-02: externalData/queries.ts helpers — 5 pure helper modules, 1,773 -> 1,387 LOC (22% reduction)
- [x] 36-03: k3martCockpit/queries.ts helpers — queryHelpers/ directory, 985 -> 760 LOC (22.8% reduction)

**Details:**
- No re-export bridges — direct import updates per CONTEXT.md override
- `queryHelpers/` directory pattern (not `helpers/`) to avoid Windows case-insensitive collision with `helpers.ts`
- 760 LOC slightly over 750 target — remaining functions too ctx-dependent for pure extraction

---

### Phase 37: Order & Dispatch Backend Simplification

**Goal**: Split order management and dispatch planner query/mutation files by extracting validation logic, enrichment helpers, and simulation code into pure helper modules.
**Depends on**: Phase 36 (shared helpers available)
**Plans**: 3 plans

Plans:
- [x] 37-01: orders/queries.ts helpers — kitchenEnrichment.ts (6 functions) + kanbanBuilders.ts (4 functions), 1,279 -> 940 LOC (-26.5%)
- [x] 37-02: orderCrud.ts helpers — customerResolution.ts + orderItemProcessing.ts, 1,085 -> 958 LOC (-11.7%)
- [x] 37-03: dispatchPlanner/queries.ts helpers — types.ts + weeklyPlanBuilder.ts + inventorySimulation.ts, 1,226 -> 313 LOC (-74.5%)

**Details:**
- Bug fixes discovered during extraction: `completedAt` filter (was using `_creationTime`), cancelled record filtering in `productionByType` loops, date range convention
- Mutation ctx prevents deeper pure extraction — 11.7% reduction acceptable for orderCrud.ts
- Total: 3,590 -> 2,211 LOC (-38.4%)

---

### Phase 38: Frontend Giant File Splits

**Goal**: Split the 4 largest frontend components (all >1,200 LOC) into focused sub-components.
**Depends on**: None (frontend-only)
**Plans**: 4 plans

Plans:
- [x] 38-01: OverviewTab.tsx — 14 new files in salesAnalytics/ + shared dateUtils.ts, 1,273 -> 283 LOC (-78%)
- [x] 38-02: GrabFoodManager.tsx — 6 tab components in salesAnalytics/, 1,486 -> 173 LOC (-88%)
- [x] 38-03: FinishedGoodsTab.tsx — 6 sub-components + utils in inventory/, 1,474 -> 488 LOC (-67%)
- [x] 38-04: VouchersManager.tsx — 5 components + barrel in vouchers/, 1,285 -> 506 LOC (-61%)

**Details:**
- Shared WIB timezone helpers consolidated in `src/lib/dateUtils.ts` (6 exports, single source of truth)
- `formatCurrencyIDR` eliminated from codebase, replaced with `formatCurrency(amount ?? 0)` pattern
- `FreeVoucherDialog` made self-contained with own state/handler (critical for LOC target)
- Parallel frontend file splits (4 agents) worked cleanly — no merge conflicts

---

### Phase 39: E2E Test Foundation & Resilience

**Goal**: Establish Playwright E2E test infrastructure and write tests for 3 critical user paths. Fix Tamtem depot silent failure.
**Depends on**: Phase 38 (component names stable for selectors)
**Plans**: 3 plans

Plans:
- [x] 39-01: Tamtem depot auto-seed — depotAutoSeed.ts (154 LOC) + 6 unit tests (407 LOC)
- [x] 39-02: Order lifecycle + kitchen production E2E tests
- [x] 39-03: Sales analytics period navigation E2E test

**Details:**
- DEPOT_CONFIG array makes adding new depot patterns trivial
- E2E tests handle address validation soft-block dialogs and stock shortage overrides
- Kitchen EoS test gracefully degrades when no production targets configured
- Period badges use `cursor-pointer` class selector (Badge components, not buttons)

---

### Phase 40: Retroactive Verification Gap Closure

**Goal**: Create missing VERIFICATION.md files for Phases 35, 36, 37. Fix Phase 37 SUMMARY frontmatter. Update REQUIREMENTS.md traceability. Close all 12 documentation gaps identified by the v1.6 audit.
**Depends on**: Phases 35-39 (verifying their work)
**Plans**: 1 plan

Plans:
- [x] 40-01: Create 3 VERIFICATION.md files, fix SUMMARY frontmatter, update REQUIREMENTS.md traceability

**Details:**
- Retroactive verification uses dual LOC recording: SUMMARY-reported value (at execution time) AND current `wc -l` (at verification time)
- All 20 v1.6 requirements now 3-source verified (VERIFICATION + SUMMARY + ROADMAP)
- Phase 37 SUMMARY files updated with `requirements-completed` frontmatter

---

## Milestone Summary

**Key Decisions:**
- Direct import updates (no re-export bridges) per CONTEXT.md override of design doc
- `queryHelpers/` directory pattern avoids Windows collision with `helpers.ts`
- Auto-seed pattern for depot locations (DEPOT_CONFIG array)
- Retroactive verification to close audit gaps before milestone completion

**Issues Resolved:**
- Tamtem depot deduction no longer silently skips unlinked outlets
- `completedAt` filter bug fixed (was using `_creationTime` for "completed today" queries)
- Cancelled record filtering added to `productionByType` loops
- 20 unused indexes removed (was inflating schema)
- 10 range bound anti-patterns fixed across 4 query files

**Issues Deferred:**
- Split queries.ts into domain-specific files (requires API path changes)
- Unit tests for extracted helper functions
- COGS resolution helper consolidation (not needed at current scale)
- `expect(true).toBe(true)` anti-pattern in 20 E2E test instances

**Technical Debt Incurred:**
- BFS-02 slightly over target: k3martCockpit/queries.ts at 760 LOC (target <750, 10 LOC over)
- E2E tests conditionally skip assertions when dev database lacks data
- `formatDateTimeId` exported from dateUtils.ts but never imported

---

_For current project status, see .planning/ROADMAP.md_
