# Plan 29.1-01: Test Suite Repair

## Git Workflow
**Branch:** `fix/test-suite-repair`
**Checkpoints:** Commit after each wave

## Implementation Waves

### Wave 1: Config & Dead Tests [SEQUENTIAL — single agent]
| Agent | Task | Files |
|-------|------|-------|
| convex-backend | **Step 1:** Add `'tests/e2e/**'` to the EXISTING `exclude` array in vitest config (Category 5). Current array: `['node_modules', 'dist', 'convex/_generated']` — append to it, do NOT replace. | `vitest.config.ts` |
| convex-backend | **Step 2:** Delete orphaned test files that reference removed modules (Category 1) | `tests/convex/recipes.test.ts`, `tests/convex/tags.test.ts`, `tests/convex/products.test.ts` |
| convex-backend | **Step 3:** Remove these 7 orphaned helper functions from `tests/convex/helpers.ts` (ONLY used by the deleted test files): `createIngredient`, `createPackagingMaterial`, `setupRecipeWithVersion`, `setupReusableRecipe`, `setupRecipeWithLinkedComponent`, `setupPackagingWithVersion`, `setupProductWithRecipe`. **Keep all other exports** — they are used by surviving tests. | `tests/convex/helpers.ts` |

**Rationale:** These 3 files test modules (`convex/recipes/`, `convex/tags/`, `convex/products/`, `convex/packaging/`) that were completely removed from the codebase during Phase 22 (Remove legacy editors). The modules no longer exist in `convex/_generated/api.d.ts`. Deleting these tests is the correct action — they cannot be "fixed" because the code they tested is gone. This clears **45 of 56 failures + 8 E2E suite errors**.

**Commit:** `fix(tests): remove dead test files, exclude E2E from Vitest, prune orphaned helpers`

### Wave 2: Assertion Fixes [PARALLEL, after Wave 1]
| Agent | Task | Files |
|-------|------|-------|
| convex-backend | Fix cron assertion tests (Category 3): `crons.ts` is now empty (no active crons). **Delete the entire cron assertion test blocks** from both files — `gobizAdapter.test.ts` (the `"Cron configuration"` describe block, ~lines 18-28) and `gofoodDepot-edge.test.ts` (the `"GoBiz auto-sync cron"` describe block, ~lines 471-479). String-matching file content is brittle and low-value; no replacement assertions needed. | `tests/convex/gobizAdapter.test.ts`, `tests/convex/gofoodDepot-edge.test.ts` |
| convex-backend | Fix K3Mart cockpit tests (Category 2): (a) Update error message assertion from `"No draft plans found"` to `"No plans found for this date"` (line ~229). (b) **CRITICAL:** `getOutletStockSummary` was renamed to `getOutletStockSummaryInternal` AND changed from public `query` to `internalQuery` — it no longer exists in `api.k3martCockpit.queries`. Change both test calls (lines ~604, ~616) from `t.query(api.k3martCockpit.queries.getOutletStockSummary, ...)` to `t.query(internal.k3martCockpit.queries.getOutletStockSummaryInternal, ...)`. Ensure `internal` is imported from `../../convex/_generated/api`. (c) Fix return type assertions for changed shapes. | `tests/convex/k3martCockpit.test.ts` |
| convex-backend | Fix voucher handling tests (Category 4): Update 2 tests (lines ~142-176) that expect `rejects.toThrow('Final price must be greater than 0')`. The 100% discount validation was intentionally removed — this is now a valid business case. **Replace both `.rejects.toThrow(...)` with positive assertions:** call `createOrderWithVoucher(t, { voucherId, orderTotal: 100000 })` and assert `result.orderId` is defined (mutation resolves successfully). | `tests/convex/voucherHandling.test.ts` |

**Commits (one per file group):**
- `fix(tests): remove stale cron assertion blocks`
- `fix(tests): update K3Mart cockpit assertions for internalQuery migration`
- `fix(tests): update voucher tests for 100% discount allowance`

### Wave 3: Verification [SEQUENTIAL]
| Agent | Task |
|-------|------|
| code-auditor | Run `npx vitest run` — must show 0 failures |
| code-auditor | Run `npm run build` — must pass |
| code-auditor | Run `npm run type-check` — must pass |
| code-auditor | Verify passing test count >= 636 (no regressions among surviving tests) |

## Documentation Updates
- [ ] CHANGELOG.md — add test repair entry
- [ ] Move debug file: `.planning/debug/test-suite-failures.md` → `.planning/debug/resolved/`

## Success Criteria
- [ ] `npx vitest run` passes with 0 failures
- [ ] `npm run build` succeeds
- [ ] `npm run type-check` passes
- [ ] Passing test count >= 636 (deleted dead tests don't count as regressions)
- [ ] E2E specs excluded from Vitest (still runnable via Playwright separately)
- [ ] No test helpers left orphaned (7 functions removed from helpers.ts)

## Estimated Impact
- **Before:** 15 failed files, 56 failed tests, 636 passed
- **After:** 0 failed files, 0 failed tests, ~647 passed (dead tests removed, remaining fixed)
- **Net:** 45 tests removed (dead code from 3 files), ~11 tests fixed across 4 files, 636 core tests preserved
