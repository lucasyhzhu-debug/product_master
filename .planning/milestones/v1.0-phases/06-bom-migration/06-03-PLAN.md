---
phase: 06-bom-migration
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/hooks/convex/useMenuProducts.ts
  - src/hooks/convex/useKitchenStats.ts
  - src/components/orders/PackageStatusDisplay.tsx
  - src/components/orders/ProductButtons.tsx
  - src/lib/types.ts
  - convex/schema.ts
  - convex/menuProducts/mutations.ts
autonomous: true

must_haves:
  truths:
    - "No frontend file reads productionType or productionUnits from API responses (except type definitions with DEPRECATED comments)"
    - "menuProducts schema fields productionType and productionUnits are v.optional() with DEPRECATED comments"
    - "MenuProducts edit form no longer shows productionType/productionUnits fields — BOM is the only way to configure ball composition"
    - "All TypeScript type definitions mark deprecated fields with DEPRECATED comments"
    - "npm run build passes with all changes applied"
    - "BOM-05 is documented as already complete (QFIX-05 in Phase 3)"
  artifacts:
    - path: "convex/schema.ts"
      provides: "menuProducts fields changed to v.optional() with DEPRECATED comments"
      contains: "DEPRECATED"
    - path: "src/hooks/convex/useMenuProducts.ts"
      provides: "Menu product hooks without deprecated field reads"
    - path: "src/hooks/convex/useKitchenStats.ts"
      provides: "Kitchen stats hook without deprecated field reads"
    - path: "src/components/orders/PackageStatusDisplay.tsx"
      provides: "Package status display without deprecated field reads"
    - path: "src/lib/types.ts"
      provides: "Type definitions with DEPRECATED comments on legacy fields"
  key_links:
    - from: "convex/schema.ts"
      to: "menuProducts table"
      via: "v.optional() change on productionType and productionUnits"
      pattern: "v\\.optional.*DEPRECATED"
    - from: "src/hooks/convex/useMenuProducts.ts"
      to: "convex/menuProducts/queries"
      via: "hook reads BOM data, not deprecated fields"
      pattern: "components|componentTypes"
---

<objective>
Implement BOM-03 (frontend migration), BOM-04 (schema changes to optional), and document BOM-05 (already complete) — Steps 4, 5, and 6 of the strangler fig migration.

Purpose: Complete the migration by updating all frontend files to stop reading deprecated fields, relaxing the schema to make deprecated fields optional, and confirming BOM-05 was already completed in Phase 3. After this plan, deprecated fields are invisible to all code paths except the dual-read fallback for historical orders.

Output: 6-7 frontend/backend files modified, schema fields marked optional, migration fully complete.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-bom-migration/06-RESEARCH.md
@.planning/phases/06-bom-migration/06-02-SUMMARY.md

@convex/schema.ts
@src/hooks/convex/useMenuProducts.ts
@src/hooks/convex/useKitchenStats.ts
@src/components/orders/PackageStatusDisplay.tsx
@src/components/orders/ProductButtons.tsx
@src/lib/types.ts
@convex/menuProducts/mutations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: BOM-03 — Migrate frontend files to stop reading deprecated fields</name>
  <files>
src/hooks/convex/useMenuProducts.ts
src/hooks/convex/useKitchenStats.ts
src/components/orders/PackageStatusDisplay.tsx
src/components/orders/ProductButtons.tsx
src/lib/types.ts
  </files>
  <action>
**Part A: `src/hooks/convex/useMenuProducts.ts`**

Research identified 9 occurrences across type interfaces and transform functions (lines 21-22, 37-38, 61-62, 125-126, 143-144, 164-165, 195-196, 220-221, 238-239).

1. **Type interfaces**: Any interface that includes `productionType` or `productionUnits` — mark as optional with `// DEPRECATED` comment, OR remove if the field is not used in any render logic.

   Per user decision: "Remove from edit forms" and "Strip from API responses". The goal is that these fields should NOT appear in any active UI code.

2. **Transform functions**: Any function that maps `productionType`/`productionUnits` from API response to local state or snake_case format — remove these mappings. If the transform produces a `production_type` or `production_units` output, remove those output fields.

3. **Create/Update input types**: Remove `productionType`/`productionUnits` from any input types used for create/update mutations. These fields are no longer written (per BOM-02 in Plan 02).

4. **Form data handling**: Remove `productionType`/`productionUnits` from any form state objects, form submission handlers, and default values.

Per user decision: "Remove from edit forms — MenuProductsManager edit form removes productionType/productionUnits fields entirely. BOM is the only way to configure ball composition going forward."

**Part B: `src/hooks/convex/useKitchenStats.ts`**

Research identified 2 occurrences (lines 52-53, 155-156) in type interfaces and item transforms.

1. Remove `productionType` and `productionUnits` from the `KitchenOrderItem` type interface (or equivalent). Mark as optional with `// DEPRECATED` if still needed for TypeScript compatibility with backend responses.
2. Remove any transform code that reads these fields from the API response.

Note: The kitchen stats hook may receive these fields from the backend query response. Since Plan 02 (BOM-01) already changed the backend to use dual-read, the kitchen stats should be BOM-derived. The frontend just needs to stop explicitly reading/depending on these fields.

**Part C: `src/components/orders/PackageStatusDisplay.tsx`**

Research identified 2 occurrences (lines 20-21, 105-107) — a type interface and a ball count display.

1. Remove `productionUnits` and `productionType` from the component's prop type interface
2. At line 105-107 where `productionUnits` is used for ball count display: Replace with the ball count derived from the parent component or from production records data. The parent component should pass pre-computed `ballsPerPackage` instead of raw `productionUnits`.

If the component needs ball count data, add a `ballsPerPackage?: number` prop (or similar) that the parent computes from production records. The parent (likely KitchenViewV2 or OrderDetail) already has access to production records from the backend query.

Alternatively, if `ballsPerPackage` is already available as part of the item data from the backend query (which now uses dual-read), just rename the prop.

**Part D: `src/components/orders/ProductButtons.tsx`**

Research identified 1 occurrence (lines 22-23) — a type interface only, not used in render logic.

1. Remove `productionType` and `productionUnits` from the `ProductButtonProduct` type interface
2. Verify no render code reads these fields (research says "Type interface only, not read in render")

**Part E: `src/lib/types.ts`**

Research identified 2 occurrences (lines 258-259, 627-628) — `production_type` and `production_units` in TypeScript type definitions.

1. Add `// DEPRECATED: Removed in Phase 8 (Schema Cleanup)` comments to these fields
2. Mark them as optional if not already: `production_type?: string; // DEPRECATED`
3. Do NOT remove them yet — they may be needed for TypeScript compatibility with data structures that include historical data

**Part F: `src/components/orders/EnhancedCancellationDialog.tsx` and `src/pages/OrderDetail.tsx`**

Research notes these already use `item.quantity` (not deprecated fields) for their calculations. Verify no changes needed:
- `EnhancedCancellationDialog.tsx` line 25: `productionUnitsAffected` — verify this is computed from `item.quantity`, not from deprecated fields
- `OrderDetail.tsx` lines 458, 468: `productionUnitsAffected` — verify same

If these are already BOM-independent, no changes needed. If they still reference deprecated fields, apply the same removal pattern.
  </action>
  <verify>
1. `npm run type-check` passes
2. Grep `src/` for `productionType` — should only appear in type definitions with DEPRECATED comments, not in active code
3. Grep `src/` for `productionUnits` — should only appear in type definitions with DEPRECATED comments, not in active code
4. Grep `src/` for `production_type` — should only appear in type definitions with DEPRECATED comments
5. Grep `src/` for `production_units` — should only appear in type definitions with DEPRECATED comments
  </verify>
  <done>
All frontend files stop reading `productionType`/`productionUnits` from API responses. Type definitions retain the fields as optional with DEPRECATED comments for TypeScript compatibility. Edit forms no longer include deprecated field inputs. Ball composition is derived from BOM data throughout the frontend.
  </done>
</task>

<task type="auto">
  <name>Task 2: BOM-04 Schema Changes + BOM-05 Documentation + menuProducts mutation cleanup</name>
  <files>
convex/schema.ts
convex/menuProducts/mutations.ts
  </files>
  <action>
**Part A: BOM-04 — Schema changes**

In `convex/schema.ts`, change the `menuProducts` table definition:

1. Line ~55: Change `productionType: v.string()` to `productionType: v.optional(v.string())` with comment `// DEPRECATED: Phase 8 removal. Ball composition from BOM (menuProductComponents + componentTypes).`
2. Line ~56: Change `productionUnits: v.number()` to `productionUnits: v.optional(v.number())` with comment `// DEPRECATED: Phase 8 removal. Ball count from BOM.`

The `orderItems` table fields are already `v.optional()` (lines 405-406), so no changes needed there.

**Verify existing data compatibility:** All 8 menuProducts currently have values for both fields (verified in research). Making required -> optional is always safe in Convex — existing data with values is still valid, and new documents can omit the fields.

**Part B: menuProducts mutations cleanup**

After the schema change to `v.optional()`, the empty defaults in `menuProducts/mutations.ts` (added in Plan 02, Task 2) are no longer needed:

1. In the `create` mutation: Remove the `productionType: args.productionType ?? ""` and `productionUnits: args.productionUnits ?? 0` defaults. Now that the schema accepts optional, simply omit these fields from the insert. If `args.productionType` is provided (for backward compatibility), still include it.

   Simplest approach: Remove `productionType` and `productionUnits` from the insert object entirely. New products will have `undefined` for these fields, and BOM entries in `menuProductComponents` are the source of truth.

2. In `seedDefaults`: Update to NOT include `productionType`/`productionUnits` in seed data. The seed function should create BOM entries via `menuProductComponents` instead, OR leave ball composition unconfigured for seeded products. Since the backfill (Plan 01) handles creating BOM entries from deprecated fields for existing products, and seeds are for dev setup only, the simplest approach is to keep the deprecated fields in seeds with a DEPRECATED comment (since seeds create specific known products where the values are correct).

   Actually, the cleanest approach: Keep `productionType`/`productionUnits` in seedDefaults with `// DEPRECATED: kept for backward compatibility in dev seeding` comments. The seed function is for development setup, not production code.

**Part C: BOM-05 — Documentation only**

BOM-05 (remove `by_production_type` index on `orderItems`) was already completed in Phase 3, Plan 03 (QFIX-05). The schema at line ~428 already has the comment: `// QFIX-05: removed by_production_type -- deprecated field, zero references`.

No code changes needed. Document this fact in the plan summary.

Verify the remaining `by_production_type` index at line ~450 is on the `orderItemProduction` table (indexing `productionUnitTypeId`) and is part of the NEW system — it must NOT be removed.
  </action>
  <verify>
1. `npm run type-check` passes
2. `npm run build` passes
3. `convex/schema.ts` shows `menuProducts.productionType` as `v.optional(v.string())` with DEPRECATED comment
4. `convex/schema.ts` shows `menuProducts.productionUnits` as `v.optional(v.number())` with DEPRECATED comment
5. `convex/schema.ts` line ~450 still has `by_production_type` index on `orderItemProduction` (NEW system, must stay)
6. Grep entire `convex/` and `src/` for non-deprecated reads of `productionType`/`productionUnits` — should find only: (a) type definitions with DEPRECATED, (b) dual-read fallback in queries.ts, (c) getBallsPerPackageForItem fallback in packaging.ts, (d) seed data with DEPRECATED comment, (e) migration files
  </verify>
  <done>
Schema fields `menuProducts.productionType` and `menuProducts.productionUnits` are `v.optional()` with DEPRECATED comments. menuProducts mutations no longer provide deprecated field defaults. BOM-05 is documented as already complete (QFIX-05). The entire BOM migration is complete — all code paths read from BOM first with seamless fallback for historical data.
  </done>
</task>

</tasks>

<verification>
1. `npm run type-check` passes
2. `npm run build` passes
3. All existing tests pass (`npm run test`)
4. **Final grep verification (all 4 success criteria from roadmap):**
   a. Grep `convex/orders/` for `productionType` reads in query files → only in dual-read fallback helper
   b. Grep `convex/` mutation files for `productionType`/`productionUnits` writes → zero results (except DEPRECATED defaults/seeds)
   c. Grep `src/` for `productionType`/`productionUnits` reads → zero results except type definitions with DEPRECATED
   d. `convex/schema.ts` shows both `menuProducts` fields as `v.optional()` with DEPRECATED comments
   e. No `by_production_type` index on `orderItems` (already removed in Phase 3)
   f. `by_production_type` on `orderItemProduction` is untouched (NEW system)
</verification>

<success_criteria>
- All frontend files stop reading deprecated fields from API responses
- Type definitions retain deprecated fields as optional with DEPRECATED comments
- MenuProducts edit form no longer shows productionType/productionUnits fields
- Schema marks deprecated fields as v.optional() with DEPRECATED comments
- menuProducts mutations no longer write deprecated fields
- BOM-05 documented as already complete
- npm run build passes
- All existing tests pass
- Entire BOM migration is complete per the 6-step roadmap sequence
</success_criteria>

<output>
After completion, create `.planning/phases/06-bom-migration/06-03-SUMMARY.md`
</output>
