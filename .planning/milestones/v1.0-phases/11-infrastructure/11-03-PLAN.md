---
phase: 11-infrastructure
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - src/hooks/convex/useKitchenProduction.ts
  - convex/integrityChecks/mutations.ts
  - convex/integrityChecks/queries.ts
  - docs/CHANGELOG.md
autonomous: true

must_haves:
  truths:
    - "Kitchen UI displays production counts derived from productionLog aggregation, not productionCounts table"
    - "Weekly integrity check compares productionLog aggregation against productionCounts archive and logs mismatches"
    - "CHANGELOG.md documents Phase 11 completion with INFRA-02 and INFRA-03 (no INFRA-01)"
  artifacts:
    - path: "src/hooks/convex/useKitchenProduction.ts"
      provides: "Kitchen production hook using productionLog aggregation query"
      contains: "productionLog"
    - path: "convex/integrityChecks/mutations.ts"
      provides: "Full weekly integrity check implementation"
      exports: ["runWeeklyCheck"]
    - path: "convex/integrityChecks/queries.ts"
      provides: "getRecentChecks query for admin review"
      exports: ["getRecentChecks"]
    - path: "docs/CHANGELOG.md"
      provides: "Phase 11 changelog entry"
      contains: "Phase 11"
  key_links:
    - from: "src/hooks/convex/useKitchenProduction.ts"
      to: "convex/productionLog/queries.ts::getAggregatedCounts"
      via: "useQuery(api.productionLog.queries.getAggregatedCounts)"
      pattern: "productionLog\\.queries\\.getAggregatedCounts"
---

<objective>
Complete the frontend switchover to productionLog aggregation, implement the full weekly integrity check, and document Phase 11 completion.

Purpose: Finishes INFRA-02 and INFRA-03 — frontend now reads from productionLog, and integrity checks validate data consistency. INFRA-01 (backup monitoring) dropped per user decision.
Output: Updated kitchen hook, integrity check implementation, changelog entry.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-infrastructure/11-RESEARCH.md
@.planning/phases/11-infrastructure/11-02-SUMMARY.md
@src/hooks/convex/useKitchenProduction.ts
@convex/integrityChecks/mutations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend switchover — kitchen hook to productionLog aggregation</name>
  <files>
    src/hooks/convex/useKitchenProduction.ts
  </files>
  <action>
**Switch `src/hooks/convex/useKitchenProduction.ts` to productionLog aggregation:**

Change the `useQuery` call from:
```typescript
useQuery(api.productionCounts.queries.getAll)
```
To:
```typescript
useQuery(api.productionLog.queries.getAggregatedCounts)
```

The return shape is identical (same fields, same types), so the `KitchenProductionData` interface should NOT need changes. Verify the interface matches the aggregation query return type. If there are any minor differences (e.g., optional vs required fields), adjust the interface.

Also update any imports that reference `productionCounts` to reference `productionLog`. Add a comment: `// INFRA-03: Production counts now derived from productionLog aggregation`.

Check if there are any other `useQuery` calls in the hook that reference `productionCounts.queries.getByMenuProduct` and switch those to `productionLog.queries.getCountsByMenuProduct`.
  </action>
  <verify>
    Run `npm run type-check`. Verify no TypeScript errors. Grep for remaining `productionCounts.queries` references in `src/` — should be zero (except possibly type imports or comments).
  </verify>
  <done>
    Kitchen hook reads from productionLog aggregation. No frontend references to productionCounts.queries remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrity check implementation + CHANGELOG</name>
  <files>
    convex/integrityChecks/mutations.ts
    convex/integrityChecks/queries.ts
    docs/CHANGELOG.md
  </files>
  <action>
**Implement full `convex/integrityChecks/mutations.ts`:**

Replace the placeholder `runWeeklyCheck` (from Plan 01) with the full implementation:

1. `runWeeklyCheck` — `internalMutation` (no args, called by cron):
   - Fetch all `productionCounts` records (the archived table)
   - For each record, aggregate from `productionLog` for the same menuProductId (respecting `productionResets` timestamps)
   - Compare: if `logBoxed !== counts.boxed` OR `logStickered !== counts.stickered` OR `logPacked !== counts.packed`, record as mismatch
   - Note: After INFRA-03 consolidation, productionCounts is frozen (no longer written to). So mismatches indicate historical discrepancies between the old dual-write data and the log-derived data. This is EXPECTED and informational — the log is now authoritative.
   - Insert result into `integrityCheckLogs` table:
     ```typescript
     {
       timestamp: Date.now(),
       type: "production_counts",
       status: mismatches.length === 0 ? "pass" : "fail",
       mismatchCount: mismatches.length,
       mismatches: mismatches.length > 0 ? JSON.stringify(mismatches) : undefined,
     }
     ```

**Create `convex/integrityChecks/queries.ts`:**
- `getRecentChecks` query with `{ limit: v.number() }` args
- Returns most recent N integrity check logs ordered by timestamp desc
- This is for admin review but no UI is built for it in this phase (future use)

**Update `docs/CHANGELOG.md`:**

Add a Phase 11 entry at the top of the changelog:

```markdown
## Phase 11: Infrastructure & Consolidation (2026-02-14)

### INFRA-02: Dependency Audit
- Full dependency compatibility audit completed
- All safe upgrades applied; breaking upgrades documented with rationale
- React 19 + Vite 7 + Convex 1.31 + TypeScript 5.9 compatibility verified

### INFRA-03: Production Counts Consolidation
- All production count reads now derived from `productionLog` aggregation
- `productionCounts` table archived (read-only, no longer written to)
- `productionResets` table tracks reset timestamps for aggregation filtering
- New `productionLog` action types: `ship_goldfinch`, `return_goldfinch` for GoFood depot tracking
- Weekly integrity check compares archived counts against log-derived aggregation
- Kitchen UI, K3Mart cockpit, and GoFood depot all use single source of truth
```
  </action>
  <verify>
    Run `npm run type-check`. Run `npm run build`. Verify `convex/integrityChecks/mutations.ts` has the full `runWeeklyCheck` implementation. Verify `docs/CHANGELOG.md` has the Phase 11 entry.
  </verify>
  <done>
    Weekly integrity check fully implemented comparing productionCounts archive against productionLog aggregation. Integrity check query exists for admin review. CHANGELOG documents INFRA-02 and INFRA-03 work (no INFRA-01). `npm run build` passes.
  </done>
</task>

</tasks>

<verification>
- `npm run type-check` passes
- `npm run build` passes
- `grep -r "productionCounts.queries" src/` returns zero results (except comments)
- `src/hooks/convex/useKitchenProduction.ts` references `productionLog.queries.getAggregatedCounts`
- `convex/integrityChecks/mutations.ts` has full runWeeklyCheck implementation
- `convex/integrityChecks/queries.ts` exports getRecentChecks
- `docs/CHANGELOG.md` has Phase 11 entry without INFRA-01
</verification>

<success_criteria>
- Kitchen UI displays production data from productionLog aggregation (single source of truth)
- Weekly integrity check is functional
- All documentation updated
- `npm run build` passes — zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/11-infrastructure/11-03-SUMMARY.md`
</output>
