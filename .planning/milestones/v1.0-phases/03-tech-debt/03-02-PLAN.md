---
phase: 03-tech-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/orderConstants.ts
  - src/components/dashboard/ProductionQueueTable.tsx
  - src/components/orders/OrderHeader.tsx
  - src/components/orders/OrderStatusPanel.tsx
  - src/pages/OrderDetail.tsx
  - src/components/dashboard/SalesWidget.tsx
autonomous: true

must_haves:
  truths:
    - "Orders with deprecated statuses (ProductionComplete, Packaging) display using mapped status badges (Boxed, InProduction) -- no 'Legacy' labels"
    - "Users can still filter by deprecated statuses in the order status panel to find old orders"
    - "Status transitions out of deprecated statuses still work (e.g., cancellation)"
    - "No TypeScript type changes -- OrderStatus type keeps both deprecated values"
    - "Backend queries, mutations, and schema are completely untouched"
  artifacts:
    - path: "src/lib/orderConstants.ts"
      provides: "getDisplayStatus() helper function that maps deprecated statuses to active equivalents"
      contains: "getDisplayStatus"
    - path: "src/lib/orderConstants.ts"
      provides: "STATUS_COLORS without deprecated status entries"
    - path: "src/components/orders/OrderStatusPanel.tsx"
      provides: "Filter options that still include deprecated statuses"
  key_links:
    - from: "src/lib/orderConstants.ts"
      to: "src/components/orders/OrderHeader.tsx"
      via: "getDisplayStatus used for badge rendering"
      pattern: "getDisplayStatus"
    - from: "src/lib/orderConstants.ts"
      to: "src/pages/OrderDetail.tsx"
      via: "getDisplayStatus used for status step display"
      pattern: "getDisplayStatus"
---

<objective>
Remove deprecated order statuses (ProductionComplete, Packaging) from UI display mappings while preserving them as filter options and keeping all backend code untouched (QFIX-04).

Purpose: Clean up confusing deprecated status entries in UI while honoring the user decision that old orders must still be findable and operable.
Output: getDisplayStatus() helper created, 5-6 UI files updated, backend unchanged.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tech-debt/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create getDisplayStatus helper and update status color/label maps</name>
  <files>
    src/lib/orderConstants.ts
    src/components/dashboard/ProductionQueueTable.tsx
    src/components/orders/OrderHeader.tsx
  </files>
  <action>
**Step 1: Add `getDisplayStatus()` helper to `src/lib/orderConstants.ts`:**

Add a new exported function that maps deprecated statuses to their active equivalents. Place it near the top of the file, after the type imports:

```typescript
/**
 * Map deprecated statuses to their display equivalents.
 * Used for UI display only -- backend and filters keep original values.
 * ProductionComplete -> Boxed (both mean "production done, ready for next step")
 * Packaging -> InProduction (packaging was the old step between Confirmed and Completion)
 */
export function getDisplayStatus(status: OrderStatus): OrderStatus {
  switch (status) {
    case 'ProductionComplete': return 'Boxed';
    case 'Packaging': return 'InProduction';
    default: return status;
  }
}
```

Note: The `OrderStatus` type must be imported if not already. Check what type is used in this file -- it may use the Convex-generated type or a local type from `src/lib/types.ts`. Use whichever is already in scope.

**Step 2: Update `STATUS_COLORS` in `src/lib/orderConstants.ts`:**
Remove the `ProductionComplete` and `Packaging` entries from the `STATUS_COLORS` record. Their colors are already covered by `Boxed` and `InProduction` respectively, and all display code will go through `getDisplayStatus()` first.

If there is a `STATUS_LABELS` or similar display label mapping, remove deprecated entries from that too.

**Step 3: Update `STATUS_CONFIG` in `src/components/dashboard/ProductionQueueTable.tsx`:**
Remove `ProductionComplete` and `Packaging` entries from the `STATUS_CONFIG` record (lines ~13-30). This component only displays active production statuses -- deprecated ones will be mapped before reaching this component.

**Step 4: Update `src/components/orders/OrderHeader.tsx`:**
- Remove deprecated status entries from the inline status color map (lines ~11-12)
- Update the badge rendering to use `getDisplayStatus(order.status)` for the displayed label and color lookup, while keeping the raw `order.status` for any data operations
- Import `getDisplayStatus` from `@/lib/orderConstants`

**CRITICAL RULES (from user decisions):**
- Do NOT change any TypeScript types -- `OrderStatus` keeps `ProductionComplete` and `Packaging`
- Do NOT change any backend files (convex/ directory is off-limits for this task)
- Do NOT remove deprecated statuses from `STATUS_CATEGORIES.kitchen` yet -- that is a display category, not a filter
  </action>
  <verify>
Run: `npm run type-check` -- should pass.
Verify `getDisplayStatus` is exported from `src/lib/orderConstants.ts`.
Verify `ProductionComplete` and `Packaging` do NOT appear as keys in `STATUS_COLORS`.
  </verify>
  <done>getDisplayStatus() helper exists and is exported. STATUS_COLORS, STATUS_CONFIG, and OrderHeader no longer have dedicated entries for deprecated statuses. All display code routes through getDisplayStatus() for mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Update OrderStatusPanel filters and OrderDetail status display</name>
  <files>
    src/components/orders/OrderStatusPanel.tsx
    src/pages/OrderDetail.tsx
    src/components/dashboard/SalesWidget.tsx
  </files>
  <action>
**Step 1: Update `src/components/orders/OrderStatusPanel.tsx`:**
- KEEP deprecated statuses (`ProductionComplete`, `Packaging`) in `STATUS_OPTIONS` array -- users need these to filter for old orders (per locked user decision)
- When rendering the status option labels/badges, use `getDisplayStatus()` to show the mapped display name. For example, if a filter chip shows "ProductionComplete", it should instead show "Boxed" (but its VALUE when filtering should remain `ProductionComplete` so the query returns the correct results)
- Import `getDisplayStatus` from `@/lib/orderConstants`
- If there are color lookups for the filter options, route through `getDisplayStatus()` before looking up colors

**Step 2: Update `src/pages/OrderDetail.tsx`:**
- Find the status step mapping (lines ~52-55) and any status display code
- Use `getDisplayStatus()` for ALL visual status display (badges, step indicators, labels)
- Keep the raw status value for all data operations (status transitions, API calls, comparisons with backend values)
- Import `getDisplayStatus` from `@/lib/orderConstants`
- Specifically handle: status step position calculation, status badge color, status label text
- Do NOT change any mutation calls or status comparison logic -- only visual display

**Step 3: Check `src/components/dashboard/SalesWidget.tsx`:**
- Read the file and check if it has any status display code (color maps, status labels)
- If it references `ProductionComplete` or `Packaging` in display code, route through `getDisplayStatus()`
- If it only uses these in data aggregation/filtering (not display), leave unchanged

**Step 4: Check for `STATUS_CATEGORIES.kitchen`:**
- In `src/lib/orderConstants.ts`, if `Packaging` appears in `STATUS_CATEGORIES.kitchen`, remove it from that list. Kitchen category should only contain active statuses that the kitchen view actually uses.
- If `ProductionComplete` appears in any STATUS_CATEGORIES, remove it too.

**CRITICAL RULES:**
- Deprecated statuses MUST remain as filter options in OrderStatusPanel (user decision: important for accounting, sales, discount review)
- The filter VALUES must stay as the original deprecated status strings so backend queries work correctly
- Only the DISPLAY (label, color, badge) uses the mapped equivalent
  </action>
  <verify>
Run: `npm run build` -- must pass.
Verify `ProductionComplete` and `Packaging` still appear as filterable options in OrderStatusPanel source code.
Verify `getDisplayStatus` is used in OrderDetail.tsx for display rendering.
Grep: `grep -r "getDisplayStatus" src/` should show usage in orderConstants, OrderHeader, OrderStatusPanel, and OrderDetail.
  </verify>
  <done>OrderStatusPanel keeps deprecated statuses as filter options but displays them with mapped names. OrderDetail uses getDisplayStatus() for all visual status rendering. SalesWidget updated if applicable. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. `grep -r "getDisplayStatus" src/` shows helper used in all status display locations
3. `ProductionComplete` and `Packaging` still appear in OrderStatusPanel filter options (as values, displayed with mapped names)
4. No backend files were modified (zero changes in `convex/` directory)
5. `OrderStatus` type still includes `ProductionComplete` and `Packaging`
</verification>

<success_criteria>
- `getDisplayStatus()` helper exported from `src/lib/orderConstants.ts`
- Deprecated statuses mapped to active equivalents in all UI display code
- Deprecated statuses preserved as filter options for finding old orders
- No backend changes, no TypeScript type changes
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-tech-debt/03-02-SUMMARY.md`
</output>
