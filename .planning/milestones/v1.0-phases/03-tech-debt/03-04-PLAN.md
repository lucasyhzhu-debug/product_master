---
phase: 03-tech-debt
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - convex/orders/mutations.ts
  - convex/orders/mutations/index.ts
  - src/hooks/convex/useOrders.ts
  - src/pages/KitchenViewV2.tsx
  - src/pages/PackagingView.tsx
  - src/hooks/convex/useKitchenStats.ts
  - tests/convex/helpers.ts
  - tests/convex/ballDistribution.test.ts
  - tests/convex/orderLifecycle.test.ts
  - tests/convex/orders.test.ts
  - src/hooks/__tests__/useConvexHooks.test.tsx
  - convex/_generated/api.d.ts
autonomous: true

must_haves:
  truths:
    - "convex/orders/mutations.ts shim file is deleted from the codebase"
    - "All frontend, hook, and test imports reference the correct API path for order mutations"
    - "npm run build passes after the shim is removed and all callers updated"
    - "npm run test passes -- no test failures from import path changes"
  artifacts:
    - path: "convex/orders/mutations/index.ts"
      provides: "Barrel export file that is now the sole source of order mutation exports"
    - path: "src/hooks/convex/useOrders.ts"
      provides: "Updated imports using correct API path for order mutations"
  key_links:
    - from: "src/hooks/convex/useOrders.ts"
      to: "convex/orders/mutations/index.ts"
      via: "api.orders.mutations.* path in useMutation calls"
      pattern: "api\\.orders\\.mutations"
    - from: "tests/convex/orderLifecycle.test.ts"
      to: "convex/orders/mutations/index.ts"
      via: "api.orders.mutations.* path in test assertions"
      pattern: "api\\.orders\\.mutations"
---

<objective>
Remove the deprecated `convex/orders/mutations.ts` shim file and update all callers to use the correct API path (QFIX-03).

Purpose: Eliminate the confusing indirection layer that re-exports all order mutations through a shim file. After removal, imports go directly to the domain-specific mutation files.
Output: Shim file deleted, all 30+ callers updated, build and tests pass.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tech-debt/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test Convex API path resolution after shim deletion</name>
  <files>
    convex/orders/mutations.ts
    convex/_generated/api.d.ts
  </files>
  <action>
This is the CRITICAL verification step that determines the approach for the rest of the plan.

**Step 1: Record current state.**
Read `convex/_generated/api.d.ts` and note the current API path structure for order mutations. Currently it should show both:
- `"orders/mutations"` -- from the shim file
- `"orders/mutations/index"` -- from the directory index

Note whether the current code uses `api.orders.mutations.X` (from shim) or `api.orders.mutations.index.X` (from directory).

**Step 2: Search ALL callers.**
Run comprehensive grep to find every file that references order mutation API paths:
```
grep -rn "api\.orders\.mutations" src/ tests/ convex/ --include="*.ts" --include="*.tsx"
```
Record the exact import pattern used (e.g., `api.orders.mutations.create` vs `api.orders.mutations.index.create`).

Also check for any imports directly from the shim path:
```
grep -rn "from.*orders/mutations" src/ tests/ convex/ --include="*.ts" --include="*.tsx"
```

**Step 3: Determine resolution behavior.**

There are two possible outcomes after deleting the shim:

**Outcome A (trivial):** Convex resolves `api.orders.mutations.X` via the directory's `index.ts` automatically. In this case, simply delete the shim and run `npx convex dev` to regenerate types. No caller changes needed.

**Outcome B (migration needed):** `api.orders.mutations.X` no longer resolves. Callers must be updated to use either:
- `api.orders.mutations.index.X` (if Convex generates this path for `convex/orders/mutations/index.ts`)
- Or domain-specific paths like `api.orders.mutations.orderCrud.create`

To test: The safest approach is to check the generated `api.d.ts` for what paths exist for `convex/orders/mutations/index.ts` CURRENTLY. If `api.orders.mutations.index` already exists as a valid module, then after shim deletion, callers can be updated to `api.orders.mutations.index.X`.

**Step 4: Document the finding** for Task 2 to use.

Do NOT delete the shim yet -- just determine the path and document which callers need updating (if any).
  </action>
  <verify>
The API path resolution behavior is documented. The list of all callers is captured. The migration strategy (trivial vs full) is determined.
  </verify>
  <done>API path resolution behavior confirmed. Full caller list captured. Migration strategy chosen (Outcome A or B).</done>
</task>

<task type="auto">
  <name>Task 2: Delete shim and update all callers</name>
  <files>
    convex/orders/mutations.ts
    src/hooks/convex/useOrders.ts
    src/pages/KitchenViewV2.tsx
    src/pages/PackagingView.tsx
    src/hooks/convex/useKitchenStats.ts
    tests/convex/helpers.ts
    tests/convex/ballDistribution.test.ts
    tests/convex/orderLifecycle.test.ts
    tests/convex/orders.test.ts
    src/hooks/__tests__/useConvexHooks.test.tsx
  </files>
  <action>
Based on Task 1's findings, execute the appropriate strategy:

**If Outcome A (trivial -- api.orders.mutations.X still resolves via directory index):**
1. Delete `convex/orders/mutations.ts`
2. Run type-check to confirm everything still resolves
3. Done -- no caller changes needed

**If Outcome B (migration needed -- callers must be updated):**

1. Delete `convex/orders/mutations.ts`

2. Update ALL callers found in Task 1. The likely new path pattern depends on how Convex generates the API:
   - If `api.orders.mutations.index.X` works: Replace `api.orders.mutations.X` with `api.orders.mutations.index.X` everywhere
   - If domain-specific paths needed: Replace `api.orders.mutations.create` with `api.orders.mutations.orderCrud.create`, etc.

3. **Known callers from research (update ALL of these):**

   Frontend files:
   - `src/hooks/convex/useOrders.ts` (~12 references) -- This is the main hook file. All `useMutation(api.orders.mutations.X)` calls need path updates.
   - `src/pages/KitchenViewV2.tsx` (~5 references) -- Kitchen mutation calls
   - `src/pages/PackagingView.tsx` (~1 reference) -- Packaging mutation calls
   - `src/hooks/convex/useKitchenStats.ts` (~5 references) -- Kitchen stats hook

   Test files:
   - `tests/convex/helpers.ts` (~2 references) -- Test helper functions
   - `tests/convex/ballDistribution.test.ts` (~30+ references) -- Ball distribution tests
   - `tests/convex/orderLifecycle.test.ts` (~50+ references) -- Order lifecycle tests
   - `tests/convex/orders.test.ts` (~25+ references) -- Order unit tests
   - `src/hooks/__tests__/useConvexHooks.test.tsx` (~10 references) -- Hook tests

4. After ALL callers are updated, run the full build and test suite.

**Mutation name to source file mapping (for domain-specific paths if needed):**
- `create`, `cancel`, `remove`, `completeOrder`, `revertToConfirmed`, `updateOrderDiscount`, `completeBalls` -> `orderCrud`
- `addItem`, `removeItem`, `updateItemQuantity`, `replaceItems` -> `itemCrud`
- `updateStatus`, `updatePayment`, `updateShipping`, `updateDetails` -> `statusUpdates`
- `addBallsToTray`, `fillPendingOrders`, `removeBallFromTray`, `boxProducts`, `stickerProducts`, `togglePackOrderLineItem`, `markOrderReady` -> `kitchen`
- `markPackagePacked`, `completePackaging`, `revertToPackaging`, `markAllItemPackagesPacked`, `unmarkPackagePacked`, `fillPackage`, `unfillPackage` -> `packaging`
- `backfillOrderItemProduction`, `migrateChannelCodes`, `backfillProductionRecords`, `migratePackagingToBoxed` -> `migrations`
- `reserveStockForOrder`, `consumeBoxingMaterials`, `consumeStickerMaterials`, `releaseReservation` -> `inventoryIntegration`

**IMPORTANT:** This is a single-commit clean break per user decision. Do NOT leave a transitional compatibility layer. All callers updated in one pass.
  </action>
  <verify>
Run: `npm run type-check` -- must pass with zero errors.
Run: `npm run build` -- must pass.
Run: `npm run test` -- must pass (all test files updated).
Run: `ls convex/orders/mutations.ts` -- should return "no such file".
Run: `grep -r "from.*['\"].*orders/mutations['\"]" convex/` -- should return zero results (no leftover import of the deleted shim).
  </verify>
  <done>Shim file deleted. All callers (frontend + tests) updated to use correct API path. Build passes. Tests pass. Single clean-break commit.</done>
</task>

</tasks>

<verification>
1. `convex/orders/mutations.ts` does not exist
2. `npm run type-check` passes
3. `npm run build` passes
4. `npm run test` passes
5. `grep -r "orders/mutations['\"]" convex/ --include="*.ts"` returns zero results (no references to deleted file)
6. All order mutation calls in frontend and tests use the correct API path
</verification>

<success_criteria>
- Shim file deleted, no backward compatibility layer remains
- Every caller (30+ across frontend, hooks, and tests) updated in one clean break
- `npm run build` and `npm run test` both pass
- No runtime regression -- all order mutations still work via the new paths
</success_criteria>

<output>
After completion, create `.planning/phases/03-tech-debt/03-04-SUMMARY.md`
</output>
