---
phase: 09-ui-brand
plan: 04
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - src/pages/Dashboard.tsx
  - src/pages/OrderManager.tsx
  - src/pages/OrderDetail.tsx
  - src/pages/KitchenViewV2.tsx
  - src/pages/Login.tsx
  - src/pages/RestockPlanner.tsx
  - src/components/orders/
  - src/components/kitchen/
  - src/components/dashboard/
autonomous: true

must_haves:
  truths:
    - "Dashboard uses PageHeader (not custom hero section)"
    - "OrderManager uses PageHeader with consistent styling (no custom header or terracotta underline)"
    - "KitchenViewV2 works correctly in full-width layout with brand colors"
    - "Login page uses teal brand accent instead of terracotta"
    - "OrderDetail uses semantic color tokens"
  artifacts: []
  key_links:
    - from: "src/pages/Dashboard.tsx"
      to: "src/components/layout/PageHeader.tsx"
      via: "PageHeader import"
      pattern: "PageHeader"
    - from: "src/pages/OrderManager.tsx"
      to: "src/components/layout/PageHeader.tsx"
      via: "PageHeader import"
      pattern: "PageHeader"
---

<objective>
Audit and fix the 6 complex pages (Dashboard, OrderManager, OrderDetail, KitchenViewV2, Login, RestockPlanner) plus their associated component files for brand consistency.

Purpose: These pages have custom layouts and embedded components that require more careful handling than standard list pages. Dashboard has a custom hero, OrderManager has terracotta styling and custom headers, KitchenViewV2 is full-width with domain-specific colors, Login is standalone.
Output: 6 updated page files + associated component files updated for semantic colors
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ui-brand/09-RESEARCH.md
@.planning/phases/09-ui-brand/09-01-SUMMARY.md
@.planning/phases/09-ui-brand/09-02-SUMMARY.md
@src/pages/Dashboard.tsx
@src/pages/OrderManager.tsx
@src/pages/KitchenViewV2.tsx
@src/pages/Login.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Dashboard, OrderManager, and OrderDetail pages</name>
  <files>
    src/pages/Dashboard.tsx
    src/pages/OrderManager.tsx
    src/pages/OrderDetail.tsx
    src/pages/RestockPlanner.tsx
  </files>
  <action>
**1. Fix `src/pages/Dashboard.tsx`:**

Current: Custom hero section with `space-y-8` wrapper, uses `text-3xl font-bold`.

Changes:
- Replace custom hero/header with `<PageHeader title="Dashboard" description="Overview of your business" />`
- Remove outer `space-y-8` wrapper (PageContainer provides padding)
- Change internal content spacing to `space-y-6`
- Replace all hardcoded colors:
  - `text-gray-*` -> semantic tokens
  - `bg-gray-*` / `bg-white` -> `bg-card` / `bg-muted`
  - `border-gray-*` -> `border-border`
  - Any terracotta references -> brand equivalents
  - `text-dark-gradient-*` or gradient classes -> use brand-derived dark gradient or remove
- If Dashboard uses `--color-dark-gradient-from/to` (removed in Plan 01), replace with a simple dark background: `bg-foreground text-background` or a brand-tinted dark: `bg-brand-darker text-white`
- Keep all dashboard cards, stats, charts — only change styling

**2. Fix `src/pages/OrderManager.tsx`:**

Current: Custom `<h1>` with `text-2xl md:text-3xl`, terracotta underline decoration, `space-y-4 md:space-y-6 pb-6` wrapper, `order-heading` class (Playfair Display).

Changes:
- Replace custom header section with `<PageHeader title="Orders" description="Manage customer orders" action={...} />`
- Remove `order-heading` class usage (Playfair Display removed) — use default Inter bold from PageHeader
- Remove terracotta underline decoration (CSS border-bottom with terracotta color) — use standard PageHeader bottom margin
- Remove outer `space-y-4 md:space-y-6 pb-6` wrapper
- Note: OrderManager renders in full-width layout (no PageContainer). So it DOES need its own horizontal padding. Add: `<div className="px-4 sm:px-6 lg:px-8 space-y-6">` as the page wrapper.
- Replace all hardcoded colors with semantic tokens
- Replace `text-terracotta` / `bg-terracotta*` / `border-terracotta*` with `text-brand` / `bg-brand*` / `border-brand*`
- Replace any `hover:bg-terracotta*` with `hover:bg-brand*`
- Check for `order-card-hover` usage — this CSS class was updated in Plan 01 to use brand colors, so no change needed in the page
- Replace any `window.innerWidth` checks with Tailwind responsive classes (research found 1 occurrence in OrderManager)

**3. Fix `src/pages/OrderDetail.tsx`:**

Current: Uses PageHeader with back button. Standard layout.

Changes:
- Remove outer padding wrapper if present (PageContainer provides it)
- Replace all hardcoded colors with semantic tokens
- Replace terracotta references with brand
- Replace `order-heading` class if used
- Ensure `space-y-6` for content spacing

**4. Fix `src/pages/RestockPlanner.tsx`:**

Current: Has `container py-6 space-y-6` wrapper and uses PageHeader.

Changes:
- Remove `container py-6` wrapper (redundant with PageContainer)
- Keep `space-y-6` for content spacing
- Replace hardcoded colors with semantic tokens
- This page redirects to K3MartCockpit but still exists — clean it up anyway

**For all pages, apply standard color replacement:**
- `text-gray-900/800` -> `text-foreground`
- `text-gray-700/600` -> `text-foreground/80` or `text-muted-foreground`
- `text-gray-500/400` -> `text-muted-foreground`
- `bg-gray-50/100` -> `bg-muted` or `bg-muted/50`
- `bg-gray-200` -> `bg-muted`
- `bg-white` -> `bg-card` or `bg-background`
- `border-gray-*` -> `border-border`
- `divide-gray-*` -> `divide-border`

**IMPORTANT:**
- Preserve all business logic, data fetching, mutations
- OrderManager is full-width — ensure it has its own horizontal padding
- Dashboard summary panels that use dark gradients need updated gradient colors
- Read each page file carefully before modifying
  </action>
  <verify>
1. `npm run type-check` passes
2. `grep -c "order-heading" src/pages/OrderManager.tsx src/pages/OrderDetail.tsx` returns 0
3. `grep -c "terracotta" src/pages/OrderManager.tsx src/pages/Dashboard.tsx` returns 0
4. `grep "PageHeader" src/pages/Dashboard.tsx src/pages/OrderManager.tsx` confirms both use PageHeader
5. `grep "container py-6" src/pages/RestockPlanner.tsx` returns 0
  </verify>
  <done>
Dashboard uses PageHeader (no custom hero). OrderManager uses PageHeader with brand colors (no terracotta underline, no Playfair Display). OrderDetail cleaned of hardcoded colors. RestockPlanner wrapper simplified. All 4 pages use semantic color tokens.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix KitchenViewV2, Login, and component-level color cleanup</name>
  <files>
    src/pages/KitchenViewV2.tsx
    src/pages/Login.tsx
    src/components/kitchen/
    src/components/orders/
    src/components/dashboard/
  </files>
  <action>
**1. Fix `src/pages/KitchenViewV2.tsx`:**

Current: Custom full-height layout with `text-xl sm:text-2xl` title, custom swipeable panels, kitchen-specific colors.

Changes:
- KitchenViewV2 renders in full-width layout (no PageContainer). It needs its own padding.
- Replace the custom title with `<PageHeader title="Kitchen" action={...} />`. The PageHeader should be inside the page's own padding wrapper: `<div className="px-4 sm:px-6 py-4">`.
- OR if the kitchen layout is too custom for PageHeader (e.g., title is part of a status bar), keep the custom title but standardize:
  - Title size: `text-2xl font-bold tracking-tight` (matches PageHeader standard)
  - Remove any `text-xl sm:text-2xl` (inconsistent)
- **KEEP all kitchen station colors** (`--color-station-*`) — these are semantic/domain colors, NOT brand accent
- Replace NON-kitchen hardcoded grays with semantic tokens
- Replace any terracotta references with brand equivalents
- Keep `--color-kitchen-page-bg` usage (dark mode override was added in Plan 01)
- DO NOT change swipe behavior, panel layout, or production logic

**2. Fix `src/pages/Login.tsx`:**

Current: Full-screen gradient layout with custom brand header, avatar grid, PIN input.

Changes:
- Keep standalone layout (no Header/Footer/MobileBottomNav — unauthenticated page)
- Replace terracotta accent colors with brand teal:
  - Gradient: replace any `from-terracotta` or terracotta-tinted gradient with teal-tinted gradient or warm neutral gradient
  - Buttons: primary button should use brand teal (it will automatically if using shadcn Button with default variant, since primary was changed to teal in Plan 01)
  - Focus rings: replace terracotta focus with brand focus
  - Any text-terracotta -> text-brand
- Replace hardcoded grays with semantic tokens (important for dark mode)
- `bg-white` -> `bg-card` or `bg-background`
- Login may have inline styles or special gradient classes — update those to use brand-derived values
- Ensure the login page looks good in both light and dark mode:
  - Light: warm gradient background, white card
  - Dark: dark gradient background, dark card
  - Use `bg-background` and `bg-card` which automatically adapt
- Remove any `Playfair Display` font usage if present
- Keep the avatar-grid + PIN entry flow exactly as-is

**3. Component-level color cleanup:**

Scan and fix hardcoded colors in shared component directories. Do a targeted sweep of:

a) **`src/components/orders/`** — Order-related components:
- Replace `text-terracotta` / `bg-terracotta*` with `text-brand` / `bg-brand*`
- Replace `order-heading` class with `font-bold`
- Replace hardcoded grays with semantic tokens
- Keep `order-card-hover` usage (CSS class was updated in Plan 01)

b) **`src/components/kitchen/`** — Kitchen components:
- **KEEP kitchen station colors** (semantic)
- Replace NON-kitchen hardcoded grays/whites with semantic tokens
- Replace any terracotta references with brand

c) **`src/components/dashboard/`** — Dashboard card components:
- Replace hardcoded grays with semantic tokens
- Replace dark gradient from/to with brand-derived alternative
- `bg-white` -> `bg-card`

For each component directory, read each file first, then make targeted color replacements. Focus on the most impactful changes — files that are visually prominent.

**Priority order for component sweep:**
1. Any file with `terracotta` in className → replace with `brand`
2. Any file with `bg-white` in className → replace with `bg-card` or `bg-background`
3. Any file with `text-gray-900` → replace with `text-foreground`
4. Any file with `bg-gray-100` or `bg-gray-50` → replace with `bg-muted` or `bg-muted/50`

Use grep to find all occurrences across these directories, then fix systematically.

**IMPORTANT:**
- Do NOT change kitchen station colors or kitchen domain colors
- Do NOT change GoFood/K3Mart semantic colors
- Login page must work for unauthenticated users (no useAuth dependency for theme)
- Preserve all component logic and functionality
- Read each component file before modifying
  </action>
  <verify>
1. `npm run type-check` passes
2. `npm run build` passes
3. `grep -rn "terracotta" src/pages/ src/components/orders/ src/components/dashboard/` returns 0 (all replaced with brand)
4. `grep -rn "order-heading" src/` returns 0 (Playfair Display class removed everywhere)
5. Kitchen station colors are preserved: `grep "station-production\|station-boxing\|station-stickering\|station-packing" src/components/kitchen/` still returns matches
6. Login page has no terracotta references
  </verify>
  <done>
KitchenViewV2 title standardized, non-kitchen colors replaced with semantic tokens, kitchen station colors preserved. Login page uses teal brand accent and works in both light/dark themes. Order components, kitchen components, and dashboard components cleaned of hardcoded grays and terracotta references. All color references now use either semantic tokens or domain-specific CSS variables.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. All 6 complex pages audited and fixed
3. `grep -rn "terracotta" src/pages/ src/components/orders/ src/components/dashboard/ src/components/kitchen/` returns 0 (kitchen station colors use `station-*` not `terracotta`)
4. `grep -rn "order-heading" src/` returns 0
5. Dashboard and OrderManager use PageHeader
6. Kitchen station colors preserved
7. Login page uses brand teal
</verification>

<success_criteria>
- Dashboard, OrderManager, OrderDetail, KitchenViewV2, Login, RestockPlanner all updated
- No terracotta color references remain in page or component files
- No `order-heading` class usage remains (Playfair Display removed)
- Kitchen station/domain colors preserved
- Component-level color cleanup complete in orders/, kitchen/, dashboard/ dirs
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-brand/09-04-SUMMARY.md`
</output>
