---
phase: 53.1-fix-gobiz-promo-discount-net-inflation
verified: 2026-03-15T21:16:00Z
status: passed
score: 6/6 must-haves verified
re_verification: false
---

# Phase 53.1: Fix GoBiz Promo Discount Net Revenue Inflation -- Verification Report

**Phase Goal:** Fix the two backend bugs causing GoFood promo discount net revenue inflation in Sales Analytics, and update the frontend to display promo discount as a visible deduction.
**Verified:** 2026-03-15T21:16:00Z
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | GOFOOD channel Net in Sales Analytics = sum of `revenueNet` from externalRevenue (not recalculated from gross - commission) | VERIFIED | `dashboardHelpers.ts` line 33: `net += r.revenueNet ?? (r.revenueGross ?? 0)` -- no recalculation formula present. Old pattern `gross - commission - adBurn - promoBurn` completely removed. 8 unit tests verify this behavior including edge cases (revenueNet=0 treated as valid zero, undefined falls back to revenueGross). |
| 2 | Promo discount amount extracted during GoBiz sync and stored in `externalRevenue.promoBurn` | VERIFIED | `gobiz/helpers.ts` lines 294-295: `promoDiscountRaw = share?.metadata?.variables?.voucher_amount ?? 0; promoDiscount = promoDiscountRaw / CENTESIMAL_DIVISOR`. `adapter.ts` line 390: `promoBurn: txn.promoDiscount` wires it to `saveRevenue` mutation. 3 unit tests verify extraction (24500 from 2450000 centesimal, 0 when absent, aggregate sum). |
| 3 | Sales Analytics shows promo discount as separate deduction line for GoFood channels | VERIFIED | `ChannelSummary.tsx` lines 116-121: conditional rendering `{seg.current.promoBurn > 0 && (...)}` with orange text styling. Renders `-{formatCurrency(seg.current.promoBurn)}` with "Promo Discount" label between Gross and Net. |
| 4 | Non-promo orders (Crystal Timur A, G347061572) show unchanged net values | VERIFIED | `dashboardHelpers.ts` aggregation sums `revenueNet` per-record -- records without promoBurn simply have promoBurn=0 and revenueNet already correct. No formula change affects non-promo records. Unit test "should produce correct net for BigSeller records with revenueNet set" confirms non-promo path unchanged. |
| 5 | BigSeller channels unaffected by aggregation refactor | VERIFIED | `dashboardHelpers.ts` uses same `aggregatePlatformChannel()` function for ALL non-internal sources. Unit test "should produce correct net for BigSeller records with revenueNet set" specifically tests `bigseller_shopee` source with revenueNet=85000, confirms net=85000. Cross-channel test "should aggregate totalPromoBurn correctly across channels" verifies gobiz+bigseller coexistence. |
| 6 | All existing tests pass, npm run build succeeds | VERIFIED | 47/47 phase-specific tests pass (8 dashboardHelpers + 39 gobiz helpers). Build completes successfully in 16.90s with no errors. |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `convex/externalData/__tests__/dashboardHelpers.test.ts` | Unit tests for aggregatePlatformChannel using revenueNet | VERIFIED | 210 lines, 8 tests covering: stored revenueNet usage, divergent revenueNet vs recalculated, revenueNet=0 edge case, undefined fallback, BigSeller regression, commission/promoBurn in channels, cross-channel totalPromoBurn, internal channel zero values |
| `convex/externalData/helpers/dashboardHelpers.ts` | Fixed aggregation using stored revenueNet + per-channel commission/promoBurn | VERIFIED | 131 lines. Line 33 reads `r.revenueNet`. Channels array type includes `commission: number; promoBurn: number`. Both platform and internal channels emit these fields. |
| `convex/integrations/gobiz/helpers.ts` | JournalMetrics with promoDiscount, extractJournalMetrics extracts voucher_amount | VERIFIED | `promoDiscount: number` on JournalMetrics interface (line 248). `extractJournalMetrics` reads `share?.metadata?.variables?.voucher_amount ?? 0` and divides by 100 (lines 294-295). `aggregateJournalMetrics` accumulates `promoDiscount` (line 339). |
| `convex/integrations/gobiz/adapter.ts` | saveJournalTransactions passes promoBurn from txn.promoDiscount | VERIFIED | Line 390: `promoBurn: txn.promoDiscount`. Line 585: conditional promo logging `(dayMetrics.promoDiscount > 0 ? ...Promo: Rp...` : "")`. |
| `convex/integrations/gobiz/__tests__/helpers.test.ts` | 3 new promo tests added to existing test file | VERIFIED | Lines 327-365: "should extract promoDiscount from variables.voucher_amount / 100" (promoHit fixture with voucher_amount=2450000, asserts promoDiscount=24500). "should return promoDiscount=0 when voucher_amount is absent". Lines 440-500: "should include promoDiscount in aggregate totals" (2 promo hits, asserts sum=40250). |
| `src/components/salesAnalytics/overviewUtils.ts` | Extended PeriodData.channels type with commission and promoBurn | VERIFIED | Lines 38-39: `commission: number; promoBurn: number;` -- required fields (not optional), matching backend output. |
| `src/components/salesAnalytics/ChannelSummary.tsx` | Promo discount display line in channel cards | VERIFIED | Lines 29: segment type includes `commission: number; promoBurn: number`. Lines 42-43: "All Channels" segment maps from totalCommission/totalPromoBurn. Line 57: per-channel maps from ch.commission/ch.promoBurn. Lines 116-121: conditional orange promo discount rendering. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `gobiz/helpers.ts` | `gobiz/adapter.ts` | JournalMetrics.promoDiscount consumed by saveJournalTransactions | WIRED | adapter.ts line 390: `promoBurn: txn.promoDiscount` -- txn is typed as JournalMetrics. adapter.ts imports `type JournalMetrics` from helpers (line 20). |
| `dashboardHelpers.ts` | externalRevenue table | r.revenueNet field read in aggregatePlatformChannel | WIRED | Line 33: `r.revenueNet ?? (r.revenueGross ?? 0)` -- r is typed as `Doc<"externalRevenue">` from Convex schema. |
| `ChannelSummary.tsx` | `overviewUtils.ts` | PeriodData type with channels.promoBurn | WIRED | ChannelSummary imports PeriodData (line 5), uses it for currentPeriod/previousPeriod props (lines 11-12). Accesses ch.commission and ch.promoBurn (line 57). |
| `overviewUtils.ts` | `dashboardHelpers.ts` | PeriodData mirrors aggregatePeriodRevenue return shape | WIRED | Both define channels with `commission: number; promoBurn: number`. Backend return type at line 68-72 matches PeriodData channels at lines 32-40. |

### Requirements Coverage

No formal requirement IDs mapped to this phase (bug fix, HAR-verified). No orphaned requirements found in REQUIREMENTS.md for Phase 53.1.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | - | - | - | No TODO, FIXME, placeholder, or stub patterns found in any modified file |

### Human Verification Required

### 1. Visual Promo Discount Display in Sales Analytics

**Test:** Navigate to Sales Analytics, select a period with Legato Gelato (GoFood) promo orders. Inspect the GoFood channel card.
**Expected:** Orange promo discount line between Gross and Net showing negative amount (e.g., "-Rp 24,500 Promo Discount"). BigSeller/internal channels should NOT show this line. "All Channels" card should show aggregated promo discount if any channel has promo data.
**Why human:** Visual rendering, conditional CSS styling, and real data display cannot be verified programmatically.

### 2. End-to-End Sync with promoBurn Population

**Test:** Trigger a GoBiz "Refresh All" sync in dev, then check externalRevenue documents for newly synced promo orders.
**Expected:** New promo order records should have `promoBurn` field populated with the merchant's campaign discount share (e.g., 24500 for a 140K order with 596f5dc6 campaign).
**Why human:** Requires live GoBiz API connection and database inspection. Adapter uses Convex mutation context that cannot be unit tested.

### Gaps Summary

No gaps found. All 6 success criteria from ROADMAP.md are satisfied:

1. Backend aggregation uses stored `revenueNet` -- confirmed by code inspection and 8 unit tests
2. GoBiz sync extracts `voucher_amount` as `promoDiscount` and wires to `promoBurn` -- confirmed by code and 3 unit tests
3. Frontend displays promo discount as separate deduction line in ChannelSummary -- confirmed by JSX inspection
4. Non-promo orders unaffected -- confirmed by unchanged aggregation path for records without promoBurn
5. BigSeller channels unaffected -- confirmed by unit test with `bigseller_shopee` source
6. All tests pass (47/47), build succeeds (16.90s, no errors)

---

_Verified: 2026-03-15T21:16:00Z_
_Verifier: Claude (gsd-verifier)_
