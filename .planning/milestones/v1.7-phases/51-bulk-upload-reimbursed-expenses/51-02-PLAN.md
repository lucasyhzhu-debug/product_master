---
phase: 51-bulk-upload-reimbursed-expenses
plan: 02
type: tdd
wave: 2
depends_on: ["51-01"]
files_modified:
  - convex/journalImport/mutations.ts
  - convex/journalImport/__tests__/mutations.test.ts
  - src/lib/csvImportValidation.ts
  - src/lib/__tests__/csvImportValidation.test.ts
  - package.json
  - package-lock.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "bulkCreateJournalEntries mutation validates and creates JEs for batches of up to 50 rows"
    - "validateImportRow rejects negative, zero, non-integer amounts and unknown/inactive account codes"
    - "parseAndValidateCsv parses CSV with Papa Parse, validates rows, converts dates to WIB epoch, and detects duplicates as warnings"
    - "Invalid rows in a batch cause the entire batch to be rejected (fail-fast, no partial writes)"
  artifacts:
    - path: "convex/journalImport/mutations.ts"
      provides: "bulkCreateJournalEntries protectedMutation + validateImportRow pure function"
      exports: ["bulkCreateJournalEntries", "validateImportRow", "MAX_BATCH_SIZE", "ImportRow", "AccountMap"]
    - path: "convex/journalImport/__tests__/mutations.test.ts"
      provides: "Backend validation tests (TDD)"
      min_lines: 40
    - path: "src/lib/csvImportValidation.ts"
      provides: "Client-side CSV parse + validate + date conversion"
      exports: ["parseAndValidateCsv", "dateToWibEpoch", "CsvParseResult", "AccountRef", "ImportRow"]
    - path: "src/lib/__tests__/csvImportValidation.test.ts"
      provides: "Client validation tests (TDD)"
      min_lines: 60
  key_links:
    - from: "convex/journalImport/mutations.ts"
      to: "convex/lib/journalEngine.ts"
      via: "createJournalEntryWithLines call"
      pattern: "createJournalEntryWithLines"
    - from: "convex/journalImport/mutations.ts"
      to: "convex/lib/functions.ts"
      via: "protectedMutation wrapper"
      pattern: "protectedMutation"
    - from: "src/lib/csvImportValidation.ts"
      to: "papaparse"
      via: "Papa.parse for CSV parsing"
      pattern: "Papa\\.parse"
---

<objective>
Build and test the backend bulk import mutation and client-side CSV validation helpers using TDD.

Purpose: The mutation is the core engine that creates one JE per CSV row (DR expense account, CR 1100 Cash) with fail-fast batch validation. The client validation helpers parse CSV via Papa Parse, validate against the active Chart of Accounts, and convert dates to WIB epoch. Both are tested first (RED), then implemented (GREEN).

Output: Working `bulkCreateJournalEntries` mutation, `validateImportRow` pure function, `parseAndValidateCsv` helper, and `dateToWibEpoch` converter -- all with passing tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/plans/2026-03-14-historical-expense-journal-import.md
@.planning/phases/51-bulk-upload-reimbursed-expenses/51-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From convex/lib/journalEngine.ts (after Plan 51-01):
```typescript
export interface CreateJournalEntryParams {
  date: number;
  description: string;
  sourceType: JournalSourceType;
  sourceId?: string;
  createdBy: Id<"users">;
  lines: JournalLine[];
  metadata?: { receiptUrl?: string };
}

export interface JournalLine {
  accountId: Id<"accounts">;
  debitAmount: number;
  creditAmount: number;
  description?: string;
}

export type JournalSourceType =
  | "expense_approval" | "expense_void"
  | "reimbursement" | "reimbursement_void"
  | "payroll" | "payroll_void"
  | "manual";
```

From convex/lib/functions.ts:
```typescript
// protectedMutation wraps mutation with SessionIdArg + role check
// ctx.user._id is available — no token: v.string() needed
export const protectedMutation = customMutation(mutation, {
  args: SessionIdArg,
  input: async (ctx, { sessionId }, { roles }: { roles: UserRole[] }) => { ... }
});
```

From src/hooks/convex/createMutationHook.ts:
```typescript
export function createMutationHook<Mutation extends FunctionReference<"mutation">>(
  mutationRef: Mutation,
  config: MutationHookConfig, // { successMessage, errorMessage }
): () => { mutate, mutateAsync }
```

From src/lib/dateUtils.ts:
```typescript
export const WIB_OFFSET_MS = 7 * 60 * 60 * 1000;
export function wibDateStrToUtcMs(dateStr: string): number {
  return new Date(dateStr).getTime() - WIB_OFFSET_MS;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend mutation tests (RED) then implementation (GREEN)</name>
  <files>convex/journalImport/__tests__/mutations.test.ts, convex/journalImport/mutations.ts</files>
  <behavior>
    - Test: valid row passes validateImportRow (returns null)
    - Test: missing description returns error containing "description"
    - Test: negative amount returns error containing "positive"
    - Test: zero amount returns error containing "positive"
    - Test: non-integer amount returns error containing "integer"
    - Test: unknown account code returns error containing "not found"
    - Test: inactive account code returns error containing "inactive"
    - Test: valid row with optional vendorName passes
    - Test: valid row with optional receiptUrl passes
    - Test: MAX_BATCH_SIZE equals 50
    - Test: metadata with receiptUrl is preserved through journal engine (regression guard for silent-drop risk)
  </behavior>
  <action>
**RED phase -- Write tests first:**

Create `convex/journalImport/__tests__/mutations.test.ts` with tests for the pure `validateImportRow` function and `MAX_BATCH_SIZE` constant. These are pure function tests (no Convex ctx needed).

Import from `../mutations`:
- `validateImportRow` -- returns `string | null` (error message or null for valid)
- `MAX_BATCH_SIZE` -- should be 50
- `ImportRow` type -- `{ date: number, amount: number, description: string, vendorName?: string, accountCode: string, receiptUrl?: string }`
- `AccountMap` type -- `Map<string, { id: Id<"accounts">, isActive: boolean }>`

Use mock account maps for testing:
- `activeAccounts`: Map with "6100", "6200", "1100" all active
- `inactiveAccounts`: Same plus "6300" with isActive: false

Add a test for metadata persistence: create a row with `receiptUrl`, call `validateImportRow` to confirm it passes validation. This ensures the interface accepts the field. (The actual DB persistence test is a regression guard — existing journal engine tests verify insert behavior, and Plan 01 already ensures the metadata spread is present.)

Run tests -- expect FAIL (module not found).
Commit: `test(51): add failing tests for bulk import validation`

**GREEN phase -- Implement mutation:**

Create `convex/journalImport/mutations.ts` with:
1. `MAX_BATCH_SIZE = 50` constant
2. `CASH_ACCOUNT_CODE = "1100"` constant
3. `DESCRIPTION_PREFIX = "[Historical Import]"` constant
4. `ImportRow` interface (exported for testing). Add comment: `// Parallel type exists in src/lib/csvImportValidation.ts for frontend use`
5. `AccountMap` type (exported for testing): `Map<string, { id: Id<"accounts">, isActive: boolean }>`
6. `validateImportRow(row: ImportRow, accounts: AccountMap): string | null` pure function (exported for testing)
7. `bulkCreateJournalEntries` protectedMutation:
   - Auth: `protectedMutation({ roles: ["admin"] })`
   - Args: `importBatchId: v.string()`, `rows: v.array(v.object({ date: v.number(), amount: v.number(), description: v.string(), vendorName: v.optional(v.string()), accountCode: v.string(), receiptUrl: v.optional(v.string()) }))`
   - Enforces `rows.length <= MAX_BATCH_SIZE`
   - Builds account map from `ctx.db.query("accounts").collect()`
   - Validates ALL rows first (fail-fast), collects errors, throws if any
   - For each valid row: calls `createJournalEntryWithLines(ctx, { date, description: "[Historical Import] desc | vendor", sourceType: "manual", sourceId: importBatchId, createdBy: ctx.user._id, metadata: receiptUrl ? { receiptUrl } : undefined, lines: [DR expense account, CR cash 1100] })`
   - Returns `{ created: number }`

Import `createJournalEntryWithLines` from `../lib/journalEngine`.
Import `protectedMutation` from `../lib/functions`.

Run tests -- expect PASS.
Commit: `feat(51): add bulkCreateJournalEntries mutation with validation`
  </action>
  <verify>
    <automated>npx vitest run convex/journalImport/__tests__/mutations.test.ts && npm run type-check</automated>
  </verify>
  <done>
- validateImportRow correctly validates all cases (9 test cases pass)
- MAX_BATCH_SIZE is 50
- bulkCreateJournalEntries mutation exists with protectedMutation wrapper
- Mutation calls createJournalEntryWithLines per row (never direct ctx.db.insert)
- Type-check passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install Papa Parse and build client CSV validation (TDD)</name>
  <files>package.json, package-lock.json, src/lib/__tests__/csvImportValidation.test.ts, src/lib/csvImportValidation.ts</files>
  <behavior>
    - Test: dateToWibEpoch("2026-01-15") produces UTC hour 17, UTC date 14 (WIB midnight)
    - Test: dateToWibEpoch("not-a-date") returns NaN
    - Test: dateToWibEpoch("") returns NaN
    - Test: valid 2-row CSV produces 2 validRows, 0 errors
    - Test: amounts converted to numbers
    - Test: dates converted to WIB epoch numbers
    - Test: optional vendorName preserved or undefined
    - Test: optional receiptUrl preserved or undefined
    - Test: missing description produces error containing "description"
    - Test: negative amount produces error containing "positive"
    - Test: non-integer amount produces error containing "integer"
    - Test: unknown accountCode produces error containing "not found"
    - Test: inactive accountCode produces error containing "inactive"
    - Test: invalid date format produces error containing "date"
    - Test: CSV with quoted fields containing commas parses correctly
    - Test: duplicate rows (same date+amount+description) produce warning containing "duplicate"
    - Test: empty CSV (header only) returns 0 validRows, 0 errors
  </behavior>
  <action>
**Step 0 -- Install Papa Parse:**

Run: `npm install papaparse && npm install -D @types/papaparse`

CRITICAL: `papaparse` must be a runtime dependency (NOT devDependency). It runs in the browser. `@types/papaparse` is dev-only (types).

Commit: `deps(51): add papaparse for CSV parsing`

**RED phase -- Write tests first:**

Create `src/lib/__tests__/csvImportValidation.test.ts` with tests for `dateToWibEpoch` and `parseAndValidateCsv`.

Import from `../csvImportValidation`:
- `parseAndValidateCsv(csvText: string, accounts: AccountRef[]): CsvParseResult`
- `dateToWibEpoch(dateStr: string): number`
- `CsvParseResult` type: `{ validRows: ImportRow[], errors: RowError[], warnings: string[] }`
- `AccountRef` type: `{ code: string, name: string, isActive: boolean }`

Use mock accounts: "6100" (Salaries, active), "6200" (Rent, active), "6300" (Marketing, inactive).

Key date test: `dateToWibEpoch("2026-01-15")` should produce a Date where `getUTCHours() === 17` and `getUTCDate() === 14` (WIB midnight = 17:00 UTC previous day).

Run tests -- expect FAIL (module not found).
Commit: `test(51): add failing tests for CSV import validation`

**GREEN phase -- Implement validation helpers:**

Create `src/lib/csvImportValidation.ts` with:
1. `AccountRef` interface: `{ code, name, isActive }`
2. `ImportRow` interface: `{ date: number, amount: number, description: string, vendorName?: string, accountCode: string, receiptUrl?: string }`. Add comment: `// Parallel type exists in convex/journalImport/mutations.ts for backend use`
3. `RowError` interface: `{ row: number, error: string }` (row is 1-based, header=1, first data=2)
4. `CsvParseResult` interface: `{ validRows: ImportRow[], errors: RowError[], warnings: string[] }`
5. `dateToWibEpoch(dateStr: string): number` -- Validate YYYY-MM-DD regex, use `Date.UTC(year, month-1, day) - WIB_OFFSET_MS`. Return NaN for invalid. Do NOT import from dateUtils.ts -- define locally with strict YYYY-MM-DD regex validation (unlike wibDateStrToUtcMs which accepts any parseable date string). Add a comment: `// Intentionally NOT importing wibDateStrToUtcMs from dateUtils.ts -- we need strict YYYY-MM-DD validation for CSV import security (wibDateStrToUtcMs accepts any Date.parse-able string)`.
6. `parseAndValidateCsv(csvText: string, accounts: AccountRef[]): CsvParseResult`:
   - Build account map from accounts array
   - Parse CSV with `Papa.parse<RawCsvRow>(csvText, { header: true, skipEmptyLines: true, transformHeader: h => h.trim() })`
   - For each row: validate required fields, date format, amount (positive integer), accountCode (exists + active)
   - Convert valid rows to ImportRow (date as WIB epoch, amount as number)
   - Track duplicates (same date+amount+description key) as warnings
   - Return `{ validRows, errors, warnings }`

Run tests -- expect PASS.
Commit: `feat(51): add CSV import validation helpers with tests`
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/csvImportValidation.test.ts && npm run type-check</automated>
  </verify>
  <done>
- Papa Parse installed as runtime dependency
- dateToWibEpoch converts YYYY-MM-DD to WIB midnight epoch ms correctly
- parseAndValidateCsv handles all validation rules (required fields, amount, date, account code)
- Duplicate detection produces warnings (not errors)
- Quoted CSV fields with commas parse correctly via Papa Parse
- All 17+ tests pass
- Type-check passes
  </done>
</task>

</tasks>

<verification>
- `npx vitest run convex/journalImport/__tests__/mutations.test.ts` -- all backend tests pass
- `npx vitest run src/lib/__tests__/csvImportValidation.test.ts` -- all client tests pass
- `npm run type-check` -- no type errors
- `npm run test` -- full suite passes (no regressions)
</verification>

<success_criteria>
- Backend mutation creates JEs through journalEngine (never direct insert)
- Client validation catches all error cases before data reaches backend
- Both modules are TDD tested (tests written first)
- Papa Parse installed correctly as runtime dep
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/51-bulk-upload-reimbursed-expenses/51-02-SUMMARY.md`
</output>
