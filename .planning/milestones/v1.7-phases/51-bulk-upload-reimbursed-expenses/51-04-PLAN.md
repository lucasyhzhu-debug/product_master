---
phase: 51-bulk-upload-reimbursed-expenses
plan: 04
type: execute
wave: 4
depends_on: ["51-03"]
files_modified:
  - docs/CHANGELOG.md
  - docs/SCHEMA.md
  - docs/API_REFERENCE.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Full test suite passes including new backend and client validation tests"
    - "Build succeeds with all new files and dependencies"
    - "Documentation reflects new metadata field, mutation endpoint, and import page"
  artifacts:
    - path: "docs/CHANGELOG.md"
      provides: "Phase 51 changelog entry"
      contains: "Historical Expense Journal Import"
    - path: "docs/SCHEMA.md"
      provides: "metadata field documentation for journalEntries"
      contains: "metadata"
    - path: "docs/API_REFERENCE.md"
      provides: "bulkCreateJournalEntries mutation documentation"
      contains: "bulkCreateJournalEntries"
  key_links: []
---

<objective>
Run full verification suite, update documentation, and perform human smoke test of the import wizard.

Purpose: Final gate before merge. Ensures all tests pass, build succeeds, documentation is current, and the import wizard works end-to-end when tested by the user.

Output: Updated CHANGELOG, SCHEMA, and API_REFERENCE docs. Verified working feature.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-bulk-upload-reimbursed-expenses/51-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full verification and documentation updates</name>
  <files>docs/CHANGELOG.md, docs/SCHEMA.md, docs/API_REFERENCE.md</files>
  <action>
**Step 1: Run full test suite**
Run: `npm run test`
Expected: ALL PASS (including new tests from Plan 02: mutations.test.ts and csvImportValidation.test.ts)

**Step 2: Run build**
Run: `npm run build`
Expected: PASS (all imports resolve, lazy loading works)

**Step 3: Update CHANGELOG.md**
Add entry at the top of `docs/CHANGELOG.md`:

```markdown
## 2026-03-14 - Historical Expense Journal Import (Phase 51)

**One-off CSV import tool for backfilling pre-system expenses as journal entries.**

- Added `metadata` optional field to `journalEntries` schema (receiptUrl support)
- Extended `CreateJournalEntryParams` in journal engine to accept metadata
- Added `bulkCreateJournalEntries` mutation with batched processing (50 rows/batch)
- Added HistoricalImportPage at `/import` (admin only)
- CSV template download with Chart of Accounts reference
- Client-side validation with row-level error reporting
- Progress indicator for batched import (350+ rows)
- Added `papaparse` runtime dependency for CSV parsing
```

**Step 4: Update SCHEMA.md**
Add the `metadata` field to the `journalEntries` table documentation:
```
metadata: v.optional(v.object({ receiptUrl: v.optional(v.string()) }))
```
Note: Optional field, backward-compatible. Used by historical import to store Google Drive receipt links.

**Step 5: Update API_REFERENCE.md**
Add the `bulkCreateJournalEntries` mutation:
- Location: `convex/journalImport/mutations.ts`
- Auth: Admin only (protectedMutation)
- Args: `importBatchId: string, rows: Array<{ date, amount, description, vendorName?, accountCode, receiptUrl? }>`
- Returns: `{ created: number }`
- Batch size: max 50 rows per call
- Behavior: Validates all rows first (fail-fast), creates one JE per row (DR expense account, CR 1100 Cash), sourceType "manual" with [Historical Import] prefix
  </action>
  <verify>
    <automated>npm run test && npm run build</automated>
  </verify>
  <done>
- All tests pass (existing + new)
- Build succeeds
- CHANGELOG.md has Phase 51 entry
- SCHEMA.md documents metadata field
- API_REFERENCE.md documents bulkCreateJournalEntries mutation
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke test the historical import wizard</name>
  <files>src/pages/HistoricalImportPage.tsx</files>
  <action>
Human verifies the complete import wizard works end-to-end. All code was built in Plans 01-03 and verified by automated tests in Task 1 above. This checkpoint confirms the visual and interactive flow works correctly.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Human confirmed: wizard works end-to-end, imported JEs appear in P&L</done>
  <what-built>
Complete historical expense import wizard:
- Schema extended with metadata field on journalEntries
- Journal engine accepts optional metadata (backward-compatible)
- bulkCreateJournalEntries mutation with batch validation
- CSV validation helpers with Papa Parse
- HistoricalImportPage wizard at /import (admin only)
- Navigation link from AccountsManager
  </what-built>
  <how-to-verify>
1. Start dev servers: `npx convex dev` (Terminal 1) + `npm run dev` (Terminal 2)
2. Log in as admin
3. Navigate to `/accounts` -- verify "Import Historical Expenses" button visible
4. Click the button -- should navigate to `/import`
5. Click "Download Template" -- verify CSV downloads with headers: date,amount,description,vendorName,accountCode,receiptUrl
6. Click "Download CoA Reference" -- verify accounts listed with code, name, type, category
7. Create a test CSV with 3-5 rows:
   - 2 valid rows (real account codes from CoA reference, positive integer amounts, YYYY-MM-DD dates)
   - 1 row with invalid account code
   - 1 row with negative amount
8. Upload the test CSV -- verify:
   - Error table shows the 2 invalid rows with clear error messages
   - Summary shows 2 valid rows, total amount
   - "Confirm Import" button is disabled (errors present)
9. Fix the CSV (remove bad rows), re-upload -- verify:
   - No errors, summary shows correct totals
   - Summary by GL account and by period visible
   - "Confirm Import" button is enabled
10. Click "Confirm Import" -- verify progress bar appears
11. After completion -- verify success message with count and amount
12. Navigate to `/financials` -- verify imported entries appear in the P&L (OpEx section)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run test` -- full suite passes
- `npm run build` -- build succeeds
- Human smoke test confirms end-to-end flow
</verification>

<success_criteria>
- Full test suite green
- Build clean
- Documentation updated (CHANGELOG, SCHEMA, API_REFERENCE)
- Human verified: wizard works end-to-end, imported JEs appear in P&L
</success_criteria>

<output>
After completion, create `.planning/phases/51-bulk-upload-reimbursed-expenses/51-04-SUMMARY.md`
</output>
